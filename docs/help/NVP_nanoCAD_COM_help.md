# NVP_nanoCAD_COM

## Группа NVP_nanoCAD_COM.OdaX.OdaSpline

### Нод _OdaSpline_Constructor

**Имя нода** = _OdaSpline_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaSpline;

**Входные данные**:

* (System.Object)dynamic;

### Нод _OdaSpline_ConstructorCast

**Имя нода** = _OdaSpline_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaSpline;

**Входные данные**:

* (System.Object)dynamic;

### Нод SetNurbsData

**Имя нода** = SetNurbsData;

**Описание** = Set all nurbs data to the spline;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaSpline;

**Входные данные**:

* (System.Object)OdaSpline;
* (System.Int32)Degree;
* (System.Object)ControlPoints;
* (System.Object)Knots;
* (System.Object)Weights;
* (System.Double)controlPtTolerance;
* (System.Double)knotTolerance;
* (System.Object)periodic;

## Группа NVP_nanoCAD_COM.OdaX.OdaRasterImage

### Нод _OdaRasterImage_Constructor

**Имя нода** = _OdaRasterImage_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaRasterImage;

**Входные данные**:

* (System.Object)dynamic;

### Нод _OdaRasterImage_ConstructorCast

**Имя нода** = _OdaRasterImage_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaRasterImage;

**Входные данные**:

* (System.Object)dynamic;

### Нод Loaded

**Имя нода** = Loaded;

**Описание** = Returns true if this Raster Image is currently loaded;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaRasterImage;

**Входные данные**:

* (System.Object)OdaRasterImage;

## Группа NVP_nanoCAD_COM.OdaX.OdaPolyfaceMesh

### Нод _OdaPolyfaceMesh_Constructor

**Имя нода** = _OdaPolyfaceMesh_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaPolyfaceMesh;

**Входные данные**:

* (System.Object)dynamic;

### Нод _OdaPolyfaceMesh_ConstructorCast

**Имя нода** = _OdaPolyfaceMesh_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaPolyfaceMesh;

**Входные данные**:

* (System.Object)dynamic;

### Нод GetFaces

**Имя нода** = GetFaces;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaPolyfaceMesh;

**Входные данные**:

* (System.Object)OdaPolyfaceMesh;
* (System.Object)__MIDL__IOdaPolyfaceMesh0000;

## Группа NVP_nanoCAD_COM.OdaX.OdaOle

### Нод _OdaOle_Constructor

**Имя нода** = _OdaOle_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaOle;

**Входные данные**:

* (System.Object)dynamic;

### Нод _OdaOle_ConstructorCast

**Имя нода** = _OdaOle_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaOle;

**Входные данные**:

* (System.Object)dynamic;

### Нод LinkName

**Имя нода** = LinkName;

**Описание** = Filename and item to which this OLE object is linked;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaOle;

**Входные данные**:

* (System.Object)OdaOle;

### Нод LinkPath

**Имя нода** = LinkPath;

**Описание** = Filename to which this OLE object is linked;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaOle;

**Входные данные**:

* (System.Object)OdaOle;

## Группа NVP_nanoCAD_COM.OdaX.OdaMLineStyles

### Нод _OdaMLineStyles_Constructor

**Имя нода** = _OdaMLineStyles_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyles;

**Входные данные**:

* (System.Object)dynamic;

### Нод _OdaMLineStyles_ConstructorCast

**Имя нода** = _OdaMLineStyles_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyles;

**Входные данные**:

* (System.Object)dynamic;

### Нод Item

**Имя нода** = Item;

**Описание** = Gets the member object at a given index in a collection, group, or selection set;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyles;

**Входные данные**:

* (System.Object)OdaMLineStyles;
* (System.Object)Index;

### Нод Count

**Имя нода** = Count;

**Описание** = Gets the number of items in the collection, dictionary, group, or selection set;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyles;

**Входные данные**:

* (System.Object)OdaMLineStyles;

### Нод Add

**Имя нода** = Add;

**Описание** = Creates a member object and adds it to the appropriate collection;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyles;

**Входные данные**:

* (System.Object)OdaMLineStyles;
* (System.String)Name;

## Группа NVP_nanoCAD_COM.OdaX.OdaMLineStyle

### Нод _OdaMLineStyle_Constructor

**Имя нода** = _OdaMLineStyle_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyle;

**Входные данные**:

* (System.Object)dynamic;

### Нод _OdaMLineStyle_ConstructorCast

**Имя нода** = _OdaMLineStyle_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyle;

**Входные данные**:

* (System.Object)dynamic;

### Нод Name

**Имя нода** = Name;

**Описание** = Specifies the name of the object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyle;

**Входные данные**:

* (System.Object)OdaMLineStyle;

### Нод Set_Name

**Имя нода** = Set_Name;

**Описание** = Specifies the name of the object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyle;

**Входные данные**:

* (System.Object)OdaMLineStyle;
* (System.String)pName;

### Нод Description

**Имя нода** = Description;

**Описание** = Specifies the style description;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyle;

**Входные данные**:

* (System.Object)OdaMLineStyle;

### Нод Set_Description

**Имя нода** = Set_Description;

**Описание** = Specifies the style description;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyle;

**Входные данные**:

* (System.Object)OdaMLineStyle;
* (System.String)bstrDes;

### Нод StartAngle

**Имя нода** = StartAngle;

**Описание** = property StartAngle;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyle;

**Входные данные**:

* (System.Object)OdaMLineStyle;

### Нод Set_StartAngle

**Имя нода** = Set_StartAngle;

**Описание** = property StartAngle;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyle;

**Входные данные**:

* (System.Object)OdaMLineStyle;
* (System.Object)pVal;

### Нод EndAngle

**Имя нода** = EndAngle;

**Описание** = property EndAngle;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyle;

**Входные данные**:

* (System.Object)OdaMLineStyle;

### Нод Set_EndAngle

**Имя нода** = Set_EndAngle;

**Описание** = property EndAngle;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyle;

**Входные данные**:

* (System.Object)OdaMLineStyle;
* (System.Object)pVal;

### Нод FillColor

**Имя нода** = FillColor;

**Описание** = property FillColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyle;

**Входные данные**:

* (System.Object)OdaMLineStyle;

### Нод Set_FillColor

**Имя нода** = Set_FillColor;

**Описание** = property FillColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyle;

**Входные данные**:

* (System.Object)OdaMLineStyle;
* (System.Object)pVal;

### Нод ElementsCount

**Имя нода** = ElementsCount;

**Описание** = returns the number of elements in style;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyle;

**Входные данные**:

* (System.Object)OdaMLineStyle;

### Нод Filled

**Имя нода** = Filled;

**Описание** = DXF 70, bit 0x01);

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyle;

**Входные данные**:

* (System.Object)OdaMLineStyle;

### Нод Set_Filled

**Имя нода** = Set_Filled;

**Описание** = DXF 70, bit 0x01);

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyle;

**Входные данные**:

* (System.Object)OdaMLineStyle;
* (System.Object)pVal;

### Нод ShowMiters

**Имя нода** = ShowMiters;

**Описание** = DXF 70, bit 0x02);

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyle;

**Входные данные**:

* (System.Object)OdaMLineStyle;

### Нод Set_ShowMiters

**Имя нода** = Set_ShowMiters;

**Описание** = DXF 70, bit 0x02);

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyle;

**Входные данные**:

* (System.Object)OdaMLineStyle;
* (System.Object)pVal;

### Нод StartSquareCap

**Имя нода** = StartSquareCap;

**Описание** = DXF 70, bit 0x10);

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyle;

**Входные данные**:

* (System.Object)OdaMLineStyle;

### Нод Set_StartSquareCap

**Имя нода** = Set_StartSquareCap;

**Описание** = DXF 70, bit 0x10);

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyle;

**Входные данные**:

* (System.Object)OdaMLineStyle;
* (System.Object)pVal;

### Нод StartRoundCap

**Имя нода** = StartRoundCap;

**Описание** = DXF 70, bit 0x40);

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyle;

**Входные данные**:

* (System.Object)OdaMLineStyle;

### Нод Set_StartRoundCap

**Имя нода** = Set_StartRoundCap;

**Описание** = DXF 70, bit 0x40);

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyle;

**Входные данные**:

* (System.Object)OdaMLineStyle;
* (System.Object)pVal;

### Нод StartInnerArcs

**Имя нода** = StartInnerArcs;

**Описание** = DXF 70, bit 0x20);

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyle;

**Входные данные**:

* (System.Object)OdaMLineStyle;

### Нод Set_StartInnerArcs

**Имя нода** = Set_StartInnerArcs;

**Описание** = DXF 70, bit 0x20);

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyle;

**Входные данные**:

* (System.Object)OdaMLineStyle;
* (System.Object)pVal;

### Нод EndSquareCap

**Имя нода** = EndSquareCap;

**Описание** = DXF 70, bit 0x80);

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyle;

**Входные данные**:

* (System.Object)OdaMLineStyle;

### Нод Set_EndSquareCap

**Имя нода** = Set_EndSquareCap;

**Описание** = DXF 70, bit 0x80);

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyle;

**Входные данные**:

* (System.Object)OdaMLineStyle;
* (System.Object)pVal;

### Нод EndRoundCap

**Имя нода** = EndRoundCap;

**Описание** = DXF 70, bit 0x200);

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyle;

**Входные данные**:

* (System.Object)OdaMLineStyle;

### Нод Set_EndRoundCap

**Имя нода** = Set_EndRoundCap;

**Описание** = DXF 70, bit 0x200);

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyle;

**Входные данные**:

* (System.Object)OdaMLineStyle;
* (System.Object)pVal;

### Нод EndInnerArcs

**Имя нода** = EndInnerArcs;

**Описание** = DXF 70, bit 0x100);

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyle;

**Входные данные**:

* (System.Object)OdaMLineStyle;

### Нод Set_EndInnerArcs

**Имя нода** = Set_EndInnerArcs;

**Описание** = DXF 70, bit 0x100);

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyle;

**Входные данные**:

* (System.Object)OdaMLineStyle;
* (System.Object)pVal;

### Нод AddElement

**Имя нода** = AddElement;

**Описание** = Adds an element to this MLine Style;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyle;

**Входные данные**:

* (System.Object)OdaMLineStyle;
* (System.Double)Offset;
* (System.Object)color;
* (System.Object)pLinetype;

### Нод OffsetAt

**Имя нода** = OffsetAt;

**Описание** = Returns the offset values for an existing element in this MLine Style;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyle;

**Входные данные**:

* (System.Object)OdaMLineStyle;
* (System.Int32)Index;

### Нод Set_OffsetAt

**Имя нода** = Set_OffsetAt;

**Описание** = Returns the offset values for an existing element in this MLine Style;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyle;

**Входные данные**:

* (System.Object)OdaMLineStyle;
* (System.Int32)Index;
* (System.Double)Value;

### Нод ColorAt

**Имя нода** = ColorAt;

**Описание** = Returns the Color object for an existing element in this MLine Style;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyle;

**Входные данные**:

* (System.Object)OdaMLineStyle;
* (System.Int32)Index;

### Нод Set_ColorAt

**Имя нода** = Set_ColorAt;

**Описание** = Returns the Color object for an existing element in this MLine Style;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyle;

**Входные данные**:

* (System.Object)OdaMLineStyle;
* (System.Int32)Index;
* (System.Object)color;

### Нод LineTypeAt

**Имя нода** = LineTypeAt;

**Описание** = Returns the Line Type object for an existing element in this MLine Style;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyle;

**Входные данные**:

* (System.Object)OdaMLineStyle;
* (System.Int32)Index;

### Нод Set_LineTypeAt

**Имя нода** = Set_LineTypeAt;

**Описание** = Returns the Line Type object for an existing element in this MLine Style;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaMLineStyle;

**Входные данные**:

* (System.Object)OdaMLineStyle;
* (System.Int32)Index;
* (System.Object)Linetype;

## Группа NVP_nanoCAD_COM.OdaX.OdaLineType

### Нод _OdaLineType_Constructor

**Имя нода** = _OdaLineType_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLineType;

**Входные данные**:

* (System.Object)dynamic;

### Нод _OdaLineType_ConstructorCast

**Имя нода** = _OdaLineType_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLineType;

**Входные данные**:

* (System.Object)dynamic;

### Нод NumDashes

**Имя нода** = NumDashes;

**Описание** = Specifies or returns the number of dashes of a linetype.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLineType;

**Входные данные**:

* (System.Object)OdaLineType;

### Нод Set_NumDashes

**Имя нода** = Set_NumDashes;

**Описание** = Specifies or returns the number of dashes of a linetype.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLineType;

**Входные данные**:

* (System.Object)OdaLineType;
* (System.Object)Count;

### Нод TextAt

**Имя нода** = TextAt;

**Описание** = Specifies or returns the text to be included as part of a linetype.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLineType;

**Входные данные**:

* (System.Object)OdaLineType;
* (System.Object)Index;

### Нод Set_TextAt

**Имя нода** = Set_TextAt;

**Описание** = Specifies or returns the text to be included as part of a linetype.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLineType;

**Входные данные**:

* (System.Object)OdaLineType;
* (System.Object)Index;
* (System.String)bstrText;

### Нод PatternLength

**Имя нода** = PatternLength;

**Описание** = Specifies or returns the pattern length of a linetype.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLineType;

**Входные данные**:

* (System.Object)OdaLineType;

### Нод Set_PatternLength

**Имя нода** = Set_PatternLength;

**Описание** = Specifies or returns the pattern length of a linetype.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLineType;

**Входные данные**:

* (System.Object)OdaLineType;
* (System.Double)pVal;

### Нод DashLengthAt

**Имя нода** = DashLengthAt;

**Описание** = Specifies or returns the dash length of a linetype.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLineType;

**Входные данные**:

* (System.Object)OdaLineType;

### Нод Set_DashLengthAt

**Имя нода** = Set_DashLengthAt;

**Описание** = Specifies or returns the dash length of a linetype.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLineType;

**Входные данные**:

* (System.Object)OdaLineType;
* (System.Double)pVal;

### Нод ShapeScaleAt

**Имя нода** = ShapeScaleAt;

**Описание** = Specifies or returns the scale of a shape that is included as part of a linetype.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLineType;

**Входные данные**:

* (System.Object)OdaLineType;

### Нод Set_ShapeScaleAt

**Имя нода** = Set_ShapeScaleAt;

**Описание** = Specifies or returns the scale of a shape that is included as part of a linetype.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLineType;

**Входные данные**:

* (System.Object)OdaLineType;
* (System.Double)pVal;

### Нод ShapeNumberAt

**Имя нода** = ShapeNumberAt;

**Описание** = Specifies or returns the shape to be included as part of a linetype.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLineType;

**Входные данные**:

* (System.Object)OdaLineType;

### Нод Set_ShapeNumberAt

**Имя нода** = Set_ShapeNumberAt;

**Описание** = Specifies or returns the shape to be included as part of a linetype.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLineType;

**Входные данные**:

* (System.Object)OdaLineType;
* (System.Object)pVal;

### Нод ShapeRotationAt

**Имя нода** = ShapeRotationAt;

**Описание** = Specifies or returns the rotation angle of a shape or text that is included as part of a linetype.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLineType;

**Входные данные**:

* (System.Object)OdaLineType;

### Нод Set_ShapeRotationAt

**Имя нода** = Set_ShapeRotationAt;

**Описание** = Specifies or returns the rotation angle of a shape or text that is included as part of a linetype.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLineType;

**Входные данные**:

* (System.Object)OdaLineType;
* (System.Double)pVal;

### Нод ShapeOffsetAt

**Имя нода** = ShapeOffsetAt;

**Описание** = Specifies or returns the offset of a shape that is included as part of a linetype.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLineType;

**Входные данные**:

* (System.Object)OdaLineType;

### Нод Set_ShapeOffsetAt

**Имя нода** = Set_ShapeOffsetAt;

**Описание** = Specifies or returns the offset of a shape that is included as part of a linetype.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLineType;

**Входные данные**:

* (System.Object)OdaLineType;
* (System.Object)pVal;

### Нод ShapeStyleAt

**Имя нода** = ShapeStyleAt;

**Описание** = Specifies or returns the style of a shape or text that is included as part of a linetype.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLineType;

**Входные данные**:

* (System.Object)OdaLineType;

### Нод Set_ShapeStyleAt

**Имя нода** = Set_ShapeStyleAt;

**Описание** = Specifies or returns the style of a shape or text that is included as part of a linetype.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLineType;

**Входные данные**:

* (System.Object)OdaLineType;
* (System.Object)pStyle;

## Группа NVP_nanoCAD_COM.OdaX.OdaLight

### Нод _OdaLight_Constructor

**Имя нода** = _OdaLight_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)dynamic;

### Нод _OdaLight_ConstructorCast

**Имя нода** = _OdaLight_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)dynamic;

### Нод Name

**Имя нода** = Name;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_Name

**Имя нода** = Set_Name;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.String)pVal;

### Нод LightType

**Имя нода** = LightType;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_LightType

**Имя нода** = Set_LightType;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.String)pVal;

### Нод Status

**Имя нода** = Status;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_Status

**Имя нода** = Set_Status;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.Object)pVal;

### Нод Shadows

**Имя нода** = Shadows;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_Shadows

**Имя нода** = Set_Shadows;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.Object)pVal;

### Нод HotSpot

**Имя нода** = HotSpot;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_HotSpot

**Имя нода** = Set_HotSpot;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.Object)pVal;

### Нод FallOff

**Имя нода** = FallOff;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_FallOff

**Имя нода** = Set_FallOff;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.Object)pVal;

### Нод Intensity

**Имя нода** = Intensity;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_Intensity

**Имя нода** = Set_Intensity;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.Object)pVal;

### Нод LightColor

**Имя нода** = LightColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_LightColor

**Имя нода** = Set_LightColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.Object)ppColor;

### Нод PlotGlyph

**Имя нода** = PlotGlyph;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_PlotGlyph

**Имя нода** = Set_PlotGlyph;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.Object)pVal;

### Нод Position

**Имя нода** = Position;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_Position

**Имя нода** = Set_Position;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.Object)pVal;

### Нод Target

**Имя нода** = Target;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_Target

**Имя нода** = Set_Target;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.Object)pVal;

### Нод AttenuationType

**Имя нода** = AttenuationType;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_AttenuationType

**Имя нода** = Set_AttenuationType;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.String)pVal;

### Нод UseLimits

**Имя нода** = UseLimits;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_UseLimits

**Имя нода** = Set_UseLimits;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.Object)pVal;

### Нод StartLimit

**Имя нода** = StartLimit;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_StartLimit

**Имя нода** = Set_StartLimit;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.Double)pVal;

### Нод EndLimit

**Имя нода** = EndLimit;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_EndLimit

**Имя нода** = Set_EndLimit;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.Double)pVal;

### Нод ShadowType

**Имя нода** = ShadowType;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_ShadowType

**Имя нода** = Set_ShadowType;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.String)pVal;

### Нод MapSize

**Имя нода** = MapSize;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_MapSize

**Имя нода** = Set_MapSize;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.String)pVal;

### Нод Softness

**Имя нода** = Softness;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_Softness

**Имя нода** = Set_Softness;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.Int32)pVal;

### Нод FromVector

**Имя нода** = FromVector;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_FromVector

**Имя нода** = Set_FromVector;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.Object)pVal;

### Нод ToVector

**Имя нода** = ToVector;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_ToVector

**Имя нода** = Set_ToVector;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.Object)pVal;

### Нод SourceVectorX

**Имя нода** = SourceVectorX;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_SourceVectorX

**Имя нода** = Set_SourceVectorX;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.Double)pVal;

### Нод SourceVectorY

**Имя нода** = SourceVectorY;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_SourceVectorY

**Имя нода** = Set_SourceVectorY;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.Double)pVal;

### Нод SourceVectorZ

**Имя нода** = SourceVectorZ;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_SourceVectorZ

**Имя нода** = Set_SourceVectorZ;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.Double)pVal;

### Нод LampIntensity

**Имя нода** = LampIntensity;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_LampIntensity

**Имя нода** = Set_LampIntensity;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.Object)pVal;

### Нод ResultIntensity

**Имя нода** = ResultIntensity;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_ResultIntensity

**Имя нода** = Set_ResultIntensity;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.String)pVal;

### Нод LampColor

**Имя нода** = LampColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_LampColor

**Имя нода** = Set_LampColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.Object)pVal;

### Нод ResultColor

**Имя нода** = ResultColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_ResultColor

**Имя нода** = Set_ResultColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.Object)ppColor;

### Нод WebFile

**Имя нода** = WebFile;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_WebFile

**Имя нода** = Set_WebFile;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.String)pVal;

### Нод WebPreview

**Имя нода** = WebPreview;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_WebPreview

**Имя нода** = Set_WebPreview;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.Object)pVal;

### Нод WebRotateX

**Имя нода** = WebRotateX;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_WebRotateX

**Имя нода** = Set_WebRotateX;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.Object)pVal;

### Нод WebRotateY

**Имя нода** = WebRotateY;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_WebRotateY

**Имя нода** = Set_WebRotateY;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.Object)pVal;

### Нод WebRotateZ

**Имя нода** = WebRotateZ;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_WebRotateZ

**Имя нода** = Set_WebRotateZ;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.Object)pVal;

### Нод HasTarget

**Имя нода** = HasTarget;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_HasTarget

**Имя нода** = Set_HasTarget;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.Object)pVal;

### Нод ShadowSamples

**Имя нода** = ShadowSamples;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_ShadowSamples

**Имя нода** = Set_ShadowSamples;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.Object)pVal;

### Нод ShadowVisible

**Имя нода** = ShadowVisible;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_ShadowVisible

**Имя нода** = Set_ShadowVisible;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.Object)pVal;

### Нод ShadowShape

**Имя нода** = ShadowShape;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_ShadowShape

**Имя нода** = Set_ShadowShape;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.String)pVal;

### Нод ShadowRadius

**Имя нода** = ShadowRadius;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_ShadowRadius

**Имя нода** = Set_ShadowRadius;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.Double)pVal;

### Нод IlluminanceDistance

**Имя нода** = IlluminanceDistance;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_IlluminanceDistance

**Имя нода** = Set_IlluminanceDistance;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.Double)pVal;

### Нод ShadowLength

**Имя нода** = ShadowLength;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_ShadowLength

**Имя нода** = Set_ShadowLength;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.Double)pVal;

### Нод ShadowWidth

**Имя нода** = ShadowWidth;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_ShadowWidth

**Имя нода** = Set_ShadowWidth;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.Double)pVal;

### Нод GlyphDisplay

**Имя нода** = GlyphDisplay;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;

### Нод Set_GlyphDisplay

**Имя нода** = Set_GlyphDisplay;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaLight;

**Входные данные**:

* (System.Object)OdaLight;
* (System.String)pVal;

## Группа NVP_nanoCAD_COM.OdaX.OdaCamera

### Нод _OdaCamera_Constructor

**Имя нода** = _OdaCamera_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaCamera;

**Входные данные**:

* (System.Object)dynamic;

### Нод _OdaCamera_ConstructorCast

**Имя нода** = _OdaCamera_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaCamera;

**Входные данные**:

* (System.Object)dynamic;

### Нод Name

**Имя нода** = Name;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaCamera;

**Входные данные**:

* (System.Object)OdaCamera;

### Нод Set_Name

**Имя нода** = Set_Name;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaCamera;

**Входные данные**:

* (System.Object)OdaCamera;
* (System.String)pVal;

### Нод Location

**Имя нода** = Location;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaCamera;

**Входные данные**:

* (System.Object)OdaCamera;

### Нод Set_Location

**Имя нода** = Set_Location;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaCamera;

**Входные данные**:

* (System.Object)OdaCamera;
* (System.Object)pVal;

### Нод Target

**Имя нода** = Target;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaCamera;

**Входные данные**:

* (System.Object)OdaCamera;

### Нод Set_Target

**Имя нода** = Set_Target;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaCamera;

**Входные данные**:

* (System.Object)OdaCamera;
* (System.Object)pVal;

### Нод LensLength

**Имя нода** = LensLength;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaCamera;

**Входные данные**:

* (System.Object)OdaCamera;

### Нод Set_LensLength

**Имя нода** = Set_LensLength;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaCamera;

**Входные данные**:

* (System.Object)OdaCamera;
* (System.Object)pVal;

### Нод FieldOfView

**Имя нода** = FieldOfView;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaCamera;

**Входные данные**:

* (System.Object)OdaCamera;

### Нод Set_FieldOfView

**Имя нода** = Set_FieldOfView;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaCamera;

**Входные данные**:

* (System.Object)OdaCamera;
* (System.Object)pVal;

### Нод FrameHeight

**Имя нода** = FrameHeight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaCamera;

**Входные данные**:

* (System.Object)OdaCamera;

### Нод Set_FrameHeight

**Имя нода** = Set_FrameHeight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaCamera;

**Входные данные**:

* (System.Object)OdaCamera;
* (System.Double)pVal;

### Нод FrameWidth

**Имя нода** = FrameWidth;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaCamera;

**Входные данные**:

* (System.Object)OdaCamera;

### Нод Set_FrameWidth

**Имя нода** = Set_FrameWidth;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaCamera;

**Входные данные**:

* (System.Object)OdaCamera;
* (System.Double)pVal;

### Нод RollAngle

**Имя нода** = RollAngle;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaCamera;

**Входные данные**:

* (System.Object)OdaCamera;

### Нод Set_RollAngle

**Имя нода** = Set_RollAngle;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaCamera;

**Входные данные**:

* (System.Object)OdaCamera;
* (System.Object)pVal;

### Нод IsPlotable

**Имя нода** = IsPlotable;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaCamera;

**Входные данные**:

* (System.Object)OdaCamera;

### Нод Set_IsPlotable

**Имя нода** = Set_IsPlotable;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaCamera;

**Входные данные**:

* (System.Object)OdaCamera;
* (System.Object)pVal;

### Нод FrontPlane

**Имя нода** = FrontPlane;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaCamera;

**Входные данные**:

* (System.Object)OdaCamera;

### Нод Set_FrontPlane

**Имя нода** = Set_FrontPlane;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaCamera;

**Входные данные**:

* (System.Object)OdaCamera;
* (System.Double)pVal;

### Нод BackPlane

**Имя нода** = BackPlane;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaCamera;

**Входные данные**:

* (System.Object)OdaCamera;

### Нод Set_BackPlane

**Имя нода** = Set_BackPlane;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaCamera;

**Входные данные**:

* (System.Object)OdaCamera;
* (System.Double)pVal;

### Нод Clipping

**Имя нода** = Clipping;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaCamera;

**Входные данные**:

* (System.Object)OdaCamera;

### Нод Set_Clipping

**Имя нода** = Set_Clipping;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaCamera;

**Входные данные**:

* (System.Object)OdaCamera;
* (System.String)pVal;

## Группа NVP_nanoCAD_COM.OdaX.OdaBlock

### Нод _OdaBlock_Constructor

**Имя нода** = _OdaBlock_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaBlock;

**Входные данные**:

* (System.Object)dynamic;

### Нод _OdaBlock_ConstructorCast

**Имя нода** = _OdaBlock_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaBlock;

**Входные данные**:

* (System.Object)dynamic;

### Нод GetReferences

**Имя нода** = GetReferences;

**Описание** = Returns all references for a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaBlock;

**Входные данные**:

* (System.Object)OdaBlock;

### Нод isFromOverlayReference

**Имя нода** = isFromOverlayReference;

**Описание** = Returns whether the block is an overlay.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaBlock;

**Входные данные**:

* (System.Object)OdaBlock;

### Нод isResolved

**Имя нода** = isResolved;

**Описание** = Returns whether the block is an external reference with a resolved path.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaBlock;

**Входные данные**:

* (System.Object)OdaBlock;

### Нод isDependent

**Имя нода** = isDependent;

**Описание** = Returns whether the block is dependent on an external reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaBlock;

**Входные данные**:

* (System.Object)OdaBlock;

### Нод isUnloaded

**Имя нода** = isUnloaded;

**Описание** = Returns whether the block is an external reference that is unloaded.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaBlock;

**Входные данные**:

* (System.Object)OdaBlock;

### Нод setOverlaid

**Имя нода** = setOverlaid;

**Описание** = Specfies whether a block is an overlay.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.OdaBlock;

**Входные данные**:

* (System.Object)OdaBlock;
* (System.Object)bOverlaid;

## Группа NVP_nanoCAD_COM.OdaX.enum_AcZoomScaleType

### Нод enum_acZoomScaledAbsolute

**Имя нода** = enum_acZoomScaledAbsolute;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcZoomScaleType;


### Нод enum_acZoomScaledRelative

**Имя нода** = enum_acZoomScaledRelative;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcZoomScaleType;


### Нод enum_acZoomScaledRelativePSpace

**Имя нода** = enum_acZoomScaledRelativePSpace;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcZoomScaleType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcXRefDemandLoad

### Нод enum_acDemandLoadDisabled

**Имя нода** = enum_acDemandLoadDisabled;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcXRefDemandLoad;


### Нод enum_acDemandLoadEnabled

**Имя нода** = enum_acDemandLoadEnabled;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcXRefDemandLoad;


### Нод enum_acDemandLoadEnabledWithCopy

**Имя нода** = enum_acDemandLoadEnabledWithCopy;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcXRefDemandLoad;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcWindowState

### Нод enum_acNorm

**Имя нода** = enum_acNorm;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcWindowState;


### Нод enum_acMin

**Имя нода** = enum_acMin;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcWindowState;


### Нод enum_acMax

**Имя нода** = enum_acMax;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcWindowState;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcViewportSplitType

### Нод enum_acViewport2Horizontal

**Имя нода** = enum_acViewport2Horizontal;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportSplitType;


### Нод enum_acViewport2Vertical

**Имя нода** = enum_acViewport2Vertical;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportSplitType;


### Нод enum_acViewport3Left

**Имя нода** = enum_acViewport3Left;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportSplitType;


### Нод enum_acViewport3Right

**Имя нода** = enum_acViewport3Right;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportSplitType;


### Нод enum_acViewport3Horizontal

**Имя нода** = enum_acViewport3Horizontal;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportSplitType;


### Нод enum_acViewport3Vertical

**Имя нода** = enum_acViewport3Vertical;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportSplitType;


### Нод enum_acViewport3Above

**Имя нода** = enum_acViewport3Above;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportSplitType;


### Нод enum_acViewport3Below

**Имя нода** = enum_acViewport3Below;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportSplitType;


### Нод enum_acViewport4

**Имя нода** = enum_acViewport4;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportSplitType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcViewportScale

### Нод enum_acVpScaleToFit

**Имя нода** = enum_acVpScaleToFit;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportScale;


### Нод enum_acVpCustomScale

**Имя нода** = enum_acVpCustomScale;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportScale;


### Нод enum_acVp1_1

**Имя нода** = enum_acVp1_1;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportScale;


### Нод enum_acVp1_2

**Имя нода** = enum_acVp1_2;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportScale;


### Нод enum_acVp1_4

**Имя нода** = enum_acVp1_4;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportScale;


### Нод enum_acVp1_5

**Имя нода** = enum_acVp1_5;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportScale;


### Нод enum_acVp1_8

**Имя нода** = enum_acVp1_8;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportScale;


### Нод enum_acVp1_10

**Имя нода** = enum_acVp1_10;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportScale;


### Нод enum_acVp1_16

**Имя нода** = enum_acVp1_16;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportScale;


### Нод enum_acVp1_20

**Имя нода** = enum_acVp1_20;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportScale;


### Нод enum_acVp1_30

**Имя нода** = enum_acVp1_30;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportScale;


### Нод enum_acVp1_40

**Имя нода** = enum_acVp1_40;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportScale;


### Нод enum_acVp1_50

**Имя нода** = enum_acVp1_50;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportScale;


### Нод enum_acVp1_100

**Имя нода** = enum_acVp1_100;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportScale;


### Нод enum_acVp2_1

**Имя нода** = enum_acVp2_1;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportScale;


### Нод enum_acVp4_1

**Имя нода** = enum_acVp4_1;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportScale;


### Нод enum_acVp8_1

**Имя нода** = enum_acVp8_1;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportScale;


### Нод enum_acVp10_1

**Имя нода** = enum_acVp10_1;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportScale;


### Нод enum_acVp100_1

**Имя нода** = enum_acVp100_1;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportScale;


### Нод enum_acVp1_128in_1ft

**Имя нода** = enum_acVp1_128in_1ft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportScale;


### Нод enum_acVp1_64in_1ft

**Имя нода** = enum_acVp1_64in_1ft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportScale;


### Нод enum_acVp1_32in_1ft

**Имя нода** = enum_acVp1_32in_1ft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportScale;


### Нод enum_acVp1_16in_1ft

**Имя нода** = enum_acVp1_16in_1ft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportScale;


### Нод enum_acVp3_32in_1ft

**Имя нода** = enum_acVp3_32in_1ft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportScale;


### Нод enum_acVp1_8in_1ft

**Имя нода** = enum_acVp1_8in_1ft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportScale;


### Нод enum_acVp3_16in_1ft

**Имя нода** = enum_acVp3_16in_1ft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportScale;


### Нод enum_acVp1_4in_1ft

**Имя нода** = enum_acVp1_4in_1ft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportScale;


### Нод enum_acVp3_8in_1ft

**Имя нода** = enum_acVp3_8in_1ft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportScale;


### Нод enum_acVp1_2in_1ft

**Имя нода** = enum_acVp1_2in_1ft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportScale;


### Нод enum_acVp3_4in_1ft

**Имя нода** = enum_acVp3_4in_1ft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportScale;


### Нод enum_acVp1in_1ft

**Имя нода** = enum_acVp1in_1ft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportScale;


### Нод enum_acVp1and1_2in_1ft

**Имя нода** = enum_acVp1and1_2in_1ft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportScale;


### Нод enum_acVp3in_1ft

**Имя нода** = enum_acVp3in_1ft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportScale;


### Нод enum_acVp6in_1ft

**Имя нода** = enum_acVp6in_1ft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportScale;


### Нод enum_acVp1ft_1ft

**Имя нода** = enum_acVp1ft_1ft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcViewportScale;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcVerticalTextAttachmentType

### Нод enum_acAttachmentCenter

**Имя нода** = enum_acAttachmentCenter;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcVerticalTextAttachmentType;


### Нод enum_acAttachmentLinedCenter

**Имя нода** = enum_acAttachmentLinedCenter;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcVerticalTextAttachmentType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcVerticalAlignment

### Нод enum_acVerticalAlignmentBaseline

**Имя нода** = enum_acVerticalAlignmentBaseline;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcVerticalAlignment;


### Нод enum_acVerticalAlignmentBottom

**Имя нода** = enum_acVerticalAlignmentBottom;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcVerticalAlignment;


### Нод enum_acVerticalAlignmentMiddle

**Имя нода** = enum_acVerticalAlignmentMiddle;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcVerticalAlignment;


### Нод enum_acVerticalAlignmentTop

**Имя нода** = enum_acVerticalAlignmentTop;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcVerticalAlignment;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcValueUnitType

### Нод enum_acUnitless

**Имя нода** = enum_acUnitless;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcValueUnitType;


### Нод enum_acUnitDistance

**Имя нода** = enum_acUnitDistance;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcValueUnitType;


### Нод enum_acUnitAngle

**Имя нода** = enum_acUnitAngle;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcValueUnitType;


### Нод enum_acUnitArea

**Имя нода** = enum_acUnitArea;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcValueUnitType;


### Нод enum_acUnitVolume

**Имя нода** = enum_acUnitVolume;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcValueUnitType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcValueDataType

### Нод enum_acUnknownDataType

**Имя нода** = enum_acUnknownDataType;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcValueDataType;


### Нод enum_acLong

**Имя нода** = enum_acLong;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcValueDataType;


### Нод enum_acDouble

**Имя нода** = enum_acDouble;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcValueDataType;


### Нод enum_acString

**Имя нода** = enum_acString;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcValueDataType;


### Нод enum_acDate

**Имя нода** = enum_acDate;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcValueDataType;


### Нод enum_acPoint2d

**Имя нода** = enum_acPoint2d;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcValueDataType;


### Нод enum_acPoint3d

**Имя нода** = enum_acPoint3d;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcValueDataType;


### Нод enum_acObjectId

**Имя нода** = enum_acObjectId;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcValueDataType;


### Нод enum_acBuffer

**Имя нода** = enum_acBuffer;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcValueDataType;


### Нод enum_acResbuf

**Имя нода** = enum_acResbuf;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcValueDataType;


### Нод enum_acGeneral

**Имя нода** = enum_acGeneral;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcValueDataType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcUnits

### Нод enum_acDefaultUnits

**Имя нода** = enum_acDefaultUnits;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcUnits;


### Нод enum_acScientific

**Имя нода** = enum_acScientific;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcUnits;


### Нод enum_acDecimal

**Имя нода** = enum_acDecimal;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcUnits;


### Нод enum_acEngineering

**Имя нода** = enum_acEngineering;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcUnits;


### Нод enum_acArchitectural

**Имя нода** = enum_acArchitectural;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcUnits;


### Нод enum_acFractional

**Имя нода** = enum_acFractional;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcUnits;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcUnderlayLayerOverrideType

### Нод enum_acNoOverrides

**Имя нода** = enum_acNoOverrides;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcUnderlayLayerOverrideType;


### Нод enum_acApplied

**Имя нода** = enum_acApplied;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcUnderlayLayerOverrideType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcToolbarItemType

### Нод enum_acToolbarButton

**Имя нода** = enum_acToolbarButton;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcToolbarItemType;


### Нод enum_acToolbarSeparator

**Имя нода** = enum_acToolbarSeparator;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcToolbarItemType;


### Нод enum_acToolbarControl

**Имя нода** = enum_acToolbarControl;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcToolbarItemType;


### Нод enum_acToolbarFlyout

**Имя нода** = enum_acToolbarFlyout;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcToolbarItemType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcToolbarDockStatus

### Нод enum_acToolbarDockTop

**Имя нода** = enum_acToolbarDockTop;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcToolbarDockStatus;


### Нод enum_acToolbarDockBottom

**Имя нода** = enum_acToolbarDockBottom;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcToolbarDockStatus;


### Нод enum_acToolbarDockLeft

**Имя нода** = enum_acToolbarDockLeft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcToolbarDockStatus;


### Нод enum_acToolbarDockRight

**Имя нода** = enum_acToolbarDockRight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcToolbarDockStatus;


### Нод enum_acToolbarFloating

**Имя нода** = enum_acToolbarFloating;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcToolbarDockStatus;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcTextGenerationFlag

### Нод enum_acTextFlagBackward

**Имя нода** = enum_acTextFlagBackward;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTextGenerationFlag;


### Нод enum_acTextFlagUpsideDown

**Имя нода** = enum_acTextFlagUpsideDown;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTextGenerationFlag;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcTextFontStyle

### Нод enum_acFontRegular

**Имя нода** = enum_acFontRegular;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTextFontStyle;


### Нод enum_acFontItalic

**Имя нода** = enum_acFontItalic;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTextFontStyle;


### Нод enum_acFontBold

**Имя нода** = enum_acFontBold;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTextFontStyle;


### Нод enum_acFontBoldItalic

**Имя нода** = enum_acFontBoldItalic;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTextFontStyle;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcTextAttachmentType

### Нод enum_acAttachmentTopOfTop

**Имя нода** = enum_acAttachmentTopOfTop;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTextAttachmentType;


### Нод enum_acAttachmentMiddleOfTop

**Имя нода** = enum_acAttachmentMiddleOfTop;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTextAttachmentType;


### Нод enum_acAttachmentBottomOfTop

**Имя нода** = enum_acAttachmentBottomOfTop;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTextAttachmentType;


### Нод enum_acAttachmentBottomOfTopLine

**Имя нода** = enum_acAttachmentBottomOfTopLine;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTextAttachmentType;


### Нод enum_acAttachmentMiddle

**Имя нода** = enum_acAttachmentMiddle;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTextAttachmentType;


### Нод enum_acAttachmentMiddleOfBottom

**Имя нода** = enum_acAttachmentMiddleOfBottom;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTextAttachmentType;


### Нод enum_acAttachmentBottomOfBottom

**Имя нода** = enum_acAttachmentBottomOfBottom;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTextAttachmentType;


### Нод enum_acAttachmentBottomLine

**Имя нода** = enum_acAttachmentBottomLine;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTextAttachmentType;


### Нод enum_acAttachmentAllLine

**Имя нода** = enum_acAttachmentAllLine;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTextAttachmentType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcTextAttachmentDirection

### Нод enum_acAttachmentHorizontal

**Имя нода** = enum_acAttachmentHorizontal;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTextAttachmentDirection;


### Нод enum_acAttachmentVertical

**Имя нода** = enum_acAttachmentVertical;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTextAttachmentDirection;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcTextAngleType

### Нод enum_acInsertAngle

**Имя нода** = enum_acInsertAngle;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTextAngleType;


### Нод enum_acHorizontalAngle

**Имя нода** = enum_acHorizontalAngle;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTextAngleType;


### Нод enum_acAlwaysRightReadingAngle

**Имя нода** = enum_acAlwaysRightReadingAngle;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTextAngleType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcTextAlignmentType

### Нод enum_acLeftAlignment

**Имя нода** = enum_acLeftAlignment;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTextAlignmentType;


### Нод enum_acCenterAlignment

**Имя нода** = enum_acCenterAlignment;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTextAlignmentType;


### Нод enum_acRightAlignment

**Имя нода** = enum_acRightAlignment;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTextAlignmentType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides

### Нод enum_acTitleSuppressed

**Имя нода** = enum_acTitleSuppressed;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acHeaderSuppressed

**Имя нода** = enum_acHeaderSuppressed;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acFlowDirection

**Имя нода** = enum_acFlowDirection;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acHorzCellMargin

**Имя нода** = enum_acHorzCellMargin;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acVertCellMargin

**Имя нода** = enum_acVertCellMargin;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acTitleRowColor

**Имя нода** = enum_acTitleRowColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acHeaderRowColor

**Имя нода** = enum_acHeaderRowColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acDataRowColor

**Имя нода** = enum_acDataRowColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acTitleRowFillNone

**Имя нода** = enum_acTitleRowFillNone;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acHeaderRowFillNone

**Имя нода** = enum_acHeaderRowFillNone;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acDataRowFillNone

**Имя нода** = enum_acDataRowFillNone;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acTitleRowFillColor

**Имя нода** = enum_acTitleRowFillColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acHeaderRowFillColor

**Имя нода** = enum_acHeaderRowFillColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acDataRowFillColor

**Имя нода** = enum_acDataRowFillColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acTitleRowAlignment

**Имя нода** = enum_acTitleRowAlignment;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acHeaderRowAlignment

**Имя нода** = enum_acHeaderRowAlignment;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acDataRowAlignment

**Имя нода** = enum_acDataRowAlignment;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acTitleRowTextStyle

**Имя нода** = enum_acTitleRowTextStyle;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acHeaderRowTextStyle

**Имя нода** = enum_acHeaderRowTextStyle;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acDataRowTextStyle

**Имя нода** = enum_acDataRowTextStyle;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acTitleRowTextHeight

**Имя нода** = enum_acTitleRowTextHeight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acHeaderRowTextHeight

**Имя нода** = enum_acHeaderRowTextHeight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acDataRowTextHeight

**Имя нода** = enum_acDataRowTextHeight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acTitleRowDataType

**Имя нода** = enum_acTitleRowDataType;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acHeaderRowDataType

**Имя нода** = enum_acHeaderRowDataType;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acDataRowDataType

**Имя нода** = enum_acDataRowDataType;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acTitleHorzTopColor

**Имя нода** = enum_acTitleHorzTopColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acTitleHorzInsideColor

**Имя нода** = enum_acTitleHorzInsideColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acTitleHorzBottomColor

**Имя нода** = enum_acTitleHorzBottomColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acTitleVertLeftColor

**Имя нода** = enum_acTitleVertLeftColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acTitleVertInsideColor

**Имя нода** = enum_acTitleVertInsideColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acTitleVertRightColor

**Имя нода** = enum_acTitleVertRightColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acHeaderHorzTopColor

**Имя нода** = enum_acHeaderHorzTopColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acHeaderHorzInsideColor

**Имя нода** = enum_acHeaderHorzInsideColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acHeaderHorzBottomColor

**Имя нода** = enum_acHeaderHorzBottomColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acHeaderVertLeftColor

**Имя нода** = enum_acHeaderVertLeftColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acHeaderVertInsideColor

**Имя нода** = enum_acHeaderVertInsideColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acHeaderVertRightColor

**Имя нода** = enum_acHeaderVertRightColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acDataHorzTopColor

**Имя нода** = enum_acDataHorzTopColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acDataHorzInsideColor

**Имя нода** = enum_acDataHorzInsideColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acDataHorzBottomColor

**Имя нода** = enum_acDataHorzBottomColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acDataVertLeftColor

**Имя нода** = enum_acDataVertLeftColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acDataVertInsideColor

**Имя нода** = enum_acDataVertInsideColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acDataVertRightColor

**Имя нода** = enum_acDataVertRightColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acTitleHorzTopLineWeight

**Имя нода** = enum_acTitleHorzTopLineWeight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acTitleHorzInsideLineWeight

**Имя нода** = enum_acTitleHorzInsideLineWeight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acTitleHorzBottomLineWeight

**Имя нода** = enum_acTitleHorzBottomLineWeight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acTitleVertLeftLineWeight

**Имя нода** = enum_acTitleVertLeftLineWeight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acTitleVertInsideLineWeight

**Имя нода** = enum_acTitleVertInsideLineWeight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acTitleVertRightLineWeight

**Имя нода** = enum_acTitleVertRightLineWeight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acHeaderHorzTopLineWeight

**Имя нода** = enum_acHeaderHorzTopLineWeight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acHeaderHorzInsideLineWeight

**Имя нода** = enum_acHeaderHorzInsideLineWeight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acHeaderHorzBottomLineWeight

**Имя нода** = enum_acHeaderHorzBottomLineWeight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acHeaderVertLeftLineWeight

**Имя нода** = enum_acHeaderVertLeftLineWeight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acHeaderVertInsideLineWeight

**Имя нода** = enum_acHeaderVertInsideLineWeight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acHeaderVertRightLineWeight

**Имя нода** = enum_acHeaderVertRightLineWeight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acDataHorzTopLineWeight

**Имя нода** = enum_acDataHorzTopLineWeight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acDataHorzInsideLineWeight

**Имя нода** = enum_acDataHorzInsideLineWeight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acDataHorzBottomLineWeight

**Имя нода** = enum_acDataHorzBottomLineWeight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acDataVertLeftLineWeight

**Имя нода** = enum_acDataVertLeftLineWeight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acDataVertInsideLineWeight

**Имя нода** = enum_acDataVertInsideLineWeight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acDataVertRightLineWeight

**Имя нода** = enum_acDataVertRightLineWeight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acTitleHorzTopVisibility

**Имя нода** = enum_acTitleHorzTopVisibility;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acTitleHorzInsideVisibility

**Имя нода** = enum_acTitleHorzInsideVisibility;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acTitleHorzBottomVisibility

**Имя нода** = enum_acTitleHorzBottomVisibility;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acTitleVertLeftVisibility

**Имя нода** = enum_acTitleVertLeftVisibility;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acTitleVertInsideVisibility

**Имя нода** = enum_acTitleVertInsideVisibility;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acTitleVertRightVisibility

**Имя нода** = enum_acTitleVertRightVisibility;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acHeaderHorzTopVisibility

**Имя нода** = enum_acHeaderHorzTopVisibility;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acHeaderHorzInsideVisibility

**Имя нода** = enum_acHeaderHorzInsideVisibility;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acHeaderHorzBottomVisibility

**Имя нода** = enum_acHeaderHorzBottomVisibility;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acHeaderVertLeftVisibility

**Имя нода** = enum_acHeaderVertLeftVisibility;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acHeaderVertInsideVisibility

**Имя нода** = enum_acHeaderVertInsideVisibility;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acHeaderVertRightVisibility

**Имя нода** = enum_acHeaderVertRightVisibility;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acDataHorzTopVisibility

**Имя нода** = enum_acDataHorzTopVisibility;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acDataHorzInsideVisibility

**Имя нода** = enum_acDataHorzInsideVisibility;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acDataHorzBottomVisibility

**Имя нода** = enum_acDataHorzBottomVisibility;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acDataVertLeftVisibility

**Имя нода** = enum_acDataVertLeftVisibility;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acDataVertInsideVisibility

**Имя нода** = enum_acDataVertInsideVisibility;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acDataVertRightVisibility

**Имя нода** = enum_acDataVertRightVisibility;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acCellAlign

**Имя нода** = enum_acCellAlign;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acCellBackgroundFillNone

**Имя нода** = enum_acCellBackgroundFillNone;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acCellBackgroundColor

**Имя нода** = enum_acCellBackgroundColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acCellContentColor

**Имя нода** = enum_acCellContentColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acCellTextStyle

**Имя нода** = enum_acCellTextStyle;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acCellTextHeight

**Имя нода** = enum_acCellTextHeight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acCellTopGridColor

**Имя нода** = enum_acCellTopGridColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acCellRightGridColor

**Имя нода** = enum_acCellRightGridColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acCellBottomGridColor

**Имя нода** = enum_acCellBottomGridColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acCellLeftGridColor

**Имя нода** = enum_acCellLeftGridColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acCellTopGridLineWeight

**Имя нода** = enum_acCellTopGridLineWeight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acCellRightGridLineWeight

**Имя нода** = enum_acCellRightGridLineWeight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acCellBottomGridLineWeight

**Имя нода** = enum_acCellBottomGridLineWeight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acCellLeftGridLineWeight

**Имя нода** = enum_acCellLeftGridLineWeight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acCellTopVisibility

**Имя нода** = enum_acCellTopVisibility;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acCellRightVisibility

**Имя нода** = enum_acCellRightVisibility;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acCellBottomVisibility

**Имя нода** = enum_acCellBottomVisibility;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acCellLeftVisibility

**Имя нода** = enum_acCellLeftVisibility;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


### Нод enum_acCellDataType

**Имя нода** = enum_acCellDataType;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableStyleOverrides;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcTableFlowDirection

### Нод enum_acTableFlowRight

**Имя нода** = enum_acTableFlowRight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableFlowDirection;


### Нод enum_acTableFlowDownOrUp

**Имя нода** = enum_acTableFlowDownOrUp;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableFlowDirection;


### Нод enum_acTableFlowLeft

**Имя нода** = enum_acTableFlowLeft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableFlowDirection;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcTableDirection

### Нод enum_acTableTopToBottom

**Имя нода** = enum_acTableTopToBottom;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableDirection;


### Нод enum_acTableBottomToTop

**Имя нода** = enum_acTableBottomToTop;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcTableDirection;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcShadowDisplayType

### Нод enum_acCastsAndReceivesShadows

**Имя нода** = enum_acCastsAndReceivesShadows;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcShadowDisplayType;


### Нод enum_acCastsShadows

**Имя нода** = enum_acCastsShadows;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcShadowDisplayType;


### Нод enum_acReceivesShadows

**Имя нода** = enum_acReceivesShadows;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcShadowDisplayType;


### Нод enum_acIgnoreShadows

**Имя нода** = enum_acIgnoreShadows;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcShadowDisplayType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcShadePlot

### Нод enum_acShadePlotAsDisplayed

**Имя нода** = enum_acShadePlotAsDisplayed;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcShadePlot;


### Нод enum_acShadePlotWireframe

**Имя нода** = enum_acShadePlotWireframe;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcShadePlot;


### Нод enum_acShadePlotHidden

**Имя нода** = enum_acShadePlotHidden;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcShadePlot;


### Нод enum_acShadePlotRendered

**Имя нода** = enum_acShadePlotRendered;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcShadePlot;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcSelectType

### Нод enum_acTableSelectWindow

**Имя нода** = enum_acTableSelectWindow;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSelectType;


### Нод enum_acTableSelectCrossing

**Имя нода** = enum_acTableSelectCrossing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSelectType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcSelect

### Нод enum_acSelectionSetWindow

**Имя нода** = enum_acSelectionSetWindow;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSelect;


### Нод enum_acSelectionSetCrossing

**Имя нода** = enum_acSelectionSetCrossing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSelect;


### Нод enum_acSelectionSetFence

**Имя нода** = enum_acSelectionSetFence;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSelect;


### Нод enum_acSelectionSetPrevious

**Имя нода** = enum_acSelectionSetPrevious;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSelect;


### Нод enum_acSelectionSetLast

**Имя нода** = enum_acSelectionSetLast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSelect;


### Нод enum_acSelectionSetAll

**Имя нода** = enum_acSelectionSetAll;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSelect;


### Нод enum_acSelectionSetWindowPolygon

**Имя нода** = enum_acSelectionSetWindowPolygon;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSelect;


### Нод enum_acSelectionSetCrossingPolygon

**Имя нода** = enum_acSelectionSetCrossingPolygon;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSelect;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcSegmentAngleType

### Нод enum_acDegreesAny

**Имя нода** = enum_acDegreesAny;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSegmentAngleType;


### Нод enum_acDegrees15

**Имя нода** = enum_acDegrees15;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSegmentAngleType;


### Нод enum_acDegrees30

**Имя нода** = enum_acDegrees30;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSegmentAngleType;


### Нод enum_acDegrees45

**Имя нода** = enum_acDegrees45;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSegmentAngleType;


### Нод enum_acDegrees60

**Имя нода** = enum_acDegrees60;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSegmentAngleType;


### Нод enum_acDegrees90

**Имя нода** = enum_acDegrees90;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSegmentAngleType;


### Нод enum_acDegreesHorz

**Имя нода** = enum_acDegreesHorz;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSegmentAngleType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcSectionType

### Нод enum_acSectionTypeLiveSection

**Имя нода** = enum_acSectionTypeLiveSection;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSectionType;


### Нод enum_acSectionType2dSection

**Имя нода** = enum_acSectionType2dSection;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSectionType;


### Нод enum_acSectionType3dSection

**Имя нода** = enum_acSectionType3dSection;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSectionType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcSectionSubItem

### Нод enum_acSectionSubItemkNone

**Имя нода** = enum_acSectionSubItemkNone;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSectionSubItem;


### Нод enum_acSectionSubItemSectionLine

**Имя нода** = enum_acSectionSubItemSectionLine;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSectionSubItem;


### Нод enum_acSectionSubItemSectionLineTop

**Имя нода** = enum_acSectionSubItemSectionLineTop;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSectionSubItem;


### Нод enum_acSectionSubItemSectionLineBottom

**Имя нода** = enum_acSectionSubItemSectionLineBottom;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSectionSubItem;


### Нод enum_acSectionSubItemBackLine

**Имя нода** = enum_acSectionSubItemBackLine;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSectionSubItem;


### Нод enum_acSectionSubItemBackLineTop

**Имя нода** = enum_acSectionSubItemBackLineTop;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSectionSubItem;


### Нод enum_acSectionSubItemBackLineBottom

**Имя нода** = enum_acSectionSubItemBackLineBottom;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSectionSubItem;


### Нод enum_acSectionSubItemVerticalLineTop

**Имя нода** = enum_acSectionSubItemVerticalLineTop;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSectionSubItem;


### Нод enum_acSectionSubItemVerticalLineBottom

**Имя нода** = enum_acSectionSubItemVerticalLineBottom;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSectionSubItem;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcSectionState

### Нод enum_acSectionStatePlane

**Имя нода** = enum_acSectionStatePlane;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSectionState;


### Нод enum_acSectionStateBoundary

**Имя нода** = enum_acSectionStateBoundary;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSectionState;


### Нод enum_acSectionStateVolume

**Имя нода** = enum_acSectionStateVolume;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSectionState;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcSectionGeneration

### Нод enum_acSectionGenerationSourceAllObjects

**Имя нода** = enum_acSectionGenerationSourceAllObjects;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSectionGeneration;


### Нод enum_acSectionGenerationSourceSelectedObjects

**Имя нода** = enum_acSectionGenerationSourceSelectedObjects;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSectionGeneration;


### Нод enum_acSectionGenerationDestinationNewBlock

**Имя нода** = enum_acSectionGenerationDestinationNewBlock;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSectionGeneration;


### Нод enum_acSectionGenerationDestinationReplaceBlock

**Имя нода** = enum_acSectionGenerationDestinationReplaceBlock;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSectionGeneration;


### Нод enum_acSectionGenerationDestinationFile

**Имя нода** = enum_acSectionGenerationDestinationFile;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSectionGeneration;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcSaveAsType

### Нод enum_acR09_dxf

**Имя нода** = enum_acR09_dxf;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSaveAsType;


### Нод enum_acR10_dxf

**Имя нода** = enum_acR10_dxf;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSaveAsType;


### Нод enum_acR12_dwg

**Имя нода** = enum_acR12_dwg;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSaveAsType;


### Нод enum_acUnknown

**Имя нода** = enum_acUnknown;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSaveAsType;


### Нод enum_acR12_dxf

**Имя нода** = enum_acR12_dxf;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSaveAsType;


### Нод enum_acR13_dwg

**Имя нода** = enum_acR13_dwg;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSaveAsType;


### Нод enum_acR13_dxf

**Имя нода** = enum_acR13_dxf;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSaveAsType;


### Нод enum_acR14_dwg

**Имя нода** = enum_acR14_dwg;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSaveAsType;


### Нод enum_acR14_dxf

**Имя нода** = enum_acR14_dxf;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSaveAsType;


### Нод enum_acR15_dwg

**Имя нода** = enum_acR15_dwg;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSaveAsType;


### Нод enum_acR15_dxf

**Имя нода** = enum_acR15_dxf;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSaveAsType;


### Нод enum_acR15_Template

**Имя нода** = enum_acR15_Template;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSaveAsType;


### Нод enum_ac2000_dwg

**Имя нода** = enum_ac2000_dwg;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSaveAsType;


### Нод enum_ac2000_dxf

**Имя нода** = enum_ac2000_dxf;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSaveAsType;


### Нод enum_ac2000_Template

**Имя нода** = enum_ac2000_Template;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSaveAsType;


### Нод enum_acR18_dwg

**Имя нода** = enum_acR18_dwg;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSaveAsType;


### Нод enum_acR18_dxf

**Имя нода** = enum_acR18_dxf;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSaveAsType;


### Нод enum_acR18_Template

**Имя нода** = enum_acR18_Template;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSaveAsType;


### Нод enum_ac2004_dwg

**Имя нода** = enum_ac2004_dwg;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSaveAsType;


### Нод enum_ac2004_dxf

**Имя нода** = enum_ac2004_dxf;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSaveAsType;


### Нод enum_ac2004_Template

**Имя нода** = enum_ac2004_Template;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSaveAsType;


### Нод enum_ac2007_dwg

**Имя нода** = enum_ac2007_dwg;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSaveAsType;


### Нод enum_ac2007_dxf

**Имя нода** = enum_ac2007_dxf;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSaveAsType;


### Нод enum_ac2007_Template

**Имя нода** = enum_ac2007_Template;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSaveAsType;


### Нод enum_ac2010_dwg

**Имя нода** = enum_ac2010_dwg;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSaveAsType;


### Нод enum_ac2010_dxf

**Имя нода** = enum_ac2010_dxf;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSaveAsType;


### Нод enum_ac2010_Template

**Имя нода** = enum_ac2010_Template;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSaveAsType;


### Нод enum_ac2013_dwg

**Имя нода** = enum_ac2013_dwg;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSaveAsType;


### Нод enum_ac2013_dxf

**Имя нода** = enum_ac2013_dxf;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSaveAsType;


### Нод enum_ac2013_Template

**Имя нода** = enum_ac2013_Template;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSaveAsType;


### Нод enum_ac2018_dwg

**Имя нода** = enum_ac2018_dwg;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSaveAsType;


### Нод enum_ac2018_dxf

**Имя нода** = enum_ac2018_dxf;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSaveAsType;


### Нод enum_ac2018_Template

**Имя нода** = enum_ac2018_Template;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSaveAsType;


### Нод enum_acNative

**Имя нода** = enum_acNative;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcSaveAsType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcRowType

### Нод enum_acUnknownRow

**Имя нода** = enum_acUnknownRow;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcRowType;


### Нод enum_acDataRow

**Имя нода** = enum_acDataRow;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcRowType;


### Нод enum_acTitleRow

**Имя нода** = enum_acTitleRow;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcRowType;


### Нод enum_acHeaderRow

**Имя нода** = enum_acHeaderRow;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcRowType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcRotationAngle

### Нод enum_acDegreesUnknown

**Имя нода** = enum_acDegreesUnknown;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcRotationAngle;


### Нод enum_acDegrees000

**Имя нода** = enum_acDegrees000;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcRotationAngle;


### Нод enum_acDegrees090

**Имя нода** = enum_acDegrees090;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcRotationAngle;


### Нод enum_acDegrees180

**Имя нода** = enum_acDegrees180;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcRotationAngle;


### Нод enum_acDegrees270

**Имя нода** = enum_acDegrees270;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcRotationAngle;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcRegenType

### Нод enum_acActiveViewport

**Имя нода** = enum_acActiveViewport;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcRegenType;


### Нод enum_acAllViewports

**Имя нода** = enum_acAllViewports;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcRegenType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcProxyImage

### Нод enum_acProxyNotShow

**Имя нода** = enum_acProxyNotShow;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcProxyImage;


### Нод enum_acProxyShow

**Имя нода** = enum_acProxyShow;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcProxyImage;


### Нод enum_acProxyBoundingBox

**Имя нода** = enum_acProxyBoundingBox;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcProxyImage;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcPrinterSpoolAlert

### Нод enum_acPrinterAlwaysAlert

**Имя нода** = enum_acPrinterAlwaysAlert;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPrinterSpoolAlert;


### Нод enum_acPrinterAlertOnce

**Имя нода** = enum_acPrinterAlertOnce;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPrinterSpoolAlert;


### Нод enum_acPrinterNeverAlertLogOnce

**Имя нода** = enum_acPrinterNeverAlertLogOnce;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPrinterSpoolAlert;


### Нод enum_acPrinterNeverAlert

**Имя нода** = enum_acPrinterNeverAlert;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPrinterSpoolAlert;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcPreviewMode

### Нод enum_acPartialPreview

**Имя нода** = enum_acPartialPreview;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPreviewMode;


### Нод enum_acFullPreview

**Имя нода** = enum_acFullPreview;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPreviewMode;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcPredefBlockType

### Нод enum_acBlockImperial

**Имя нода** = enum_acBlockImperial;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPredefBlockType;


### Нод enum_acBlockSlot

**Имя нода** = enum_acBlockSlot;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPredefBlockType;


### Нод enum_acBlockCircle

**Имя нода** = enum_acBlockCircle;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPredefBlockType;


### Нод enum_acBlockBox

**Имя нода** = enum_acBlockBox;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPredefBlockType;


### Нод enum_acBlockHexagon

**Имя нода** = enum_acBlockHexagon;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPredefBlockType;


### Нод enum_acBlockTriangle

**Имя нода** = enum_acBlockTriangle;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPredefBlockType;


### Нод enum_acBlockUserDefined

**Имя нода** = enum_acBlockUserDefined;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPredefBlockType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcPolymeshType

### Нод enum_acSimpleMesh

**Имя нода** = enum_acSimpleMesh;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPolymeshType;


### Нод enum_acQuadSurfaceMesh

**Имя нода** = enum_acQuadSurfaceMesh;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPolymeshType;


### Нод enum_acCubicSurfaceMesh

**Имя нода** = enum_acCubicSurfaceMesh;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPolymeshType;


### Нод enum_acBezierSurfaceMesh

**Имя нода** = enum_acBezierSurfaceMesh;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPolymeshType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcPolylineType

### Нод enum_acSimplePoly

**Имя нода** = enum_acSimplePoly;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPolylineType;


### Нод enum_acFitCurvePoly

**Имя нода** = enum_acFitCurvePoly;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPolylineType;


### Нод enum_acQuadSplinePoly

**Имя нода** = enum_acQuadSplinePoly;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPolylineType;


### Нод enum_acCubicSplinePoly

**Имя нода** = enum_acCubicSplinePoly;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPolylineType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcPlotType

### Нод enum_acDisplay

**Имя нода** = enum_acDisplay;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotType;


### Нод enum_acExtents

**Имя нода** = enum_acExtents;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotType;


### Нод enum_acLimits

**Имя нода** = enum_acLimits;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotType;


### Нод enum_acView

**Имя нода** = enum_acView;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotType;


### Нод enum_acWindow

**Имя нода** = enum_acWindow;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotType;


### Нод enum_acLayout

**Имя нода** = enum_acLayout;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcPlotScale

### Нод enum_acScaleToFit

**Имя нода** = enum_acScaleToFit;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotScale;


### Нод enum_ac1_128in_1ft

**Имя нода** = enum_ac1_128in_1ft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotScale;


### Нод enum_ac1_64in_1ft

**Имя нода** = enum_ac1_64in_1ft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotScale;


### Нод enum_ac1_32in_1ft

**Имя нода** = enum_ac1_32in_1ft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotScale;


### Нод enum_ac1_16in_1ft

**Имя нода** = enum_ac1_16in_1ft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotScale;


### Нод enum_ac3_32in_1ft

**Имя нода** = enum_ac3_32in_1ft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotScale;


### Нод enum_ac1_8in_1ft

**Имя нода** = enum_ac1_8in_1ft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotScale;


### Нод enum_ac3_16in_1ft

**Имя нода** = enum_ac3_16in_1ft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotScale;


### Нод enum_ac1_4in_1ft

**Имя нода** = enum_ac1_4in_1ft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotScale;


### Нод enum_ac3_8in_1ft

**Имя нода** = enum_ac3_8in_1ft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotScale;


### Нод enum_ac1_2in_1ft

**Имя нода** = enum_ac1_2in_1ft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotScale;


### Нод enum_ac3_4in_1ft

**Имя нода** = enum_ac3_4in_1ft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotScale;


### Нод enum_ac1in_1ft

**Имя нода** = enum_ac1in_1ft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotScale;


### Нод enum_ac3in_1ft

**Имя нода** = enum_ac3in_1ft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotScale;


### Нод enum_ac6in_1ft

**Имя нода** = enum_ac6in_1ft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotScale;


### Нод enum_ac1ft_1ft

**Имя нода** = enum_ac1ft_1ft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotScale;


### Нод enum_ac1_1

**Имя нода** = enum_ac1_1;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotScale;


### Нод enum_ac1_2

**Имя нода** = enum_ac1_2;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotScale;


### Нод enum_ac1_4

**Имя нода** = enum_ac1_4;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotScale;


### Нод enum_ac1_5

**Имя нода** = enum_ac1_5;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotScale;


### Нод enum_ac1_8

**Имя нода** = enum_ac1_8;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotScale;


### Нод enum_ac1_10

**Имя нода** = enum_ac1_10;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotScale;


### Нод enum_ac1_16

**Имя нода** = enum_ac1_16;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotScale;


### Нод enum_ac1_20

**Имя нода** = enum_ac1_20;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotScale;


### Нод enum_ac1_30

**Имя нода** = enum_ac1_30;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotScale;


### Нод enum_ac1_40

**Имя нода** = enum_ac1_40;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotScale;


### Нод enum_ac1_50

**Имя нода** = enum_ac1_50;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotScale;


### Нод enum_ac1_100

**Имя нода** = enum_ac1_100;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotScale;


### Нод enum_ac2_1

**Имя нода** = enum_ac2_1;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotScale;


### Нод enum_ac4_1

**Имя нода** = enum_ac4_1;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotScale;


### Нод enum_ac8_1

**Имя нода** = enum_ac8_1;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotScale;


### Нод enum_ac10_1

**Имя нода** = enum_ac10_1;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotScale;


### Нод enum_ac100_1

**Имя нода** = enum_ac100_1;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotScale;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcPlotRotation

### Нод enum_ac0degrees

**Имя нода** = enum_ac0degrees;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotRotation;


### Нод enum_ac90degrees

**Имя нода** = enum_ac90degrees;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotRotation;


### Нод enum_ac180degrees

**Имя нода** = enum_ac180degrees;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotRotation;


### Нод enum_ac270degrees

**Имя нода** = enum_ac270degrees;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotRotation;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcPlotPolicyForNewDwgs

### Нод enum_acPolicyNewDefault

**Имя нода** = enum_acPolicyNewDefault;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotPolicyForNewDwgs;


### Нод enum_acPolicyNewLegacy

**Имя нода** = enum_acPolicyNewLegacy;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotPolicyForNewDwgs;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcPlotPolicyForLegacyDwgs

### Нод enum_acPolicyLegacyDefault

**Имя нода** = enum_acPolicyLegacyDefault;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotPolicyForLegacyDwgs;


### Нод enum_acPolicyLegacyQuery

**Имя нода** = enum_acPolicyLegacyQuery;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotPolicyForLegacyDwgs;


### Нод enum_acPolicyLegacyLegacy

**Имя нода** = enum_acPolicyLegacyLegacy;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotPolicyForLegacyDwgs;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcPlotPolicy

### Нод enum_acPolicyNamed

**Имя нода** = enum_acPolicyNamed;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotPolicy;


### Нод enum_acPolicyLegacy

**Имя нода** = enum_acPolicyLegacy;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotPolicy;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcPlotPaperUnits

### Нод enum_acInches

**Имя нода** = enum_acInches;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotPaperUnits;


### Нод enum_acMillimeters

**Имя нода** = enum_acMillimeters;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotPaperUnits;


### Нод enum_acPixels

**Имя нода** = enum_acPixels;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotPaperUnits;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcPlotOrientation

### Нод enum_acPlotOrientationPortrait

**Имя нода** = enum_acPlotOrientationPortrait;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotOrientation;


### Нод enum_acPlotOrientationLandscape

**Имя нода** = enum_acPlotOrientationLandscape;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPlotOrientation;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcPatternType

### Нод enum_acHatchPatternTypeUserDefined

**Имя нода** = enum_acHatchPatternTypeUserDefined;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPatternType;


### Нод enum_acHatchPatternTypePreDefined

**Имя нода** = enum_acHatchPatternTypePreDefined;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPatternType;


### Нод enum_acHatchPatternTypeCustomDefined

**Имя нода** = enum_acHatchPatternTypeCustomDefined;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcPatternType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcParseOption

### Нод enum_acParseOptionNone

**Имя нода** = enum_acParseOptionNone;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcParseOption;


### Нод enum_acSetDefaultFormat

**Имя нода** = enum_acSetDefaultFormat;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcParseOption;


### Нод enum_acPreserveMtextFormat

**Имя нода** = enum_acPreserveMtextFormat;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcParseOption;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcOnOff

### Нод enum_acOff

**Имя нода** = enum_acOff;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcOnOff;


### Нод enum_acOn

**Имя нода** = enum_acOn;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcOnOff;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcOleType

### Нод enum_acOTLink

**Имя нода** = enum_acOTLink;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcOleType;


### Нод enum_acOTEmbedded

**Имя нода** = enum_acOTEmbedded;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcOleType;


### Нод enum_acOTStatic

**Имя нода** = enum_acOTStatic;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcOleType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcOleQuality

### Нод enum_acOQLineArt

**Имя нода** = enum_acOQLineArt;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcOleQuality;


### Нод enum_acOQText

**Имя нода** = enum_acOQText;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcOleQuality;


### Нод enum_acOQGraphics

**Имя нода** = enum_acOQGraphics;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcOleQuality;


### Нод enum_acOQPhoto

**Имя нода** = enum_acOQPhoto;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcOleQuality;


### Нод enum_acOQHighPhoto

**Имя нода** = enum_acOQHighPhoto;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcOleQuality;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcOlePlotQuality

### Нод enum_acOPQMonochrome

**Имя нода** = enum_acOPQMonochrome;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcOlePlotQuality;


### Нод enum_acOPQLowGraphics

**Имя нода** = enum_acOPQLowGraphics;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcOlePlotQuality;


### Нод enum_acOPQHighGraphics

**Имя нода** = enum_acOPQHighGraphics;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcOlePlotQuality;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcMLineJustification

### Нод enum_acTop

**Имя нода** = enum_acTop;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcMLineJustification;


### Нод enum_acZero

**Имя нода** = enum_acZero;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcMLineJustification;


### Нод enum_acBottom

**Имя нода** = enum_acBottom;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcMLineJustification;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcMLeaderType

### Нод enum_acStraightLeader

**Имя нода** = enum_acStraightLeader;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcMLeaderType;


### Нод enum_acSplineLeader

**Имя нода** = enum_acSplineLeader;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcMLeaderType;


### Нод enum_acInVisibleLeader

**Имя нода** = enum_acInVisibleLeader;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcMLeaderType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcMLeaderContentType

### Нод enum_acNoneContent

**Имя нода** = enum_acNoneContent;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcMLeaderContentType;


### Нод enum_acBlockContent

**Имя нода** = enum_acBlockContent;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcMLeaderContentType;


### Нод enum_acMTextContent

**Имя нода** = enum_acMTextContent;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcMLeaderContentType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcMeshCreaseType

### Нод enum_acNoneCrease

**Имя нода** = enum_acNoneCrease;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcMeshCreaseType;


### Нод enum_acAlwaysCrease

**Имя нода** = enum_acAlwaysCrease;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcMeshCreaseType;


### Нод enum_acCreaseByLevel

**Имя нода** = enum_acCreaseByLevel;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcMeshCreaseType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcMergeCellStyleOption

### Нод enum_acMergeCellStyleNone

**Имя нода** = enum_acMergeCellStyleNone;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcMergeCellStyleOption;


### Нод enum_acMergeCellStyleCopyDuplicates

**Имя нода** = enum_acMergeCellStyleCopyDuplicates;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcMergeCellStyleOption;


### Нод enum_acMergeCellStyleOverwriteDuplicates

**Имя нода** = enum_acMergeCellStyleOverwriteDuplicates;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcMergeCellStyleOption;


### Нод enum_acMergeCellStyleConvertDuplicatesToOverrides

**Имя нода** = enum_acMergeCellStyleConvertDuplicatesToOverrides;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcMergeCellStyleOption;


### Нод enum_acMergeCellStyleIgnoreNewStyles

**Имя нода** = enum_acMergeCellStyleIgnoreNewStyles;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcMergeCellStyleOption;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcMenuItemType

### Нод enum_acMenuItem

**Имя нода** = enum_acMenuItem;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcMenuItemType;


### Нод enum_acMenuSeparator

**Имя нода** = enum_acMenuSeparator;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcMenuItemType;


### Нод enum_acMenuSubMenu

**Имя нода** = enum_acMenuSubMenu;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcMenuItemType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcMenuGroupType

### Нод enum_acBaseMenuGroup

**Имя нода** = enum_acBaseMenuGroup;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcMenuGroupType;


### Нод enum_acPartialMenuGroup

**Имя нода** = enum_acPartialMenuGroup;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcMenuGroupType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcMenuFileType

### Нод enum_acMenuFileCompiled

**Имя нода** = enum_acMenuFileCompiled;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcMenuFileType;


### Нод enum_acMenuFileSource

**Имя нода** = enum_acMenuFileSource;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcMenuFileType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcMeasurementUnits

### Нод enum_acEnglish

**Имя нода** = enum_acEnglish;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcMeasurementUnits;


### Нод enum_acMetric

**Имя нода** = enum_acMetric;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcMeasurementUnits;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcLoopType

### Нод enum_acHatchLoopTypeDefault

**Имя нода** = enum_acHatchLoopTypeDefault;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLoopType;


### Нод enum_acHatchLoopTypeExternal

**Имя нода** = enum_acHatchLoopTypeExternal;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLoopType;


### Нод enum_acHatchLoopTypePolyline

**Имя нода** = enum_acHatchLoopTypePolyline;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLoopType;


### Нод enum_acHatchLoopTypeDerived

**Имя нода** = enum_acHatchLoopTypeDerived;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLoopType;


### Нод enum_acHatchLoopTypeTextbox

**Имя нода** = enum_acHatchLoopTypeTextbox;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLoopType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcLoftedSurfaceNormalType

### Нод enum_acRuled

**Имя нода** = enum_acRuled;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLoftedSurfaceNormalType;


### Нод enum_acSmooth

**Имя нода** = enum_acSmooth;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLoftedSurfaceNormalType;


### Нод enum_acFirstNormal

**Имя нода** = enum_acFirstNormal;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLoftedSurfaceNormalType;


### Нод enum_acLastNormal

**Имя нода** = enum_acLastNormal;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLoftedSurfaceNormalType;


### Нод enum_acEndsNormal

**Имя нода** = enum_acEndsNormal;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLoftedSurfaceNormalType;


### Нод enum_acAllNormal

**Имя нода** = enum_acAllNormal;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLoftedSurfaceNormalType;


### Нод enum_acUseDraftAngles

**Имя нода** = enum_acUseDraftAngles;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLoftedSurfaceNormalType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcLoadPalette

### Нод enum_acPaletteByDrawing

**Имя нода** = enum_acPaletteByDrawing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLoadPalette;


### Нод enum_acPaletteBySession

**Имя нода** = enum_acPaletteBySession;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLoadPalette;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcLineWeight

### Нод enum_acLnWt000

**Имя нода** = enum_acLnWt000;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLineWeight;


### Нод enum_acLnWt005

**Имя нода** = enum_acLnWt005;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLineWeight;


### Нод enum_acLnWt009

**Имя нода** = enum_acLnWt009;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLineWeight;


### Нод enum_acLnWt013

**Имя нода** = enum_acLnWt013;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLineWeight;


### Нод enum_acLnWt015

**Имя нода** = enum_acLnWt015;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLineWeight;


### Нод enum_acLnWt018

**Имя нода** = enum_acLnWt018;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLineWeight;


### Нод enum_acLnWt020

**Имя нода** = enum_acLnWt020;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLineWeight;


### Нод enum_acLnWt025

**Имя нода** = enum_acLnWt025;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLineWeight;


### Нод enum_acLnWt030

**Имя нода** = enum_acLnWt030;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLineWeight;


### Нод enum_acLnWt035

**Имя нода** = enum_acLnWt035;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLineWeight;


### Нод enum_acLnWt040

**Имя нода** = enum_acLnWt040;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLineWeight;


### Нод enum_acLnWt050

**Имя нода** = enum_acLnWt050;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLineWeight;


### Нод enum_acLnWt053

**Имя нода** = enum_acLnWt053;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLineWeight;


### Нод enum_acLnWt060

**Имя нода** = enum_acLnWt060;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLineWeight;


### Нод enum_acLnWt070

**Имя нода** = enum_acLnWt070;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLineWeight;


### Нод enum_acLnWt080

**Имя нода** = enum_acLnWt080;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLineWeight;


### Нод enum_acLnWt090

**Имя нода** = enum_acLnWt090;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLineWeight;


### Нод enum_acLnWt100

**Имя нода** = enum_acLnWt100;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLineWeight;


### Нод enum_acLnWt106

**Имя нода** = enum_acLnWt106;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLineWeight;


### Нод enum_acLnWt120

**Имя нода** = enum_acLnWt120;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLineWeight;


### Нод enum_acLnWt140

**Имя нода** = enum_acLnWt140;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLineWeight;


### Нод enum_acLnWt158

**Имя нода** = enum_acLnWt158;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLineWeight;


### Нод enum_acLnWt200

**Имя нода** = enum_acLnWt200;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLineWeight;


### Нод enum_acLnWt211

**Имя нода** = enum_acLnWt211;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLineWeight;


### Нод enum_acLnWtByLayer

**Имя нода** = enum_acLnWtByLayer;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLineWeight;


### Нод enum_acLnWtByBlock

**Имя нода** = enum_acLnWtByBlock;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLineWeight;


### Нод enum_acLnWtByLwDefault

**Имя нода** = enum_acLnWtByLwDefault;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLineWeight;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcLineSpacingStyle

### Нод enum_acLineSpacingStyleAtLeast

**Имя нода** = enum_acLineSpacingStyleAtLeast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLineSpacingStyle;


### Нод enum_acLineSpacingStyleExactly

**Имя нода** = enum_acLineSpacingStyleExactly;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLineSpacingStyle;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcLeaderType

### Нод enum_acLineNoArrow

**Имя нода** = enum_acLineNoArrow;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLeaderType;


### Нод enum_acSplineNoArrow

**Имя нода** = enum_acSplineNoArrow;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLeaderType;


### Нод enum_acLineWithArrow

**Имя нода** = enum_acLineWithArrow;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLeaderType;


### Нод enum_acSplineWithArrow

**Имя нода** = enum_acSplineWithArrow;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcLeaderType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcKeyboardPriority

### Нод enum_acKeyboardRunningObjSnap

**Имя нода** = enum_acKeyboardRunningObjSnap;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcKeyboardPriority;


### Нод enum_acKeyboardEntry

**Имя нода** = enum_acKeyboardEntry;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcKeyboardPriority;


### Нод enum_acKeyboardEntryExceptScripts

**Имя нода** = enum_acKeyboardEntryExceptScripts;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcKeyboardPriority;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcKeyboardAccelerator

### Нод enum_acPreferenceClassic

**Имя нода** = enum_acPreferenceClassic;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcKeyboardAccelerator;


### Нод enum_acPreferenceCustom

**Имя нода** = enum_acPreferenceCustom;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcKeyboardAccelerator;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcISOPenWidth

### Нод enum_acPenWidth013

**Имя нода** = enum_acPenWidth013;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcISOPenWidth;


### Нод enum_acPenWidth018

**Имя нода** = enum_acPenWidth018;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcISOPenWidth;


### Нод enum_acPenWidth025

**Имя нода** = enum_acPenWidth025;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcISOPenWidth;


### Нод enum_acPenWidth035

**Имя нода** = enum_acPenWidth035;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcISOPenWidth;


### Нод enum_acPenWidth050

**Имя нода** = enum_acPenWidth050;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcISOPenWidth;


### Нод enum_acPenWidth070

**Имя нода** = enum_acPenWidth070;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcISOPenWidth;


### Нод enum_acPenWidth100

**Имя нода** = enum_acPenWidth100;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcISOPenWidth;


### Нод enum_acPenWidth140

**Имя нода** = enum_acPenWidth140;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcISOPenWidth;


### Нод enum_acPenWidth200

**Имя нода** = enum_acPenWidth200;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcISOPenWidth;


### Нод enum_acPenWidthUnk

**Имя нода** = enum_acPenWidthUnk;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcISOPenWidth;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcInsertUnitsAction

### Нод enum_acInsertUnitsPrompt

**Имя нода** = enum_acInsertUnitsPrompt;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcInsertUnitsAction;


### Нод enum_acInsertUnitsAutoAssign

**Имя нода** = enum_acInsertUnitsAutoAssign;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcInsertUnitsAction;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcInsertUnits

### Нод enum_acInsertUnitsUnitless

**Имя нода** = enum_acInsertUnitsUnitless;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcInsertUnits;


### Нод enum_acInsertUnitsInches

**Имя нода** = enum_acInsertUnitsInches;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcInsertUnits;


### Нод enum_acInsertUnitsFeet

**Имя нода** = enum_acInsertUnitsFeet;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcInsertUnits;


### Нод enum_acInsertUnitsMiles

**Имя нода** = enum_acInsertUnitsMiles;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcInsertUnits;


### Нод enum_acInsertUnitsMillimeters

**Имя нода** = enum_acInsertUnitsMillimeters;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcInsertUnits;


### Нод enum_acInsertUnitsCentimeters

**Имя нода** = enum_acInsertUnitsCentimeters;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcInsertUnits;


### Нод enum_acInsertUnitsMeters

**Имя нода** = enum_acInsertUnitsMeters;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcInsertUnits;


### Нод enum_acInsertUnitsKilometers

**Имя нода** = enum_acInsertUnitsKilometers;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcInsertUnits;


### Нод enum_acInsertUnitsMicroinches

**Имя нода** = enum_acInsertUnitsMicroinches;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcInsertUnits;


### Нод enum_acInsertUnitsMils

**Имя нода** = enum_acInsertUnitsMils;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcInsertUnits;


### Нод enum_acInsertUnitsYards

**Имя нода** = enum_acInsertUnitsYards;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcInsertUnits;


### Нод enum_acInsertUnitsAngstroms

**Имя нода** = enum_acInsertUnitsAngstroms;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcInsertUnits;


### Нод enum_acInsertUnitsNanometers

**Имя нода** = enum_acInsertUnitsNanometers;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcInsertUnits;


### Нод enum_acInsertUnitsMicrons

**Имя нода** = enum_acInsertUnitsMicrons;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcInsertUnits;


### Нод enum_acInsertUnitsDecimeters

**Имя нода** = enum_acInsertUnitsDecimeters;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcInsertUnits;


### Нод enum_acInsertUnitsDecameters

**Имя нода** = enum_acInsertUnitsDecameters;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcInsertUnits;


### Нод enum_acInsertUnitsHectometers

**Имя нода** = enum_acInsertUnitsHectometers;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcInsertUnits;


### Нод enum_acInsertUnitsGigameters

**Имя нода** = enum_acInsertUnitsGigameters;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcInsertUnits;


### Нод enum_acInsertUnitsAstronomicalUnits

**Имя нода** = enum_acInsertUnitsAstronomicalUnits;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcInsertUnits;


### Нод enum_acInsertUnitsLightYears

**Имя нода** = enum_acInsertUnitsLightYears;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcInsertUnits;


### Нод enum_acInsertUnitsParsecs

**Имя нода** = enum_acInsertUnitsParsecs;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcInsertUnits;


### Нод enum_acInsertUnitsUSSurveyFeet

**Имя нода** = enum_acInsertUnitsUSSurveyFeet;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcInsertUnits;


### Нод enum_acInsertUnitsUSSurveyInch

**Имя нода** = enum_acInsertUnitsUSSurveyInch;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcInsertUnits;


### Нод enum_acInsertUnitsUSSurveyYard

**Имя нода** = enum_acInsertUnitsUSSurveyYard;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcInsertUnits;


### Нод enum_acInsertUnitsUSSurveyMile

**Имя нода** = enum_acInsertUnitsUSSurveyMile;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcInsertUnits;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcHorizontalAlignment

### Нод enum_acHorizontalAlignmentLeft

**Имя нода** = enum_acHorizontalAlignmentLeft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcHorizontalAlignment;


### Нод enum_acHorizontalAlignmentCenter

**Имя нода** = enum_acHorizontalAlignmentCenter;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcHorizontalAlignment;


### Нод enum_acHorizontalAlignmentRight

**Имя нода** = enum_acHorizontalAlignmentRight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcHorizontalAlignment;


### Нод enum_acHorizontalAlignmentAligned

**Имя нода** = enum_acHorizontalAlignmentAligned;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcHorizontalAlignment;


### Нод enum_acHorizontalAlignmentMiddle

**Имя нода** = enum_acHorizontalAlignmentMiddle;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcHorizontalAlignment;


### Нод enum_acHorizontalAlignmentFit

**Имя нода** = enum_acHorizontalAlignmentFit;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcHorizontalAlignment;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcHelixTwistType

### Нод enum_acCCW

**Имя нода** = enum_acCCW;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcHelixTwistType;


### Нод enum_acCW

**Имя нода** = enum_acCW;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcHelixTwistType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcHelixConstrainType

### Нод enum_acTurnHeight

**Имя нода** = enum_acTurnHeight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcHelixConstrainType;


### Нод enum_acTurns

**Имя нода** = enum_acTurns;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcHelixConstrainType;


### Нод enum_acHeight

**Имя нода** = enum_acHeight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcHelixConstrainType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcHatchStyle

### Нод enum_acHatchStyleNormal

**Имя нода** = enum_acHatchStyleNormal;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcHatchStyle;


### Нод enum_acHatchStyleOuter

**Имя нода** = enum_acHatchStyleOuter;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcHatchStyle;


### Нод enum_acHatchStyleIgnore

**Имя нода** = enum_acHatchStyleIgnore;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcHatchStyle;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcHatchObjectType

### Нод enum_acHatchObject

**Имя нода** = enum_acHatchObject;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcHatchObjectType;


### Нод enum_acGradientObject

**Имя нода** = enum_acGradientObject;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcHatchObjectType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcGridLineType

### Нод enum_acInvalidGridLine

**Имя нода** = enum_acInvalidGridLine;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcGridLineType;


### Нод enum_acHorzTop

**Имя нода** = enum_acHorzTop;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcGridLineType;


### Нод enum_acHorzInside

**Имя нода** = enum_acHorzInside;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcGridLineType;


### Нод enum_acHorzBottom

**Имя нода** = enum_acHorzBottom;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcGridLineType;


### Нод enum_acVertLeft

**Имя нода** = enum_acVertLeft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcGridLineType;


### Нод enum_acVertInside

**Имя нода** = enum_acVertInside;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcGridLineType;


### Нод enum_acVertRight

**Имя нода** = enum_acVertRight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcGridLineType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcGridLineStyle

### Нод enum_acGridLineStyleSingle

**Имя нода** = enum_acGridLineStyleSingle;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcGridLineStyle;


### Нод enum_acGridLineStyleDouble

**Имя нода** = enum_acGridLineStyleDouble;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcGridLineStyle;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcGradientPatternType

### Нод enum_acPreDefinedGradient

**Имя нода** = enum_acPreDefinedGradient;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcGradientPatternType;


### Нод enum_acUserDefinedGradient

**Имя нода** = enum_acUserDefinedGradient;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcGradientPatternType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcFormatOption

### Нод enum_kFormatOptionNone

**Имя нода** = enum_kFormatOptionNone;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcFormatOption;


### Нод enum_acForEditing

**Имя нода** = enum_acForEditing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcFormatOption;


### Нод enum_acForExpression

**Имя нода** = enum_acForExpression;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcFormatOption;


### Нод enum_acUseMaximumPrecision

**Имя нода** = enum_acUseMaximumPrecision;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcFormatOption;


### Нод enum_acIgnoreMtextFormat

**Имя нода** = enum_acIgnoreMtextFormat;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcFormatOption;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcExtendOption

### Нод enum_acExtendNone

**Имя нода** = enum_acExtendNone;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcExtendOption;


### Нод enum_acExtendThisEntity

**Имя нода** = enum_acExtendThisEntity;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcExtendOption;


### Нод enum_acExtendOtherEntity

**Имя нода** = enum_acExtendOtherEntity;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcExtendOption;


### Нод enum_acExtendBoth

**Имя нода** = enum_acExtendBoth;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcExtendOption;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcEntityName

### Нод enum_ac3dFace

**Имя нода** = enum_ac3dFace;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_ac3dPolyline

**Имя нода** = enum_ac3dPolyline;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_ac3dSolid

**Имя нода** = enum_ac3dSolid;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acArc

**Имя нода** = enum_acArc;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acAttribute

**Имя нода** = enum_acAttribute;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acAttributeReference

**Имя нода** = enum_acAttributeReference;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acBlockReference

**Имя нода** = enum_acBlockReference;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acCircle

**Имя нода** = enum_acCircle;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acDimAligned

**Имя нода** = enum_acDimAligned;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acDimAngular

**Имя нода** = enum_acDimAngular;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acDimDiametric

**Имя нода** = enum_acDimDiametric;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acDimOrdinate

**Имя нода** = enum_acDimOrdinate;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acDimRadial

**Имя нода** = enum_acDimRadial;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acDimRotated

**Имя нода** = enum_acDimRotated;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acEllipse

**Имя нода** = enum_acEllipse;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acHatch

**Имя нода** = enum_acHatch;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acLeader

**Имя нода** = enum_acLeader;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acLine

**Имя нода** = enum_acLine;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acMtext

**Имя нода** = enum_acMtext;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acPoint

**Имя нода** = enum_acPoint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acPolyline

**Имя нода** = enum_acPolyline;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acPolylineLight

**Имя нода** = enum_acPolylineLight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acPolymesh

**Имя нода** = enum_acPolymesh;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acRaster

**Имя нода** = enum_acRaster;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acRay

**Имя нода** = enum_acRay;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acRegion

**Имя нода** = enum_acRegion;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acShape

**Имя нода** = enum_acShape;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acSolid

**Имя нода** = enum_acSolid;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acSpline

**Имя нода** = enum_acSpline;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acText

**Имя нода** = enum_acText;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acTolerance

**Имя нода** = enum_acTolerance;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acTrace

**Имя нода** = enum_acTrace;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acPViewport

**Имя нода** = enum_acPViewport;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acXline

**Имя нода** = enum_acXline;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acGroup

**Имя нода** = enum_acGroup;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acMInsertBlock

**Имя нода** = enum_acMInsertBlock;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acPolyfaceMesh

**Имя нода** = enum_acPolyfaceMesh;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acMLine

**Имя нода** = enum_acMLine;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acDim3PointAngular

**Имя нода** = enum_acDim3PointAngular;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acExternalReference

**Имя нода** = enum_acExternalReference;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acTable

**Имя нода** = enum_acTable;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acDimArcLength

**Имя нода** = enum_acDimArcLength;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acDimRadialLarge

**Имя нода** = enum_acDimRadialLarge;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acDwfUnderlay

**Имя нода** = enum_acDwfUnderlay;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acDgnUnderlay

**Имя нода** = enum_acDgnUnderlay;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acMLeader

**Имя нода** = enum_acMLeader;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acSubDMesh

**Имя нода** = enum_acSubDMesh;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


### Нод enum_acPdfUnderlay

**Имя нода** = enum_acPdfUnderlay;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcEntityName;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcDynamicBlockReferencePropertyUnitsType

### Нод enum_acNoUnits

**Имя нода** = enum_acNoUnits;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDynamicBlockReferencePropertyUnitsType;


### Нод enum_acAngular

**Имя нода** = enum_acAngular;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDynamicBlockReferencePropertyUnitsType;


### Нод enum_acDistance

**Имя нода** = enum_acDistance;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDynamicBlockReferencePropertyUnitsType;


### Нод enum_acArea

**Имя нода** = enum_acArea;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDynamicBlockReferencePropertyUnitsType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcDrawMLeaderOrderType

### Нод enum_acDrawContentFirst

**Имя нода** = enum_acDrawContentFirst;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDrawMLeaderOrderType;


### Нод enum_acDrawLeaderFirst

**Имя нода** = enum_acDrawLeaderFirst;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDrawMLeaderOrderType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcDrawLeaderOrderType

### Нод enum_acDrawLeaderHeadFirst

**Имя нода** = enum_acDrawLeaderHeadFirst;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDrawLeaderOrderType;


### Нод enum_acDrawLeaderTailFirst

**Имя нода** = enum_acDrawLeaderTailFirst;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDrawLeaderOrderType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcDrawingDirection

### Нод enum_acLeftToRight

**Имя нода** = enum_acLeftToRight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDrawingDirection;


### Нод enum_acRightToLeft

**Имя нода** = enum_acRightToLeft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDrawingDirection;


### Нод enum_acTopToBottom

**Имя нода** = enum_acTopToBottom;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDrawingDirection;


### Нод enum_acBottomToTop

**Имя нода** = enum_acBottomToTop;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDrawingDirection;


### Нод enum_acByStyle

**Имя нода** = enum_acByStyle;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDrawingDirection;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcDrawingAreaShortCutMenu

### Нод enum_acNoDrawingAreaShortCutMenu

**Имя нода** = enum_acNoDrawingAreaShortCutMenu;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDrawingAreaShortCutMenu;


### Нод enum_acUseDefaultDrawingAreaShortCutMenu

**Имя нода** = enum_acUseDefaultDrawingAreaShortCutMenu;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDrawingAreaShortCutMenu;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcDrawingAreaSCMEdit

### Нод enum_acEdRepeatLastCommand

**Имя нода** = enum_acEdRepeatLastCommand;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDrawingAreaSCMEdit;


### Нод enum_acEdSCM

**Имя нода** = enum_acEdSCM;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDrawingAreaSCMEdit;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcDrawingAreaSCMDefault

### Нод enum_acRepeatLastCommand

**Имя нода** = enum_acRepeatLastCommand;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDrawingAreaSCMDefault;


### Нод enum_acSCM

**Имя нода** = enum_acSCM;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDrawingAreaSCMDefault;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcDrawingAreaSCMCommand

### Нод enum_acEnter

**Имя нода** = enum_acEnter;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDrawingAreaSCMCommand;


### Нод enum_acEnableSCMOptions

**Имя нода** = enum_acEnableSCMOptions;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDrawingAreaSCMCommand;


### Нод enum_acEnableSCM

**Имя нода** = enum_acEnableSCM;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDrawingAreaSCMCommand;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcDragDisplayMode

### Нод enum_acDragDoNotDisplay

**Имя нода** = enum_acDragDoNotDisplay;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDragDisplayMode;


### Нод enum_acDragDisplayOnRequest

**Имя нода** = enum_acDragDisplayOnRequest;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDragDisplayMode;


### Нод enum_acDragDisplayAutomatically

**Имя нода** = enum_acDragDisplayAutomatically;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDragDisplayMode;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcDimVerticalJustification

### Нод enum_acVertCentered

**Имя нода** = enum_acVertCentered;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimVerticalJustification;


### Нод enum_acAbove

**Имя нода** = enum_acAbove;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimVerticalJustification;


### Нод enum_acOutside

**Имя нода** = enum_acOutside;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimVerticalJustification;


### Нод enum_acJIS

**Имя нода** = enum_acJIS;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimVerticalJustification;


### Нод enum_acUnder

**Имя нода** = enum_acUnder;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimVerticalJustification;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcDimUnits

### Нод enum_acDimScientific

**Имя нода** = enum_acDimScientific;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimUnits;


### Нод enum_acDimDecimal

**Имя нода** = enum_acDimDecimal;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimUnits;


### Нод enum_acDimEngineering

**Имя нода** = enum_acDimEngineering;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimUnits;


### Нод enum_acDimArchitecturalStacked

**Имя нода** = enum_acDimArchitecturalStacked;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimUnits;


### Нод enum_acDimFractionalStacked

**Имя нода** = enum_acDimFractionalStacked;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimUnits;


### Нод enum_acDimArchitectural

**Имя нода** = enum_acDimArchitectural;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimUnits;


### Нод enum_acDimFractional

**Имя нода** = enum_acDimFractional;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimUnits;


### Нод enum_acDimWindowsDesktop

**Имя нода** = enum_acDimWindowsDesktop;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimUnits;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcDimToleranceMethod

### Нод enum_acTolNone

**Имя нода** = enum_acTolNone;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimToleranceMethod;


### Нод enum_acTolSymmetrical

**Имя нода** = enum_acTolSymmetrical;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimToleranceMethod;


### Нод enum_acTolDeviation

**Имя нода** = enum_acTolDeviation;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimToleranceMethod;


### Нод enum_acTolLimits

**Имя нода** = enum_acTolLimits;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimToleranceMethod;


### Нод enum_acTolBasic

**Имя нода** = enum_acTolBasic;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimToleranceMethod;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcDimToleranceJustify

### Нод enum_acTolBottom

**Имя нода** = enum_acTolBottom;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimToleranceJustify;


### Нод enum_acTolMiddle

**Имя нода** = enum_acTolMiddle;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimToleranceJustify;


### Нод enum_acTolTop

**Имя нода** = enum_acTolTop;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimToleranceJustify;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcDimTextMovement

### Нод enum_acDimLineWithText

**Имя нода** = enum_acDimLineWithText;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimTextMovement;


### Нод enum_acMoveTextAddLeader

**Имя нода** = enum_acMoveTextAddLeader;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimTextMovement;


### Нод enum_acMoveTextNoLeader

**Имя нода** = enum_acMoveTextNoLeader;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimTextMovement;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcDimPrecision

### Нод enum_acDimPrecisionZero

**Имя нода** = enum_acDimPrecisionZero;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimPrecision;


### Нод enum_acDimPrecisionOne

**Имя нода** = enum_acDimPrecisionOne;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimPrecision;


### Нод enum_acDimPrecisionTwo

**Имя нода** = enum_acDimPrecisionTwo;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimPrecision;


### Нод enum_acDimPrecisionThree

**Имя нода** = enum_acDimPrecisionThree;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimPrecision;


### Нод enum_acDimPrecisionFour

**Имя нода** = enum_acDimPrecisionFour;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimPrecision;


### Нод enum_acDimPrecisionFive

**Имя нода** = enum_acDimPrecisionFive;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimPrecision;


### Нод enum_acDimPrecisionSix

**Имя нода** = enum_acDimPrecisionSix;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimPrecision;


### Нод enum_acDimPrecisionSeven

**Имя нода** = enum_acDimPrecisionSeven;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimPrecision;


### Нод enum_acDimPrecisionEight

**Имя нода** = enum_acDimPrecisionEight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimPrecision;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcDimLUnits

### Нод enum_acDimLScientific

**Имя нода** = enum_acDimLScientific;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimLUnits;


### Нод enum_acDimLDecimal

**Имя нода** = enum_acDimLDecimal;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimLUnits;


### Нод enum_acDimLEngineering

**Имя нода** = enum_acDimLEngineering;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimLUnits;


### Нод enum_acDimLArchitectural

**Имя нода** = enum_acDimLArchitectural;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimLUnits;


### Нод enum_acDimLFractional

**Имя нода** = enum_acDimLFractional;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimLUnits;


### Нод enum_acDimLWindowsDesktop

**Имя нода** = enum_acDimLWindowsDesktop;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimLUnits;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcDimHorizontalJustification

### Нод enum_acHorzCentered

**Имя нода** = enum_acHorzCentered;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimHorizontalJustification;


### Нод enum_acFirstExtensionLine

**Имя нода** = enum_acFirstExtensionLine;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimHorizontalJustification;


### Нод enum_acSecondExtensionLine

**Имя нода** = enum_acSecondExtensionLine;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimHorizontalJustification;


### Нод enum_acOverFirstExtension

**Имя нода** = enum_acOverFirstExtension;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimHorizontalJustification;


### Нод enum_acOverSecondExtension

**Имя нода** = enum_acOverSecondExtension;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimHorizontalJustification;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcDimFractionType

### Нод enum_acHorizontal

**Имя нода** = enum_acHorizontal;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimFractionType;


### Нод enum_acDiagonal

**Имя нода** = enum_acDiagonal;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimFractionType;


### Нод enum_acNotStacked

**Имя нода** = enum_acNotStacked;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimFractionType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcDimFit

### Нод enum_acTextAndArrows

**Имя нода** = enum_acTextAndArrows;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimFit;


### Нод enum_acArrowsOnly

**Имя нода** = enum_acArrowsOnly;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimFit;


### Нод enum_acTextOnly

**Имя нода** = enum_acTextOnly;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimFit;


### Нод enum_acBestFit

**Имя нода** = enum_acBestFit;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimFit;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcDimCenterType

### Нод enum_acCenterMark

**Имя нода** = enum_acCenterMark;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimCenterType;


### Нод enum_acCenterLine

**Имя нода** = enum_acCenterLine;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimCenterType;


### Нод enum_acCenterNone

**Имя нода** = enum_acCenterNone;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimCenterType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcDimArrowheadType

### Нод enum_acArrowDefault

**Имя нода** = enum_acArrowDefault;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimArrowheadType;


### Нод enum_acArrowClosedBlank

**Имя нода** = enum_acArrowClosedBlank;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimArrowheadType;


### Нод enum_acArrowClosed

**Имя нода** = enum_acArrowClosed;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimArrowheadType;


### Нод enum_acArrowDot

**Имя нода** = enum_acArrowDot;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimArrowheadType;


### Нод enum_acArrowArchTick

**Имя нода** = enum_acArrowArchTick;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimArrowheadType;


### Нод enum_acArrowOblique

**Имя нода** = enum_acArrowOblique;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimArrowheadType;


### Нод enum_acArrowOpen

**Имя нода** = enum_acArrowOpen;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimArrowheadType;


### Нод enum_acArrowOrigin

**Имя нода** = enum_acArrowOrigin;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimArrowheadType;


### Нод enum_acArrowOrigin2

**Имя нода** = enum_acArrowOrigin2;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimArrowheadType;


### Нод enum_acArrowOpen90

**Имя нода** = enum_acArrowOpen90;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimArrowheadType;


### Нод enum_acArrowOpen30

**Имя нода** = enum_acArrowOpen30;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimArrowheadType;


### Нод enum_acArrowDotSmall

**Имя нода** = enum_acArrowDotSmall;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimArrowheadType;


### Нод enum_acArrowDotBlank

**Имя нода** = enum_acArrowDotBlank;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimArrowheadType;


### Нод enum_acArrowSmall

**Имя нода** = enum_acArrowSmall;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimArrowheadType;


### Нод enum_acArrowBoxBlank

**Имя нода** = enum_acArrowBoxBlank;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimArrowheadType;


### Нод enum_acArrowBoxFilled

**Имя нода** = enum_acArrowBoxFilled;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimArrowheadType;


### Нод enum_acArrowDatumBlank

**Имя нода** = enum_acArrowDatumBlank;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimArrowheadType;


### Нод enum_acArrowDatumFilled

**Имя нода** = enum_acArrowDatumFilled;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimArrowheadType;


### Нод enum_acArrowIntegral

**Имя нода** = enum_acArrowIntegral;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimArrowheadType;


### Нод enum_acArrowNone

**Имя нода** = enum_acArrowNone;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimArrowheadType;


### Нод enum_acArrowUserDefined

**Имя нода** = enum_acArrowUserDefined;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimArrowheadType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcDimArcLengthSymbol

### Нод enum_acSymInFront

**Имя нода** = enum_acSymInFront;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimArcLengthSymbol;


### Нод enum_acSymAbove

**Имя нода** = enum_acSymAbove;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimArcLengthSymbol;


### Нод enum_acSymNone

**Имя нода** = enum_acSymNone;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDimArcLengthSymbol;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcDataLinkUpdateOption

### Нод enum_acUpdateOptionNone

**Имя нода** = enum_acUpdateOptionNone;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDataLinkUpdateOption;


### Нод enum_acUpdateOptionOverwriteContentModifiedAfterUpdate

**Имя нода** = enum_acUpdateOptionOverwriteContentModifiedAfterUpdate;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDataLinkUpdateOption;


### Нод enum_acUpdateOptionOverwriteFormatModifiedAfterUpdate

**Имя нода** = enum_acUpdateOptionOverwriteFormatModifiedAfterUpdate;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDataLinkUpdateOption;


### Нод enum_acUpdateOptionUpdateFullSourceRange

**Имя нода** = enum_acUpdateOptionUpdateFullSourceRange;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDataLinkUpdateOption;


### Нод enum_acUpdateOptionIncludeXrefs

**Имя нода** = enum_acUpdateOptionIncludeXrefs;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDataLinkUpdateOption;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcDataLinkUpdateDirection

### Нод enum_acUpdateDataFromSource

**Имя нода** = enum_acUpdateDataFromSource;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDataLinkUpdateDirection;


### Нод enum_acUpdateSourceFromData

**Имя нода** = enum_acUpdateSourceFromData;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcDataLinkUpdateDirection;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcCoordinateSystem

### Нод enum_acWorld

**Имя нода** = enum_acWorld;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCoordinateSystem;


### Нод enum_acUCS

**Имя нода** = enum_acUCS;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCoordinateSystem;


### Нод enum_acDisplayDCS

**Имя нода** = enum_acDisplayDCS;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCoordinateSystem;


### Нод enum_acPaperSpaceDCS

**Имя нода** = enum_acPaperSpaceDCS;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCoordinateSystem;


### Нод enum_acOCS

**Имя нода** = enum_acOCS;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCoordinateSystem;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcColorMethod

### Нод enum_acColorMethodByLayer

**Имя нода** = enum_acColorMethodByLayer;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcColorMethod;


### Нод enum_acColorMethodByBlock

**Имя нода** = enum_acColorMethodByBlock;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcColorMethod;


### Нод enum_acColorMethodByRGB

**Имя нода** = enum_acColorMethodByRGB;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcColorMethod;


### Нод enum_acColorMethodByACI

**Имя нода** = enum_acColorMethodByACI;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcColorMethod;


### Нод enum_acColorMethodForeground

**Имя нода** = enum_acColorMethodForeground;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcColorMethod;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcColor

### Нод enum_acByBlock

**Имя нода** = enum_acByBlock;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcColor;


### Нод enum_acRed

**Имя нода** = enum_acRed;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcColor;


### Нод enum_acYellow

**Имя нода** = enum_acYellow;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcColor;


### Нод enum_acGreen

**Имя нода** = enum_acGreen;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcColor;


### Нод enum_acCyan

**Имя нода** = enum_acCyan;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcColor;


### Нод enum_acBlue

**Имя нода** = enum_acBlue;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcColor;


### Нод enum_acMagenta

**Имя нода** = enum_acMagenta;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcColor;


### Нод enum_acWhite

**Имя нода** = enum_acWhite;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcColor;


### Нод enum_acByLayer

**Имя нода** = enum_acByLayer;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcColor;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcCellType

### Нод enum_acUnknownCell

**Имя нода** = enum_acUnknownCell;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellType;


### Нод enum_acTextCell

**Имя нода** = enum_acTextCell;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellType;


### Нод enum_acBlockCell

**Имя нода** = enum_acBlockCell;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcCellState

### Нод enum_acCellStateNone

**Имя нода** = enum_acCellStateNone;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellState;


### Нод enum_acCellStateContentLocked

**Имя нода** = enum_acCellStateContentLocked;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellState;


### Нод enum_acCellStateContentReadOnly

**Имя нода** = enum_acCellStateContentReadOnly;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellState;


### Нод enum_acCellStateFormatLocked

**Имя нода** = enum_acCellStateFormatLocked;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellState;


### Нод enum_acCellStateFormatReadOnly

**Имя нода** = enum_acCellStateFormatReadOnly;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellState;


### Нод enum_acCellStateLinked

**Имя нода** = enum_acCellStateLinked;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellState;


### Нод enum_acCellStateContentModified

**Имя нода** = enum_acCellStateContentModified;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellState;


### Нод enum_acCellStateFormatModified

**Имя нода** = enum_acCellStateFormatModified;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellState;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcCellProperty

### Нод enum_acInvalidCellProperty

**Имя нода** = enum_acInvalidCellProperty;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellProperty;


### Нод enum_acLock

**Имя нода** = enum_acLock;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellProperty;


### Нод enum_acDataType

**Имя нода** = enum_acDataType;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellProperty;


### Нод enum_acDataFormat

**Имя нода** = enum_acDataFormat;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellProperty;


### Нод enum_acRotation

**Имя нода** = enum_acRotation;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellProperty;


### Нод enum_acScale

**Имя нода** = enum_acScale;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellProperty;


### Нод enum_acAlignmentProperty

**Имя нода** = enum_acAlignmentProperty;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellProperty;


### Нод enum_acContentColor

**Имя нода** = enum_acContentColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellProperty;


### Нод enum_acBackgroundColor

**Имя нода** = enum_acBackgroundColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellProperty;


### Нод enum_acTextStyle

**Имя нода** = enum_acTextStyle;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellProperty;


### Нод enum_acTextHeight

**Имя нода** = enum_acTextHeight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellProperty;


### Нод enum_acMarginLeft

**Имя нода** = enum_acMarginLeft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellProperty;


### Нод enum_acMarginTop

**Имя нода** = enum_acMarginTop;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellProperty;


### Нод enum_acMarginRight

**Имя нода** = enum_acMarginRight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellProperty;


### Нод enum_acMarginBottom

**Имя нода** = enum_acMarginBottom;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellProperty;


### Нод enum_acEnableBackgroundColor

**Имя нода** = enum_acEnableBackgroundColor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellProperty;


### Нод enum_acAutoScale

**Имя нода** = enum_acAutoScale;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellProperty;


### Нод enum_acMergeAll

**Имя нода** = enum_acMergeAll;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellProperty;


### Нод enum_acFlowDirBtoT

**Имя нода** = enum_acFlowDirBtoT;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellProperty;


### Нод enum_acContentLayout

**Имя нода** = enum_acContentLayout;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellProperty;


### Нод enum_acDataTypeAndFormat

**Имя нода** = enum_acDataTypeAndFormat;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellProperty;


### Нод enum_acContentProperties

**Имя нода** = enum_acContentProperties;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellProperty;


### Нод enum_acBitProperties

**Имя нода** = enum_acBitProperties;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellProperty;


### Нод enum_acAllCellProperties

**Имя нода** = enum_acAllCellProperties;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellProperty;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcCellOption

### Нод enum_kCellOptionNone

**Имя нода** = enum_kCellOptionNone;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellOption;


### Нод enum_kInheritCellFormat

**Имя нода** = enum_kInheritCellFormat;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellOption;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcCellMargin

### Нод enum_acCellMarginTop

**Имя нода** = enum_acCellMarginTop;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellMargin;


### Нод enum_acCellMarginLeft

**Имя нода** = enum_acCellMarginLeft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellMargin;


### Нод enum_acCellMarginBottom

**Имя нода** = enum_acCellMarginBottom;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellMargin;


### Нод enum_acCellMarginRight

**Имя нода** = enum_acCellMarginRight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellMargin;


### Нод enum_acCellMarginHorzSpacing

**Имя нода** = enum_acCellMarginHorzSpacing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellMargin;


### Нод enum_acCellMarginVertSpacing

**Имя нода** = enum_acCellMarginVertSpacing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellMargin;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcCellEdgeMask

### Нод enum_acTopMask

**Имя нода** = enum_acTopMask;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellEdgeMask;


### Нод enum_acRightMask

**Имя нода** = enum_acRightMask;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellEdgeMask;


### Нод enum_acBottomMask

**Имя нода** = enum_acBottomMask;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellEdgeMask;


### Нод enum_acLeftMask

**Имя нода** = enum_acLeftMask;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellEdgeMask;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcCellContentType

### Нод enum_acCellContentTypeUnknown

**Имя нода** = enum_acCellContentTypeUnknown;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellContentType;


### Нод enum_acCellContentTypeValue

**Имя нода** = enum_acCellContentTypeValue;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellContentType;


### Нод enum_acCellContentTypeField

**Имя нода** = enum_acCellContentTypeField;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellContentType;


### Нод enum_acCellContentTypeBlock

**Имя нода** = enum_acCellContentTypeBlock;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellContentType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcCellContentLayout

### Нод enum_acCellContentLayoutFlow

**Имя нода** = enum_acCellContentLayoutFlow;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellContentLayout;


### Нод enum_acCellContentLayoutStackedHorizontal

**Имя нода** = enum_acCellContentLayoutStackedHorizontal;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellContentLayout;


### Нод enum_acCellContentLayoutStackedVertical

**Имя нода** = enum_acCellContentLayoutStackedVertical;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellContentLayout;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcCellAlignment

### Нод enum_acTopLeft

**Имя нода** = enum_acTopLeft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellAlignment;


### Нод enum_acTopCenter

**Имя нода** = enum_acTopCenter;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellAlignment;


### Нод enum_acTopRight

**Имя нода** = enum_acTopRight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellAlignment;


### Нод enum_acMiddleLeft

**Имя нода** = enum_acMiddleLeft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellAlignment;


### Нод enum_acMiddleCenter

**Имя нода** = enum_acMiddleCenter;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellAlignment;


### Нод enum_acMiddleRight

**Имя нода** = enum_acMiddleRight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellAlignment;


### Нод enum_acBottomLeft

**Имя нода** = enum_acBottomLeft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellAlignment;


### Нод enum_acBottomCenter

**Имя нода** = enum_acBottomCenter;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellAlignment;


### Нод enum_acBottomRight

**Имя нода** = enum_acBottomRight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcCellAlignment;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcBooleanType

### Нод enum_acUnion

**Имя нода** = enum_acUnion;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcBooleanType;


### Нод enum_acIntersection

**Имя нода** = enum_acIntersection;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcBooleanType;


### Нод enum_acSubtraction

**Имя нода** = enum_acSubtraction;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcBooleanType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcBoolean

### Нод enum_acFalse

**Имя нода** = enum_acFalse;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcBoolean;


### Нод enum_acTrue

**Имя нода** = enum_acTrue;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcBoolean;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcBlockScaling

### Нод enum_acAny

**Имя нода** = enum_acAny;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcBlockScaling;


### Нод enum_acUniform

**Имя нода** = enum_acUniform;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcBlockScaling;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcBlockConnectionType

### Нод enum_acConnectExtents

**Имя нода** = enum_acConnectExtents;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcBlockConnectionType;


### Нод enum_acConnectBase

**Имя нода** = enum_acConnectBase;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcBlockConnectionType;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcAttributeMode

### Нод enum_acAttributeModeNormal

**Имя нода** = enum_acAttributeModeNormal;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAttributeMode;


### Нод enum_acAttributeModeInvisible

**Имя нода** = enum_acAttributeModeInvisible;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAttributeMode;


### Нод enum_acAttributeModeConstant

**Имя нода** = enum_acAttributeModeConstant;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAttributeMode;


### Нод enum_acAttributeModeVerify

**Имя нода** = enum_acAttributeModeVerify;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAttributeMode;


### Нод enum_acAttributeModePreset

**Имя нода** = enum_acAttributeModePreset;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAttributeMode;


### Нод enum_acAttributeModeLockPosition

**Имя нода** = enum_acAttributeModeLockPosition;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAttributeMode;


### Нод enum_acAttributeModeMultipleLine

**Имя нода** = enum_acAttributeModeMultipleLine;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAttributeMode;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcAttachmentPoint

### Нод enum_acAttachmentPointTopLeft

**Имя нода** = enum_acAttachmentPointTopLeft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAttachmentPoint;


### Нод enum_acAttachmentPointTopCenter

**Имя нода** = enum_acAttachmentPointTopCenter;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAttachmentPoint;


### Нод enum_acAttachmentPointTopRight

**Имя нода** = enum_acAttachmentPointTopRight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAttachmentPoint;


### Нод enum_acAttachmentPointMiddleLeft

**Имя нода** = enum_acAttachmentPointMiddleLeft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAttachmentPoint;


### Нод enum_acAttachmentPointMiddleCenter

**Имя нода** = enum_acAttachmentPointMiddleCenter;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAttachmentPoint;


### Нод enum_acAttachmentPointMiddleRight

**Имя нода** = enum_acAttachmentPointMiddleRight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAttachmentPoint;


### Нод enum_acAttachmentPointBottomLeft

**Имя нода** = enum_acAttachmentPointBottomLeft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAttachmentPoint;


### Нод enum_acAttachmentPointBottomCenter

**Имя нода** = enum_acAttachmentPointBottomCenter;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAttachmentPoint;


### Нод enum_acAttachmentPointBottomRight

**Имя нода** = enum_acAttachmentPointBottomRight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAttachmentPoint;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcARXDemandLoad

### Нод enum_acDemanLoadDisable

**Имя нода** = enum_acDemanLoadDisable;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcARXDemandLoad;


### Нод enum_acDemandLoadOnObjectDetect

**Имя нода** = enum_acDemandLoadOnObjectDetect;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcARXDemandLoad;


### Нод enum_acDemandLoadCmdInvoke

**Имя нода** = enum_acDemandLoadCmdInvoke;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcARXDemandLoad;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcAngleUnits

### Нод enum_acDegrees

**Имя нода** = enum_acDegrees;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAngleUnits;


### Нод enum_acDegreeMinuteSeconds

**Имя нода** = enum_acDegreeMinuteSeconds;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAngleUnits;


### Нод enum_acGrads

**Имя нода** = enum_acGrads;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAngleUnits;


### Нод enum_acRadians

**Имя нода** = enum_acRadians;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAngleUnits;


### Нод enum_acSurveyors

**Имя нода** = enum_acSurveyors;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAngleUnits;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcAlignmentPointAcquisition

### Нод enum_acAlignPntAcquisitionAutomatic

**Имя нода** = enum_acAlignPntAcquisitionAutomatic;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAlignmentPointAcquisition;


### Нод enum_acAlignPntAcquisitionShiftToAcquire

**Имя нода** = enum_acAlignPntAcquisitionShiftToAcquire;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAlignmentPointAcquisition;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcAlignment

### Нод enum_acAlignmentLeft

**Имя нода** = enum_acAlignmentLeft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAlignment;


### Нод enum_acAlignmentCenter

**Имя нода** = enum_acAlignmentCenter;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAlignment;


### Нод enum_acAlignmentRight

**Имя нода** = enum_acAlignmentRight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAlignment;


### Нод enum_acAlignmentAligned

**Имя нода** = enum_acAlignmentAligned;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAlignment;


### Нод enum_acAlignmentMiddle

**Имя нода** = enum_acAlignmentMiddle;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAlignment;


### Нод enum_acAlignmentFit

**Имя нода** = enum_acAlignmentFit;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAlignment;


### Нод enum_acAlignmentTopLeft

**Имя нода** = enum_acAlignmentTopLeft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAlignment;


### Нод enum_acAlignmentTopCenter

**Имя нода** = enum_acAlignmentTopCenter;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAlignment;


### Нод enum_acAlignmentTopRight

**Имя нода** = enum_acAlignmentTopRight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAlignment;


### Нод enum_acAlignmentMiddleLeft

**Имя нода** = enum_acAlignmentMiddleLeft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAlignment;


### Нод enum_acAlignmentMiddleCenter

**Имя нода** = enum_acAlignmentMiddleCenter;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAlignment;


### Нод enum_acAlignmentMiddleRight

**Имя нода** = enum_acAlignmentMiddleRight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAlignment;


### Нод enum_acAlignmentBottomLeft

**Имя нода** = enum_acAlignmentBottomLeft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAlignment;


### Нод enum_acAlignmentBottomCenter

**Имя нода** = enum_acAlignmentBottomCenter;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAlignment;


### Нод enum_acAlignmentBottomRight

**Имя нода** = enum_acAlignmentBottomRight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcAlignment;


## Группа NVP_nanoCAD_COM.OdaX.enum_AcActiveSpace

### Нод enum_acPaperSpace

**Имя нода** = enum_acPaperSpace;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcActiveSpace;


### Нод enum_acModelSpace

**Имя нода** = enum_acModelSpace;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_AcActiveSpace;


## Группа NVP_nanoCAD_COM.OdaX.enum_Ac3DPolylineType

### Нод enum_acSimple3DPoly

**Имя нода** = enum_acSimple3DPoly;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_Ac3DPolylineType;


### Нод enum_acQuadSpline3DPoly

**Имя нода** = enum_acQuadSpline3DPoly;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_Ac3DPolylineType;


### Нод enum_acCubicSpline3DPoly

**Имя нода** = enum_acCubicSpline3DPoly;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.enum_Ac3DPolylineType;


## Группа NVP_nanoCAD_COM.OdaX.AcadXRecord

### Нод _AcadXRecord_Constructor

**Имя нода** = _AcadXRecord_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadXRecord;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadXRecord_ConstructorCast

**Имя нода** = _AcadXRecord_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadXRecord;

**Входные данные**:

* (System.Object)dynamic;

### Нод Name

**Имя нода** = Name;

**Описание** = Specifies the name of the object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadXRecord;

**Входные данные**:

* (System.Object)AcadXRecord;

### Нод Set_Name

**Имя нода** = Set_Name;

**Описание** = Specifies the name of the object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadXRecord;

**Входные данные**:

* (System.Object)AcadXRecord;
* (System.String)bstrName;

### Нод GetXRecordData

**Имя нода** = GetXRecordData;

**Описание** = XRecordData) associated with a dictionary;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadXRecord;

**Входные данные**:

* (System.Object)AcadXRecord;
* (System.Object)XRecordDataType;
* (System.Object)XRecordDataValue;

### Нод SetXRecordData

**Имя нода** = SetXRecordData;

**Описание** = XRecordData) associated with a dictionary;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadXRecord;

**Входные данные**:

* (System.Object)AcadXRecord;
* (System.Object)XRecordDataType;
* (System.Object)XRecordDataValue;

### Нод TranslateIDs

**Имя нода** = TranslateIDs;

**Описание** = Specifies the translation of any contained object IDs during deepClone or wblockClone operations;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadXRecord;

**Входные данные**:

* (System.Object)AcadXRecord;

### Нод Set_TranslateIDs

**Имя нода** = Set_TranslateIDs;

**Описание** = Specifies the translation of any contained object IDs during deepClone or wblockClone operations;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadXRecord;

**Входные данные**:

* (System.Object)AcadXRecord;
* (System.Object)xlateIds;

## Группа NVP_nanoCAD_COM.OdaX.AcadXline

### Нод _AcadXline_Constructor

**Имя нода** = _AcadXline_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadXline;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadXline_ConstructorCast

**Имя нода** = _AcadXline_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadXline;

**Входные данные**:

* (System.Object)dynamic;

### Нод BasePoint

**Имя нода** = BasePoint;

**Описание** = Specify the X, Y, Z coordinate for the base point of the Xline or use the Pick Point button to set X, Y, Z values simultaneously;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadXline;

**Входные данные**:

* (System.Object)AcadXline;

### Нод Set_BasePoint

**Имя нода** = Set_BasePoint;

**Описание** = Specify the X, Y, Z coordinate for the base point of the Xline or use the Pick Point button to set X, Y, Z values simultaneously;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadXline;

**Входные данные**:

* (System.Object)AcadXline;
* (System.Object)BasePoint;

### Нод SecondPoint

**Имя нода** = SecondPoint;

**Описание** = Specify the X, Y, Z coordinate for the second point of the Xline or use the Pick Point button to set X, Y, Z values simultaneously;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadXline;

**Входные данные**:

* (System.Object)AcadXline;

### Нод Set_SecondPoint

**Имя нода** = Set_SecondPoint;

**Описание** = Specify the X, Y, Z coordinate for the second point of the Xline or use the Pick Point button to set X, Y, Z values simultaneously;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadXline;

**Входные данные**:

* (System.Object)AcadXline;
* (System.Object)SecondPoint;

### Нод DirectionVector

**Имя нода** = DirectionVector;

**Описание** = Specify the X, Y, Z coordinate for the direction vector of the Xline;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadXline;

**Входные данные**:

* (System.Object)AcadXline;

### Нод Set_DirectionVector

**Имя нода** = Set_DirectionVector;

**Описание** = Specify the X, Y, Z coordinate for the direction vector of the Xline;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadXline;

**Входные данные**:

* (System.Object)AcadXline;
* (System.Object)dirVector;

### Нод Offset

**Имя нода** = Offset;

**Описание** = Creates a new entity object by offsetting the Xline by a specified distance;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadXline;

**Входные данные**:

* (System.Object)AcadXline;
* (System.Double)Distance;

## Группа NVP_nanoCAD_COM.OdaX.AcadWipeout

### Нод _AcadWipeout_Constructor

**Имя нода** = _AcadWipeout_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadWipeout;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadWipeout_ConstructorCast

**Имя нода** = _AcadWipeout_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadWipeout;

**Входные данные**:

* (System.Object)dynamic;

## Группа NVP_nanoCAD_COM.OdaX.AcadViews

### Нод _AcadViews_Constructor

**Имя нода** = _AcadViews_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViews;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadViews_ConstructorCast

**Имя нода** = _AcadViews_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViews;

**Входные данные**:

* (System.Object)dynamic;

### Нод Item

**Имя нода** = Item;

**Описание** = Gets the member object at a given index in a collection, group, or selection set;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViews;

**Входные данные**:

* (System.Object)AcadViews;
* (System.Object)Index;

### Нод Count

**Имя нода** = Count;

**Описание** = Gets the number of items in the collection, dictionary, group, or selection set;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViews;

**Входные данные**:

* (System.Object)AcadViews;

### Нод Add

**Имя нода** = Add;

**Описание** = Creates a member object and adds it to the appropriate collection;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViews;

**Входные данные**:

* (System.Object)AcadViews;
* (System.String)Name;

### Нод GetAll_Views

**Имя нода** = GetAll_Views;

**Описание** = Возвращает все Виды чертежа в виде OdaX.AcadView;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViews;

**Входные данные**:

* (System.Object)AcadViews;

## Группа NVP_nanoCAD_COM.OdaX.AcadViewports

### Нод _AcadViewports_Constructor

**Имя нода** = _AcadViewports_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewports;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadViewports_ConstructorCast

**Имя нода** = _AcadViewports_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewports;

**Входные данные**:

* (System.Object)dynamic;

### Нод Item

**Имя нода** = Item;

**Описание** = Gets the member object at a given index in a collection, group, or selection set;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewports;

**Входные данные**:

* (System.Object)AcadViewports;
* (System.Object)Index;

### Нод Count

**Имя нода** = Count;

**Описание** = Gets the number of items in the collection, dictionary, group, or selection set;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewports;

**Входные данные**:

* (System.Object)AcadViewports;

### Нод Add

**Имя нода** = Add;

**Описание** = Creates a member object and adds it to the appropriate collection;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewports;

**Входные данные**:

* (System.Object)AcadViewports;
* (System.String)Name;

### Нод DeleteConfiguration

**Имя нода** = DeleteConfiguration;

**Описание** = Deletes a viewport configuration;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewports;

**Входные данные**:

* (System.Object)AcadViewports;
* (System.String)Name;

### Нод GetAll_Viewports

**Имя нода** = GetAll_Viewports;

**Описание** = Возвращает все ВЭ чертежа в виде OdaX.AcadViewport;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewports;

**Входные данные**:

* (System.Object)AcadUCSs;

## Группа NVP_nanoCAD_COM.OdaX.AcadViewport

### Нод _AcadViewport_Constructor

**Имя нода** = _AcadViewport_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadViewport_ConstructorCast

**Имя нода** = _AcadViewport_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)dynamic;

### Нод Center

**Имя нода** = Center;

**Описание** = Specifies the center of an arc, circle, ellipse, view, or viewport;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)AcadViewport;

### Нод Set_Center

**Имя нода** = Set_Center;

**Описание** = Specifies the center of an arc, circle, ellipse, view, or viewport;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)AcadViewport;
* (System.Object)Center;

### Нод Height

**Имя нода** = Height;

**Описание** = Height of the attribute, shape, text, or view toolbar or the main application window;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)AcadViewport;

### Нод Set_Height

**Имя нода** = Set_Height;

**Описание** = Height of the attribute, shape, text, or view toolbar or the main application window;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)AcadViewport;
* (System.Double)Height;

### Нод Width

**Имя нода** = Width;

**Описание** = Specifies the width of the text boundary, view, image, toolbar, or main application window;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)AcadViewport;

### Нод Set_Width

**Имя нода** = Set_Width;

**Описание** = Specifies the width of the text boundary, view, image, toolbar, or main application window;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)AcadViewport;
* (System.Double)Width;

### Нод Target

**Имя нода** = Target;

**Описание** = Specifies the target point for the view or viewport;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)AcadViewport;

### Нод Set_Target

**Имя нода** = Set_Target;

**Описание** = Specifies the target point for the view or viewport;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)AcadViewport;
* (System.Object)targetPoint;

### Нод Direction

**Имя нода** = Direction;

**Описание** = Specifies the viewing direction for a 3D visualization of the drawing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)AcadViewport;

### Нод Set_Direction

**Имя нода** = Set_Direction;

**Описание** = Specifies the viewing direction for a 3D visualization of the drawing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)AcadViewport;
* (System.Object)dirVec;

### Нод Name

**Имя нода** = Name;

**Описание** = Specifies the name of the object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)AcadViewport;

### Нод Set_Name

**Имя нода** = Set_Name;

**Описание** = Specifies the name of the object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)AcadViewport;
* (System.String)bstrName;

### Нод GridOn

**Имя нода** = GridOn;

**Описание** = Specifies the status of the viewport grid;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)AcadViewport;

### Нод Set_GridOn

**Имя нода** = Set_GridOn;

**Описание** = Specifies the status of the viewport grid;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)AcadViewport;
* (System.Object)bGridOn;

### Нод OrthoOn

**Имя нода** = OrthoOn;

**Описание** = Specifies the status of the Ortho mode for the viewport;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)AcadViewport;

### Нод Set_OrthoOn

**Имя нода** = Set_OrthoOn;

**Описание** = Specifies the status of the Ortho mode for the viewport;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)AcadViewport;
* (System.Object)bOrthoOn;

### Нод SnapBasePoint

**Имя нода** = SnapBasePoint;

**Описание** = Specifies the snap base point for the viewport;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)AcadViewport;

### Нод Set_SnapBasePoint

**Имя нода** = Set_SnapBasePoint;

**Описание** = Specifies the snap base point for the viewport;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)AcadViewport;
* (System.Object)lowLeft;

### Нод SnapOn

**Имя нода** = SnapOn;

**Описание** = Specifies the status of snap;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)AcadViewport;

### Нод Set_SnapOn

**Имя нода** = Set_SnapOn;

**Описание** = Specifies the status of snap;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)AcadViewport;
* (System.Object)bSnapOn;

### Нод SnapRotationAngle

**Имя нода** = SnapRotationAngle;

**Описание** = Specifies the snap rotation angle of the viewport relative to the current UCS;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)AcadViewport;

### Нод Set_SnapRotationAngle

**Имя нода** = Set_SnapRotationAngle;

**Описание** = Specifies the snap rotation angle of the viewport relative to the current UCS;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)AcadViewport;
* (System.Object)Angle;

### Нод UCSIconOn

**Имя нода** = UCSIconOn;

**Описание** = Specifies if the UCS icon is on;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)AcadViewport;

### Нод Set_UCSIconOn

**Имя нода** = Set_UCSIconOn;

**Описание** = Specifies if the UCS icon is on;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)AcadViewport;
* (System.Object)bIconOn;

### Нод UCSIconAtOrigin

**Имя нода** = UCSIconAtOrigin;

**Описание** = Specifies if the UCS icon is displayed at the origin;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)AcadViewport;

### Нод Set_UCSIconAtOrigin

**Имя нода** = Set_UCSIconAtOrigin;

**Описание** = Specifies if the UCS icon is displayed at the origin;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)AcadViewport;
* (System.Object)bIconAtOrigin;

### Нод LowerLeftCorner

**Имя нода** = LowerLeftCorner;

**Описание** = Gets the lower-left corner of the current active viewport;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)AcadViewport;

### Нод UpperRightCorner

**Имя нода** = UpperRightCorner;

**Описание** = Gets the upper-right corner of the current active viewport;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)AcadViewport;

### Нод Split

**Имя нода** = Split;

**Описание** = Splits a viewport into the given number of views;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)AcadViewport;
* (System.Object)OdaX.AcViewportSplitType;

### Нод GetGridSpacing

**Имя нода** = GetGridSpacing;

**Описание** = Gets the grid spacing for the viewport;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)AcadViewport;
* (System.Double)XSpacing;
* (System.Double)YSpacing;

### Нод SetGridSpacing

**Имя нода** = SetGridSpacing;

**Описание** = Sets the grid spacing for the viewport;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)AcadViewport;
* (System.Double)XSpacing;
* (System.Double)YSpacing;

### Нод GetSnapSpacing

**Имя нода** = GetSnapSpacing;

**Описание** = Gets the snap spacing for the viewport;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)AcadViewport;
* (System.Double)XSpacing;
* (System.Double)YSpacing;

### Нод SetSnapSpacing

**Имя нода** = SetSnapSpacing;

**Описание** = Sets the snap spacing for the viewport;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)AcadViewport;
* (System.Double)XSpacing;
* (System.Double)YSpacing;

### Нод SetView

**Имя нода** = SetView;

**Описание** = Sets the view in a viewport to a saved view in the Views Collection object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)AcadViewport;
* (System.Object)View;

### Нод ArcSmoothness

**Имя нода** = ArcSmoothness;

**Описание** = Specifies the smoothness of circles, arcs, and ellipses;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)AcadViewport;

### Нод Set_ArcSmoothness

**Имя нода** = Set_ArcSmoothness;

**Описание** = Specifies the smoothness of circles, arcs, and ellipses;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadViewport;

**Входные данные**:

* (System.Object)AcadViewport;
* (System.Object)arcSmooth;

## Группа NVP_nanoCAD_COM.OdaX.AcadView

### Нод _AcadView_Constructor

**Имя нода** = _AcadView_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadView;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadView_ConstructorCast

**Имя нода** = _AcadView_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadView;

**Входные данные**:

* (System.Object)dynamic;

### Нод Center

**Имя нода** = Center;

**Описание** = Specifies the center of an arc, circle, ellipse, view, or viewport;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadView;

**Входные данные**:

* (System.Object)AcadView;

### Нод Set_Center

**Имя нода** = Set_Center;

**Описание** = Specifies the center of an arc, circle, ellipse, view, or viewport;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadView;

**Входные данные**:

* (System.Object)AcadView;
* (System.Object)Center;

### Нод Height

**Имя нода** = Height;

**Описание** = Height of the object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadView;

**Входные данные**:

* (System.Object)AcadView;

### Нод Set_Height

**Имя нода** = Set_Height;

**Описание** = Height of the object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadView;

**Входные данные**:

* (System.Object)AcadView;
* (System.Double)Height;

### Нод Width

**Имя нода** = Width;

**Описание** = Specifies the width of the object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadView;

**Входные данные**:

* (System.Object)AcadView;

### Нод Set_Width

**Имя нода** = Set_Width;

**Описание** = Specifies the width of the object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadView;

**Входные данные**:

* (System.Object)AcadView;
* (System.Double)Width;

### Нод Target

**Имя нода** = Target;

**Описание** = Specifies the target point for the view or viewport;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadView;

**Входные данные**:

* (System.Object)AcadView;

### Нод Set_Target

**Имя нода** = Set_Target;

**Описание** = Specifies the target point for the view or viewport;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadView;

**Входные данные**:

* (System.Object)AcadView;
* (System.Object)targetPoint;

### Нод Direction

**Имя нода** = Direction;

**Описание** = Specifies the viewing direction for a 3D visualization of the drawing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadView;

**Входные данные**:

* (System.Object)AcadView;

### Нод Set_Direction

**Имя нода** = Set_Direction;

**Описание** = Specifies the viewing direction for a 3D visualization of the drawing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadView;

**Входные данные**:

* (System.Object)AcadView;
* (System.Object)dirVec;

### Нод Name

**Имя нода** = Name;

**Описание** = Specifies the name of the object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadView;

**Входные данные**:

* (System.Object)AcadView;

### Нод Set_Name

**Имя нода** = Set_Name;

**Описание** = Specifies the name of the object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadView;

**Входные данные**:

* (System.Object)AcadView;
* (System.String)bstrName;

### Нод CategoryName

**Имя нода** = CategoryName;

**Описание** = Returns and sets the name of the category of the view.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadView;

**Входные данные**:

* (System.Object)AcadView;

### Нод Set_CategoryName

**Имя нода** = Set_CategoryName;

**Описание** = Returns and sets the name of the category of the view.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadView;

**Входные данные**:

* (System.Object)AcadView;
* (System.String)category;

### Нод LayoutId

**Имя нода** = LayoutId;

**Описание** = Returns and sets the layout of the view.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadView;

**Входные данные**:

* (System.Object)AcadView;

### Нод Set_LayoutId

**Имя нода** = Set_LayoutId;

**Описание** = Returns and sets the layout of the view.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadView;

**Входные данные**:

* (System.Object)AcadView;
* (System.Object)ObjectID;

### Нод LayerState

**Имя нода** = LayerState;

**Описание** = Returns and sets the name of the layer state of the view.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadView;

**Входные данные**:

* (System.Object)AcadView;

### Нод Set_LayerState

**Имя нода** = Set_LayerState;

**Описание** = Returns and sets the name of the layer state of the view.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadView;

**Входные данные**:

* (System.Object)AcadView;
* (System.String)LayerState;

### Нод HasVpAssociation

**Имя нода** = HasVpAssociation;

**Описание** = Specifies whether the view is associated with a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadView;

**Входные данные**:

* (System.Object)AcadView;

### Нод Set_HasVpAssociation

**Имя нода** = Set_HasVpAssociation;

**Описание** = Specifies whether the view is associated with a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadView;

**Входные данные**:

* (System.Object)AcadView;
* (System.Object)bVpAssoc;

## Группа NVP_nanoCAD_COM.OdaX.AcadUnderlay

### Нод _AcadUnderlay_Constructor

**Имя нода** = _AcadUnderlay_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUnderlay;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadUnderlay_ConstructorCast

**Имя нода** = _AcadUnderlay_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUnderlay;

**Входные данные**:

* (System.Object)dynamic;

### Нод Contrast

**Имя нода** = Contrast;

**Описание** = Specifies or returns the contrast of the underlay.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUnderlay;

**Входные данные**:

* (System.Object)AcadUnderlay;

### Нод Set_Contrast

**Имя нода** = Set_Contrast;

**Описание** = Specifies or returns the contrast of the underlay.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUnderlay;

**Входные данные**:

* (System.Object)AcadUnderlay;
* (System.Object)Contrast;

### Нод Fade

**Имя нода** = Fade;

**Описание** = Specifies or returns the fade of the underlay.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUnderlay;

**Входные данные**:

* (System.Object)AcadUnderlay;

### Нод Set_Fade

**Имя нода** = Set_Fade;

**Описание** = Specifies or returns the fade of the underlay.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUnderlay;

**Входные данные**:

* (System.Object)AcadUnderlay;
* (System.Object)Fade;

### Нод Position

**Имя нода** = Position;

**Описание** = Specifies or returns the lower left point of the underlay position.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUnderlay;

**Входные данные**:

* (System.Object)AcadUnderlay;

### Нод Set_Position

**Имя нода** = Set_Position;

**Описание** = Specifies or returns the lower left point of the underlay position.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUnderlay;

**Входные данные**:

* (System.Object)AcadUnderlay;
* (System.Object)pos;

### Нод Rotation

**Имя нода** = Rotation;

**Описание** = Specifies or returns the rotation of the underlay.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUnderlay;

**Входные данные**:

* (System.Object)AcadUnderlay;

### Нод Set_Rotation

**Имя нода** = Set_Rotation;

**Описание** = Specifies or returns the rotation of the underlay.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUnderlay;

**Входные данные**:

* (System.Object)AcadUnderlay;
* (System.Object)rotAngle;

### Нод Width

**Имя нода** = Width;

**Описание** = Specifies or returns the width of the underlay.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUnderlay;

**Входные данные**:

* (System.Object)AcadUnderlay;

### Нод Set_Width

**Имя нода** = Set_Width;

**Описание** = Specifies or returns the width of the underlay.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUnderlay;

**Входные данные**:

* (System.Object)AcadUnderlay;
* (System.Double)Width;

### Нод Height

**Имя нода** = Height;

**Описание** = Specifies or returns the height of the underlay.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUnderlay;

**Входные данные**:

* (System.Object)AcadUnderlay;

### Нод Set_Height

**Имя нода** = Set_Height;

**Описание** = Specifies or returns the height of the underlay.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUnderlay;

**Входные данные**:

* (System.Object)AcadUnderlay;
* (System.Double)Height;

### Нод UnderlayName

**Имя нода** = UnderlayName;

**Описание** = Specifies or returns the name of the underlay file.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUnderlay;

**Входные данные**:

* (System.Object)AcadUnderlay;

### Нод Set_UnderlayName

**Имя нода** = Set_UnderlayName;

**Описание** = Specifies or returns the name of the underlay file.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUnderlay;

**Входные данные**:

* (System.Object)AcadUnderlay;
* (System.String)Name;

### Нод Set_ItemName

**Имя нода** = Set_ItemName;

**Описание** = Specifies or returns the item name of the underlay file.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUnderlay;

**Входные данные**:

* (System.Object)AcadUnderlay;
* (System.String)sheetName;

### Нод ItemName

**Имя нода** = ItemName;

**Описание** = Specifies or returns the item name of the underlay file.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUnderlay;

**Входные данные**:

* (System.Object)AcadUnderlay;

### Нод Monochrome

**Имя нода** = Monochrome;

**Описание** = Specifies or returns whether the underlay displays in monochrome.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUnderlay;

**Входные данные**:

* (System.Object)AcadUnderlay;

### Нод Set_Monochrome

**Имя нода** = Set_Monochrome;

**Описание** = Specifies or returns whether the underlay displays in monochrome.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUnderlay;

**Входные данные**:

* (System.Object)AcadUnderlay;
* (System.Object)bMono;

### Нод AdjustForBackground

**Имя нода** = AdjustForBackground;

**Описание** = Specifies or returns whether underlay colors are adjusted according to the background color.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUnderlay;

**Входные данные**:

* (System.Object)AcadUnderlay;

### Нод Set_AdjustForBackground

**Имя нода** = Set_AdjustForBackground;

**Описание** = Specifies or returns whether underlay colors are adjusted according to the background color.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUnderlay;

**Входные данные**:

* (System.Object)AcadUnderlay;
* (System.Object)Value;

### Нод ClipBoundary

**Имя нода** = ClipBoundary;

**Описание** = Specifies an area for displaying and printing the underlay.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUnderlay;

**Входные данные**:

* (System.Object)AcadUnderlay;
* (System.Object)boundry;

### Нод ScaleFactor

**Имя нода** = ScaleFactor;

**Описание** = Specifies or returns the scale factor of the underlay.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUnderlay;

**Входные данные**:

* (System.Object)AcadUnderlay;

### Нод Set_ScaleFactor

**Имя нода** = Set_ScaleFactor;

**Описание** = Specifies or returns the scale factor of the underlay.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUnderlay;

**Входные данные**:

* (System.Object)AcadUnderlay;
* (System.Object)ScaleFactor;

### Нод File

**Имя нода** = File;

**Описание** = Specifies or returns the path to the underlay file.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUnderlay;

**Входные данные**:

* (System.Object)AcadUnderlay;

### Нод Set_File

**Имя нода** = Set_File;

**Описание** = Specifies or returns the path to the underlay file.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUnderlay;

**Входные данные**:

* (System.Object)AcadUnderlay;
* (System.String)Name;

### Нод UnderlayVisibility

**Имя нода** = UnderlayVisibility;

**Описание** = Specifies or returns whether underlay displays in the drawing.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUnderlay;

**Входные данные**:

* (System.Object)AcadUnderlay;

### Нод Set_UnderlayVisibility

**Имя нода** = Set_UnderlayVisibility;

**Описание** = Specifies or returns whether underlay displays in the drawing.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUnderlay;

**Входные данные**:

* (System.Object)AcadUnderlay;
* (System.Object)fVisible;

### Нод ClippingEnabled

**Имя нода** = ClippingEnabled;

**Описание** = Specifies or returns whether the clipping boundary of the underlay is turned on.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUnderlay;

**Входные данные**:

* (System.Object)AcadUnderlay;

### Нод Set_ClippingEnabled

**Имя нода** = Set_ClippingEnabled;

**Описание** = Specifies or returns whether the clipping boundary of the underlay is turned on.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUnderlay;

**Входные данные**:

* (System.Object)AcadUnderlay;
* (System.Object)kClip;

### Нод UnderlayLayerOverrideApplied

**Имя нода** = UnderlayLayerOverrideApplied;

**Описание** = Specifies or returns whether the layers of a DWF underlay are applied.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUnderlay;

**Входные данные**:

* (System.Object)AcadUnderlay;

### Нод Set_UnderlayLayerOverrideApplied

**Имя нода** = Set_UnderlayLayerOverrideApplied;

**Описание** = Specifies or returns whether the layers of a DWF underlay are applied.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUnderlay;

**Входные данные**:

* (System.Object)AcadUnderlay;
* (System.Object)OdaX.AcUnderlayLayerOverrideType;

## Группа NVP_nanoCAD_COM.OdaX.AcadUCSs

### Нод _AcadUCSs_Constructor

**Имя нода** = _AcadUCSs_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUCSs;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadUCSs_ConstructorCast

**Имя нода** = _AcadUCSs_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUCSs;

**Входные данные**:

* (System.Object)dynamic;

### Нод Item

**Имя нода** = Item;

**Описание** = Gets the member object at a given index in a collection, group, or selection set;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUCSs;

**Входные данные**:

* (System.Object)AcadUCSs;
* (System.Object)Index;

### Нод Count

**Имя нода** = Count;

**Описание** = Gets the number of items in the collection, dictionary, group, or selection set;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUCSs;

**Входные данные**:

* (System.Object)AcadUCSs;

### Нод Add

**Имя нода** = Add;

**Описание** = Creates a member object and adds it to the appropriate collection;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUCSs;

**Входные данные**:

* (System.Object)AcadUCSs;
* (System.Object)Origin;
* (System.Object)XAxisPoint;
* (System.Object)YAxisPoint;
* (System.String)Name;

### Нод GetAll_UCSs

**Имя нода** = GetAll_UCSs;

**Описание** = Возвращает все ПСК чертежа в виде OdaX.AcadUCS;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUCSs;

**Входные данные**:

* (System.Object)AcadUCSs;

## Группа NVP_nanoCAD_COM.OdaX.AcadUCS

### Нод _AcadUCS_Constructor

**Имя нода** = _AcadUCS_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUCS;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadUCS_ConstructorCast

**Имя нода** = _AcadUCS_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUCS;

**Входные данные**:

* (System.Object)dynamic;

### Нод Name

**Имя нода** = Name;

**Описание** = Specifies the name of the object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUCS;

**Входные данные**:

* (System.Object)AcadUCS;

### Нод Set_Name

**Имя нода** = Set_Name;

**Описание** = Specifies the name of the object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUCS;

**Входные данные**:

* (System.Object)AcadUCS;
* (System.String)bstrName;

### Нод Origin

**Имя нода** = Origin;

**Описание** = Specifies the origin of the UCS, block, layout, or raster image in WCS coordinates;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUCS;

**Входные данные**:

* (System.Object)AcadUCS;

### Нод Set_Origin

**Имя нода** = Set_Origin;

**Описание** = Specifies the origin of the UCS, block, layout, or raster image in WCS coordinates;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUCS;

**Входные данные**:

* (System.Object)AcadUCS;
* (System.Object)Origin;

### Нод XVector

**Имя нода** = XVector;

**Описание** = Specifies the X direction of the given UCS;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUCS;

**Входные данные**:

* (System.Object)AcadUCS;

### Нод Set_XVector

**Имя нода** = Set_XVector;

**Описание** = Specifies the X direction of the given UCS;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUCS;

**Входные данные**:

* (System.Object)AcadUCS;
* (System.Object)XVector;

### Нод YVector

**Имя нода** = YVector;

**Описание** = Specifies the Y direction of the given UCS;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUCS;

**Входные данные**:

* (System.Object)AcadUCS;

### Нод Set_YVector

**Имя нода** = Set_YVector;

**Описание** = Specifies the Y direction of the given UCS;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUCS;

**Входные данные**:

* (System.Object)AcadUCS;
* (System.Object)YVector;

### Нод GetUCSMatrix

**Имя нода** = GetUCSMatrix;

**Описание** = Gets the transformation matrix consisting of UCS coordinate system data;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadUCS;

**Входные данные**:

* (System.Object)AcadUCS;

## Группа NVP_nanoCAD_COM.OdaX.AcadTrace

### Нод _AcadTrace_Constructor

**Имя нода** = _AcadTrace_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTrace;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadTrace_ConstructorCast

**Имя нода** = _AcadTrace_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTrace;

**Входные данные**:

* (System.Object)dynamic;

### Нод Coordinates

**Имя нода** = Coordinates;

**Описание** = Specifies the coordinates of the trace;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTrace;

**Входные данные**:

* (System.Object)AcadTrace;

### Нод Set_Coordinates

**Имя нода** = Set_Coordinates;

**Описание** = Specifies the coordinates of the trace;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTrace;

**Входные данные**:

* (System.Object)AcadTrace;
* (System.Object)corners;

### Нод Normal

**Имя нода** = Normal;

**Описание** = Specifies the three-dimensional normal unit vector for the entity;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTrace;

**Входные данные**:

* (System.Object)AcadTrace;

### Нод Set_Normal

**Имя нода** = Set_Normal;

**Описание** = Specifies the three-dimensional normal unit vector for the entity;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTrace;

**Входные данные**:

* (System.Object)AcadTrace;
* (System.Object)Normal;

### Нод Thickness

**Имя нода** = Thickness;

**Описание** = z-direction value) of the trace;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTrace;

**Входные данные**:

* (System.Object)AcadTrace;

### Нод Set_Thickness

**Имя нода** = Set_Thickness;

**Описание** = z-direction value) of the trace;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTrace;

**Входные данные**:

* (System.Object)AcadTrace;
* (System.Double)Thickness;

### Нод Coordinate

**Имя нода** = Coordinate;

**Описание** = Specifies or returns the coordinate of a single vertex in an object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTrace;

**Входные данные**:

* (System.Object)AcadTrace;
* (System.Int32)Index;

### Нод Set_Coordinate

**Имя нода** = Set_Coordinate;

**Описание** = Specifies or returns the coordinate of a single vertex in an object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTrace;

**Входные данные**:

* (System.Object)AcadTrace;
* (System.Int32)Index;
* (System.Object)pVal;

## Группа NVP_nanoCAD_COM.OdaX.AcadTolerance

### Нод _AcadTolerance_Constructor

**Имя нода** = _AcadTolerance_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTolerance;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadTolerance_ConstructorCast

**Имя нода** = _AcadTolerance_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTolerance;

**Входные данные**:

* (System.Object)dynamic;

### Нод DirectionVector

**Имя нода** = DirectionVector;

**Описание** = Specifies the direction for the ray, tolerance, or xline through a vector;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTolerance;

**Входные данные**:

* (System.Object)AcadTolerance;

### Нод Set_DirectionVector

**Имя нода** = Set_DirectionVector;

**Описание** = Specifies the direction for the ray, tolerance, or xline through a vector;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTolerance;

**Входные данные**:

* (System.Object)AcadTolerance;
* (System.Object)dirVector;

### Нод InsertionPoint

**Имя нода** = InsertionPoint;

**Описание** = Specify the X, Y, Z coordinate for insertion point of the tolerance;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTolerance;

**Входные данные**:

* (System.Object)AcadTolerance;

### Нод Set_InsertionPoint

**Имя нода** = Set_InsertionPoint;

**Описание** = Specify the X, Y, Z coordinate for insertion point of the tolerance;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTolerance;

**Входные данные**:

* (System.Object)AcadTolerance;
* (System.Object)insPoint;

### Нод Normal

**Имя нода** = Normal;

**Описание** = Specifies the three-dimensional normal unit vector for the entity;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTolerance;

**Входные данные**:

* (System.Object)AcadTolerance;

### Нод Set_Normal

**Имя нода** = Set_Normal;

**Описание** = Specifies the three-dimensional normal unit vector for the entity;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTolerance;

**Входные данные**:

* (System.Object)AcadTolerance;
* (System.Object)Normal;

### Нод StyleName

**Имя нода** = StyleName;

**Описание** = Specifies the style name of the tolerance;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTolerance;

**Входные данные**:

* (System.Object)AcadTolerance;

### Нод Set_StyleName

**Имя нода** = Set_StyleName;

**Описание** = Specifies the style name of the tolerance;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTolerance;

**Входные данные**:

* (System.Object)AcadTolerance;
* (System.String)bstrName;

### Нод TextColor

**Имя нода** = TextColor;

**Описание** = Specifies the color of the dimension text;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTolerance;

**Входные данные**:

* (System.Object)AcadTolerance;

### Нод Set_TextColor

**Имя нода** = Set_TextColor;

**Описание** = Specifies the color of the dimension text;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTolerance;

**Входные данные**:

* (System.Object)AcadTolerance;
* (System.Object)color;

### Нод TextString

**Имя нода** = TextString;

**Описание** = Specifies the text string of the tolerance;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTolerance;

**Входные данные**:

* (System.Object)AcadTolerance;

### Нод Set_TextString

**Имя нода** = Set_TextString;

**Описание** = Specifies the text string of the tolerance;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTolerance;

**Входные данные**:

* (System.Object)AcadTolerance;
* (System.String)bstrText;

### Нод TextStyle

**Имя нода** = TextStyle;

**Описание** = Specifies the text style of the tolerance;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTolerance;

**Входные данные**:

* (System.Object)AcadTolerance;

### Нод Set_TextStyle

**Имя нода** = Set_TextStyle;

**Описание** = Specifies the text style of the tolerance;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTolerance;

**Входные данные**:

* (System.Object)AcadTolerance;
* (System.String)style;

### Нод TextHeight

**Имя нода** = TextHeight;

**Описание** = Specifies the text height of the tolerance;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTolerance;

**Входные данные**:

* (System.Object)AcadTolerance;

### Нод Set_TextHeight

**Имя нода** = Set_TextHeight;

**Описание** = Specifies the text height of the tolerance;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTolerance;

**Входные данные**:

* (System.Object)AcadTolerance;
* (System.Double)Height;

### Нод ScaleFactor

**Имя нода** = ScaleFactor;

**Описание** = Specifies the overall scale factor applied to properties that specify sizes, distances, or offsets;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTolerance;

**Входные данные**:

* (System.Object)AcadTolerance;

### Нод Set_ScaleFactor

**Имя нода** = Set_ScaleFactor;

**Описание** = Specifies the overall scale factor applied to properties that specify sizes, distances, or offsets;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTolerance;

**Входные данные**:

* (System.Object)AcadTolerance;
* (System.Object)factor;

### Нод DimensionLineColor

**Имя нода** = DimensionLineColor;

**Описание** = Specifies the color of the dimension lines;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTolerance;

**Входные данные**:

* (System.Object)AcadTolerance;

### Нод Set_DimensionLineColor

**Имя нода** = Set_DimensionLineColor;

**Описание** = Specifies the color of the dimension lines;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTolerance;

**Входные данные**:

* (System.Object)AcadTolerance;
* (System.Object)Type;

## Группа NVP_nanoCAD_COM.OdaX.AcadTextStyles

### Нод _AcadTextStyles_Constructor

**Имя нода** = _AcadTextStyles_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTextStyles;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadTextStyles_ConstructorCast

**Имя нода** = _AcadTextStyles_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTextStyles;

**Входные данные**:

* (System.Object)dynamic;

### Нод Item

**Имя нода** = Item;

**Описание** = Gets the member object at a given index in a collection, group, or selection set;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTextStyles;

**Входные данные**:

* (System.Object)AcadTextStyles;
* (System.Object)Index;

### Нод Count

**Имя нода** = Count;

**Описание** = Gets the number of items in the collection, dictionary, group, or selection set;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTextStyles;

**Входные данные**:

* (System.Object)AcadTextStyles;

### Нод Add

**Имя нода** = Add;

**Описание** = Creates a member object and adds it to the appropriate collection;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTextStyles;

**Входные данные**:

* (System.Object)AcadTextStyles;
* (System.String)Name;

## Группа NVP_nanoCAD_COM.OdaX.AcadTextStyle

### Нод _AcadTextStyle_Constructor

**Имя нода** = _AcadTextStyle_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTextStyle;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadTextStyle_ConstructorCast

**Имя нода** = _AcadTextStyle_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTextStyle;

**Входные данные**:

* (System.Object)dynamic;

### Нод BigFontFile

**Имя нода** = BigFontFile;

**Описание** = Specifies the name of the big font file associated with the text or attribute;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTextStyle;

**Входные данные**:

* (System.Object)AcadTextStyle;

### Нод Set_BigFontFile

**Имя нода** = Set_BigFontFile;

**Описание** = Specifies the name of the big font file associated with the text or attribute;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTextStyle;

**Входные данные**:

* (System.Object)AcadTextStyle;
* (System.String)fontFile;

### Нод fontFile

**Имя нода** = fontFile;

**Описание** = Specifies the primary font file path and name;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTextStyle;

**Входные данные**:

* (System.Object)AcadTextStyle;

### Нод Set_fontFile

**Имя нода** = Set_fontFile;

**Описание** = Specifies the primary font file path and name;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTextStyle;

**Входные данные**:

* (System.Object)AcadTextStyle;
* (System.String)fontFile;

### Нод Height

**Имя нода** = Height;

**Описание** = Height of the attribute, shape, text, or view toolbar or the main application window;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTextStyle;

**Входные данные**:

* (System.Object)AcadTextStyle;

### Нод Set_Height

**Имя нода** = Set_Height;

**Описание** = Height of the attribute, shape, text, or view toolbar or the main application window;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTextStyle;

**Входные данные**:

* (System.Object)AcadTextStyle;
* (System.Double)Height;

### Нод LastHeight

**Имя нода** = LastHeight;

**Описание** = Specifies the last text height used;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTextStyle;

**Входные данные**:

* (System.Object)AcadTextStyle;

### Нод Set_LastHeight

**Имя нода** = Set_LastHeight;

**Описание** = Specifies the last text height used;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTextStyle;

**Входные данные**:

* (System.Object)AcadTextStyle;
* (System.Double)Height;

### Нод Name

**Имя нода** = Name;

**Описание** = Specifies the name of the object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTextStyle;

**Входные данные**:

* (System.Object)AcadTextStyle;

### Нод ObliqueAngle

**Имя нода** = ObliqueAngle;

**Описание** = Specifies the oblique angle of the object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTextStyle;

**Входные данные**:

* (System.Object)AcadTextStyle;

### Нод Set_ObliqueAngle

**Имя нода** = Set_ObliqueAngle;

**Описание** = Specifies the oblique angle of the object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTextStyle;

**Входные данные**:

* (System.Object)AcadTextStyle;
* (System.Object)obliAngle;

### Нод TextGenerationFlag

**Имя нода** = TextGenerationFlag;

**Описание** = Specifies the attribute text generation flag;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTextStyle;

**Входные данные**:

* (System.Object)AcadTextStyle;

### Нод Set_TextGenerationFlag

**Имя нода** = Set_TextGenerationFlag;

**Описание** = Specifies the attribute text generation flag;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTextStyle;

**Входные данные**:

* (System.Object)AcadTextStyle;
* (System.Object)textGenFlag;

### Нод Width

**Имя нода** = Width;

**Описание** = Specifies or returns the spacing of characters.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTextStyle;

**Входные данные**:

* (System.Object)AcadTextStyle;

### Нод Set_Width

**Имя нода** = Set_Width;

**Описание** = Specifies or returns the spacing of characters.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTextStyle;

**Входные данные**:

* (System.Object)AcadTextStyle;
* (System.Double)Width;

### Нод GetFont

**Имя нода** = GetFont;

**Описание** = Gets the definition data of the font for the TextStyle;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTextStyle;

**Входные данные**:

* (System.Object)AcadTextStyle;
* (System.String)TypeFace;
* (System.Object)Bold;
* (System.Object)Italic;
* (System.Object)Charset;
* (System.Object)PitchAndFamily;

### Нод SetFont

**Имя нода** = SetFont;

**Описание** = Sets the definition data of the font for the TextStyle;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTextStyle;

**Входные данные**:

* (System.Object)AcadTextStyle;
* (System.String)TypeFace;
* (System.Object)Bold;
* (System.Object)Italic;
* (System.Object)Charset;
* (System.Object)PitchAndFamily;

## Группа NVP_nanoCAD_COM.OdaX.AcadText

### Нод _AcadText_Constructor

**Имя нода** = _AcadText_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadText;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadText_ConstructorCast

**Имя нода** = _AcadText_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadText;

**Входные данные**:

* (System.Object)dynamic;

### Нод TextString

**Имя нода** = TextString;

**Описание** = Specifies the text string of the text;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadText;

**Входные данные**:

* (System.Object)AcadText;

### Нод Set_TextString

**Имя нода** = Set_TextString;

**Описание** = Specifies the text string of the text;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadText;

**Входные данные**:

* (System.Object)AcadText;
* (System.String)bstrText;

### Нод StyleName

**Имя нода** = StyleName;

**Описание** = Specifies the style name of the text;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadText;

**Входные данные**:

* (System.Object)AcadText;

### Нод Set_StyleName

**Имя нода** = Set_StyleName;

**Описание** = Specifies the style name of the text;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadText;

**Входные данные**:

* (System.Object)AcadText;
* (System.String)bstrName;

### Нод Alignment

**Имя нода** = Alignment;

**Описание** = Specifies both text height and text orientation by designating the endpoints of the baseline;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadText;

**Входные данные**:

* (System.Object)AcadText;

### Нод Set_Alignment

**Имя нода** = Set_Alignment;

**Описание** = Specifies both text height and text orientation by designating the endpoints of the baseline;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadText;

**Входные данные**:

* (System.Object)AcadText;
* (System.Object)OdaX.AcAlignment;

### Нод HorizontalAlignment

**Имя нода** = HorizontalAlignment;

**Описание** = Specifies the horizontal alignment of the text;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadText;

**Входные данные**:

* (System.Object)AcadText;

### Нод Set_HorizontalAlignment

**Имя нода** = Set_HorizontalAlignment;

**Описание** = Specifies the horizontal alignment of the text;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadText;

**Входные данные**:

* (System.Object)AcadText;
* (System.Object)OdaX.AcHorizontalAlignment;

### Нод VerticalAlignment

**Имя нода** = VerticalAlignment;

**Описание** = Specifies the vertical alignment of the text;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadText;

**Входные данные**:

* (System.Object)AcadText;

### Нод Set_VerticalAlignment

**Имя нода** = Set_VerticalAlignment;

**Описание** = Specifies the vertical alignment of the text;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadText;

**Входные данные**:

* (System.Object)AcadText;
* (System.Object)OdaX.AcVerticalAlignment;

### Нод Height

**Имя нода** = Height;

**Описание** = Specifies the height of the text;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadText;

**Входные данные**:

* (System.Object)AcadText;

### Нод Set_Height

**Имя нода** = Set_Height;

**Описание** = Specifies the height of the text;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadText;

**Входные данные**:

* (System.Object)AcadText;
* (System.Double)Height;

### Нод Rotation

**Имя нода** = Rotation;

**Описание** = Specifies the rotation angle of the text;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadText;

**Входные данные**:

* (System.Object)AcadText;

### Нод Set_Rotation

**Имя нода** = Set_Rotation;

**Описание** = Specifies the rotation angle of the text;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadText;

**Входные данные**:

* (System.Object)AcadText;
* (System.Object)rotAngle;

### Нод ScaleFactor

**Имя нода** = ScaleFactor;

**Описание** = Specifies the width scale factor of the text;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadText;

**Входные данные**:

* (System.Object)AcadText;

### Нод Set_ScaleFactor

**Имя нода** = Set_ScaleFactor;

**Описание** = Specifies the width scale factor of the text;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadText;

**Входные данные**:

* (System.Object)AcadText;
* (System.Object)scalFactor;

### Нод ObliqueAngle

**Имя нода** = ObliqueAngle;

**Описание** = Specifies the oblique angle of the text;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadText;

**Входные данные**:

* (System.Object)AcadText;

### Нод Set_ObliqueAngle

**Имя нода** = Set_ObliqueAngle;

**Описание** = Specifies the oblique angle of the text;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadText;

**Входные данные**:

* (System.Object)AcadText;
* (System.Object)obliAngle;

### Нод TextAlignmentPoint

**Имя нода** = TextAlignmentPoint;

**Описание** = Specify the X, Y, Z coordinate of the alignment point of the text;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadText;

**Входные данные**:

* (System.Object)AcadText;

### Нод Set_TextAlignmentPoint

**Имя нода** = Set_TextAlignmentPoint;

**Описание** = Specify the X, Y, Z coordinate of the alignment point of the text;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadText;

**Входные данные**:

* (System.Object)AcadText;
* (System.Object)alignPoint;

### Нод InsertionPoint

**Имя нода** = InsertionPoint;

**Описание** = Specify the X, Y, Z coordinate of the insertion point of the text;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadText;

**Входные данные**:

* (System.Object)AcadText;

### Нод Set_InsertionPoint

**Имя нода** = Set_InsertionPoint;

**Описание** = Specify the X, Y, Z coordinate of the insertion point of the text;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadText;

**Входные данные**:

* (System.Object)AcadText;
* (System.Object)insPoint;

### Нод Normal

**Имя нода** = Normal;

**Описание** = Specifies the three-dimensional normal unit vector for the entity;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadText;

**Входные данные**:

* (System.Object)AcadText;

### Нод Set_Normal

**Имя нода** = Set_Normal;

**Описание** = Specifies the three-dimensional normal unit vector for the entity;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadText;

**Входные данные**:

* (System.Object)AcadText;
* (System.Object)Normal;

### Нод TextGenerationFlag

**Имя нода** = TextGenerationFlag;

**Описание** = Specifies the attribute text generation flag;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadText;

**Входные данные**:

* (System.Object)AcadText;

### Нод Set_TextGenerationFlag

**Имя нода** = Set_TextGenerationFlag;

**Описание** = Specifies the attribute text generation flag;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadText;

**Входные данные**:

* (System.Object)AcadText;
* (System.Object)textGenFlag;

### Нод Thickness

**Имя нода** = Thickness;

**Описание** = z-direction value) of the text;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadText;

**Входные данные**:

* (System.Object)AcadText;

### Нод Set_Thickness

**Имя нода** = Set_Thickness;

**Описание** = z-direction value) of the text;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadText;

**Входные данные**:

* (System.Object)AcadText;
* (System.Double)Thickness;

### Нод UpsideDown

**Имя нода** = UpsideDown;

**Описание** = Determines whether the text is upside down or not;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadText;

**Входные данные**:

* (System.Object)AcadText;

### Нод Set_UpsideDown

**Имя нода** = Set_UpsideDown;

**Описание** = Determines whether the text is upside down or not;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadText;

**Входные данные**:

* (System.Object)AcadText;
* (System.Object)UpsideDown;

### Нод Backward

**Имя нода** = Backward;

**Описание** = Determines whether the text is backward or not;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadText;

**Входные данные**:

* (System.Object)AcadText;

### Нод Set_Backward

**Имя нода** = Set_Backward;

**Описание** = Determines whether the text is backward or not;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadText;

**Входные данные**:

* (System.Object)AcadText;
* (System.Object)Backward;

### Нод FieldCode

**Имя нода** = FieldCode;

**Описание** = Returns the text string with field codes of the text;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadText;

**Входные данные**:

* (System.Object)AcadText;

## Группа NVP_nanoCAD_COM.OdaX.AcadTableStyle

### Нод _AcadTableStyle_Constructor

**Имя нода** = _AcadTableStyle_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadTableStyle_ConstructorCast

**Имя нода** = _AcadTableStyle_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)dynamic;

### Нод Name

**Имя нода** = Name;

**Описание** = Not implemented. Specifies or returns the name of a table style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;

### Нод Set_Name

**Имя нода** = Set_Name;

**Описание** = Not implemented. Specifies or returns the name of a table style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.String)bstrValue;

### Нод Description

**Имя нода** = Description;

**Описание** = Specifies or returns the description of a tables tyle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;

### Нод Set_Description

**Имя нода** = Set_Description;

**Описание** = Specifies or returns the description of a tables tyle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.String)bstr;

### Нод BitFlags

**Имя нода** = BitFlags;

**Описание** = Specifies or returns the operation bit set of a table style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;

### Нод Set_BitFlags

**Имя нода** = Set_BitFlags;

**Описание** = Specifies or returns the operation bit set of a table style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.Object)bitFlag;

### Нод FlowDirection

**Имя нода** = FlowDirection;

**Описание** = Specifies or returns the flow direction of a table style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;

### Нод Set_FlowDirection

**Имя нода** = Set_FlowDirection;

**Описание** = Specifies or returns the flow direction of a table style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.Object)OdaX.AcTableDirection;

### Нод HorzCellMargin

**Имя нода** = HorzCellMargin;

**Описание** = Specifies or returns the horizontal cell margin of a table style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;

### Нод Set_HorzCellMargin

**Имя нода** = Set_HorzCellMargin;

**Описание** = Specifies or returns the horizontal cell margin of a table style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.Double)dHorzCellMargin;

### Нод VertCellMargin

**Имя нода** = VertCellMargin;

**Описание** = Specifies or returns the vertical cell margin of a table style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;

### Нод Set_VertCellMargin

**Имя нода** = Set_VertCellMargin;

**Описание** = Specifies or returns the vertical cell margin of a table style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.Double)dVertCellMargin;

### Нод TitleSuppressed

**Имя нода** = TitleSuppressed;

**Описание** = Specifies or returns whether the table title is visible for a table style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;

### Нод Set_TitleSuppressed

**Имя нода** = Set_TitleSuppressed;

**Описание** = Specifies or returns whether the table title is visible for a table style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.Object)bValue;

### Нод HeaderSuppressed

**Имя нода** = HeaderSuppressed;

**Описание** = Specifies or returns whether the header titles are visible for a table style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;

### Нод Set_HeaderSuppressed

**Имя нода** = Set_HeaderSuppressed;

**Описание** = Specifies or returns whether the header titles are visible for a table style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.Object)bValue;

### Нод GetTextStyle

**Имя нода** = GetTextStyle;

**Описание** = Returns the text style name of a row type.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.Object)OdaX.AcRowType;

### Нод SetTextStyle

**Имя нода** = SetTextStyle;

**Описание** = Specifies the text style name of row types.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.Int32)rowTypes;
* (System.String)bstrName;

### Нод GetTextHeight

**Имя нода** = GetTextHeight;

**Описание** = Returns the text height of a row type.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.Object)OdaX.AcRowType;

### Нод SetTextHeight

**Имя нода** = SetTextHeight;

**Описание** = Specifies the text height of row types.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.Int32)rowTypes;
* (System.Double)TextHeight;

### Нод GetAlignment

**Имя нода** = GetAlignment;

**Описание** = Returns the cell alignment of a row type.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.Object)OdaX.AcRowType;

### Нод SetAlignment

**Имя нода** = SetAlignment;

**Описание** = Specifies the cell alignment of row types.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.Int32)rowTypes;
* (System.Object)OdaX.AcCellAlignment;

### Нод GetColor

**Имя нода** = GetColor;

**Описание** = Returns the color value of a row type.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.Object)OdaX.AcRowType;

### Нод SetColor

**Имя нода** = SetColor;

**Описание** = Specifies the color value of row types.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.Int32)rowTypes;
* (System.Object)pColor;

### Нод GetBackgroundColor

**Имя нода** = GetBackgroundColor;

**Описание** = Returns the background color value of a row type.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.Object)OdaX.AcRowType;

### Нод SetBackgroundColor

**Имя нода** = SetBackgroundColor;

**Описание** = Specifies the background color value of row types.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.Int32)rowTypes;
* (System.Object)pColor;

### Нод GetBackgroundColorNone

**Имя нода** = GetBackgroundColorNone;

**Описание** = Returns whether the background color is set to None for a row type.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.Object)OdaX.AcRowType;

### Нод SetBackgroundColorNone

**Имя нода** = SetBackgroundColorNone;

**Описание** = Specifies whether the background color is set to None for row types.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.Int32)rowTypes;
* (System.Object)bValue;

### Нод GetGridLineWeight

**Имя нода** = GetGridLineWeight;

**Описание** = Returns the grid lineweight value for a grid linetype and row type.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.Object)OdaX.AcGridLineType;
* (System.Object)OdaX.AcRowType;

### Нод SetGridLineWeight

**Имя нода** = SetGridLineWeight;

**Описание** = Specifies the grid lineweight value for grid linetypes and row types.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.Int32)gridLineTypes;
* (System.Int32)rowTypes;
* (System.Object)Lineweight;

### Нод GetGridColor

**Имя нода** = GetGridColor;

**Описание** = Returns the grid color value for a grid linetype and row type.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.Object)OdaX.AcGridLineType;
* (System.Object)OdaX.AcRowType;

### Нод SetGridColor

**Имя нода** = SetGridColor;

**Описание** = Specifies the grid color value for grid linetypes and row types.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.Int32)gridLineTypes;
* (System.Int32)rowTypes;
* (System.Object)pColor;

### Нод GetGridVisibility

**Имя нода** = GetGridVisibility;

**Описание** = Returns whether the grid is visible for a grid linetype and row type.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.Object)OdaX.AcGridLineType;
* (System.Object)OdaX.AcRowType;

### Нод SetGridVisibility

**Имя нода** = SetGridVisibility;

**Описание** = Specifies whether the grid is visible for grid linetypes and row types.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.Int32)gridLineTypes;
* (System.Int32)rowTypes;
* (System.Object)bValue;

### Нод GetDataType

**Имя нода** = GetDataType;

**Описание** = Not implemented. Returns the data type and unit type of a row type.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.Object)OdaX.AcRowType;
* (System.Object)OdaX.AcValueDataType;
* (System.Object)OdaX.AcValueUnitType;

### Нод SetDataType

**Имя нода** = SetDataType;

**Описание** = Not implemented. Specifies the data type and unit type of a row type.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.Int32)rowTypes;
* (System.Object)OdaX.AcValueDataType;
* (System.Object)OdaX.AcValueUnitType;

### Нод GetFormat

**Имя нода** = GetFormat;

**Описание** = Not implemented. Returns the format of a row type.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.Object)OdaX.AcRowType;

### Нод SetFormat

**Имя нода** = SetFormat;

**Описание** = Not implemented. Specifies the format of a row type.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.Int32)rowTypes;
* (System.String)val;

### Нод CreateCellStyle

**Имя нода** = CreateCellStyle;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.String)bstrCellStyle;

### Нод CreateCellStyleFromStyle

**Имя нода** = CreateCellStyleFromStyle;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.String)bstrCellStyle;
* (System.String)bstrSourceCellStyle;

### Нод RenameCellStyle

**Имя нода** = RenameCellStyle;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.String)bstrOldName;
* (System.String)bstrNewName;

### Нод DeleteCellStyle

**Имя нода** = DeleteCellStyle;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.String)bstrCellStyle;

### Нод GetUniqueCellStyleName

**Имя нода** = GetUniqueCellStyleName;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.String)pszBaseName;

### Нод GetIsCellStyleInUse

**Имя нода** = GetIsCellStyleInUse;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.String)pszCellStyle;

### Нод NumCellStyles

**Имя нода** = NumCellStyles;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;

### Нод GetCellStyles

**Имя нода** = GetCellStyles;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.Object)cellStylesArray;

### Нод GetTextStyleId

**Имя нода** = GetTextStyleId;

**Описание** = Returns the text style name for the specified cellStyle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.String)bstrCellStyle;

### Нод SetTextStyleId

**Имя нода** = SetTextStyleId;

**Описание** = Sets the text style name for the specified cellStyle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.String)bstrCellStyle;
* (System.Object)val;

### Нод GetTextHeight2

**Имя нода** = GetTextHeight2;

**Описание** = Returns the text height for the specified cellStyle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.String)bstrCellStyle;

### Нод SetTextHeight2

**Имя нода** = SetTextHeight2;

**Описание** = Sets the text height for the specified cellStyle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.String)bstrCellStyle;
* (System.Double)Height;

### Нод GetAlignment2

**Имя нода** = GetAlignment2;

**Описание** = Returns the cell alignment for the specified cellStyle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.String)bstrCellStyle;

### Нод SetAlignment2

**Имя нода** = SetAlignment2;

**Описание** = Sets the cell alignment for the specified cellStyle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.String)bstrCellStyle;
* (System.Object)OdaX.AcCellAlignment;

### Нод GetColor2

**Имя нода** = GetColor2;

**Описание** = Returns the true color value for the specified cellStyle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.String)bstrCellStyle;

### Нод SetColor2

**Имя нода** = SetColor2;

**Описание** = Sets the true color value for the specified cellStyle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.String)bstrCellStyle;
* (System.Object)color;

### Нод GetBackgroundColor2

**Имя нода** = GetBackgroundColor2;

**Описание** = Returns the background true color value for the specified cellStyle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.String)bstrCellStyle;

### Нод SetBackgroundColor2

**Имя нода** = SetBackgroundColor2;

**Описание** = Sets the background true color value for the specified cellStyle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.String)bstrCellStyle;
* (System.Object)color;

### Нод GetDataType2

**Имя нода** = GetDataType2;

**Описание** = Returns the data type and unit type for the specifed cellStyle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.String)bstrCellStyle;
* (System.Object)OdaX.AcValueDataType;
* (System.Object)OdaX.AcValueUnitType;

### Нод SetDataType2

**Имя нода** = SetDataType2;

**Описание** = Sets the data type and unit type for the specifed cellStyle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.String)bstrCellStyle;
* (System.Object)OdaX.AcValueDataType;
* (System.Object)OdaX.AcValueUnitType;

### Нод GetFormat2

**Имя нода** = GetFormat2;

**Описание** = Returns the format for the specifed cellStyle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.String)bstrCellStyle;
* (System.String)pbstrFormat;

### Нод SetFormat2

**Имя нода** = SetFormat2;

**Описание** = Sets the format for the specifed cellStyle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.String)bstrCellStyle;
* (System.String)bstrFormat;

### Нод GetCellClass

**Имя нода** = GetCellClass;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.String)bstrCellStyle;

### Нод SetCellClass

**Имя нода** = SetCellClass;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.String)bstrCellStyle;
* (System.Int32)cellClass;

### Нод GetRotation

**Имя нода** = GetRotation;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.String)bstrCellStyle;

### Нод SetRotation

**Имя нода** = SetRotation;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.String)bstrCellStyle;
* (System.Double)Rotation;

### Нод GetIsMergeAllEnabled

**Имя нода** = GetIsMergeAllEnabled;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.String)bstrCellStyle;

### Нод EnableMergeAll

**Имя нода** = EnableMergeAll;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.String)bstrCellStyle;
* (System.Object)bEnable;

### Нод GetGridLineWeight2

**Имя нода** = GetGridLineWeight2;

**Описание** = s) and cellStyle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.String)bstrCellStyle;
* (System.Object)OdaX.AcGridLineType;

### Нод SetGridLineWeight2

**Имя нода** = SetGridLineWeight2;

**Описание** = s) and cellStyle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.String)bstrCellStyle;
* (System.Object)OdaX.AcGridLineType;
* (System.Object)Lineweight;

### Нод GetGridColor2

**Имя нода** = GetGridColor2;

**Описание** = Returns the gridColor value for the specified gridLineType and cellStyle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.String)bstrCellStyle;
* (System.Object)OdaX.AcGridLineType;

### Нод SetGridColor2

**Имя нода** = SetGridColor2;

**Описание** = Sets the gridColor value for the specified gridLineType and cellStyle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.String)bstrCellStyle;
* (System.Object)OdaX.AcGridLineType;
* (System.Object)pColor;

### Нод GetGridVisibility2

**Имя нода** = GetGridVisibility2;

**Описание** = Returns the gridVisibility value for the specified gridLineType and cellStyle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.String)bstrCellStyle;
* (System.Object)OdaX.AcGridLineType;

### Нод SetGridVisibility2

**Имя нода** = SetGridVisibility2;

**Описание** = Sets the gridVisibility value for the specified gridLineType and cellStyle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.String)bstrCellStyle;
* (System.Object)OdaX.AcGridLineType;
* (System.Object)bValue;

### Нод TemplateId

**Имя нода** = TemplateId;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;

### Нод Set_TemplateId

**Имя нода** = Set_TemplateId;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.Object)pVal;

### Нод SetTemplateId

**Имя нода** = SetTemplateId;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTableStyle;

**Входные данные**:

* (System.Object)AcadTableStyle;
* (System.Object)val;

## Группа NVP_nanoCAD_COM.OdaX.AcadTable

### Нод _AcadTable_Constructor

**Имя нода** = _AcadTable_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadTable_ConstructorCast

**Имя нода** = _AcadTable_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)dynamic;

### Нод StyleName

**Имя нода** = StyleName;

**Описание** = Specifies or returns the name of the table style assigned to a table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;

### Нод Set_StyleName

**Имя нода** = Set_StyleName;

**Описание** = Specifies or returns the name of the table style assigned to a table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.String)bstrName;

### Нод Rows

**Имя нода** = Rows;

**Описание** = Specifies or returns the number of rows in a table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;

### Нод Set_Rows

**Имя нода** = Set_Rows;

**Описание** = Specifies or returns the number of rows in a table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)pRows;

### Нод Columns

**Имя нода** = Columns;

**Описание** = Specifies or returns the number of columns in a table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;

### Нод Set_Columns

**Имя нода** = Set_Columns;

**Описание** = Specifies or returns the number of columns in a table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)pColumns;

### Нод FlowDirection

**Имя нода** = FlowDirection;

**Описание** = Specifies or returns the position a table's title and row headings.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;

### Нод Set_FlowDirection

**Имя нода** = Set_FlowDirection;

**Описание** = Specifies or returns the position a table's title and row headings.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Object)OdaX.AcTableDirection;

### Нод Width

**Имя нода** = Width;

**Описание** = Specifies or returns the width of a table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;

### Нод Set_Width

**Имя нода** = Set_Width;

**Описание** = Specifies or returns the width of a table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Double)pWidth;

### Нод Height

**Имя нода** = Height;

**Описание** = Specifies or returns the height of a table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;

### Нод Set_Height

**Имя нода** = Set_Height;

**Описание** = Specifies or returns the height of a table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Double)pHeight;

### Нод VertCellMargin

**Имя нода** = VertCellMargin;

**Описание** = Specifies or returns the distance between cell text and the top/bottom cell edge.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;

### Нод Set_VertCellMargin

**Имя нода** = Set_VertCellMargin;

**Описание** = Specifies or returns the distance between cell text and the top/bottom cell edge.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Double)pGap;

### Нод HorzCellMargin

**Имя нода** = HorzCellMargin;

**Описание** = Specifies or returns the distance between cell text and the left/right cell edge.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;

### Нод Set_HorzCellMargin

**Имя нода** = Set_HorzCellMargin;

**Описание** = Specifies or returns the distance between cell text and the left/right cell edge.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Double)pGap;

### Нод InsertionPoint

**Имя нода** = InsertionPoint;

**Описание** = Specifies or returns the x, y, and z coordinates of the table insertion point.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;

### Нод Set_InsertionPoint

**Имя нода** = Set_InsertionPoint;

**Описание** = Specifies or returns the x, y, and z coordinates of the table insertion point.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Object)insPoint;

### Нод GetColumnWidth

**Имя нода** = GetColumnWidth;

**Описание** = Returns the width of a column in a table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)col;

### Нод SetColumnWidth

**Имя нода** = SetColumnWidth;

**Описание** = Sets the width of a column in a table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)col;
* (System.Double)Width;

### Нод Set_ColumnWidth

**Имя нода** = Set_ColumnWidth;

**Описание** = Sets the width of all columns in a table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Double)rhs;

### Нод GetRowHeight

**Имя нода** = GetRowHeight;

**Описание** = Returns the height of a row in a table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;

### Нод SetRowHeight

**Имя нода** = SetRowHeight;

**Описание** = Sets the height of a row in a table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Double)Height;

### Нод Set_RowHeight

**Имя нода** = Set_RowHeight;

**Описание** = Sets the height of all rows in a table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Double)rhs;

### Нод GetMinimumColumnWidth

**Имя нода** = GetMinimumColumnWidth;

**Описание** = Returns the minimum width of a column in a table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)col;

### Нод GetMinimumRowHeight

**Имя нода** = GetMinimumRowHeight;

**Описание** = Returns the minimum height of a row in a table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;

### Нод MinimumTableWidth

**Имя нода** = MinimumTableWidth;

**Описание** = Returns the minimum width of a table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;

### Нод MinimumTableHeight

**Имя нода** = MinimumTableHeight;

**Описание** = Returns the minimum height of a table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;

### Нод Direction

**Имя нода** = Direction;

**Описание** = Specifies or returns the x plane where a table is positioned.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;

### Нод Set_Direction

**Имя нода** = Set_Direction;

**Описание** = Specifies or returns the x plane where a table is positioned.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Object)DirectionVector;

### Нод TitleSuppressed

**Имя нода** = TitleSuppressed;

**Описание** = Specifies or returns whether the title of a table is visible.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;

### Нод Set_TitleSuppressed

**Имя нода** = Set_TitleSuppressed;

**Описание** = Specifies or returns whether the title of a table is visible.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Object)bValue;

### Нод HeaderSuppressed

**Имя нода** = HeaderSuppressed;

**Описание** = Specifies or returns whether row headings of a table are visible.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;

### Нод Set_HeaderSuppressed

**Имя нода** = Set_HeaderSuppressed;

**Описание** = Specifies or returns whether row headings of a table are visible.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Object)bValue;

### Нод GetAlignment

**Имя нода** = GetAlignment;

**Описание** = Returns how the cells of a row type are aligned.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Object)OdaX.AcRowType;

### Нод SetAlignment

**Имя нода** = SetAlignment;

**Описание** = Specifies how the cells of a row type are aligned.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)rowTypes;
* (System.Object)OdaX.AcCellAlignment;

### Нод GetBackgroundColorNone

**Имя нода** = GetBackgroundColorNone;

**Описание** = Specifies or returns whether the row type of a table has a background color.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Object)OdaX.AcRowType;

### Нод SetBackgroundColorNone

**Имя нода** = SetBackgroundColorNone;

**Описание** = Specifies or returns whether the row type of a table has a background color.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)rowTypes;
* (System.Object)bValue;

### Нод GetBackgroundColor

**Имя нода** = GetBackgroundColor;

**Описание** = Returns the background color value of a table's row type.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Object)OdaX.AcRowType;

### Нод SetBackgroundColor

**Имя нода** = SetBackgroundColor;

**Описание** = Specifies the background color value of a table's row type.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)rowTypes;
* (System.Object)pColor;

### Нод GetContentColor

**Имя нода** = GetContentColor;

**Описание** = Returns the color value of a table's row type.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Object)OdaX.AcRowType;

### Нод SetContentColor

**Имя нода** = SetContentColor;

**Описание** = Specifies the color value of a table's row type.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)rowTypes;
* (System.Object)pColor;

### Нод GetTextStyle

**Имя нода** = GetTextStyle;

**Описание** = Returns the name of the text style assigned to the row type of a table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Object)OdaX.AcRowType;

### Нод SetTextStyle

**Имя нода** = SetTextStyle;

**Описание** = Specifies the text style assigned to the row type of a table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)rowTypes;
* (System.String)bstrName;

### Нод GetTextHeight

**Имя нода** = GetTextHeight;

**Описание** = Returns the text height of a table's row type.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Object)OdaX.AcRowType;

### Нод SetTextHeight

**Имя нода** = SetTextHeight;

**Описание** = Specifies the text height of a table's row type.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)rowTypes;
* (System.Double)TextHeight;

### Нод GetGridLineWeight

**Имя нода** = GetGridLineWeight;

**Описание** = Returns the grid lineweight value for a grid linetype and row type.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Object)OdaX.AcGridLineType;
* (System.Object)OdaX.AcRowType;

### Нод SetGridLineWeight

**Имя нода** = SetGridLineWeight;

**Описание** = Specifies the grid lineweight value for a grid linetype and row type.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)gridLineTypes;
* (System.Int32)rowTypes;
* (System.Object)Lineweight;

### Нод GetGridColor

**Имя нода** = GetGridColor;

**Описание** = Returns the grid color value for a grid linetype and row type.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Object)OdaX.AcGridLineType;
* (System.Object)OdaX.AcRowType;

### Нод SetGridColor

**Имя нода** = SetGridColor;

**Описание** = Specifies the grid color value for a grid linetype and row type.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)gridLineTypes;
* (System.Int32)rowTypes;
* (System.Object)pColor;

### Нод GetGridVisibility

**Имя нода** = GetGridVisibility;

**Описание** = Returns the grid visibility value for a Returns the grid color value for a grid linetype and row type.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Object)OdaX.AcGridLineType;
* (System.Object)OdaX.AcRowType;

### Нод SetGridVisibility

**Имя нода** = SetGridVisibility;

**Описание** = Specifies the grid visibility value for a grid linetype and row type.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)gridLineTypes;
* (System.Int32)rowTypes;
* (System.Object)bValue;

### Нод TableStyleOverrides

**Имя нода** = TableStyleOverrides;

**Описание** = Returns the overrides of a table style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;

### Нод ClearTableStyleOverrides

**Имя нода** = ClearTableStyleOverrides;

**Описание** = Removes the overrides of a table style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)flag;

### Нод GetCellType

**Имя нода** = GetCellType;

**Описание** = Returns the cell type of a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;

### Нод SetCellType

**Имя нода** = SetCellType;

**Описание** = Specifies the cell type of a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;
* (System.Object)OdaX.AcCellType;

### Нод GetCellExtents

**Имя нода** = GetCellExtents;

**Описание** = Returns whether a table cell is in the inner part of a table or on the outer edge.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;
* (System.Object)bOuterCell;

### Нод GetAttachmentPoint

**Имя нода** = GetAttachmentPoint;

**Описание** = Returns the attachment point of a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;

### Нод GetCellAlignment

**Имя нода** = GetCellAlignment;

**Описание** = Returns the alignment of a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;

### Нод SetCellAlignment

**Имя нода** = SetCellAlignment;

**Описание** = Specifies the cell alignment of a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;
* (System.Object)OdaX.AcCellAlignment;

### Нод GetCellBackgroundColorNone

**Имя нода** = GetCellBackgroundColorNone;

**Описание** = Returns whether the background color is set to None for a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;

### Нод SetCellBackgroundColorNone

**Имя нода** = SetCellBackgroundColorNone;

**Описание** = Specifies whether the background color is set to None for a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;
* (System.Object)bValue;

### Нод GetCellBackgroundColor

**Имя нода** = GetCellBackgroundColor;

**Описание** = Returns the background color for a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;

### Нод SetCellBackgroundColor

**Имя нода** = SetCellBackgroundColor;

**Описание** = Specifies the background color for a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;
* (System.Object)pColor;

### Нод GetCellContentColor

**Имя нода** = GetCellContentColor;

**Описание** = Returns the cell content color for a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;

### Нод SetCellContentColor

**Имя нода** = SetCellContentColor;

**Описание** = Specifies the cell content color for a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;
* (System.Object)pColor;

### Нод GetCellStyleOverrides

**Имя нода** = GetCellStyleOverrides;

**Описание** = Returns the overrides of a cell style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;

### Нод DeleteCellContent

**Имя нода** = DeleteCellContent;

**Описание** = Deletes all cell content for a row and coluumn.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;

### Нод GetRowType

**Имя нода** = GetRowType;

**Описание** = Returns the row type of a row.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;

### Нод GetText

**Имя нода** = GetText;

**Описание** = Returns the text of a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;

### Нод SetText

**Имя нода** = SetText;

**Описание** = Specifies the text of a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;
* (System.String)pStr;

### Нод GetCellTextStyle

**Имя нода** = GetCellTextStyle;

**Описание** = Returns the text style name of a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;

### Нод SetCellTextStyle

**Имя нода** = SetCellTextStyle;

**Описание** = Specifies the text style name of a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;
* (System.String)bstrName;

### Нод GetCellTextHeight

**Имя нода** = GetCellTextHeight;

**Описание** = Returns the text height of a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;

### Нод SetCellTextHeight

**Имя нода** = SetCellTextHeight;

**Описание** = Specifies the text height of a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;
* (System.Double)TextHeight;

### Нод GetTextRotation

**Имя нода** = GetTextRotation;

**Описание** = Returns the text rotation of a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;

### Нод SetTextRotation

**Имя нода** = SetTextRotation;

**Описание** = Specifies the text rotation of a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;
* (System.Object)OdaX.AcRotationAngle;

### Нод GetAutoScale

**Имя нода** = GetAutoScale;

**Описание** = Returns whether auto scaling is turned on for a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;

### Нод SetAutoScale

**Имя нода** = SetAutoScale;

**Описание** = Specifies whether auto scaling is turned on for a row nad column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;
* (System.Object)bValue;

### Нод GetBlockTableRecordId

**Имя нода** = GetBlockTableRecordId;

**Описание** = Returns the block table record ID associated with the block-type cell.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;

### Нод SetBlockTableRecordId

**Имя нода** = SetBlockTableRecordId;

**Описание** = Specifies the block table record ID associated with the block-type cell.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;
* (System.Object)blkId;
* (System.Object)bAutoFit;

### Нод GetBlockScale

**Имя нода** = GetBlockScale;

**Описание** = Returns the block scale value for a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;

### Нод SetBlockScale

**Имя нода** = SetBlockScale;

**Описание** = Specifies the block scale value for a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;
* (System.Double)blkScale;

### Нод GetBlockRotation

**Имя нода** = GetBlockRotation;

**Описание** = Returns the block rotation of a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;

### Нод SetBlockRotation

**Имя нода** = SetBlockRotation;

**Описание** = Specifies the block rotation of a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;
* (System.Double)blkRotation;

### Нод GetBlockAttributeValue

**Имя нода** = GetBlockAttributeValue;

**Описание** = Returns the attribute value of a block cell.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;
* (System.Object)attdefId;

### Нод SetBlockAttributeValue

**Имя нода** = SetBlockAttributeValue;

**Описание** = Specifies the attribute value of a block cell.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;
* (System.Object)attdefId;
* (System.String)bstrValue;

### Нод GetCellGridLineWeight

**Имя нода** = GetCellGridLineWeight;

**Описание** = Returns the grid lineweight value for the edge of a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;
* (System.Object)OdaX.AcCellEdgeMask;

### Нод SetCellGridLineWeight

**Имя нода** = SetCellGridLineWeight;

**Описание** = Specifies the grid lineweight value for the edges of a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;
* (System.Int32)edges;
* (System.Object)Lineweight;

### Нод GetCellGridColor

**Имя нода** = GetCellGridColor;

**Описание** = Returns the grid color value for the edge of a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;
* (System.Object)OdaX.AcCellEdgeMask;

### Нод SetCellGridColor

**Имя нода** = SetCellGridColor;

**Описание** = Specifies the grid color value for the edges of a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;
* (System.Int32)edges;
* (System.Object)pColor;

### Нод GetCellGridVisibility

**Имя нода** = GetCellGridVisibility;

**Описание** = Returns whether the grid is visible for the edge of a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;
* (System.Object)OdaX.AcCellEdgeMask;

### Нод SetCellGridVisibility

**Имя нода** = SetCellGridVisibility;

**Описание** = Specifies whether the grid is visible for the edges of a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;
* (System.Int32)edges;
* (System.Object)bValue;

### Нод InsertColumns

**Имя нода** = InsertColumns;

**Описание** = Adds columns to a table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)col;
* (System.Double)Width;
* (System.Int32)cols;

### Нод DeleteColumns

**Имя нода** = DeleteColumns;

**Описание** = Deletes columns from a table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)col;
* (System.Int32)cols;

### Нод InsertRows

**Имя нода** = InsertRows;

**Описание** = Adds rows to a table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Double)Height;
* (System.Int32)Rows;

### Нод DeleteRows

**Имя нода** = DeleteRows;

**Описание** = Deletes rows from a table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)Rows;

### Нод MergeCells

**Имя нода** = MergeCells;

**Описание** = Merges cells together into a single cell.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)minRow;
* (System.Int32)maxRow;
* (System.Int32)minCol;
* (System.Int32)maxCol;

### Нод UnmergeCells

**Имя нода** = UnmergeCells;

**Описание** = Unmerges cells.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)minRow;
* (System.Int32)maxRow;
* (System.Int32)minCol;
* (System.Int32)maxCol;

### Нод IsMergedCell

**Имя нода** = IsMergedCell;

**Описание** = Returns whether the specified cells are merged.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;
* (System.Int32)minRow;
* (System.Int32)maxRow;
* (System.Int32)minCol;
* (System.Int32)maxCol;

### Нод GetFieldId

**Имя нода** = GetFieldId;

**Описание** = Returns the field object ID that corresponds to a cell.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;

### Нод SetFieldId

**Имя нода** = SetFieldId;

**Описание** = Specifies the field object ID for a cell.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;
* (System.Object)fieldId;

### Нод GenerateLayout

**Имя нода** = GenerateLayout;

**Описание** = Generates the layout and content of a table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;

### Нод RecomputeTableBlock

**Имя нода** = RecomputeTableBlock;

**Описание** = Recomputes the referenced table block with changes made to the table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Object)bForceUpdate;

### Нод HitTest

**Имя нода** = HitTest;

**Описание** = Not implemented. Determines whether a vertex is on a table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Object)wpt;
* (System.Object)wviewVec;
* (System.Int32)resultRowIndex;
* (System.Int32)resultColumnIndex;

### Нод Select

**Имя нода** = Select;

**Описание** = Not implemented. Selects a cell in a table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Object)wpt;
* (System.Object)wvwVec;
* (System.Object)wvwxVec;
* (System.Double)wxaper;
* (System.Double)wyaper;
* (System.Object)allowOutside;
* (System.Int32)resultRowIndex;
* (System.Int32)resultColumnIndex;

### Нод SelectSubRegion

**Имя нода** = SelectSubRegion;

**Описание** = Not implemented. Selects a subregion in a table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Object)wpt1;
* (System.Object)wpt2;
* (System.Object)wvwVec;
* (System.Object)wvwxVec;
* (System.Object)OdaX.AcSelect;
* (System.Object)bIncludeCurrentSelection;
* (System.Int32)rowMin;
* (System.Int32)rowMax;
* (System.Int32)colMin;
* (System.Int32)colMax;

### Нод ReselectSubRegion

**Имя нода** = ReselectSubRegion;

**Описание** = Not implemented. Reselects a subregion in a table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;

### Нод GetSubSelection

**Имя нода** = GetSubSelection;

**Описание** = Not implemented. Returns a subregion in a table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)rowMin;
* (System.Int32)rowMax;
* (System.Int32)colMin;
* (System.Int32)colMax;

### Нод SetSubSelection

**Имя нода** = SetSubSelection;

**Описание** = Not implemented. Specifies a subregion in a table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)rowMin;
* (System.Int32)rowMax;
* (System.Int32)colMin;
* (System.Int32)colMax;

### Нод ClearSubSelection

**Имя нода** = ClearSubSelection;

**Описание** = Not implemented. Clears a subregion in a table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;

### Нод HasSubSelection

**Имя нода** = HasSubSelection;

**Описание** = Not implemented. Determines whether a table has a subselection set.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;

### Нод RegenerateTableSuppressed

**Имя нода** = RegenerateTableSuppressed;

**Описание** = Not implemented. Determines whether a table can be regenerated.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;

### Нод Set_RegenerateTableSuppressed

**Имя нода** = Set_RegenerateTableSuppressed;

**Описание** = Not implemented. Determines whether a table can be regenerated.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Object)bValue;

### Нод GetDataType

**Имя нода** = GetDataType;

**Описание** = Not implemented. Returns the row data type and unit type for the specified row type.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Object)OdaX.AcRowType;
* (System.Object)OdaX.AcValueDataType;
* (System.Object)OdaX.AcValueUnitType;

### Нод SetDataType

**Имя нода** = SetDataType;

**Описание** = Not implemented. Specifies the row data type and unit type for a row type.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)rowTypes;
* (System.Object)OdaX.AcValueDataType;
* (System.Object)OdaX.AcValueUnitType;

### Нод GetFormat

**Имя нода** = GetFormat;

**Описание** = Not implemented. Returns the format of a row type.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Object)OdaX.AcRowType;

### Нод SetFormat

**Имя нода** = SetFormat;

**Описание** = Not implemented. Specifies the format of a row type.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)rowTypes;

### Нод FormatValue

**Имя нода** = FormatValue;

**Описание** = Not implemented. Returns the formatted text string of a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;

### Нод GetCellDataType

**Имя нода** = GetCellDataType;

**Описание** = Not implemented. Returns the cell data type and unit type of a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;
* (System.Object)OdaX.AcValueDataType;
* (System.Object)OdaX.AcValueUnitType;

### Нод SetCellDataType

**Имя нода** = SetCellDataType;

**Описание** = Not implemented. Specifies the cell data type and unit type of a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;

### Нод GetCellFormat

**Имя нода** = GetCellFormat;

**Описание** = Not implemented. Returns the cell format for the specified row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;

### Нод SetCellFormat

**Имя нода** = SetCellFormat;

**Описание** = Not implemented. Specifies the cell format of a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;

### Нод GetCellValue

**Имя нода** = GetCellValue;

**Описание** = Not implemented. Returns the cell value of a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;

### Нод SetCellValue

**Имя нода** = SetCellValue;

**Описание** = Not implemented. Specifies the cell value of a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;

### Нод SetCellValueFromText

**Имя нода** = SetCellValueFromText;

**Описание** = Not implemented. Specifies the cell value from existing text in a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;
* (System.String)val;
* (System.Object)OdaX.AcParseOption;

### Нод ResetCellValue

**Имя нода** = ResetCellValue;

**Описание** = Not implemented. Resets the cell value of a row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;

### Нод IsEmpty

**Имя нода** = IsEmpty;

**Описание** = Checks if the content of the specified cell is empty.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;

### Нод CreateContent

**Имя нода** = CreateContent;

**Описание** = Creates new content in a cell;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Int32)nIndex;

### Нод MoveContent

**Имя нода** = MoveContent;

**Описание** = Moves a content in a cell from one position to another position within the cell;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Int32)nFromIndex;
* (System.Int32)nToIndex;

### Нод DeleteContent

**Имя нода** = DeleteContent;

**Описание** = Deletes a content from a cell;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;

### Нод GetValue

**Имя нода** = GetValue;

**Описание** = Gets the cell value for the specified row and column and nContent.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Int32)nContent;

### Нод SetValue

**Имя нода** = SetValue;

**Описание** = Sets the cell value by parsing the text for the specified row and column and nContent.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Int32)nContent;
* (System.Object)acValue;

### Нод SetValueFromText

**Имя нода** = SetValueFromText;

**Описание** = Sets the value of the content at the specified content index.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Int32)nContent;
* (System.String)szText;
* (System.Object)OdaX.AcParseOption;

### Нод GetDataFormat

**Имя нода** = GetDataFormat;

**Описание** = Gets the cell format for the specified row and column and nContent.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Int32)nContent;

### Нод SetDataFormat

**Имя нода** = SetDataFormat;

**Описание** = Sets the cell format for the specified row and column and nContent.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Int32)nContent;
* (System.String)szFormat;

### Нод GetTextString

**Имя нода** = GetTextString;

**Описание** = Gets the text value value for the specified row and column  and nContent.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Int32)nContent;

### Нод SetTextString

**Имя нода** = SetTextString;

**Описание** = Sets the text value value for the specified row and column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Int32)nContent;
* (System.String)Text;

### Нод GetFieldId2

**Имя нода** = GetFieldId2;

**Описание** = Returns the field object id associated to the specifed cell  and nContent.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Int32)nContent;

### Нод SetFieldId2

**Имя нода** = SetFieldId2;

**Описание** = Sets  the field object id associated to the specifed cell  and nContent.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Int32)nContent;
* (System.Object)acDbObjectId;
* (System.Object)OdaX.AcCellOption;

### Нод GetBlockTableRecordId2

**Имя нода** = GetBlockTableRecordId2;

**Описание** = Gets the block table record id associated to the block-type cell  and nContent.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Int32)nContent;

### Нод SetBlockTableRecordId2

**Имя нода** = SetBlockTableRecordId2;

**Описание** = Sets the block table record id associated to the block-type cell  and nContent.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Int32)nContent;
* (System.Object)blkId;
* (System.Object)autoFit;

### Нод GetBlockAttributeValue2

**Имя нода** = GetBlockAttributeValue2;

**Описание** = Returns the attribute value from the Specified block cell for the attribute definition object contained in the block  and nContent.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Int32)nContent;
* (System.Object)blkId;

### Нод SetBlockAttributeValue2

**Имя нода** = SetBlockAttributeValue2;

**Описание** = Sets the attribute value from the Specified block cell for the attribute definition object contained in the block  and nContent.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Int32)nContent;
* (System.Object)blkId;
* (System.String)Value;

### Нод GetCustomData

**Имя нода** = GetCustomData;

**Описание** = Gets the custom data value set in cell, row, or column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.String)szKey;
* (System.Object)pData;

### Нод SetCustomData

**Имя нода** = SetCustomData;

**Описание** = Sets the custom data value set in cell, row, or column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.String)szKey;
* (System.Object)data;

### Нод GetCellStyle

**Имя нода** = GetCellStyle;

**Описание** = Gets the cell style of cell, row, or column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;

### Нод SetCellStyle

**Имя нода** = SetCellStyle;

**Описание** = Sets the the cell style of cell, row, or column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.String)szCellStyle;

### Нод GetContentColor2

**Имя нода** = GetContentColor2;

**Описание** = Returns the true color value for the specified row type  and nContent.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Int32)nContent;

### Нод SetContentColor2

**Имя нода** = SetContentColor2;

**Описание** = Sets the true color value for the specified row type  and nContent.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Int32)nContent;
* (System.Object)pColor;

### Нод GetDataType2

**Имя нода** = GetDataType2;

**Описание** = Gets the row data type and unit type for the specified row type  and nContent.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Int32)nContent;
* (System.Object)OdaX.AcValueDataType;
* (System.Object)OdaX.AcValueUnitType;

### Нод SetDataType2

**Имя нода** = SetDataType2;

**Описание** = Sets the row data type and unit type for the specified row type  and nContent.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Int32)nContent;
* (System.Object)OdaX.AcValueDataType;
* (System.Object)OdaX.AcValueUnitType;

### Нод GetTextStyle2

**Имя нода** = GetTextStyle2;

**Описание** = Gets the text style name for the specified row and column  and nContent.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Int32)nContent;

### Нод SetTextStyle2

**Имя нода** = SetTextStyle2;

**Описание** = Sets the text style name for the specified row and column  and nContent.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Int32)nContent;
* (System.String)bstrStyleName;

### Нод GetTextHeight2

**Имя нода** = GetTextHeight2;

**Описание** = Returns the text height for the specified row and column  and nContent.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Int32)nContent;

### Нод SetTextHeight2

**Имя нода** = SetTextHeight2;

**Описание** = Gets the text height for the specified row and column  and nContent.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Int32)nContent;
* (System.Double)Height;

### Нод GetRotation

**Имя нода** = GetRotation;

**Описание** = Gets the rotation angle of the content at the specified content index.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Int32)nContent;

### Нод SetRotation

**Имя нода** = SetRotation;

**Описание** = Sets the rotation angle of the content at the specified content index.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Int32)nContent;
* (System.Double)Value;

### Нод GetAutoScale2

**Имя нода** = GetAutoScale2;

**Описание** = Returns the auto scale flag value for the specified row and column  and nContent.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Int32)nContent;

### Нод SetAutoScale2

**Имя нода** = SetAutoScale2;

**Описание** = Sets the auto scale flag value for the specified row and column  and nContent.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Int32)nContent;
* (System.Object)bAutoFit;

### Нод GetScale

**Имя нода** = GetScale;

**Описание** = Gets the scale of the content at the specified content index.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Int32)nContent;

### Нод SetScale

**Имя нода** = SetScale;

**Описание** = Sets the scale of the content at the specified content index.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Int32)nContent;
* (System.Double)scale;

### Нод RemoveAllOverrides

**Имя нода** = RemoveAllOverrides;

**Описание** = Removes all the overrides in cell, row, or column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;

### Нод GetGridLineWeight2

**Имя нода** = GetGridLineWeight2;

**Описание** = s)   and nContent.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Object)OdaX.AcGridLineType;

### Нод SetGridLineWeight2

**Имя нода** = SetGridLineWeight2;

**Описание** = s)   and nContent.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Object)OdaX.AcGridLineType;
* (System.Object)Lineweight;

### Нод GetGridLinetype

**Имя нода** = GetGridLinetype;

**Описание** = Gets the grid line type of cell, row, or column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Object)OdaX.AcGridLineType;

### Нод SetGridLinetype

**Имя нода** = SetGridLinetype;

**Описание** = Sets the grid line type of cell, row, or column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Object)OdaX.AcGridLineType;
* (System.Object)idLinetype;

### Нод GetGridColor2

**Имя нода** = GetGridColor2;

**Описание** = Returns the gridColor value for the specified gridLineType and row type  and nContent.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Object)OdaX.AcGridLineType;

### Нод SetGridColor2

**Имя нода** = SetGridColor2;

**Описание** = Sets the gridColor value for the specified gridLineType and row type  and nContent.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Object)OdaX.AcGridLineType;
* (System.Object)pColor;

### Нод GetGridVisibility2

**Имя нода** = GetGridVisibility2;

**Описание** = Returns the gridVisibility value for the specified gridLineType and row type.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Object)OdaX.AcGridLineType;

### Нод SetGridVisibility2

**Имя нода** = SetGridVisibility2;

**Описание** = Sets the gridVisibility value for the specified gridLineType and row type.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Object)OdaX.AcGridLineType;
* (System.Object)bVisible;

### Нод GetGridDoubleLineSpacing

**Имя нода** = GetGridDoubleLineSpacing;

**Описание** = Gets the grid double line spacing from cell, row, or column;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Object)OdaX.AcGridLineType;

### Нод SetGridDoubleLineSpacing

**Имя нода** = SetGridDoubleLineSpacing;

**Описание** = Sets the grid double line spacing in cell, row, or column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Object)OdaX.AcGridLineType;
* (System.Double)fSpacing;

### Нод Set_EnableBreak

**Имя нода** = Set_EnableBreak;

**Описание** = Enables or disables table breaking;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Object)rhs;

### Нод GetBreakHeight

**Имя нода** = GetBreakHeight;

**Описание** = Returns the break height of the specified table when table breaking is enabled.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nIndex;

### Нод SetBreakHeight

**Имя нода** = SetBreakHeight;

**Описание** = Sets the break height of the specified table when table breaking is enabled.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nIndex;
* (System.Double)Height;

### Нод GetContentType

**Имя нода** = GetContentType;

**Описание** = Gets the content type of the content at the specified content index.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;

### Нод GetMargin

**Имя нода** = GetMargin;

**Описание** = Gets the margin of cell, row, or column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Object)OdaX.AcCellMargin;

### Нод SetMargin

**Имя нода** = SetMargin;

**Описание** = Sets the margin of cell, row, or column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Object)OdaX.AcCellMargin;
* (System.Double)fMargin;

### Нод GetContentLayout

**Имя нода** = GetContentLayout;

**Описание** = Gets the content layout of the cell.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;

### Нод SetContentLayout

**Имя нода** = SetContentLayout;

**Описание** = Sets the content layout of the cell.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)row;
* (System.Int32)col;
* (System.Object)OdaX.AcCellContentLayout;

### Нод GetOverride

**Имя нода** = GetOverride;

**Описание** = Gets the override in cell, row, column, or content.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Int32)nContent;

### Нод SetOverride

**Имя нода** = SetOverride;

**Описание** = Sets the override in cell, row, column, or content.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Int32)nContent;
* (System.Object)OdaX.AcCellProperty;

### Нод GetGridLineStyle

**Имя нода** = GetGridLineStyle;

**Описание** = Gets the grid line style of cell, row, or column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Object)OdaX.AcGridLineType;

### Нод SetGridLineStyle

**Имя нода** = SetGridLineStyle;

**Описание** = Sets the grid line style of cell, row, or column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Object)OdaX.AcGridLineType;
* (System.Object)OdaX.AcGridLineStyle;

### Нод InsertRowsAndInherit

**Имя нода** = InsertRowsAndInherit;

**Описание** = Inserts one or more rows at the specified index and inherits the row properties from specified row.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nIndex;
* (System.Int32)nInheritFrom;
* (System.Int32)nNumRows;

### Нод InsertColumnsAndInherit

**Имя нода** = InsertColumnsAndInherit;

**Описание** = Inserts one or more columns at the specified index and inherits the column properties from specified column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)col;
* (System.Int32)nInheritFrom;
* (System.Int32)nNumCols;

### Нод GetHasFormula

**Имя нода** = GetHasFormula;

**Описание** = Returns true if the content at the specified index is a formula.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Int32)nContent;

### Нод GetFormula

**Имя нода** = GetFormula;

**Описание** = Gets the formula if the content at the specified content index has a formula.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Int32)nContent;

### Нод SetFormula

**Имя нода** = SetFormula;

**Описание** = Sets the formula at the specified content index.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Int32)nContent;
* (System.String)pszFormula;

### Нод IsContentEditable

**Имя нода** = IsContentEditable;

**Описание** = Checks if the content of the specified cell can be modified.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;

### Нод IsFormatEditable

**Имя нода** = IsFormatEditable;

**Описание** = Checks if the format of the specified cell can be modified.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;

### Нод GetCellState

**Имя нода** = GetCellState;

**Описание** = Gets the cell state.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;

### Нод SetCellState

**Имя нода** = SetCellState;

**Описание** = Sets the cell state.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Object)OdaX.AcCellState;

### Нод EnableMergeAll

**Имя нода** = EnableMergeAll;

**Описание** = Enables or disables the merge all flag in row or column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.Object)bEnable;

### Нод IsMergeAllEnabled

**Имя нода** = IsMergeAllEnabled;

**Описание** = Returns whether merge all flag is enabled or not in row or column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;

### Нод BreaksEnabled

**Имя нода** = BreaksEnabled;

**Описание** = Determines whether table breaking is in use. When enabled, the table will break automatically at the current specified height.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;

### Нод Set_BreaksEnabled

**Имя нода** = Set_BreaksEnabled;

**Описание** = Determines whether table breaking is in use. When enabled, the table will break automatically at the current specified height.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Object)bEnabled;

### Нод RepeatTopLabels

**Имя нода** = RepeatTopLabels;

**Описание** = Determines whether the first set of label rows will be repeated at the top of each broken table part.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;

### Нод Set_RepeatTopLabels

**Имя нода** = Set_RepeatTopLabels;

**Описание** = Determines whether the first set of label rows will be repeated at the top of each broken table part.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Object)bEnabled;

### Нод RepeatBottomLabels

**Имя нода** = RepeatBottomLabels;

**Описание** = Determines whether the bottom set of label rows is repeated at the bottom of each broken table part.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;

### Нод Set_RepeatBottomLabels

**Имя нода** = Set_RepeatBottomLabels;

**Описание** = Determines whether the bottom set of label rows is repeated at the bottom of each broken table part.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Object)bEnabled;

### Нод TableBreakFlowDirection

**Имя нода** = TableBreakFlowDirection;

**Описание** = Determines the direction that parts of the table flow.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;

### Нод Set_TableBreakFlowDirection

**Имя нода** = Set_TableBreakFlowDirection;

**Описание** = Determines the direction that parts of the table flow.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Object)OdaX.AcTableFlowDirection;

### Нод AllowManualPositions

**Имя нода** = AllowManualPositions;

**Описание** = When enabled, each table part can be moved outside of the boundary. When not enabled, the table parts are contained within a rectangular boundary.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;

### Нод Set_AllowManualPositions

**Имя нода** = Set_AllowManualPositions;

**Описание** = When enabled, each table part can be moved outside of the boundary. When not enabled, the table parts are contained within a rectangular boundary.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Object)bEnabled;

### Нод AllowManualHeights

**Имя нода** = AllowManualHeights;

**Описание** = Allows the heights of broken table parts to have their own breaking height. When set to No, all table parts will break at the height of the initial table part.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;

### Нод Set_AllowManualHeights

**Имя нода** = Set_AllowManualHeights;

**Описание** = Allows the heights of broken table parts to have their own breaking height. When set to No, all table parts will break at the height of the initial table part.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Object)bEnabled;

### Нод TableBreakHeight

**Имя нода** = TableBreakHeight;

**Описание** = Sets the breaking height for the initial table part and any other table parts that do not have manual heights set.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;

### Нод Set_TableBreakHeight

**Имя нода** = Set_TableBreakHeight;

**Описание** = Sets the breaking height for the initial table part and any other table parts that do not have manual heights set.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Double)pHeight;

### Нод BreakSpacing

**Имя нода** = BreakSpacing;

**Описание** = Controls the spacing between the broken table parts. Depending on the break direction this will be a horizontal or vertical spacing.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;

### Нод Set_BreakSpacing

**Имя нода** = Set_BreakSpacing;

**Описание** = Controls the spacing between the broken table parts. Depending on the break direction this will be a horizontal or vertical spacing.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Double)pSpacing;

### Нод GetColumnName

**Имя нода** = GetColumnName;

**Описание** = Gets the columns name.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nIndex;

### Нод SetColumnName

**Имя нода** = SetColumnName;

**Описание** = Sets the columns name.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nIndex;
* (System.String)Name;

### Нод SetToolTip

**Имя нода** = SetToolTip;

**Описание** = Sets the tooltip string for cell, row, or column.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadTable;

**Входные данные**:

* (System.Object)AcadTable;
* (System.Int32)nRow;
* (System.Int32)nCol;
* (System.String)tip;

## Группа NVP_nanoCAD_COM.OdaX.AcadSweptSurface

### Нод _AcadSweptSurface_Constructor

**Имя нода** = _AcadSweptSurface_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSweptSurface;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadSweptSurface_ConstructorCast

**Имя нода** = _AcadSweptSurface_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSweptSurface;

**Входные данные**:

* (System.Object)dynamic;

### Нод ProfileRotation

**Имя нода** = ProfileRotation;

**Описание** = Specifies or returns the rotation of the profile used for a swept surface.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSweptSurface;

**Входные данные**:

* (System.Object)AcadSweptSurface;

### Нод Set_ProfileRotation

**Имя нода** = Set_ProfileRotation;

**Описание** = Specifies or returns the rotation of the profile used for a swept surface.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSweptSurface;

**Входные данные**:

* (System.Object)AcadSweptSurface;
* (System.Object)profileRotationAngle;

### Нод Bank

**Имя нода** = Bank;

**Описание** = Specifies or returns whether the profile of a swept surface is banked along the path.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSweptSurface;

**Входные данные**:

* (System.Object)AcadSweptSurface;

### Нод Set_Bank

**Имя нода** = Set_Bank;

**Описание** = Specifies or returns whether the profile of a swept surface is banked along the path.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSweptSurface;

**Входные данные**:

* (System.Object)AcadSweptSurface;
* (System.Object)bBank;

### Нод Twist

**Имя нода** = Twist;

**Описание** = Specifies or returns the rotation value applied along the path of a swept surface.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSweptSurface;

**Входные данные**:

* (System.Object)AcadSweptSurface;

### Нод Set_Twist

**Имя нода** = Set_Twist;

**Описание** = Specifies or returns the rotation value applied along the path of a swept surface.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSweptSurface;

**Входные данные**:

* (System.Object)AcadSweptSurface;
* (System.Object)TwistAngle;

### Нод scale

**Имя нода** = scale;

**Описание** = Specifies or returns the scale factor of a swept surface.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSweptSurface;

**Входные данные**:

* (System.Object)AcadSweptSurface;

### Нод Set_scale

**Имя нода** = Set_scale;

**Описание** = Specifies or returns the scale factor of a swept surface.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSweptSurface;

**Входные данные**:

* (System.Object)AcadSweptSurface;
* (System.Double)scale;

### Нод Length

**Имя нода** = Length;

**Описание** = Not implemented. Returns the length of the path for a swept surface.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSweptSurface;

**Входные данные**:

* (System.Object)AcadSweptSurface;

## Группа NVP_nanoCAD_COM.OdaX.AcadSurface

### Нод _AcadSurface_Constructor

**Имя нода** = _AcadSurface_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSurface;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadSurface_ConstructorCast

**Имя нода** = _AcadSurface_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSurface;

**Входные данные**:

* (System.Object)dynamic;

### Нод SurfaceType

**Имя нода** = SurfaceType;

**Описание** = Returns the type of a surface.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSurface;

**Входные данные**:

* (System.Object)AcadSurface;

### Нод UIsolineDensity

**Имя нода** = UIsolineDensity;

**Описание** = Specifies or returns the number of U isolines per surface that display.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSurface;

**Входные данные**:

* (System.Object)AcadSurface;

### Нод Set_UIsolineDensity

**Имя нода** = Set_UIsolineDensity;

**Описание** = Specifies or returns the number of U isolines per surface that display.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSurface;

**Входные данные**:

* (System.Object)AcadSurface;
* (System.Object)density;

### Нод VIsolineDensity

**Имя нода** = VIsolineDensity;

**Описание** = Specifies or returns the number of V isolines per surface that display.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSurface;

**Входные данные**:

* (System.Object)AcadSurface;

### Нод Set_VIsolineDensity

**Имя нода** = Set_VIsolineDensity;

**Описание** = Specifies or returns the number of V isolines per surface that display.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSurface;

**Входные данные**:

* (System.Object)AcadSurface;
* (System.Object)density;

## Группа NVP_nanoCAD_COM.OdaX.AcadSummaryInfo

### Нод _AcadSummaryInfo_Constructor

**Имя нода** = _AcadSummaryInfo_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSummaryInfo;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadSummaryInfo_ConstructorCast

**Имя нода** = _AcadSummaryInfo_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSummaryInfo;

**Входные данные**:

* (System.Object)dynamic;

### Нод Author

**Имя нода** = Author;

**Описание** = Specifies or returns the author value of the summary information.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSummaryInfo;

**Входные данные**:

* (System.Object)AcadSummaryInfo;

### Нод Set_Author

**Имя нода** = Set_Author;

**Описание** = Specifies or returns the author value of the summary information.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSummaryInfo;

**Входные данные**:

* (System.Object)AcadSummaryInfo;
* (System.String)pAuthor;

### Нод Comments

**Имя нода** = Comments;

**Описание** = Specifies or returns the comments value of the summary information.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSummaryInfo;

**Входные данные**:

* (System.Object)AcadSummaryInfo;

### Нод Set_Comments

**Имя нода** = Set_Comments;

**Описание** = Specifies or returns the comments value of the summary information.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSummaryInfo;

**Входные данные**:

* (System.Object)AcadSummaryInfo;
* (System.String)pComments;

### Нод HyperlinkBase

**Имя нода** = HyperlinkBase;

**Описание** = Specifies or returns the hyperlink base value of the summary information.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSummaryInfo;

**Входные данные**:

* (System.Object)AcadSummaryInfo;

### Нод Set_HyperlinkBase

**Имя нода** = Set_HyperlinkBase;

**Описание** = Specifies or returns the hyperlink base value of the summary information.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSummaryInfo;

**Входные данные**:

* (System.Object)AcadSummaryInfo;
* (System.String)pHyperlinkBase;

### Нод Keywords

**Имя нода** = Keywords;

**Описание** = Specifies or returns the keywords value of the summary information.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSummaryInfo;

**Входные данные**:

* (System.Object)AcadSummaryInfo;

### Нод Set_Keywords

**Имя нода** = Set_Keywords;

**Описание** = Specifies or returns the keywords value of the summary information.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSummaryInfo;

**Входные данные**:

* (System.Object)AcadSummaryInfo;
* (System.String)pKeywords;

### Нод LastSavedBy

**Имя нода** = LastSavedBy;

**Описание** = Specifies or returns the login name of the user who last saved the drawing.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSummaryInfo;

**Входные данные**:

* (System.Object)AcadSummaryInfo;

### Нод Set_LastSavedBy

**Имя нода** = Set_LastSavedBy;

**Описание** = Specifies or returns the login name of the user who last saved the drawing.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSummaryInfo;

**Входные данные**:

* (System.Object)AcadSummaryInfo;
* (System.String)pLastSavedBy;

### Нод RevisionNumber

**Имя нода** = RevisionNumber;

**Описание** = Specifies or returns the revision number value of the summary information.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSummaryInfo;

**Входные данные**:

* (System.Object)AcadSummaryInfo;

### Нод Set_RevisionNumber

**Имя нода** = Set_RevisionNumber;

**Описание** = Specifies or returns the revision number value of the summary information.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSummaryInfo;

**Входные данные**:

* (System.Object)AcadSummaryInfo;
* (System.String)pRevisionNumber;

### Нод Subject

**Имя нода** = Subject;

**Описание** = Specifies or returns the subject value of the summary information.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSummaryInfo;

**Входные данные**:

* (System.Object)AcadSummaryInfo;

### Нод Set_Subject

**Имя нода** = Set_Subject;

**Описание** = Specifies or returns the subject value of the summary information.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSummaryInfo;

**Входные данные**:

* (System.Object)AcadSummaryInfo;
* (System.String)pSubject;

### Нод Title

**Имя нода** = Title;

**Описание** = Specifies or returns the title value of the summary information.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSummaryInfo;

**Входные данные**:

* (System.Object)AcadSummaryInfo;

### Нод Set_Title

**Имя нода** = Set_Title;

**Описание** = Specifies or returns the title value of the summary information.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSummaryInfo;

**Входные данные**:

* (System.Object)AcadSummaryInfo;
* (System.String)pTitle;

### Нод NumCustomInfo

**Имя нода** = NumCustomInfo;

**Описание** = Returns the number of custom information items that have been defined for the summary information.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSummaryInfo;

**Входные данные**:

* (System.Object)AcadSummaryInfo;

### Нод GetCustomByIndex

**Имя нода** = GetCustomByIndex;

**Описание** = Using an index, returns the key and value of custom information that is defined for the summary information.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSummaryInfo;

**Входные данные**:

* (System.Object)AcadSummaryInfo;
* (System.Int32)Index;
* (System.String)pKey;
* (System.String)pValue;

### Нод GetCustomByKey

**Имя нода** = GetCustomByKey;

**Описание** = Using a key, returns the key and value of custom information that is defined for the summary information.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSummaryInfo;

**Входные данные**:

* (System.Object)AcadSummaryInfo;
* (System.String)key;
* (System.String)pValue;

### Нод SetCustomByIndex

**Имя нода** = SetCustomByIndex;

**Описание** = Using an index, specifies the key and value of custom information defined for the summary information.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSummaryInfo;

**Входные данные**:

* (System.Object)AcadSummaryInfo;
* (System.Int32)Index;
* (System.String)key;
* (System.String)Value;

### Нод SetCustomByKey

**Имя нода** = SetCustomByKey;

**Описание** = Using a key, specifies the key and value of custom information defined for the summary information.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSummaryInfo;

**Входные данные**:

* (System.Object)AcadSummaryInfo;
* (System.String)key;
* (System.String)Value;

### Нод AddCustomInfo

**Имя нода** = AddCustomInfo;

**Описание** = name) and value to the summary information.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSummaryInfo;

**Входные данные**:

* (System.Object)AcadSummaryInfo;
* (System.String)key;
* (System.String)Value;

### Нод RemoveCustomByIndex

**Имя нода** = RemoveCustomByIndex;

**Описание** = Removes custom information using its index.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSummaryInfo;

**Входные данные**:

* (System.Object)AcadSummaryInfo;
* (System.Int32)Index;

### Нод RemoveCustomByKey

**Имя нода** = RemoveCustomByKey;

**Описание** = Removes custom information using its key.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSummaryInfo;

**Входные данные**:

* (System.Object)AcadSummaryInfo;
* (System.String)key;

### Нод GetAll_CustomInfo

**Имя нода** = GetAll_CustomInfo;

**Описание** = Возвращает все пользовательские свойства чертежа;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSummaryInfo;

**Входные данные**:

* (System.Object)AcadSummaryInfo;

### Нод Add_GroupOfCustomInfo

**Имя нода** = Add_GroupOfCustomInfo;

**Описание** = Добавляет новые пользовательские свойства. Если Перезапись = true, то при наличии одноименных свойств их значения будут перезаписаны на новые, в противном случае будут оставлены, как есть;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSummaryInfo;

**Входные данные**:

* (System.Object)AcadSummaryInfo;
* (System.Collections.Generic.Dictionary`2[System.String,System.String])Новые свойства;
* (System.Boolean)Перезапись;

## Группа NVP_nanoCAD_COM.OdaX.AcadSubEntSolidVertex

### Нод _AcadSubEntSolidVertex_Constructor

**Имя нода** = _AcadSubEntSolidVertex_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSubEntSolidVertex;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadSubEntSolidVertex_ConstructorCast

**Имя нода** = _AcadSubEntSolidVertex_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSubEntSolidVertex;

**Входные данные**:

* (System.Object)dynamic;

## Группа NVP_nanoCAD_COM.OdaX.AcadSubEntSolidFace

### Нод _AcadSubEntSolidFace_Constructor

**Имя нода** = _AcadSubEntSolidFace_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSubEntSolidFace;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadSubEntSolidFace_ConstructorCast

**Имя нода** = _AcadSubEntSolidFace_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSubEntSolidFace;

**Входные данные**:

* (System.Object)dynamic;

### Нод Material

**Имя нода** = Material;

**Описание** = Specifies the material;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSubEntSolidFace;

**Входные данные**:

* (System.Object)AcadSubEntSolidFace;

### Нод Set_Material

**Имя нода** = Set_Material;

**Описание** = Specifies the material;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSubEntSolidFace;

**Входные данные**:

* (System.Object)AcadSubEntSolidFace;
* (System.String)Material;

## Группа NVP_nanoCAD_COM.OdaX.AcadSubEntSolidEdge

### Нод _AcadSubEntSolidEdge_Constructor

**Имя нода** = _AcadSubEntSolidEdge_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSubEntSolidEdge;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadSubEntSolidEdge_ConstructorCast

**Имя нода** = _AcadSubEntSolidEdge_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSubEntSolidEdge;

**Входные данные**:

* (System.Object)dynamic;

## Группа NVP_nanoCAD_COM.OdaX.AcadSubEntity

### Нод _AcadSubEntity_Constructor

**Имя нода** = _AcadSubEntity_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSubEntity;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadSubEntity_ConstructorCast

**Имя нода** = _AcadSubEntity_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSubEntity;

**Входные данные**:

* (System.Object)dynamic;

### Нод OnModified

**Имя нода** = OnModified;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSubEntity;

**Входные данные**:

* (System.Object)AcadSubEntity;

### Нод ObjectName

**Имя нода** = ObjectName;

**Описание** = Gets the AutoCAD class name of the object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSubEntity;

**Входные данные**:

* (System.Object)AcadSubEntity;

### Нод color

**Имя нода** = color;

**Описание** = Returns the true color of the object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSubEntity;

**Входные данные**:

* (System.Object)AcadSubEntity;

### Нод Set_color

**Имя нода** = Set_color;

**Описание** = Returns the true color of the object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSubEntity;

**Входные данные**:

* (System.Object)AcadSubEntity;
* (System.Object)pColor;

### Нод Layer

**Имя нода** = Layer;

**Описание** = Specifies the current layer of the object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSubEntity;

**Входные данные**:

* (System.Object)AcadSubEntity;

### Нод Linetype

**Имя нода** = Linetype;

**Описание** = Specifies the current linetype of the object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSubEntity;

**Входные данные**:

* (System.Object)AcadSubEntity;

### Нод LinetypeScale

**Имя нода** = LinetypeScale;

**Описание** = Specifies the linetype scale factor of the object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSubEntity;

**Входные данные**:

* (System.Object)AcadSubEntity;

### Нод PlotStyleName

**Имя нода** = PlotStyleName;

**Описание** = Specifies the plotstyle name for the object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSubEntity;

**Входные данные**:

* (System.Object)AcadSubEntity;

### Нод Lineweight

**Имя нода** = Lineweight;

**Описание** = Specifies the lineweight for the object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSubEntity;

**Входные данные**:

* (System.Object)AcadSubEntity;

### Нод Hyperlinks

**Имя нода** = Hyperlinks;

**Описание** = if one is specified);

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSubEntity;

**Входные данные**:

* (System.Object)AcadSubEntity;

## Группа NVP_nanoCAD_COM.OdaX.AcadSpline

### Нод _AcadSpline_Constructor

**Имя нода** = _AcadSpline_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSpline;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadSpline_ConstructorCast

**Имя нода** = _AcadSpline_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSpline;

**Входные данные**:

* (System.Object)dynamic;

### Нод NumberOfControlPoints

**Имя нода** = NumberOfControlPoints;

**Описание** = Specifies or returns the number of control points defined for a spline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSpline;

**Входные данные**:

* (System.Object)AcadSpline;

### Нод ControlPoints

**Имя нода** = ControlPoints;

**Описание** = Specifies or returns the number of control points defined for a spline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSpline;

**Входные данные**:

* (System.Object)AcadSpline;

### Нод Set_ControlPoints

**Имя нода** = Set_ControlPoints;

**Описание** = Specifies or returns the number of control points defined for a spline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSpline;

**Входные данные**:

* (System.Object)AcadSpline;
* (System.Object)controlPoint;

### Нод NumberOfFitPoints

**Имя нода** = NumberOfFitPoints;

**Описание** = Specifies or returns the current control point of a spline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSpline;

**Входные данные**:

* (System.Object)AcadSpline;

### Нод FitPoints

**Имя нода** = FitPoints;

**Описание** = Specifies or returns the current fit points of a spline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSpline;

**Входные данные**:

* (System.Object)AcadSpline;

### Нод Set_FitPoints

**Имя нода** = Set_FitPoints;

**Описание** = Specifies or returns the current fit points of a spline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSpline;

**Входные данные**:

* (System.Object)AcadSpline;
* (System.Object)fitPoint;

### Нод Degree

**Имя нода** = Degree;

**Описание** = Returns the degree of a spline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSpline;

**Входные данные**:

* (System.Object)AcadSpline;

### Нод Closed

**Имя нода** = Closed;

**Описание** = Returns whether a spline is closed.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSpline;

**Входные данные**:

* (System.Object)AcadSpline;

### Нод IsPlanar

**Имя нода** = IsPlanar;

**Описание** = Returns whether a spline is planar.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSpline;

**Входные данные**:

* (System.Object)AcadSpline;

### Нод IsRational

**Имя нода** = IsRational;

**Описание** = Returns whether a spline is rational.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSpline;

**Входные данные**:

* (System.Object)AcadSpline;

### Нод IsPeriodic

**Имя нода** = IsPeriodic;

**Описание** = Returns whether a spline is periodic.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSpline;

**Входные данные**:

* (System.Object)AcadSpline;

### Нод StartTangent

**Имя нода** = StartTangent;

**Описание** = Specifies or returns the x, y, z coordinate for the start tangent of a spline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSpline;

**Входные данные**:

* (System.Object)AcadSpline;

### Нод Set_StartTangent

**Имя нода** = Set_StartTangent;

**Описание** = Specifies or returns the x, y, z coordinate for the start tangent of a spline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSpline;

**Входные данные**:

* (System.Object)AcadSpline;
* (System.Object)StartTangent;

### Нод EndTangent

**Имя нода** = EndTangent;

**Описание** = Specifies or returns the x, y, z coordinate for the end tangent of a spline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSpline;

**Входные данные**:

* (System.Object)AcadSpline;

### Нод Set_EndTangent

**Имя нода** = Set_EndTangent;

**Описание** = Specifies or returns the x, y, z coordinate for the end tangent of a spline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSpline;

**Входные данные**:

* (System.Object)AcadSpline;
* (System.Object)EndTangent;

### Нод FitTolerance

**Имя нода** = FitTolerance;

**Описание** = Specifies or returns the fit tolerance of a spline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSpline;

**Входные данные**:

* (System.Object)AcadSpline;

### Нод Set_FitTolerance

**Имя нода** = Set_FitTolerance;

**Описание** = Specifies or returns the fit tolerance of a spline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSpline;

**Входные данные**:

* (System.Object)AcadSpline;
* (System.Double)fitTol;

### Нод Area

**Имя нода** = Area;

**Описание** = Not implemented. Returns the area of a spline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSpline;

**Входные данные**:

* (System.Object)AcadSpline;

### Нод SetControlPoint

**Имя нода** = SetControlPoint;

**Описание** = Specifies the coordinates for a control point of a spline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSpline;

**Входные данные**:

* (System.Object)AcadSpline;
* (System.Int32)Index;
* (System.Object)controlPoint;

### Нод GetControlPoint

**Имя нода** = GetControlPoint;

**Описание** = Returns the coordinates for a control point of a spline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSpline;

**Входные данные**:

* (System.Object)AcadSpline;
* (System.Int32)Index;

### Нод SetFitPoint

**Имя нода** = SetFitPoint;

**Описание** = Specifies the fit point of a spline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSpline;

**Входные данные**:

* (System.Object)AcadSpline;
* (System.Int32)Index;
* (System.Object)fitPoint;

### Нод GetFitPoint

**Имя нода** = GetFitPoint;

**Описание** = Returns the fit point of a spline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSpline;

**Входные данные**:

* (System.Object)AcadSpline;
* (System.Int32)Index;

### Нод SetWeight

**Имя нода** = SetWeight;

**Описание** = Specifies the weight of a spline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSpline;

**Входные данные**:

* (System.Object)AcadSpline;
* (System.Int32)Index;
* (System.Double)weight;

### Нод GetWeight

**Имя нода** = GetWeight;

**Описание** = Returns the weight of a spline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSpline;

**Входные данные**:

* (System.Object)AcadSpline;
* (System.Int32)Index;

### Нод AddFitPoint

**Имя нода** = AddFitPoint;

**Описание** = Adds a fit point to a spline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSpline;

**Входные данные**:

* (System.Object)AcadSpline;
* (System.Int32)Index;
* (System.Object)fitPoint;

### Нод DeleteFitPoint

**Имя нода** = DeleteFitPoint;

**Описание** = Deletes a fit point from a spline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSpline;

**Входные данные**:

* (System.Object)AcadSpline;
* (System.Int32)Index;

### Нод ElevateOrder

**Имя нода** = ElevateOrder;

**Описание** = Not implemented. Increases the order of a spline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSpline;

**Входные данные**:

* (System.Object)AcadSpline;
* (System.Int32)Order;

### Нод Offset

**Имя нода** = Offset;

**Описание** = Not implemented. Creates a new spline at a specified distance from an original spline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSpline;

**Входные данные**:

* (System.Object)AcadSpline;
* (System.Double)Distance;

### Нод PurgeFitData

**Имя нода** = PurgeFitData;

**Описание** = Removes the fit data of a spline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSpline;

**Входные данные**:

* (System.Object)AcadSpline;

### Нод Reverse

**Имя нода** = Reverse;

**Описание** = Reverses the direction of a spline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSpline;

**Входные данные**:

* (System.Object)AcadSpline;

### Нод Knots

**Имя нода** = Knots;

**Описание** = Specifies or returns the knot vector of a spline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSpline;

**Входные данные**:

* (System.Object)AcadSpline;

### Нод Set_Knots

**Имя нода** = Set_Knots;

**Описание** = Specifies or returns the knot vector of a spline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSpline;

**Входные данные**:

* (System.Object)AcadSpline;
* (System.Object)KnotValues;

### Нод Weights

**Имя нода** = Weights;

**Описание** = Specifies or returns the weight vector of a spline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSpline;

**Входные данные**:

* (System.Object)AcadSpline;

### Нод Set_Weights

**Имя нода** = Set_Weights;

**Описание** = Specifies or returns the weight vector of a spline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSpline;

**Входные данные**:

* (System.Object)AcadSpline;
* (System.Object)WeightValues;

## Группа NVP_nanoCAD_COM.OdaX.AcadSortentsTable

### Нод _AcadSortentsTable_Constructor

**Имя нода** = _AcadSortentsTable_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSortentsTable;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadSortentsTable_ConstructorCast

**Имя нода** = _AcadSortentsTable_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSortentsTable;

**Входные данные**:

* (System.Object)dynamic;

### Нод MoveToBottom

**Имя нода** = MoveToBottom;

**Описание** = Moves objects to the bottom of the draw order.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSortentsTable;

**Входные данные**:

* (System.Object)AcadSortentsTable;
* (System.Object)Objects;

### Нод MoveToTop

**Имя нода** = MoveToTop;

**Описание** = Moves objects to the top of the draw order.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSortentsTable;

**Входные данные**:

* (System.Object)AcadSortentsTable;
* (System.Object)Objects;

### Нод MoveBelow

**Имя нода** = MoveBelow;

**Описание** = Moves objects below a specified object in the draw order.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSortentsTable;

**Входные данные**:

* (System.Object)AcadSortentsTable;
* (System.Object)Objects;
* (System.Object)Target;

### Нод MoveAbove

**Имя нода** = MoveAbove;

**Описание** = Moves objects above a specified object in the draw order.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSortentsTable;

**Входные данные**:

* (System.Object)AcadSortentsTable;
* (System.Object)Objects;
* (System.Object)Target;

### Нод SwapOrder

**Имя нода** = SwapOrder;

**Описание** = Exchanges draw order position for two specified objects.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSortentsTable;

**Входные данные**:

* (System.Object)AcadSortentsTable;
* (System.Object)Object1;
* (System.Object)Object2;

### Нод Block

**Имя нода** = Block;

**Описание** = Returns the block referenced by a sortents table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSortentsTable;

**Входные данные**:

* (System.Object)AcadSortentsTable;

### Нод GetFullDrawOrder

**Имя нода** = GetFullDrawOrder;

**Описание** = Returns all objects in the referenced block, sorted by draw order with the bottom-most object first.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSortentsTable;

**Входные данные**:

* (System.Object)AcadSortentsTable;
* (System.Object)Objects;
* (System.Object)honorSortentsSysvar;

### Нод GetRelativeDrawOrder

**Имя нода** = GetRelativeDrawOrder;

**Описание** = Returns the specified objects, sorted by draw order with the bottom-most object first.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSortentsTable;

**Входные данные**:

* (System.Object)AcadSortentsTable;
* (System.Object)Objects;
* (System.Object)honorSortentsSysvar;

### Нод SetRelativeDrawOrder

**Имя нода** = SetRelativeDrawOrder;

**Описание** = Specifies the relative draw order for the specified objects.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSortentsTable;

**Входные данные**:

* (System.Object)AcadSortentsTable;
* (System.Object)Objects;

## Группа NVP_nanoCAD_COM.OdaX.AcadSolid

### Нод _AcadSolid_Constructor

**Имя нода** = _AcadSolid_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSolid;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadSolid_ConstructorCast

**Имя нода** = _AcadSolid_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSolid;

**Входные данные**:

* (System.Object)dynamic;

### Нод Coordinates

**Имя нода** = Coordinates;

**Описание** = Specifies or returns the current vertex of a solid.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSolid;

**Входные данные**:

* (System.Object)AcadSolid;

### Нод Set_Coordinates

**Имя нода** = Set_Coordinates;

**Описание** = Specifies or returns the current vertex of a solid.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSolid;

**Входные данные**:

* (System.Object)AcadSolid;
* (System.Object)corners;

### Нод Normal

**Имя нода** = Normal;

**Описание** = Specifies or returns the x, y, and z coordinates of the normal direction vector.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSolid;

**Входные данные**:

* (System.Object)AcadSolid;

### Нод Set_Normal

**Имя нода** = Set_Normal;

**Описание** = Specifies or returns the x, y, and z coordinates of the normal direction vector.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSolid;

**Входные данные**:

* (System.Object)AcadSolid;
* (System.Object)Normal;

### Нод Thickness

**Имя нода** = Thickness;

**Описание** = z-direction value) of a solid.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSolid;

**Входные данные**:

* (System.Object)AcadSolid;

### Нод Set_Thickness

**Имя нода** = Set_Thickness;

**Описание** = z-direction value) of a solid.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSolid;

**Входные данные**:

* (System.Object)AcadSolid;
* (System.Double)Thickness;

### Нод Coordinate

**Имя нода** = Coordinate;

**Описание** = Specifies or returns the coordinates of a single vertex in a solid.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSolid;

**Входные данные**:

* (System.Object)AcadSolid;
* (System.Int32)Index;

### Нод Set_Coordinate

**Имя нода** = Set_Coordinate;

**Описание** = Specifies or returns the coordinates of a single vertex in a solid.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSolid;

**Входные данные**:

* (System.Object)AcadSolid;
* (System.Int32)Index;
* (System.Object)pVal;

## Группа NVP_nanoCAD_COM.OdaX.AcadShape

### Нод _AcadShape_Constructor

**Имя нода** = _AcadShape_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadShape;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadShape_ConstructorCast

**Имя нода** = _AcadShape_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadShape;

**Входные данные**:

* (System.Object)dynamic;

### Нод InsertionPoint

**Имя нода** = InsertionPoint;

**Описание** = Specifies or returns the x, y, and z coordinates of the shape insertion point.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadShape;

**Входные данные**:

* (System.Object)AcadShape;

### Нод Set_InsertionPoint

**Имя нода** = Set_InsertionPoint;

**Описание** = Specifies or returns the x, y, and z coordinates of the shape insertion point.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadShape;

**Входные данные**:

* (System.Object)AcadShape;
* (System.Object)insPoint;

### Нод Name

**Имя нода** = Name;

**Описание** = Specifies or returns the name of a shape.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadShape;

**Входные данные**:

* (System.Object)AcadShape;

### Нод Set_Name

**Имя нода** = Set_Name;

**Описание** = Specifies or returns the name of a shape.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadShape;

**Входные данные**:

* (System.Object)AcadShape;
* (System.String)bstrName;

### Нод Height

**Имя нода** = Height;

**Описание** = Specifies or returns the height of a shape.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadShape;

**Входные данные**:

* (System.Object)AcadShape;

### Нод Set_Height

**Имя нода** = Set_Height;

**Описание** = Specifies or returns the height of a shape.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadShape;

**Входные данные**:

* (System.Object)AcadShape;
* (System.Double)Height;

### Нод Rotation

**Имя нода** = Rotation;

**Описание** = Specifies or returns the rotation angle of a shape.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadShape;

**Входные данные**:

* (System.Object)AcadShape;

### Нод Set_Rotation

**Имя нода** = Set_Rotation;

**Описание** = Specifies or returns the rotation angle of a shape.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadShape;

**Входные данные**:

* (System.Object)AcadShape;
* (System.Object)rotAngle;

### Нод ScaleFactor

**Имя нода** = ScaleFactor;

**Описание** = Specifies or returns the width scale factor of a shape.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadShape;

**Входные данные**:

* (System.Object)AcadShape;

### Нод Set_ScaleFactor

**Имя нода** = Set_ScaleFactor;

**Описание** = Specifies or returns the width scale factor of a shape.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadShape;

**Входные данные**:

* (System.Object)AcadShape;
* (System.Object)scalFactor;

### Нод ObliqueAngle

**Имя нода** = ObliqueAngle;

**Описание** = Specifies or returns the oblique angle of a shape.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadShape;

**Входные данные**:

* (System.Object)AcadShape;

### Нод Set_ObliqueAngle

**Имя нода** = Set_ObliqueAngle;

**Описание** = Specifies or returns the oblique angle of a shape.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadShape;

**Входные данные**:

* (System.Object)AcadShape;
* (System.Object)obliAngle;

### Нод Normal

**Имя нода** = Normal;

**Описание** = Specifies or returns the x, y, and z coordinates of the normal direction vector.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadShape;

**Входные данные**:

* (System.Object)AcadShape;

### Нод Set_Normal

**Имя нода** = Set_Normal;

**Описание** = Specifies or returns the x, y, and z coordinates of the normal direction vector.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadShape;

**Входные данные**:

* (System.Object)AcadShape;
* (System.Object)Normal;

### Нод Thickness

**Имя нода** = Thickness;

**Описание** = z-direction value) of a shape.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadShape;

**Входные данные**:

* (System.Object)AcadShape;

### Нод Set_Thickness

**Имя нода** = Set_Thickness;

**Описание** = z-direction value) of a shape.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadShape;

**Входные данные**:

* (System.Object)AcadShape;
* (System.Double)Thickness;

## Группа NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings2

### Нод _AcadSectionTypeSettings2_Constructor

**Имя нода** = _AcadSectionTypeSettings2_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings2;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadSectionTypeSettings2_ConstructorCast

**Имя нода** = _AcadSectionTypeSettings2_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings2;

**Входные данные**:

* (System.Object)dynamic;

### Нод IntersectionBoundaryVisible

**Имя нода** = IntersectionBoundaryVisible;

**Описание** = Specifies the visibility of intersection boundary;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings2;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings2;

### Нод Set_IntersectionBoundaryVisible

**Имя нода** = Set_IntersectionBoundaryVisible;

**Описание** = Specifies the visibility of intersection boundary;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings2;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings2;
* (System.Object)pVal;

## Группа NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings

### Нод _AcadSectionTypeSettings_Constructor

**Имя нода** = _AcadSectionTypeSettings_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadSectionTypeSettings_ConstructorCast

**Имя нода** = _AcadSectionTypeSettings_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)dynamic;

### Нод GenerationOptions

**Имя нода** = GenerationOptions;

**Описание** = Specifies or returns the options used to generate a section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_GenerationOptions

**Имя нода** = Set_GenerationOptions;

**Описание** = Specifies or returns the options used to generate a section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.Object)OdaX.AcSectionGeneration;

### Нод SourceObjects

**Имя нода** = SourceObjects;

**Описание** = Specifies or returns the objects used to generate a section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_SourceObjects

**Имя нода** = Set_SourceObjects;

**Описание** = Specifies or returns the objects used to generate a section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.Object)pVal;

### Нод DestinationBlock

**Имя нода** = DestinationBlock;

**Описание** = Specifies or returns the block used for the output of section generation.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_DestinationBlock

**Имя нода** = Set_DestinationBlock;

**Описание** = Specifies or returns the block used for the output of section generation.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.Object)pVal;

### Нод DestinationFile

**Имя нода** = DestinationFile;

**Описание** = Specifies or returns the file used for the output of section generation.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_DestinationFile

**Имя нода** = Set_DestinationFile;

**Описание** = Specifies or returns the file used for the output of section generation.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.String)pVal;

### Нод IntersectionBoundaryColor

**Имя нода** = IntersectionBoundaryColor;

**Описание** = Specifies or returns the color of an intersection boundary section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_IntersectionBoundaryColor

**Имя нода** = Set_IntersectionBoundaryColor;

**Описание** = Specifies or returns the color of an intersection boundary section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.Object)pColor;

### Нод IntersectionBoundaryLayer

**Имя нода** = IntersectionBoundaryLayer;

**Описание** = Specifies or returns the layer of an intersection boundary section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_IntersectionBoundaryLayer

**Имя нода** = Set_IntersectionBoundaryLayer;

**Описание** = Specifies or returns the layer of an intersection boundary section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.String)Layer;

### Нод IntersectionBoundaryLinetype

**Имя нода** = IntersectionBoundaryLinetype;

**Описание** = Specifies or returns the linetype of an intersection boundary section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_IntersectionBoundaryLinetype

**Имя нода** = Set_IntersectionBoundaryLinetype;

**Описание** = Specifies or returns the linetype of an intersection boundary section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.String)Linetype;

### Нод IntersectionBoundaryLinetypeScale

**Имя нода** = IntersectionBoundaryLinetypeScale;

**Описание** = Specifies or returns the linetype scale of an intersection boundary section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_IntersectionBoundaryLinetypeScale

**Имя нода** = Set_IntersectionBoundaryLinetypeScale;

**Описание** = Specifies or returns the linetype scale of an intersection boundary section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.Object)ltScale;

### Нод IntersectionBoundaryPlotStyleName

**Имя нода** = IntersectionBoundaryPlotStyleName;

**Описание** = Specifies or returns the plot style name of an intersection boundary section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_IntersectionBoundaryPlotStyleName

**Имя нода** = Set_IntersectionBoundaryPlotStyleName;

**Описание** = Specifies or returns the plot style name of an intersection boundary section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.String)plotStyle;

### Нод IntersectionBoundaryLineweight

**Имя нода** = IntersectionBoundaryLineweight;

**Описание** = Specifies or returns the lineweight of an intersection boundary section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_IntersectionBoundaryLineweight

**Имя нода** = Set_IntersectionBoundaryLineweight;

**Описание** = Specifies or returns the lineweight of an intersection boundary section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.Object)Lineweight;

### Нод IntersectionBoundaryDivisionLines

**Имя нода** = IntersectionBoundaryDivisionLines;

**Описание** = Specifies or returns whether division lines are shown for an intersection boundary section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_IntersectionBoundaryDivisionLines

**Имя нода** = Set_IntersectionBoundaryDivisionLines;

**Описание** = Specifies or returns whether division lines are shown for an intersection boundary section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.Object)pVal;

### Нод IntersectionFillVisible

**Имя нода** = IntersectionFillVisible;

**Описание** = Specifies or returns whether an intersection fill section is visible.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_IntersectionFillVisible

**Имя нода** = Set_IntersectionFillVisible;

**Описание** = Specifies or returns whether an intersection fill section is visible.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.Object)pVal;

### Нод IntersectionFillHatchPatternType

**Имя нода** = IntersectionFillHatchPatternType;

**Описание** = Specifies or returns the hatch pattern type of an intersection fill section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_IntersectionFillHatchPatternType

**Имя нода** = Set_IntersectionFillHatchPatternType;

**Описание** = Specifies or returns the hatch pattern type of an intersection fill section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.Object)OdaX.AcPatternType;

### Нод IntersectionFillHatchPatternName

**Имя нода** = IntersectionFillHatchPatternName;

**Описание** = Specifies or returns the hatch pattern name of an intersection fill section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_IntersectionFillHatchPatternName

**Имя нода** = Set_IntersectionFillHatchPatternName;

**Описание** = Specifies or returns the hatch pattern name of an intersection fill section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.String)pVal;

### Нод IntersectionFillHatchAngle

**Имя нода** = IntersectionFillHatchAngle;

**Описание** = Specifies or returns the hatch angle of an intersection fill section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_IntersectionFillHatchAngle

**Имя нода** = Set_IntersectionFillHatchAngle;

**Описание** = Specifies or returns the hatch angle of an intersection fill section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.Object)pVal;

### Нод IntersectionFillHatchScale

**Имя нода** = IntersectionFillHatchScale;

**Описание** = Specifies or returns the hatch scale of an intersection fill section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_IntersectionFillHatchScale

**Имя нода** = Set_IntersectionFillHatchScale;

**Описание** = Specifies or returns the hatch scale of an intersection fill section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.Object)pVal;

### Нод IntersectionFillHatchSpacing

**Имя нода** = IntersectionFillHatchSpacing;

**Описание** = Specifies or returns the hatch spacing of an intersection fill section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_IntersectionFillHatchSpacing

**Имя нода** = Set_IntersectionFillHatchSpacing;

**Описание** = Specifies or returns the hatch spacing of an intersection fill section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.Double)pVal;

### Нод IntersectionFillColor

**Имя нода** = IntersectionFillColor;

**Описание** = Specifies or returns the color of an intersection fill section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_IntersectionFillColor

**Имя нода** = Set_IntersectionFillColor;

**Описание** = Specifies or returns the color of an intersection fill section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.Object)pColor;

### Нод IntersectionFillLayer

**Имя нода** = IntersectionFillLayer;

**Описание** = Specifies or returns the layer of an intersection fill section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_IntersectionFillLayer

**Имя нода** = Set_IntersectionFillLayer;

**Описание** = Specifies or returns the layer of an intersection fill section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.String)Layer;

### Нод IntersectionFillLinetype

**Имя нода** = IntersectionFillLinetype;

**Описание** = Specifies or returns the linetype of an intersection fill section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_IntersectionFillLinetype

**Имя нода** = Set_IntersectionFillLinetype;

**Описание** = Specifies or returns the linetype of an intersection fill section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.String)Linetype;

### Нод IntersectionFillLinetypeScale

**Имя нода** = IntersectionFillLinetypeScale;

**Описание** = Specifies or returns the linetype scale of an intersection fill section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_IntersectionFillLinetypeScale

**Имя нода** = Set_IntersectionFillLinetypeScale;

**Описание** = Specifies or returns the linetype scale of an intersection fill section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.Object)ltScale;

### Нод IntersectionFillPlotStyleName

**Имя нода** = IntersectionFillPlotStyleName;

**Описание** = Specifies or returns the plot style name of an intersection fill section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_IntersectionFillPlotStyleName

**Имя нода** = Set_IntersectionFillPlotStyleName;

**Описание** = Specifies or returns the plot style name of an intersection fill section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.String)plotStyle;

### Нод IntersectionFillLineweight

**Имя нода** = IntersectionFillLineweight;

**Описание** = Specifies or returns the lineweight of an intersection fill section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_IntersectionFillLineweight

**Имя нода** = Set_IntersectionFillLineweight;

**Описание** = Specifies or returns the lineweight of an intersection fill section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.Object)Lineweight;

### Нод IntersectionFillFaceTransparency

**Имя нода** = IntersectionFillFaceTransparency;

**Описание** = Specifies or returns the face transparency value of an intersection fill section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_IntersectionFillFaceTransparency

**Имя нода** = Set_IntersectionFillFaceTransparency;

**Описание** = Specifies or returns the face transparency value of an intersection fill section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.Object)pVal;

### Нод BackgroundLinesVisible

**Имя нода** = BackgroundLinesVisible;

**Описание** = Specifies or returns whether a background geometry section is visible.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_BackgroundLinesVisible

**Имя нода** = Set_BackgroundLinesVisible;

**Описание** = Specifies or returns whether a background geometry section is visible.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.Object)pVal;

### Нод BackgroundLinesHiddenLine

**Имя нода** = BackgroundLinesHiddenLine;

**Описание** = Specifies or returns whether hidden lines are visble for a background geometry section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_BackgroundLinesHiddenLine

**Имя нода** = Set_BackgroundLinesHiddenLine;

**Описание** = Specifies or returns whether hidden lines are visble for a background geometry section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.Object)pVal;

### Нод BackgroundLinesColor

**Имя нода** = BackgroundLinesColor;

**Описание** = Specifies or returns the color of a background geometry section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_BackgroundLinesColor

**Имя нода** = Set_BackgroundLinesColor;

**Описание** = Specifies or returns the color of a background geometry section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.Object)pColor;

### Нод BackgroundLinesLayer

**Имя нода** = BackgroundLinesLayer;

**Описание** = Specifies or returns the layer of a background geometry section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_BackgroundLinesLayer

**Имя нода** = Set_BackgroundLinesLayer;

**Описание** = Specifies or returns the layer of a background geometry section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.String)Layer;

### Нод BackgroundLinesLinetype

**Имя нода** = BackgroundLinesLinetype;

**Описание** = Specifies or returns the linetype of a background geometry section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_BackgroundLinesLinetype

**Имя нода** = Set_BackgroundLinesLinetype;

**Описание** = Specifies or returns the linetype of a background geometry section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.String)Linetype;

### Нод BackgroundLinesLinetypeScale

**Имя нода** = BackgroundLinesLinetypeScale;

**Описание** = Specifies or returns the linetype scale of a background geometry section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_BackgroundLinesLinetypeScale

**Имя нода** = Set_BackgroundLinesLinetypeScale;

**Описание** = Specifies or returns the linetype scale of a background geometry section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.Object)ltScale;

### Нод BackgroundLinesPlotStyleName

**Имя нода** = BackgroundLinesPlotStyleName;

**Описание** = Specifies or returns the plot style name of a background geometry section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_BackgroundLinesPlotStyleName

**Имя нода** = Set_BackgroundLinesPlotStyleName;

**Описание** = Specifies or returns the plot style name of a background geometry section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.String)plotStyle;

### Нод BackgroundLinesLineweight

**Имя нода** = BackgroundLinesLineweight;

**Описание** = Specifies or returns the lineweight of a background geometry section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_BackgroundLinesLineweight

**Имя нода** = Set_BackgroundLinesLineweight;

**Описание** = Specifies or returns the lineweight of a background geometry section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.Object)Lineweight;

### Нод ForegroundLinesVisible

**Имя нода** = ForegroundLinesVisible;

**Описание** = Specifies or returns whether a foreground geometry section is visible.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_ForegroundLinesVisible

**Имя нода** = Set_ForegroundLinesVisible;

**Описание** = Specifies or returns whether a foreground geometry section is visible.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.Object)pVal;

### Нод ForegroundLinesHiddenLine

**Имя нода** = ForegroundLinesHiddenLine;

**Описание** = Specifies or returns whether hidden lines are visble for a foreground geometry section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_ForegroundLinesHiddenLine

**Имя нода** = Set_ForegroundLinesHiddenLine;

**Описание** = Specifies or returns whether hidden lines are visble for a foreground geometry section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.Object)pVal;

### Нод ForegroundLinesColor

**Имя нода** = ForegroundLinesColor;

**Описание** = Specifies or returns the color of a foreground geometry section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_ForegroundLinesColor

**Имя нода** = Set_ForegroundLinesColor;

**Описание** = Specifies or returns the color of a foreground geometry section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.Object)pColor;

### Нод ForegroundLinesLayer

**Имя нода** = ForegroundLinesLayer;

**Описание** = Specifies or returns the layer of a foreground geometry section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_ForegroundLinesLayer

**Имя нода** = Set_ForegroundLinesLayer;

**Описание** = Specifies or returns the layer of a foreground geometry section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.String)Layer;

### Нод ForegroundLinesLinetype

**Имя нода** = ForegroundLinesLinetype;

**Описание** = Specifies or returns the linetype of a foreground geometry section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_ForegroundLinesLinetype

**Имя нода** = Set_ForegroundLinesLinetype;

**Описание** = Specifies or returns the linetype of a foreground geometry section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.String)Linetype;

### Нод ForegroundLinesLinetypeScale

**Имя нода** = ForegroundLinesLinetypeScale;

**Описание** = Specifies or returns the linetype scale of a foreground geometry section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_ForegroundLinesLinetypeScale

**Имя нода** = Set_ForegroundLinesLinetypeScale;

**Описание** = Specifies or returns the linetype scale of a foreground geometry section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.Object)ltScale;

### Нод ForegroundLinesPlotStyleName

**Имя нода** = ForegroundLinesPlotStyleName;

**Описание** = Specifies or returns the plot style name of a foreground geometry section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_ForegroundLinesPlotStyleName

**Имя нода** = Set_ForegroundLinesPlotStyleName;

**Описание** = Specifies or returns the plot style name of a foreground geometry section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.String)plotStyle;

### Нод ForegroundLinesLineweight

**Имя нода** = ForegroundLinesLineweight;

**Описание** = Specifies or returns the lineweight of a foreground geometry section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_ForegroundLinesLineweight

**Имя нода** = Set_ForegroundLinesLineweight;

**Описание** = Specifies or returns the lineweight of a foreground geometry section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.Object)Lineweight;

### Нод ForegroundLinesFaceTransparency

**Имя нода** = ForegroundLinesFaceTransparency;

**Описание** = Specifies or returns the face transparency value of a foreground geometry section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_ForegroundLinesFaceTransparency

**Имя нода** = Set_ForegroundLinesFaceTransparency;

**Описание** = Specifies or returns the face transparency value of a foreground geometry section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.Object)pVal;

### Нод ForegroundLinesEdgeTransparency

**Имя нода** = ForegroundLinesEdgeTransparency;

**Описание** = Specifies or returns the edge transparency value of a foreground geometry section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_ForegroundLinesEdgeTransparency

**Имя нода** = Set_ForegroundLinesEdgeTransparency;

**Описание** = Specifies or returns the edge transparency value of a foreground geometry section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.Object)pVal;

### Нод CurveTangencyLinesVisible

**Имя нода** = CurveTangencyLinesVisible;

**Описание** = Specifies or returns whether a curve tangency line section is visible.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_CurveTangencyLinesVisible

**Имя нода** = Set_CurveTangencyLinesVisible;

**Описание** = Specifies or returns whether a curve tangency line section is visible.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.Object)pVal;

### Нод CurveTangencyLinesColor

**Имя нода** = CurveTangencyLinesColor;

**Описание** = Specifies or returns the color of a curve tangency line section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_CurveTangencyLinesColor

**Имя нода** = Set_CurveTangencyLinesColor;

**Описание** = Specifies or returns the color of a curve tangency line section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.Object)pColor;

### Нод CurveTangencyLinesLayer

**Имя нода** = CurveTangencyLinesLayer;

**Описание** = Specifies or returns the layer of a curve tangency line section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_CurveTangencyLinesLayer

**Имя нода** = Set_CurveTangencyLinesLayer;

**Описание** = Specifies or returns the layer of a curve tangency line section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.String)Layer;

### Нод CurveTangencyLinesLinetype

**Имя нода** = CurveTangencyLinesLinetype;

**Описание** = Specifies or returns the linetype of a curve tangency line section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_CurveTangencyLinesLinetype

**Имя нода** = Set_CurveTangencyLinesLinetype;

**Описание** = Specifies or returns the linetype of a curve tangency line section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.String)Linetype;

### Нод CurveTangencyLinesLinetypeScale

**Имя нода** = CurveTangencyLinesLinetypeScale;

**Описание** = Specifies or returns the linetype scale of a curve tangency line section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_CurveTangencyLinesLinetypeScale

**Имя нода** = Set_CurveTangencyLinesLinetypeScale;

**Описание** = Specifies or returns the linetype scale of a curve tangency line section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.Object)ltScale;

### Нод CurveTangencyLinesPlotStyleName

**Имя нода** = CurveTangencyLinesPlotStyleName;

**Описание** = Specifies the plot style name of a curve tangency line section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_CurveTangencyLinesPlotStyleName

**Имя нода** = Set_CurveTangencyLinesPlotStyleName;

**Описание** = Specifies the plot style name of a curve tangency line section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.String)plotStyle;

### Нод CurveTangencyLinesLineweight

**Имя нода** = CurveTangencyLinesLineweight;

**Описание** = Specifies or returns the lineweight of a curve tangency line section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;

### Нод Set_CurveTangencyLinesLineweight

**Имя нода** = Set_CurveTangencyLinesLineweight;

**Описание** = Specifies or returns the lineweight of a curve tangency line section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionTypeSettings;

**Входные данные**:

* (System.Object)AcadSectionTypeSettings;
* (System.Object)Lineweight;

## Группа NVP_nanoCAD_COM.OdaX.AcadSectionSettings

### Нод _AcadSectionSettings_Constructor

**Имя нода** = _AcadSectionSettings_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionSettings;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadSectionSettings_ConstructorCast

**Имя нода** = _AcadSectionSettings_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionSettings;

**Входные данные**:

* (System.Object)dynamic;

### Нод CurrentSectionType

**Имя нода** = CurrentSectionType;

**Описание** = Specifies or returns the type for a section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionSettings;

**Входные данные**:

* (System.Object)AcadSectionSettings;

### Нод Set_CurrentSectionType

**Имя нода** = Set_CurrentSectionType;

**Описание** = Specifies or returns the type for a section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionSettings;

**Входные данные**:

* (System.Object)AcadSectionSettings;
* (System.Object)OdaX.AcSectionType;

### Нод GetSectionTypeSettings

**Имя нода** = GetSectionTypeSettings;

**Описание** = Returns the type for a Section Settings object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionSettings;

**Входные данные**:

* (System.Object)AcadSectionSettings;
* (System.Object)OdaX.AcSectionType;

## Группа NVP_nanoCAD_COM.OdaX.AcadSectionManager

### Нод _AcadSectionManager_Constructor

**Имя нода** = _AcadSectionManager_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionManager;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadSectionManager_ConstructorCast

**Имя нода** = _AcadSectionManager_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionManager;

**Входные данные**:

* (System.Object)dynamic;

### Нод Item

**Имя нода** = Item;

**Описание** = Returns an object at a given index in the collection of sections.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionManager;

**Входные данные**:

* (System.Object)AcadSectionManager;
* (System.Object)Index;

### Нод Count

**Имя нода** = Count;

**Описание** = Returns the number of items in the collection of sections.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionManager;

**Входные данные**:

* (System.Object)AcadSectionManager;

### Нод GetLiveSection

**Имя нода** = GetLiveSection;

**Описание** = Returns the current live section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionManager;

**Входные данные**:

* (System.Object)AcadSectionManager;

### Нод GetUniqueSectionName

**Имя нода** = GetUniqueSectionName;

**Описание** = Returns a unique section name.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSectionManager;

**Входные данные**:

* (System.Object)AcadSectionManager;
* (System.String)pBaseName;

## Группа NVP_nanoCAD_COM.OdaX.AcadSection

### Нод _AcadSection_Constructor

**Имя нода** = _AcadSection_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSection;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadSection_ConstructorCast

**Имя нода** = _AcadSection_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSection;

**Входные данные**:

* (System.Object)dynamic;

### Нод Name

**Имя нода** = Name;

**Описание** = Specifies or returns the name of a section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSection;

**Входные данные**:

* (System.Object)AcadSection;

### Нод Set_Name

**Имя нода** = Set_Name;

**Описание** = Specifies or returns the name of a section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSection;

**Входные данные**:

* (System.Object)AcadSection;
* (System.String)pbstrName;

### Нод State

**Имя нода** = State;

**Описание** = Specifies or returns the type of a section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSection;

**Входные данные**:

* (System.Object)AcadSection;

### Нод Set_State

**Имя нода** = Set_State;

**Описание** = Specifies or returns the type of a section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSection;

**Входные данные**:

* (System.Object)AcadSection;
* (System.Object)OdaX.AcSectionState;

### Нод ViewingDirection

**Имя нода** = ViewingDirection;

**Описание** = Specifies or returns the direction in which a section is viewed.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSection;

**Входные данные**:

* (System.Object)AcadSection;

### Нод Set_ViewingDirection

**Имя нода** = Set_ViewingDirection;

**Описание** = Specifies or returns the direction in which a section is viewed.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSection;

**Входные данные**:

* (System.Object)AcadSection;
* (System.Object)pVal;

### Нод VerticalDirection

**Имя нода** = VerticalDirection;

**Описание** = Specifies or returns the vertical direction for a section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSection;

**Входные данные**:

* (System.Object)AcadSection;

### Нод Set_VerticalDirection

**Имя нода** = Set_VerticalDirection;

**Описание** = Specifies or returns the vertical direction for a section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSection;

**Входные данные**:

* (System.Object)AcadSection;
* (System.Object)pVal;

### Нод Normal

**Имя нода** = Normal;

**Описание** = Specifies or returns the x, y, and z coordinates of the normal direction.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSection;

**Входные данные**:

* (System.Object)AcadSection;

### Нод LiveSectionEnabled

**Имя нода** = LiveSectionEnabled;

**Описание** = Specifies or returns whether live section is turned on for a section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSection;

**Входные данные**:

* (System.Object)AcadSection;

### Нод Set_LiveSectionEnabled

**Имя нода** = Set_LiveSectionEnabled;

**Описание** = Specifies or returns whether live section is turned on for a section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSection;

**Входные данные**:

* (System.Object)AcadSection;
* (System.Object)pVal;

### Нод IndicatorTransparency

**Имя нода** = IndicatorTransparency;

**Описание** = Specifies or returns whether a section is transparent when shading is used.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSection;

**Входные данные**:

* (System.Object)AcadSection;

### Нод Set_IndicatorTransparency

**Имя нода** = Set_IndicatorTransparency;

**Описание** = Specifies or returns whether a section is transparent when shading is used.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSection;

**Входные данные**:

* (System.Object)AcadSection;
* (System.Int32)pVal;

### Нод IndicatorFillColor

**Имя нода** = IndicatorFillColor;

**Описание** = Specifies or returns the color of a section when shading is used.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSection;

**Входные данные**:

* (System.Object)AcadSection;

### Нод Set_IndicatorFillColor

**Имя нода** = Set_IndicatorFillColor;

**Описание** = Specifies or returns the color of a section when shading is used.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSection;

**Входные данные**:

* (System.Object)AcadSection;
* (System.Object)pColor;

### Нод Elevation

**Имя нода** = Elevation;

**Описание** = Specifies or returns the elevation of a section relative to the z-axis of the object's coordinate system.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSection;

**Входные данные**:

* (System.Object)AcadSection;

### Нод Set_Elevation

**Имя нода** = Set_Elevation;

**Описание** = Specifies or returns the elevation of a section relative to the z-axis of the object's coordinate system.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSection;

**Входные данные**:

* (System.Object)AcadSection;
* (System.Double)pVal;

### Нод TopHeight

**Имя нода** = TopHeight;

**Описание** = Specifies or returns top extents relative to the section’s elevation.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSection;

**Входные данные**:

* (System.Object)AcadSection;

### Нод Set_TopHeight

**Имя нода** = Set_TopHeight;

**Описание** = Specifies or returns top extents relative to the section’s elevation.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSection;

**Входные данные**:

* (System.Object)AcadSection;
* (System.Double)pVal;

### Нод BottomHeight

**Имя нода** = BottomHeight;

**Описание** = Specifies or returns bottom extents relative to the section’s elevation.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSection;

**Входные данные**:

* (System.Object)AcadSection;

### Нод Set_BottomHeight

**Имя нода** = Set_BottomHeight;

**Описание** = Specifies or returns bottom extents relative to the section’s elevation.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSection;

**Входные данные**:

* (System.Object)AcadSection;
* (System.Double)pVal;

### Нод NumVertices

**Имя нода** = NumVertices;

**Описание** = Returns the number of vertices in the section line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSection;

**Входные данные**:

* (System.Object)AcadSection;

### Нод Vertices

**Имя нода** = Vertices;

**Описание** = Specifies or returns the vertices in the section line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSection;

**Входные данные**:

* (System.Object)AcadSection;

### Нод Set_Vertices

**Имя нода** = Set_Vertices;

**Описание** = Specifies or returns the vertices in the section line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSection;

**Входные данные**:

* (System.Object)AcadSection;
* (System.Object)pVal;

### Нод Coordinate

**Имя нода** = Coordinate;

**Описание** = Specifies or returns the coordinates of a single vertex in a section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSection;

**Входные данные**:

* (System.Object)AcadSection;
* (System.Int32)Index;

### Нод Set_Coordinate

**Имя нода** = Set_Coordinate;

**Описание** = Specifies or returns the coordinates of a single vertex in a section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSection;

**Входные данные**:

* (System.Object)AcadSection;
* (System.Int32)Index;
* (System.Object)pVal;

### Нод AddVertex

**Имя нода** = AddVertex;

**Описание** = Adds a new vertex to a section line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSection;

**Входные данные**:

* (System.Object)AcadSection;
* (System.Int32)nIndex;
* (System.Object)val;

### Нод RemoveVertex

**Имя нода** = RemoveVertex;

**Описание** = Removes a vertex from a section line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSection;

**Входные данные**:

* (System.Object)AcadSection;
* (System.Int32)nIndex;

### Нод HitTest

**Имя нода** = HitTest;

**Описание** = Not implemented. Determines whether a vertex is on a section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSection;

**Входные данные**:

* (System.Object)AcadSection;
* (System.Object)varPtHit;
* (System.Object)pHit;
* (System.Int32)pSegmentIndex;
* (System.Object)pPtOnSegment;
* (System.Object)OdaX.AcSectionSubItem;

### Нод CreateJog

**Имя нода** = CreateJog;

**Описание** = Creates a jog in a section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSection;

**Входные данные**:

* (System.Object)AcadSection;
* (System.Object)varPt;

### Нод Settings

**Имя нода** = Settings;

**Описание** = Gets the Section Settings object for a section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSection;

**Входные данные**:

* (System.Object)AcadSection;

### Нод GenerateSectionGeometry

**Имя нода** = GenerateSectionGeometry;

**Описание** = Not implemented. Generates two- or three-dimensional geometry for a section.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadSection;

**Входные данные**:

* (System.Object)AcadSection;
* (System.Object)pEntity;
* (System.Object)pIntersectionBoundaryObjs;
* (System.Object)pIntersectionFillObjs;
* (System.Object)pBackgroudnObjs;
* (System.Object)pForegroudObjs;
* (System.Object)pCurveTangencyObjs;

## Группа NVP_nanoCAD_COM.OdaX.AcadRevolvedSurface

### Нод _AcadRevolvedSurface_Constructor

**Имя нода** = _AcadRevolvedSurface_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRevolvedSurface;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadRevolvedSurface_ConstructorCast

**Имя нода** = _AcadRevolvedSurface_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRevolvedSurface;

**Входные данные**:

* (System.Object)dynamic;

### Нод RevolutionAngle

**Имя нода** = RevolutionAngle;

**Описание** = Specifies or returns the angle of revolution for a revolved surface.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRevolvedSurface;

**Входные данные**:

* (System.Object)AcadRevolvedSurface;

### Нод Set_RevolutionAngle

**Имя нода** = Set_RevolutionAngle;

**Описание** = Specifies or returns the angle of revolution for a revolved surface.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRevolvedSurface;

**Входные данные**:

* (System.Object)AcadRevolvedSurface;
* (System.Object)revAngle;

### Нод AxisPosition

**Имя нода** = AxisPosition;

**Описание** = Specifies or returns the start point of the revolution axis for a revolved surface.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRevolvedSurface;

**Входные данные**:

* (System.Object)AcadRevolvedSurface;

### Нод Set_AxisPosition

**Имя нода** = Set_AxisPosition;

**Описание** = Specifies or returns the start point of the revolution axis for a revolved surface.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRevolvedSurface;

**Входные данные**:

* (System.Object)AcadRevolvedSurface;
* (System.Object)AxisPosition;

### Нод AxisDirection

**Имя нода** = AxisDirection;

**Описание** = Returns the direction of the revolution axis for a revolved surface.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRevolvedSurface;

**Входные данные**:

* (System.Object)AcadRevolvedSurface;

## Группа NVP_nanoCAD_COM.OdaX.AcadRegisteredApplications

### Нод _AcadRegisteredApplications_Constructor

**Имя нода** = _AcadRegisteredApplications_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRegisteredApplications;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadRegisteredApplications_ConstructorCast

**Имя нода** = _AcadRegisteredApplications_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRegisteredApplications;

**Входные данные**:

* (System.Object)dynamic;

### Нод Item

**Имя нода** = Item;

**Описание** = Returns an object at a given index in the collection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRegisteredApplications;

**Входные данные**:

* (System.Object)AcadRegisteredApplications;
* (System.Object)Index;

### Нод Count

**Имя нода** = Count;

**Описание** = Returns the number of objects in the collection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRegisteredApplications;

**Входные данные**:

* (System.Object)AcadRegisteredApplications;

### Нод Add

**Имя нода** = Add;

**Описание** = Creates an object and adds it to the collection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRegisteredApplications;

**Входные данные**:

* (System.Object)AcadRegisteredApplications;
* (System.String)Name;

### Нод GetAll_RegisteredApplications

**Имя нода** = GetAll_RegisteredApplications;

**Описание** = Возвращает все Зарегистрированные приложения в виде OdaX.AcadRegisteredApplication;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRegisteredApplications;

**Входные данные**:

* (System.Object)AcadRegisteredApplications;

## Группа NVP_nanoCAD_COM.OdaX.AcadRegisteredApplication

### Нод _AcadRegisteredApplication_Constructor

**Имя нода** = _AcadRegisteredApplication_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRegisteredApplication;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadRegisteredApplication_ConstructorCast

**Имя нода** = _AcadRegisteredApplication_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRegisteredApplication;

**Входные данные**:

* (System.Object)dynamic;

### Нод Name

**Имя нода** = Name;

**Описание** = Specifies or returns the name of the registered application.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRegisteredApplication;

**Входные данные**:

* (System.Object)AcadRegisteredApplication;

### Нод Set_Name

**Имя нода** = Set_Name;

**Описание** = Specifies or returns the name of the registered application.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRegisteredApplication;

**Входные данные**:

* (System.Object)AcadRegisteredApplication;
* (System.String)bstrName;

## Группа NVP_nanoCAD_COM.OdaX.AcadRegion

### Нод _AcadRegion_Constructor

**Имя нода** = _AcadRegion_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRegion;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadRegion_ConstructorCast

**Имя нода** = _AcadRegion_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRegion;

**Входные данные**:

* (System.Object)dynamic;

### Нод Area

**Имя нода** = Area;

**Описание** = Not implemented. Returns the area of a region.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRegion;

**Входные данные**:

* (System.Object)AcadRegion;

### Нод Centroid

**Имя нода** = Centroid;

**Описание** = Not implemented. Returns the center point of the area for a region.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRegion;

**Входные данные**:

* (System.Object)AcadRegion;

### Нод MomentOfInertia

**Имя нода** = MomentOfInertia;

**Описание** = Not implemented. Returns the moment of inertia for a region.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRegion;

**Входные данные**:

* (System.Object)AcadRegion;

### Нод Normal

**Имя нода** = Normal;

**Описание** = Not implemented. Returns the x, y, and z coordinates of the normal direction vector.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRegion;

**Входные данные**:

* (System.Object)AcadRegion;

### Нод Perimeter

**Имя нода** = Perimeter;

**Описание** = Not implemented. Returns the perimeter of a region.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRegion;

**Входные данные**:

* (System.Object)AcadRegion;

### Нод PrincipalDirections

**Имя нода** = PrincipalDirections;

**Описание** = Not implemented. Returns the set of principal direction points for a region.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRegion;

**Входные данные**:

* (System.Object)AcadRegion;

### Нод PrincipalMoments

**Имя нода** = PrincipalMoments;

**Описание** = Not implemented. Returns the set of principal moment points for a region.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRegion;

**Входные данные**:

* (System.Object)AcadRegion;

### Нод ProductOfInertia

**Имя нода** = ProductOfInertia;

**Описание** = Not implemented. Returns the product of inertia for a region.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRegion;

**Входные данные**:

* (System.Object)AcadRegion;

### Нод RadiiOfGyration

**Имя нода** = RadiiOfGyration;

**Описание** = Not implemented. Returns the radius of gyration for a region.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRegion;

**Входные данные**:

* (System.Object)AcadRegion;

### Нод Boolean

**Имя нода** = Boolean;

**Описание** = Not implemented. Performs a Boolean operation with another region.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRegion;

**Входные данные**:

* (System.Object)AcadRegion;
* (System.Object)OdaX.AcBoolean;
* (System.Object)Object;

### Нод Explode

**Имя нода** = Explode;

**Описание** = Explodes the region and returns the individual entities as an array of the object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRegion;

**Входные данные**:

* (System.Object)AcadRegion;

## Группа NVP_nanoCAD_COM.OdaX.AcadRay

### Нод _AcadRay_Constructor

**Имя нода** = _AcadRay_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRay;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadRay_ConstructorCast

**Имя нода** = _AcadRay_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRay;

**Входные данные**:

* (System.Object)dynamic;

### Нод BasePoint

**Имя нода** = BasePoint;

**Описание** = Specifies or returns the start point of a ray.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRay;

**Входные данные**:

* (System.Object)AcadRay;

### Нод Set_BasePoint

**Имя нода** = Set_BasePoint;

**Описание** = Specifies or returns the start point of a ray.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRay;

**Входные данные**:

* (System.Object)AcadRay;
* (System.Object)BasePoint;

### Нод SecondPoint

**Имя нода** = SecondPoint;

**Описание** = Specifies or returns the second point of a ray.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRay;

**Входные данные**:

* (System.Object)AcadRay;

### Нод Set_SecondPoint

**Имя нода** = Set_SecondPoint;

**Описание** = Specifies or returns the second point of a ray.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRay;

**Входные данные**:

* (System.Object)AcadRay;
* (System.Object)SecondPoint;

### Нод DirectionVector

**Имя нода** = DirectionVector;

**Описание** = Specifies or returns the direction vector of a ray.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRay;

**Входные данные**:

* (System.Object)AcadRay;

### Нод Set_DirectionVector

**Имя нода** = Set_DirectionVector;

**Описание** = Specifies or returns the direction vector of a ray.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRay;

**Входные данные**:

* (System.Object)AcadRay;
* (System.Object)dirVector;

## Группа NVP_nanoCAD_COM.OdaX.AcadRasterImage

### Нод _AcadRasterImage_Constructor

**Имя нода** = _AcadRasterImage_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRasterImage;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadRasterImage_ConstructorCast

**Имя нода** = _AcadRasterImage_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRasterImage;

**Входные данные**:

* (System.Object)dynamic;

### Нод Brightness

**Имя нода** = Brightness;

**Описание** = Specifies the current brightness value of the raster image;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRasterImage;

**Входные данные**:

* (System.Object)AcadRasterImage;

### Нод Set_Brightness

**Имя нода** = Set_Brightness;

**Описание** = Specifies the current brightness value of the raster image;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRasterImage;

**Входные данные**:

* (System.Object)AcadRasterImage;
* (System.Object)Brightness;

### Нод Contrast

**Имя нода** = Contrast;

**Описание** = Specifies the current contrast value of the raster image;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRasterImage;

**Входные данные**:

* (System.Object)AcadRasterImage;

### Нод Set_Contrast

**Имя нода** = Set_Contrast;

**Описание** = Specifies the current contrast value of the raster image;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRasterImage;

**Входные данные**:

* (System.Object)AcadRasterImage;
* (System.Object)Contrast;

### Нод Fade

**Имя нода** = Fade;

**Описание** = Specifies the current fade value of the raster image;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRasterImage;

**Входные данные**:

* (System.Object)AcadRasterImage;

### Нод Set_Fade

**Имя нода** = Set_Fade;

**Описание** = Specifies the current fade value of the raster image;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRasterImage;

**Входные данные**:

* (System.Object)AcadRasterImage;
* (System.Object)Fade;

### Нод Origin

**Имя нода** = Origin;

**Описание** = lower left corner) of the raster image;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRasterImage;

**Входные данные**:

* (System.Object)AcadRasterImage;

### Нод Set_Origin

**Имя нода** = Set_Origin;

**Описание** = lower left corner) of the raster image;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRasterImage;

**Входные данные**:

* (System.Object)AcadRasterImage;
* (System.Object)Origin;

### Нод Rotation

**Имя нода** = Rotation;

**Описание** = Specifies the rotation angle of the raster image;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRasterImage;

**Входные данные**:

* (System.Object)AcadRasterImage;

### Нод Set_Rotation

**Имя нода** = Set_Rotation;

**Описание** = Specifies the rotation angle of the raster image;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRasterImage;

**Входные данные**:

* (System.Object)AcadRasterImage;
* (System.Object)rotAngle;

### Нод ImageWidth

**Имя нода** = ImageWidth;

**Описание** = Specifies the width of the raster image;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRasterImage;

**Входные данные**:

* (System.Object)AcadRasterImage;

### Нод Set_ImageWidth

**Имя нода** = Set_ImageWidth;

**Описание** = Specifies the width of the raster image;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRasterImage;

**Входные данные**:

* (System.Object)AcadRasterImage;
* (System.Double)Width;

### Нод ImageHeight

**Имя нода** = ImageHeight;

**Описание** = Specifies the height of the raster image;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRasterImage;

**Входные данные**:

* (System.Object)AcadRasterImage;

### Нод Set_ImageHeight

**Имя нода** = Set_ImageHeight;

**Описание** = Specifies the height of the raster image;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRasterImage;

**Входные данные**:

* (System.Object)AcadRasterImage;
* (System.Double)Height;

### Нод Name

**Имя нода** = Name;

**Описание** = Specifies the name of the image file;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRasterImage;

**Входные данные**:

* (System.Object)AcadRasterImage;

### Нод Set_Name

**Имя нода** = Set_Name;

**Описание** = Specifies the name of the image file;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRasterImage;

**Входные данные**:

* (System.Object)AcadRasterImage;
* (System.String)Name;

### Нод Set_ImageFile

**Имя нода** = Set_ImageFile;

**Описание** = Specifies the path to the image file;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRasterImage;

**Входные данные**:

* (System.Object)AcadRasterImage;
* (System.String)imageFileName;

### Нод ImageFile

**Имя нода** = ImageFile;

**Описание** = Specifies the path to the image file;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRasterImage;

**Входные данные**:

* (System.Object)AcadRasterImage;

### Нод ImageVisibility

**Имя нода** = ImageVisibility;

**Описание** = Determines whether image is visible or not;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRasterImage;

**Входные данные**:

* (System.Object)AcadRasterImage;

### Нод Set_ImageVisibility

**Имя нода** = Set_ImageVisibility;

**Описание** = Determines whether image is visible or not;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRasterImage;

**Входные данные**:

* (System.Object)AcadRasterImage;
* (System.Object)fVisible;

### Нод ClippingEnabled

**Имя нода** = ClippingEnabled;

**Описание** = Enables or disables the clipping boundary of the image;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRasterImage;

**Входные данные**:

* (System.Object)AcadRasterImage;

### Нод Set_ClippingEnabled

**Имя нода** = Set_ClippingEnabled;

**Описание** = Enables or disables the clipping boundary of the image;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRasterImage;

**Входные данные**:

* (System.Object)AcadRasterImage;
* (System.Object)kClip;

### Нод Transparency

**Имя нода** = Transparency;

**Описание** = Determines whether transparency for a bitonal image is On or Off;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRasterImage;

**Входные данные**:

* (System.Object)AcadRasterImage;

### Нод Set_Transparency

**Имя нода** = Set_Transparency;

**Описание** = Determines whether transparency for a bitonal image is On or Off;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRasterImage;

**Входные данные**:

* (System.Object)AcadRasterImage;
* (System.Object)bTransp;

### Нод ClipBoundary

**Имя нода** = ClipBoundary;

**Описание** = Set clipping boundary;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRasterImage;

**Входные данные**:

* (System.Object)AcadRasterImage;
* (System.Object)boundry;

### Нод Height

**Имя нода** = Height;

**Описание** = Height of the attribute, shape, text, or view toolbar or the main application window;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRasterImage;

**Входные данные**:

* (System.Object)AcadRasterImage;

### Нод Width

**Имя нода** = Width;

**Описание** = Specifies the width of the text boundary, view, image, toolbar, or main application window;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRasterImage;

**Входные данные**:

* (System.Object)AcadRasterImage;

### Нод ShowRotation

**Имя нода** = ShowRotation;

**Описание** = Determines if a raster image is displayed at its rotation value;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRasterImage;

**Входные данные**:

* (System.Object)AcadRasterImage;

### Нод Set_ShowRotation

**Имя нода** = Set_ShowRotation;

**Описание** = Determines if a raster image is displayed at its rotation value;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRasterImage;

**Входные данные**:

* (System.Object)AcadRasterImage;
* (System.Object)bShow;

### Нод ScaleFactor

**Имя нода** = ScaleFactor;

**Описание** = Specifies the scale factor of the raster image;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRasterImage;

**Входные данные**:

* (System.Object)AcadRasterImage;

### Нод Set_ScaleFactor

**Имя нода** = Set_ScaleFactor;

**Описание** = Specifies the scale factor of the raster image;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadRasterImage;

**Входные данные**:

* (System.Object)AcadRasterImage;
* (System.Object)ScaleFactor;

## Группа NVP_nanoCAD_COM.OdaX.AcadPViewport

### Нод _AcadPViewport_Constructor

**Имя нода** = _AcadPViewport_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadPViewport_ConstructorCast

**Имя нода** = _AcadPViewport_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)dynamic;

### Нод Center

**Имя нода** = Center;

**Описание** = Specifies or returns the x, y, and z coordinates of a paperspace viewports's center point.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;

### Нод Set_Center

**Имя нода** = Set_Center;

**Описание** = Specifies or returns the x, y, and z coordinates of a paperspace viewports's center point.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;
* (System.Object)CenterPoint;

### Нод Direction

**Имя нода** = Direction;

**Описание** = Specifies or returns the viewing direction fof a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;

### Нод Set_Direction

**Имя нода** = Set_Direction;

**Описание** = Specifies or returns the viewing direction fof a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;
* (System.Object)dirVector;

### Нод GridOn

**Имя нода** = GridOn;

**Описание** = Specifies or returns whether the grid is on for a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;

### Нод Set_GridOn

**Имя нода** = Set_GridOn;

**Описание** = Specifies or returns whether the grid is on for a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;
* (System.Object)bGridOn;

### Нод Height

**Имя нода** = Height;

**Описание** = Specifies or returns the height of a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;

### Нод Set_Height

**Имя нода** = Set_Height;

**Описание** = Specifies or returns the height of a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;
* (System.Double)Height;

### Нод Width

**Имя нода** = Width;

**Описание** = Specifies or returns the width of a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;

### Нод Set_Width

**Имя нода** = Set_Width;

**Описание** = Specifies or returns the width of a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;
* (System.Double)Width;

### Нод ViewportOn

**Имя нода** = ViewportOn;

**Описание** = Specifies or returns whether a paperspace viewport is on.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;

### Нод Set_ViewportOn

**Имя нода** = Set_ViewportOn;

**Описание** = Specifies or returns whether a paperspace viewport is on.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;
* (System.Object)bOn;

### Нод Clipped

**Имя нода** = Clipped;

**Описание** = Returns whether a paperspace viewport is clipped.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;

### Нод DisplayLocked

**Имя нода** = DisplayLocked;

**Описание** = Specifies or returns whether a paperspace viewport is locked.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;

### Нод Set_DisplayLocked

**Имя нода** = Set_DisplayLocked;

**Описание** = Specifies or returns whether a paperspace viewport is locked.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;
* (System.Object)bLocked;

### Нод StandardScale

**Имя нода** = StandardScale;

**Описание** = Specifies or returns the standard scale of a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;

### Нод Set_StandardScale

**Имя нода** = Set_StandardScale;

**Описание** = Specifies or returns the standard scale of a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;
* (System.Object)OdaX.AcViewportScale;

### Нод CustomScale

**Имя нода** = CustomScale;

**Описание** = Specifies or returns a custom scale for a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;

### Нод Set_CustomScale

**Имя нода** = Set_CustomScale;

**Описание** = Specifies or returns a custom scale for a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;
* (System.Double)scale;

### Нод StyleSheet

**Имя нода** = StyleSheet;

**Описание** = Specifies or returns the style sheet to use for a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;

### Нод Set_StyleSheet

**Имя нода** = Set_StyleSheet;

**Описание** = Specifies or returns the style sheet to use for a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;
* (System.String)pName;

### Нод UCSPerViewport

**Имя нода** = UCSPerViewport;

**Описание** = Specifies or returns whether the UCS is saved with a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;

### Нод Set_UCSPerViewport

**Имя нода** = Set_UCSPerViewport;

**Описание** = Specifies or returns whether the UCS is saved with a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;
* (System.Object)UCSSaved;

### Нод SnapBasePoint

**Имя нода** = SnapBasePoint;

**Описание** = Specifies or returns the snap base point for a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;

### Нод Set_SnapBasePoint

**Имя нода** = Set_SnapBasePoint;

**Описание** = Specifies or returns the snap base point for a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;
* (System.Object)lowLeft;

### Нод SnapOn

**Имя нода** = SnapOn;

**Описание** = Specifies or returns whether the snap is turned on for a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;

### Нод Set_SnapOn

**Имя нода** = Set_SnapOn;

**Описание** = Specifies or returns whether the snap is turned on for a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;
* (System.Object)bSnapOn;

### Нод SnapRotationAngle

**Имя нода** = SnapRotationAngle;

**Описание** = Specifies or returns the snap rotation angle of a paperspace viewport relative to the current UCS.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;

### Нод Set_SnapRotationAngle

**Имя нода** = Set_SnapRotationAngle;

**Описание** = Specifies or returns the snap rotation angle of a paperspace viewport relative to the current UCS.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;
* (System.Double)Angle;

### Нод UCSIconOn

**Имя нода** = UCSIconOn;

**Описание** = Specifies or returns whether the UCS icon is turned on for a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;

### Нод Set_UCSIconOn

**Имя нода** = Set_UCSIconOn;

**Описание** = Specifies or returns whether the UCS icon is turned on for a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;
* (System.Object)bIconOn;

### Нод UCSIconAtOrigin

**Имя нода** = UCSIconAtOrigin;

**Описание** = Specifies or returns whether the UCS icon displays at the origin for a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;

### Нод Set_UCSIconAtOrigin

**Имя нода** = Set_UCSIconAtOrigin;

**Описание** = Specifies or returns whether the UCS icon displays at the origin for a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;
* (System.Object)bIconAtOrigin;

### Нод GetGridSpacing

**Имя нода** = GetGridSpacing;

**Описание** = Specifies or returns the grid spacing for a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;
* (System.Double)XSpacing;
* (System.Double)YSpacing;

### Нод SetGridSpacing

**Имя нода** = SetGridSpacing;

**Описание** = Specifies or returns the grid spacing for a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;
* (System.Double)XSpacing;
* (System.Double)YSpacing;

### Нод GetSnapSpacing

**Имя нода** = GetSnapSpacing;

**Описание** = Specifies or returns the snap spacing for a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;
* (System.Double)XSpacing;
* (System.Double)YSpacing;

### Нод SetSnapSpacing

**Имя нода** = SetSnapSpacing;

**Описание** = Specifies or returns the snap spacing for a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;
* (System.Double)XSpacing;
* (System.Double)YSpacing;

### Нод Display

**Имя нода** = Display;

**Описание** = Turns the display of a paperspace viewport on or off.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;
* (System.Object)bStatus;

### Нод TwistAngle

**Имя нода** = TwistAngle;

**Описание** = Specifies or returns the twist angle, in radians, for a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;

### Нод Set_TwistAngle

**Имя нода** = Set_TwistAngle;

**Описание** = Specifies or returns the twist angle, in radians, for a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;
* (System.Object)Angle;

### Нод LensLength

**Имя нода** = LensLength;

**Описание** = Specifies or returns the lens length used in perspective viewing for a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;

### Нод Set_LensLength

**Имя нода** = Set_LensLength;

**Описание** = Specifies or returns the lens length used in perspective viewing for a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;
* (System.Double)Length;

### Нод RemoveHiddenLines

**Имя нода** = RemoveHiddenLines;

**Описание** = Specifies or returns whether hidden line removal is turned on for a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;

### Нод Set_RemoveHiddenLines

**Имя нода** = Set_RemoveHiddenLines;

**Описание** = Specifies or returns whether hidden line removal is turned on for a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;
* (System.Object)bRemoval;

### Нод Target

**Имя нода** = Target;

**Описание** = Specifies or returns the target point for a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;

### Нод Set_Target

**Имя нода** = Set_Target;

**Описание** = Specifies or returns the target point for a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;
* (System.Object)targetPoint;

### Нод ArcSmoothness

**Имя нода** = ArcSmoothness;

**Описание** = Specifies or returns the smoothness of circles, arcs, and ellipses that display in a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;

### Нод Set_ArcSmoothness

**Имя нода** = Set_ArcSmoothness;

**Описание** = Specifies or returns the smoothness of circles, arcs, and ellipses that display in a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;
* (System.Object)arcSmooth;

### Нод VisualStyle

**Имя нода** = VisualStyle;

**Описание** = Not implemented. Specifies or returns the visual style of a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;

### Нод Set_VisualStyle

**Имя нода** = Set_VisualStyle;

**Описание** = Not implemented. Specifies or returns the visual style of a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;
* (System.Int32)pVisualStyleIndex;

### Нод ShadePlot

**Имя нода** = ShadePlot;

**Описание** = Specifies or returns the shade plot mode of a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;

### Нод Set_ShadePlot

**Имя нода** = Set_ShadePlot;

**Описание** = Specifies or returns the shade plot mode of a paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;
* (System.Int32)pShadePlotIndex;

### Нод ModelView

**Имя нода** = ModelView;

**Описание** = Not implemented. Specifies or returns the model view associated with the paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;

### Нод Set_ModelView

**Имя нода** = Set_ModelView;

**Описание** = Not implemented. Specifies or returns the model view associated with the paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;
* (System.Object)View;

### Нод SheetView

**Имя нода** = SheetView;

**Описание** = Not implemented. Specifies or returns the sheet view associated with the paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;

### Нод Set_SheetView

**Имя нода** = Set_SheetView;

**Описание** = Not implemented. Specifies or returns the sheet view associated with the paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;
* (System.Object)View;

### Нод LabelBlockId

**Имя нода** = LabelBlockId;

**Описание** = Not implemented. Specifies or returns the label block ID associated with the paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;

### Нод Set_LabelBlockId

**Имя нода** = Set_LabelBlockId;

**Описание** = Not implemented. Specifies or returns the label block ID associated with the paperspace viewport.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;
* (System.Object)ObjectID;

### Нод HasSheetView

**Имя нода** = HasSheetView;

**Описание** = Not implemented. Specifies whether the paperspace viewport is linked to a corresponding sheet view.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;

### Нод SyncModelView

**Имя нода** = SyncModelView;

**Описание** = Not implemented. Updates the paperspace viewport parameters with the parameters in the associated model view.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;

### Нод StandardScale2

**Имя нода** = StandardScale2;

**Описание** = Specifies the standard scale for the viewport;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;

### Нод Set_StandardScale2

**Имя нода** = Set_StandardScale2;

**Описание** = Specifies the standard scale for the viewport;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;
* (System.Int32)scale;

### Нод LayerPropertyOverrides

**Имя нода** = LayerPropertyOverrides;

**Описание** = Specifies whether the viewport has layer property overrides.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPViewport;

**Входные данные**:

* (System.Object)AcadPViewport;

## Группа NVP_nanoCAD_COM.OdaX.AcadPolyline

### Нод _AcadPolyline_Constructor

**Имя нода** = _AcadPolyline_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyline;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadPolyline_ConstructorCast

**Имя нода** = _AcadPolyline_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyline;

**Входные данные**:

* (System.Object)dynamic;

### Нод Coordinates

**Имя нода** = Coordinates;

**Описание** = Specifies or returns the current vertex of a polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyline;

**Входные данные**:

* (System.Object)AcadPolyline;

### Нод Set_Coordinates

**Имя нода** = Set_Coordinates;

**Описание** = Specifies or returns the current vertex of a polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyline;

**Входные данные**:

* (System.Object)AcadPolyline;
* (System.Object)Coordinates;

### Нод Normal

**Имя нода** = Normal;

**Описание** = Specifies or returns the x, y, and z coordinates of the normal direction vector.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyline;

**Входные данные**:

* (System.Object)AcadPolyline;

### Нод Set_Normal

**Имя нода** = Set_Normal;

**Описание** = Specifies or returns the x, y, and z coordinates of the normal direction vector.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyline;

**Входные данные**:

* (System.Object)AcadPolyline;
* (System.Object)Normal;

### Нод Thickness

**Имя нода** = Thickness;

**Описание** = z-direction value) of a polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyline;

**Входные данные**:

* (System.Object)AcadPolyline;

### Нод Set_Thickness

**Имя нода** = Set_Thickness;

**Описание** = z-direction value) of a polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyline;

**Входные данные**:

* (System.Object)AcadPolyline;
* (System.Double)Thickness;

### Нод AppendVertex

**Имя нода** = AppendVertex;

**Описание** = Appends a new vertex to a polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyline;

**Входные данные**:

* (System.Object)AcadPolyline;
* (System.Object)vertex;

### Нод Explode

**Имя нода** = Explode;

**Описание** = Explodes the polyline and returns the individual entities as an array of the object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyline;

**Входные данные**:

* (System.Object)AcadPolyline;

### Нод GetBulge

**Имя нода** = GetBulge;

**Описание** = Returns the bulge value for a vertex of a polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyline;

**Входные данные**:

* (System.Object)AcadPolyline;
* (System.Int32)Index;

### Нод SetBulge

**Имя нода** = SetBulge;

**Описание** = Specifies the bulge value for a vertex of a polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyline;

**Входные данные**:

* (System.Object)AcadPolyline;
* (System.Int32)Index;
* (System.Double)bulge;

### Нод GetWidth

**Имя нода** = GetWidth;

**Описание** = Returns the width for a segment of a polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyline;

**Входные данные**:

* (System.Object)AcadPolyline;
* (System.Int32)Index;
* (System.Double)StartWidth;

### Нод SetWidth

**Имя нода** = SetWidth;

**Описание** = Specifies the width for a segment of a polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyline;

**Входные данные**:

* (System.Object)AcadPolyline;
* (System.Int32)Index;
* (System.Double)StartWidth;

### Нод ConstantWidth

**Имя нода** = ConstantWidth;

**Описание** = Specifies or returns the width of the entire polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyline;

**Входные данные**:

* (System.Object)AcadPolyline;

### Нод Set_ConstantWidth

**Имя нода** = Set_ConstantWidth;

**Описание** = Specifies or returns the width of the entire polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyline;

**Входные данные**:

* (System.Object)AcadPolyline;
* (System.Double)Width;

### Нод Offset

**Имя нода** = Offset;

**Описание** = Not implemented. Creates a new polyline at a specified distance from an original lightweight polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyline;

**Входные данные**:

* (System.Object)AcadPolyline;
* (System.Double)Distance;

### Нод Elevation

**Имя нода** = Elevation;

**Описание** = Specifies or returns the elevation of the polyline relative to the z-axis of the object's coordinate system.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyline;

**Входные данные**:

* (System.Object)AcadPolyline;

### Нод Set_Elevation

**Имя нода** = Set_Elevation;

**Описание** = Specifies or returns the elevation of the polyline relative to the z-axis of the object's coordinate system.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyline;

**Входные данные**:

* (System.Object)AcadPolyline;
* (System.Double)Elevation;

### Нод Type

**Имя нода** = Type;

**Описание** = Specifies or returns the type of polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyline;

**Входные данные**:

* (System.Object)AcadPolyline;

### Нод Set_Type

**Имя нода** = Set_Type;

**Описание** = Specifies or returns the type of polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyline;

**Входные данные**:

* (System.Object)AcadPolyline;
* (System.Object)OdaX.AcPolylineType;

### Нод Closed

**Имя нода** = Closed;

**Описание** = Specifies or returns whether a polyline is open or closed.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyline;

**Входные данные**:

* (System.Object)AcadPolyline;

### Нод Set_Closed

**Имя нода** = Set_Closed;

**Описание** = Specifies or returns whether a polyline is open or closed.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyline;

**Входные данные**:

* (System.Object)AcadPolyline;
* (System.Object)fClose;

### Нод LinetypeGeneration

**Имя нода** = LinetypeGeneration;

**Описание** = Specifies or returns whether linetype generation is used for a polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyline;

**Входные данные**:

* (System.Object)AcadPolyline;

### Нод Set_LinetypeGeneration

**Имя нода** = Set_LinetypeGeneration;

**Описание** = Specifies or returns whether linetype generation is used for a polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyline;

**Входные данные**:

* (System.Object)AcadPolyline;
* (System.Object)bLinetypeGen;

### Нод Area

**Имя нода** = Area;

**Описание** = Specifies the area of a polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyline;

**Входные данные**:

* (System.Object)AcadPolyline;

### Нод Coordinate

**Имя нода** = Coordinate;

**Описание** = Specifies or returns the coordinate of a single vertex in an object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyline;

**Входные данные**:

* (System.Object)AcadPolyline;
* (System.Int32)Index;

### Нод Set_Coordinate

**Имя нода** = Set_Coordinate;

**Описание** = Specifies or returns the coordinate of a single vertex in an object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyline;

**Входные данные**:

* (System.Object)AcadPolyline;
* (System.Int32)Index;
* (System.Object)pVal;

### Нод Length

**Имя нода** = Length;

**Описание** = Specifies the length of a polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyline;

**Входные данные**:

* (System.Object)AcadPolyline;

## Группа NVP_nanoCAD_COM.OdaX.AcadPolygonMesh

### Нод _AcadPolygonMesh_Constructor

**Имя нода** = _AcadPolygonMesh_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolygonMesh;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadPolygonMesh_ConstructorCast

**Имя нода** = _AcadPolygonMesh_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolygonMesh;

**Входные данные**:

* (System.Object)dynamic;

### Нод Coordinates

**Имя нода** = Coordinates;

**Описание** = Specifies or returns the coordinates of a polygon mesh.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolygonMesh;

**Входные данные**:

* (System.Object)AcadPolygonMesh;

### Нод Set_Coordinates

**Имя нода** = Set_Coordinates;

**Описание** = Specifies or returns the coordinates of a polygon mesh.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolygonMesh;

**Входные данные**:

* (System.Object)AcadPolygonMesh;
* (System.Object)Coordinates;

### Нод MClose

**Имя нода** = MClose;

**Описание** = Specifies or returns whether the mesh is open or closed in the M direction.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolygonMesh;

**Входные данные**:

* (System.Object)AcadPolygonMesh;

### Нод Set_MClose

**Имя нода** = Set_MClose;

**Описание** = Specifies or returns whether the mesh is open or closed in the M direction.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolygonMesh;

**Входные данные**:

* (System.Object)AcadPolygonMesh;
* (System.Object)bClose;

### Нод NClose

**Имя нода** = NClose;

**Описание** = Specifies or returns whether the mesh is open or closed in the N direction.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolygonMesh;

**Входные данные**:

* (System.Object)AcadPolygonMesh;

### Нод Set_NClose

**Имя нода** = Set_NClose;

**Описание** = Specifies or returns whether the mesh is open or closed in the N direction.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolygonMesh;

**Входные данные**:

* (System.Object)AcadPolygonMesh;
* (System.Object)bClose;

### Нод MDensity

**Имя нода** = MDensity;

**Описание** = Specifies or returns the density of a polygon mesh in the M direction.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolygonMesh;

**Входные данные**:

* (System.Object)AcadPolygonMesh;

### Нод Set_MDensity

**Имя нода** = Set_MDensity;

**Описание** = Specifies or returns the density of a polygon mesh in the M direction.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolygonMesh;

**Входные данные**:

* (System.Object)AcadPolygonMesh;
* (System.Object)density;

### Нод NDensity

**Имя нода** = NDensity;

**Описание** = Specifies or returns the density of a polygon mesh in the N direction;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolygonMesh;

**Входные данные**:

* (System.Object)AcadPolygonMesh;

### Нод Set_NDensity

**Имя нода** = Set_NDensity;

**Описание** = Specifies or returns the density of a polygon mesh in the N direction;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolygonMesh;

**Входные данные**:

* (System.Object)AcadPolygonMesh;
* (System.Object)density;

### Нод MVertexCount

**Имя нода** = MVertexCount;

**Описание** = Returns the number of verticies in the M direction of a polygon mesh.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolygonMesh;

**Входные данные**:

* (System.Object)AcadPolygonMesh;

### Нод NVertexCount

**Имя нода** = NVertexCount;

**Описание** = Returns the number of verticies in the N direction of a polygon mesh.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolygonMesh;

**Входные данные**:

* (System.Object)AcadPolygonMesh;

### Нод Type

**Имя нода** = Type;

**Описание** = Specifies or returns the type of a polygon mesh.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolygonMesh;

**Входные данные**:

* (System.Object)AcadPolygonMesh;

### Нод Set_Type

**Имя нода** = Set_Type;

**Описание** = Specifies or returns the type of a polygon mesh.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolygonMesh;

**Входные данные**:

* (System.Object)AcadPolygonMesh;
* (System.Object)OdaX.AcPolymeshType;

### Нод AppendVertex

**Имя нода** = AppendVertex;

**Описание** = Appends a new vertex to a polygon mesh.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolygonMesh;

**Входные данные**:

* (System.Object)AcadPolygonMesh;
* (System.Object)vertex;

### Нод Explode

**Имя нода** = Explode;

**Описание** = Explodes the polygon mesh and returns the individual entities as an array of the object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolygonMesh;

**Входные данные**:

* (System.Object)AcadPolygonMesh;

### Нод Coordinate

**Имя нода** = Coordinate;

**Описание** = Specifies or returns the coordinate of a single vertex in a polygon mesh.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolygonMesh;

**Входные данные**:

* (System.Object)AcadPolygonMesh;
* (System.Int32)Index;

### Нод Set_Coordinate

**Имя нода** = Set_Coordinate;

**Описание** = Specifies or returns the coordinate of a single vertex in a polygon mesh.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolygonMesh;

**Входные данные**:

* (System.Object)AcadPolygonMesh;
* (System.Int32)Index;
* (System.Object)pVal;

## Группа NVP_nanoCAD_COM.OdaX.AcadPolyfaceMesh

### Нод _AcadPolyfaceMesh_Constructor

**Имя нода** = _AcadPolyfaceMesh_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyfaceMesh;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadPolyfaceMesh_ConstructorCast

**Имя нода** = _AcadPolyfaceMesh_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyfaceMesh;

**Входные данные**:

* (System.Object)dynamic;

### Нод Set_Coordinates

**Имя нода** = Set_Coordinates;

**Описание** = Specifies or returns the vertices of a polyface mesh.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyfaceMesh;

**Входные данные**:

* (System.Object)AcadPolyfaceMesh;
* (System.Object)Vertices;

### Нод Coordinates

**Имя нода** = Coordinates;

**Описание** = Specifies or returns the vertices of a polyface mesh.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyfaceMesh;

**Входные данные**:

* (System.Object)AcadPolyfaceMesh;

### Нод Coordinate

**Имя нода** = Coordinate;

**Описание** = Specifies or returns the coordinate of a single vertex in a polyface mesh.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyfaceMesh;

**Входные данные**:

* (System.Object)AcadPolyfaceMesh;
* (System.Int32)Index;

### Нод Set_Coordinate

**Имя нода** = Set_Coordinate;

**Описание** = Specifies or returns the coordinate of a single vertex in a polyface mesh.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyfaceMesh;

**Входные данные**:

* (System.Object)AcadPolyfaceMesh;
* (System.Int32)Index;
* (System.Object)pVal;

### Нод NumberOfVertices

**Имя нода** = NumberOfVertices;

**Описание** = Returns the number of vertices in a polyface mesh.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyfaceMesh;

**Входные данные**:

* (System.Object)AcadPolyfaceMesh;

### Нод NumberOfFaces

**Имя нода** = NumberOfFaces;

**Описание** = Specifies the number of faces in a polyface mesh.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyfaceMesh;

**Входные данные**:

* (System.Object)AcadPolyfaceMesh;

### Нод Set_Faces

**Имя нода** = Set_Faces;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPolyfaceMesh;

**Входные данные**:

* (System.Object)AcadPolyfaceMesh;
* (System.Object)rhs;

## Группа NVP_nanoCAD_COM.OdaX.AcadPoint

### Нод _AcadPoint_Constructor

**Имя нода** = _AcadPoint_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPoint;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadPoint_ConstructorCast

**Имя нода** = _AcadPoint_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPoint;

**Входные данные**:

* (System.Object)dynamic;

### Нод Coordinates

**Имя нода** = Coordinates;

**Описание** = Specifies or returns the current vertex of a point.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPoint;

**Входные данные**:

* (System.Object)AcadPoint;

### Нод Set_Coordinates

**Имя нода** = Set_Coordinates;

**Описание** = Specifies or returns the current vertex of a point.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPoint;

**Входные данные**:

* (System.Object)AcadPoint;
* (System.Object)Coordinates;

### Нод Normal

**Имя нода** = Normal;

**Описание** = Specifies or returns the x, y, and z coordinates of the normal direction vector.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPoint;

**Входные данные**:

* (System.Object)AcadPoint;

### Нод Set_Normal

**Имя нода** = Set_Normal;

**Описание** = Specifies or returns the x, y, and z coordinates of the normal direction vector.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPoint;

**Входные данные**:

* (System.Object)AcadPoint;
* (System.Object)Normal;

### Нод Thickness

**Имя нода** = Thickness;

**Описание** = z-direction value) of a point.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPoint;

**Входные данные**:

* (System.Object)AcadPoint;

### Нод Set_Thickness

**Имя нода** = Set_Thickness;

**Описание** = z-direction value) of a point.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPoint;

**Входные данные**:

* (System.Object)AcadPoint;
* (System.Double)Thickness;

## Группа NVP_nanoCAD_COM.OdaX.AcadPlotConfigurations

### Нод _AcadPlotConfigurations_Constructor

**Имя нода** = _AcadPlotConfigurations_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfigurations;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadPlotConfigurations_ConstructorCast

**Имя нода** = _AcadPlotConfigurations_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfigurations;

**Входные данные**:

* (System.Object)dynamic;

### Нод Item

**Имя нода** = Item;

**Описание** = Returns an object at a given index in the collection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfigurations;

**Входные данные**:

* (System.Object)AcadPlotConfigurations;
* (System.Object)Index;

### Нод Count

**Имя нода** = Count;

**Описание** = Returns the number of items in the collection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfigurations;

**Входные данные**:

* (System.Object)AcadPlotConfigurations;

### Нод Add

**Имя нода** = Add;

**Описание** = Creates an object and adds it to the collection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfigurations;

**Входные данные**:

* (System.Object)AcadPlotConfigurations;
* (System.String)Name;
* (System.Object)ModelType;

### Нод GetAll_PlotConfigurations

**Имя нода** = GetAll_PlotConfigurations;

**Описание** = Возвращает все Конфигурации печати в виде OdaX.AcadPlotConfiguration;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfigurations;

**Входные данные**:

* (System.Object)AcadPlotConfigurations;

## Группа NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration

### Нод _AcadPlotConfiguration_Constructor

**Имя нода** = _AcadPlotConfiguration_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadPlotConfiguration_ConstructorCast

**Имя нода** = _AcadPlotConfiguration_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)dynamic;

### Нод Name

**Имя нода** = Name;

**Описание** = Specifies or returns the name of the object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;

### Нод Set_Name

**Имя нода** = Set_Name;

**Описание** = Specifies or returns the name of the object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;
* (System.String)pName;

### Нод ConfigName

**Имя нода** = ConfigName;

**Описание** = Specifies or returns the name of a plot configuration file.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;

### Нод Set_ConfigName

**Имя нода** = Set_ConfigName;

**Описание** = Specifies or returns the name of a plot configuration file.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;
* (System.String)pName;

### Нод CanonicalMediaName

**Имя нода** = CanonicalMediaName;

**Описание** = Specifies or returns the name of the media size.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;

### Нод Set_CanonicalMediaName

**Имя нода** = Set_CanonicalMediaName;

**Описание** = Specifies or returns the name of the media size.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;
* (System.String)pName;

### Нод PaperUnits

**Имя нода** = PaperUnits;

**Описание** = Specifies or returns the units used for plot configuration properties.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;

### Нод Set_PaperUnits

**Имя нода** = Set_PaperUnits;

**Описание** = Specifies or returns the units used for plot configuration properties.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;
* (System.Object)OdaX.AcPlotPaperUnits;

### Нод PlotViewportBorders

**Имя нода** = PlotViewportBorders;

**Описание** = Specifies or returns whether to print the edges of viewports.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;

### Нод Set_PlotViewportBorders

**Имя нода** = Set_PlotViewportBorders;

**Описание** = Specifies or returns whether to print the edges of viewports.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;
* (System.Object)pViewportBorders;

### Нод ShowPlotStyles

**Имя нода** = ShowPlotStyles;

**Описание** = Specifies or returns whether plot styles are applied when displaying objects on the screen.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;

### Нод Set_ShowPlotStyles

**Имя нода** = Set_ShowPlotStyles;

**Описание** = Specifies or returns whether plot styles are applied when displaying objects on the screen.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;
* (System.Object)pStyles;

### Нод PlotRotation

**Имя нода** = PlotRotation;

**Описание** = Specifies or returns the rotation angle of the plot configuration.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;

### Нод Set_PlotRotation

**Имя нода** = Set_PlotRotation;

**Описание** = Specifies or returns the rotation angle of the plot configuration.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;
* (System.Object)OdaX.AcPlotRotation;

### Нод CenterPlot

**Имя нода** = CenterPlot;

**Описание** = Specifies or returns whether objects are centered on the printed media.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;

### Нод Set_CenterPlot

**Имя нода** = Set_CenterPlot;

**Описание** = Specifies or returns whether objects are centered on the printed media.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;
* (System.Object)pCentered;

### Нод PlotHidden

**Имя нода** = PlotHidden;

**Описание** = Specifies or returns whether objects are printed.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;

### Нод Set_PlotHidden

**Имя нода** = Set_PlotHidden;

**Описание** = Specifies or returns whether objects are printed.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;
* (System.Object)pHidden;

### Нод PlotType

**Имя нода** = PlotType;

**Описание** = Specifies or returns the method used to determine which objects print.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;

### Нод Set_PlotType

**Имя нода** = Set_PlotType;

**Описание** = Specifies or returns the method used to determine which objects print.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;
* (System.Object)OdaX.AcPlotType;

### Нод ViewToPlot

**Имя нода** = ViewToPlot;

**Описание** = Specifies or returns the name of the view to print.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;

### Нод Set_ViewToPlot

**Имя нода** = Set_ViewToPlot;

**Описание** = Specifies or returns the name of the view to print.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;
* (System.String)pName;

### Нод UseStandardScale

**Имя нода** = UseStandardScale;

**Описание** = Specifies or returns whether to print using a standard scale or custom scale.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;

### Нод Set_UseStandardScale

**Имя нода** = Set_UseStandardScale;

**Описание** = Specifies or returns whether to print using a standard scale or custom scale.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;
* (System.Object)pUseStdScale;

### Нод StandardScale

**Имя нода** = StandardScale;

**Описание** = Specifies or returns the standard scale value.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;

### Нод Set_StandardScale

**Имя нода** = Set_StandardScale;

**Описание** = Specifies or returns the standard scale value.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;
* (System.Object)OdaX.AcPlotScale;

### Нод GetCustomScale

**Имя нода** = GetCustomScale;

**Описание** = Returns the custom scale value.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;
* (System.Double)Numerator;
* (System.Double)Denominator;

### Нод SetCustomScale

**Имя нода** = SetCustomScale;

**Описание** = Sets the custom scale value.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;
* (System.Double)Numerator;
* (System.Double)Denominator;

### Нод ScaleLineweights

**Имя нода** = ScaleLineweights;

**Описание** = Specifies or returns whether lineweights are scaled during printing.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;

### Нод Set_ScaleLineweights

**Имя нода** = Set_ScaleLineweights;

**Описание** = Specifies or returns whether lineweights are scaled during printing.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;
* (System.Object)pScale;

### Нод PlotWithLineweights

**Имя нода** = PlotWithLineweights;

**Описание** = Specifies or returns whether objects print with lineweights.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;

### Нод Set_PlotWithLineweights

**Имя нода** = Set_PlotWithLineweights;

**Описание** = Specifies or returns whether objects print with lineweights.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;
* (System.Object)pPlot;

### Нод PlotViewportsFirst

**Имя нода** = PlotViewportsFirst;

**Описание** = Specifies or returns whether objects in paperspace print first.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;

### Нод Set_PlotViewportsFirst

**Имя нода** = Set_PlotViewportsFirst;

**Описание** = Specifies or returns whether objects in paperspace print first.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;
* (System.Object)pViewportsFirst;

### Нод StyleSheet

**Имя нода** = StyleSheet;

**Описание** = Specifies or returns the style sheet used for printing.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;

### Нод Set_StyleSheet

**Имя нода** = Set_StyleSheet;

**Описание** = Specifies or returns the style sheet used for printing.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;
* (System.String)pName;

### Нод GetPaperMargins

**Имя нода** = GetPaperMargins;

**Описание** = Returns the margins used for printing.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;
* (System.Object)LowerLeft;
* (System.Object)UpperRight;

### Нод GetPaperSize

**Имя нода** = GetPaperSize;

**Описание** = Returns the width and height of the specified media.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;
* (System.Double)Width;
* (System.Double)Height;

### Нод PlotOrigin

**Имя нода** = PlotOrigin;

**Описание** = Specifies or returns the origin point used for printing.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;

### Нод Set_PlotOrigin

**Имя нода** = Set_PlotOrigin;

**Описание** = Specifies or returns the origin point used for printing.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;
* (System.Object)pOrigin;

### Нод GetWindowToPlot

**Имя нода** = GetWindowToPlot;

**Описание** = Returns the coordinates that define the area to print.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;
* (System.Object)LowerLeft;
* (System.Object)UpperRight;

### Нод SetWindowToPlot

**Имя нода** = SetWindowToPlot;

**Описание** = Sets the coordinates that define the area to print.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;
* (System.Object)LowerLeft;
* (System.Object)UpperRight;

### Нод PlotWithPlotStyles

**Имя нода** = PlotWithPlotStyles;

**Описание** = Specifies or returns whether objects print with settings from the plot configuration file or settings from the drawing.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;

### Нод Set_PlotWithPlotStyles

**Имя нода** = Set_PlotWithPlotStyles;

**Описание** = Specifies or returns whether objects print with settings from the plot configuration file or settings from the drawing.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;
* (System.Object)pStyles;

### Нод ModelType

**Имя нода** = ModelType;

**Описание** = Returns whether the settings apply to objects in a modelspace layout only or both modelspace and paperspace layouts.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;

### Нод CopyFrom

**Имя нода** = CopyFrom;

**Описание** = Copies settings from a plot configuration file.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;
* (System.Object)pPlotConfig;

### Нод GetCanonicalMediaNames

**Имя нода** = GetCanonicalMediaNames;

**Описание** = Returns names of the media available for a print device.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;

### Нод GetPlotDeviceNames

**Имя нода** = GetPlotDeviceNames;

**Описание** = Returns print device names.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;

### Нод GetPlotStyleTableNames

**Имя нода** = GetPlotStyleTableNames;

**Описание** = Returns plot style table names.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;

### Нод RefreshPlotDeviceInfo

**Имя нода** = RefreshPlotDeviceInfo;

**Описание** = Updates print device information with the current settings.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;

### Нод GetLocaleMediaName

**Имя нода** = GetLocaleMediaName;

**Описание** = Returns the localized version of a media name.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlotConfiguration;

**Входные данные**:

* (System.Object)AcadPlotConfiguration;
* (System.String)Name;

## Группа NVP_nanoCAD_COM.OdaX.AcadPlaneSurface

### Нод _AcadPlaneSurface_Constructor

**Имя нода** = _AcadPlaneSurface_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlaneSurface;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadPlaneSurface_ConstructorCast

**Имя нода** = _AcadPlaneSurface_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPlaneSurface;

**Входные данные**:

* (System.Object)dynamic;

## Группа NVP_nanoCAD_COM.OdaX.AcadPaperSpace

### Нод _AcadPaperSpace_Constructor

**Имя нода** = _AcadPaperSpace_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPaperSpace;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadPaperSpace_ConstructorCast

**Имя нода** = _AcadPaperSpace_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPaperSpace;

**Входные данные**:

* (System.Object)dynamic;

### Нод AddPViewport

**Имя нода** = AddPViewport;

**Описание** = Adds a paperspace viewport to paperspace.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadPaperSpace;

**Входные данные**:

* (System.Object)AcadPaperSpace;
* (System.Object)Center;
* (System.Double)Width;
* (System.Double)Height;

## Группа NVP_nanoCAD_COM.OdaX.AcadOle

### Нод _AcadOle_Constructor

**Имя нода** = _AcadOle_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadOle;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadOle_ConstructorCast

**Имя нода** = _AcadOle_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadOle;

**Входные данные**:

* (System.Object)dynamic;

### Нод InsertionPoint

**Имя нода** = InsertionPoint;

**Описание** = upper left corner) of the OLE object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadOle;

**Входные данные**:

* (System.Object)AcadOle;

### Нод Set_InsertionPoint

**Имя нода** = Set_InsertionPoint;

**Описание** = upper left corner) of the OLE object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadOle;

**Входные данные**:

* (System.Object)AcadOle;
* (System.Object)insPoint;

### Нод Rotation

**Имя нода** = Rotation;

**Описание** = Specifies the rotation angle of the OLE object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadOle;

**Входные данные**:

* (System.Object)AcadOle;

### Нод Set_Rotation

**Имя нода** = Set_Rotation;

**Описание** = Specifies the rotation angle of the OLE object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadOle;

**Входные данные**:

* (System.Object)AcadOle;
* (System.Object)rot;

### Нод Width

**Имя нода** = Width;

**Описание** = Specifies the width of the OLE object box;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadOle;

**Входные данные**:

* (System.Object)AcadOle;

### Нод Set_Width

**Имя нода** = Set_Width;

**Описание** = Specifies the width of the OLE object box;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadOle;

**Входные данные**:

* (System.Object)AcadOle;
* (System.Double)Width;

### Нод Height

**Имя нода** = Height;

**Описание** = Specifies the height of the OLE object box;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadOle;

**Входные данные**:

* (System.Object)AcadOle;

### Нод Set_Height

**Имя нода** = Set_Height;

**Описание** = Specifies the height of the OLE object box;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadOle;

**Входные данные**:

* (System.Object)AcadOle;
* (System.Double)Height;

### Нод ScaleWidth

**Имя нода** = ScaleWidth;

**Описание** = Specifies the width of the object as a percentage of original width;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadOle;

**Входные данные**:

* (System.Object)AcadOle;

### Нод Set_ScaleWidth

**Имя нода** = Set_ScaleWidth;

**Описание** = Specifies the width of the object as a percentage of original width;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadOle;

**Входные данные**:

* (System.Object)AcadOle;
* (System.Double)swidth;

### Нод ScaleHeight

**Имя нода** = ScaleHeight;

**Описание** = Specifies the height of the object as a percentage of original height;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadOle;

**Входные данные**:

* (System.Object)AcadOle;

### Нод Set_ScaleHeight

**Имя нода** = Set_ScaleHeight;

**Описание** = Specifies the height of the object as a percentage of original height;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadOle;

**Входные данные**:

* (System.Object)AcadOle;
* (System.Double)sheight;

### Нод LockAspectRatio

**Имя нода** = LockAspectRatio;

**Описание** = Ensures the width and height of the object stay in proportion;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadOle;

**Входные данные**:

* (System.Object)AcadOle;

### Нод Set_LockAspectRatio

**Имя нода** = Set_LockAspectRatio;

**Описание** = Ensures the width and height of the object stay in proportion;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadOle;

**Входные данные**:

* (System.Object)AcadOle;
* (System.Object)aspect;

### Нод OleItemType

**Имя нода** = OleItemType;

**Описание** = Specifies whether the OLE object is linked to the original pasted file when opening object for editing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadOle;

**Входные данные**:

* (System.Object)AcadOle;

### Нод Set_OleItemType

**Имя нода** = Set_OleItemType;

**Описание** = Specifies whether the OLE object is linked to the original pasted file when opening object for editing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadOle;

**Входные данные**:

* (System.Object)AcadOle;
* (System.Object)OdaX.AcOleType;

### Нод OlePlotQuality

**Имя нода** = OlePlotQuality;

**Описание** = Controls plot quality of OLE object based on file type selected from list;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadOle;

**Входные данные**:

* (System.Object)AcadOle;

### Нод Set_OlePlotQuality

**Имя нода** = Set_OlePlotQuality;

**Описание** = Controls plot quality of OLE object based on file type selected from list;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadOle;

**Входные данные**:

* (System.Object)AcadOle;
* (System.Object)OdaX.AcOlePlotQuality;

### Нод OleSourceApp

**Имя нода** = OleSourceApp;

**Описание** = Application for editing OLE object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadOle;

**Входные данные**:

* (System.Object)AcadOle;

### Нод Set_OleSourceApp

**Имя нода** = Set_OleSourceApp;

**Описание** = Application for editing OLE object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadOle;

**Входные данные**:

* (System.Object)AcadOle;
* (System.String)srcApp;

## Группа NVP_nanoCAD_COM.OdaX.AcadObject

### Нод _AcadObject_Constructor

**Имя нода** = _AcadObject_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadObject;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadObject_ConstructorCast

**Имя нода** = _AcadObject_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadObject;

**Входные данные**:

* (System.Object)dynamic;

### Нод Handle

**Имя нода** = Handle;

**Описание** = Returns the handle of an object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadObject;

**Входные данные**:

* (System.Object)AcadObject;

### Нод ObjectName

**Имя нода** = ObjectName;

**Описание** = Returns the TeighaX class name of an object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadObject;

**Входные данные**:

* (System.Object)AcadObject;

### Нод GetXData

**Имя нода** = GetXData;

**Описание** = XData) associated with an object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadObject;

**Входные данные**:

* (System.Object)AcadObject;
* (System.String)AppName;
* (System.Object)XDataType;
* (System.Object)XDataValue;

### Нод SetXData

**Имя нода** = SetXData;

**Описание** = XData) associated with an object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadObject;

**Входные данные**:

* (System.Object)AcadObject;
* (System.Object)XDataType;
* (System.Object)XDataValue;

### Нод Delete

**Имя нода** = Delete;

**Описание** = Deletes a specified object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadObject;

**Входные данные**:

* (System.Object)AcadObject;

### Нод ObjectID

**Имя нода** = ObjectID;

**Описание** = Returns the object ID of an object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadObject;

**Входные данные**:

* (System.Object)AcadObject;

### Нод Application

**Имя нода** = Application;

**Описание** = Returns the Application object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadObject;

**Входные данные**:

* (System.Object)AcadObject;

### Нод Database

**Имя нода** = Database;

**Описание** = Returns the database in which the object belongs.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadObject;

**Входные данные**:

* (System.Object)AcadObject;

### Нод HasExtensionDictionary

**Имя нода** = HasExtensionDictionary;

**Описание** = Returns whether an object has an associated extension dictionary.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadObject;

**Входные данные**:

* (System.Object)AcadObject;

### Нод GetExtensionDictionary

**Имя нода** = GetExtensionDictionary;

**Описание** = Returns the extension dictionary associated with an object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadObject;

**Входные данные**:

* (System.Object)AcadObject;

### Нод OwnerID

**Имя нода** = OwnerID;

**Описание** = Returns the object ID of the parent object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadObject;

**Входные данные**:

* (System.Object)AcadObject;

### Нод Document

**Имя нода** = Document;

**Описание** = Returns the drawing to which an object belongs.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadObject;

**Входные данные**:

* (System.Object)AcadObject;

### Нод HiddenField_Erase

**Имя нода** = HiddenField_Erase;

**Описание** = Erases all the objects in a selection set;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadObject;

**Входные данные**:

* (System.Object)AcadObject;

## Группа NVP_nanoCAD_COM.OdaX.AcadMText

### Нод _AcadMText_Constructor

**Имя нода** = _AcadMText_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMText;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadMText_ConstructorCast

**Имя нода** = _AcadMText_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMText;

**Входные данные**:

* (System.Object)dynamic;

### Нод TextString

**Имя нода** = TextString;

**Описание** = Specifies or returns the characters of multline text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMText;

**Входные данные**:

* (System.Object)AcadMText;

### Нод Set_TextString

**Имя нода** = Set_TextString;

**Описание** = Specifies or returns the characters of multline text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMText;

**Входные данные**:

* (System.Object)AcadMText;
* (System.String)bstrText;

### Нод StyleName

**Имя нода** = StyleName;

**Описание** = Specifies or returns the style name of multiline text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMText;

**Входные данные**:

* (System.Object)AcadMText;

### Нод Set_StyleName

**Имя нода** = Set_StyleName;

**Описание** = Specifies or returns the style name of multiline text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMText;

**Входные данные**:

* (System.Object)AcadMText;
* (System.String)bstrName;

### Нод AttachmentPoint

**Имя нода** = AttachmentPoint;

**Описание** = Specifies or returns the position of multline text in relation to the insertion point.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMText;

**Входные данные**:

* (System.Object)AcadMText;

### Нод Set_AttachmentPoint

**Имя нода** = Set_AttachmentPoint;

**Описание** = Specifies or returns the position of multline text in relation to the insertion point.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMText;

**Входные данные**:

* (System.Object)AcadMText;
* (System.Object)OdaX.AcAttachmentPoint;

### Нод DrawingDirection

**Имя нода** = DrawingDirection;

**Описание** = Specifies or returns the direction in which the characters of multiline text display.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMText;

**Входные данные**:

* (System.Object)AcadMText;

### Нод Set_DrawingDirection

**Имя нода** = Set_DrawingDirection;

**Описание** = Specifies or returns the direction in which the characters of multiline text display.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMText;

**Входные данные**:

* (System.Object)AcadMText;
* (System.Object)OdaX.AcDrawingDirection;

### Нод Width

**Имя нода** = Width;

**Описание** = Specifies or returns the width of multiline text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMText;

**Входные данные**:

* (System.Object)AcadMText;

### Нод Set_Width

**Имя нода** = Set_Width;

**Описание** = Specifies or returns the width of multiline text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMText;

**Входные данные**:

* (System.Object)AcadMText;
* (System.Double)Width;

### Нод Height

**Имя нода** = Height;

**Описание** = Specifies or returns the height of multiline text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMText;

**Входные данные**:

* (System.Object)AcadMText;

### Нод Set_Height

**Имя нода** = Set_Height;

**Описание** = Specifies or returns the height of multiline text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMText;

**Входные данные**:

* (System.Object)AcadMText;
* (System.Double)Height;

### Нод Rotation

**Имя нода** = Rotation;

**Описание** = Specifies or returns the rotation angle of multiline text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMText;

**Входные данные**:

* (System.Object)AcadMText;

### Нод Set_Rotation

**Имя нода** = Set_Rotation;

**Описание** = Specifies or returns the rotation angle of multiline text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMText;

**Входные данные**:

* (System.Object)AcadMText;
* (System.Object)rotAngle;

### Нод InsertionPoint

**Имя нода** = InsertionPoint;

**Описание** = Specifies or returns the x, y, and z coordinates of the attribute text insertion point.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMText;

**Входные данные**:

* (System.Object)AcadMText;

### Нод Set_InsertionPoint

**Имя нода** = Set_InsertionPoint;

**Описание** = Specifies or returns the x, y, and z coordinates of the attribute text insertion point.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMText;

**Входные данные**:

* (System.Object)AcadMText;
* (System.Object)insPoint;

### Нод Normal

**Имя нода** = Normal;

**Описание** = Specifies or returns the x, y, and z coordinates of the normal direction vector.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMText;

**Входные данные**:

* (System.Object)AcadMText;

### Нод Set_Normal

**Имя нода** = Set_Normal;

**Описание** = Specifies or returns the x, y, and z coordinates of the normal direction vector.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMText;

**Входные данные**:

* (System.Object)AcadMText;
* (System.Object)Normal;

### Нод LineSpacingFactor

**Имя нода** = LineSpacingFactor;

**Описание** = Specifies or returns the line spacing factor of multiline text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMText;

**Входные данные**:

* (System.Object)AcadMText;

### Нод Set_LineSpacingFactor

**Имя нода** = Set_LineSpacingFactor;

**Описание** = Specifies or returns the line spacing factor of multiline text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMText;

**Входные данные**:

* (System.Object)AcadMText;
* (System.Double)factor;

### Нод LineSpacingStyle

**Имя нода** = LineSpacingStyle;

**Описание** = Specifies or returns the line spacing style of multiline text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMText;

**Входные данные**:

* (System.Object)AcadMText;

### Нод Set_LineSpacingStyle

**Имя нода** = Set_LineSpacingStyle;

**Описание** = Specifies or returns the line spacing style of multiline text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMText;

**Входные данные**:

* (System.Object)AcadMText;
* (System.Object)OdaX.AcLineSpacingStyle;

### Нод LineSpacingDistance

**Имя нода** = LineSpacingDistance;

**Описание** = Specifies or returns the distance between lines of multiline text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMText;

**Входные данные**:

* (System.Object)AcadMText;

### Нод Set_LineSpacingDistance

**Имя нода** = Set_LineSpacingDistance;

**Описание** = Specifies or returns the distance between lines of multiline text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMText;

**Входные данные**:

* (System.Object)AcadMText;
* (System.Double)Value;

### Нод BackgroundFill

**Имя нода** = BackgroundFill;

**Описание** = Specifies or returns whether the multiline text is filled with a background.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMText;

**Входные данные**:

* (System.Object)AcadMText;

### Нод Set_BackgroundFill

**Имя нода** = Set_BackgroundFill;

**Описание** = Specifies or returns whether the multiline text is filled with a background.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMText;

**Входные данные**:

* (System.Object)AcadMText;
* (System.Object)bUseBackgroundFill;

### Нод FieldCode

**Имя нода** = FieldCode;

**Описание** = Returns the characters and field codes of multiline text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMText;

**Входные данные**:

* (System.Object)AcadMText;

## Группа NVP_nanoCAD_COM.OdaX.AcadModelSpace

### Нод _AcadModelSpace_Constructor

**Имя нода** = _AcadModelSpace_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadModelSpace;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadModelSpace_ConstructorCast

**Имя нода** = _AcadModelSpace_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadModelSpace;

**Входные данные**:

* (System.Object)dynamic;

## Группа NVP_nanoCAD_COM.OdaX.AcadMLine

### Нод _AcadMLine_Constructor

**Имя нода** = _AcadMLine_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLine;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadMLine_ConstructorCast

**Имя нода** = _AcadMLine_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLine;

**Входные данные**:

* (System.Object)dynamic;

### Нод StyleName

**Имя нода** = StyleName;

**Описание** = Returns the style name of a multiline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLine;

**Входные данные**:

* (System.Object)AcadMLine;

### Нод Coordinates

**Имя нода** = Coordinates;

**Описание** = Specifies or returns the vertices of a multline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLine;

**Входные данные**:

* (System.Object)AcadMLine;

### Нод Set_Coordinates

**Имя нода** = Set_Coordinates;

**Описание** = Specifies or returns the vertices of a multline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLine;

**Входные данные**:

* (System.Object)AcadMLine;
* (System.Object)Vertices;

### Нод Justification

**Имя нода** = Justification;

**Описание** = Specifies or returns the orientation of a multiline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLine;

**Входные данные**:

* (System.Object)AcadMLine;

### Нод Set_Justification

**Имя нода** = Set_Justification;

**Описание** = Specifies or returns the orientation of a multiline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLine;

**Входные данные**:

* (System.Object)AcadMLine;
* (System.Object)OdaX.AcMLineJustification;

### Нод MLineScale

**Имя нода** = MLineScale;

**Описание** = Specifies or returns the scale of a multiline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLine;

**Входные данные**:

* (System.Object)AcadMLine;

### Нод Set_MLineScale

**Имя нода** = Set_MLineScale;

**Описание** = Specifies or returns the scale of a multiline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLine;

**Входные данные**:

* (System.Object)AcadMLine;
* (System.Double)scale;

## Группа NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle

### Нод _AcadMLeaderStyle_Constructor

**Имя нода** = _AcadMLeaderStyle_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadMLeaderStyle_ConstructorCast

**Имя нода** = _AcadMLeaderStyle_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)dynamic;

### Нод Name

**Имя нода** = Name;

**Описание** = Specifies or returns the name of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_Name

**Имя нода** = Set_Name;

**Описание** = Specifies or returns the name of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.String)Name;

### Нод Description

**Имя нода** = Description;

**Описание** = Specifies or returns the description of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_Description

**Имя нода** = Set_Description;

**Описание** = Specifies or returns the description of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.String)Description;

### Нод BitFlags

**Имя нода** = BitFlags;

**Описание** = Specifies or returns the operation bit set of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_BitFlags

**Имя нода** = Set_BitFlags;

**Описание** = Specifies or returns the operation bit set of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.Object)bitFlag;

### Нод ContentType

**Имя нода** = ContentType;

**Описание** = Specifies or returns the content type of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_ContentType

**Имя нода** = Set_ContentType;

**Описание** = Specifies or returns the content type of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.Object)OdaX.AcMLeaderContentType;

### Нод DrawMLeaderOrderType

**Имя нода** = DrawMLeaderOrderType;

**Описание** = Specifies or returns the order of multileader creation for a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_DrawMLeaderOrderType

**Имя нода** = Set_DrawMLeaderOrderType;

**Описание** = Specifies or returns the order of multileader creation for a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.Object)OdaX.AcDrawMLeaderOrderType;

### Нод DrawLeaderOrderType

**Имя нода** = DrawLeaderOrderType;

**Описание** = Specifies or returns the order of leader creation for a multileader of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_DrawLeaderOrderType

**Имя нода** = Set_DrawLeaderOrderType;

**Описание** = Specifies or returns the order of leader creation for a multileader of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.Object)OdaX.AcDrawLeaderOrderType;

### Нод MaxLeaderSegmentsPoints

**Имя нода** = MaxLeaderSegmentsPoints;

**Описание** = Specifies or returns the maximum number of segment points in multileader lines of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_MaxLeaderSegmentsPoints

**Имя нода** = Set_MaxLeaderSegmentsPoints;

**Описание** = Specifies or returns the maximum number of segment points in multileader lines of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.Int32)number;

### Нод FirstSegmentAngleConstraint

**Имя нода** = FirstSegmentAngleConstraint;

**Описание** = Specifies or returns the first segment angle constraint for a multileader of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_FirstSegmentAngleConstraint

**Имя нода** = Set_FirstSegmentAngleConstraint;

**Описание** = Specifies or returns the first segment angle constraint for a multileader of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.Object)OdaX.AcSegmentAngleType;

### Нод SecondSegmentAngleConstraint

**Имя нода** = SecondSegmentAngleConstraint;

**Описание** = Specifies or returns the second segment angle constraint for a multileader of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_SecondSegmentAngleConstraint

**Имя нода** = Set_SecondSegmentAngleConstraint;

**Описание** = Specifies or returns the second segment angle constraint for a multileader of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.Object)OdaX.AcSegmentAngleType;

### Нод LeaderLineType

**Имя нода** = LeaderLineType;

**Описание** = Specifies or returns the leader line type of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_LeaderLineType

**Имя нода** = Set_LeaderLineType;

**Описание** = Specifies or returns the leader line type of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.Object)OdaX.AcMLeaderType;

### Нод LeaderLineColor

**Имя нода** = LeaderLineColor;

**Описание** = Specifies or returns the color of leader lines for a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_LeaderLineColor

**Имя нода** = Set_LeaderLineColor;

**Описание** = Specifies or returns the color of leader lines for a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.Object)color;

### Нод LeaderLineTypeId

**Имя нода** = LeaderLineTypeId;

**Описание** = Specifies or returns the linetype of leader lines for a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_LeaderLineTypeId

**Имя нода** = Set_LeaderLineTypeId;

**Описание** = Specifies or returns the linetype of leader lines for a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.Object)Type;

### Нод LeaderLineWeight

**Имя нода** = LeaderLineWeight;

**Описание** = Specifies or returns the lineweight of leader lines for a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_LeaderLineWeight

**Имя нода** = Set_LeaderLineWeight;

**Описание** = Specifies or returns the lineweight of leader lines for a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.Object)OdaX.AcLineWeight;

### Нод EnableLanding

**Имя нода** = EnableLanding;

**Описание** = Specifies or returns whether the landing of leader lines is enabled for a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_EnableLanding

**Имя нода** = Set_EnableLanding;

**Описание** = Specifies or returns whether the landing of leader lines is enabled for a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.Object)enabled;

### Нод LandingGap

**Имя нода** = LandingGap;

**Описание** = Specifies or returns the gap between multiline text and the tail of leader lines for a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_LandingGap

**Имя нода** = Set_LandingGap;

**Описание** = Specifies or returns the gap between multiline text and the tail of leader lines for a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.Double)LandingGap;

### Нод EnableDogleg

**Имя нода** = EnableDogleg;

**Описание** = Specifies or returns whether dog legs are enabled for leader lines of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_EnableDogleg

**Имя нода** = Set_EnableDogleg;

**Описание** = Specifies or returns whether dog legs are enabled for leader lines of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.Object)enabled;

### Нод DoglegLength

**Имя нода** = DoglegLength;

**Описание** = Specifies or returns the dog leg length for leader lines of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_DoglegLength

**Имя нода** = Set_DoglegLength;

**Описание** = Specifies or returns the dog leg length for leader lines of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.Double)DoglegLength;

### Нод ArrowSymbol

**Имя нода** = ArrowSymbol;

**Описание** = Specifies or returns the arrow symbol of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_ArrowSymbol

**Имя нода** = Set_ArrowSymbol;

**Описание** = Specifies or returns the arrow symbol of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.String)Name;

### Нод ArrowSize

**Имя нода** = ArrowSize;

**Описание** = Specifies or returns the arrow size of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_ArrowSize

**Имя нода** = Set_ArrowSize;

**Описание** = Specifies or returns the arrow size of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.Double)size;

### Нод TextStyle

**Имя нода** = TextStyle;

**Описание** = Specifies or returns the text style of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_TextStyle

**Имя нода** = Set_TextStyle;

**Описание** = Specifies or returns the text style of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.String)Name;

### Нод TextLeftAttachmentType

**Имя нода** = TextLeftAttachmentType;

**Описание** = Specifies or returns how left-aligned text is positioned for a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_TextLeftAttachmentType

**Имя нода** = Set_TextLeftAttachmentType;

**Описание** = Specifies or returns how left-aligned text is positioned for a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.Object)OdaX.AcTextAttachmentType;

### Нод TextRightAttachmentType

**Имя нода** = TextRightAttachmentType;

**Описание** = Specifies or returns how right-aligned text is positioned for a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_TextRightAttachmentType

**Имя нода** = Set_TextRightAttachmentType;

**Описание** = Specifies or returns how right-aligned text is positioned for a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.Object)OdaX.AcTextAttachmentType;

### Нод TextColor

**Имя нода** = TextColor;

**Описание** = Specifies or returns the multline text color of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_TextColor

**Имя нода** = Set_TextColor;

**Описание** = Specifies or returns the multline text color of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.Object)color;

### Нод TextHeight

**Имя нода** = TextHeight;

**Описание** = Specifies or returns the multline text height of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_TextHeight

**Имя нода** = Set_TextHeight;

**Описание** = Specifies or returns the multline text height of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.Double)Height;

### Нод EnableFrameText

**Имя нода** = EnableFrameText;

**Описание** = Specifies or returns whether a frame displays around multline text of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_EnableFrameText

**Имя нода** = Set_EnableFrameText;

**Описание** = Specifies or returns whether a frame displays around multline text of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.Object)enabled;

### Нод AlignSpace

**Имя нода** = AlignSpace;

**Описание** = Specifies or returns the alignment space value of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_AlignSpace

**Имя нода** = Set_AlignSpace;

**Описание** = Specifies or returns the alignment space value of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.Double)AlignSpace;

### Нод Block

**Имя нода** = Block;

**Описание** = Specifies or returns the block content of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_Block

**Имя нода** = Set_Block;

**Описание** = Specifies or returns the block content of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.String)Name;

### Нод BlockColor

**Имя нода** = BlockColor;

**Описание** = Specifies or returns the color of block content for a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_BlockColor

**Имя нода** = Set_BlockColor;

**Описание** = Specifies or returns the color of block content for a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.Object)color;

### Нод EnableBlockScale

**Имя нода** = EnableBlockScale;

**Описание** = Specifies or returns whether the blocks are scaled for a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_EnableBlockScale

**Имя нода** = Set_EnableBlockScale;

**Описание** = Specifies or returns whether the blocks are scaled for a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.Object)enabled;

### Нод BlockScale

**Имя нода** = BlockScale;

**Описание** = Specifies or returns the block scale value for a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_BlockScale

**Имя нода** = Set_BlockScale;

**Описание** = Specifies or returns the block scale value for a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.Double)ScaleFactor;

### Нод EnableBlockRotation

**Имя нода** = EnableBlockRotation;

**Описание** = Specifies or returns whether blocks are rotated for a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_EnableBlockRotation

**Имя нода** = Set_EnableBlockRotation;

**Описание** = Specifies or returns whether blocks are rotated for a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.Object)enabled;

### Нод BlockRotation

**Имя нода** = BlockRotation;

**Описание** = Specifies or returns the rotation value for a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_BlockRotation

**Имя нода** = Set_BlockRotation;

**Описание** = Specifies or returns the rotation value for a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.Double)Rotation;

### Нод BlockConnectionType

**Имя нода** = BlockConnectionType;

**Описание** = Specifies or returns the block connection type of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_BlockConnectionType

**Имя нода** = Set_BlockConnectionType;

**Описание** = Specifies or returns the block connection type of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.Object)OdaX.AcBlockConnectionType;

### Нод ScaleFactor

**Имя нода** = ScaleFactor;

**Описание** = Specifies or returns the multileader scale of a specified multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_ScaleFactor

**Имя нода** = Set_ScaleFactor;

**Описание** = Specifies or returns the multileader scale of a specified multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.Double)scale;

### Нод OverwritePropChanged

**Имя нода** = OverwritePropChanged;

**Описание** = Returns whether properties were changed for a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Annotative

**Имя нода** = Annotative;

**Описание** = Specifies or returns the annotative status of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_Annotative

**Имя нода** = Set_Annotative;

**Описание** = Specifies or returns the annotative status of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.Object)Annotative;

### Нод BreakSize

**Имя нода** = BreakSize;

**Описание** = Specifies or returns the gap size used for breaking leader lines of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_BreakSize

**Имя нода** = Set_BreakSize;

**Описание** = Specifies or returns the gap size used for breaking leader lines of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.Double)size;

### Нод TextString

**Имя нода** = TextString;

**Описание** = Specifies or returns the multiline text text string of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_TextString

**Имя нода** = Set_TextString;

**Описание** = Specifies or returns the multiline text text string of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.String)Text;

### Нод TextAngleType

**Имя нода** = TextAngleType;

**Описание** = Specifies or returns the text angle type of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_TextAngleType

**Имя нода** = Set_TextAngleType;

**Описание** = Specifies or returns the text angle type of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.Object)OdaX.AcTextAngleType;

### Нод TextAlignmentType

**Имя нода** = TextAlignmentType;

**Описание** = Specifies or returns the text alignment type of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;

### Нод Set_TextAlignmentType

**Имя нода** = Set_TextAlignmentType;

**Описание** = Specifies or returns the text alignment type of a multileader style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderStyle;

**Входные данные**:

* (System.Object)AcadMLeaderStyle;
* (System.Object)OdaX.AcTextAlignmentType;

## Группа NVP_nanoCAD_COM.OdaX.AcadMLeaderLeader

### Нод _AcadMLeaderLeader_Constructor

**Имя нода** = _AcadMLeaderLeader_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderLeader;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadMLeaderLeader_ConstructorCast

**Имя нода** = _AcadMLeaderLeader_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderLeader;

**Входные данные**:

* (System.Object)dynamic;

### Нод LeaderType

**Имя нода** = LeaderType;

**Описание** = Specifies the leader type;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderLeader;

**Входные данные**:

* (System.Object)AcadMLeaderLeader;

### Нод Set_LeaderType

**Имя нода** = Set_LeaderType;

**Описание** = Specifies the leader type;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderLeader;

**Входные данные**:

* (System.Object)AcadMLeaderLeader;
* (System.Object)OdaX.AcMLeaderType;

### Нод LeaderLineColor

**Имя нода** = LeaderLineColor;

**Описание** = Specifies the color of the leader lines;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderLeader;

**Входные данные**:

* (System.Object)AcadMLeaderLeader;

### Нод Set_LeaderLineColor

**Имя нода** = Set_LeaderLineColor;

**Описание** = Specifies the color of the leader lines;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderLeader;

**Входные данные**:

* (System.Object)AcadMLeaderLeader;
* (System.Object)Type;

### Нод LeaderLineType

**Имя нода** = LeaderLineType;

**Описание** = Specifies the linetype of leader lines;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderLeader;

**Входные данные**:

* (System.Object)AcadMLeaderLeader;

### Нод Set_LeaderLineType

**Имя нода** = Set_LeaderLineType;

**Описание** = Specifies the linetype of leader lines;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderLeader;

**Входные данные**:

* (System.Object)AcadMLeaderLeader;
* (System.Object)Linetype;

### Нод LeaderLineWeight

**Имя нода** = LeaderLineWeight;

**Описание** = Specifies the line weight of leader lines;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderLeader;

**Входные данные**:

* (System.Object)AcadMLeaderLeader;

### Нод Set_LeaderLineWeight

**Имя нода** = Set_LeaderLineWeight;

**Описание** = Specifies the line weight of leader lines;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderLeader;

**Входные данные**:

* (System.Object)AcadMLeaderLeader;
* (System.Object)Lineweight;

### Нод ArrowheadType

**Имя нода** = ArrowheadType;

**Описание** = Specifies the type of leader arrowhead;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderLeader;

**Входные данные**:

* (System.Object)AcadMLeaderLeader;

### Нод Set_ArrowheadType

**Имя нода** = Set_ArrowheadType;

**Описание** = Specifies the type of leader arrowhead;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderLeader;

**Входные данные**:

* (System.Object)AcadMLeaderLeader;
* (System.Object)OdaX.AcDimArrowheadType;

### Нод ArrowheadSize

**Имя нода** = ArrowheadSize;

**Описание** = Specifies the size of leader arrowhead;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderLeader;

**Входные данные**:

* (System.Object)AcadMLeaderLeader;

### Нод Set_ArrowheadSize

**Имя нода** = Set_ArrowheadSize;

**Описание** = Specifies the size of leader arrowhead;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderLeader;

**Входные данные**:

* (System.Object)AcadMLeaderLeader;
* (System.Double)size;

### Нод ArrowheadBlock

**Имя нода** = ArrowheadBlock;

**Описание** = Specifies the block to use as the custom arrowhead for leader lines of multileader;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderLeader;

**Входные данные**:

* (System.Object)AcadMLeaderLeader;

### Нод Set_ArrowheadBlock

**Имя нода** = Set_ArrowheadBlock;

**Описание** = Specifies the block to use as the custom arrowhead for leader lines of multileader;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeaderLeader;

**Входные данные**:

* (System.Object)AcadMLeaderLeader;
* (System.String)BlockName;

## Группа NVP_nanoCAD_COM.OdaX.AcadMLeader

### Нод _AcadMLeader_Constructor

**Имя нода** = _AcadMLeader_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadMLeader_ConstructorCast

**Имя нода** = _AcadMLeader_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)dynamic;

### Нод ScaleFactor

**Имя нода** = ScaleFactor;

**Описание** = Specifies or returns the scale factor of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;

### Нод Set_ScaleFactor

**Имя нода** = Set_ScaleFactor;

**Описание** = Specifies or returns the scale factor of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Double)scale;

### Нод LeaderType

**Имя нода** = LeaderType;

**Описание** = Specifies or returns the type of multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;

### Нод Set_LeaderType

**Имя нода** = Set_LeaderType;

**Описание** = Specifies or returns the type of multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Object)OdaX.AcMLeaderType;

### Нод LeaderLineColor

**Имя нода** = LeaderLineColor;

**Описание** = Specifies or returns the color of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;

### Нод Set_LeaderLineColor

**Имя нода** = Set_LeaderLineColor;

**Описание** = Specifies or returns the color of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Object)Type;

### Нод LeaderLineType

**Имя нода** = LeaderLineType;

**Описание** = Specifies or returns the linetype of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;

### Нод Set_LeaderLineType

**Имя нода** = Set_LeaderLineType;

**Описание** = Specifies or returns the linetype of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Object)Linetype;

### Нод LeaderLineWeight

**Имя нода** = LeaderLineWeight;

**Описание** = Specifies or returns the lineweight of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;

### Нод Set_LeaderLineWeight

**Имя нода** = Set_LeaderLineWeight;

**Описание** = Specifies or returns the lineweight of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Object)Lineweight;

### Нод ArrowheadType

**Имя нода** = ArrowheadType;

**Описание** = Specifies or returns the arrowhead type of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;

### Нод Set_ArrowheadType

**Имя нода** = Set_ArrowheadType;

**Описание** = Specifies or returns the arrowhead type of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Object)OdaX.AcDimArrowheadType;

### Нод ArrowheadSize

**Имя нода** = ArrowheadSize;

**Описание** = Specifies or returns the arrowhead size of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;

### Нод Set_ArrowheadSize

**Имя нода** = Set_ArrowheadSize;

**Описание** = Specifies or returns the arrowhead size of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Double)size;

### Нод DogLegged

**Имя нода** = DogLegged;

**Описание** = Specifies or returns whether a dog leg is used for a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;

### Нод Set_DogLegged

**Имя нода** = Set_DogLegged;

**Описание** = Specifies or returns whether a dog leg is used for a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Object)val;

### Нод DoglegLength

**Имя нода** = DoglegLength;

**Описание** = Specifies or returns the dog leg length of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;

### Нод Set_DoglegLength

**Имя нода** = Set_DoglegLength;

**Описание** = Specifies or returns the dog leg length of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Double)DoglegLength;

### Нод ContentBlockName

**Имя нода** = ContentBlockName;

**Описание** = Specifies or returns the block name of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;

### Нод Set_ContentBlockName

**Имя нода** = Set_ContentBlockName;

**Описание** = Specifies or returns the block name of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.String)BlockName;

### Нод BlockConnectionType

**Имя нода** = BlockConnectionType;

**Описание** = Specifies or returns how a block connects with lines of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;

### Нод Set_BlockConnectionType

**Имя нода** = Set_BlockConnectionType;

**Описание** = Specifies or returns how a block connects with lines of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Object)OdaX.AcBlockConnectionType;

### Нод BlockScale

**Имя нода** = BlockScale;

**Описание** = Specify how leaders connect with content block;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;

### Нод Set_BlockScale

**Имя нода** = Set_BlockScale;

**Описание** = Specify how leaders connect with content block;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Object)factor;

### Нод TextString

**Имя нода** = TextString;

**Описание** = Specifies or returns the text string of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;

### Нод Set_TextString

**Имя нода** = Set_TextString;

**Описание** = Specifies or returns the text string of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.String)bstrText;

### Нод TextStyleName

**Имя нода** = TextStyleName;

**Описание** = Specifies or returns the style for text of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;

### Нод Set_TextStyleName

**Имя нода** = Set_TextStyleName;

**Описание** = Specifies or returns the style for text of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.String)bstrName;

### Нод TextJustify

**Имя нода** = TextJustify;

**Описание** = Specifies or returns the attachment point for text of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;

### Нод Set_TextJustify

**Имя нода** = Set_TextJustify;

**Описание** = Specifies or returns the attachment point for text of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Object)OdaX.AcAttachmentPoint;

### Нод TextDirection

**Имя нода** = TextDirection;

**Описание** = Specifies or returns the text direction for text of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;

### Нод Set_TextDirection

**Имя нода** = Set_TextDirection;

**Описание** = Specifies or returns the text direction for text of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Object)OdaX.AcDrawingDirection;

### Нод TextWidth

**Имя нода** = TextWidth;

**Описание** = Specifies or returns text width for text of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;

### Нод Set_TextWidth

**Имя нода** = Set_TextWidth;

**Описание** = Specifies or returns text width for text of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Double)Width;

### Нод TextHeight

**Имя нода** = TextHeight;

**Описание** = Specifies or returns text height for text of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;

### Нод Set_TextHeight

**Имя нода** = Set_TextHeight;

**Описание** = Specifies or returns text height for text of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Double)Height;

### Нод TextRotation

**Имя нода** = TextRotation;

**Описание** = Specifies or returns the rotation for text of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;

### Нод Set_TextRotation

**Имя нода** = Set_TextRotation;

**Описание** = Specifies or returns the rotation for text of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Object)rotAngle;

### Нод TextLineSpacingFactor

**Имя нода** = TextLineSpacingFactor;

**Описание** = Specifies or returns the line spacing factor for text of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;

### Нод Set_TextLineSpacingFactor

**Имя нода** = Set_TextLineSpacingFactor;

**Описание** = Specifies or returns the line spacing factor for text of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Double)factor;

### Нод TextLineSpacingDistance

**Имя нода** = TextLineSpacingDistance;

**Описание** = Specifies or returns the line spacing distance for text of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;

### Нод Set_TextLineSpacingDistance

**Имя нода** = Set_TextLineSpacingDistance;

**Описание** = Specifies or returns the line spacing distance for text of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Double)Value;

### Нод TextLineSpacingStyle

**Имя нода** = TextLineSpacingStyle;

**Описание** = Specifies or returns the line spacing style for text of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;

### Нод Set_TextLineSpacingStyle

**Имя нода** = Set_TextLineSpacingStyle;

**Описание** = Specifies or returns the line spacing style for text of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Object)OdaX.AcLineSpacingStyle;

### Нод TextBackgroundFill

**Имя нода** = TextBackgroundFill;

**Описание** = Specifies or returns whether a background is used used for text of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;

### Нод Set_TextBackgroundFill

**Имя нода** = Set_TextBackgroundFill;

**Описание** = Specifies or returns whether a background is used used for text of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Object)bUseBackgroundFill;

### Нод TextAttachmentDirection

**Имя нода** = TextAttachmentDirection;

**Описание** = Specifies if leaders connect with the text horizontally or vertically;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;

### Нод Set_TextAttachmentDirection

**Имя нода** = Set_TextAttachmentDirection;

**Описание** = Specifies if leaders connect with the text horizontally or vertically;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Object)OdaX.AcTextAttachmentDirection;

### Нод TextLeftAttachmentType

**Имя нода** = TextLeftAttachmentType;

**Описание** = Specifies or returns how leaders on the left connect with text of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;

### Нод Set_TextLeftAttachmentType

**Имя нода** = Set_TextLeftAttachmentType;

**Описание** = Specifies or returns how leaders on the left connect with text of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Object)OdaX.AcTextAttachmentType;

### Нод TextRightAttachmentType

**Имя нода** = TextRightAttachmentType;

**Описание** = Specifies or returns how leaders on the right connect with text of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;

### Нод Set_TextRightAttachmentType

**Имя нода** = Set_TextRightAttachmentType;

**Описание** = Specifies or returns how leaders on the right connect with text of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Object)OdaX.AcTextAttachmentType;

### Нод TextTopAttachmentType

**Имя нода** = TextTopAttachmentType;

**Описание** = Specifies how leaders on the top connect with the text;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;

### Нод Set_TextTopAttachmentType

**Имя нода** = Set_TextTopAttachmentType;

**Описание** = Specifies how leaders on the top connect with the text;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Object)OdaX.AcVerticalTextAttachmentType;

### Нод TextBottomAttachmentType

**Имя нода** = TextBottomAttachmentType;

**Описание** = Specifies how leaders on the bottom connect with the text;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;

### Нод Set_TextBottomAttachmentType

**Имя нода** = Set_TextBottomAttachmentType;

**Описание** = Specifies how leaders on the bottom connect with the text;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Object)OdaX.AcVerticalTextAttachmentType;

### Нод LandingGap

**Имя нода** = LandingGap;

**Описание** = Specify or returns the landing gap for text of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;

### Нод Set_LandingGap

**Имя нода** = Set_LandingGap;

**Описание** = Specify or returns the landing gap for text of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Double)gap;

### Нод ArrowheadBlock

**Имя нода** = ArrowheadBlock;

**Описание** = Specifies or returns the block to use as the custom arrowhead of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;

### Нод Set_ArrowheadBlock

**Имя нода** = Set_ArrowheadBlock;

**Описание** = Specifies or returns the block to use as the custom arrowhead of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.String)BlockName;

### Нод ContentBlockType

**Имя нода** = ContentBlockType;

**Описание** = Specifies or returns the type of block used as the custom arrowhead of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;

### Нод Set_ContentBlockType

**Имя нода** = Set_ContentBlockType;

**Описание** = Specifies or returns the type of block used as the custom arrowhead of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Object)OdaX.AcPredefBlockType;

### Нод LeaderCount

**Имя нода** = LeaderCount;

**Описание** = Returns the number of leader lines for a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;

### Нод AddLeader

**Имя нода** = AddLeader;

**Описание** = Adds a new cluster of leader lines to a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;

### Нод RemoveLeader

**Имя нода** = RemoveLeader;

**Описание** = Deletes a cluster of leader lines from a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Int32)leaderIndex;

### Нод AddLeaderLine

**Имя нода** = AddLeaderLine;

**Описание** = Adds a leader line to the leader cluster of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Int32)leaderIndex;
* (System.Object)pointArray;

### Нод AddLeaderLineEx

**Имя нода** = AddLeaderLineEx;

**Описание** = Adds a new leader line to a multileader using a start vertex for the leader line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Object)pointArray;

### Нод RemoveLeaderLine

**Имя нода** = RemoveLeaderLine;

**Описание** = Deletes a leader line from a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Int32)leaderLineIndex;

### Нод SetLeaderLineVertices

**Имя нода** = SetLeaderLineVertices;

**Описание** = Specifies the vertices for a leader line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Int32)leaderLineIndex;
* (System.Object)pointArray;

### Нод GetLeaderLineVertices

**Имя нода** = GetLeaderLineVertices;

**Описание** = Specifies the vertices for a leader line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Int32)leaderLineIndex;

### Нод Set_ContentType

**Имя нода** = Set_ContentType;

**Описание** = Specifies or returns the content type of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Object)OdaX.AcMLeaderContentType;

### Нод ContentType

**Имя нода** = ContentType;

**Описание** = Specifies or returns the content type of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;

### Нод GetLeaderIndex

**Имя нода** = GetLeaderIndex;

**Описание** = Returns the index of the leader cluster to which a specified leader line belongs.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Int32)leaderLineIndex;

### Нод GetLeaderLineIndexes

**Имя нода** = GetLeaderLineIndexes;

**Описание** = Returns the index of a leader line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Int32)leaderIndex;

### Нод GetVertexCount

**Имя нода** = GetVertexCount;

**Описание** = Returns the number of vertices in a leader line of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Int32)leaderLineIndex;

### Нод TextFrameDisplay

**Имя нода** = TextFrameDisplay;

**Описание** = Specifies or returns whether a text frame displays for a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;

### Нод Set_TextFrameDisplay

**Имя нода** = Set_TextFrameDisplay;

**Описание** = Specifies or returns whether a text frame displays for a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Object)pVal;

### Нод StyleName

**Имя нода** = StyleName;

**Описание** = Specifies or returns the style name of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;

### Нод Set_StyleName

**Имя нода** = Set_StyleName;

**Описание** = Specifies or returns the style name of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.String)Name;

### Нод GetDoglegDirection

**Имя нода** = GetDoglegDirection;

**Описание** = Returns the dog leg direction of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Int32)leaderIndex;

### Нод SetDoglegDirection

**Имя нода** = SetDoglegDirection;

**Описание** = Specifies the dog leg direction of a multileader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Int32)leaderIndex;
* (System.Object)dirVec;

### Нод GetBlockAttributeValue

**Имя нода** = GetBlockAttributeValue;

**Описание** = Returns the attribute value of a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Object)attdefId;

### Нод SetBlockAttributeValue

**Имя нода** = SetBlockAttributeValue;

**Описание** = Specifies the attribute value of a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMLeader;

**Входные данные**:

* (System.Object)AcadMLeader;
* (System.Object)attdefId;
* (System.String)Value;

## Группа NVP_nanoCAD_COM.OdaX.AcadMInsertBlock

### Нод _AcadMInsertBlock_Constructor

**Имя нода** = _AcadMInsertBlock_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMInsertBlock;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadMInsertBlock_ConstructorCast

**Имя нода** = _AcadMInsertBlock_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMInsertBlock;

**Входные данные**:

* (System.Object)dynamic;

### Нод Set_Columns

**Имя нода** = Set_Columns;

**Описание** = Specifies or returns the number of vertical columns in the block pattern.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMInsertBlock;

**Входные данные**:

* (System.Object)AcadMInsertBlock;
* (System.Object)NumColumns;

### Нод Columns

**Имя нода** = Columns;

**Описание** = Specifies or returns the number of vertical columns in the block pattern.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMInsertBlock;

**Входные данные**:

* (System.Object)AcadMInsertBlock;

### Нод Set_ColumnSpacing

**Имя нода** = Set_ColumnSpacing;

**Описание** = Specifies or returns the spacing between columns in the block pattern.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMInsertBlock;

**Входные данные**:

* (System.Object)AcadMInsertBlock;
* (System.Double)Spacing;

### Нод ColumnSpacing

**Имя нода** = ColumnSpacing;

**Описание** = Specifies or returns the spacing between columns in the block pattern.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMInsertBlock;

**Входные данные**:

* (System.Object)AcadMInsertBlock;

### Нод Set_Rows

**Имя нода** = Set_Rows;

**Описание** = Specifies or returns the number of horizontal rows in the block pattern.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMInsertBlock;

**Входные данные**:

* (System.Object)AcadMInsertBlock;
* (System.Object)NumRows;

### Нод Rows

**Имя нода** = Rows;

**Описание** = Specifies or returns the number of horizontal rows in the block pattern.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMInsertBlock;

**Входные данные**:

* (System.Object)AcadMInsertBlock;

### Нод Set_RowSpacing

**Имя нода** = Set_RowSpacing;

**Описание** = Specifies or returns the spacing between rows in the block pattern.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMInsertBlock;

**Входные данные**:

* (System.Object)AcadMInsertBlock;
* (System.Double)Spacing;

### Нод RowSpacing

**Имя нода** = RowSpacing;

**Описание** = Specifies or returns the spacing between rows in the block pattern.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMInsertBlock;

**Входные данные**:

* (System.Object)AcadMInsertBlock;

## Группа NVP_nanoCAD_COM.OdaX.AcadMaterials

### Нод _AcadMaterials_Constructor

**Имя нода** = _AcadMaterials_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMaterials;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadMaterials_ConstructorCast

**Имя нода** = _AcadMaterials_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMaterials;

**Входные данные**:

* (System.Object)dynamic;

### Нод Item

**Имя нода** = Item;

**Описание** = Returns an object at a given index in the collection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMaterials;

**Входные данные**:

* (System.Object)AcadMaterials;
* (System.Object)Index;

### Нод Count

**Имя нода** = Count;

**Описание** = Returns the number of items in the collection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMaterials;

**Входные данные**:

* (System.Object)AcadMaterials;

### Нод Add

**Имя нода** = Add;

**Описание** = Creates an object and adds it to the collection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMaterials;

**Входные данные**:

* (System.Object)AcadMaterials;
* (System.String)Name;

### Нод GetAll_Materials

**Имя нода** = GetAll_Materials;

**Описание** = Возвращает все Материалы в виде OdaX.AcadMaterial;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMaterials;

**Входные данные**:

* (System.Object)AcadMaterials;

## Группа NVP_nanoCAD_COM.OdaX.AcadMaterial

### Нод _AcadMaterial_Constructor

**Имя нода** = _AcadMaterial_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMaterial;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadMaterial_ConstructorCast

**Имя нода** = _AcadMaterial_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMaterial;

**Входные данные**:

* (System.Object)dynamic;

### Нод Description

**Имя нода** = Description;

**Описание** = Specifies or returns the description of a material.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMaterial;

**Входные данные**:

* (System.Object)AcadMaterial;

### Нод Set_Description

**Имя нода** = Set_Description;

**Описание** = Specifies or returns the description of a material.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMaterial;

**Входные данные**:

* (System.Object)AcadMaterial;
* (System.String)bstrDes;

### Нод Name

**Имя нода** = Name;

**Описание** = Specifies or returns the name of a material.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMaterial;

**Входные данные**:

* (System.Object)AcadMaterial;

### Нод Set_Name

**Имя нода** = Set_Name;

**Описание** = Specifies or returns the name of a material.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadMaterial;

**Входные данные**:

* (System.Object)AcadMaterial;
* (System.String)bstrName;

## Группа NVP_nanoCAD_COM.OdaX.AcadLWPolyline

### Нод _AcadLWPolyline_Constructor

**Имя нода** = _AcadLWPolyline_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLWPolyline;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadLWPolyline_ConstructorCast

**Имя нода** = _AcadLWPolyline_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLWPolyline;

**Входные данные**:

* (System.Object)dynamic;

### Нод Coordinates

**Имя нода** = Coordinates;

**Описание** = Specifies or returns the current vertex of a lightweight polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLWPolyline;

**Входные данные**:

* (System.Object)AcadLWPolyline;

### Нод Set_Coordinates

**Имя нода** = Set_Coordinates;

**Описание** = Specifies or returns the current vertex of a lightweight polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLWPolyline;

**Входные данные**:

* (System.Object)AcadLWPolyline;
* (System.Object)Coordinates;

### Нод Normal

**Имя нода** = Normal;

**Описание** = Specifies or returns the x, y, and z coordinates of the normal direction vector.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLWPolyline;

**Входные данные**:

* (System.Object)AcadLWPolyline;

### Нод Set_Normal

**Имя нода** = Set_Normal;

**Описание** = Specifies or returns the x, y, and z coordinates of the normal direction vector.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLWPolyline;

**Входные данные**:

* (System.Object)AcadLWPolyline;
* (System.Object)Normal;

### Нод Thickness

**Имя нода** = Thickness;

**Описание** = z-direction value) of a lightweight polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLWPolyline;

**Входные данные**:

* (System.Object)AcadLWPolyline;

### Нод Set_Thickness

**Имя нода** = Set_Thickness;

**Описание** = z-direction value) of a lightweight polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLWPolyline;

**Входные данные**:

* (System.Object)AcadLWPolyline;
* (System.Double)Thickness;

### Нод AddVertex

**Имя нода** = AddVertex;

**Описание** = Creates and adds a new vertex to a lightweight polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLWPolyline;

**Входные данные**:

* (System.Object)AcadLWPolyline;
* (System.Int32)Index;
* (System.Object)vertex;

### Нод Explode

**Имя нода** = Explode;

**Описание** = Explodes the lightweight polyline and returns the individual entities as an array of lines and arcs.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLWPolyline;

**Входные данные**:

* (System.Object)AcadLWPolyline;

### Нод GetBulge

**Имя нода** = GetBulge;

**Описание** = Returns the bulge value for a vertex of a lightweight polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLWPolyline;

**Входные данные**:

* (System.Object)AcadLWPolyline;
* (System.Int32)Index;

### Нод SetBulge

**Имя нода** = SetBulge;

**Описание** = Specifies the bulge value for a vertex of a lightweight polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLWPolyline;

**Входные данные**:

* (System.Object)AcadLWPolyline;
* (System.Int32)Index;
* (System.Double)bulge;

### Нод GetWidth

**Имя нода** = GetWidth;

**Описание** = Returns the width for a segment of a lightweight polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLWPolyline;

**Входные данные**:

* (System.Object)AcadLWPolyline;
* (System.Int32)Index;
* (System.Double)StartWidth;
* (System.Double)EndWidth;

### Нод SetWidth

**Имя нода** = SetWidth;

**Описание** = Specifies the width for a segment of a lightweight polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLWPolyline;

**Входные данные**:

* (System.Object)AcadLWPolyline;
* (System.Int32)Index;
* (System.Double)StartWidth;
* (System.Double)EndWidth;

### Нод ConstantWidth

**Имя нода** = ConstantWidth;

**Описание** = Specifies or returns the width of the entire lightweight polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLWPolyline;

**Входные данные**:

* (System.Object)AcadLWPolyline;

### Нод Set_ConstantWidth

**Имя нода** = Set_ConstantWidth;

**Описание** = Specifies or returns the width of the entire lightweight polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLWPolyline;

**Входные данные**:

* (System.Object)AcadLWPolyline;
* (System.Double)Width;

### Нод Offset

**Имя нода** = Offset;

**Описание** = Not implemented. Creates a new lightweight polyline at a specified distance from an original lightweight polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLWPolyline;

**Входные данные**:

* (System.Object)AcadLWPolyline;
* (System.Double)Distance;

### Нод Elevation

**Имя нода** = Elevation;

**Описание** = Specifies or returns the elevation of the lightweight polyline relative to the z-axis of the object's coordinate system.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLWPolyline;

**Входные данные**:

* (System.Object)AcadLWPolyline;

### Нод Set_Elevation

**Имя нода** = Set_Elevation;

**Описание** = Specifies or returns the elevation of the lightweight polyline relative to the z-axis of the object's coordinate system.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLWPolyline;

**Входные данные**:

* (System.Object)AcadLWPolyline;
* (System.Double)Elevation;

### Нод Area

**Имя нода** = Area;

**Описание** = Returns the area encompassed by a lightweight polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLWPolyline;

**Входные данные**:

* (System.Object)AcadLWPolyline;

### Нод Coordinate

**Имя нода** = Coordinate;

**Описание** = Specifies or returns the coordinate of a single vertex in an object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLWPolyline;

**Входные данные**:

* (System.Object)AcadLWPolyline;
* (System.Int32)Index;

### Нод Set_Coordinate

**Имя нода** = Set_Coordinate;

**Описание** = Specifies or returns the coordinate of a single vertex in an object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLWPolyline;

**Входные данные**:

* (System.Object)AcadLWPolyline;
* (System.Int32)Index;
* (System.Object)pVal;

### Нод Closed

**Имя нода** = Closed;

**Описание** = Specifies or returns whether a lightweight polyline is open or closed.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLWPolyline;

**Входные данные**:

* (System.Object)AcadLWPolyline;

### Нод Set_Closed

**Имя нода** = Set_Closed;

**Описание** = Specifies or returns whether a lightweight polyline is open or closed.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLWPolyline;

**Входные данные**:

* (System.Object)AcadLWPolyline;
* (System.Object)fClose;

### Нод LinetypeGeneration

**Имя нода** = LinetypeGeneration;

**Описание** = Specifies or returns whether linetype generation is used for a lightweight polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLWPolyline;

**Входные данные**:

* (System.Object)AcadLWPolyline;

### Нод Set_LinetypeGeneration

**Имя нода** = Set_LinetypeGeneration;

**Описание** = Specifies or returns whether linetype generation is used for a lightweight polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLWPolyline;

**Входные данные**:

* (System.Object)AcadLWPolyline;
* (System.Object)bLinetypeGen;

### Нод Length

**Имя нода** = Length;

**Описание** = Specifies the length of a lightweight polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLWPolyline;

**Входные данные**:

* (System.Object)AcadLWPolyline;

## Группа NVP_nanoCAD_COM.OdaX.AcadLoftedSurface

### Нод _AcadLoftedSurface_Constructor

**Имя нода** = _AcadLoftedSurface_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLoftedSurface;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadLoftedSurface_ConstructorCast

**Имя нода** = _AcadLoftedSurface_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLoftedSurface;

**Входные данные**:

* (System.Object)dynamic;

### Нод NumCrossSections

**Имя нода** = NumCrossSections;

**Описание** = Returns the number of cross-sections used to create a lofted surface.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLoftedSurface;

**Входные данные**:

* (System.Object)AcadLoftedSurface;

### Нод NumGuidePaths

**Имя нода** = NumGuidePaths;

**Описание** = Returns the number of guide paths used to create a lofted surface.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLoftedSurface;

**Входные данные**:

* (System.Object)AcadLoftedSurface;

### Нод SurfaceNormals

**Имя нода** = SurfaceNormals;

**Описание** = Specifies or returns which curves are normal to the cross-sections.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLoftedSurface;

**Входные данные**:

* (System.Object)AcadLoftedSurface;

### Нод Set_SurfaceNormals

**Имя нода** = Set_SurfaceNormals;

**Описание** = Specifies or returns which curves are normal to the cross-sections.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLoftedSurface;

**Входные данные**:

* (System.Object)AcadLoftedSurface;
* (System.Object)OdaX.AcLoftedSurfaceNormalType;

### Нод StartDraftAngle

**Имя нода** = StartDraftAngle;

**Описание** = Specifies or returns the angle of the first cross-section surface.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLoftedSurface;

**Входные данные**:

* (System.Object)AcadLoftedSurface;

### Нод Set_StartDraftAngle

**Имя нода** = Set_StartDraftAngle;

**Описание** = Specifies or returns the angle of the first cross-section surface.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLoftedSurface;

**Входные данные**:

* (System.Object)AcadLoftedSurface;
* (System.Object)StartDraftAngle;

### Нод StartDraftMagnitude

**Имя нода** = StartDraftMagnitude;

**Описание** = Specifies or returns the magnitude of the first cross-section surface.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLoftedSurface;

**Входные данные**:

* (System.Object)AcadLoftedSurface;

### Нод Set_StartDraftMagnitude

**Имя нода** = Set_StartDraftMagnitude;

**Описание** = Specifies or returns the magnitude of the first cross-section surface.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLoftedSurface;

**Входные данные**:

* (System.Object)AcadLoftedSurface;
* (System.Double)startDraftMag;

### Нод EndDraftAngle

**Имя нода** = EndDraftAngle;

**Описание** = Specifies or returns the angle of the last cross-section surface.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLoftedSurface;

**Входные данные**:

* (System.Object)AcadLoftedSurface;

### Нод Set_EndDraftAngle

**Имя нода** = Set_EndDraftAngle;

**Описание** = Specifies or returns the angle of the last cross-section surface.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLoftedSurface;

**Входные данные**:

* (System.Object)AcadLoftedSurface;
* (System.Object)EndDraftAngle;

### Нод EndDraftMagnitude

**Имя нода** = EndDraftMagnitude;

**Описание** = Specifies or returns the magnitude of the last cross-section surface.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLoftedSurface;

**Входные данные**:

* (System.Object)AcadLoftedSurface;

### Нод Set_EndDraftMagnitude

**Имя нода** = Set_EndDraftMagnitude;

**Описание** = Specifies or returns the magnitude of the last cross-section surface.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLoftedSurface;

**Входные данные**:

* (System.Object)AcadLoftedSurface;
* (System.Double)endDraftMag;

### Нод Closed

**Имя нода** = Closed;

**Описание** = Specfies or returns whether the lofted surface is open or closed.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLoftedSurface;

**Входные данные**:

* (System.Object)AcadLoftedSurface;

### Нод Set_Closed

**Имя нода** = Set_Closed;

**Описание** = Specfies or returns whether the lofted surface is open or closed.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLoftedSurface;

**Входные данные**:

* (System.Object)AcadLoftedSurface;
* (System.Object)bClosed;

## Группа NVP_nanoCAD_COM.OdaX.AcadLineTypes

### Нод _AcadLineTypes_Constructor

**Имя нода** = _AcadLineTypes_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLineTypes;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadLineTypes_ConstructorCast

**Имя нода** = _AcadLineTypes_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLineTypes;

**Входные данные**:

* (System.Object)dynamic;

### Нод Item

**Имя нода** = Item;

**Описание** = Returns an object at a given index in a collection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLineTypes;

**Входные данные**:

* (System.Object)AcadLineTypes;
* (System.Object)Index;

### Нод Count

**Имя нода** = Count;

**Описание** = Returns the number of items in the collection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLineTypes;

**Входные данные**:

* (System.Object)AcadLineTypes;

### Нод Add

**Имя нода** = Add;

**Описание** = Creates an object and adds it to the collection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLineTypes;

**Входные данные**:

* (System.Object)AcadLineTypes;
* (System.String)Name;

### Нод Load

**Имя нода** = Load;

**Описание** = Loads and makes available a linetype from an LIN file.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLineTypes;

**Входные данные**:

* (System.Object)AcadLineTypes;
* (System.String)Name;
* (System.String)FileName;

### Нод GetAll_LineTypes

**Имя нода** = GetAll_LineTypes;

**Описание** = Возвращает все Листы в виде OdaX.AcadLineType;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLineTypes;

**Входные данные**:

* (System.Object)AcadLineTypes;

## Группа NVP_nanoCAD_COM.OdaX.AcadLineType

### Нод _AcadLineType_Constructor

**Имя нода** = _AcadLineType_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLineType;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadLineType_ConstructorCast

**Имя нода** = _AcadLineType_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLineType;

**Входные данные**:

* (System.Object)dynamic;

### Нод Description

**Имя нода** = Description;

**Описание** = Specifies or returns the description of a linetype.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLineType;

**Входные данные**:

* (System.Object)AcadLineType;

### Нод Set_Description

**Имя нода** = Set_Description;

**Описание** = Specifies or returns the description of a linetype.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLineType;

**Входные данные**:

* (System.Object)AcadLineType;
* (System.String)bstrDes;

### Нод Name

**Имя нода** = Name;

**Описание** = Specifies or returns the name of a linetype.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLineType;

**Входные данные**:

* (System.Object)AcadLineType;

### Нод Set_Name

**Имя нода** = Set_Name;

**Описание** = Specifies or returns the name of a linetype.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLineType;

**Входные данные**:

* (System.Object)AcadLineType;
* (System.String)bstrName;

## Группа NVP_nanoCAD_COM.OdaX.AcadLine

### Нод _AcadLine_Constructor

**Имя нода** = _AcadLine_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLine;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadLine_ConstructorCast

**Имя нода** = _AcadLine_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLine;

**Входные данные**:

* (System.Object)dynamic;

### Нод StartPoint

**Имя нода** = StartPoint;

**Описание** = Specifies or returns the x, y, z coordinates of the start point of a line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLine;

**Входные данные**:

* (System.Object)AcadLine;

### Нод Set_StartPoint

**Имя нода** = Set_StartPoint;

**Описание** = Specifies or returns the x, y, z coordinates of the start point of a line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLine;

**Входные данные**:

* (System.Object)AcadLine;
* (System.Object)StartPoint;

### Нод EndPoint

**Имя нода** = EndPoint;

**Описание** = Specifies or returns the x, y, z coordinates of the end point of a line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLine;

**Входные данные**:

* (System.Object)AcadLine;

### Нод Set_EndPoint

**Имя нода** = Set_EndPoint;

**Описание** = Specifies or returns the x, y, z coordinates of the end point of a line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLine;

**Входные данные**:

* (System.Object)AcadLine;
* (System.Object)EndPoint;

### Нод Normal

**Имя нода** = Normal;

**Описание** = Specifies or returns the x, y, and z coordinates of the normal direction vector.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLine;

**Входные данные**:

* (System.Object)AcadLine;

### Нод Set_Normal

**Имя нода** = Set_Normal;

**Описание** = Specifies or returns the x, y, and z coordinates of the normal direction vector.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLine;

**Входные данные**:

* (System.Object)AcadLine;
* (System.Object)Normal;

### Нод Thickness

**Имя нода** = Thickness;

**Описание** = z-direction value) of a line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLine;

**Входные данные**:

* (System.Object)AcadLine;

### Нод Set_Thickness

**Имя нода** = Set_Thickness;

**Описание** = z-direction value) of a line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLine;

**Входные данные**:

* (System.Object)AcadLine;
* (System.Double)Thickness;

### Нод Offset

**Имя нода** = Offset;

**Описание** = Not implemented. Copies an existing line and places the copy at a specified distance from the existing line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLine;

**Входные данные**:

* (System.Object)AcadLine;
* (System.Double)Distance;

### Нод Delta

**Имя нода** = Delta;

**Описание** = Specifies the x, y, z, delta values of a line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLine;

**Входные данные**:

* (System.Object)AcadLine;

### Нод Length

**Имя нода** = Length;

**Описание** = Specifies the length of a line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLine;

**Входные данные**:

* (System.Object)AcadLine;

### Нод Angle

**Имя нода** = Angle;

**Описание** = Specifies the angle of a line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLine;

**Входные данные**:

* (System.Object)AcadLine;

## Группа NVP_nanoCAD_COM.OdaX.AcadLeader

### Нод _AcadLeader_Constructor

**Имя нода** = _AcadLeader_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLeader;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadLeader_ConstructorCast

**Имя нода** = _AcadLeader_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLeader;

**Входные данные**:

* (System.Object)dynamic;

### Нод Coordinates

**Имя нода** = Coordinates;

**Описание** = Specifies or returns the vertices of a leader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLeader;

**Входные данные**:

* (System.Object)AcadLeader;

### Нод Set_Coordinates

**Имя нода** = Set_Coordinates;

**Описание** = Specifies or returns the vertices of a leader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLeader;

**Входные данные**:

* (System.Object)AcadLeader;
* (System.Object)Coordinates;

### Нод Normal

**Имя нода** = Normal;

**Описание** = Specifies the x, y, and z coordinates of the normal direction vector.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLeader;

**Входные данные**:

* (System.Object)AcadLeader;

### Нод StyleName

**Имя нода** = StyleName;

**Описание** = Specifies or returns the style of a leader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLeader;

**Входные данные**:

* (System.Object)AcadLeader;

### Нод Set_StyleName

**Имя нода** = Set_StyleName;

**Описание** = Specifies or returns the style of a leader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLeader;

**Входные данные**:

* (System.Object)AcadLeader;
* (System.String)bstrName;

### Нод Type

**Имя нода** = Type;

**Описание** = Specifies or returns the type of a leader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLeader;

**Входные данные**:

* (System.Object)AcadLeader;

### Нод Set_Type

**Имя нода** = Set_Type;

**Описание** = Specifies or returns the type of a leader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLeader;

**Входные данные**:

* (System.Object)AcadLeader;
* (System.Object)OdaX.AcLeaderType;

### Нод Evaluate

**Имя нода** = Evaluate;

**Описание** = Evaluates and updates a leader and its associated entity and annotation.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLeader;

**Входные данные**:

* (System.Object)AcadLeader;

### Нод Coordinate

**Имя нода** = Coordinate;

**Описание** = Specifies or returns the coordinate of a single vertex in an object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLeader;

**Входные данные**:

* (System.Object)AcadLeader;
* (System.Int32)Index;

### Нод Set_Coordinate

**Имя нода** = Set_Coordinate;

**Описание** = Specifies or returns the coordinate of a single vertex in an object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLeader;

**Входные данные**:

* (System.Object)AcadLeader;
* (System.Int32)Index;
* (System.Object)pVal;

### Нод Annotation

**Имя нода** = Annotation;

**Описание** = Specifies or returns the annotation of a leader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLeader;

**Входные данные**:

* (System.Object)AcadLeader;

### Нод Set_Annotation

**Имя нода** = Set_Annotation;

**Описание** = Specifies or returns the annotation of a leader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLeader;

**Входные данные**:

* (System.Object)AcadLeader;
* (System.Object)pVal;

### Нод ArrowheadSize

**Имя нода** = ArrowheadSize;

**Описание** = Specifies or returns the arrowhead size of a leader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLeader;

**Входные данные**:

* (System.Object)AcadLeader;

### Нод Set_ArrowheadSize

**Имя нода** = Set_ArrowheadSize;

**Описание** = Specifies or returns the arrowhead size of a leader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLeader;

**Входные данные**:

* (System.Object)AcadLeader;
* (System.Double)size;

### Нод ArrowheadType

**Имя нода** = ArrowheadType;

**Описание** = Specifies or returns the arrowhead type of a leader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLeader;

**Входные данные**:

* (System.Object)AcadLeader;

### Нод Set_ArrowheadType

**Имя нода** = Set_ArrowheadType;

**Описание** = Specifies or returns the arrowhead type of a leader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLeader;

**Входные данные**:

* (System.Object)AcadLeader;
* (System.Object)OdaX.AcDimArrowheadType;

### Нод DimensionLineColor

**Имя нода** = DimensionLineColor;

**Описание** = Specifies or returns the line color of a leader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLeader;

**Входные данные**:

* (System.Object)AcadLeader;

### Нод Set_DimensionLineColor

**Имя нода** = Set_DimensionLineColor;

**Описание** = Specifies or returns the line color of a leader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLeader;

**Входные данные**:

* (System.Object)AcadLeader;
* (System.Object)Type;

### Нод DimensionLineWeight

**Имя нода** = DimensionLineWeight;

**Описание** = Specifies or returns the lineweight of a leader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLeader;

**Входные данные**:

* (System.Object)AcadLeader;

### Нод Set_DimensionLineWeight

**Имя нода** = Set_DimensionLineWeight;

**Описание** = Specifies or returns the lineweight of a leader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLeader;

**Входные данные**:

* (System.Object)AcadLeader;
* (System.Object)weight;

### Нод ScaleFactor

**Имя нода** = ScaleFactor;

**Описание** = Specifies or returns the scale of a leader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLeader;

**Входные данные**:

* (System.Object)AcadLeader;

### Нод Set_ScaleFactor

**Имя нода** = Set_ScaleFactor;

**Описание** = Specifies or returns the scale of a leader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLeader;

**Входные данные**:

* (System.Object)AcadLeader;
* (System.Object)factor;

### Нод VerticalTextPosition

**Имя нода** = VerticalTextPosition;

**Описание** = Specifies or returns the vertical placement of a leader annotation.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLeader;

**Входные данные**:

* (System.Object)AcadLeader;

### Нод Set_VerticalTextPosition

**Имя нода** = Set_VerticalTextPosition;

**Описание** = Specifies or returns the vertical placement of a leader annotation.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLeader;

**Входные данные**:

* (System.Object)AcadLeader;
* (System.Object)OdaX.AcDimVerticalJustification;

### Нод TextGap

**Имя нода** = TextGap;

**Описание** = Specifies or returns the distance between the leader and the annotation.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLeader;

**Входные данные**:

* (System.Object)AcadLeader;

### Нод Set_TextGap

**Имя нода** = Set_TextGap;

**Описание** = Specifies or returns the distance between the leader and the annotation.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLeader;

**Входные данные**:

* (System.Object)AcadLeader;
* (System.Double)Offset;

### Нод ArrowheadBlock

**Имя нода** = ArrowheadBlock;

**Описание** = Specifies or returns the block used for a custom arrowhead of a leader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLeader;

**Входные данные**:

* (System.Object)AcadLeader;

### Нод Set_ArrowheadBlock

**Имя нода** = Set_ArrowheadBlock;

**Описание** = Specifies or returns the block used for a custom arrowhead of a leader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLeader;

**Входные данные**:

* (System.Object)AcadLeader;
* (System.String)BlockName;

## Группа NVP_nanoCAD_COM.OdaX.AcadLayouts

### Нод _AcadLayouts_Constructor

**Имя нода** = _AcadLayouts_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayouts;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadLayouts_ConstructorCast

**Имя нода** = _AcadLayouts_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayouts;

**Входные данные**:

* (System.Object)dynamic;

### Нод Item

**Имя нода** = Item;

**Описание** = Returns an object at a given index in the collection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayouts;

**Входные данные**:

* (System.Object)AcadLayouts;
* (System.Object)Index;

### Нод Count

**Имя нода** = Count;

**Описание** = Returns the number of items in the collection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayouts;

**Входные данные**:

* (System.Object)AcadLayouts;

### Нод Add

**Имя нода** = Add;

**Описание** = Creates an object and adds it to the collection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayouts;

**Входные данные**:

* (System.Object)AcadLayouts;
* (System.String)Name;

### Нод GetAll_Layouts

**Имя нода** = GetAll_Layouts;

**Описание** = Возвращает все Листы в виде OdaX.AcadLayout;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayouts;

**Входные данные**:

* (System.Object)AcadLayers;

## Группа NVP_nanoCAD_COM.OdaX.AcadLayout

### Нод _AcadLayout_Constructor

**Имя нода** = _AcadLayout_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayout;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadLayout_ConstructorCast

**Имя нода** = _AcadLayout_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayout;

**Входные данные**:

* (System.Object)dynamic;

### Нод Block

**Имя нода** = Block;

**Описание** = Returns the block of a layout.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayout;

**Входные данные**:

* (System.Object)AcadLayout;

### Нод TabOrder

**Имя нода** = TabOrder;

**Описание** = Specifies or returns the order of the specified layout.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayout;

**Входные данные**:

* (System.Object)AcadLayout;

### Нод Set_TabOrder

**Имя нода** = Set_TabOrder;

**Описание** = Specifies or returns the order of the specified layout.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayout;

**Входные данные**:

* (System.Object)AcadLayout;
* (System.Object)pOrder;

## Группа NVP_nanoCAD_COM.OdaX.AcadLayers

### Нод _AcadLayers_Constructor

**Имя нода** = _AcadLayers_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayers;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadLayers_ConstructorCast

**Имя нода** = _AcadLayers_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayers;

**Входные данные**:

* (System.Object)dynamic;

### Нод Item

**Имя нода** = Item;

**Описание** = Returns an object at a given index in a collection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayers;

**Входные данные**:

* (System.Object)AcadLayers;
* (System.Object)Index;

### Нод Count

**Имя нода** = Count;

**Описание** = Returns the number of items in the collection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayers;

**Входные данные**:

* (System.Object)AcadLayers;

### Нод Add

**Имя нода** = Add;

**Описание** = Creates an object and adds it to the collection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayers;

**Входные данные**:

* (System.Object)AcadLayers;
* (System.String)Name;

### Нод GenerateUsageData

**Имя нода** = GenerateUsageData;

**Описание** = Generates data about whether a layer is in use.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayers;

**Входные данные**:

* (System.Object)AcadLayers;

### Нод GetAll_Layers

**Имя нода** = GetAll_Layers;

**Описание** = Возвращает все Слои в виде OdaX.AcadLayer;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayers;

**Входные данные**:

* (System.Object)AcadLayers;

## Группа NVP_nanoCAD_COM.OdaX.AcadLayer

### Нод _AcadLayer_Constructor

**Имя нода** = _AcadLayer_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayer;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadLayer_ConstructorCast

**Имя нода** = _AcadLayer_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayer;

**Входные данные**:

* (System.Object)dynamic;

### Нод color

**Имя нода** = color;

**Описание** = Specifies or returns the color of a layer.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayer;

**Входные данные**:

* (System.Object)AcadLayer;

### Нод Set_color

**Имя нода** = Set_color;

**Описание** = Specifies or returns the color of a layer.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayer;

**Входные данные**:

* (System.Object)AcadLayer;
* (System.Object)OdaX.AcColor;

### Нод TrueColor

**Имя нода** = TrueColor;

**Описание** = Specifies or returns the true color of a layer.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayer;

**Входные данные**:

* (System.Object)AcadLayer;

### Нод Set_TrueColor

**Имя нода** = Set_TrueColor;

**Описание** = Specifies or returns the true color of a layer.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayer;

**Входные данные**:

* (System.Object)AcadLayer;
* (System.Object)pColor;

### Нод Freeze

**Имя нода** = Freeze;

**Описание** = Specifies or returns whether objects on a layer are visible, editable, and printable.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayer;

**Входные данные**:

* (System.Object)AcadLayer;

### Нод Set_Freeze

**Имя нода** = Set_Freeze;

**Описание** = Specifies or returns whether objects on a layer are visible, editable, and printable.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayer;

**Входные данные**:

* (System.Object)AcadLayer;
* (System.Object)bFreeze;

### Нод LayerOn

**Имя нода** = LayerOn;

**Описание** = Specifies or returns whether objects on a layer are visible and printable.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayer;

**Входные данные**:

* (System.Object)AcadLayer;

### Нод Set_LayerOn

**Имя нода** = Set_LayerOn;

**Описание** = Specifies or returns whether objects on a layer are visible and printable.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayer;

**Входные данные**:

* (System.Object)AcadLayer;
* (System.Object)bOn;

### Нод Linetype

**Имя нода** = Linetype;

**Описание** = Specifies or returns the linetype of a layer.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayer;

**Входные данные**:

* (System.Object)AcadLayer;

### Нод Set_Linetype

**Имя нода** = Set_Linetype;

**Описание** = Specifies or returns the linetype of a layer.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayer;

**Входные данные**:

* (System.Object)AcadLayer;
* (System.String)Linetype;

### Нод Lock

**Имя нода** = Lock;

**Описание** = Specifies or returns whether objects on a layer are editable.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayer;

**Входные данные**:

* (System.Object)AcadLayer;

### Нод Set_Lock

**Имя нода** = Set_Lock;

**Описание** = Specifies or returns whether objects on a layer are editable.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayer;

**Входные данные**:

* (System.Object)AcadLayer;
* (System.Object)Block;

### Нод Name

**Имя нода** = Name;

**Описание** = Specifies or returns the name of a layer.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayer;

**Входные данные**:

* (System.Object)AcadLayer;

### Нод Set_Name

**Имя нода** = Set_Name;

**Описание** = Specifies or returns the name of a layer.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayer;

**Входные данные**:

* (System.Object)AcadLayer;
* (System.String)bstrName;

### Нод Plottable

**Имя нода** = Plottable;

**Описание** = Specifies or returns whether objects of a layer can be printed.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayer;

**Входные данные**:

* (System.Object)AcadLayer;

### Нод Set_Plottable

**Имя нода** = Set_Plottable;

**Описание** = Specifies or returns whether objects of a layer can be printed.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayer;

**Входные данные**:

* (System.Object)AcadLayer;
* (System.Object)bPlottable;

### Нод ViewportDefault

**Имя нода** = ViewportDefault;

**Описание** = Specifies or returns whether the layer is frozen in new viewports.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayer;

**Входные данные**:

* (System.Object)AcadLayer;

### Нод Set_ViewportDefault

**Имя нода** = Set_ViewportDefault;

**Описание** = Specifies or returns whether the layer is frozen in new viewports.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayer;

**Входные данные**:

* (System.Object)AcadLayer;
* (System.Object)bDefault;

### Нод PlotStyleName

**Имя нода** = PlotStyleName;

**Описание** = Specifies or returns the plot style name of a layer.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayer;

**Входные данные**:

* (System.Object)AcadLayer;

### Нод Set_PlotStyleName

**Имя нода** = Set_PlotStyleName;

**Описание** = Specifies or returns the plot style name of a layer.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayer;

**Входные данные**:

* (System.Object)AcadLayer;
* (System.String)plotStyle;

### Нод Lineweight

**Имя нода** = Lineweight;

**Описание** = Specifies or returns the lineweight of a layer.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayer;

**Входные данные**:

* (System.Object)AcadLayer;

### Нод Set_Lineweight

**Имя нода** = Set_Lineweight;

**Описание** = Specifies or returns the lineweight of a layer.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayer;

**Входные данные**:

* (System.Object)AcadLayer;
* (System.Object)Lineweight;

### Нод Description

**Имя нода** = Description;

**Описание** = Specifies or returns the description of a layer.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayer;

**Входные данные**:

* (System.Object)AcadLayer;

### Нод Set_Description

**Имя нода** = Set_Description;

**Описание** = Specifies or returns the description of a layer.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayer;

**Входные данные**:

* (System.Object)AcadLayer;
* (System.String)Description;

### Нод Used

**Имя нода** = Used;

**Описание** = Returns whether the layer is in use.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayer;

**Входные данные**:

* (System.Object)AcadLayer;

### Нод Material

**Имя нода** = Material;

**Описание** = Specifies or returns the material of a layer.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayer;

**Входные данные**:

* (System.Object)AcadLayer;

### Нод Set_Material

**Имя нода** = Set_Material;

**Описание** = Specifies or returns the material of a layer.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadLayer;

**Входные данные**:

* (System.Object)AcadLayer;
* (System.String)Material;

## Группа NVP_nanoCAD_COM.OdaX.AcadIdPair

### Нод _AcadIdPair_Constructor

**Имя нода** = _AcadIdPair_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadIdPair;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadIdPair_ConstructorCast

**Имя нода** = _AcadIdPair_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadIdPair;

**Входные данные**:

* (System.Object)dynamic;

### Нод IsCloned

**Имя нода** = IsCloned;

**Описание** = Determines if the source object in a CopyObjects operation has been cloned;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadIdPair;

**Входные данные**:

* (System.Object)AcadIdPair;

### Нод IsOwnerXlated

**Имя нода** = IsOwnerXlated;

**Описание** = Determines if the owning object in a CopyObjects operation has been translated;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadIdPair;

**Входные данные**:

* (System.Object)AcadIdPair;

### Нод IsPrimary

**Имя нода** = IsPrimary;

**Описание** = Determines if the source object in a CopyObjects operation was part of the primary set of objects being copied, or if it was simply owned by a member in the primary set;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadIdPair;

**Входные данные**:

* (System.Object)AcadIdPair;

### Нод key

**Имя нода** = key;

**Описание** = The object ID of the source object in the CopyObjects operation;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadIdPair;

**Входные данные**:

* (System.Object)AcadIdPair;

### Нод Value

**Имя нода** = Value;

**Описание** = The object ID of the newly created cloned object in the CopyObjects operation;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadIdPair;

**Входные данные**:

* (System.Object)AcadIdPair;

### Нод Application

**Имя нода** = Application;

**Описание** = Gets the Application object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadIdPair;

**Входные данные**:

* (System.Object)AcadIdPair;

## Группа NVP_nanoCAD_COM.OdaX.AcadHyperlinks

### Нод _AcadHyperlinks_Constructor

**Имя нода** = _AcadHyperlinks_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHyperlinks;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadHyperlinks_ConstructorCast

**Имя нода** = _AcadHyperlinks_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHyperlinks;

**Входные данные**:

* (System.Object)dynamic;

### Нод Item

**Имя нода** = Item;

**Описание** = Returns an object at a given index in a collection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHyperlinks;

**Входные данные**:

* (System.Object)AcadHyperlinks;
* (System.Object)Index;

### Нод Count

**Имя нода** = Count;

**Описание** = Returns the number of items in the collection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHyperlinks;

**Входные данные**:

* (System.Object)AcadHyperlinks;

### Нод Application

**Имя нода** = Application;

**Описание** = Returns the Application object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHyperlinks;

**Входные данные**:

* (System.Object)AcadHyperlinks;

### Нод Add

**Имя нода** = Add;

**Описание** = Creates an object and adds it to the collection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHyperlinks;

**Входные данные**:

* (System.Object)AcadHyperlinks;
* (System.String)Name;
* (System.Object)Description;
* (System.Object)NamedLocation;

### Нод GetAll_Links

**Имя нода** = GetAll_Links;

**Описание** = Возвращает все Ссылки в виде OdaX.AcadHyperlink;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHyperlinks;

**Входные данные**:

* (System.Object)AcadHyperlinks;

## Группа NVP_nanoCAD_COM.OdaX.AcadHyperlink

### Нод _AcadHyperlink_Constructor

**Имя нода** = _AcadHyperlink_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHyperlink;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadHyperlink_ConstructorCast

**Имя нода** = _AcadHyperlink_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHyperlink;

**Входные данные**:

* (System.Object)dynamic;

### Нод Set_URL

**Имя нода** = Set_URL;

**Описание** = Specifies or returns a link to an external file.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHyperlink;

**Входные данные**:

* (System.Object)AcadHyperlink;
* (System.String)URLPath;

### Нод URL

**Имя нода** = URL;

**Описание** = Specifies or returns a link to an external file.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHyperlink;

**Входные данные**:

* (System.Object)AcadHyperlink;

### Нод Set_URLDescription

**Имя нода** = Set_URLDescription;

**Описание** = Specifies or returns a text description of the URL.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHyperlink;

**Входные данные**:

* (System.Object)AcadHyperlink;
* (System.String)Description;

### Нод URLDescription

**Имя нода** = URLDescription;

**Описание** = Specifies or returns a text description of the URL.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHyperlink;

**Входные данные**:

* (System.Object)AcadHyperlink;

### Нод Application

**Имя нода** = Application;

**Описание** = Returns the Application object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHyperlink;

**Входные данные**:

* (System.Object)AcadHyperlink;

### Нод Delete

**Имя нода** = Delete;

**Описание** = Deletes a specified object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHyperlink;

**Входные данные**:

* (System.Object)AcadHyperlink;

### Нод Set_URLNamedLocation

**Имя нода** = Set_URLNamedLocation;

**Описание** = Specifies or returns the named location within a file that is referenced by a URL.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHyperlink;

**Входные данные**:

* (System.Object)AcadHyperlink;
* (System.String)Location;

### Нод URLNamedLocation

**Имя нода** = URLNamedLocation;

**Описание** = Specifies or returns the named location within a file that is referenced by a URL.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHyperlink;

**Входные данные**:

* (System.Object)AcadHyperlink;

## Группа NVP_nanoCAD_COM.OdaX.AcadHelix

### Нод _AcadHelix_Constructor

**Имя нода** = _AcadHelix_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHelix;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadHelix_ConstructorCast

**Имя нода** = _AcadHelix_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHelix;

**Входные данные**:

* (System.Object)dynamic;

### Нод Position

**Имя нода** = Position;

**Описание** = Specifies or returns the x, y, and z coordinates for the base center point of a helix.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHelix;

**Входные данные**:

* (System.Object)AcadHelix;

### Нод Set_Position

**Имя нода** = Set_Position;

**Описание** = Specifies or returns the x, y, and z coordinates for the base center point of a helix.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHelix;

**Входные данные**:

* (System.Object)AcadHelix;
* (System.Object)StartPoint;

### Нод Constrain

**Имя нода** = Constrain;

**Описание** = Specifies or returns the property that is contrained to remain constant when changing other properties of a helix.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHelix;

**Входные данные**:

* (System.Object)AcadHelix;

### Нод Set_Constrain

**Имя нода** = Set_Constrain;

**Описание** = Specifies or returns the property that is contrained to remain constant when changing other properties of a helix.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHelix;

**Входные данные**:

* (System.Object)AcadHelix;
* (System.Object)OdaX.AcHelixConstrainType;

### Нод Height

**Имя нода** = Height;

**Описание** = Specifies or returns the height of a helix.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHelix;

**Входные данные**:

* (System.Object)AcadHelix;

### Нод Set_Height

**Имя нода** = Set_Height;

**Описание** = Specifies or returns the height of a helix.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHelix;

**Входные данные**:

* (System.Object)AcadHelix;
* (System.Double)Length;

### Нод Turns

**Имя нода** = Turns;

**Описание** = Specifies or returns the number of turns for a helix.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHelix;

**Входные данные**:

* (System.Object)AcadHelix;

### Нод Set_Turns

**Имя нода** = Set_Turns;

**Описание** = Specifies or returns the number of turns for a helix.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHelix;

**Входные данные**:

* (System.Object)AcadHelix;
* (System.Object)Turns;

### Нод TurnHeight

**Имя нода** = TurnHeight;

**Описание** = Specifies or returns the height of a single turn for a helix.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHelix;

**Входные данные**:

* (System.Object)AcadHelix;

### Нод Set_TurnHeight

**Имя нода** = Set_TurnHeight;

**Описание** = Specifies or returns the height of a single turn for a helix.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHelix;

**Входные данные**:

* (System.Object)AcadHelix;
* (System.Double)Distance;

### Нод BaseRadius

**Имя нода** = BaseRadius;

**Описание** = Specifies or returns the radius of the base of a helix.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHelix;

**Входные данные**:

* (System.Object)AcadHelix;

### Нод Set_BaseRadius

**Имя нода** = Set_BaseRadius;

**Описание** = Specifies or returns the radius of the base of a helix.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHelix;

**Входные данные**:

* (System.Object)AcadHelix;
* (System.Double)Radius;

### Нод TopRadius

**Имя нода** = TopRadius;

**Описание** = Specifies or returns the radius of the top of a helix.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHelix;

**Входные данные**:

* (System.Object)AcadHelix;

### Нод Set_TopRadius

**Имя нода** = Set_TopRadius;

**Описание** = Specifies or returns the radius of the top of a helix.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHelix;

**Входные данные**:

* (System.Object)AcadHelix;
* (System.Double)Radius;

### Нод Twist

**Имя нода** = Twist;

**Описание** = Specifies or returns the direction in which a helix turns.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHelix;

**Входные данные**:

* (System.Object)AcadHelix;

### Нод Set_Twist

**Имя нода** = Set_Twist;

**Описание** = Specifies or returns the direction in which a helix turns.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHelix;

**Входные данные**:

* (System.Object)AcadHelix;
* (System.Object)OdaX.AcHelixTwistType;

### Нод TurnSlope

**Имя нода** = TurnSlope;

**Описание** = Returns the slope angle in which a helix turns along a path.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHelix;

**Входные данные**:

* (System.Object)AcadHelix;

### Нод TotalLength

**Имя нода** = TotalLength;

**Описание** = Specifies the length of a helix.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHelix;

**Входные данные**:

* (System.Object)AcadHelix;

## Группа NVP_nanoCAD_COM.OdaX.AcadHatch2

### Нод _AcadHatch2_Constructor

**Имя нода** = _AcadHatch2_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch2;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadHatch2_ConstructorCast

**Имя нода** = _AcadHatch2_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch2;

**Входные данные**:

* (System.Object)dynamic;

### Нод BackgroundColor

**Имя нода** = BackgroundColor;

**Описание** = Returns the hatch background color.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch2;

**Входные данные**:

* (System.Object)AcadHatch2;

### Нод Set_BackgroundColor

**Имя нода** = Set_BackgroundColor;

**Описание** = Returns the hatch background color.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch2;

**Входные данные**:

* (System.Object)AcadHatch2;
* (System.Object)pColor;

## Группа NVP_nanoCAD_COM.OdaX.AcadHatch

### Нод _AcadHatch_Constructor

**Имя нода** = _AcadHatch_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadHatch_ConstructorCast

**Имя нода** = _AcadHatch_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)dynamic;

### Нод Normal

**Имя нода** = Normal;

**Описание** = Specifies or returns the x, y, and z coordinates of the normal direction vector.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;

### Нод Set_Normal

**Имя нода** = Set_Normal;

**Описание** = Specifies or returns the x, y, and z coordinates of the normal direction vector.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;
* (System.Object)Normal;

### Нод NumberOfLoops

**Имя нода** = NumberOfLoops;

**Описание** = Returns the number of loops in a hatch.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;

### Нод PatternType

**Имя нода** = PatternType;

**Описание** = Returns the pattern type of a hatch.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;

### Нод PatternName

**Имя нода** = PatternName;

**Описание** = Returns the pattern name of a hatch.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;

### Нод PatternAngle

**Имя нода** = PatternAngle;

**Описание** = Specifies or returns the pattern angle of a hatch.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;

### Нод Set_PatternAngle

**Имя нода** = Set_PatternAngle;

**Описание** = Specifies or returns the pattern angle of a hatch.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;
* (System.Object)PatternAngle;

### Нод PatternScale

**Имя нода** = PatternScale;

**Описание** = Specifies or returns the pattern scale of a hatch.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;

### Нод Set_PatternScale

**Имя нода** = Set_PatternScale;

**Описание** = Specifies or returns the pattern scale of a hatch.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;
* (System.Object)PatternScale;

### Нод PatternSpace

**Имя нода** = PatternSpace;

**Описание** = Specifies or returns the pattern spacing of a hatch.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;

### Нод Set_PatternSpace

**Имя нода** = Set_PatternSpace;

**Описание** = Specifies or returns the pattern spacing of a hatch.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;
* (System.Double)PatternSpace;

### Нод ISOPenWidth

**Имя нода** = ISOPenWidth;

**Описание** = Specifies or returns the ISO pen width of an ISO hatch pattern.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;

### Нод Set_ISOPenWidth

**Имя нода** = Set_ISOPenWidth;

**Описание** = Specifies or returns the ISO pen width of an ISO hatch pattern.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;
* (System.Object)OdaX.AcISOPenWidth;

### Нод PatternDouble

**Имя нода** = PatternDouble;

**Описание** = Specifies or returns whether cross-hatching is used to create a double pattern.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;

### Нод Set_PatternDouble

**Имя нода** = Set_PatternDouble;

**Описание** = Specifies or returns whether cross-hatching is used to create a double pattern.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;
* (System.Object)bDouble;

### Нод Elevation

**Имя нода** = Elevation;

**Описание** = Specifies or returns the elevation of the hatch relative to the z-axis of the object's coordinate system.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;

### Нод Set_Elevation

**Имя нода** = Set_Elevation;

**Описание** = Specifies or returns the elevation of the hatch relative to the z-axis of the object's coordinate system.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;
* (System.Double)Elevation;

### Нод AssociativeHatch

**Имя нода** = AssociativeHatch;

**Описание** = Specifies or returns whether a hatch updates automatically when its boundary changes.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;

### Нод Set_AssociativeHatch

**Имя нода** = Set_AssociativeHatch;

**Описание** = Specifies or returns whether a hatch updates automatically when its boundary changes.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;
* (System.Object)fAssoc;

### Нод HatchStyle

**Имя нода** = HatchStyle;

**Описание** = Specifies or returns the hatch style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;

### Нод Set_HatchStyle

**Имя нода** = Set_HatchStyle;

**Описание** = Specifies or returns the hatch style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;
* (System.Object)OdaX.AcHatchStyle;

### Нод SetPattern

**Имя нода** = SetPattern;

**Описание** = Specifies the pattern of a hatch.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;
* (System.Int32)PatternType;
* (System.String)PatternName;

### Нод AppendOuterLoop_Single

**Имя нода** = AppendOuterLoop_Single;

**Описание** = Adds the outer loop (one entity) to a hatch.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;
* (System.Object)AcadEntity;

### Нод AppendOuterLoop_Group

**Имя нода** = AppendOuterLoop_Group;

**Описание** = Adds the outer loop (some entities) to a hatch.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;
* (System.Object)Список AcadEntity;

### Нод AppendInnerLoop

**Имя нода** = AppendInnerLoop;

**Описание** = Adds an inner loop to a hatch.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;
* (System.Object)ObjectArray;

### Нод InsertLoopAt

**Имя нода** = InsertLoopAt;

**Описание** = Adds a loop at a specified index of a hatch.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;
* (System.Int32)Index;
* (System.Object)OdaX.AcLoopType;
* (System.Object)ObjectArray;

### Нод GetLoopAt

**Имя нода** = GetLoopAt;

**Описание** = Returns a loop at a specified index of a hatch.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;
* (System.Int32)Index;
* (System.Object)ObjectArray;

### Нод Evaluate

**Имя нода** = Evaluate;

**Описание** = Calculates and fills the pattern of a hatch.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;

### Нод GradientColor1

**Имя нода** = GradientColor1;

**Описание** = Specifies or returns the gradient start color of a hatch.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;

### Нод Set_GradientColor1

**Имя нода** = Set_GradientColor1;

**Описание** = Specifies or returns the gradient start color of a hatch.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;
* (System.Object)pColor;

### Нод GradientColor2

**Имя нода** = GradientColor2;

**Описание** = Specifies or returns the gradient end color of a hatch.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;

### Нод Set_GradientColor2

**Имя нода** = Set_GradientColor2;

**Описание** = Specifies or returns the gradient end color of a hatch.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;
* (System.Object)pColor;

### Нод GradientAngle

**Имя нода** = GradientAngle;

**Описание** = Specifies or returns the gradient angle of a hatch.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;

### Нод Set_GradientAngle

**Имя нода** = Set_GradientAngle;

**Описание** = Specifies or returns the gradient angle of a hatch.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;
* (System.Object)GradientAngle;

### Нод GradientCentered

**Имя нода** = GradientCentered;

**Описание** = Specifies or returns whether the gradient is centered.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;

### Нод Set_GradientCentered

**Имя нода** = Set_GradientCentered;

**Описание** = Specifies or returns whether the gradient is centered.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;
* (System.Object)fCentered;

### Нод GradientName

**Имя нода** = GradientName;

**Описание** = Specifies or returns the pattern name of the gradient.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;

### Нод Set_GradientName

**Имя нода** = Set_GradientName;

**Описание** = Specifies or returns the pattern name of the gradient.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;
* (System.String)bstrName;

### Нод HatchObjectType

**Имя нода** = HatchObjectType;

**Описание** = Specifies or returns the hatch type.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;

### Нод Set_HatchObjectType

**Имя нода** = Set_HatchObjectType;

**Описание** = Specifies or returns the hatch type.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;
* (System.Object)OdaX.AcHatchObjectType;

### Нод Area

**Имя нода** = Area;

**Описание** = Not implemented. Returns the area of the hatch.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;

### Нод Origin

**Имя нода** = Origin;

**Описание** = Specifies or returns the coordinates for the origin of the hatch pattern.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;

### Нод Set_Origin

**Имя нода** = Set_Origin;

**Описание** = Specifies or returns the coordinates for the origin of the hatch pattern.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadHatch;

**Входные данные**:

* (System.Object)AcadHatch;
* (System.Object)Origin;

## Группа NVP_nanoCAD_COM.OdaX.AcadGroups

### Нод _AcadGroups_Constructor

**Имя нода** = _AcadGroups_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadGroups;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadGroups_ConstructorCast

**Имя нода** = _AcadGroups_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadGroups;

**Входные данные**:

* (System.Object)dynamic;

### Нод Item

**Имя нода** = Item;

**Описание** = Returns an object at a given index in the collection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadGroups;

**Входные данные**:

* (System.Object)AcadGroups;
* (System.Object)Index;

### Нод Count

**Имя нода** = Count;

**Описание** = Returns the number of items in the collection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadGroups;

**Входные данные**:

* (System.Object)AcadGroups;

### Нод Add

**Имя нода** = Add;

**Описание** = Creates an object and adds it to the collection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadGroups;

**Входные данные**:

* (System.Object)AcadGroups;
* (System.String)Name;

### Нод GetAll_Groups

**Имя нода** = GetAll_Groups;

**Описание** = Возвращает все Группы в виде OdaX.AcadHyperlink;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadGroups;

**Входные данные**:

* (System.Object)AcadHyperlinks;

## Группа NVP_nanoCAD_COM.OdaX.AcadGroup

### Нод _AcadGroup_Constructor

**Имя нода** = _AcadGroup_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadGroup;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadGroup_ConstructorCast

**Имя нода** = _AcadGroup_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadGroup;

**Входные данные**:

* (System.Object)dynamic;

### Нод Item

**Имя нода** = Item;

**Описание** = Returns an object at a given index in the group.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadGroup;

**Входные данные**:

* (System.Object)AcadGroup;
* (System.Object)Index;

### Нод Count

**Имя нода** = Count;

**Описание** = Returns the number of items in the group.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadGroup;

**Входные данные**:

* (System.Object)AcadGroup;

### Нод Set_TrueColor

**Имя нода** = Set_TrueColor;

**Описание** = Specifies the true color of all entities in the group.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadGroup;

**Входные данные**:

* (System.Object)AcadGroup;
* (System.Object)rhs;

### Нод Set_Layer

**Имя нода** = Set_Layer;

**Описание** = Specifies the layer of all entites in the group.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadGroup;

**Входные данные**:

* (System.Object)AcadGroup;
* (System.String)rhs;

### Нод Set_Linetype

**Имя нода** = Set_Linetype;

**Описание** = Specifies the linetype of all entites in the group.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadGroup;

**Входные данные**:

* (System.Object)AcadGroup;
* (System.String)rhs;

### Нод Set_LinetypeScale

**Имя нода** = Set_LinetypeScale;

**Описание** = Specifies the linetype scale of all entites in the group.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadGroup;

**Входные данные**:

* (System.Object)AcadGroup;
* (System.Object)rhs;

### Нод Set_Visible

**Имя нода** = Set_Visible;

**Описание** = Specifies the visibility of all entites in the group.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadGroup;

**Входные данные**:

* (System.Object)AcadGroup;
* (System.Object)rhs;

### Нод Highlight

**Имя нода** = Highlight;

**Описание** = Not implemented. Sets the highlight status of all entites in the group.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadGroup;

**Входные данные**:

* (System.Object)AcadGroup;
* (System.Object)HighlightFlag;

### Нод Set_PlotStyleName

**Имя нода** = Set_PlotStyleName;

**Описание** = Specifies the plot style of all entites in the group.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadGroup;

**Входные данные**:

* (System.Object)AcadGroup;
* (System.String)rhs;

### Нод Set_Lineweight

**Имя нода** = Set_Lineweight;

**Описание** = Specifies the lineweight of all entites in the group.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadGroup;

**Входные данные**:

* (System.Object)AcadGroup;
* (System.Object)rhs;

### Нод Name

**Имя нода** = Name;

**Описание** = Specifies or returns the name of the group.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadGroup;

**Входные данные**:

* (System.Object)AcadGroup;

### Нод Set_Name

**Имя нода** = Set_Name;

**Описание** = Specifies or returns the name of the group.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadGroup;

**Входные данные**:

* (System.Object)AcadGroup;
* (System.String)pVal;

### Нод AppendItems

**Имя нода** = AppendItems;

**Описание** = Adds one or more objects to the group.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadGroup;

**Входные данные**:

* (System.Object)AcadGroup;
* (System.Object)Objects;

### Нод RemoveItems

**Имя нода** = RemoveItems;

**Описание** = Removes objects from the group.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadGroup;

**Входные данные**:

* (System.Object)AcadGroup;
* (System.Object)Objects;

### Нод Update

**Имя нода** = Update;

**Описание** = Updates the group on the screen.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadGroup;

**Входные данные**:

* (System.Object)AcadGroup;

### Нод Set_Material

**Имя нода** = Set_Material;

**Описание** = Specifies the material assigned to all entities in the group.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadGroup;

**Входные данные**:

* (System.Object)AcadGroup;
* (System.String)rhs;

### Нод Set_color

**Имя нода** = Set_color;

**Описание** = Specifies the color of all entities in the group.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadGroup;

**Входные данные**:

* (System.Object)AcadGroup;
* (System.Object)OdaX.AcColor;

## Группа NVP_nanoCAD_COM.OdaX.AcadFileDependency

### Нод _AcadFileDependency_Constructor

**Имя нода** = _AcadFileDependency_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadFileDependency;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadFileDependency_ConstructorCast

**Имя нода** = _AcadFileDependency_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadFileDependency;

**Входные данные**:

* (System.Object)dynamic;

### Нод FullFileName

**Имя нода** = FullFileName;

**Описание** = Returns the path and file name of an entry in the File Dependencies list.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadFileDependency;

**Входные данные**:

* (System.Object)AcadFileDependency;

### Нод FileName

**Имя нода** = FileName;

**Описание** = Returns the file name of an entry in the File Dependencies list.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadFileDependency;

**Входные данные**:

* (System.Object)AcadFileDependency;

### Нод FoundPath

**Имя нода** = FoundPath;

**Описание** = Returns the alternative path for an entry in the File Dependencies list.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadFileDependency;

**Входные данные**:

* (System.Object)AcadFileDependency;

### Нод FingerprintGuid

**Имя нода** = FingerprintGuid;

**Описание** = Returns the fingerprint GUID for an entry in the File Dependencies list.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadFileDependency;

**Входные данные**:

* (System.Object)AcadFileDependency;

### Нод VersionGuid

**Имя нода** = VersionGuid;

**Описание** = Returns the version GUID for an entry in the File Dependencies list.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadFileDependency;

**Входные данные**:

* (System.Object)AcadFileDependency;

### Нод Feature

**Имя нода** = Feature;

**Описание** = Returns the feature, such as XRef, for an entry in the File Dependencies list.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadFileDependency;

**Входные данные**:

* (System.Object)AcadFileDependency;

### Нод IsModified

**Имя нода** = IsModified;

**Описание** = Returns whether the entry in the File Dependencies list has been changed.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadFileDependency;

**Входные данные**:

* (System.Object)AcadFileDependency;

### Нод AffectsGraphics

**Имя нода** = AffectsGraphics;

**Описание** = Returns whether an entry in the File Dependencies list affects graphics.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadFileDependency;

**Входные данные**:

* (System.Object)AcadFileDependency;

### Нод Index

**Имя нода** = Index;

**Описание** = Returns the index of an entry in the File Dependencies list.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadFileDependency;

**Входные данные**:

* (System.Object)AcadFileDependency;

### Нод TimeStamp

**Имя нода** = TimeStamp;

**Описание** = Returns the timestamp of an entry in the File Dependencies list.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadFileDependency;

**Входные данные**:

* (System.Object)AcadFileDependency;

### Нод FileSize

**Имя нода** = FileSize;

**Описание** = Returns the file size of an entry in the File Dependencies list.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadFileDependency;

**Входные данные**:

* (System.Object)AcadFileDependency;

### Нод ReferenceCount

**Имя нода** = ReferenceCount;

**Описание** = Returns the reference count of an entry in the File Dependencies list.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadFileDependency;

**Входные данные**:

* (System.Object)AcadFileDependency;

## Группа NVP_nanoCAD_COM.OdaX.AcadFileDependencies

### Нод _AcadFileDependencies_Constructor

**Имя нода** = _AcadFileDependencies_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadFileDependencies;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadFileDependencies_ConstructorCast

**Имя нода** = _AcadFileDependencies_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadFileDependencies;

**Входные данные**:

* (System.Object)dynamic;

### Нод Item

**Имя нода** = Item;

**Описание** = Returns an object at a given index in the collection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadFileDependencies;

**Входные данные**:

* (System.Object)AcadFileDependencies;
* (System.Object)Index;

### Нод Application

**Имя нода** = Application;

**Описание** = Returns the Application object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadFileDependencies;

**Входные данные**:

* (System.Object)AcadFileDependencies;

### Нод Count

**Имя нода** = Count;

**Описание** = Returns the number of items in the collection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadFileDependencies;

**Входные данные**:

* (System.Object)AcadFileDependencies;

### Нод CreateEntry

**Имя нода** = CreateEntry;

**Описание** = Creates an entry in the File Dependencis list.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadFileDependencies;

**Входные данные**:

* (System.Object)AcadFileDependencies;
* (System.String)Feature;
* (System.String)FullFileName;
* (System.Object)AffectsGraphics;
* (System.Object)noIncrement;

### Нод IndexOf

**Имя нода** = IndexOf;

**Описание** = Returns the index in the collection of a specified entry.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadFileDependencies;

**Входные данные**:

* (System.Object)AcadFileDependencies;
* (System.String)Feature;
* (System.String)FullFileName;

### Нод RemoveEntry

**Имя нода** = RemoveEntry;

**Описание** = Removes an entry from the File Dependencies list.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadFileDependencies;

**Входные данные**:

* (System.Object)AcadFileDependencies;
* (System.Object)Index;
* (System.Object)forceRemove;

### Нод UpdateEntry

**Имя нода** = UpdateEntry;

**Описание** = Updates an entry in the File Dependencies list.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadFileDependencies;

**Входные данные**:

* (System.Object)AcadFileDependencies;
* (System.Object)Index;

## Группа NVP_nanoCAD_COM.OdaX.AcadExtrudedSurface

### Нод _AcadExtrudedSurface_Constructor

**Имя нода** = _AcadExtrudedSurface_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadExtrudedSurface;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadExtrudedSurface_ConstructorCast

**Имя нода** = _AcadExtrudedSurface_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadExtrudedSurface;

**Входные данные**:

* (System.Object)dynamic;

### Нод Height

**Имя нода** = Height;

**Описание** = Specifies or returns the height of the extruded surface.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadExtrudedSurface;

**Входные данные**:

* (System.Object)AcadExtrudedSurface;

### Нод Set_Height

**Имя нода** = Set_Height;

**Описание** = Specifies or returns the height of the extruded surface.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadExtrudedSurface;

**Входные данные**:

* (System.Object)AcadExtrudedSurface;
* (System.Double)Height;

### Нод TaperAngle

**Имя нода** = TaperAngle;

**Описание** = Specifies or returns the taper angle of the extruded surface.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadExtrudedSurface;

**Входные данные**:

* (System.Object)AcadExtrudedSurface;

### Нод Set_TaperAngle

**Имя нода** = Set_TaperAngle;

**Описание** = Specifies or returns the taper angle of the extruded surface.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadExtrudedSurface;

**Входные данные**:

* (System.Object)AcadExtrudedSurface;
* (System.Object)TaperAngle;

### Нод Direction

**Имя нода** = Direction;

**Описание** = Returns the direction of the extruded surface.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadExtrudedSurface;

**Входные данные**:

* (System.Object)AcadExtrudedSurface;

## Группа NVP_nanoCAD_COM.OdaX.AcadExternalReference

### Нод _AcadExternalReference_Constructor

**Имя нода** = _AcadExternalReference_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadExternalReference;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadExternalReference_ConstructorCast

**Имя нода** = _AcadExternalReference_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadExternalReference;

**Входные данные**:

* (System.Object)dynamic;

### Нод Path

**Имя нода** = Path;

**Описание** = Specifies or returns the source path of an external reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadExternalReference;

**Входные данные**:

* (System.Object)AcadExternalReference;

### Нод Set_Path

**Имя нода** = Set_Path;

**Описание** = Specifies or returns the source path of an external reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadExternalReference;

**Входные данные**:

* (System.Object)AcadExternalReference;
* (System.String)bstrName;

## Группа NVP_nanoCAD_COM.OdaX.AcadEntity

### Нод _AcadEntity_Constructor

**Имя нода** = _AcadEntity_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadEntity_ConstructorCast

**Имя нода** = _AcadEntity_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)dynamic;

### Нод TrueColor

**Имя нода** = TrueColor;

**Описание** = Specifies or returns the true color of an object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)AcadEntity;

### Нод Set_TrueColor

**Имя нода** = Set_TrueColor;

**Описание** = Specifies or returns the true color of an object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)AcadEntity;
* (System.Object)pColor;

### Нод Layer

**Имя нода** = Layer;

**Описание** = Specifies or returns the current layer of an object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)AcadEntity;

### Нод Set_Layer

**Имя нода** = Set_Layer;

**Описание** = Specifies or returns the current layer of an object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)AcadEntity;
* (System.String)Layer;

### Нод Linetype

**Имя нода** = Linetype;

**Описание** = Specifies or returns the current linetype of an object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)AcadEntity;

### Нод Set_Linetype

**Имя нода** = Set_Linetype;

**Описание** = Specifies or returns the current linetype of an object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)AcadEntity;
* (System.String)Linetype;

### Нод LinetypeScale

**Имя нода** = LinetypeScale;

**Описание** = Specifies or returns the linetype scale factor of an object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)AcadEntity;

### Нод Set_LinetypeScale

**Имя нода** = Set_LinetypeScale;

**Описание** = Specifies or returns the linetype scale factor of an object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)AcadEntity;
* (System.Object)ltScale;

### Нод Visible

**Имя нода** = Visible;

**Описание** = Specifies or returns the visibility of an object or the application.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)AcadEntity;

### Нод Set_Visible

**Имя нода** = Set_Visible;

**Описание** = Specifies or returns the visibility of an object or the application.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)AcadEntity;
* (System.Object)bVisible;

### Нод ArrayPolar

**Имя нода** = ArrayPolar;

**Описание** = Not implemented. Creates an array of selected objects in a polar pattern.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)AcadEntity;
* (System.Int32)NumberOfObjects;
* (System.Double)AngleToFill;
* (System.Object)CenterPoint;

### Нод ArrayRectangular

**Имя нода** = ArrayRectangular;

**Описание** = Not implemented. Creates an array of selected objects in a rectangular pattern.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)AcadEntity;
* (System.Int32)NumberOfRows;
* (System.Int32)NumberOfColumns;
* (System.Int32)NumberOfLevels;
* (System.Double)DistBetweenRows;
* (System.Double)DistBetweenCols;
* (System.Double)DistBetweenLevels;

### Нод Highlight

**Имя нода** = Highlight;

**Описание** = Not implemented. Highlights or removes highlight from an entity.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)AcadEntity;
* (System.Object)HighlightFlag;

### Нод Copy

**Имя нода** = Copy;

**Описание** = Copies a specified entity.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)AcadEntity;

### Нод Move

**Имя нода** = Move;

**Описание** = Moves an entity from one point to a second point.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)AcadEntity;
* (System.Object)FromPoint;
* (System.Object)ToPoint;

### Нод Rotate

**Имя нода** = Rotate;

**Описание** = Rotates an entity around a point.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)AcadEntity;
* (System.Object)BasePoint;
* (System.Double)RotationAngle;

### Нод Rotate3D

**Имя нода** = Rotate3D;

**Описание** = Rotates an entity around a line defined by two points, where the direction of the axis if from the first point to the second point.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)AcadEntity;
* (System.Object)Point1;
* (System.Object)Point2;
* (System.Double)RotationAngle;

### Нод Mirror

**Имя нода** = Mirror;

**Описание** = Not implemented. Mirrors an object around a line defined by two points.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)AcadEntity;
* (System.Object)Point1;
* (System.Object)Point2;

### Нод Mirror3D

**Имя нода** = Mirror3D;

**Описание** = Not implemented. Mirrors an object around a plane defined by three points.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)AcadEntity;
* (System.Object)Point1;
* (System.Object)Point2;
* (System.Object)point3;

### Нод ScaleEntity

**Имя нода** = ScaleEntity;

**Описание** = Scales an object with respect to a base point and scale factor.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)AcadEntity;
* (System.Object)BasePoint;
* (System.Double)ScaleFactor;

### Нод TransformBy

**Имя нода** = TransformBy;

**Описание** = Performs the specified transformation on an entity.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)AcadEntity;
* (System.Object)TransformationMatrix;

### Нод Update

**Имя нода** = Update;

**Описание** = Not implemented. Updates the display of an entity.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)AcadEntity;

### Нод GetBoundingBox

**Имя нода** = GetBoundingBox;

**Описание** = Returns the minimum and maximum point of an entity's bounding box.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)AcadEntity;
* (System.Object)MinPoint;
* (System.Object)MaxPoint;

### Нод IntersectWith

**Имя нода** = IntersectWith;

**Описание** = input) IntersectObject.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)AcadEntity;
* (System.Object)IntersectObject;
* (System.Object)OdaX.AcExtendOption;

### Нод PlotStyleName

**Имя нода** = PlotStyleName;

**Описание** = Specifies or returns the plot style name of an object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)AcadEntity;

### Нод Set_PlotStyleName

**Имя нода** = Set_PlotStyleName;

**Описание** = Specifies or returns the plot style name of an object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)AcadEntity;
* (System.String)plotStyle;

### Нод Lineweight

**Имя нода** = Lineweight;

**Описание** = Specifies or returns the lineweight of an object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)AcadEntity;

### Нод Set_Lineweight

**Имя нода** = Set_Lineweight;

**Описание** = Specifies or returns the lineweight of an object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)AcadEntity;
* (System.Object)Lineweight;

### Нод Hyperlinks

**Имя нода** = Hyperlinks;

**Описание** = if one is specified).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)AcadEntity;

### Нод Material

**Имя нода** = Material;

**Описание** = Specifies or returns the material of an object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)AcadEntity;

### Нод Set_Material

**Имя нода** = Set_Material;

**Описание** = Specifies or returns the material of an object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)AcadEntity;
* (System.String)Material;

### Нод EntityName

**Имя нода** = EntityName;

**Описание** = Returns the class name of the object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)AcadEntity;

### Нод EntityType

**Имя нода** = EntityType;

**Описание** = Returns the entity type of the object as an integer.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)AcadEntity;

### Нод color

**Имя нода** = color;

**Описание** = Specifies the color for objects;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)AcadEntity;

### Нод Set_color

**Имя нода** = Set_color;

**Описание** = Specifies the color for objects;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEntity;

**Входные данные**:

* (System.Object)AcadEntity;
* (System.Object)color;

## Группа NVP_nanoCAD_COM.OdaX.AcadEllipse

### Нод _AcadEllipse_Constructor

**Имя нода** = _AcadEllipse_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEllipse;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadEllipse_ConstructorCast

**Имя нода** = _AcadEllipse_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEllipse;

**Входные данные**:

* (System.Object)dynamic;

### Нод StartPoint

**Имя нода** = StartPoint;

**Описание** = Returns the x, y, and z coordinates of an ellipse's start point.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEllipse;

**Входные данные**:

* (System.Object)AcadEllipse;

### Нод Center

**Имя нода** = Center;

**Описание** = Specifies or returns the x, y, and z coordinates of an ellipse's center point.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEllipse;

**Входные данные**:

* (System.Object)AcadEllipse;

### Нод Set_Center

**Имя нода** = Set_Center;

**Описание** = Specifies or returns the x, y, and z coordinates of an ellipse's center point.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEllipse;

**Входные данные**:

* (System.Object)AcadEllipse;
* (System.Object)Center;

### Нод EndPoint

**Имя нода** = EndPoint;

**Описание** = Returns the x, y, and z coordinates of an ellipse's end point.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEllipse;

**Входные данные**:

* (System.Object)AcadEllipse;

### Нод MajorRadius

**Имя нода** = MajorRadius;

**Описание** = Specifies or returns the radius of the longer axis of an ellipse.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEllipse;

**Входные данные**:

* (System.Object)AcadEllipse;

### Нод Set_MajorRadius

**Имя нода** = Set_MajorRadius;

**Описание** = Specifies or returns the radius of the longer axis of an ellipse.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEllipse;

**Входные данные**:

* (System.Object)AcadEllipse;
* (System.Double)MajorRadius;

### Нод MinorRadius

**Имя нода** = MinorRadius;

**Описание** = Specifies or returns the radius of the smaller axis of an ellipse.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEllipse;

**Входные данные**:

* (System.Object)AcadEllipse;

### Нод Set_MinorRadius

**Имя нода** = Set_MinorRadius;

**Описание** = Specifies or returns the radius of the smaller axis of an ellipse.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEllipse;

**Входные данные**:

* (System.Object)AcadEllipse;
* (System.Double)MinorRadius;

### Нод RadiusRatio

**Имя нода** = RadiusRatio;

**Описание** = Specifies or returns the ratio of the ellipse's major axis length to the minor axis length.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEllipse;

**Входные данные**:

* (System.Object)AcadEllipse;

### Нод Set_RadiusRatio

**Имя нода** = Set_RadiusRatio;

**Описание** = Specifies or returns the ratio of the ellipse's major axis length to the minor axis length.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEllipse;

**Входные данные**:

* (System.Object)AcadEllipse;
* (System.Double)RadiusRatio;

### Нод StartAngle

**Имя нода** = StartAngle;

**Описание** = Specifies or returns the start angle of an ellipse.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEllipse;

**Входные данные**:

* (System.Object)AcadEllipse;

### Нод Set_StartAngle

**Имя нода** = Set_StartAngle;

**Описание** = Specifies or returns the start angle of an ellipse.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEllipse;

**Входные данные**:

* (System.Object)AcadEllipse;
* (System.Object)StartAngle;

### Нод EndAngle

**Имя нода** = EndAngle;

**Описание** = Specifies or returns the end angle of an ellipse.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEllipse;

**Входные данные**:

* (System.Object)AcadEllipse;

### Нод Set_EndAngle

**Имя нода** = Set_EndAngle;

**Описание** = Specifies or returns the end angle of an ellipse.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEllipse;

**Входные данные**:

* (System.Object)AcadEllipse;
* (System.Object)EndAngle;

### Нод StartParameter

**Имя нода** = StartParameter;

**Описание** = Specifies or returns the start parameter of an ellipse.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEllipse;

**Входные данные**:

* (System.Object)AcadEllipse;

### Нод Set_StartParameter

**Имя нода** = Set_StartParameter;

**Описание** = Specifies or returns the start parameter of an ellipse.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEllipse;

**Входные данные**:

* (System.Object)AcadEllipse;
* (System.Double)StartParameter;

### Нод EndParameter

**Имя нода** = EndParameter;

**Описание** = Specifies or returns the end parameter of an ellipse.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEllipse;

**Входные данные**:

* (System.Object)AcadEllipse;

### Нод Set_EndParameter

**Имя нода** = Set_EndParameter;

**Описание** = Specifies or returns the end parameter of an ellipse.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEllipse;

**Входные данные**:

* (System.Object)AcadEllipse;
* (System.Double)EndParameter;

### Нод MajorAxis

**Имя нода** = MajorAxis;

**Описание** = Specifies or returns the length of the longer axis of an ellipse.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEllipse;

**Входные данные**:

* (System.Object)AcadEllipse;

### Нод Set_MajorAxis

**Имя нода** = Set_MajorAxis;

**Описание** = Specifies or returns the length of the longer axis of an ellipse.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEllipse;

**Входные данные**:

* (System.Object)AcadEllipse;
* (System.Object)MajorAxis;

### Нод MinorAxis

**Имя нода** = MinorAxis;

**Описание** = Returns the length of the shorter axis of an ellipse.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEllipse;

**Входные данные**:

* (System.Object)AcadEllipse;

### Нод Normal

**Имя нода** = Normal;

**Описание** = Specifies or returns the x, y, and z coordinates of the normal direction vector.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEllipse;

**Входные данные**:

* (System.Object)AcadEllipse;

### Нод Set_Normal

**Имя нода** = Set_Normal;

**Описание** = Specifies or returns the x, y, and z coordinates of the normal direction vector.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEllipse;

**Входные данные**:

* (System.Object)AcadEllipse;
* (System.Object)Normal;

### Нод Area

**Имя нода** = Area;

**Описание** = Returns the area of an ellipse.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEllipse;

**Входные данные**:

* (System.Object)AcadEllipse;

### Нод Offset

**Имя нода** = Offset;

**Описание** = Not implemented. Creates a new ellipse at a specified distance from an original ellipse.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadEllipse;

**Входные данные**:

* (System.Object)AcadEllipse;
* (System.Double)Distance;

## Группа NVP_nanoCAD_COM.OdaX.AcadDynamicBlockReferenceProperty

### Нод _AcadDynamicBlockReferenceProperty_Constructor

**Имя нода** = _AcadDynamicBlockReferenceProperty_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDynamicBlockReferenceProperty;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadDynamicBlockReferenceProperty_ConstructorCast

**Имя нода** = _AcadDynamicBlockReferenceProperty_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDynamicBlockReferenceProperty;

**Входные данные**:

* (System.Object)dynamic;

### Нод PropertyName

**Имя нода** = PropertyName;

**Описание** = Returns the name of the property.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDynamicBlockReferenceProperty;

**Входные данные**:

* (System.Object)AcadDynamicBlockReferenceProperty;

### Нод ReadOnly

**Имя нода** = ReadOnly;

**Описание** = Returns whether a property is read-only.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDynamicBlockReferenceProperty;

**Входные данные**:

* (System.Object)AcadDynamicBlockReferenceProperty;

### Нод Show

**Имя нода** = Show;

**Описание** = Returns whether a property is available in the application user interface.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDynamicBlockReferenceProperty;

**Входные данные**:

* (System.Object)AcadDynamicBlockReferenceProperty;

### Нод Description

**Имя нода** = Description;

**Описание** = Returns the description of the property.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDynamicBlockReferenceProperty;

**Входные данные**:

* (System.Object)AcadDynamicBlockReferenceProperty;

### Нод AllowedValues

**Имя нода** = AllowedValues;

**Описание** = Returns the values that are valid for a property.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDynamicBlockReferenceProperty;

**Входные данные**:

* (System.Object)AcadDynamicBlockReferenceProperty;

### Нод Value

**Имя нода** = Value;

**Описание** = Specifies or returns the value of a property.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDynamicBlockReferenceProperty;

**Входные данные**:

* (System.Object)AcadDynamicBlockReferenceProperty;

### Нод Set_Value

**Имя нода** = Set_Value;

**Описание** = Specifies or returns the value of a property.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDynamicBlockReferenceProperty;

**Входные данные**:

* (System.Object)AcadDynamicBlockReferenceProperty;
* (System.Object)Value;

### Нод UnitsType

**Имя нода** = UnitsType;

**Описание** = Returns the current units type for the display of a property.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDynamicBlockReferenceProperty;

**Входные данные**:

* (System.Object)AcadDynamicBlockReferenceProperty;

## Группа NVP_nanoCAD_COM.OdaX.AcadDwfUnderlay

### Нод _AcadDwfUnderlay_Constructor

**Имя нода** = _AcadDwfUnderlay_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDwfUnderlay;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadDwfUnderlay_ConstructorCast

**Имя нода** = _AcadDwfUnderlay_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDwfUnderlay;

**Входные данные**:

* (System.Object)dynamic;

### Нод DWFFormat

**Имя нода** = DWFFormat;

**Описание** = Specifies the format of DWF file;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDwfUnderlay;

**Входные данные**:

* (System.Object)AcadDwfUnderlay;

### Нод Set_DWFFormat

**Имя нода** = Set_DWFFormat;

**Описание** = Specifies the format of DWF file;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDwfUnderlay;

**Входные данные**:

* (System.Object)AcadDwfUnderlay;
* (System.String)Name;

## Группа NVP_nanoCAD_COM.OdaX.AcadDimStyles

### Нод _AcadDimStyles_Constructor

**Имя нода** = _AcadDimStyles_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimStyles;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadDimStyles_ConstructorCast

**Имя нода** = _AcadDimStyles_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimStyles;

**Входные данные**:

* (System.Object)dynamic;

### Нод Item

**Имя нода** = Item;

**Описание** = Returns an object at a given index in the dimension style collection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimStyles;

**Входные данные**:

* (System.Object)AcadDimStyles;
* (System.Object)Index;

### Нод Count

**Имя нода** = Count;

**Описание** = Returns the number of items in the dimension style collection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimStyles;

**Входные данные**:

* (System.Object)AcadDimStyles;

### Нод Add

**Имя нода** = Add;

**Описание** = Creates an object and adds it to the dimension style collection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimStyles;

**Входные данные**:

* (System.Object)AcadDimStyles;
* (System.String)Name;

## Группа NVP_nanoCAD_COM.OdaX.AcadDimStyle

### Нод _AcadDimStyle_Constructor

**Имя нода** = _AcadDimStyle_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimStyle;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadDimStyle_ConstructorCast

**Имя нода** = _AcadDimStyle_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimStyle;

**Входные данные**:

* (System.Object)dynamic;

### Нод Name

**Имя нода** = Name;

**Описание** = Specifies or returns the name of a dimension style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimStyle;

**Входные данные**:

* (System.Object)AcadDimStyle;

### Нод Set_Name

**Имя нода** = Set_Name;

**Описание** = Specifies or returns the name of a dimension style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimStyle;

**Входные данные**:

* (System.Object)AcadDimStyle;
* (System.String)bstrName;

### Нод CopyFrom

**Имя нода** = CopyFrom;

**Описание** = Copies dimension style data to an existing dimension style.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimStyle;

**Входные данные**:

* (System.Object)AcadDimStyle;
* (System.Object)StyleSource;

## Группа NVP_nanoCAD_COM.OdaX.AcadDimRotated

### Нод _AcadDimRotated_Constructor

**Имя нода** = _AcadDimRotated_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadDimRotated_ConstructorCast

**Имя нода** = _AcadDimRotated_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)dynamic;

### Нод AltUnits

**Имя нода** = AltUnits;

**Описание** = Specifies or returns whether alternate units are used for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_AltUnits

**Имя нода** = Set_AltUnits;

**Описание** = Specifies or returns whether alternate units are used for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)bAlternate;

### Нод AltUnitsPrecision

**Имя нода** = AltUnitsPrecision;

**Описание** = Specifies or returns the number of decimal places for alternate unit dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_AltUnitsPrecision

**Имя нода** = Set_AltUnitsPrecision;

**Описание** = Specifies or returns the number of decimal places for alternate unit dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)OdaX.AcDimPrecision;

### Нод AltUnitsScale

**Имя нода** = AltUnitsScale;

**Описание** = Specifies or returns the scale factor for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_AltUnitsScale

**Имя нода** = Set_AltUnitsScale;

**Описание** = Specifies or returns the scale factor for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)scale;

### Нод AltRoundDistance

**Имя нода** = AltRoundDistance;

**Описание** = Specifies or returns the rounding value for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_AltRoundDistance

**Имя нода** = Set_AltRoundDistance;

**Описание** = Specifies or returns the rounding value for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Double)Distance;

### Нод AltTolerancePrecision

**Имя нода** = AltTolerancePrecision;

**Описание** = Specifies or returns the number of decimal places for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_AltTolerancePrecision

**Имя нода** = Set_AltTolerancePrecision;

**Описание** = Specifies or returns the number of decimal places for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)OdaX.AcDimPrecision;

### Нод AltUnitsFormat

**Имя нода** = AltUnitsFormat;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_AltUnitsFormat

**Имя нода** = Set_AltUnitsFormat;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)OdaX.AcDimUnits;

### Нод AltTextPrefix

**Имя нода** = AltTextPrefix;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_AltTextPrefix

**Имя нода** = Set_AltTextPrefix;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.String)prefix;

### Нод AltTextSuffix

**Имя нода** = AltTextSuffix;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_AltTextSuffix

**Имя нода** = Set_AltTextSuffix;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.String)prefix;

### Нод DimensionLineColor

**Имя нода** = DimensionLineColor;

**Описание** = Specifies or returns the color of dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_DimensionLineColor

**Имя нода** = Set_DimensionLineColor;

**Описание** = Specifies or returns the color of dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)Type;

### Нод ExtensionLineColor

**Имя нода** = ExtensionLineColor;

**Описание** = Specifies or returns the color of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_ExtensionLineColor

**Имя нода** = Set_ExtensionLineColor;

**Описание** = Specifies or returns the color of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)Type;

### Нод PrimaryUnitsPrecision

**Имя нода** = PrimaryUnitsPrecision;

**Описание** = Specifies or returns the number of decimal places for the primary units of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_PrimaryUnitsPrecision

**Имя нода** = Set_PrimaryUnitsPrecision;

**Описание** = Specifies or returns the number of decimal places for the primary units of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)OdaX.AcDimPrecision;

### Нод DimensionLineExtend

**Имя нода** = DimensionLineExtend;

**Описание** = Specifies or returns how far dimension lines extend beyond extension lines when using dimension lines with oblique strokes.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_DimensionLineExtend

**Имя нода** = Set_DimensionLineExtend;

**Описание** = Specifies or returns how far dimension lines extend beyond extension lines when using dimension lines with oblique strokes.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Double)extend;

### Нод ExtensionLineExtend

**Имя нода** = ExtensionLineExtend;

**Описание** = Specifies or returns how far extension lines extend beyond dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_ExtensionLineExtend

**Имя нода** = Set_ExtensionLineExtend;

**Описание** = Specifies or returns how far extension lines extend beyond dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Double)extend;

### Нод Fit

**Имя нода** = Fit;

**Описание** = Specifies or returns how text and arrowheads are positioned when they do not fit in the space between extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_Fit

**Имя нода** = Set_Fit;

**Описание** = Specifies or returns how text and arrowheads are positioned when they do not fit in the space between extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)OdaX.AcDimFit;

### Нод FractionFormat

**Имя нода** = FractionFormat;

**Описание** = Specifies or returns how fractions display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_FractionFormat

**Имя нода** = Set_FractionFormat;

**Описание** = Specifies or returns how fractions display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)OdaX.AcDimFractionType;

### Нод HorizontalTextPosition

**Имя нода** = HorizontalTextPosition;

**Описание** = Specifies or returns the horizontal position of dimension text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_HorizontalTextPosition

**Имя нода** = Set_HorizontalTextPosition;

**Описание** = Specifies or returns the horizontal position of dimension text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)OdaX.AcDimHorizontalJustification;

### Нод LinearScaleFactor

**Имя нода** = LinearScaleFactor;

**Описание** = Specifies or returns the scale factor for linear dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_LinearScaleFactor

**Имя нода** = Set_LinearScaleFactor;

**Описание** = Specifies or returns the scale factor for linear dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)Type;

### Нод UnitsFormat

**Имя нода** = UnitsFormat;

**Описание** = Specifies or returns the unit of measurement for linear dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_UnitsFormat

**Имя нода** = Set_UnitsFormat;

**Описание** = Specifies or returns the unit of measurement for linear dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)OdaX.AcDimLUnits;

### Нод ExtensionLineWeight

**Имя нода** = ExtensionLineWeight;

**Описание** = Specifies or returns the lineweight of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_ExtensionLineWeight

**Имя нода** = Set_ExtensionLineWeight;

**Описание** = Specifies or returns the lineweight of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)lweight;

### Нод RoundDistance

**Имя нода** = RoundDistance;

**Описание** = Specifies or returns how measurements are rounded for all dimensions except angular dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_RoundDistance

**Имя нода** = Set_RoundDistance;

**Описание** = Specifies or returns how measurements are rounded for all dimensions except angular dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Double)Distance;

### Нод DimLine1Suppress

**Имя нода** = DimLine1Suppress;

**Описание** = Specifies or returns whether the first dimension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_DimLine1Suppress

**Имя нода** = Set_DimLine1Suppress;

**Описание** = Specifies or returns whether the first dimension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)bSuppress;

### Нод DimLine2Suppress

**Имя нода** = DimLine2Suppress;

**Описание** = Specifies or returns whether the second dimension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_DimLine2Suppress

**Имя нода** = Set_DimLine2Suppress;

**Описание** = Specifies or returns whether the second dimension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)bSuppress;

### Нод ExtLine1Suppress

**Имя нода** = ExtLine1Suppress;

**Описание** = Specifies or returns whether the first extension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_ExtLine1Suppress

**Имя нода** = Set_ExtLine1Suppress;

**Описание** = Specifies or returns whether the first extension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)bSuppress;

### Нод ExtLine2Suppress

**Имя нода** = ExtLine2Suppress;

**Описание** = Specifies or returns whether the second extension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_ExtLine2Suppress

**Имя нода** = Set_ExtLine2Suppress;

**Описание** = Specifies or returns whether the second extension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)bSuppress;

### Нод DimLineInside

**Имя нода** = DimLineInside;

**Описание** = Specifies or returns whether dimension lines are drawn outside of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_DimLineInside

**Имя нода** = Set_DimLineInside;

**Описание** = Specifies or returns whether dimension lines are drawn outside of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)bInside;

### Нод TextInsideAlign

**Имя нода** = TextInsideAlign;

**Описание** = Specifies or returns whether dimension text displays between extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_TextInsideAlign

**Имя нода** = Set_TextInsideAlign;

**Описание** = Specifies or returns whether dimension text displays between extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)bInside;

### Нод TextInside

**Имя нода** = TextInside;

**Описание** = Specifies or returns whether dimension text between extension lines is aligned horizontally or with the dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_TextInside

**Имя нода** = Set_TextInside;

**Описание** = Specifies or returns whether dimension text between extension lines is aligned horizontally or with the dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)bInside;

### Нод ForceLineInside

**Имя нода** = ForceLineInside;

**Описание** = Specifies or returns whether dimension lines display between extension lines when text is positioned outside of the extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_ForceLineInside

**Имя нода** = Set_ForceLineInside;

**Описание** = Specifies or returns whether dimension lines display between extension lines when text is positioned outside of the extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)bInside;

### Нод TextOutsideAlign

**Имя нода** = TextOutsideAlign;

**Описание** = Specifies or returns whether dimension text displays outside of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_TextOutsideAlign

**Имя нода** = Set_TextOutsideAlign;

**Описание** = Specifies or returns whether dimension text displays outside of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)bInside;

### Нод ExtensionLineOffset

**Имя нода** = ExtensionLineOffset;

**Описание** = Specifies or returns the distance between extension lines and the object being dimensioned.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_ExtensionLineOffset

**Имя нода** = Set_ExtensionLineOffset;

**Описание** = Specifies or returns the distance between extension lines and the object being dimensioned.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Double)Offset;

### Нод AltSuppressLeadingZeros

**Имя нода** = AltSuppressLeadingZeros;

**Описание** = Specifies or returns whether leading zeros display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_AltSuppressLeadingZeros

**Имя нода** = Set_AltSuppressLeadingZeros;

**Описание** = Specifies or returns whether leading zeros display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)bVal;

### Нод AltSuppressTrailingZeros

**Имя нода** = AltSuppressTrailingZeros;

**Описание** = Specifies or returns whether leading zeros display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_AltSuppressTrailingZeros

**Имя нода** = Set_AltSuppressTrailingZeros;

**Описание** = Specifies or returns whether leading zeros display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)bVal;

### Нод AltSuppressZeroFeet

**Имя нода** = AltSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_AltSuppressZeroFeet

**Имя нода** = Set_AltSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)bVal;

### Нод AltSuppressZeroInches

**Имя нода** = AltSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_AltSuppressZeroInches

**Имя нода** = Set_AltSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)bVal;

### Нод AltToleranceSuppressLeadingZeros

**Имя нода** = AltToleranceSuppressLeadingZeros;

**Описание** = Specifies or returns whether leading zeros display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_AltToleranceSuppressLeadingZeros

**Имя нода** = Set_AltToleranceSuppressLeadingZeros;

**Описание** = Specifies or returns whether leading zeros display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)bVal;

### Нод AltToleranceSuppressTrailingZeros

**Имя нода** = AltToleranceSuppressTrailingZeros;

**Описание** = Specifies or returns whether trailing zeros display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_AltToleranceSuppressTrailingZeros

**Имя нода** = Set_AltToleranceSuppressTrailingZeros;

**Описание** = Specifies or returns whether trailing zeros display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)bVal;

### Нод AltToleranceSuppressZeroFeet

**Имя нода** = AltToleranceSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_AltToleranceSuppressZeroFeet

**Имя нода** = Set_AltToleranceSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)bVal;

### Нод AltToleranceSuppressZeroInches

**Имя нода** = AltToleranceSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_AltToleranceSuppressZeroInches

**Имя нода** = Set_AltToleranceSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)bVal;

### Нод SuppressZeroFeet

**Имя нода** = SuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_SuppressZeroFeet

**Имя нода** = Set_SuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)bVal;

### Нод SuppressZeroInches

**Имя нода** = SuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_SuppressZeroInches

**Имя нода** = Set_SuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)bVal;

### Нод ToleranceSuppressZeroFeet

**Имя нода** = ToleranceSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for tolerance values of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_ToleranceSuppressZeroFeet

**Имя нода** = Set_ToleranceSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for tolerance values of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)bVal;

### Нод ToleranceSuppressZeroInches

**Имя нода** = ToleranceSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for tolerance values of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_ToleranceSuppressZeroInches

**Имя нода** = Set_ToleranceSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for tolerance values of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)bVal;

### Нод DimensionLineWeight

**Имя нода** = DimensionLineWeight;

**Описание** = Specifies or returns the lineweight of dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_DimensionLineWeight

**Имя нода** = Set_DimensionLineWeight;

**Описание** = Specifies or returns the lineweight of dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)weight;

### Нод ArrowheadSize

**Имя нода** = ArrowheadSize;

**Описание** = Specifies or returns the size of dimension arrowheads.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_ArrowheadSize

**Имя нода** = Set_ArrowheadSize;

**Описание** = Specifies or returns the size of dimension arrowheads.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Double)size;

### Нод Arrowhead1Type

**Имя нода** = Arrowhead1Type;

**Описание** = Specifies or returns the arrowhead of the first dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_Arrowhead1Type

**Имя нода** = Set_Arrowhead1Type;

**Описание** = Specifies or returns the arrowhead of the first dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)OdaX.AcDimArrowheadType;

### Нод Arrowhead2Type

**Имя нода** = Arrowhead2Type;

**Описание** = Specifies or returns the arrowhead of the second dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_Arrowhead2Type

**Имя нода** = Set_Arrowhead2Type;

**Описание** = Specifies or returns the arrowhead of the second dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)OdaX.AcDimArrowheadType;

### Нод Measurement

**Имя нода** = Measurement;

**Описание** = Returns the length measurement of linear dimensions or the angle measurement for angular dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Arrowhead1Block

**Имя нода** = Arrowhead1Block;

**Описание** = Specifies or returns a custom block to replace the arrowhead of the first dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_Arrowhead1Block

**Имя нода** = Set_Arrowhead1Block;

**Описание** = Specifies or returns a custom block to replace the arrowhead of the first dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.String)BlockName;

### Нод Arrowhead2Block

**Имя нода** = Arrowhead2Block;

**Описание** = Specifies or returns a custom block to replace the arrowhead of the second dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_Arrowhead2Block

**Имя нода** = Set_Arrowhead2Block;

**Описание** = Specifies or returns a custom block to replace the arrowhead of the second dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.String)BlockName;

### Нод DimensionLinetype

**Имя нода** = DimensionLinetype;

**Описание** = Specifies the linetype of a dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_DimensionLinetype

**Имя нода** = Set_DimensionLinetype;

**Описание** = Specifies the linetype of a dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.String)Linetype;

### Нод ExtLine1Linetype

**Имя нода** = ExtLine1Linetype;

**Описание** = Specifies or returns the linetype of the first extension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_ExtLine1Linetype

**Имя нода** = Set_ExtLine1Linetype;

**Описание** = Specifies or returns the linetype of the first extension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.String)Linetype;

### Нод ExtLine2Linetype

**Имя нода** = ExtLine2Linetype;

**Описание** = Specifies or returns the linetype of the second extension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_ExtLine2Linetype

**Имя нода** = Set_ExtLine2Linetype;

**Описание** = Specifies or returns the linetype of the second extension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.String)Linetype;

### Нод ExtLineFixedLenSuppress

**Имя нода** = ExtLineFixedLenSuppress;

**Описание** = Specifies or returns whether fixed-length extension lines display.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_ExtLineFixedLenSuppress

**Имя нода** = Set_ExtLineFixedLenSuppress;

**Описание** = Specifies or returns whether fixed-length extension lines display.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)bFixedLen;

### Нод ExtLineFixedLen

**Имя нода** = ExtLineFixedLen;

**Описание** = Specifies or returns whether extension lines are a fixed length.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_ExtLineFixedLen

**Имя нода** = Set_ExtLineFixedLen;

**Описание** = Specifies or returns whether extension lines are a fixed length.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Double)FixedLen;

### Нод DimConstrForm

**Имя нода** = DimConstrForm;

**Описание** = Specifies the constraint type - Dynamic or Annotational;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_DimConstrForm

**Имя нода** = Set_DimConstrForm;

**Описание** = Specifies the constraint type - Dynamic or Annotational;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)bIsDynamic;

### Нод DimConstrReference

**Имя нода** = DimConstrReference;

**Описание** = Specifies whether the parameter is a reference constraint. Reference dimensions can be used in expressions but don't drive geometry - similar to an associative dimension;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_DimConstrReference

**Имя нода** = Set_DimConstrReference;

**Описание** = Specifies whether the parameter is a reference constraint. Reference dimensions can be used in expressions but don't drive geometry - similar to an associative dimension;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Object)bIsReference;

### Нод DimConstrName

**Имя нода** = DimConstrName;

**Описание** = Specifies the name of the dimensional constraint. Names cannot have spaces;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_DimConstrName

**Имя нода** = Set_DimConstrName;

**Описание** = Specifies the name of the dimensional constraint. Names cannot have spaces;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.String)bstrName;

### Нод DimConstrExpression

**Имя нода** = DimConstrExpression;

**Описание** = Specifies the expression or the value of the constraint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_DimConstrExpression

**Имя нода** = Set_DimConstrExpression;

**Описание** = Specifies the expression or the value of the constraint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.String)bstrExpression;

### Нод DimConstrValue

**Имя нода** = DimConstrValue;

**Описание** = Specifies the value of the constraint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_DimConstrValue

**Имя нода** = Set_DimConstrValue;

**Описание** = Specifies the value of the constraint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.String)Value;

### Нод DimConstrDesc

**Имя нода** = DimConstrDesc;

**Описание** = Specifies description for constraint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_DimConstrDesc

**Имя нода** = Set_DimConstrDesc;

**Описание** = Specifies description for constraint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.String)bstrDescription;

### Нод SubUnitsSuffix

**Имя нода** = SubUnitsSuffix;

**Описание** = Specifies the text suffix for all applicable linear dimension when change to sub-units;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_SubUnitsSuffix

**Имя нода** = Set_SubUnitsSuffix;

**Описание** = Specifies the text suffix for all applicable linear dimension when change to sub-units;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.String)suffix;

### Нод SubUnitsFactor

**Имя нода** = SubUnitsFactor;

**Описание** = Specifies the sub-units scale factor for all applicable linear dimension;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_SubUnitsFactor

**Имя нода** = Set_SubUnitsFactor;

**Описание** = Specifies the sub-units scale factor for all applicable linear dimension;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Double)factor;

### Нод AltSubUnitsSuffix

**Имя нода** = AltSubUnitsSuffix;

**Описание** = Specifies the text suffix for the alternate dimension when change to alternate sub-units;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_AltSubUnitsSuffix

**Имя нода** = Set_AltSubUnitsSuffix;

**Описание** = Specifies the text suffix for the alternate dimension when change to alternate sub-units;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.String)suffix;

### Нод AltSubUnitsFactor

**Имя нода** = AltSubUnitsFactor;

**Описание** = Specifies the alternate sub-units scale factor for all applicable linear dimension;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;

### Нод Set_AltSubUnitsFactor

**Имя нода** = Set_AltSubUnitsFactor;

**Описание** = Specifies the alternate sub-units scale factor for all applicable linear dimension;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRotated;

**Входные данные**:

* (System.Object)AcadDimRotated;
* (System.Double)factor;

## Группа NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge

### Нод _AcadDimRadialLarge_Constructor

**Имя нода** = _AcadDimRadialLarge_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadDimRadialLarge_ConstructorCast

**Имя нода** = _AcadDimRadialLarge_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)dynamic;

### Нод AltUnits

**Имя нода** = AltUnits;

**Описание** = Specifies or returns whether alternate units are used for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_AltUnits

**Имя нода** = Set_AltUnits;

**Описание** = Specifies or returns whether alternate units are used for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Object)bAlternate;

### Нод AltUnitsPrecision

**Имя нода** = AltUnitsPrecision;

**Описание** = Specifies or returns the number of decimal places for alternate unit dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_AltUnitsPrecision

**Имя нода** = Set_AltUnitsPrecision;

**Описание** = Specifies or returns the number of decimal places for alternate unit dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Object)OdaX.AcDimPrecision;

### Нод AltUnitsScale

**Имя нода** = AltUnitsScale;

**Описание** = Specifies or returns the scale factor for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_AltUnitsScale

**Имя нода** = Set_AltUnitsScale;

**Описание** = Specifies or returns the scale factor for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Object)scale;

### Нод AltRoundDistance

**Имя нода** = AltRoundDistance;

**Описание** = Specifies or returns the rounding value for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_AltRoundDistance

**Имя нода** = Set_AltRoundDistance;

**Описание** = Specifies or returns the rounding value for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Double)Distance;

### Нод AltTolerancePrecision

**Имя нода** = AltTolerancePrecision;

**Описание** = Specifies or returns the number of decimal places for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_AltTolerancePrecision

**Имя нода** = Set_AltTolerancePrecision;

**Описание** = Specifies or returns the number of decimal places for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Object)OdaX.AcDimPrecision;

### Нод AltUnitsFormat

**Имя нода** = AltUnitsFormat;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_AltUnitsFormat

**Имя нода** = Set_AltUnitsFormat;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Object)OdaX.AcDimUnits;

### Нод AltTextPrefix

**Имя нода** = AltTextPrefix;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_AltTextPrefix

**Имя нода** = Set_AltTextPrefix;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.String)prefix;

### Нод AltTextSuffix

**Имя нода** = AltTextSuffix;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_AltTextSuffix

**Имя нода** = Set_AltTextSuffix;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.String)prefix;

### Нод CenterType

**Имя нода** = CenterType;

**Описание** = Specifies or returns the type of center mark used for radial dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_CenterType

**Имя нода** = Set_CenterType;

**Описание** = Specifies or returns the type of center mark used for radial dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Object)OdaX.AcDimCenterType;

### Нод CenterMarkSize

**Имя нода** = CenterMarkSize;

**Описание** = Specifies or returns the size of the center mark used for radial dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_CenterMarkSize

**Имя нода** = Set_CenterMarkSize;

**Описание** = Specifies or returns the size of the center mark used for radial dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Double)Type;

### Нод DimensionLineColor

**Имя нода** = DimensionLineColor;

**Описание** = Specifies or returns the color of dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_DimensionLineColor

**Имя нода** = Set_DimensionLineColor;

**Описание** = Specifies or returns the color of dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Object)Type;

### Нод PrimaryUnitsPrecision

**Имя нода** = PrimaryUnitsPrecision;

**Описание** = Specifies or returns the number of decimal places for the primary units of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_PrimaryUnitsPrecision

**Имя нода** = Set_PrimaryUnitsPrecision;

**Описание** = Specifies or returns the number of decimal places for the primary units of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Object)OdaX.AcDimPrecision;

### Нод FractionFormat

**Имя нода** = FractionFormat;

**Описание** = Specifies or returns how fractions display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_FractionFormat

**Имя нода** = Set_FractionFormat;

**Описание** = Specifies or returns how fractions display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Object)OdaX.AcDimFractionType;

### Нод Fit

**Имя нода** = Fit;

**Описание** = Specifies or returns how text and arrowheads are positioned when they do not fit in the space between extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_Fit

**Имя нода** = Set_Fit;

**Описание** = Specifies or returns how text and arrowheads are positioned when they do not fit in the space between extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Object)OdaX.AcDimFit;

### Нод LinearScaleFactor

**Имя нода** = LinearScaleFactor;

**Описание** = Specifies or returns the scale factor for linear dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_LinearScaleFactor

**Имя нода** = Set_LinearScaleFactor;

**Описание** = Specifies or returns the scale factor for linear dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Object)Type;

### Нод UnitsFormat

**Имя нода** = UnitsFormat;

**Описание** = Specifies or returns the unit of measurement for linear dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_UnitsFormat

**Имя нода** = Set_UnitsFormat;

**Описание** = Specifies or returns the unit of measurement for linear dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Object)OdaX.AcDimLUnits;

### Нод RoundDistance

**Имя нода** = RoundDistance;

**Описание** = Specifies or returns how measurements are rounded for all dimensions except angular dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_RoundDistance

**Имя нода** = Set_RoundDistance;

**Описание** = Specifies or returns how measurements are rounded for all dimensions except angular dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Double)Distance;

### Нод DimLineSuppress

**Имя нода** = DimLineSuppress;

**Описание** = Specifies or returns whether the dimension line displays for radial dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_DimLineSuppress

**Имя нода** = Set_DimLineSuppress;

**Описание** = Specifies or returns whether the dimension line displays for radial dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Object)bSuppress;

### Нод TextInsideAlign

**Имя нода** = TextInsideAlign;

**Описание** = Specifies or returns whether dimension text displays between extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_TextInsideAlign

**Имя нода** = Set_TextInsideAlign;

**Описание** = Specifies or returns whether dimension text displays between extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Object)bInside;

### Нод TextInside

**Имя нода** = TextInside;

**Описание** = Specifies or returns whether dimension text between extension lines is aligned horizontally or with the dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_TextInside

**Имя нода** = Set_TextInside;

**Описание** = Specifies or returns whether dimension text between extension lines is aligned horizontally or with the dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Object)bInside;

### Нод ForceLineInside

**Имя нода** = ForceLineInside;

**Описание** = Specifies or returns whether dimension lines display between extension lines when text is positioned outside of the extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_ForceLineInside

**Имя нода** = Set_ForceLineInside;

**Описание** = Specifies or returns whether dimension lines display between extension lines when text is positioned outside of the extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Object)bInside;

### Нод TextOutsideAlign

**Имя нода** = TextOutsideAlign;

**Описание** = Specifies or returns whether dimension text displays outside of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_TextOutsideAlign

**Имя нода** = Set_TextOutsideAlign;

**Описание** = Specifies or returns whether dimension text displays outside of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Object)bInside;

### Нод AltSuppressLeadingZeros

**Имя нода** = AltSuppressLeadingZeros;

**Описание** = Specifies or returns whether leading zeros display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_AltSuppressLeadingZeros

**Имя нода** = Set_AltSuppressLeadingZeros;

**Описание** = Specifies or returns whether leading zeros display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Object)bVal;

### Нод AltSuppressTrailingZeros

**Имя нода** = AltSuppressTrailingZeros;

**Описание** = Specifies or returns whether leading zeros display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_AltSuppressTrailingZeros

**Имя нода** = Set_AltSuppressTrailingZeros;

**Описание** = Specifies or returns whether leading zeros display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Object)bVal;

### Нод AltSuppressZeroFeet

**Имя нода** = AltSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_AltSuppressZeroFeet

**Имя нода** = Set_AltSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Object)bVal;

### Нод AltSuppressZeroInches

**Имя нода** = AltSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_AltSuppressZeroInches

**Имя нода** = Set_AltSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Object)bVal;

### Нод AltToleranceSuppressLeadingZeros

**Имя нода** = AltToleranceSuppressLeadingZeros;

**Описание** = Specifies or returns whether leading zeros display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_AltToleranceSuppressLeadingZeros

**Имя нода** = Set_AltToleranceSuppressLeadingZeros;

**Описание** = Specifies or returns whether leading zeros display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Object)bVal;

### Нод AltToleranceSuppressTrailingZeros

**Имя нода** = AltToleranceSuppressTrailingZeros;

**Описание** = Specifies or returns whether trailing zeros display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_AltToleranceSuppressTrailingZeros

**Имя нода** = Set_AltToleranceSuppressTrailingZeros;

**Описание** = Specifies or returns whether trailing zeros display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Object)bVal;

### Нод AltToleranceSuppressZeroFeet

**Имя нода** = AltToleranceSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_AltToleranceSuppressZeroFeet

**Имя нода** = Set_AltToleranceSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Object)bVal;

### Нод AltToleranceSuppressZeroInches

**Имя нода** = AltToleranceSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_AltToleranceSuppressZeroInches

**Имя нода** = Set_AltToleranceSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Object)bVal;

### Нод SuppressZeroFeet

**Имя нода** = SuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_SuppressZeroFeet

**Имя нода** = Set_SuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Object)bVal;

### Нод SuppressZeroInches

**Имя нода** = SuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_SuppressZeroInches

**Имя нода** = Set_SuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Object)bVal;

### Нод ToleranceSuppressZeroFeet

**Имя нода** = ToleranceSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for tolerance values of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_ToleranceSuppressZeroFeet

**Имя нода** = Set_ToleranceSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for tolerance values of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Object)bVal;

### Нод ToleranceSuppressZeroInches

**Имя нода** = ToleranceSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for tolerance values of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_ToleranceSuppressZeroInches

**Имя нода** = Set_ToleranceSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for tolerance values of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Object)bVal;

### Нод DimensionLineWeight

**Имя нода** = DimensionLineWeight;

**Описание** = Specifies or returns the lineweight of dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_DimensionLineWeight

**Имя нода** = Set_DimensionLineWeight;

**Описание** = Specifies or returns the lineweight of dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Object)weight;

### Нод ArrowheadSize

**Имя нода** = ArrowheadSize;

**Описание** = Specifies or returns the size of dimension arrowheads.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_ArrowheadSize

**Имя нода** = Set_ArrowheadSize;

**Описание** = Specifies or returns the size of dimension arrowheads.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Double)size;

### Нод ArrowheadType

**Имя нода** = ArrowheadType;

**Описание** = Specifies or returns the arrowhead of the dimension line for radial dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_ArrowheadType

**Имя нода** = Set_ArrowheadType;

**Описание** = Specifies or returns the arrowhead of the dimension line for radial dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Object)OdaX.AcDimArrowheadType;

### Нод Measurement

**Имя нода** = Measurement;

**Описание** = Returns the length measurement of linear dimensions or the angle measurement for angular dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод ArrowheadBlock

**Имя нода** = ArrowheadBlock;

**Описание** = Specifies or returns a custom block to replace the arrowhead of the dimension line for radial dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_ArrowheadBlock

**Имя нода** = Set_ArrowheadBlock;

**Описание** = Specifies or returns a custom block to replace the arrowhead of the dimension line for radial dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.String)BlockName;

### Нод OverrideCenter

**Имя нода** = OverrideCenter;

**Описание** = Not implemented. Specifies or returns the override center point.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_OverrideCenter

**Имя нода** = Set_OverrideCenter;

**Описание** = Not implemented. Specifies or returns the override center point.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Object)overrideCenterPos;

### Нод JogLocation

**Имя нода** = JogLocation;

**Описание** = Not implemented. Specifies or returns the jog location.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_JogLocation

**Имя нода** = Set_JogLocation;

**Описание** = Not implemented. Specifies or returns the jog location.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Object)jogPos;

### Нод JogAngle

**Имя нода** = JogAngle;

**Описание** = Not implemented. Specifies or returns the jog angle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_JogAngle

**Имя нода** = Set_JogAngle;

**Описание** = Not implemented. Specifies or returns the jog angle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Object)JogAngle;

### Нод Center

**Имя нода** = Center;

**Описание** = Not implemented. Specifies or returns the center point of the circle or arc.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_Center

**Имя нода** = Set_Center;

**Описание** = Not implemented. Specifies or returns the center point of the circle or arc.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Object)pVar;

### Нод ChordPoint

**Имя нода** = ChordPoint;

**Описание** = Not implemented. Specifies or returns the chord point of the circle or arc.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_ChordPoint

**Имя нода** = Set_ChordPoint;

**Описание** = Not implemented. Specifies or returns the chord point of the circle or arc.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.Object)pVal;

### Нод DimensionLinetype

**Имя нода** = DimensionLinetype;

**Описание** = Specifies the linetype of a dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;

### Нод Set_DimensionLinetype

**Имя нода** = Set_DimensionLinetype;

**Описание** = Specifies the linetype of a dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadialLarge;

**Входные данные**:

* (System.Object)AcadDimRadialLarge;
* (System.String)Linetype;

## Группа NVP_nanoCAD_COM.OdaX.AcadDimRadial

### Нод _AcadDimRadial_Constructor

**Имя нода** = _AcadDimRadial_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadDimRadial_ConstructorCast

**Имя нода** = _AcadDimRadial_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)dynamic;

### Нод Set_LeaderLength

**Имя нода** = Set_LeaderLength;

**Описание** = Specifies the length of the leader used with the radial dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Double)rhs;

### Нод AltUnits

**Имя нода** = AltUnits;

**Описание** = Specifies or returns whether alternate units are used for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_AltUnits

**Имя нода** = Set_AltUnits;

**Описание** = Specifies or returns whether alternate units are used for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Object)bAlternate;

### Нод AltUnitsPrecision

**Имя нода** = AltUnitsPrecision;

**Описание** = Specifies or returns the number of decimal places for alternate unit dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_AltUnitsPrecision

**Имя нода** = Set_AltUnitsPrecision;

**Описание** = Specifies or returns the number of decimal places for alternate unit dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Object)OdaX.AcDimPrecision;

### Нод AltUnitsScale

**Имя нода** = AltUnitsScale;

**Описание** = Specifies or returns the scale factor for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_AltUnitsScale

**Имя нода** = Set_AltUnitsScale;

**Описание** = Specifies or returns the scale factor for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Object)scale;

### Нод AltRoundDistance

**Имя нода** = AltRoundDistance;

**Описание** = Specifies or returns the rounding value for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_AltRoundDistance

**Имя нода** = Set_AltRoundDistance;

**Описание** = Specifies or returns the rounding value for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Double)Distance;

### Нод AltTolerancePrecision

**Имя нода** = AltTolerancePrecision;

**Описание** = Specifies or returns the number of decimal places for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_AltTolerancePrecision

**Имя нода** = Set_AltTolerancePrecision;

**Описание** = Specifies or returns the number of decimal places for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Object)OdaX.AcDimPrecision;

### Нод AltUnitsFormat

**Имя нода** = AltUnitsFormat;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_AltUnitsFormat

**Имя нода** = Set_AltUnitsFormat;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Object)OdaX.AcDimUnits;

### Нод AltTextPrefix

**Имя нода** = AltTextPrefix;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_AltTextPrefix

**Имя нода** = Set_AltTextPrefix;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.String)prefix;

### Нод AltTextSuffix

**Имя нода** = AltTextSuffix;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_AltTextSuffix

**Имя нода** = Set_AltTextSuffix;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.String)prefix;

### Нод CenterType

**Имя нода** = CenterType;

**Описание** = Specifies or returns the type of center mark used for radial dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_CenterType

**Имя нода** = Set_CenterType;

**Описание** = Specifies or returns the type of center mark used for radial dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Object)OdaX.AcDimCenterType;

### Нод CenterMarkSize

**Имя нода** = CenterMarkSize;

**Описание** = Specifies or returns the size of the center mark used for radial dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_CenterMarkSize

**Имя нода** = Set_CenterMarkSize;

**Описание** = Specifies or returns the size of the center mark used for radial dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Double)Type;

### Нод DimensionLineColor

**Имя нода** = DimensionLineColor;

**Описание** = Specifies or returns the color of dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_DimensionLineColor

**Имя нода** = Set_DimensionLineColor;

**Описание** = Specifies or returns the color of dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Object)Type;

### Нод PrimaryUnitsPrecision

**Имя нода** = PrimaryUnitsPrecision;

**Описание** = Specifies or returns the number of decimal places for the primary units of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_PrimaryUnitsPrecision

**Имя нода** = Set_PrimaryUnitsPrecision;

**Описание** = Specifies or returns the number of decimal places for the primary units of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Object)OdaX.AcDimPrecision;

### Нод FractionFormat

**Имя нода** = FractionFormat;

**Описание** = Specifies or returns how fractions display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_FractionFormat

**Имя нода** = Set_FractionFormat;

**Описание** = Specifies or returns how fractions display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Object)OdaX.AcDimFractionType;

### Нод Fit

**Имя нода** = Fit;

**Описание** = Specifies or returns how text and arrowheads are positioned when they do not fit in the space between extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_Fit

**Имя нода** = Set_Fit;

**Описание** = Specifies or returns how text and arrowheads are positioned when they do not fit in the space between extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Object)OdaX.AcDimFit;

### Нод LinearScaleFactor

**Имя нода** = LinearScaleFactor;

**Описание** = Specifies or returns the scale factor for linear dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_LinearScaleFactor

**Имя нода** = Set_LinearScaleFactor;

**Описание** = Specifies or returns the scale factor for linear dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Object)Type;

### Нод UnitsFormat

**Имя нода** = UnitsFormat;

**Описание** = Specifies or returns the unit of measurement for linear dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_UnitsFormat

**Имя нода** = Set_UnitsFormat;

**Описание** = Specifies or returns the unit of measurement for linear dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Object)OdaX.AcDimLUnits;

### Нод RoundDistance

**Имя нода** = RoundDistance;

**Описание** = Specifies or returns how measurements are rounded for all dimensions except angular dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_RoundDistance

**Имя нода** = Set_RoundDistance;

**Описание** = Specifies or returns how measurements are rounded for all dimensions except angular dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Double)Distance;

### Нод DimLineSuppress

**Имя нода** = DimLineSuppress;

**Описание** = Specifies or returns whether the dimension line displays for radial dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_DimLineSuppress

**Имя нода** = Set_DimLineSuppress;

**Описание** = Specifies or returns whether the dimension line displays for radial dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Object)bSuppress;

### Нод TextInsideAlign

**Имя нода** = TextInsideAlign;

**Описание** = Specifies or returns whether dimension text displays between extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_TextInsideAlign

**Имя нода** = Set_TextInsideAlign;

**Описание** = Specifies or returns whether dimension text displays between extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Object)bInside;

### Нод TextInside

**Имя нода** = TextInside;

**Описание** = Specifies or returns whether dimension text between extension lines is aligned horizontally or with the dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_TextInside

**Имя нода** = Set_TextInside;

**Описание** = Specifies or returns whether dimension text between extension lines is aligned horizontally or with the dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Object)bInside;

### Нод ForceLineInside

**Имя нода** = ForceLineInside;

**Описание** = Specifies or returns whether dimension lines display between extension lines when text is positioned outside of the extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_ForceLineInside

**Имя нода** = Set_ForceLineInside;

**Описание** = Specifies or returns whether dimension lines display between extension lines when text is positioned outside of the extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Object)bInside;

### Нод TextOutsideAlign

**Имя нода** = TextOutsideAlign;

**Описание** = Specifies or returns whether dimension text displays outside of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_TextOutsideAlign

**Имя нода** = Set_TextOutsideAlign;

**Описание** = Specifies or returns whether dimension text displays outside of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Object)bInside;

### Нод AltSuppressLeadingZeros

**Имя нода** = AltSuppressLeadingZeros;

**Описание** = Specifies or returns whether leading zeros display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_AltSuppressLeadingZeros

**Имя нода** = Set_AltSuppressLeadingZeros;

**Описание** = Specifies or returns whether leading zeros display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Object)bVal;

### Нод AltSuppressTrailingZeros

**Имя нода** = AltSuppressTrailingZeros;

**Описание** = Specifies or returns whether leading zeros display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_AltSuppressTrailingZeros

**Имя нода** = Set_AltSuppressTrailingZeros;

**Описание** = Specifies or returns whether leading zeros display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Object)bVal;

### Нод AltSuppressZeroFeet

**Имя нода** = AltSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_AltSuppressZeroFeet

**Имя нода** = Set_AltSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Object)bVal;

### Нод AltSuppressZeroInches

**Имя нода** = AltSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_AltSuppressZeroInches

**Имя нода** = Set_AltSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Object)bVal;

### Нод AltToleranceSuppressLeadingZeros

**Имя нода** = AltToleranceSuppressLeadingZeros;

**Описание** = Specifies or returns whether leading zeros display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_AltToleranceSuppressLeadingZeros

**Имя нода** = Set_AltToleranceSuppressLeadingZeros;

**Описание** = Specifies or returns whether leading zeros display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Object)bVal;

### Нод AltToleranceSuppressTrailingZeros

**Имя нода** = AltToleranceSuppressTrailingZeros;

**Описание** = Specifies or returns whether trailing zeros display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_AltToleranceSuppressTrailingZeros

**Имя нода** = Set_AltToleranceSuppressTrailingZeros;

**Описание** = Specifies or returns whether trailing zeros display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Object)bVal;

### Нод AltToleranceSuppressZeroFeet

**Имя нода** = AltToleranceSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_AltToleranceSuppressZeroFeet

**Имя нода** = Set_AltToleranceSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Object)bVal;

### Нод AltToleranceSuppressZeroInches

**Имя нода** = AltToleranceSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_AltToleranceSuppressZeroInches

**Имя нода** = Set_AltToleranceSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Object)bVal;

### Нод SuppressZeroFeet

**Имя нода** = SuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_SuppressZeroFeet

**Имя нода** = Set_SuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Object)bVal;

### Нод SuppressZeroInches

**Имя нода** = SuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_SuppressZeroInches

**Имя нода** = Set_SuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Object)bVal;

### Нод ToleranceSuppressZeroFeet

**Имя нода** = ToleranceSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for tolerance values of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_ToleranceSuppressZeroFeet

**Имя нода** = Set_ToleranceSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for tolerance values of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Object)bVal;

### Нод ToleranceSuppressZeroInches

**Имя нода** = ToleranceSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for tolerance values of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_ToleranceSuppressZeroInches

**Имя нода** = Set_ToleranceSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for tolerance values of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Object)bVal;

### Нод DimensionLineWeight

**Имя нода** = DimensionLineWeight;

**Описание** = Specifies or returns the lineweight of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_DimensionLineWeight

**Имя нода** = Set_DimensionLineWeight;

**Описание** = Specifies or returns the lineweight of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Object)weight;

### Нод ArrowheadSize

**Имя нода** = ArrowheadSize;

**Описание** = Specifies or returns the size of dimension arrowheads.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_ArrowheadSize

**Имя нода** = Set_ArrowheadSize;

**Описание** = Specifies or returns the size of dimension arrowheads.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Double)size;

### Нод ArrowheadType

**Имя нода** = ArrowheadType;

**Описание** = Specifies or returns the arrowhead of the dimension line for radial dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_ArrowheadType

**Имя нода** = Set_ArrowheadType;

**Описание** = Specifies or returns the arrowhead of the dimension line for radial dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Object)OdaX.AcDimArrowheadType;

### Нод Measurement

**Имя нода** = Measurement;

**Описание** = Returns the length measurement of linear dimensions or the angle measurement for angular dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод ArrowheadBlock

**Имя нода** = ArrowheadBlock;

**Описание** = Specifies or returns a custom block to replace the arrowhead of the dimension line for radial dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_ArrowheadBlock

**Имя нода** = Set_ArrowheadBlock;

**Описание** = Specifies or returns a custom block to replace the arrowhead of the dimension line for radial dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.String)BlockName;

### Нод DimensionLinetype

**Имя нода** = DimensionLinetype;

**Описание** = Specifies the linetype of a dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_DimensionLinetype

**Имя нода** = Set_DimensionLinetype;

**Описание** = Specifies the linetype of a dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.String)Linetype;

### Нод DimConstrForm

**Имя нода** = DimConstrForm;

**Описание** = Specifies the constraint type - Dynamic or Annotational;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_DimConstrForm

**Имя нода** = Set_DimConstrForm;

**Описание** = Specifies the constraint type - Dynamic or Annotational;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Object)bIsDynamic;

### Нод DimConstrReference

**Имя нода** = DimConstrReference;

**Описание** = Specifies whether the parameter is a reference constraint. Reference dimensions can be used in expressions but don't drive geometry - similar to an associative dimension;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_DimConstrReference

**Имя нода** = Set_DimConstrReference;

**Описание** = Specifies whether the parameter is a reference constraint. Reference dimensions can be used in expressions but don't drive geometry - similar to an associative dimension;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.Object)bIsReference;

### Нод DimConstrName

**Имя нода** = DimConstrName;

**Описание** = Specifies the name of the dimensional constraint. Names cannot have spaces;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_DimConstrName

**Имя нода** = Set_DimConstrName;

**Описание** = Specifies the name of the dimensional constraint. Names cannot have spaces;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.String)bstrName;

### Нод DimConstrExpression

**Имя нода** = DimConstrExpression;

**Описание** = Specifies the expression or the value of the constraint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_DimConstrExpression

**Имя нода** = Set_DimConstrExpression;

**Описание** = Specifies the expression or the value of the constraint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.String)bstrExpression;

### Нод DimConstrValue

**Имя нода** = DimConstrValue;

**Описание** = Specifies the value of the constraint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_DimConstrValue

**Имя нода** = Set_DimConstrValue;

**Описание** = Specifies the value of the constraint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.String)Value;

### Нод DimConstrDesc

**Имя нода** = DimConstrDesc;

**Описание** = Specifies description for constraint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;

### Нод Set_DimConstrDesc

**Имя нода** = Set_DimConstrDesc;

**Описание** = Specifies description for constraint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimRadial;

**Входные данные**:

* (System.Object)AcadDimRadial;
* (System.String)bstrDescription;

## Группа NVP_nanoCAD_COM.OdaX.AcadDimOrdinate

### Нод _AcadDimOrdinate_Constructor

**Имя нода** = _AcadDimOrdinate_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadDimOrdinate_ConstructorCast

**Имя нода** = _AcadDimOrdinate_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)dynamic;

### Нод AltUnits

**Имя нода** = AltUnits;

**Описание** = Specifies or returns whether alternate units are used for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;

### Нод Set_AltUnits

**Имя нода** = Set_AltUnits;

**Описание** = Specifies or returns whether alternate units are used for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;
* (System.Object)bAlternate;

### Нод AltUnitsPrecision

**Имя нода** = AltUnitsPrecision;

**Описание** = Specifies or returns the number of decimal places for alternate unit dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;

### Нод Set_AltUnitsPrecision

**Имя нода** = Set_AltUnitsPrecision;

**Описание** = Specifies or returns the number of decimal places for alternate unit dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;
* (System.Object)OdaX.AcDimPrecision;

### Нод AltUnitsScale

**Имя нода** = AltUnitsScale;

**Описание** = Specifies or returns the scale factor for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;

### Нод Set_AltUnitsScale

**Имя нода** = Set_AltUnitsScale;

**Описание** = Specifies or returns the scale factor for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;
* (System.Object)scale;

### Нод AltRoundDistance

**Имя нода** = AltRoundDistance;

**Описание** = Specifies or returns the rounding value for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;

### Нод Set_AltRoundDistance

**Имя нода** = Set_AltRoundDistance;

**Описание** = Specifies or returns the rounding value for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;
* (System.Double)Distance;

### Нод AltTolerancePrecision

**Имя нода** = AltTolerancePrecision;

**Описание** = Specifies or returns the number of decimal places for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;

### Нод Set_AltTolerancePrecision

**Имя нода** = Set_AltTolerancePrecision;

**Описание** = Specifies or returns the number of decimal places for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;
* (System.Object)OdaX.AcDimPrecision;

### Нод AltUnitsFormat

**Имя нода** = AltUnitsFormat;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;

### Нод Set_AltUnitsFormat

**Имя нода** = Set_AltUnitsFormat;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;
* (System.Object)OdaX.AcDimUnits;

### Нод AltTextPrefix

**Имя нода** = AltTextPrefix;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;

### Нод Set_AltTextPrefix

**Имя нода** = Set_AltTextPrefix;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;
* (System.String)prefix;

### Нод AltTextSuffix

**Имя нода** = AltTextSuffix;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;

### Нод Set_AltTextSuffix

**Имя нода** = Set_AltTextSuffix;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;
* (System.String)prefix;

### Нод ExtensionLineColor

**Имя нода** = ExtensionLineColor;

**Описание** = Specifies or returns the color of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;

### Нод Set_ExtensionLineColor

**Имя нода** = Set_ExtensionLineColor;

**Описание** = Specifies or returns the color of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;
* (System.Object)Type;

### Нод PrimaryUnitsPrecision

**Имя нода** = PrimaryUnitsPrecision;

**Описание** = Specifies or returns the number of decimal places for the primary units of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;

### Нод Set_PrimaryUnitsPrecision

**Имя нода** = Set_PrimaryUnitsPrecision;

**Описание** = Specifies or returns the number of decimal places for the primary units of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;
* (System.Object)OdaX.AcDimPrecision;

### Нод FractionFormat

**Имя нода** = FractionFormat;

**Описание** = Specifies or returns how fractions display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;

### Нод Set_FractionFormat

**Имя нода** = Set_FractionFormat;

**Описание** = Specifies or returns how fractions display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;
* (System.Object)OdaX.AcDimFractionType;

### Нод LinearScaleFactor

**Имя нода** = LinearScaleFactor;

**Описание** = Specifies or returns the scale factor for linear dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;

### Нод Set_LinearScaleFactor

**Имя нода** = Set_LinearScaleFactor;

**Описание** = Specifies or returns the scale factor for linear dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;
* (System.Object)Type;

### Нод UnitsFormat

**Имя нода** = UnitsFormat;

**Описание** = Specifies or returns the unit of measurement for linear dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;

### Нод Set_UnitsFormat

**Имя нода** = Set_UnitsFormat;

**Описание** = Specifies or returns the unit of measurement for linear dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;
* (System.Object)OdaX.AcDimLUnits;

### Нод ExtensionLineWeight

**Имя нода** = ExtensionLineWeight;

**Описание** = Specifies or returns the lineweight of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;

### Нод Set_ExtensionLineWeight

**Имя нода** = Set_ExtensionLineWeight;

**Описание** = Specifies or returns the lineweight of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;
* (System.Object)lweight;

### Нод RoundDistance

**Имя нода** = RoundDistance;

**Описание** = Specifies or returns how measurements are rounded for all dimensions except angular dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;

### Нод Set_RoundDistance

**Имя нода** = Set_RoundDistance;

**Описание** = Specifies or returns how measurements are rounded for all dimensions except angular dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;
* (System.Double)Distance;

### Нод ExtensionLineOffset

**Имя нода** = ExtensionLineOffset;

**Описание** = Specifies or returns the distance between extension lines and the object being dimensioned.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;

### Нод Set_ExtensionLineOffset

**Имя нода** = Set_ExtensionLineOffset;

**Описание** = Specifies or returns the distance between extension lines and the object being dimensioned.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;
* (System.Double)Offset;

### Нод AltSuppressLeadingZeros

**Имя нода** = AltSuppressLeadingZeros;

**Описание** = Specifies or returns whether leading zeros display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;

### Нод Set_AltSuppressLeadingZeros

**Имя нода** = Set_AltSuppressLeadingZeros;

**Описание** = Specifies or returns whether leading zeros display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;
* (System.Object)bVal;

### Нод AltSuppressTrailingZeros

**Имя нода** = AltSuppressTrailingZeros;

**Описание** = Specifies or returns whether leading zeros display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;

### Нод Set_AltSuppressTrailingZeros

**Имя нода** = Set_AltSuppressTrailingZeros;

**Описание** = Specifies or returns whether leading zeros display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;
* (System.Object)bVal;

### Нод AltSuppressZeroFeet

**Имя нода** = AltSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;

### Нод Set_AltSuppressZeroFeet

**Имя нода** = Set_AltSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;
* (System.Object)bVal;

### Нод AltSuppressZeroInches

**Имя нода** = AltSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;

### Нод Set_AltSuppressZeroInches

**Имя нода** = Set_AltSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;
* (System.Object)bVal;

### Нод AltToleranceSuppressLeadingZeros

**Имя нода** = AltToleranceSuppressLeadingZeros;

**Описание** = Specifies or returns whether leading zeros display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;

### Нод Set_AltToleranceSuppressLeadingZeros

**Имя нода** = Set_AltToleranceSuppressLeadingZeros;

**Описание** = Specifies or returns whether leading zeros display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;
* (System.Object)bVal;

### Нод AltToleranceSuppressTrailingZeros

**Имя нода** = AltToleranceSuppressTrailingZeros;

**Описание** = Specifies or returns whether trailing zeros display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;

### Нод Set_AltToleranceSuppressTrailingZeros

**Имя нода** = Set_AltToleranceSuppressTrailingZeros;

**Описание** = Specifies or returns whether trailing zeros display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;
* (System.Object)bVal;

### Нод AltToleranceSuppressZeroFeet

**Имя нода** = AltToleranceSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;

### Нод Set_AltToleranceSuppressZeroFeet

**Имя нода** = Set_AltToleranceSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;
* (System.Object)bVal;

### Нод AltToleranceSuppressZeroInches

**Имя нода** = AltToleranceSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;

### Нод Set_AltToleranceSuppressZeroInches

**Имя нода** = Set_AltToleranceSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;
* (System.Object)bVal;

### Нод SuppressZeroFeet

**Имя нода** = SuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;

### Нод Set_SuppressZeroFeet

**Имя нода** = Set_SuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;
* (System.Object)bVal;

### Нод SuppressZeroInches

**Имя нода** = SuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;

### Нод Set_SuppressZeroInches

**Имя нода** = Set_SuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;
* (System.Object)bVal;

### Нод ToleranceSuppressZeroFeet

**Имя нода** = ToleranceSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for tolerance values of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;

### Нод Set_ToleranceSuppressZeroFeet

**Имя нода** = Set_ToleranceSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for tolerance values of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;
* (System.Object)bVal;

### Нод ToleranceSuppressZeroInches

**Имя нода** = ToleranceSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for tolerance values of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;

### Нод Set_ToleranceSuppressZeroInches

**Имя нода** = Set_ToleranceSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for tolerance values of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;
* (System.Object)bVal;

### Нод ArrowheadSize

**Имя нода** = ArrowheadSize;

**Описание** = Specifies or returns the size of dimension arrowheads.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;

### Нод Set_ArrowheadSize

**Имя нода** = Set_ArrowheadSize;

**Описание** = Specifies or returns the size of dimension arrowheads.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;
* (System.Double)size;

### Нод Measurement

**Имя нода** = Measurement;

**Описание** = Returns the length measurement of linear dimensions or the angle measurement for angular dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;

### Нод ExtLineFixedLenSuppress

**Имя нода** = ExtLineFixedLenSuppress;

**Описание** = Specifies or returns whether fixed-length extension lines display.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;

### Нод Set_ExtLineFixedLenSuppress

**Имя нода** = Set_ExtLineFixedLenSuppress;

**Описание** = Specifies or returns whether fixed-length extension lines display.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;
* (System.Object)bFixedLen;

### Нод ExtLineFixedLen

**Имя нода** = ExtLineFixedLen;

**Описание** = Specifies or returns whether extension lines are a fixed length.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;

### Нод Set_ExtLineFixedLen

**Имя нода** = Set_ExtLineFixedLen;

**Описание** = Specifies or returns whether extension lines are a fixed length.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;
* (System.Double)FixedLen;

### Нод SubUnitsSuffix

**Имя нода** = SubUnitsSuffix;

**Описание** = Specifies the text suffix for all applicable linear dimension when change to sub-units;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;

### Нод Set_SubUnitsSuffix

**Имя нода** = Set_SubUnitsSuffix;

**Описание** = Specifies the text suffix for all applicable linear dimension when change to sub-units;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;
* (System.String)suffix;

### Нод SubUnitsFactor

**Имя нода** = SubUnitsFactor;

**Описание** = Specifies the sub-units scale factor for all applicable linear dimension;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;

### Нод Set_SubUnitsFactor

**Имя нода** = Set_SubUnitsFactor;

**Описание** = Specifies the sub-units scale factor for all applicable linear dimension;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;
* (System.Double)factor;

### Нод AltSubUnitsSuffix

**Имя нода** = AltSubUnitsSuffix;

**Описание** = Specifies the text suffix for the alternate dimension when change to alternate sub-units;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;

### Нод Set_AltSubUnitsSuffix

**Имя нода** = Set_AltSubUnitsSuffix;

**Описание** = Specifies the text suffix for the alternate dimension when change to alternate sub-units;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;
* (System.String)suffix;

### Нод AltSubUnitsFactor

**Имя нода** = AltSubUnitsFactor;

**Описание** = Specifies the alternate sub-units scale factor for all applicable linear dimension;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;

### Нод Set_AltSubUnitsFactor

**Имя нода** = Set_AltSubUnitsFactor;

**Описание** = Specifies the alternate sub-units scale factor for all applicable linear dimension;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimOrdinate;

**Входные данные**:

* (System.Object)AcadDimOrdinate;
* (System.Double)factor;

## Группа NVP_nanoCAD_COM.OdaX.AcadDimension

### Нод _AcadDimension_Constructor

**Имя нода** = _AcadDimension_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadDimension_ConstructorCast

**Имя нода** = _AcadDimension_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)dynamic;

### Нод Normal

**Имя нода** = Normal;

**Описание** = Specifies or returns the x, y, and z coordinates of the normal direction vector.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;

### Нод Set_Normal

**Имя нода** = Set_Normal;

**Описание** = Specifies or returns the x, y, and z coordinates of the normal direction vector.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;
* (System.Object)Normal;

### Нод Rotation

**Имя нода** = Rotation;

**Описание** = Specifies or returns the rotation angle of a dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;

### Нод Set_Rotation

**Имя нода** = Set_Rotation;

**Описание** = Specifies or returns the rotation angle of a dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;
* (System.Object)rotAngle;

### Нод TextPosition

**Имя нода** = TextPosition;

**Описание** = Specifies or returns the x, y, z coordinates for the text position of a dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;

### Нод Set_TextPosition

**Имя нода** = Set_TextPosition;

**Описание** = Specifies or returns the x, y, z coordinates for the text position of a dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;
* (System.Object)textPos;

### Нод TextRotation

**Имя нода** = TextRotation;

**Описание** = Specifies or returns the rotation angle of dimension text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;

### Нод Set_TextRotation

**Имя нода** = Set_TextRotation;

**Описание** = Specifies or returns the rotation angle of dimension text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;
* (System.Object)rotAngle;

### Нод TextOverride

**Имя нода** = TextOverride;

**Описание** = Specifies or returns a dimension text string that displays in addition to or instead of the dimension measurement.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;

### Нод Set_TextOverride

**Имя нода** = Set_TextOverride;

**Описание** = Specifies or returns a dimension text string that displays in addition to or instead of the dimension measurement.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;
* (System.String)bstrText;

### Нод StyleName

**Имя нода** = StyleName;

**Описание** = Specifies or returns the name of the dimension style assigned to a dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;

### Нод Set_StyleName

**Имя нода** = Set_StyleName;

**Описание** = Specifies or returns the name of the dimension style assigned to a dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;
* (System.String)bstrName;

### Нод TextColor

**Имя нода** = TextColor;

**Описание** = Specifies or returns the text color of a dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;

### Нод Set_TextColor

**Имя нода** = Set_TextColor;

**Описание** = Specifies or returns the text color of a dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;
* (System.Object)color;

### Нод DecimalSeparator

**Имя нода** = DecimalSeparator;

**Описание** = Specifies or returns the decimal separator of a dimension formatted for decimals.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;

### Нод Set_DecimalSeparator

**Имя нода** = Set_DecimalSeparator;

**Описание** = Specifies or returns the decimal separator of a dimension formatted for decimals.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;
* (System.String)character;

### Нод TextGap

**Имя нода** = TextGap;

**Описание** = Specifies or returns the amount of blank space surrounding dimension text when the text causes a dimension line to break.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;

### Нод Set_TextGap

**Имя нода** = Set_TextGap;

**Описание** = Specifies or returns the amount of blank space surrounding dimension text when the text causes a dimension line to break.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;
* (System.Double)Offset;

### Нод TextPrefix

**Имя нода** = TextPrefix;

**Описание** = Specifies or returns the prefix for text of a dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;

### Нод Set_TextPrefix

**Имя нода** = Set_TextPrefix;

**Описание** = Specifies or returns the prefix for text of a dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;
* (System.String)prefix;

### Нод TextSuffix

**Имя нода** = TextSuffix;

**Описание** = Specifies or returns the suffix for text of a dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;

### Нод Set_TextSuffix

**Имя нода** = Set_TextSuffix;

**Описание** = Specifies or returns the suffix for text of a dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;
* (System.String)suffix;

### Нод ScaleFactor

**Имя нода** = ScaleFactor;

**Описание** = Specifies or returns the scale factor of a dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;

### Нод Set_ScaleFactor

**Имя нода** = Set_ScaleFactor;

**Описание** = Specifies or returns the scale factor of a dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;
* (System.Object)factor;

### Нод VerticalTextPosition

**Имя нода** = VerticalTextPosition;

**Описание** = Specifies or returns the vertical position of text for a dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;

### Нод Set_VerticalTextPosition

**Имя нода** = Set_VerticalTextPosition;

**Описание** = Specifies or returns the vertical position of text for a dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;
* (System.Object)OdaX.AcDimVerticalJustification;

### Нод TolerancePrecision

**Имя нода** = TolerancePrecision;

**Описание** = Specifies or returns the number of decimal places for tolerance values of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;

### Нод Set_TolerancePrecision

**Имя нода** = Set_TolerancePrecision;

**Описание** = Specifies or returns the number of decimal places for tolerance values of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;
* (System.Object)OdaX.AcDimPrecision;

### Нод ToleranceHeightScale

**Имя нода** = ToleranceHeightScale;

**Описание** = Specifies or returns the scale factor of tolerance text height in relation to dimension text height.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;

### Нод Set_ToleranceHeightScale

**Имя нода** = Set_ToleranceHeightScale;

**Описание** = Specifies or returns the scale factor of tolerance text height in relation to dimension text height.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;
* (System.Object)scale;

### Нод ToleranceLowerLimit

**Имя нода** = ToleranceLowerLimit;

**Описание** = Specifies or returns the lower tolerance limit for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;

### Нод Set_ToleranceLowerLimit

**Имя нода** = Set_ToleranceLowerLimit;

**Описание** = Specifies or returns the lower tolerance limit for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;
* (System.Double)lower;

### Нод TextMovement

**Имя нода** = TextMovement;

**Описание** = Specifies or returns how dimension lines move when the corresponding dimension text moves.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;

### Нод Set_TextMovement

**Имя нода** = Set_TextMovement;

**Описание** = Specifies or returns how dimension lines move when the corresponding dimension text moves.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;
* (System.Object)OdaX.AcDimTextMovement;

### Нод ToleranceDisplay

**Имя нода** = ToleranceDisplay;

**Описание** = Specifies or returns whether dimension tolerance values display with dimension text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;

### Нод Set_ToleranceDisplay

**Имя нода** = Set_ToleranceDisplay;

**Описание** = Specifies or returns whether dimension tolerance values display with dimension text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;
* (System.Object)OdaX.AcDimToleranceMethod;

### Нод ToleranceJustification

**Имя нода** = ToleranceJustification;

**Описание** = Specifies or returns the vertical placement of tolerance values for dimension text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;

### Нод Set_ToleranceJustification

**Имя нода** = Set_ToleranceJustification;

**Описание** = Specifies or returns the vertical placement of tolerance values for dimension text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;
* (System.Object)OdaX.AcDimToleranceJustify;

### Нод ToleranceUpperLimit

**Имя нода** = ToleranceUpperLimit;

**Описание** = Specifies or returns the upper tolerance limit for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;

### Нод Set_ToleranceUpperLimit

**Имя нода** = Set_ToleranceUpperLimit;

**Описание** = Specifies or returns the upper tolerance limit for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;
* (System.Double)upper;

### Нод TextStyle

**Имя нода** = TextStyle;

**Описание** = Specifies or returns the text style of a dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;

### Нод Set_TextStyle

**Имя нода** = Set_TextStyle;

**Описание** = Specifies or returns the text style of a dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;
* (System.String)style;

### Нод TextHeight

**Имя нода** = TextHeight;

**Описание** = Specifies or returns the text height of a dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;

### Нод Set_TextHeight

**Имя нода** = Set_TextHeight;

**Описание** = Specifies or returns the text height of a dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;
* (System.Double)Height;

### Нод SuppressLeadingZeros

**Имя нода** = SuppressLeadingZeros;

**Описание** = Specifies or returns whether leading zeros display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;

### Нод Set_SuppressLeadingZeros

**Имя нода** = Set_SuppressLeadingZeros;

**Описание** = Specifies or returns whether leading zeros display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;
* (System.Object)bVal;

### Нод SuppressTrailingZeros

**Имя нода** = SuppressTrailingZeros;

**Описание** = Specifies or returns whether trailing zeros display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;

### Нод Set_SuppressTrailingZeros

**Имя нода** = Set_SuppressTrailingZeros;

**Описание** = Specifies or returns whether trailing zeros display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;
* (System.Object)bVal;

### Нод ToleranceSuppressLeadingZeros

**Имя нода** = ToleranceSuppressLeadingZeros;

**Описание** = Specifies or returns whether leading zeros display for tolerance values of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;

### Нод Set_ToleranceSuppressLeadingZeros

**Имя нода** = Set_ToleranceSuppressLeadingZeros;

**Описание** = Specifies or returns whether leading zeros display for tolerance values of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;
* (System.Object)bVal;

### Нод ToleranceSuppressTrailingZeros

**Имя нода** = ToleranceSuppressTrailingZeros;

**Описание** = Specifies or returns whether trailing zeros display for tolerance values of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;

### Нод Set_ToleranceSuppressTrailingZeros

**Имя нода** = Set_ToleranceSuppressTrailingZeros;

**Описание** = Specifies or returns whether trailing zeros display for tolerance values of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;
* (System.Object)bVal;

### Нод TextFill

**Имя нода** = TextFill;

**Описание** = Not implemented. Specifies or returns the background fill of dimension text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;

### Нод Set_TextFill

**Имя нода** = Set_TextFill;

**Описание** = Not implemented. Specifies or returns the background fill of dimension text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;
* (System.Object)bVal;

### Нод TextFillColor

**Имя нода** = TextFillColor;

**Описание** = Not implemented. Specifies or returns the fill color for dimension text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;

### Нод Set_TextFillColor

**Имя нода** = Set_TextFillColor;

**Описание** = Not implemented. Specifies or returns the fill color for dimension text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimension;

**Входные данные**:

* (System.Object)AcadDimension;
* (System.Object)color;

## Группа NVP_nanoCAD_COM.OdaX.AcadDimDiametric

### Нод _AcadDimDiametric_Constructor

**Имя нода** = _AcadDimDiametric_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadDimDiametric_ConstructorCast

**Имя нода** = _AcadDimDiametric_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)dynamic;

### Нод Set_LeaderLength

**Имя нода** = Set_LeaderLength;

**Описание** = Specifies the length of the leader used with the diametral dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Double)rhs;

### Нод AltUnits

**Имя нода** = AltUnits;

**Описание** = Specifies or returns whether alternate units are used for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_AltUnits

**Имя нода** = Set_AltUnits;

**Описание** = Specifies or returns whether alternate units are used for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Object)bAlternate;

### Нод AltUnitsPrecision

**Имя нода** = AltUnitsPrecision;

**Описание** = Specifies or returns the number of decimal places for alternate unit dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_AltUnitsPrecision

**Имя нода** = Set_AltUnitsPrecision;

**Описание** = Specifies or returns the number of decimal places for alternate unit dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Object)OdaX.AcDimPrecision;

### Нод AltUnitsScale

**Имя нода** = AltUnitsScale;

**Описание** = Specifies or returns the scale factor for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_AltUnitsScale

**Имя нода** = Set_AltUnitsScale;

**Описание** = Specifies or returns the scale factor for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Object)scale;

### Нод AltRoundDistance

**Имя нода** = AltRoundDistance;

**Описание** = Specifies or returns the rounding value for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_AltRoundDistance

**Имя нода** = Set_AltRoundDistance;

**Описание** = Specifies or returns the rounding value for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Double)Distance;

### Нод AltTolerancePrecision

**Имя нода** = AltTolerancePrecision;

**Описание** = Specifies or returns the number of decimal places for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_AltTolerancePrecision

**Имя нода** = Set_AltTolerancePrecision;

**Описание** = Specifies or returns the number of decimal places for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Object)OdaX.AcDimPrecision;

### Нод AltUnitsFormat

**Имя нода** = AltUnitsFormat;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_AltUnitsFormat

**Имя нода** = Set_AltUnitsFormat;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Object)OdaX.AcDimUnits;

### Нод AltTextPrefix

**Имя нода** = AltTextPrefix;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_AltTextPrefix

**Имя нода** = Set_AltTextPrefix;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.String)prefix;

### Нод AltTextSuffix

**Имя нода** = AltTextSuffix;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_AltTextSuffix

**Имя нода** = Set_AltTextSuffix;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.String)prefix;

### Нод DimensionLineColor

**Имя нода** = DimensionLineColor;

**Описание** = Specifies or returns the color of dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_DimensionLineColor

**Имя нода** = Set_DimensionLineColor;

**Описание** = Specifies or returns the color of dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Object)Type;

### Нод PrimaryUnitsPrecision

**Имя нода** = PrimaryUnitsPrecision;

**Описание** = Specifies or returns the number of decimal places for the primary units of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_PrimaryUnitsPrecision

**Имя нода** = Set_PrimaryUnitsPrecision;

**Описание** = Specifies or returns the number of decimal places for the primary units of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Object)OdaX.AcDimPrecision;

### Нод FractionFormat

**Имя нода** = FractionFormat;

**Описание** = Specifies or returns how fractions display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_FractionFormat

**Имя нода** = Set_FractionFormat;

**Описание** = Specifies or returns how fractions display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Object)OdaX.AcDimFractionType;

### Нод Fit

**Имя нода** = Fit;

**Описание** = Specifies or returns how text and arrowheads are positioned when they do not fit in the space between extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_Fit

**Имя нода** = Set_Fit;

**Описание** = Specifies or returns how text and arrowheads are positioned when they do not fit in the space between extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Object)OdaX.AcDimFit;

### Нод LinearScaleFactor

**Имя нода** = LinearScaleFactor;

**Описание** = Specifies or returns the scale factor for linear dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_LinearScaleFactor

**Имя нода** = Set_LinearScaleFactor;

**Описание** = Specifies or returns the scale factor for linear dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Object)Type;

### Нод UnitsFormat

**Имя нода** = UnitsFormat;

**Описание** = Specifies or returns the unit of measurement for linear dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_UnitsFormat

**Имя нода** = Set_UnitsFormat;

**Описание** = Specifies or returns the unit of measurement for linear dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Object)OdaX.AcDimLUnits;

### Нод RoundDistance

**Имя нода** = RoundDistance;

**Описание** = Specifies or returns how measurements are rounded for all dimensions except angular dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_RoundDistance

**Имя нода** = Set_RoundDistance;

**Описание** = Specifies or returns how measurements are rounded for all dimensions except angular dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Double)Distance;

### Нод DimLine1Suppress

**Имя нода** = DimLine1Suppress;

**Описание** = Specifies or returns whether the first dimension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_DimLine1Suppress

**Имя нода** = Set_DimLine1Suppress;

**Описание** = Specifies or returns whether the first dimension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Object)bSuppress;

### Нод DimLine2Suppress

**Имя нода** = DimLine2Suppress;

**Описание** = Specifies or returns whether the second dimension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_DimLine2Suppress

**Имя нода** = Set_DimLine2Suppress;

**Описание** = Specifies or returns whether the second dimension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Object)bSuppress;

### Нод TextInsideAlign

**Имя нода** = TextInsideAlign;

**Описание** = Specifies or returns whether dimension text displays between extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_TextInsideAlign

**Имя нода** = Set_TextInsideAlign;

**Описание** = Specifies or returns whether dimension text displays between extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Object)bInside;

### Нод TextInside

**Имя нода** = TextInside;

**Описание** = Specifies or returns whether dimension text between extension lines is aligned horizontally or with the dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_TextInside

**Имя нода** = Set_TextInside;

**Описание** = Specifies or returns whether dimension text between extension lines is aligned horizontally or with the dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Object)bInside;

### Нод ForceLineInside

**Имя нода** = ForceLineInside;

**Описание** = Specifies or returns whether dimension lines display between extension lines when text is positioned outside of the extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_ForceLineInside

**Имя нода** = Set_ForceLineInside;

**Описание** = Specifies or returns whether dimension lines display between extension lines when text is positioned outside of the extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Object)bInside;

### Нод TextOutsideAlign

**Имя нода** = TextOutsideAlign;

**Описание** = Specifies or returns whether dimension text displays outside of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_TextOutsideAlign

**Имя нода** = Set_TextOutsideAlign;

**Описание** = Specifies or returns whether dimension text displays outside of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Object)bInside;

### Нод CenterType

**Имя нода** = CenterType;

**Описание** = Specifies or returns the type of center mark used for diametral dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_CenterType

**Имя нода** = Set_CenterType;

**Описание** = Specifies or returns the type of center mark used for diametral dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Object)OdaX.AcDimCenterType;

### Нод CenterMarkSize

**Имя нода** = CenterMarkSize;

**Описание** = Specifies or returns the size of the center mark used for diametral dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_CenterMarkSize

**Имя нода** = Set_CenterMarkSize;

**Описание** = Specifies or returns the size of the center mark used for diametral dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Double)Type;

### Нод AltSuppressLeadingZeros

**Имя нода** = AltSuppressLeadingZeros;

**Описание** = Specifies or returns whether leading zeros display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_AltSuppressLeadingZeros

**Имя нода** = Set_AltSuppressLeadingZeros;

**Описание** = Specifies or returns whether leading zeros display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Object)bVal;

### Нод AltSuppressTrailingZeros

**Имя нода** = AltSuppressTrailingZeros;

**Описание** = Specifies or returns whether leading zeros display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_AltSuppressTrailingZeros

**Имя нода** = Set_AltSuppressTrailingZeros;

**Описание** = Specifies or returns whether leading zeros display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Object)bVal;

### Нод AltSuppressZeroFeet

**Имя нода** = AltSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_AltSuppressZeroFeet

**Имя нода** = Set_AltSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Object)bVal;

### Нод AltSuppressZeroInches

**Имя нода** = AltSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_AltSuppressZeroInches

**Имя нода** = Set_AltSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Object)bVal;

### Нод AltToleranceSuppressLeadingZeros

**Имя нода** = AltToleranceSuppressLeadingZeros;

**Описание** = Specifies or returns whether leading zeros display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_AltToleranceSuppressLeadingZeros

**Имя нода** = Set_AltToleranceSuppressLeadingZeros;

**Описание** = Specifies or returns whether leading zeros display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Object)bVal;

### Нод AltToleranceSuppressTrailingZeros

**Имя нода** = AltToleranceSuppressTrailingZeros;

**Описание** = Specifies or returns whether trailing zeros display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_AltToleranceSuppressTrailingZeros

**Имя нода** = Set_AltToleranceSuppressTrailingZeros;

**Описание** = Specifies or returns whether trailing zeros display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Object)bVal;

### Нод AltToleranceSuppressZeroFeet

**Имя нода** = AltToleranceSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_AltToleranceSuppressZeroFeet

**Имя нода** = Set_AltToleranceSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Object)bVal;

### Нод AltToleranceSuppressZeroInches

**Имя нода** = AltToleranceSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_AltToleranceSuppressZeroInches

**Имя нода** = Set_AltToleranceSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Object)bVal;

### Нод SuppressZeroFeet

**Имя нода** = SuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_SuppressZeroFeet

**Имя нода** = Set_SuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Object)bVal;

### Нод SuppressZeroInches

**Имя нода** = SuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_SuppressZeroInches

**Имя нода** = Set_SuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Object)bVal;

### Нод ToleranceSuppressZeroFeet

**Имя нода** = ToleranceSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for tolerance values of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_ToleranceSuppressZeroFeet

**Имя нода** = Set_ToleranceSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for tolerance values of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Object)bVal;

### Нод ToleranceSuppressZeroInches

**Имя нода** = ToleranceSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for tolerance values of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_ToleranceSuppressZeroInches

**Имя нода** = Set_ToleranceSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for tolerance values of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Object)bVal;

### Нод DimensionLineWeight

**Имя нода** = DimensionLineWeight;

**Описание** = Specifies or returns the lineweight of dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_DimensionLineWeight

**Имя нода** = Set_DimensionLineWeight;

**Описание** = Specifies or returns the lineweight of dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Object)weight;

### Нод ArrowheadSize

**Имя нода** = ArrowheadSize;

**Описание** = Specifies or returns the size of dimension arrowheads.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_ArrowheadSize

**Имя нода** = Set_ArrowheadSize;

**Описание** = Specifies or returns the size of dimension arrowheads.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Double)size;

### Нод Arrowhead1Type

**Имя нода** = Arrowhead1Type;

**Описание** = Specifies or returns the arrowhead of the first dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_Arrowhead1Type

**Имя нода** = Set_Arrowhead1Type;

**Описание** = Specifies or returns the arrowhead of the first dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Object)OdaX.AcDimArrowheadType;

### Нод Arrowhead2Type

**Имя нода** = Arrowhead2Type;

**Описание** = Specifies or returns the arrowhead of the second dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_Arrowhead2Type

**Имя нода** = Set_Arrowhead2Type;

**Описание** = Specifies or returns the arrowhead of the second dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Object)OdaX.AcDimArrowheadType;

### Нод Measurement

**Имя нода** = Measurement;

**Описание** = Returns the length measurement of linear dimensions or the angle measurement for angular dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Arrowhead1Block

**Имя нода** = Arrowhead1Block;

**Описание** = Specifies or returns a custom block to replace the arrowhead of the first dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_Arrowhead1Block

**Имя нода** = Set_Arrowhead1Block;

**Описание** = Specifies or returns a custom block to replace the arrowhead of the first dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.String)BlockName;

### Нод Arrowhead2Block

**Имя нода** = Arrowhead2Block;

**Описание** = Specifies or returns a custom block to replace the arrowhead of the second dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_Arrowhead2Block

**Имя нода** = Set_Arrowhead2Block;

**Описание** = Specifies or returns a custom block to replace the arrowhead of the second dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.String)BlockName;

### Нод DimensionLinetype

**Имя нода** = DimensionLinetype;

**Описание** = Specifies the linetype of a dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_DimensionLinetype

**Имя нода** = Set_DimensionLinetype;

**Описание** = Specifies the linetype of a dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.String)Linetype;

### Нод DimConstrForm

**Имя нода** = DimConstrForm;

**Описание** = Specifies the constraint type - Dynamic or Annotational;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_DimConstrForm

**Имя нода** = Set_DimConstrForm;

**Описание** = Specifies the constraint type - Dynamic or Annotational;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Object)bIsDynamic;

### Нод DimConstrReference

**Имя нода** = DimConstrReference;

**Описание** = Specifies whether the parameter is a reference constraint. Reference dimensions can be used in expressions but don't drive geometry - similar to an associative dimension;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_DimConstrReference

**Имя нода** = Set_DimConstrReference;

**Описание** = Specifies whether the parameter is a reference constraint. Reference dimensions can be used in expressions but don't drive geometry - similar to an associative dimension;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.Object)bIsReference;

### Нод DimConstrName

**Имя нода** = DimConstrName;

**Описание** = Specifies the name of the dimensional constraint. Names cannot have spaces;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_DimConstrName

**Имя нода** = Set_DimConstrName;

**Описание** = Specifies the name of the dimensional constraint. Names cannot have spaces;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.String)bstrName;

### Нод DimConstrExpression

**Имя нода** = DimConstrExpression;

**Описание** = Specifies the expression or the value of the constraint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_DimConstrExpression

**Имя нода** = Set_DimConstrExpression;

**Описание** = Specifies the expression or the value of the constraint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.String)bstrExpression;

### Нод DimConstrValue

**Имя нода** = DimConstrValue;

**Описание** = Specifies the value of the constraint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_DimConstrValue

**Имя нода** = Set_DimConstrValue;

**Описание** = Specifies the value of the constraint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.String)Value;

### Нод DimConstrDesc

**Имя нода** = DimConstrDesc;

**Описание** = Specifies description for constraint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;

### Нод Set_DimConstrDesc

**Имя нода** = Set_DimConstrDesc;

**Описание** = Specifies description for constraint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimDiametric;

**Входные данные**:

* (System.Object)AcadDimDiametric;
* (System.String)bstrDescription;

## Группа NVP_nanoCAD_COM.OdaX.AcadDimArcLength

### Нод _AcadDimArcLength_Constructor

**Имя нода** = _AcadDimArcLength_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadDimArcLength_ConstructorCast

**Имя нода** = _AcadDimArcLength_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)dynamic;

### Нод AltUnits

**Имя нода** = AltUnits;

**Описание** = Specifies or returns whether alternate units are used for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_AltUnits

**Имя нода** = Set_AltUnits;

**Описание** = Specifies or returns whether alternate units are used for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)bAlternate;

### Нод AltUnitsPrecision

**Имя нода** = AltUnitsPrecision;

**Описание** = Specifies or returns the number of decimal places for alternate unit dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_AltUnitsPrecision

**Имя нода** = Set_AltUnitsPrecision;

**Описание** = Specifies or returns the number of decimal places for alternate unit dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)OdaX.AcDimPrecision;

### Нод AltUnitsScale

**Имя нода** = AltUnitsScale;

**Описание** = Specifies or returns the scale factor for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_AltUnitsScale

**Имя нода** = Set_AltUnitsScale;

**Описание** = Specifies or returns the scale factor for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)scale;

### Нод AltRoundDistance

**Имя нода** = AltRoundDistance;

**Описание** = Specifies or returns the rounding value for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_AltRoundDistance

**Имя нода** = Set_AltRoundDistance;

**Описание** = Specifies or returns the rounding value for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Double)Distance;

### Нод AltTolerancePrecision

**Имя нода** = AltTolerancePrecision;

**Описание** = Specifies or returns the number of decimal places for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_AltTolerancePrecision

**Имя нода** = Set_AltTolerancePrecision;

**Описание** = Specifies or returns the number of decimal places for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)OdaX.AcDimPrecision;

### Нод AltUnitsFormat

**Имя нода** = AltUnitsFormat;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_AltUnitsFormat

**Имя нода** = Set_AltUnitsFormat;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)OdaX.AcDimUnits;

### Нод AltTextPrefix

**Имя нода** = AltTextPrefix;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_AltTextPrefix

**Имя нода** = Set_AltTextPrefix;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.String)prefix;

### Нод AltTextSuffix

**Имя нода** = AltTextSuffix;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_AltTextSuffix

**Имя нода** = Set_AltTextSuffix;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.String)prefix;

### Нод DimensionLineColor

**Имя нода** = DimensionLineColor;

**Описание** = Specifies or returns the color of dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_DimensionLineColor

**Имя нода** = Set_DimensionLineColor;

**Описание** = Specifies or returns the color of dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)Type;

### Нод ExtensionLineColor

**Имя нода** = ExtensionLineColor;

**Описание** = Specifies or returns the color of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_ExtensionLineColor

**Имя нода** = Set_ExtensionLineColor;

**Описание** = Specifies or returns the color of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)Type;

### Нод PrimaryUnitsPrecision

**Имя нода** = PrimaryUnitsPrecision;

**Описание** = Specifies or returns the number of decimal places for the primary units of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_PrimaryUnitsPrecision

**Имя нода** = Set_PrimaryUnitsPrecision;

**Описание** = Specifies or returns the number of decimal places for the primary units of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)OdaX.AcDimPrecision;

### Нод DimensionLineExtend

**Имя нода** = DimensionLineExtend;

**Описание** = Specifies or returns how far dimension lines extend beyond extension lines when using dimension lines with oblique strokes.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_DimensionLineExtend

**Имя нода** = Set_DimensionLineExtend;

**Описание** = Specifies or returns how far dimension lines extend beyond extension lines when using dimension lines with oblique strokes.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Double)extend;

### Нод ExtensionLineExtend

**Имя нода** = ExtensionLineExtend;

**Описание** = Specifies or returns how far extension lines extend beyond dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_ExtensionLineExtend

**Имя нода** = Set_ExtensionLineExtend;

**Описание** = Specifies or returns how far extension lines extend beyond dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Double)extend;

### Нод Fit

**Имя нода** = Fit;

**Описание** = Specifies or returns how text and arrowheads are positioned when they do not fit in the space between extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_Fit

**Имя нода** = Set_Fit;

**Описание** = Specifies or returns how text and arrowheads are positioned when they do not fit in the space between extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)OdaX.AcDimFit;

### Нод FractionFormat

**Имя нода** = FractionFormat;

**Описание** = Specifies or returns how fractions display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_FractionFormat

**Имя нода** = Set_FractionFormat;

**Описание** = Specifies or returns how fractions display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)OdaX.AcDimFractionType;

### Нод HorizontalTextPosition

**Имя нода** = HorizontalTextPosition;

**Описание** = Specifies or returns the horizontal position of dimension text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_HorizontalTextPosition

**Имя нода** = Set_HorizontalTextPosition;

**Описание** = Specifies or returns the horizontal position of dimension text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)OdaX.AcDimHorizontalJustification;

### Нод LinearScaleFactor

**Имя нода** = LinearScaleFactor;

**Описание** = Specifies or returns the scale factor for linear dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_LinearScaleFactor

**Имя нода** = Set_LinearScaleFactor;

**Описание** = Specifies or returns the scale factor for linear dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)Type;

### Нод UnitsFormat

**Имя нода** = UnitsFormat;

**Описание** = Specifies or returns the unit of measurement for linear dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_UnitsFormat

**Имя нода** = Set_UnitsFormat;

**Описание** = Specifies or returns the unit of measurement for linear dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)OdaX.AcDimLUnits;

### Нод ExtensionLineWeight

**Имя нода** = ExtensionLineWeight;

**Описание** = Specifies or returns the lineweight of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_ExtensionLineWeight

**Имя нода** = Set_ExtensionLineWeight;

**Описание** = Specifies or returns the lineweight of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)lweight;

### Нод RoundDistance

**Имя нода** = RoundDistance;

**Описание** = Specifies or returns how measurements are rounded for all dimensions except angular dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_RoundDistance

**Имя нода** = Set_RoundDistance;

**Описание** = Specifies or returns how measurements are rounded for all dimensions except angular dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Double)Distance;

### Нод DimLine1Suppress

**Имя нода** = DimLine1Suppress;

**Описание** = Specifies or returns whether the first dimension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_DimLine1Suppress

**Имя нода** = Set_DimLine1Suppress;

**Описание** = Specifies or returns whether the first dimension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)bSuppress;

### Нод DimLine2Suppress

**Имя нода** = DimLine2Suppress;

**Описание** = Specifies or returns whether the second dimension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_DimLine2Suppress

**Имя нода** = Set_DimLine2Suppress;

**Описание** = Specifies or returns whether the second dimension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)bSuppress;

### Нод ExtLine1Suppress

**Имя нода** = ExtLine1Suppress;

**Описание** = Specifies or returns whether the first extension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_ExtLine1Suppress

**Имя нода** = Set_ExtLine1Suppress;

**Описание** = Specifies or returns whether the first extension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)bSuppress;

### Нод ExtLine2Suppress

**Имя нода** = ExtLine2Suppress;

**Описание** = Specifies or returns whether the second extension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_ExtLine2Suppress

**Имя нода** = Set_ExtLine2Suppress;

**Описание** = Specifies or returns whether the second extension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)bSuppress;

### Нод DimLineInside

**Имя нода** = DimLineInside;

**Описание** = Specifies or returns whether dimension lines are drawn outside of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_DimLineInside

**Имя нода** = Set_DimLineInside;

**Описание** = Specifies or returns whether dimension lines are drawn outside of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)bInside;

### Нод TextInsideAlign

**Имя нода** = TextInsideAlign;

**Описание** = Specifies or returns whether dimension text displays between extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_TextInsideAlign

**Имя нода** = Set_TextInsideAlign;

**Описание** = Specifies or returns whether dimension text displays between extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)bInside;

### Нод TextInside

**Имя нода** = TextInside;

**Описание** = Specifies or returns whether dimension text between extension lines is aligned horizontally or with the dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_TextInside

**Имя нода** = Set_TextInside;

**Описание** = Specifies or returns whether dimension text between extension lines is aligned horizontally or with the dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)bInside;

### Нод ForceLineInside

**Имя нода** = ForceLineInside;

**Описание** = Specifies or returns whether dimension lines display between extension lines when text is positioned outside of the extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_ForceLineInside

**Имя нода** = Set_ForceLineInside;

**Описание** = Specifies or returns whether dimension lines display between extension lines when text is positioned outside of the extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)bInside;

### Нод TextOutsideAlign

**Имя нода** = TextOutsideAlign;

**Описание** = Specifies or returns whether dimension text displays outside of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_TextOutsideAlign

**Имя нода** = Set_TextOutsideAlign;

**Описание** = Specifies or returns whether dimension text displays outside of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)bInside;

### Нод ExtensionLineOffset

**Имя нода** = ExtensionLineOffset;

**Описание** = Specifies or returns the distance between extension lines and the object being dimensioned.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_ExtensionLineOffset

**Имя нода** = Set_ExtensionLineOffset;

**Описание** = Specifies or returns the distance between extension lines and the object being dimensioned.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Double)Offset;

### Нод AltSuppressLeadingZeros

**Имя нода** = AltSuppressLeadingZeros;

**Описание** = Specifies or returns whether leading zeros display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_AltSuppressLeadingZeros

**Имя нода** = Set_AltSuppressLeadingZeros;

**Описание** = Specifies or returns whether leading zeros display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)bVal;

### Нод AltSuppressTrailingZeros

**Имя нода** = AltSuppressTrailingZeros;

**Описание** = Specifies or returns whether leading zeros display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_AltSuppressTrailingZeros

**Имя нода** = Set_AltSuppressTrailingZeros;

**Описание** = Specifies or returns whether leading zeros display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)bVal;

### Нод AltSuppressZeroFeet

**Имя нода** = AltSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_AltSuppressZeroFeet

**Имя нода** = Set_AltSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)bVal;

### Нод AltSuppressZeroInches

**Имя нода** = AltSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_AltSuppressZeroInches

**Имя нода** = Set_AltSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)bVal;

### Нод AltToleranceSuppressLeadingZeros

**Имя нода** = AltToleranceSuppressLeadingZeros;

**Описание** = Specifies or returns whether leading zeros display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_AltToleranceSuppressLeadingZeros

**Имя нода** = Set_AltToleranceSuppressLeadingZeros;

**Описание** = Specifies or returns whether leading zeros display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)bVal;

### Нод AltToleranceSuppressTrailingZeros

**Имя нода** = AltToleranceSuppressTrailingZeros;

**Описание** = Specifies or returns whether trailing zeros display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_AltToleranceSuppressTrailingZeros

**Имя нода** = Set_AltToleranceSuppressTrailingZeros;

**Описание** = Specifies or returns whether trailing zeros display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)bVal;

### Нод AltToleranceSuppressZeroFeet

**Имя нода** = AltToleranceSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_AltToleranceSuppressZeroFeet

**Имя нода** = Set_AltToleranceSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)bVal;

### Нод AltToleranceSuppressZeroInches

**Имя нода** = AltToleranceSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_AltToleranceSuppressZeroInches

**Имя нода** = Set_AltToleranceSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)bVal;

### Нод SuppressZeroFeet

**Имя нода** = SuppressZeroFeet;

**Описание** = SSpecifies or returns whether zero feet measurements display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_SuppressZeroFeet

**Имя нода** = Set_SuppressZeroFeet;

**Описание** = SSpecifies or returns whether zero feet measurements display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)bVal;

### Нод SuppressZeroInches

**Имя нода** = SuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_SuppressZeroInches

**Имя нода** = Set_SuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)bVal;

### Нод ToleranceSuppressZeroFeet

**Имя нода** = ToleranceSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for tolerance values of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_ToleranceSuppressZeroFeet

**Имя нода** = Set_ToleranceSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for tolerance values of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)bVal;

### Нод ToleranceSuppressZeroInches

**Имя нода** = ToleranceSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for tolerance values of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_ToleranceSuppressZeroInches

**Имя нода** = Set_ToleranceSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for tolerance values of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)bVal;

### Нод DimensionLineWeight

**Имя нода** = DimensionLineWeight;

**Описание** = Specifies or returns the lineweight of dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_DimensionLineWeight

**Имя нода** = Set_DimensionLineWeight;

**Описание** = Specifies or returns the lineweight of dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)weight;

### Нод ArrowheadSize

**Имя нода** = ArrowheadSize;

**Описание** = Specifies or returns the size of dimension arrowheads.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_ArrowheadSize

**Имя нода** = Set_ArrowheadSize;

**Описание** = Specifies or returns the size of dimension arrowheads.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Double)size;

### Нод Arrowhead1Type

**Имя нода** = Arrowhead1Type;

**Описание** = Specifies or returns the arrowhead of the first dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_Arrowhead1Type

**Имя нода** = Set_Arrowhead1Type;

**Описание** = Specifies or returns the arrowhead of the first dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)OdaX.AcDimArrowheadType;

### Нод Arrowhead2Type

**Имя нода** = Arrowhead2Type;

**Описание** = Specifies or returns the arrowhead of the second dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_Arrowhead2Type

**Имя нода** = Set_Arrowhead2Type;

**Описание** = Specifies or returns the arrowhead of the second dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)OdaX.AcDimArrowheadType;

### Нод Measurement

**Имя нода** = Measurement;

**Описание** = Returns the length measurement of linear dimensions or the angle measurement for angular dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Arrowhead1Block

**Имя нода** = Arrowhead1Block;

**Описание** = Specifies or returns the arrowhead of the first dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_Arrowhead1Block

**Имя нода** = Set_Arrowhead1Block;

**Описание** = Specifies or returns the arrowhead of the first dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.String)BlockName;

### Нод Arrowhead2Block

**Имя нода** = Arrowhead2Block;

**Описание** = Specifies or returns the arrowhead of the second dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_Arrowhead2Block

**Имя нода** = Set_Arrowhead2Block;

**Описание** = Specifies or returns the arrowhead of the second dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.String)BlockName;

### Нод ArcPoint

**Имя нода** = ArcPoint;

**Описание** = Specifies or returns a point on the arc.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_ArcPoint

**Имя нода** = Set_ArcPoint;

**Описание** = Specifies or returns a point on the arc.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)pVal;

### Нод ExtLine1Point

**Имя нода** = ExtLine1Point;

**Описание** = Specifies or returns the start point of the first extension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_ExtLine1Point

**Имя нода** = Set_ExtLine1Point;

**Описание** = Specifies or returns the start point of the first extension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)pVal;

### Нод ExtLine2Point

**Имя нода** = ExtLine2Point;

**Описание** = Specifies or returns the start point of the second extension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_ExtLine2Point

**Имя нода** = Set_ExtLine2Point;

**Описание** = Specifies or returns the start point of the second extension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)pVal;

### Нод CenterPoint

**Имя нода** = CenterPoint;

**Описание** = Specifies or returns the center point of the arc.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_CenterPoint

**Имя нода** = Set_CenterPoint;

**Описание** = Specifies or returns the center point of the arc.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)pVal;

### Нод Leader1Point

**Имя нода** = Leader1Point;

**Описание** = Specifies or returns the start point of the first leader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_Leader1Point

**Имя нода** = Set_Leader1Point;

**Описание** = Specifies or returns the start point of the first leader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)pVal;

### Нод Leader2Point

**Имя нода** = Leader2Point;

**Описание** = Specifies or returns the start point of the second leader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_Leader2Point

**Имя нода** = Set_Leader2Point;

**Описание** = Specifies or returns the start point of the second leader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)pVal;

### Нод IsPartial

**Имя нода** = IsPartial;

**Описание** = Specifies or returns whether the dimension is for a partial arc.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_IsPartial

**Имя нода** = Set_IsPartial;

**Описание** = Specifies or returns whether the dimension is for a partial arc.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)pVal;

### Нод ArcStartParam

**Имя нода** = ArcStartParam;

**Описание** = Specifies or returns the start parameter of the arc.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_ArcStartParam

**Имя нода** = Set_ArcStartParam;

**Описание** = Specifies or returns the start parameter of the arc.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Double)pVal;

### Нод ArcEndParam

**Имя нода** = ArcEndParam;

**Описание** = Specifies or returns the end parameter of the arc.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_ArcEndParam

**Имя нода** = Set_ArcEndParam;

**Описание** = Specifies or returns the end parameter of the arc.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Double)pVal;

### Нод HasLeader

**Имя нода** = HasLeader;

**Описание** = Specifies or returns whether the dimension has a leader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_HasLeader

**Имя нода** = Set_HasLeader;

**Описание** = Specifies or returns whether the dimension has a leader.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)pVal;

### Нод SymbolPosition

**Имя нода** = SymbolPosition;

**Описание** = Specifies or returns the location of the arc length dimension symbol.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_SymbolPosition

**Имя нода** = Set_SymbolPosition;

**Описание** = Specifies or returns the location of the arc length dimension symbol.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)OdaX.AcDimArcLengthSymbol;

### Нод DimensionLinetype

**Имя нода** = DimensionLinetype;

**Описание** = Not implemented. Specifies the linetype of a dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_DimensionLinetype

**Имя нода** = Set_DimensionLinetype;

**Описание** = Not implemented. Specifies the linetype of a dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.String)Linetype;

### Нод ExtLine1Linetype

**Имя нода** = ExtLine1Linetype;

**Описание** = Not implemented. Specifies or returns the linetype of the first extension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_ExtLine1Linetype

**Имя нода** = Set_ExtLine1Linetype;

**Описание** = Not implemented. Specifies or returns the linetype of the first extension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.String)Linetype;

### Нод ExtLine2Linetype

**Имя нода** = ExtLine2Linetype;

**Описание** = Not implemented. Specifies or returns the linetype of the second extension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_ExtLine2Linetype

**Имя нода** = Set_ExtLine2Linetype;

**Описание** = Not implemented. Specifies or returns the linetype of the second extension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.String)Linetype;

### Нод ExtLineFixedLenSuppress

**Имя нода** = ExtLineFixedLenSuppress;

**Описание** = Not implemented. Specifies or returns whether fixed-length extension lines display.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_ExtLineFixedLenSuppress

**Имя нода** = Set_ExtLineFixedLenSuppress;

**Описание** = Not implemented. Specifies or returns whether fixed-length extension lines display.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Object)bFixedLen;

### Нод ExtLineFixedLen

**Имя нода** = ExtLineFixedLen;

**Описание** = Not implemented. Specifies or returns whether extension lines are a fixed length.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_ExtLineFixedLen

**Имя нода** = Set_ExtLineFixedLen;

**Описание** = Not implemented. Specifies or returns whether extension lines are a fixed length.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Double)FixedLen;

### Нод Set_SubUnitsSuffix

**Имя нода** = Set_SubUnitsSuffix;

**Описание** = Specifies the text suffix for all applicable linear dimension when change to sub-units;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.String)rhs;

### Нод SubUnitsFactor

**Имя нода** = SubUnitsFactor;

**Описание** = Specifies the sub-units scale factor for all applicable linear dimension;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_SubUnitsFactor

**Имя нода** = Set_SubUnitsFactor;

**Описание** = Specifies the sub-units scale factor for all applicable linear dimension;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Double)factor;

### Нод AltSubUnitsSuffix

**Имя нода** = AltSubUnitsSuffix;

**Описание** = Specifies the text suffix for the alternate dimension when change to alternate sub-units;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_AltSubUnitsSuffix

**Имя нода** = Set_AltSubUnitsSuffix;

**Описание** = Specifies the text suffix for the alternate dimension when change to alternate sub-units;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.String)suffix;

### Нод AltSubUnitsFactor

**Имя нода** = AltSubUnitsFactor;

**Описание** = Specifies the alternate sub-units scale factor for all applicable linear dimension;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;

### Нод Set_AltSubUnitsFactor

**Имя нода** = Set_AltSubUnitsFactor;

**Описание** = Specifies the alternate sub-units scale factor for all applicable linear dimension;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimArcLength;

**Входные данные**:

* (System.Object)AcadDimArcLength;
* (System.Double)factor;

## Группа NVP_nanoCAD_COM.OdaX.AcadDimAngular

### Нод _AcadDimAngular_Constructor

**Имя нода** = _AcadDimAngular_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadDimAngular_ConstructorCast

**Имя нода** = _AcadDimAngular_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)dynamic;

### Нод ExtLine1StartPoint

**Имя нода** = ExtLine1StartPoint;

**Описание** = Specifies or returns the start point of the first extension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_ExtLine1StartPoint

**Имя нода** = Set_ExtLine1StartPoint;

**Описание** = Specifies or returns the start point of the first extension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.Object)xLine1Point;

### Нод ExtLine1EndPoint

**Имя нода** = ExtLine1EndPoint;

**Описание** = Specifies or returns the end point of the first extension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_ExtLine1EndPoint

**Имя нода** = Set_ExtLine1EndPoint;

**Описание** = Specifies or returns the end point of the first extension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.Object)xLine1Point;

### Нод ExtLine2StartPoint

**Имя нода** = ExtLine2StartPoint;

**Описание** = Specifies or returns the start point of the second extension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_ExtLine2StartPoint

**Имя нода** = Set_ExtLine2StartPoint;

**Описание** = Specifies or returns the start point of the second extension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.Object)xLine2Point;

### Нод ExtLine2EndPoint

**Имя нода** = ExtLine2EndPoint;

**Описание** = Specifies or returns the end point of the second extension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_ExtLine2EndPoint

**Имя нода** = Set_ExtLine2EndPoint;

**Описание** = Specifies or returns the end point of the second extension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.Object)xLine2Point;

### Нод AngleFormat

**Имя нода** = AngleFormat;

**Описание** = Specifies or returns the unit of measurement for angular dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_AngleFormat

**Имя нода** = Set_AngleFormat;

**Описание** = Specifies or returns the unit of measurement for angular dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.Object)OdaX.AcAngleUnits;

### Нод DimensionLineColor

**Имя нода** = DimensionLineColor;

**Описание** = Specifies or returns the color of dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_DimensionLineColor

**Имя нода** = Set_DimensionLineColor;

**Описание** = Specifies or returns the color of dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.Object)Type;

### Нод ExtensionLineColor

**Имя нода** = ExtensionLineColor;

**Описание** = Specifies or returns the color of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_ExtensionLineColor

**Имя нода** = Set_ExtensionLineColor;

**Описание** = Specifies or returns the color of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.Object)Type;

### Нод ExtensionLineExtend

**Имя нода** = ExtensionLineExtend;

**Описание** = Specifies or returns how far extension lines extend beyond dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_ExtensionLineExtend

**Имя нода** = Set_ExtensionLineExtend;

**Описание** = Specifies or returns how far extension lines extend beyond dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.Double)extend;

### Нод Fit

**Имя нода** = Fit;

**Описание** = Specifies or returns how text and arrowheads are positioned when they do not fit in the space between extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_Fit

**Имя нода** = Set_Fit;

**Описание** = Specifies or returns how text and arrowheads are positioned when they do not fit in the space between extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.Object)OdaX.AcDimFit;

### Нод HorizontalTextPosition

**Имя нода** = HorizontalTextPosition;

**Описание** = Specifies or returns the horizontal position of dimension text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_HorizontalTextPosition

**Имя нода** = Set_HorizontalTextPosition;

**Описание** = Specifies or returns the horizontal position of dimension text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.Object)OdaX.AcDimHorizontalJustification;

### Нод ExtensionLineWeight

**Имя нода** = ExtensionLineWeight;

**Описание** = Specifies or returns the lineweight of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_ExtensionLineWeight

**Имя нода** = Set_ExtensionLineWeight;

**Описание** = Specifies or returns the lineweight of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.Object)lweight;

### Нод DimLine1Suppress

**Имя нода** = DimLine1Suppress;

**Описание** = Specifies or returns whether the first dimension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_DimLine1Suppress

**Имя нода** = Set_DimLine1Suppress;

**Описание** = Specifies or returns whether the first dimension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.Object)bSuppress;

### Нод DimLine2Suppress

**Имя нода** = DimLine2Suppress;

**Описание** = Specifies or returns whether the second dimension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_DimLine2Suppress

**Имя нода** = Set_DimLine2Suppress;

**Описание** = Specifies or returns whether the second dimension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.Object)bSuppress;

### Нод ExtLine1Suppress

**Имя нода** = ExtLine1Suppress;

**Описание** = Specifies or returns whether the first extension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_ExtLine1Suppress

**Имя нода** = Set_ExtLine1Suppress;

**Описание** = Specifies or returns whether the first extension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.Object)bSuppress;

### Нод ExtLine2Suppress

**Имя нода** = ExtLine2Suppress;

**Описание** = Specifies or returns whether the second extension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_ExtLine2Suppress

**Имя нода** = Set_ExtLine2Suppress;

**Описание** = Specifies or returns whether the second extension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.Object)bSuppress;

### Нод DimLineInside

**Имя нода** = DimLineInside;

**Описание** = Specifies or returns whether dimension lines are drawn outside of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_DimLineInside

**Имя нода** = Set_DimLineInside;

**Описание** = Specifies or returns whether dimension lines are drawn outside of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.Object)bInside;

### Нод TextInsideAlign

**Имя нода** = TextInsideAlign;

**Описание** = Specifies or returns whether dimension text displays between extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_TextInsideAlign

**Имя нода** = Set_TextInsideAlign;

**Описание** = Specifies or returns whether dimension text displays between extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.Object)bInside;

### Нод TextInside

**Имя нода** = TextInside;

**Описание** = Specifies or returns whether dimension text between extension lines is aligned horizontally or with the dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_TextInside

**Имя нода** = Set_TextInside;

**Описание** = Specifies or returns whether dimension text between extension lines is aligned horizontally or with the dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.Object)bInside;

### Нод ForceLineInside

**Имя нода** = ForceLineInside;

**Описание** = Specifies or returns whether dimension lines display between extension lines when text is positioned outside of the extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_ForceLineInside

**Имя нода** = Set_ForceLineInside;

**Описание** = Specifies or returns whether dimension lines display between extension lines when text is positioned outside of the extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.Object)bInside;

### Нод TextOutsideAlign

**Имя нода** = TextOutsideAlign;

**Описание** = Specifies or returns whether dimension text displays outside of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_TextOutsideAlign

**Имя нода** = Set_TextOutsideAlign;

**Описание** = Specifies or returns whether dimension text displays outside of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.Object)bInside;

### Нод TextPrecision

**Имя нода** = TextPrecision;

**Описание** = Specifies or returns the number of decimal places for angular dimension text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_TextPrecision

**Имя нода** = Set_TextPrecision;

**Описание** = Specifies or returns the number of decimal places for angular dimension text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.Object)OdaX.AcDimPrecision;

### Нод ExtensionLineOffset

**Имя нода** = ExtensionLineOffset;

**Описание** = Specifies or returns the distance between extension lines and the object being dimensioned.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_ExtensionLineOffset

**Имя нода** = Set_ExtensionLineOffset;

**Описание** = Specifies or returns the distance between extension lines and the object being dimensioned.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.Double)Offset;

### Нод DimensionLineWeight

**Имя нода** = DimensionLineWeight;

**Описание** = Specifies or returns the lineweight of dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_DimensionLineWeight

**Имя нода** = Set_DimensionLineWeight;

**Описание** = Specifies or returns the lineweight of dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.Object)weight;

### Нод ArrowheadSize

**Имя нода** = ArrowheadSize;

**Описание** = Specifies or returns the size of dimension arrowheads.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_ArrowheadSize

**Имя нода** = Set_ArrowheadSize;

**Описание** = Specifies or returns the size of dimension arrowheads.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.Double)size;

### Нод Arrowhead1Type

**Имя нода** = Arrowhead1Type;

**Описание** = Specifies or returns the arrowhead of the first dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_Arrowhead1Type

**Имя нода** = Set_Arrowhead1Type;

**Описание** = Specifies or returns the arrowhead of the first dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.Object)OdaX.AcDimArrowheadType;

### Нод Arrowhead2Type

**Имя нода** = Arrowhead2Type;

**Описание** = Specifies or returns the arrowhead of the second dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_Arrowhead2Type

**Имя нода** = Set_Arrowhead2Type;

**Описание** = Specifies or returns the arrowhead of the second dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.Object)OdaX.AcDimArrowheadType;

### Нод Measurement

**Имя нода** = Measurement;

**Описание** = Returns the length measurement of linear dimensions or the angle measurement for angular dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Arrowhead1Block

**Имя нода** = Arrowhead1Block;

**Описание** = Specifies or returns a custom block to replace the arrowhead of the first dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_Arrowhead1Block

**Имя нода** = Set_Arrowhead1Block;

**Описание** = Specifies or returns a custom block to replace the arrowhead of the first dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.String)BlockName;

### Нод Arrowhead2Block

**Имя нода** = Arrowhead2Block;

**Описание** = Specifies or returns a custom block to replace the arrowhead of the second dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_Arrowhead2Block

**Имя нода** = Set_Arrowhead2Block;

**Описание** = Specifies or returns a custom block to replace the arrowhead of the second dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.String)BlockName;

### Нод DimensionLinetype

**Имя нода** = DimensionLinetype;

**Описание** = Specifies the linetype of a dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_DimensionLinetype

**Имя нода** = Set_DimensionLinetype;

**Описание** = Specifies the linetype of a dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.String)Linetype;

### Нод ExtLine1Linetype

**Имя нода** = ExtLine1Linetype;

**Описание** = Specifies or returns the linetype of the first extension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_ExtLine1Linetype

**Имя нода** = Set_ExtLine1Linetype;

**Описание** = Specifies or returns the linetype of the first extension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.String)Linetype;

### Нод ExtLine2Linetype

**Имя нода** = ExtLine2Linetype;

**Описание** = Specifies or returns the linetype of the second extension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_ExtLine2Linetype

**Имя нода** = Set_ExtLine2Linetype;

**Описание** = Specifies or returns the linetype of the second extension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.String)Linetype;

### Нод ExtLineFixedLenSuppress

**Имя нода** = ExtLineFixedLenSuppress;

**Описание** = Specifies or returns whether fixed-length extension lines display.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_ExtLineFixedLenSuppress

**Имя нода** = Set_ExtLineFixedLenSuppress;

**Описание** = Specifies or returns whether fixed-length extension lines display.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.Object)bFixedLen;

### Нод ExtLineFixedLen

**Имя нода** = ExtLineFixedLen;

**Описание** = Specifies or returns whether extension lines are a fixed length.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_ExtLineFixedLen

**Имя нода** = Set_ExtLineFixedLen;

**Описание** = Specifies or returns whether extension lines are a fixed length.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.Double)FixedLen;

### Нод DimConstrForm

**Имя нода** = DimConstrForm;

**Описание** = Specifies the constraint type - Dynamic or Annotational;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_DimConstrForm

**Имя нода** = Set_DimConstrForm;

**Описание** = Specifies the constraint type - Dynamic or Annotational;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.Object)bIsDynamic;

### Нод DimConstrReference

**Имя нода** = DimConstrReference;

**Описание** = Specifies whether the parameter is a reference constraint. Reference dimensions can be used in expressions but don't drive geometry - similar to an associative dimension;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_DimConstrReference

**Имя нода** = Set_DimConstrReference;

**Описание** = Specifies whether the parameter is a reference constraint. Reference dimensions can be used in expressions but don't drive geometry - similar to an associative dimension;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.Object)bIsReference;

### Нод DimConstrName

**Имя нода** = DimConstrName;

**Описание** = Specifies the name of the dimensional constraint. Names cannot have spaces;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_DimConstrName

**Имя нода** = Set_DimConstrName;

**Описание** = Specifies the name of the dimensional constraint. Names cannot have spaces;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.String)bstrName;

### Нод DimConstrExpression

**Имя нода** = DimConstrExpression;

**Описание** = Specifies the expression or the value of the constraint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_DimConstrExpression

**Имя нода** = Set_DimConstrExpression;

**Описание** = Specifies the expression or the value of the constraint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.String)bstrExpression;

### Нод DimConstrValue

**Имя нода** = DimConstrValue;

**Описание** = Specifies the value of the constraint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_DimConstrValue

**Имя нода** = Set_DimConstrValue;

**Описание** = Specifies the value of the constraint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.String)Value;

### Нод DimConstrDesc

**Имя нода** = DimConstrDesc;

**Описание** = Specifies description for constraint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;

### Нод Set_DimConstrDesc

**Имя нода** = Set_DimConstrDesc;

**Описание** = Specifies description for constraint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAngular;

**Входные данные**:

* (System.Object)AcadDimAngular;
* (System.String)bstrDescription;

## Группа NVP_nanoCAD_COM.OdaX.AcadDimAligned

### Нод _AcadDimAligned_Constructor

**Имя нода** = _AcadDimAligned_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadDimAligned_ConstructorCast

**Имя нода** = _AcadDimAligned_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)dynamic;

### Нод ExtLine1Point

**Имя нода** = ExtLine1Point;

**Описание** = Specifies or returns the start point of the first extension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_ExtLine1Point

**Имя нода** = Set_ExtLine1Point;

**Описание** = Specifies or returns the start point of the first extension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)xLine1Point;

### Нод ExtLine2Point

**Имя нода** = ExtLine2Point;

**Описание** = Specifies or returns the start point of the second extension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_ExtLine2Point

**Имя нода** = Set_ExtLine2Point;

**Описание** = Specifies or returns the start point of the second extension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)xLine2Point;

### Нод AltUnits

**Имя нода** = AltUnits;

**Описание** = Specifies or returns whether alternate units are used for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_AltUnits

**Имя нода** = Set_AltUnits;

**Описание** = Specifies or returns whether alternate units are used for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)bAlternate;

### Нод AltUnitsPrecision

**Имя нода** = AltUnitsPrecision;

**Описание** = Specifies or returns the number of decimal places for alternate unit dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_AltUnitsPrecision

**Имя нода** = Set_AltUnitsPrecision;

**Описание** = Specifies or returns the number of decimal places for alternate unit dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)OdaX.AcDimPrecision;

### Нод AltUnitsScale

**Имя нода** = AltUnitsScale;

**Описание** = Specifies or returns the scale factor for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_AltUnitsScale

**Имя нода** = Set_AltUnitsScale;

**Описание** = Specifies or returns the scale factor for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)scale;

### Нод AltRoundDistance

**Имя нода** = AltRoundDistance;

**Описание** = Specifies or returns the rounding value for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_AltRoundDistance

**Имя нода** = Set_AltRoundDistance;

**Описание** = Specifies or returns the rounding value for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Double)Distance;

### Нод AltTolerancePrecision

**Имя нода** = AltTolerancePrecision;

**Описание** = Specifies or returns the number of decimal places for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_AltTolerancePrecision

**Имя нода** = Set_AltTolerancePrecision;

**Описание** = Specifies or returns the number of decimal places for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)OdaX.AcDimPrecision;

### Нод AltUnitsFormat

**Имя нода** = AltUnitsFormat;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_AltUnitsFormat

**Имя нода** = Set_AltUnitsFormat;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)OdaX.AcDimUnits;

### Нод AltTextPrefix

**Имя нода** = AltTextPrefix;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_AltTextPrefix

**Имя нода** = Set_AltTextPrefix;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.String)prefix;

### Нод AltTextSuffix

**Имя нода** = AltTextSuffix;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_AltTextSuffix

**Имя нода** = Set_AltTextSuffix;

**Описание** = not including angular dimensions).;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.String)suffix;

### Нод DimensionLineColor

**Имя нода** = DimensionLineColor;

**Описание** = Specifies or returns the color of dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_DimensionLineColor

**Имя нода** = Set_DimensionLineColor;

**Описание** = Specifies or returns the color of dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)color;

### Нод ExtensionLineColor

**Имя нода** = ExtensionLineColor;

**Описание** = Specifies or returns the color of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_ExtensionLineColor

**Имя нода** = Set_ExtensionLineColor;

**Описание** = Specifies or returns the color of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)color;

### Нод PrimaryUnitsPrecision

**Имя нода** = PrimaryUnitsPrecision;

**Описание** = Specifies or returns the number of decimal places for the primary units of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_PrimaryUnitsPrecision

**Имя нода** = Set_PrimaryUnitsPrecision;

**Описание** = Specifies or returns the number of decimal places for the primary units of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)OdaX.AcDimPrecision;

### Нод DimensionLineExtend

**Имя нода** = DimensionLineExtend;

**Описание** = Specifies or returns how far dimension lines extend beyond extension lines when using dimension lines with oblique strokes.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_DimensionLineExtend

**Имя нода** = Set_DimensionLineExtend;

**Описание** = Specifies or returns how far dimension lines extend beyond extension lines when using dimension lines with oblique strokes.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Double)extend;

### Нод ExtensionLineExtend

**Имя нода** = ExtensionLineExtend;

**Описание** = Specifies or returns how far extension lines extend beyond dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_ExtensionLineExtend

**Имя нода** = Set_ExtensionLineExtend;

**Описание** = Specifies or returns how far extension lines extend beyond dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Double)extend;

### Нод Fit

**Имя нода** = Fit;

**Описание** = Specifies or returns how text and arrowheads are positioned when they do not fit in the space between extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_Fit

**Имя нода** = Set_Fit;

**Описание** = Specifies or returns how text and arrowheads are positioned when they do not fit in the space between extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)OdaX.AcDimFit;

### Нод FractionFormat

**Имя нода** = FractionFormat;

**Описание** = Specifies or returns how fractions display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_FractionFormat

**Имя нода** = Set_FractionFormat;

**Описание** = Specifies or returns how fractions display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)OdaX.AcDimFractionType;

### Нод HorizontalTextPosition

**Имя нода** = HorizontalTextPosition;

**Описание** = Specifies or returns the horizontal position of dimension text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_HorizontalTextPosition

**Имя нода** = Set_HorizontalTextPosition;

**Описание** = Specifies or returns the horizontal position of dimension text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)OdaX.AcDimHorizontalJustification;

### Нод LinearScaleFactor

**Имя нода** = LinearScaleFactor;

**Описание** = Specifies or returns the scale factor for linear dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_LinearScaleFactor

**Имя нода** = Set_LinearScaleFactor;

**Описание** = Specifies or returns the scale factor for linear dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)Type;

### Нод UnitsFormat

**Имя нода** = UnitsFormat;

**Описание** = Specifies or returns the unit of measurement for linear dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_UnitsFormat

**Имя нода** = Set_UnitsFormat;

**Описание** = Specifies or returns the unit of measurement for linear dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)OdaX.AcDimLUnits;

### Нод ExtensionLineWeight

**Имя нода** = ExtensionLineWeight;

**Описание** = Specifies or returns the lineweight of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_ExtensionLineWeight

**Имя нода** = Set_ExtensionLineWeight;

**Описание** = Specifies or returns the lineweight of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)lweight;

### Нод RoundDistance

**Имя нода** = RoundDistance;

**Описание** = Specifies or returns how measurements are rounded for all dimensions except angular dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_RoundDistance

**Имя нода** = Set_RoundDistance;

**Описание** = Specifies or returns how measurements are rounded for all dimensions except angular dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Double)Distance;

### Нод DimLine1Suppress

**Имя нода** = DimLine1Suppress;

**Описание** = Specifies or returns whether the first dimension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_DimLine1Suppress

**Имя нода** = Set_DimLine1Suppress;

**Описание** = Specifies or returns whether the first dimension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)bSuppress;

### Нод DimLine2Suppress

**Имя нода** = DimLine2Suppress;

**Описание** = Specifies or returns whether the second dimension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_DimLine2Suppress

**Имя нода** = Set_DimLine2Suppress;

**Описание** = Specifies or returns whether the second dimension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)bSuppress;

### Нод ExtLine1Suppress

**Имя нода** = ExtLine1Suppress;

**Описание** = Specifies or returns whether the first extension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_ExtLine1Suppress

**Имя нода** = Set_ExtLine1Suppress;

**Описание** = Specifies or returns whether the first extension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)bSuppress;

### Нод ExtLine2Suppress

**Имя нода** = ExtLine2Suppress;

**Описание** = Specifies or returns whether the second extension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_ExtLine2Suppress

**Имя нода** = Set_ExtLine2Suppress;

**Описание** = Specifies or returns whether the second extension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)bSuppress;

### Нод DimLineInside

**Имя нода** = DimLineInside;

**Описание** = Specifies or returns whether dimension lines are drawn outside of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_DimLineInside

**Имя нода** = Set_DimLineInside;

**Описание** = Specifies or returns whether dimension lines are drawn outside of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)bInside;

### Нод TextInsideAlign

**Имя нода** = TextInsideAlign;

**Описание** = Specifies or returns whether dimension text displays between extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_TextInsideAlign

**Имя нода** = Set_TextInsideAlign;

**Описание** = Specifies or returns whether dimension text displays between extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)bInside;

### Нод TextInside

**Имя нода** = TextInside;

**Описание** = Specifies or returns whether dimension text between extension lines is aligned horizontally or with the dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_TextInside

**Имя нода** = Set_TextInside;

**Описание** = Specifies or returns whether dimension text between extension lines is aligned horizontally or with the dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)bInside;

### Нод ForceLineInside

**Имя нода** = ForceLineInside;

**Описание** = Specifies or returns whether dimension lines display between extension lines when text is positioned outside of the extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_ForceLineInside

**Имя нода** = Set_ForceLineInside;

**Описание** = Specifies or returns whether dimension lines display between extension lines when text is positioned outside of the extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)bInside;

### Нод TextOutsideAlign

**Имя нода** = TextOutsideAlign;

**Описание** = Specifies or returns whether dimension text displays outside of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_TextOutsideAlign

**Имя нода** = Set_TextOutsideAlign;

**Описание** = Specifies or returns whether dimension text displays outside of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)bInside;

### Нод ExtensionLineOffset

**Имя нода** = ExtensionLineOffset;

**Описание** = Specifies or returns the distance between extension lines and the object being dimensioned.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_ExtensionLineOffset

**Имя нода** = Set_ExtensionLineOffset;

**Описание** = Specifies or returns the distance between extension lines and the object being dimensioned.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Double)Offset;

### Нод AltSuppressLeadingZeros

**Имя нода** = AltSuppressLeadingZeros;

**Описание** = Specifies or returns whether leading zeros display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_AltSuppressLeadingZeros

**Имя нода** = Set_AltSuppressLeadingZeros;

**Описание** = Specifies or returns whether leading zeros display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)bVal;

### Нод AltSuppressTrailingZeros

**Имя нода** = AltSuppressTrailingZeros;

**Описание** = Specifies or returns whether leading zeros display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_AltSuppressTrailingZeros

**Имя нода** = Set_AltSuppressTrailingZeros;

**Описание** = Specifies or returns whether leading zeros display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)bVal;

### Нод AltSuppressZeroFeet

**Имя нода** = AltSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_AltSuppressZeroFeet

**Имя нода** = Set_AltSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)bVal;

### Нод AltSuppressZeroInches

**Имя нода** = AltSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_AltSuppressZeroInches

**Имя нода** = Set_AltSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)bVal;

### Нод AltToleranceSuppressLeadingZeros

**Имя нода** = AltToleranceSuppressLeadingZeros;

**Описание** = Specifies or returns whether leading zeros display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_AltToleranceSuppressLeadingZeros

**Имя нода** = Set_AltToleranceSuppressLeadingZeros;

**Описание** = Specifies or returns whether leading zeros display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)bVal;

### Нод AltToleranceSuppressTrailingZeros

**Имя нода** = AltToleranceSuppressTrailingZeros;

**Описание** = Specifies or returns whether trailing zeros display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_AltToleranceSuppressTrailingZeros

**Имя нода** = Set_AltToleranceSuppressTrailingZeros;

**Описание** = Specifies or returns whether trailing zeros display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)bVal;

### Нод AltToleranceSuppressZeroFeet

**Имя нода** = AltToleranceSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_AltToleranceSuppressZeroFeet

**Имя нода** = Set_AltToleranceSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)bVal;

### Нод AltToleranceSuppressZeroInches

**Имя нода** = AltToleranceSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_AltToleranceSuppressZeroInches

**Имя нода** = Set_AltToleranceSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for tolerance values of alternate unit dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)bVal;

### Нод SuppressZeroFeet

**Имя нода** = SuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_SuppressZeroFeet

**Имя нода** = Set_SuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)bVal;

### Нод SuppressZeroInches

**Имя нода** = SuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_SuppressZeroInches

**Имя нода** = Set_SuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)bVal;

### Нод ToleranceSuppressZeroFeet

**Имя нода** = ToleranceSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for tolerance values of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_ToleranceSuppressZeroFeet

**Имя нода** = Set_ToleranceSuppressZeroFeet;

**Описание** = Specifies or returns whether zero feet measurements display for tolerance values of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)bVal;

### Нод ToleranceSuppressZeroInches

**Имя нода** = ToleranceSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for tolerance values of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_ToleranceSuppressZeroInches

**Имя нода** = Set_ToleranceSuppressZeroInches;

**Описание** = Specifies or returns whether zero inch measurements display for tolerance values of dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)bVal;

### Нод DimensionLineWeight

**Имя нода** = DimensionLineWeight;

**Описание** = Specifies or returns the lineweight of dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_DimensionLineWeight

**Имя нода** = Set_DimensionLineWeight;

**Описание** = Specifies or returns the lineweight of dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)weight;

### Нод ArrowheadSize

**Имя нода** = ArrowheadSize;

**Описание** = Specifies or returns the size of dimension arrowheads.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_ArrowheadSize

**Имя нода** = Set_ArrowheadSize;

**Описание** = Specifies or returns the size of dimension arrowheads.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Double)size;

### Нод Arrowhead1Type

**Имя нода** = Arrowhead1Type;

**Описание** = Specifies or returns the arrowhead of the first dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_Arrowhead1Type

**Имя нода** = Set_Arrowhead1Type;

**Описание** = Specifies or returns the arrowhead of the first dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)OdaX.AcDimArrowheadType;

### Нод Arrowhead2Type

**Имя нода** = Arrowhead2Type;

**Описание** = Specifies or returns the arrowhead of the second dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_Arrowhead2Type

**Имя нода** = Set_Arrowhead2Type;

**Описание** = Specifies or returns the arrowhead of the second dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)OdaX.AcDimArrowheadType;

### Нод Measurement

**Имя нода** = Measurement;

**Описание** = Returns the length measurement of linear dimensions or the angle measurement for angular dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Arrowhead1Block

**Имя нода** = Arrowhead1Block;

**Описание** = Specifies or returns a custom block to replace the arrowhead of the first dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_Arrowhead1Block

**Имя нода** = Set_Arrowhead1Block;

**Описание** = Specifies or returns a custom block to replace the arrowhead of the first dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.String)BlockName;

### Нод Arrowhead2Block

**Имя нода** = Arrowhead2Block;

**Описание** = Specifies or returns a custom block to replace the arrowhead of the second dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_Arrowhead2Block

**Имя нода** = Set_Arrowhead2Block;

**Описание** = Specifies or returns a custom block to replace the arrowhead of the second dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.String)BlockName;

### Нод DimensionLinetype

**Имя нода** = DimensionLinetype;

**Описание** = Specifies the linetype of a dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_DimensionLinetype

**Имя нода** = Set_DimensionLinetype;

**Описание** = Specifies the linetype of a dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.String)Linetype;

### Нод ExtLine1Linetype

**Имя нода** = ExtLine1Linetype;

**Описание** = Specifies or returns the linetype of the first extension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_ExtLine1Linetype

**Имя нода** = Set_ExtLine1Linetype;

**Описание** = Specifies or returns the linetype of the first extension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.String)Linetype;

### Нод ExtLine2Linetype

**Имя нода** = ExtLine2Linetype;

**Описание** = Specifies or returns the linetype of the second extension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_ExtLine2Linetype

**Имя нода** = Set_ExtLine2Linetype;

**Описание** = Specifies or returns the linetype of the second extension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.String)Linetype;

### Нод ExtLineFixedLenSuppress

**Имя нода** = ExtLineFixedLenSuppress;

**Описание** = Specifies or returns whether fixed-length extension lines display.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_ExtLineFixedLenSuppress

**Имя нода** = Set_ExtLineFixedLenSuppress;

**Описание** = Specifies or returns whether fixed-length extension lines display.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)bFixedLen;

### Нод ExtLineFixedLen

**Имя нода** = ExtLineFixedLen;

**Описание** = Specifies or returns whether extension lines are a fixed length.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_ExtLineFixedLen

**Имя нода** = Set_ExtLineFixedLen;

**Описание** = Specifies or returns whether extension lines are a fixed length.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Double)FixedLen;

### Нод DimConstrForm

**Имя нода** = DimConstrForm;

**Описание** = Specifies the constraint type - Dynamic or Annotational;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_DimConstrForm

**Имя нода** = Set_DimConstrForm;

**Описание** = Specifies the constraint type - Dynamic or Annotational;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)bIsDynamic;

### Нод DimConstrReference

**Имя нода** = DimConstrReference;

**Описание** = Specifies whether the parameter is a reference constraint. Reference dimensions can be used in expressions but don't drive geometry - similar to an associative dimension;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_DimConstrReference

**Имя нода** = Set_DimConstrReference;

**Описание** = Specifies whether the parameter is a reference constraint. Reference dimensions can be used in expressions but don't drive geometry - similar to an associative dimension;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Object)bIsReference;

### Нод DimConstrName

**Имя нода** = DimConstrName;

**Описание** = Specifies the name of the dimensional constraint. Names cannot have spaces;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_DimConstrName

**Имя нода** = Set_DimConstrName;

**Описание** = Specifies the name of the dimensional constraint. Names cannot have spaces;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.String)bstrName;

### Нод DimConstrExpression

**Имя нода** = DimConstrExpression;

**Описание** = Specifies the expression or the value of the constraint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_DimConstrExpression

**Имя нода** = Set_DimConstrExpression;

**Описание** = Specifies the expression or the value of the constraint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.String)bstrExpression;

### Нод DimConstrValue

**Имя нода** = DimConstrValue;

**Описание** = Specifies the value of the constraint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_DimConstrValue

**Имя нода** = Set_DimConstrValue;

**Описание** = Specifies the value of the constraint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.String)Value;

### Нод DimConstrDesc

**Имя нода** = DimConstrDesc;

**Описание** = Specifies description for constraint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_DimConstrDesc

**Имя нода** = Set_DimConstrDesc;

**Описание** = Specifies description for constraint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.String)bstrDescription;

### Нод SubUnitsSuffix

**Имя нода** = SubUnitsSuffix;

**Описание** = Specifies the text suffix for all applicable linear dimension when change to sub-units;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_SubUnitsSuffix

**Имя нода** = Set_SubUnitsSuffix;

**Описание** = Specifies the text suffix for all applicable linear dimension when change to sub-units;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.String)suffix;

### Нод SubUnitsFactor

**Имя нода** = SubUnitsFactor;

**Описание** = Specifies the sub-units scale factor for all applicable linear dimension;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_SubUnitsFactor

**Имя нода** = Set_SubUnitsFactor;

**Описание** = Specifies the sub-units scale factor for all applicable linear dimension;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Double)factor;

### Нод AltSubUnitsSuffix

**Имя нода** = AltSubUnitsSuffix;

**Описание** = Specifies the text suffix for the alternate dimension when change to alternate sub-units;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_AltSubUnitsSuffix

**Имя нода** = Set_AltSubUnitsSuffix;

**Описание** = Specifies the text suffix for the alternate dimension when change to alternate sub-units;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.String)suffix;

### Нод AltSubUnitsFactor

**Имя нода** = AltSubUnitsFactor;

**Описание** = Specifies the alternate sub-units scale factor for all applicable linear dimension;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;

### Нод Set_AltSubUnitsFactor

**Имя нода** = Set_AltSubUnitsFactor;

**Описание** = Specifies the alternate sub-units scale factor for all applicable linear dimension;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDimAligned;

**Входные данные**:

* (System.Object)AcadDimAligned;
* (System.Double)factor;

## Группа NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular

### Нод _AcadDim3PointAngular_Constructor

**Имя нода** = _AcadDim3PointAngular_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadDim3PointAngular_ConstructorCast

**Имя нода** = _AcadDim3PointAngular_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)dynamic;

### Нод ExtLine1EndPoint

**Имя нода** = ExtLine1EndPoint;

**Описание** = Specifies or returns the point through which the first extension line passes.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_ExtLine1EndPoint

**Имя нода** = Set_ExtLine1EndPoint;

**Описание** = Specifies or returns the point through which the first extension line passes.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.Object)xLine1Point;

### Нод ExtLine2EndPoint

**Имя нода** = ExtLine2EndPoint;

**Описание** = Specifies or returns the point through which the second extension line passes.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_ExtLine2EndPoint

**Имя нода** = Set_ExtLine2EndPoint;

**Описание** = Specifies or returns the point through which the second extension line passes.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.Object)xLine2Point;

### Нод AngleVertex

**Имя нода** = AngleVertex;

**Описание** = Specifies or returns the vertex of the angle measured by the dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_AngleVertex

**Имя нода** = Set_AngleVertex;

**Описание** = Specifies or returns the vertex of the angle measured by the dimension.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.Object)AngleVertex;

### Нод TextPrecision

**Имя нода** = TextPrecision;

**Описание** = Specifies or returns the number of decimal places for angular dimension text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_TextPrecision

**Имя нода** = Set_TextPrecision;

**Описание** = Specifies or returns the number of decimal places for angular dimension text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.Object)OdaX.AcDimPrecision;

### Нод AngleFormat

**Имя нода** = AngleFormat;

**Описание** = Specifies or returns the unit of measurement for angular dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_AngleFormat

**Имя нода** = Set_AngleFormat;

**Описание** = Specifies or returns the unit of measurement for angular dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.Object)OdaX.AcAngleUnits;

### Нод DimensionLineColor

**Имя нода** = DimensionLineColor;

**Описание** = Specifies or returns the color of dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_DimensionLineColor

**Имя нода** = Set_DimensionLineColor;

**Описание** = Specifies or returns the color of dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.Object)Type;

### Нод ExtensionLineColor

**Имя нода** = ExtensionLineColor;

**Описание** = Specifies or returns the color of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_ExtensionLineColor

**Имя нода** = Set_ExtensionLineColor;

**Описание** = Specifies or returns the color of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.Object)Type;

### Нод ExtensionLineExtend

**Имя нода** = ExtensionLineExtend;

**Описание** = Specifies or returns how far extension lines extend beyond dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_ExtensionLineExtend

**Имя нода** = Set_ExtensionLineExtend;

**Описание** = Specifies or returns how far extension lines extend beyond dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.Double)extend;

### Нод Fit

**Имя нода** = Fit;

**Описание** = Specifies or returns how text and arrowheads are positioned when they do not fit in the space between extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_Fit

**Имя нода** = Set_Fit;

**Описание** = Specifies or returns how text and arrowheads are positioned when they do not fit in the space between extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.Object)OdaX.AcDimFit;

### Нод HorizontalTextPosition

**Имя нода** = HorizontalTextPosition;

**Описание** = Specifies or returns the horizontal position of dimension text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_HorizontalTextPosition

**Имя нода** = Set_HorizontalTextPosition;

**Описание** = Specifies or returns the horizontal position of dimension text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.Object)OdaX.AcDimHorizontalJustification;

### Нод ExtensionLineWeight

**Имя нода** = ExtensionLineWeight;

**Описание** = Specifies or returns the lineweight of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_ExtensionLineWeight

**Имя нода** = Set_ExtensionLineWeight;

**Описание** = Specifies or returns the lineweight of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.Object)lweight;

### Нод DimLine1Suppress

**Имя нода** = DimLine1Suppress;

**Описание** = Specifies or returns whether the first dimension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_DimLine1Suppress

**Имя нода** = Set_DimLine1Suppress;

**Описание** = Specifies or returns whether the first dimension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.Object)bSuppress;

### Нод DimLine2Suppress

**Имя нода** = DimLine2Suppress;

**Описание** = Specifies or returns whether the second dimension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_DimLine2Suppress

**Имя нода** = Set_DimLine2Suppress;

**Описание** = Specifies or returns whether the second dimension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.Object)bSuppress;

### Нод ExtLine1Suppress

**Имя нода** = ExtLine1Suppress;

**Описание** = Specifies or returns whether the first extension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_ExtLine1Suppress

**Имя нода** = Set_ExtLine1Suppress;

**Описание** = Specifies or returns whether the first extension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.Object)bSuppress;

### Нод ExtLine2Suppress

**Имя нода** = ExtLine2Suppress;

**Описание** = Specifies or returns whether the second extension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_ExtLine2Suppress

**Имя нода** = Set_ExtLine2Suppress;

**Описание** = Specifies or returns whether the second extension line displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.Object)bSuppress;

### Нод DimLineInside

**Имя нода** = DimLineInside;

**Описание** = Specifies or returns whether dimension lines are drawn outside of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_DimLineInside

**Имя нода** = Set_DimLineInside;

**Описание** = Specifies or returns whether dimension lines are drawn outside of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.Object)bInside;

### Нод TextInsideAlign

**Имя нода** = TextInsideAlign;

**Описание** = Specifies or returns whether dimension text displays between extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_TextInsideAlign

**Имя нода** = Set_TextInsideAlign;

**Описание** = Specifies or returns whether dimension text displays between extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.Object)bInside;

### Нод TextInside

**Имя нода** = TextInside;

**Описание** = Specifies or returns whether dimension text between extension lines is aligned horizontally or with the dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_TextInside

**Имя нода** = Set_TextInside;

**Описание** = Specifies or returns whether dimension text between extension lines is aligned horizontally or with the dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.Object)bInside;

### Нод ForceLineInside

**Имя нода** = ForceLineInside;

**Описание** = Specifies or returns whether dimension lines display between extension lines when text is positioned outside of the extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_ForceLineInside

**Имя нода** = Set_ForceLineInside;

**Описание** = Specifies or returns whether dimension lines display between extension lines when text is positioned outside of the extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.Object)bInside;

### Нод TextOutsideAlign

**Имя нода** = TextOutsideAlign;

**Описание** = Specifies or returns whether dimension text displays outside of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_TextOutsideAlign

**Имя нода** = Set_TextOutsideAlign;

**Описание** = Specifies or returns whether dimension text displays outside of extension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.Object)bInside;

### Нод ExtensionLineOffset

**Имя нода** = ExtensionLineOffset;

**Описание** = Specifies or returns the distance between extension lines and the object being dimensioned.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_ExtensionLineOffset

**Имя нода** = Set_ExtensionLineOffset;

**Описание** = Specifies or returns the distance between extension lines and the object being dimensioned.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.Double)Offset;

### Нод DimensionLineWeight

**Имя нода** = DimensionLineWeight;

**Описание** = Specifies or returns the lineweight of dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_DimensionLineWeight

**Имя нода** = Set_DimensionLineWeight;

**Описание** = Specifies or returns the lineweight of dimension lines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.Object)weight;

### Нод ArrowheadSize

**Имя нода** = ArrowheadSize;

**Описание** = Specifies or returns the size of dimension arrowheads.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_ArrowheadSize

**Имя нода** = Set_ArrowheadSize;

**Описание** = Specifies or returns the size of dimension arrowheads.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.Double)size;

### Нод Arrowhead1Type

**Имя нода** = Arrowhead1Type;

**Описание** = Specifies or returns the arrowhead of the first dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_Arrowhead1Type

**Имя нода** = Set_Arrowhead1Type;

**Описание** = Specifies or returns the arrowhead of the first dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.Object)OdaX.AcDimArrowheadType;

### Нод Arrowhead2Type

**Имя нода** = Arrowhead2Type;

**Описание** = Specifies or returns the arrowhead of the second dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_Arrowhead2Type

**Имя нода** = Set_Arrowhead2Type;

**Описание** = Specifies or returns the arrowhead of the second dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.Object)OdaX.AcDimArrowheadType;

### Нод Measurement

**Имя нода** = Measurement;

**Описание** = Returns the length measurement of linear dimensions or the angle measurement for angular dimensions.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Arrowhead1Block

**Имя нода** = Arrowhead1Block;

**Описание** = Specifies or returns a custom block to replace the arrowhead of the first dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_Arrowhead1Block

**Имя нода** = Set_Arrowhead1Block;

**Описание** = Specifies or returns a custom block to replace the arrowhead of the first dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.String)BlockName;

### Нод Arrowhead2Block

**Имя нода** = Arrowhead2Block;

**Описание** = Specifies or returns a custom block to replace the arrowhead of the second dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_Arrowhead2Block

**Имя нода** = Set_Arrowhead2Block;

**Описание** = Specifies or returns a custom block to replace the arrowhead of the second dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.String)BlockName;

### Нод Set_DimensionLinetype

**Имя нода** = Set_DimensionLinetype;

**Описание** = Specifies the linetype of a dimension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.String)rhs;

### Нод ExtLine1Linetype

**Имя нода** = ExtLine1Linetype;

**Описание** = Specifies or returns the linetype of the first extension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_ExtLine1Linetype

**Имя нода** = Set_ExtLine1Linetype;

**Описание** = Specifies or returns the linetype of the first extension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.String)Linetype;

### Нод ExtLine2Linetype

**Имя нода** = ExtLine2Linetype;

**Описание** = Specifies or returns the linetype of the second extension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_ExtLine2Linetype

**Имя нода** = Set_ExtLine2Linetype;

**Описание** = Specifies or returns the linetype of the second extension line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.String)Linetype;

### Нод ExtLineFixedLenSuppress

**Имя нода** = ExtLineFixedLenSuppress;

**Описание** = Specifies or returns whether fixed-length extension lines display.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_ExtLineFixedLenSuppress

**Имя нода** = Set_ExtLineFixedLenSuppress;

**Описание** = Specifies or returns whether fixed-length extension lines display.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.Object)bFixedLen;

### Нод ExtLineFixedLen

**Имя нода** = ExtLineFixedLen;

**Описание** = Specifies or returns whether extension lines are a fixed length.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_ExtLineFixedLen

**Имя нода** = Set_ExtLineFixedLen;

**Описание** = Specifies or returns whether extension lines are a fixed length.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.Double)FixedLen;

### Нод DimConstrForm

**Имя нода** = DimConstrForm;

**Описание** = Specifies the constraint type - Dynamic or Annotational;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_DimConstrForm

**Имя нода** = Set_DimConstrForm;

**Описание** = Specifies the constraint type - Dynamic or Annotational;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.Object)bIsDynamic;

### Нод DimConstrReference

**Имя нода** = DimConstrReference;

**Описание** = Specifies whether the parameter is a reference constraint. Reference dimensions can be used in expressions but don't drive geometry - similar to an associative dimension;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_DimConstrReference

**Имя нода** = Set_DimConstrReference;

**Описание** = Specifies whether the parameter is a reference constraint. Reference dimensions can be used in expressions but don't drive geometry - similar to an associative dimension;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.Object)bIsReference;

### Нод DimConstrName

**Имя нода** = DimConstrName;

**Описание** = Specifies the name of the dimensional constraint. Names cannot have spaces;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_DimConstrName

**Имя нода** = Set_DimConstrName;

**Описание** = Specifies the name of the dimensional constraint. Names cannot have spaces;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.String)bstrName;

### Нод DimConstrExpression

**Имя нода** = DimConstrExpression;

**Описание** = Specifies the expression or the value of the constraint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_DimConstrExpression

**Имя нода** = Set_DimConstrExpression;

**Описание** = Specifies the expression or the value of the constraint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.String)bstrExpression;

### Нод DimConstrValue

**Имя нода** = DimConstrValue;

**Описание** = Specifies the value of the constraint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_DimConstrValue

**Имя нода** = Set_DimConstrValue;

**Описание** = Specifies the value of the constraint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.String)Value;

### Нод DimConstrDesc

**Имя нода** = DimConstrDesc;

**Описание** = Specifies description for constraint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;

### Нод Set_DimConstrDesc

**Имя нода** = Set_DimConstrDesc;

**Описание** = Specifies description for constraint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDim3PointAngular;

**Входные данные**:

* (System.Object)AcadDim3PointAngular;
* (System.String)bstrDescription;

## Группа NVP_nanoCAD_COM.OdaX.AcadDictionary

### Нод _AcadDictionary_Constructor

**Имя нода** = _AcadDictionary_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDictionary;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadDictionary_ConstructorCast

**Имя нода** = _AcadDictionary_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDictionary;

**Входные данные**:

* (System.Object)dynamic;

### Нод Name

**Имя нода** = Name;

**Описание** = Specifies or returns the name of a dictionary.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDictionary;

**Входные данные**:

* (System.Object)AcadDictionary;

### Нод Set_Name

**Имя нода** = Set_Name;

**Описание** = Specifies or returns the name of a dictionary.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDictionary;

**Входные данные**:

* (System.Object)AcadDictionary;
* (System.String)pVal;

### Нод AddObject

**Имя нода** = AddObject;

**Описание** = Adds an object to a dictionary.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDictionary;

**Входные данные**:

* (System.Object)AcadDictionary;
* (System.String)Keyword;
* (System.String)ObjectName;

### Нод GetName

**Имя нода** = GetName;

**Описание** = Returns the keyword string of an object in a dictionary.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDictionary;

**Входные данные**:

* (System.Object)AcadDictionary;
* (System.Object)Object;

### Нод GetObject

**Имя нода** = GetObject;

**Описание** = Returns an object in a dictionary using the keyword string associated with the object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDictionary;

**Входные данные**:

* (System.Object)AcadDictionary;
* (System.String)Name;

### Нод Remove

**Имя нода** = Remove;

**Описание** = Deletes an object from a dictionary.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDictionary;

**Входные данные**:

* (System.Object)AcadDictionary;
* (System.String)Name;

### Нод Rename

**Имя нода** = Rename;

**Описание** = Renames an object in a dictionary.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDictionary;

**Входные данные**:

* (System.Object)AcadDictionary;
* (System.String)OldName;
* (System.String)NewName;

### Нод Replace

**Имя нода** = Replace;

**Описание** = Replaces an object in a dictionary with another object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDictionary;

**Входные данные**:

* (System.Object)AcadDictionary;
* (System.String)OldName;
* (System.Object)pObj;

### Нод Item

**Имя нода** = Item;

**Описание** = Returns an object in a dictionary using the index associated with the object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDictionary;

**Входные данные**:

* (System.Object)AcadDictionary;
* (System.Object)Index;

### Нод Count

**Имя нода** = Count;

**Описание** = Returns the number of objects in a dictionary.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDictionary;

**Входные данные**:

* (System.Object)AcadDictionary;

### Нод AddXRecord

**Имя нода** = AddXRecord;

**Описание** = Adds an XRecord object to a dictionary.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDictionary;

**Входные данные**:

* (System.Object)AcadDictionary;
* (System.String)Keyword;

## Группа NVP_nanoCAD_COM.OdaX.AcadDictionaries

### Нод _AcadDictionaries_Constructor

**Имя нода** = _AcadDictionaries_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDictionaries;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadDictionaries_ConstructorCast

**Имя нода** = _AcadDictionaries_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDictionaries;

**Входные данные**:

* (System.Object)dynamic;

### Нод Item

**Имя нода** = Item;

**Описание** = Returns an object in a dictionary using the index associated with the object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDictionaries;

**Входные данные**:

* (System.Object)AcadDictionaries;
* (System.Object)Index;

### Нод Count

**Имя нода** = Count;

**Описание** = Returns the number of objects in a dictionary.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDictionaries;

**Входные данные**:

* (System.Object)AcadDictionaries;

### Нод Add

**Имя нода** = Add;

**Описание** = Creates an object and adds it to the appropriate collection, block, dictionary, group, or selection set.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDictionaries;

**Входные данные**:

* (System.Object)AcadDictionaries;
* (System.String)Name;

## Группа NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences

### Нод _AcadDatabasePreferences_Constructor

**Имя нода** = _AcadDatabasePreferences_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadDatabasePreferences_ConstructorCast

**Имя нода** = _AcadDatabasePreferences_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)dynamic;

### Нод Application

**Имя нода** = Application;

**Описание** = Returns the Application object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;

### Нод SolidFill

**Имя нода** = SolidFill;

**Описание** = Specifies or returns whether certain objects in a document are filled in.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;

### Нод Set_SolidFill

**Имя нода** = Set_SolidFill;

**Описание** = Specifies or returns whether certain objects in a document are filled in.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;
* (System.Object)Fill;

### Нод XRefEdit

**Имя нода** = XRefEdit;

**Описание** = Specifies or returns whether a document can be edited when it is used as an external reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;

### Нод Set_XRefEdit

**Имя нода** = Set_XRefEdit;

**Описание** = Specifies or returns whether a document can be edited when it is used as an external reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;
* (System.Object)Edit;

### Нод XRefLayerVisibility

**Имя нода** = XRefLayerVisibility;

**Описание** = Specifies or returns whether changes to layers and paths of an external reference are saved with the document.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;

### Нод Set_XRefLayerVisibility

**Имя нода** = Set_XRefLayerVisibility;

**Описание** = Specifies or returns whether changes to layers and paths of an external reference are saved with the document.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;
* (System.Object)XRefLayerVis;

### Нод OLELaunch

**Имя нода** = OLELaunch;

**Описание** = Specifies or returns whether to open an OLE object's corresponding application when printing the OLE object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;

### Нод Set_OLELaunch

**Имя нода** = Set_OLELaunch;

**Описание** = Specifies or returns whether to open an OLE object's corresponding application when printing the OLE object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;
* (System.Object)Launch;

### Нод AllowLongSymbolNames

**Имя нода** = AllowLongSymbolNames;

**Описание** = Specifies or returns whether symbol names can include extended characters.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;

### Нод Set_AllowLongSymbolNames

**Имя нода** = Set_AllowLongSymbolNames;

**Описание** = Specifies or returns whether symbol names can include extended characters.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;
* (System.Object)LongNames;

### Нод ObjectSortBySelection

**Имя нода** = ObjectSortBySelection;

**Описание** = Specifies or returns whether objects are sorted by entity selection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;

### Нод Set_ObjectSortBySelection

**Имя нода** = Set_ObjectSortBySelection;

**Описание** = Specifies or returns whether objects are sorted by entity selection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;
* (System.Object)Sort;

### Нод ObjectSortBySnap

**Имя нода** = ObjectSortBySnap;

**Описание** = Specifies or returns whether objects are sorted by object snap.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;

### Нод Set_ObjectSortBySnap

**Имя нода** = Set_ObjectSortBySnap;

**Описание** = Specifies or returns whether objects are sorted by object snap.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;
* (System.Object)Sort;

### Нод ObjectSortByRedraws

**Имя нода** = ObjectSortByRedraws;

**Описание** = Specifies or returns whether objects are sorted by redraw order.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;

### Нод Set_ObjectSortByRedraws

**Имя нода** = Set_ObjectSortByRedraws;

**Описание** = Specifies or returns whether objects are sorted by redraw order.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;
* (System.Object)Sort;

### Нод ObjectSortByRegens

**Имя нода** = ObjectSortByRegens;

**Описание** = Specifies or returns whether objects are sorted by regeneration order.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;

### Нод Set_ObjectSortByRegens

**Имя нода** = Set_ObjectSortByRegens;

**Описание** = Specifies or returns whether objects are sorted by regeneration order.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;
* (System.Object)Sort;

### Нод ObjectSortByPlotting

**Имя нода** = ObjectSortByPlotting;

**Описание** = Specifies or returns whether objects are sorted by printing order.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;

### Нод Set_ObjectSortByPlotting

**Имя нода** = Set_ObjectSortByPlotting;

**Описание** = Specifies or returns whether objects are sorted by printing order.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;
* (System.Object)Sort;

### Нод ObjectSortByPSOutput

**Имя нода** = ObjectSortByPSOutput;

**Описание** = Specifies or returns whether objects are sorted by PostScript output order.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;

### Нод Set_ObjectSortByPSOutput

**Имя нода** = Set_ObjectSortByPSOutput;

**Описание** = Specifies or returns whether objects are sorted by PostScript output order.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;
* (System.Object)Sort;

### Нод Set_ContourLinesPerSurface

**Имя нода** = Set_ContourLinesPerSurface;

**Описание** = Specifies or returns the number of isolines per surface for objects.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;
* (System.Object)Path;

### Нод ContourLinesPerSurface

**Имя нода** = ContourLinesPerSurface;

**Описание** = Specifies or returns the number of isolines per surface for objects.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;

### Нод Set_DisplaySilhouette

**Имя нода** = Set_DisplaySilhouette;

**Описание** = Specifies or returns whether silhouette curves of 3D solids display in wireframe view.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;
* (System.Object)Path;

### Нод DisplaySilhouette

**Имя нода** = DisplaySilhouette;

**Описание** = Specifies or returns whether silhouette curves of 3D solids display in wireframe view.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;

### Нод Set_MaxActiveViewports

**Имя нода** = Set_MaxActiveViewports;

**Описание** = Specifies or returns the maximum number of active viewports allowed in a document.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;
* (System.Object)Path;

### Нод MaxActiveViewports

**Имя нода** = MaxActiveViewports;

**Описание** = Specifies or returns the maximum number of active viewports allowed in a document.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;

### Нод Set_RenderSmoothness

**Имя нода** = Set_RenderSmoothness;

**Описание** = Specifies or returns the quality of hidden, shaded, and rendered objects.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;
* (System.Double)Path;

### Нод RenderSmoothness

**Имя нода** = RenderSmoothness;

**Описание** = Specifies or returns the quality of hidden, shaded, and rendered objects.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;

### Нод Set_SegmentPerPolyline

**Имя нода** = Set_SegmentPerPolyline;

**Описание** = Specifies or returns the number of line segments generated for polylines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;
* (System.Object)Path;

### Нод SegmentPerPolyline

**Имя нода** = SegmentPerPolyline;

**Описание** = Specifies or returns the number of line segments generated for polylines.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;

### Нод Set_TextFrameDisplay

**Имя нода** = Set_TextFrameDisplay;

**Описание** = Specifies or returns whether text frames display instead of text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;
* (System.Object)Path;

### Нод TextFrameDisplay

**Имя нода** = TextFrameDisplay;

**Описание** = Specifies or returns whether text frames display instead of text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;

### Нод Set_Lineweight

**Имя нода** = Set_Lineweight;

**Описание** = Not implemented. Specifies or returns the default lineweight for a document.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;
* (System.Object)OdaX.AcLineWeight;

### Нод Lineweight

**Имя нода** = Lineweight;

**Описание** = Not implemented. Specifies or returns the default lineweight for a document.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;

### Нод Set_LineWeightDisplay

**Имя нода** = Set_LineWeightDisplay;

**Описание** = Specifies or returns whether lineweights display in a document.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;
* (System.Object)Path;

### Нод LineWeightDisplay

**Имя нода** = LineWeightDisplay;

**Описание** = Specifies or returns whether lineweights display in a document.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabasePreferences;

**Входные данные**:

* (System.Object)AcadDatabasePreferences;

## Группа NVP_nanoCAD_COM.OdaX.AcadDatabase

### Нод _AcadDatabase_Constructor

**Имя нода** = _AcadDatabase_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabase;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadDatabase_ConstructorCast

**Имя нода** = _AcadDatabase_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabase;

**Входные данные**:

* (System.Object)dynamic;

### Нод ModelSpace

**Имя нода** = ModelSpace;

**Описание** = Returns the ModelSpace collection for a document.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabase;

**Входные данные**:

* (System.Object)AcadDatabase;

### Нод PaperSpace

**Имя нода** = PaperSpace;

**Описание** = Returns the PaperSpace collection for a document.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabase;

**Входные данные**:

* (System.Object)AcadDatabase;

### Нод Blocks

**Имя нода** = Blocks;

**Описание** = Returns the Blocks collection for a document.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabase;

**Входные данные**:

* (System.Object)AcadDatabase;

### Нод CopyObjects

**Имя нода** = CopyObjects;

**Описание** = Duplicates multiple objects.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabase;

**Входные данные**:

* (System.Object)AcadDatabase;
* (System.Object)Objects;
* (System.Object)Owner;
* (System.Object)IdPairs;

### Нод Groups

**Имя нода** = Groups;

**Описание** = Returns the Groups Collection object for a document.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabase;

**Входные данные**:

* (System.Object)AcadDatabase;

### Нод DimStyles

**Имя нода** = DimStyles;

**Описание** = Returns the DimStyles Collection object for a document.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabase;

**Входные данные**:

* (System.Object)AcadDatabase;

### Нод Layers

**Имя нода** = Layers;

**Описание** = Returns the Layers Collection object for a document.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabase;

**Входные данные**:

* (System.Object)AcadDatabase;

### Нод Linetypes

**Имя нода** = Linetypes;

**Описание** = Returns the Linetypes Collection object for a document.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabase;

**Входные данные**:

* (System.Object)AcadDatabase;

### Нод Dictionaries

**Имя нода** = Dictionaries;

**Описание** = Returns the Dictionaries Collection object for a document.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabase;

**Входные данные**:

* (System.Object)AcadDatabase;

### Нод RegisteredApplications

**Имя нода** = RegisteredApplications;

**Описание** = Returns the RegisteredApplications Collection object for a document.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabase;

**Входные данные**:

* (System.Object)AcadDatabase;

### Нод TextStyles

**Имя нода** = TextStyles;

**Описание** = Returns the TextStyles Collection object of a document.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabase;

**Входные данные**:

* (System.Object)AcadDatabase;

### Нод UserCoordinateSystems

**Имя нода** = UserCoordinateSystems;

**Описание** = Returns the UCSs Collection object of a document.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabase;

**Входные данные**:

* (System.Object)AcadDatabase;

### Нод Views

**Имя нода** = Views;

**Описание** = Returns the Views Collection object of a document.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabase;

**Входные данные**:

* (System.Object)AcadDatabase;

### Нод Viewports

**Имя нода** = Viewports;

**Описание** = Returns the Viewports Collection object of a document.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabase;

**Входные данные**:

* (System.Object)AcadDatabase;

### Нод ElevationModelSpace

**Имя нода** = ElevationModelSpace;

**Описание** = Specifies or returns the elevation in modelspace.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabase;

**Входные данные**:

* (System.Object)AcadDatabase;

### Нод Set_ElevationModelSpace

**Имя нода** = Set_ElevationModelSpace;

**Описание** = Specifies or returns the elevation in modelspace.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabase;

**Входные данные**:

* (System.Object)AcadDatabase;
* (System.Double)Elevation;

### Нод ElevationPaperSpace

**Имя нода** = ElevationPaperSpace;

**Описание** = Specifies or returns the elevation in paperspace.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabase;

**Входные данные**:

* (System.Object)AcadDatabase;

### Нод Set_ElevationPaperSpace

**Имя нода** = Set_ElevationPaperSpace;

**Описание** = Specifies or returns the elevation in paperspace.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabase;

**Входные данные**:

* (System.Object)AcadDatabase;
* (System.Double)Elevation;

### Нод Limits

**Имя нода** = Limits;

**Описание** = Specifies or returns the drawing limits of a document.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabase;

**Входные данные**:

* (System.Object)AcadDatabase;

### Нод Set_Limits

**Имя нода** = Set_Limits;

**Описание** = Specifies or returns the drawing limits of a document.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabase;

**Входные данные**:

* (System.Object)AcadDatabase;
* (System.Object)Limits;

### Нод HandleToObject

**Имя нода** = HandleToObject;

**Описание** = Returns the object that corresponds with a specified handle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabase;

**Входные данные**:

* (System.Object)AcadDatabase;
* (System.String)Handle;

### Нод ObjectIdToObject

**Имя нода** = ObjectIdToObject;

**Описание** = Returns the object that corresponds with a specified object ID.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabase;

**Входные данные**:

* (System.Object)AcadDatabase;
* (System.Object)ObjectID;

### Нод Layouts

**Имя нода** = Layouts;

**Описание** = Returns the Layouts Collection object for a document.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabase;

**Входные данные**:

* (System.Object)AcadDatabase;

### Нод PlotConfigurations

**Имя нода** = PlotConfigurations;

**Описание** = Returns the PlotConfigurations Collection object for a document.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabase;

**Входные данные**:

* (System.Object)AcadDatabase;

### Нод Preferences

**Имя нода** = Preferences;

**Описание** = Returns the Preferences object for a document.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabase;

**Входные данные**:

* (System.Object)AcadDatabase;

### Нод FileDependencies

**Имя нода** = FileDependencies;

**Описание** = Returns the FileDependencies Collection object for a document.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabase;

**Входные данные**:

* (System.Object)AcadDatabase;

### Нод SummaryInfo

**Имя нода** = SummaryInfo;

**Описание** = Returns the SummaryInfo object for a document.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabase;

**Входные данные**:

* (System.Object)AcadDatabase;

### Нод SectionManager

**Имя нода** = SectionManager;

**Описание** = Returns the SectionManager Collection object for a document.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabase;

**Входные данные**:

* (System.Object)AcadDatabase;

### Нод Materials

**Имя нода** = Materials;

**Описание** = Returns the Materials Collection object for a document.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadDatabase;

**Входные данные**:

* (System.Object)AcadDatabase;

## Группа NVP_nanoCAD_COM.OdaX.AcadCircle

### Нод _AcadCircle_Constructor

**Имя нода** = _AcadCircle_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadCircle;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadCircle_ConstructorCast

**Имя нода** = _AcadCircle_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadCircle;

**Входные данные**:

* (System.Object)dynamic;

### Нод Center

**Имя нода** = Center;

**Описание** = Specifies or returns the x, y, z coordinates of the center of a circle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadCircle;

**Входные данные**:

* (System.Object)AcadCircle;

### Нод Set_Center

**Имя нода** = Set_Center;

**Описание** = Specifies or returns the x, y, z coordinates of the center of a circle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadCircle;

**Входные данные**:

* (System.Object)AcadCircle;
* (System.Object)CenterPoint;

### Нод Radius

**Имя нода** = Radius;

**Описание** = Specifies or returns the radius of a circle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadCircle;

**Входные данные**:

* (System.Object)AcadCircle;

### Нод Set_Radius

**Имя нода** = Set_Radius;

**Описание** = Specifies or returns the radius of a circle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadCircle;

**Входные данные**:

* (System.Object)AcadCircle;
* (System.Double)Radius;

### Нод Diameter

**Имя нода** = Diameter;

**Описание** = Specifies or returns the diameter of a circle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadCircle;

**Входные данные**:

* (System.Object)AcadCircle;

### Нод Set_Diameter

**Имя нода** = Set_Diameter;

**Описание** = Specifies or returns the diameter of a circle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadCircle;

**Входные данные**:

* (System.Object)AcadCircle;
* (System.Double)Diameter;

### Нод Circumference

**Имя нода** = Circumference;

**Описание** = Specifies or returns the circumference of a circle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadCircle;

**Входные данные**:

* (System.Object)AcadCircle;

### Нод Set_Circumference

**Имя нода** = Set_Circumference;

**Описание** = Specifies or returns the circumference of a circle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadCircle;

**Входные данные**:

* (System.Object)AcadCircle;
* (System.Double)Circumference;

### Нод Area

**Имя нода** = Area;

**Описание** = Specifies or returns the area of a circle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadCircle;

**Входные данные**:

* (System.Object)AcadCircle;

### Нод Set_Area

**Имя нода** = Set_Area;

**Описание** = Specifies or returns the area of a circle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadCircle;

**Входные данные**:

* (System.Object)AcadCircle;
* (System.Double)Area;

### Нод Normal

**Имя нода** = Normal;

**Описание** = Specifies or returns the x, y, z coordinates of the normal direction vector.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadCircle;

**Входные данные**:

* (System.Object)AcadCircle;

### Нод Set_Normal

**Имя нода** = Set_Normal;

**Описание** = Specifies or returns the x, y, z coordinates of the normal direction vector.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadCircle;

**Входные данные**:

* (System.Object)AcadCircle;
* (System.Object)Normal;

### Нод Thickness

**Имя нода** = Thickness;

**Описание** = z-direction value) of a circle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadCircle;

**Входные данные**:

* (System.Object)AcadCircle;

### Нод Set_Thickness

**Имя нода** = Set_Thickness;

**Описание** = z-direction value) of a circle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadCircle;

**Входные данные**:

* (System.Object)AcadCircle;
* (System.Double)Thickness;

### Нод Offset

**Имя нода** = Offset;

**Описание** = Not implemented. Creates a new circle at a specified distance from an original circle.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadCircle;

**Входные данные**:

* (System.Object)AcadCircle;
* (System.Double)Distance;

## Группа NVP_nanoCAD_COM.OdaX.AcadBlocks

### Нод _AcadBlocks_Constructor

**Имя нода** = _AcadBlocks_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlocks;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadBlocks_ConstructorCast

**Имя нода** = _AcadBlocks_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlocks;

**Входные данные**:

* (System.Object)dynamic;

### Нод Item

**Имя нода** = Item;

**Описание** = Returns an object at a given index in a collection, block, group, or selection set.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlocks;

**Входные данные**:

* (System.Object)AcadBlocks;
* (System.Object)Index;

### Нод Count

**Имя нода** = Count;

**Описание** = Returns the number of items in the collection, block, dictionary, group, or selection set.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlocks;

**Входные данные**:

* (System.Object)AcadBlocks;

### Нод Add

**Имя нода** = Add;

**Описание** = Creates an object and adds it to the collection, block, dictionary, group, or selection set.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlocks;

**Входные данные**:

* (System.Object)AcadBlocks;
* (System.Object)InsertionPoint;
* (System.String)Name;

### Нод GetAll_Blocks

**Имя нода** = GetAll_Blocks;

**Описание** = Возвращает все Блоки в виде OdaX.AcadBlock;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlocks;

**Входные данные**:

* (System.Object)AcadBlocks;

## Группа NVP_nanoCAD_COM.OdaX.AcadBlockReference

### Нод _AcadBlockReference_Constructor

**Имя нода** = _AcadBlockReference_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlockReference;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadBlockReference_ConstructorCast

**Имя нода** = _AcadBlockReference_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlockReference;

**Входные данные**:

* (System.Object)dynamic;

### Нод InsertionPoint

**Имя нода** = InsertionPoint;

**Описание** = Specifies or returns the x, y, z coordinates for the insertion point of a block or external reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlockReference;

**Входные данные**:

* (System.Object)AcadBlockReference;

### Нод Set_InsertionPoint

**Имя нода** = Set_InsertionPoint;

**Описание** = Specifies or returns the x, y, z coordinates for the insertion point of a block or external reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlockReference;

**Входные данные**:

* (System.Object)AcadBlockReference;
* (System.Object)insPoint;

### Нод Name

**Имя нода** = Name;

**Описание** = Specifies or returns the name of a block or external reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlockReference;

**Входные данные**:

* (System.Object)AcadBlockReference;

### Нод Set_Name

**Имя нода** = Set_Name;

**Описание** = Specifies or returns the name of a block or external reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlockReference;

**Входные данные**:

* (System.Object)AcadBlockReference;
* (System.String)bstrName;

### Нод Normal

**Имя нода** = Normal;

**Описание** = Specifies or returns the x, y, and z coordinates of the normal direction vector.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlockReference;

**Входные данные**:

* (System.Object)AcadBlockReference;

### Нод Set_Normal

**Имя нода** = Set_Normal;

**Описание** = Specifies or returns the x, y, and z coordinates of the normal direction vector.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlockReference;

**Входные данные**:

* (System.Object)AcadBlockReference;
* (System.Object)Normal;

### Нод Rotation

**Имя нода** = Rotation;

**Описание** = Specifies or returns the rotation angle of a block or external reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlockReference;

**Входные данные**:

* (System.Object)AcadBlockReference;

### Нод Set_Rotation

**Имя нода** = Set_Rotation;

**Описание** = Specifies or returns the rotation angle of a block or external reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlockReference;

**Входные данные**:

* (System.Object)AcadBlockReference;
* (System.Object)rotAngle;

### Нод XScaleFactor

**Имя нода** = XScaleFactor;

**Описание** = Specifies or returns the XScale factor of a block or external reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlockReference;

**Входные данные**:

* (System.Object)AcadBlockReference;

### Нод Set_XScaleFactor

**Имя нода** = Set_XScaleFactor;

**Описание** = Specifies or returns the XScale factor of a block or external reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlockReference;

**Входные данные**:

* (System.Object)AcadBlockReference;
* (System.Object)ScaleFactor;

### Нод YScaleFactor

**Имя нода** = YScaleFactor;

**Описание** = Specifies or returns the YScale factor of a block or external reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlockReference;

**Входные данные**:

* (System.Object)AcadBlockReference;

### Нод Set_YScaleFactor

**Имя нода** = Set_YScaleFactor;

**Описание** = Specifies or returns the YScale factor of a block or external reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlockReference;

**Входные данные**:

* (System.Object)AcadBlockReference;
* (System.Object)ScaleFactor;

### Нод ZScaleFactor

**Имя нода** = ZScaleFactor;

**Описание** = Specifies or returns the ZScale factor of a block or external reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlockReference;

**Входные данные**:

* (System.Object)AcadBlockReference;

### Нод Set_ZScaleFactor

**Имя нода** = Set_ZScaleFactor;

**Описание** = Specifies or returns the ZScale factor of a block or external reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlockReference;

**Входные данные**:

* (System.Object)AcadBlockReference;
* (System.Object)ScaleFactor;

### Нод Explode

**Имя нода** = Explode;

**Описание** = Explodes the block or external reference and returns the individual entities as an array of the object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlockReference;

**Входные данные**:

* (System.Object)AcadBlockReference;

### Нод GetAttributes

**Имя нода** = GetAttributes;

**Описание** = Returns the attributes of a block or external reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlockReference;

**Входные данные**:

* (System.Object)AcadBlockReference;

### Нод GetConstantAttributes

**Имя нода** = GetConstantAttributes;

**Описание** = Returns the constant attributes of a block or external reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlockReference;

**Входные данные**:

* (System.Object)AcadBlockReference;

### Нод HasAttributes

**Имя нода** = HasAttributes;

**Описание** = Returns if a block or external reference has any attributes associated with it.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlockReference;

**Входные данные**:

* (System.Object)AcadBlockReference;

### Нод EffectiveName

**Имя нода** = EffectiveName;

**Описание** = Returns the original name of a block reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlockReference;

**Входные данные**:

* (System.Object)AcadBlockReference;

### Нод IsDynamicBlock

**Имя нода** = IsDynamicBlock;

**Описание** = Returns if a block reference is dynamic.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlockReference;

**Входные данные**:

* (System.Object)AcadBlockReference;

### Нод GetDynamicBlockProperties

**Имя нода** = GetDynamicBlockProperties;

**Описание** = Returns the properties of a dynamic block reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlockReference;

**Входные данные**:

* (System.Object)AcadBlockReference;

### Нод ResetBlock

**Имя нода** = ResetBlock;

**Описание** = Changes a dynamic block reference to its original state.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlockReference;

**Входные данные**:

* (System.Object)AcadBlockReference;

### Нод ConvertToAnonymousBlock

**Имя нода** = ConvertToAnonymousBlock;

**Описание** = Converts a dynamic block reference to a regular block reference with no name.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlockReference;

**Входные данные**:

* (System.Object)AcadBlockReference;

### Нод ConvertToStaticBlock

**Имя нода** = ConvertToStaticBlock;

**Описание** = Converts a dynamic block reference to a regular block reference with a name.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlockReference;

**Входные данные**:

* (System.Object)AcadBlockReference;
* (System.String)newBlockName;

### Нод XEffectiveScaleFactor

**Имя нода** = XEffectiveScaleFactor;

**Описание** = Specifies or returns the effective XScale factor of a block or external reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlockReference;

**Входные данные**:

* (System.Object)AcadBlockReference;

### Нод Set_XEffectiveScaleFactor

**Имя нода** = Set_XEffectiveScaleFactor;

**Описание** = Specifies or returns the effective XScale factor of a block or external reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlockReference;

**Входные данные**:

* (System.Object)AcadBlockReference;
* (System.Object)ScaleFactor;

### Нод YEffectiveScaleFactor

**Имя нода** = YEffectiveScaleFactor;

**Описание** = Specifies or returns the effective YScale factor of a block or external reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlockReference;

**Входные данные**:

* (System.Object)AcadBlockReference;

### Нод Set_YEffectiveScaleFactor

**Имя нода** = Set_YEffectiveScaleFactor;

**Описание** = Specifies or returns the effective YScale factor of a block or external reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlockReference;

**Входные данные**:

* (System.Object)AcadBlockReference;
* (System.Object)ScaleFactor;

### Нод ZEffectiveScaleFactor

**Имя нода** = ZEffectiveScaleFactor;

**Описание** = Specifies or returns the effective ZScale factor of a block or external reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlockReference;

**Входные данные**:

* (System.Object)AcadBlockReference;

### Нод Set_ZEffectiveScaleFactor

**Имя нода** = Set_ZEffectiveScaleFactor;

**Описание** = Specifies or returns the effective ZScale factor of a block or external reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlockReference;

**Входные данные**:

* (System.Object)AcadBlockReference;
* (System.Object)ScaleFactor;

### Нод InsUnits

**Имя нода** = InsUnits;

**Описание** = Returns the insertion units of a block or external reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlockReference;

**Входные данные**:

* (System.Object)AcadBlockReference;

### Нод InsUnitsFactor

**Имя нода** = InsUnitsFactor;

**Описание** = Returns the insertion units factor of a block or external reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlockReference;

**Входные данные**:

* (System.Object)AcadBlockReference;

## Группа NVP_nanoCAD_COM.OdaX.AcadBlock

### Нод _AcadBlock_Constructor

**Имя нода** = _AcadBlock_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadBlock_ConstructorCast

**Имя нода** = _AcadBlock_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)dynamic;

### Нод Item

**Имя нода** = Item;

**Описание** = Returns an object at a given index in a collection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)Index;

### Нод Count

**Имя нода** = Count;

**Описание** = Returns the number of items in a collection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;

### Нод Name

**Имя нода** = Name;

**Описание** = Specifies or returns the name of an object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;

### Нод Set_Name

**Имя нода** = Set_Name;

**Описание** = Specifies or returns the name of an object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.String)bstrName;

### Нод Origin

**Имя нода** = Origin;

**Описание** = Specifies or returns the coordinates for the origin of a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;

### Нод Set_Origin

**Имя нода** = Set_Origin;

**Описание** = Specifies or returns the coordinates for the origin of a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)Origin;

### Нод AddCustomObject

**Имя нода** = AddCustomObject;

**Описание** = Creates a custom object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.String)ClassName;

### Нод Add3DFace

**Имя нода** = Add3DFace;

**Описание** = Creates a 3DFace object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)Point1;
* (System.Object)Point2;
* (System.Object)point3;
* (System.Object)Point4;

### Нод Add3DMesh

**Имя нода** = Add3DMesh;

**Описание** = Creates a PolyfaceMesh object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Int32)M;
* (System.Int32)N;
* (System.Object)PointsMatrix;

### Нод Add3DPoly

**Имя нода** = Add3DPoly;

**Описание** = Creates a 3DPolyline object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)PointsArray;

### Нод AddArc

**Имя нода** = AddArc;

**Описание** = Creates an Arc object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)Center;
* (System.Double)Radius;
* (System.Double)StartAngle;
* (System.Double)EndAngle;

### Нод AddAttribute

**Имя нода** = AddAttribute;

**Описание** = Creates an Attribute object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Double)Height;
* (System.Object)OdaX.AcAttributeMode;
* (System.String)Prompt;
* (System.Object)InsertionPoint;
* (System.String)Tag;
* (System.String)Value;

### Нод AddBox

**Имя нода** = AddBox;

**Описание** = Creates a 3D solid box and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)Origin;
* (System.Double)Length;
* (System.Double)Width;
* (System.Double)Height;

### Нод AddCircle

**Имя нода** = AddCircle;

**Описание** = Creates a Circle object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)Center;
* (System.Double)Radius;

### Нод AddCone

**Имя нода** = AddCone;

**Описание** = Creates a 3D solid cone and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)Center;
* (System.Double)BaseRadius;
* (System.Double)Height;

### Нод AddCylinder

**Имя нода** = AddCylinder;

**Описание** = Creates a 3D solid cylinder and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)Center;
* (System.Double)Radius;
* (System.Double)Height;

### Нод AddDimAligned

**Имя нода** = AddDimAligned;

**Описание** = Creates an aligned dimension object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)ExtLine1Point;
* (System.Object)ExtLine2Point;
* (System.Object)TextPosition;

### Нод AddDimAngular

**Имя нода** = AddDimAngular;

**Описание** = Creates an angular dimension object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)AngleVertex;
* (System.Object)FirstEndPoint;
* (System.Object)SecondEndPoint;
* (System.Object)TextPoint;

### Нод AddDimDiametric

**Имя нода** = AddDimDiametric;

**Описание** = Creates a diametric dimension object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)ChordPoint;
* (System.Object)FarChordPoint;
* (System.Double)LeaderLength;

### Нод AddDimRotated

**Имя нода** = AddDimRotated;

**Описание** = Creates a rotated linear dimension object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)ExtLine1Point;
* (System.Object)ExtLine2Point;
* (System.Object)DimLineLocation;
* (System.Double)RotationAngle;

### Нод AddDimOrdinate

**Имя нода** = AddDimOrdinate;

**Описание** = Creates an ordinate dimension object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)DefinitionPoint;
* (System.Object)LeaderEndPoint;
* (System.Int32)UseXAxis;

### Нод AddDimRadial

**Имя нода** = AddDimRadial;

**Описание** = Creates a radial dimension object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)Center;
* (System.Object)ChordPoint;
* (System.Double)LeaderLength;

### Нод AddEllipse

**Имя нода** = AddEllipse;

**Описание** = Creates an Ellipse object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)Center;
* (System.Object)MajorAxis;
* (System.Double)RadiusRatio;

### Нод AddEllipticalCone

**Имя нода** = AddEllipticalCone;

**Описание** = Creates a 3D solid elliptical cone object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)Center;
* (System.Double)MajorRadius;
* (System.Double)MinorRadius;
* (System.Double)Height;

### Нод AddEllipticalCylinder

**Имя нода** = AddEllipticalCylinder;

**Описание** = Creates a 3D solid elliptical cylinder object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)Center;
* (System.Double)MajorRadius;
* (System.Double)MinorRadius;
* (System.Double)Height;

### Нод AddExtrudedSolid

**Имя нода** = AddExtrudedSolid;

**Описание** = Creates an extruded solid object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)Profile;
* (System.Double)Height;
* (System.Double)TaperAngle;

### Нод AddExtrudedSolidAlongPath

**Имя нода** = AddExtrudedSolidAlongPath;

**Описание** = Creates an extruded solid object along a path and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)Profile;
* (System.Object)Path;

### Нод AddLeader

**Имя нода** = AddLeader;

**Описание** = Creates a Leader object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)PointsArray;
* (System.Object)Annotation;
* (System.Object)OdaX.AcLeaderType;

### Нод AddMText

**Имя нода** = AddMText;

**Описание** = Creates an Multiline Text object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)InsertionPoint;
* (System.Double)Width;
* (System.String)Text;

### Нод AddPoint

**Имя нода** = AddPoint;

**Описание** = Creates a Point object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)Point;

### Нод AddLightWeightPolyline

**Имя нода** = AddLightWeightPolyline;

**Описание** = Creates a Lightweight Polyline object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)VerticesList;

### Нод AddPolyline

**Имя нода** = AddPolyline;

**Описание** = Creates a Polyline object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)VerticesList;

### Нод AddRay

**Имя нода** = AddRay;

**Описание** = Creates a Ray object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)Point1;
* (System.Object)Point2;

### Нод AddRegion

**Имя нода** = AddRegion;

**Описание** = Creates a Region object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)ObjectList;

### Нод AddRevolvedSolid

**Имя нода** = AddRevolvedSolid;

**Описание** = Creates a revolved solid object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)Profile;
* (System.Object)AxisPoint;
* (System.Object)AxisDir;
* (System.Double)Angle;

### Нод AddShape

**Имя нода** = AddShape;

**Описание** = Creates a Shape object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.String)Name;
* (System.Object)InsertionPoint;
* (System.Double)ScaleFactor;
* (System.Double)RotationAngle;

### Нод AddSolid

**Имя нода** = AddSolid;

**Описание** = Creates a 2D solid polygon object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)Point1;
* (System.Object)Point2;
* (System.Object)point3;
* (System.Object)Point4;

### Нод AddSphere

**Имя нода** = AddSphere;

**Описание** = Creates a Sphere object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)Center;
* (System.Double)Radius;

### Нод AddSpline

**Имя нода** = AddSpline;

**Описание** = Creates a Spline object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)PointsArray;
* (System.Object)StartTangent;
* (System.Object)EndTangent;

### Нод AddText

**Имя нода** = AddText;

**Описание** = Creates a Text object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.String)TextString;
* (System.Object)InsertionPoint;
* (System.Double)Height;

### Нод AddTolerance

**Имя нода** = AddTolerance;

**Описание** = Creates a Tolerance object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.String)Text;
* (System.Object)InsertionPoint;
* (System.Object)Direction;

### Нод AddTorus

**Имя нода** = AddTorus;

**Описание** = Creates a Torus object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)Center;
* (System.Double)TorusRadius;
* (System.Double)TubeRadius;

### Нод AddTrace

**Имя нода** = AddTrace;

**Описание** = Creates a Trace object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)PointsArray;

### Нод AddWedge

**Имя нода** = AddWedge;

**Описание** = Creates a Wedge object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)Center;
* (System.Double)Length;
* (System.Double)Width;
* (System.Double)Height;

### Нод AddXline

**Имя нода** = AddXline;

**Описание** = Creates an XLine object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)Point1;
* (System.Object)Point2;

### Нод InsertBlock

**Имя нода** = InsertBlock;

**Описание** = Inserts a drawing or named block to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)InsertionPoint;
* (System.String)Name;
* (System.Double)Xscale;
* (System.Double)Yscale;
* (System.Double)Zscale;
* (System.Double)Rotation;
* (System.Object)Password;

### Нод AddHatch

**Имя нода** = AddHatch;

**Описание** = Creates a Hatch object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Int32)PatternType;
* (System.String)PatternName;
* (System.Object)Associativity;
* (System.Object)HatchObjectType;

### Нод AddRaster

**Имя нода** = AddRaster;

**Описание** = Adds a raster image as a Raster object to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.String)imageFileName;
* (System.Object)InsertionPoint;
* (System.Double)ScaleFactor;
* (System.Double)RotationAngle;

### Нод AddLine

**Имя нода** = AddLine;

**Описание** = Creates a Line object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)StartPoint;
* (System.Object)EndPoint;

### Нод IsLayout

**Имя нода** = IsLayout;

**Описание** = Returns whether a specified block is a layout block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;

### Нод Layout

**Имя нода** = Layout;

**Описание** = Returns the layout associated with the specified block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;

### Нод IsXRef

**Имя нода** = IsXRef;

**Описание** = Returns whether a specified block is an external reference block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;

### Нод AddMInsertBlock

**Имя нода** = AddMInsertBlock;

**Описание** = Adds an array of blocks to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)InsertionPoint;
* (System.String)Name;
* (System.Double)Xscale;
* (System.Double)Yscale;
* (System.Double)Zscale;
* (System.Double)Rotation;
* (System.Object)NumRows;
* (System.Object)NumColumns;
* (System.Object)RowSpacing;
* (System.Object)ColumnSpacing;
* (System.Object)Password;

### Нод AddPolyfaceMesh

**Имя нода** = AddPolyfaceMesh;

**Описание** = Creates a Polyface Mesh object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)VertexList;
* (System.Object)FaceList;

### Нод AddMLine

**Имя нода** = AddMLine;

**Описание** = Creates a Multiline object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)VertexList;

### Нод AddDim3PointAngular

**Имя нода** = AddDim3PointAngular;

**Описание** = Creates a 3-point angular dimension object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)AngleVertex;
* (System.Object)FirstEndPoint;
* (System.Object)SecondEndPoint;
* (System.Object)TextPoint;

### Нод XRefDatabase

**Имя нода** = XRefDatabase;

**Описание** = Returns the Database object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;

### Нод AttachExternalReference

**Имя нода** = AttachExternalReference;

**Описание** = Attaches an external reference to a drawing.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.String)PathName;
* (System.String)Name;
* (System.Object)InsertionPoint;
* (System.Double)Xscale;
* (System.Double)Yscale;
* (System.Double)Zscale;
* (System.Double)Rotation;
* (System.Object)bOverlay;
* (System.Object)Password;

### Нод Unload

**Имя нода** = Unload;

**Описание** = Unloads an external reference from a drawing.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;

### Нод Reload

**Имя нода** = Reload;

**Описание** = Reloads an external reference to a drawing.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;

### Нод Bind

**Имя нода** = Bind;

**Описание** = Binds an external reference to a drawing.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)bPrefixName;

### Нод Detach

**Имя нода** = Detach;

**Описание** = Detachs an external reference from a drawing.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;

### Нод AddTable

**Имя нода** = AddTable;

**Описание** = Creates a Table object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)InsertionPoint;
* (System.Int32)NumRows;
* (System.Int32)NumColumns;
* (System.Double)RowHeight;
* (System.Double)ColWidth;

### Нод Path

**Имя нода** = Path;

**Описание** = Specifies or returns the path of an external reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;

### Нод Set_Path

**Имя нода** = Set_Path;

**Описание** = Specifies or returns the path of an external reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.String)bstrName;

### Нод Comments

**Имя нода** = Comments;

**Описание** = Specifies or returns the comments of a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;

### Нод Set_Comments

**Имя нода** = Set_Comments;

**Описание** = Specifies or returns the comments of a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.String)bstrName;

### Нод Units

**Имя нода** = Units;

**Описание** = Specifies or returns the units of measure for a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;

### Нод Set_Units

**Имя нода** = Set_Units;

**Описание** = Specifies or returns the units of measure for a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)OdaX.AcInsertUnits;

### Нод Explodable

**Имя нода** = Explodable;

**Описание** = Specifies or returns whether a block can be exploded.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;

### Нод Set_Explodable

**Имя нода** = Set_Explodable;

**Описание** = Specifies or returns whether a block can be exploded.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)bExplodable;

### Нод BlockScaling

**Имя нода** = BlockScaling;

**Описание** = Specifies or returns whether a block can be scaled.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;

### Нод Set_BlockScaling

**Имя нода** = Set_BlockScaling;

**Описание** = Specifies or returns whether a block can be scaled.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)OdaX.AcBlockScaling;

### Нод IsDynamicBlock

**Имя нода** = IsDynamicBlock;

**Описание** = Returns whether the block is a dynamic block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;

### Нод AddDimArc

**Имя нода** = AddDimArc;

**Описание** = Creates an arc length dimension object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)ArcCenter;
* (System.Object)FirstEndPoint;
* (System.Object)SecondEndPoint;
* (System.Object)ArcPoint;

### Нод AddDimRadialLarge

**Имя нода** = AddDimRadialLarge;

**Описание** = Creates a jogged radial dimension object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)Center;
* (System.Object)ChordPoint;
* (System.Object)OverrideCenter;
* (System.Object)JogPoint;
* (System.Double)JogAngle;

### Нод AddSection

**Имя нода** = AddSection;

**Описание** = Creates a Section object and adds it to modelspace, paperspace, or a block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)FromPoint;
* (System.Object)ToPoint;
* (System.Object)planeVector;

### Нод AddMLeader

**Имя нода** = AddMLeader;

**Описание** = Creates a multileader;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Object)PointsArray;
* (System.Int32)leaderLineIndex;

### Нод AddEmbeddedRaster

**Имя нода** = AddEmbeddedRaster;

**Описание** = Embed raster as an OLE item.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.String)ImagePath;
* (System.Object)InsertionPoint;
* (System.Double)Height;
* (System.Double)Width;

### Нод GetAll_Entities

**Имя нода** = GetAll_Entities;

**Описание** = Возвращает все Объекты модели в данном блоке в виде OdaX.AcadEntity;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;

### Нод GetAll_EntitiesByName

**Имя нода** = GetAll_EntitiesByName;

**Описание** = Возвращает все Объекты модели в данном блоке в виде OdaX.AcadEntity для данного Типа и с режимом сравнения типа (true: ==, false: Contains);

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.String)Тип;
* (System.Boolean)Точное соответствие;

### Нод GetAll_EntitiesByNames

**Имя нода** = GetAll_EntitiesByNames;

**Описание** = Возвращает все Объекты модели в данном блоке в виде OdaX.AcadEntity для данного Типа и с режимом сравнения типов (true: ==, false: Contains);

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;
* (System.Collections.Generic.List`1[System.String])Список типов;
* (System.Boolean)Точное соответствие;

### Нод GetAll_EntitiesMST

**Имя нода** = GetAll_EntitiesMST;

**Описание** = Возвращает все Объекты модели ModelStudioCS в данном Блоке в виде OdaX.AcadEntity;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadBlock;

**Входные данные**:

* (System.Object)AcadBlock;

## Группа NVP_nanoCAD_COM.OdaX.AcadAttributeReference

### Нод _AcadAttributeReference_Constructor

**Имя нода** = _AcadAttributeReference_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadAttributeReference_ConstructorCast

**Имя нода** = _AcadAttributeReference_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)dynamic;

### Нод Height

**Имя нода** = Height;

**Описание** = Specifies or returns the height of an attribute reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;

### Нод Set_Height

**Имя нода** = Set_Height;

**Описание** = Specifies or returns the height of an attribute reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;
* (System.Double)Height;

### Нод HorizontalAlignment

**Имя нода** = HorizontalAlignment;

**Описание** = Specifies or returns the horizontal alignment of an attribute reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;

### Нод Set_HorizontalAlignment

**Имя нода** = Set_HorizontalAlignment;

**Описание** = Specifies or returns the horizontal alignment of an attribute reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;
* (System.Object)OdaX.AcHorizontalAlignment;

### Нод InsertionPoint

**Имя нода** = InsertionPoint;

**Описание** = Specifies or returns the insertion point of attribute reference text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;

### Нод Set_InsertionPoint

**Имя нода** = Set_InsertionPoint;

**Описание** = Specifies or returns the insertion point of attribute reference text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;
* (System.Object)insPoint;

### Нод Normal

**Имя нода** = Normal;

**Описание** = Specifies or returns the x, y, and z coordinates of the normal direction vector.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;

### Нод Set_Normal

**Имя нода** = Set_Normal;

**Описание** = Specifies or returns the x, y, and z coordinates of the normal direction vector.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;
* (System.Object)Normal;

### Нод ObliqueAngle

**Имя нода** = ObliqueAngle;

**Описание** = Specifies or returns the oblique angle of an attribute reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;

### Нод Set_ObliqueAngle

**Имя нода** = Set_ObliqueAngle;

**Описание** = Specifies or returns the oblique angle of an attribute reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;
* (System.Object)obliAngle;

### Нод Rotation

**Имя нода** = Rotation;

**Описание** = Specifies or returns the rotation angle of an attribute reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;

### Нод Set_Rotation

**Имя нода** = Set_Rotation;

**Описание** = Specifies or returns the rotation angle of an attribute reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;
* (System.Object)rotAngle;

### Нод ScaleFactor

**Имя нода** = ScaleFactor;

**Описание** = Specifies or returns the scale factor of an attribute reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;

### Нод Set_ScaleFactor

**Имя нода** = Set_ScaleFactor;

**Описание** = Specifies or returns the scale factor of an attribute reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;
* (System.Object)scalFactor;

### Нод StyleName

**Имя нода** = StyleName;

**Описание** = Specifies or returns the style name of an attribute reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;

### Нод Set_StyleName

**Имя нода** = Set_StyleName;

**Описание** = Specifies or returns the style name of an attribute reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;
* (System.String)Name;

### Нод TagString

**Имя нода** = TagString;

**Описание** = Specifies or returns the tag of an attribute reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;

### Нод Set_TagString

**Имя нода** = Set_TagString;

**Описание** = Specifies or returns the tag of an attribute reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;
* (System.String)bstrTag;

### Нод TextAlignmentPoint

**Имя нода** = TextAlignmentPoint;

**Описание** = Specifies or returns the x, y, z alignment point of an attribute reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;

### Нод Set_TextAlignmentPoint

**Имя нода** = Set_TextAlignmentPoint;

**Описание** = Specifies or returns the x, y, z alignment point of an attribute reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;
* (System.Object)alignPoint;

### Нод TextGenerationFlag

**Имя нода** = TextGenerationFlag;

**Описание** = Specifies or returns the text generation flag of an attribute reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;

### Нод Set_TextGenerationFlag

**Имя нода** = Set_TextGenerationFlag;

**Описание** = Specifies or returns the text generation flag of an attribute reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;
* (System.Object)textGenFlag;

### Нод TextString

**Имя нода** = TextString;

**Описание** = Specifies or returns the text of an attribute reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;

### Нод Set_TextString

**Имя нода** = Set_TextString;

**Описание** = Specifies or returns the text of an attribute reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;
* (System.String)bstrText;

### Нод Thickness

**Имя нода** = Thickness;

**Описание** = z-direction value) of an attribute reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;

### Нод Set_Thickness

**Имя нода** = Set_Thickness;

**Описание** = z-direction value) of an attribute reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;
* (System.Double)Thickness;

### Нод VerticalAlignment

**Имя нода** = VerticalAlignment;

**Описание** = Specifies or returns the vertical alignment of an attribute reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;

### Нод Set_VerticalAlignment

**Имя нода** = Set_VerticalAlignment;

**Описание** = Specifies or returns the vertical alignment of an attribute reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;
* (System.Object)OdaX.AcVerticalAlignment;

### Нод FieldLength

**Имя нода** = FieldLength;

**Описание** = Specifies or returns the field length of an attribute reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;

### Нод Set_FieldLength

**Имя нода** = Set_FieldLength;

**Описание** = Specifies or returns the field length of an attribute reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;
* (System.Object)fieldLen;

### Нод Alignment

**Имя нода** = Alignment;

**Описание** = Specifies or returns the horizontal and vertical alignment of an attribute reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;

### Нод Set_Alignment

**Имя нода** = Set_Alignment;

**Описание** = Specifies or returns the horizontal and vertical alignment of an attribute reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;
* (System.Object)OdaX.AcAlignment;

### Нод UpsideDown

**Имя нода** = UpsideDown;

**Описание** = Specifies or returns whether the text of an attribute reference is upside down.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;

### Нод Set_UpsideDown

**Имя нода** = Set_UpsideDown;

**Описание** = Specifies or returns whether the text of an attribute reference is upside down.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;
* (System.Object)bUpsideDown;

### Нод Backward

**Имя нода** = Backward;

**Описание** = Specifies or returns whether the text of an attribute reference is backward.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;

### Нод Set_Backward

**Имя нода** = Set_Backward;

**Описание** = Specifies or returns whether the text of an attribute reference is backward.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;
* (System.Object)bBackward;

### Нод Invisible

**Имя нода** = Invisible;

**Описание** = Specifies or returns the invisible mode of an attribute reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;

### Нод Set_Invisible

**Имя нода** = Set_Invisible;

**Описание** = Specifies or returns the invisible mode of an attribute reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;
* (System.Object)bInvisible;

### Нод Constant

**Имя нода** = Constant;

**Описание** = Returns the constant mode of an attribute reference.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;

### Нод LockPosition

**Имя нода** = LockPosition;

**Описание** = Specifies whether the attribute may be moved relative to the geometry in the block;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;

### Нод MTextAttribute

**Имя нода** = MTextAttribute;

**Описание** = Determines whether if the attribute reference is multiline;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;

### Нод Set_MTextAttribute

**Имя нода** = Set_MTextAttribute;

**Описание** = Determines whether if the attribute reference is multiline;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;
* (System.Object)bMTextAttribute;

### Нод MTextAttributeContent

**Имя нода** = MTextAttributeContent;

**Описание** = Gets the multiline attribute reference content;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;

### Нод Set_MTextAttributeContent

**Имя нода** = Set_MTextAttributeContent;

**Описание** = Gets the multiline attribute reference content;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;
* (System.String)content;

### Нод UpdateMTextAttribute

**Имя нода** = UpdateMTextAttribute;

**Описание** = Updates attribute reference from the multiline mtext and vice versa;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;

### Нод MTextBoundaryWidth

**Имя нода** = MTextBoundaryWidth;

**Описание** = Gets the width of text boundary of the Mtext;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;

### Нод Set_MTextBoundaryWidth

**Имя нода** = Set_MTextBoundaryWidth;

**Описание** = Gets the width of text boundary of the Mtext;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;

### Нод MTextDrawingDirection

**Имя нода** = MTextDrawingDirection;

**Описание** = Gets the drawing direction of the Mtext;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;

### Нод Set_MTextDrawingDirection

**Имя нода** = Set_MTextDrawingDirection;

**Описание** = Gets the drawing direction of the Mtext;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttributeReference;

**Входные данные**:

* (System.Object)AcadAttributeReference;
* (System.Object)OdaX.AcDrawingDirection;

## Группа NVP_nanoCAD_COM.OdaX.AcadAttribute

### Нод _AcadAttribute_Constructor

**Имя нода** = _AcadAttribute_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadAttribute_ConstructorCast

**Имя нода** = _AcadAttribute_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)dynamic;

### Нод FieldLength

**Имя нода** = FieldLength;

**Описание** = Specifies or returns the field length of an attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;

### Нод Set_FieldLength

**Имя нода** = Set_FieldLength;

**Описание** = Specifies or returns the field length of an attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;
* (System.Object)fieldLen;

### Нод TagString

**Имя нода** = TagString;

**Описание** = Specifies or returns the tag of an attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;

### Нод Set_TagString

**Имя нода** = Set_TagString;

**Описание** = Specifies or returns the tag of an attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;
* (System.String)Tag;

### Нод PromptString

**Имя нода** = PromptString;

**Описание** = Specifies or returns the prompt string of an attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;

### Нод Set_PromptString

**Имя нода** = Set_PromptString;

**Описание** = Specifies or returns the prompt string of an attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;
* (System.String)bstrPrompt;

### Нод TextString

**Имя нода** = TextString;

**Описание** = Specifies or returns the text that displays for an attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;

### Нод Set_TextString

**Имя нода** = Set_TextString;

**Описание** = Specifies or returns the text that displays for an attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;
* (System.String)bstrText;

### Нод StyleName

**Имя нода** = StyleName;

**Описание** = Specifies or returns the text style of an attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;

### Нод Set_StyleName

**Имя нода** = Set_StyleName;

**Описание** = Specifies or returns the text style of an attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;
* (System.String)Name;

### Нод Alignment

**Имя нода** = Alignment;

**Описание** = Specifies or returns the horizontal and vertical alignment of the attribute text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;

### Нод Set_Alignment

**Имя нода** = Set_Alignment;

**Описание** = Specifies or returns the horizontal and vertical alignment of the attribute text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;
* (System.Object)OdaX.AcAlignment;

### Нод HorizontalAlignment

**Имя нода** = HorizontalAlignment;

**Описание** = Specifies or returns the horizontal alignment of an attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;

### Нод Set_HorizontalAlignment

**Имя нода** = Set_HorizontalAlignment;

**Описание** = Specifies or returns the horizontal alignment of an attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;
* (System.Object)OdaX.AcHorizontalAlignment;

### Нод VerticalAlignment

**Имя нода** = VerticalAlignment;

**Описание** = Specifies or returns the vertical alignment of an attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;

### Нод Set_VerticalAlignment

**Имя нода** = Set_VerticalAlignment;

**Описание** = Specifies or returns the vertical alignment of an attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;
* (System.Object)OdaX.AcVerticalAlignment;

### Нод Height

**Имя нода** = Height;

**Описание** = Specifies or returns the height of an attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;

### Нод Set_Height

**Имя нода** = Set_Height;

**Описание** = Specifies or returns the height of an attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;
* (System.Double)Height;

### Нод Rotation

**Имя нода** = Rotation;

**Описание** = Specifies or returns the rotation angle of an attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;

### Нод Set_Rotation

**Имя нода** = Set_Rotation;

**Описание** = Specifies or returns the rotation angle of an attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;
* (System.Object)rotAngle;

### Нод ScaleFactor

**Имя нода** = ScaleFactor;

**Описание** = Specifies or returns the scale factor of an attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;

### Нод Set_ScaleFactor

**Имя нода** = Set_ScaleFactor;

**Описание** = Specifies or returns the scale factor of an attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;
* (System.Object)scalFactor;

### Нод ObliqueAngle

**Имя нода** = ObliqueAngle;

**Описание** = Specifies or returns the oblique angle of an attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;

### Нод Set_ObliqueAngle

**Имя нода** = Set_ObliqueAngle;

**Описание** = Specifies or returns the oblique angle of an attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;
* (System.Object)obliAngle;

### Нод TextAlignmentPoint

**Имя нода** = TextAlignmentPoint;

**Описание** = Specifies or returns the x, y, z alignment point of an attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;

### Нод Set_TextAlignmentPoint

**Имя нода** = Set_TextAlignmentPoint;

**Описание** = Specifies or returns the x, y, z alignment point of an attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;
* (System.Object)alignPoint;

### Нод InsertionPoint

**Имя нода** = InsertionPoint;

**Описание** = Specifies or returns the x, y, and z coordinates of the attribute text insertion point.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;

### Нод Set_InsertionPoint

**Имя нода** = Set_InsertionPoint;

**Описание** = Specifies or returns the x, y, and z coordinates of the attribute text insertion point.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;
* (System.Object)insPoint;

### Нод Normal

**Имя нода** = Normal;

**Описание** = Specifies or returns the x, y, and z coordinates of the normal direction vector.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;

### Нод Set_Normal

**Имя нода** = Set_Normal;

**Описание** = Specifies or returns the x, y, and z coordinates of the normal direction vector.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;
* (System.Object)Normal;

### Нод TextGenerationFlag

**Имя нода** = TextGenerationFlag;

**Описание** = Specifies or returns the text generation flag of an attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;

### Нод Set_TextGenerationFlag

**Имя нода** = Set_TextGenerationFlag;

**Описание** = Specifies or returns the text generation flag of an attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;
* (System.Object)textGenFlag;

### Нод Thickness

**Имя нода** = Thickness;

**Описание** = z-direction value) of an attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;

### Нод Set_Thickness

**Имя нода** = Set_Thickness;

**Описание** = z-direction value) of an attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;
* (System.Double)Thickness;

### Нод Mode

**Имя нода** = Mode;

**Описание** = Specifies or returns the mode of an attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;

### Нод Set_Mode

**Имя нода** = Set_Mode;

**Описание** = Specifies or returns the mode of an attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;
* (System.Object)Mode;

### Нод UpsideDown

**Имя нода** = UpsideDown;

**Описание** = Specifies or returns whether the text of an attribute is upside down.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;

### Нод Set_UpsideDown

**Имя нода** = Set_UpsideDown;

**Описание** = Specifies or returns whether the text of an attribute is upside down.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;
* (System.Object)bUpsideDown;

### Нод Backward

**Имя нода** = Backward;

**Описание** = Specifies or returns whether the attribute text is backward.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;

### Нод Set_Backward

**Имя нода** = Set_Backward;

**Описание** = Specifies or returns whether the attribute text is backward.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;
* (System.Object)bBackward;

### Нод Invisible

**Имя нода** = Invisible;

**Описание** = Specifies or returns the invisible mode of the attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;

### Нод Set_Invisible

**Имя нода** = Set_Invisible;

**Описание** = Specifies or returns the invisible mode of the attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;
* (System.Object)bInvisible;

### Нод Constant

**Имя нода** = Constant;

**Описание** = Specifies or returns the constant mode of the attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;

### Нод Set_Constant

**Имя нода** = Set_Constant;

**Описание** = Specifies or returns the constant mode of the attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;
* (System.Object)bConstant;

### Нод Verify

**Имя нода** = Verify;

**Описание** = Specifies or returns the verify mode of the attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;

### Нод Set_Verify

**Имя нода** = Set_Verify;

**Описание** = Specifies or returns the verify mode of the attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;
* (System.Object)bVerify;

### Нод Preset

**Имя нода** = Preset;

**Описание** = Specifies or returns the preset mode of the attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;

### Нод Set_Preset

**Имя нода** = Set_Preset;

**Описание** = Specifies or returns the preset mode of the attribute.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;
* (System.Object)bPreset;

### Нод LockPosition

**Имя нода** = LockPosition;

**Описание** = Not implemented. Specifies or returns whether an attribute is moved relative to its associated block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;

### Нод Set_LockPosition

**Имя нода** = Set_LockPosition;

**Описание** = Not implemented. Specifies or returns whether an attribute is moved relative to its associated block.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;
* (System.Object)bLockPosition;

### Нод MTextAttribute

**Имя нода** = MTextAttribute;

**Описание** = Determines whether if the attribute is multiline;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;

### Нод Set_MTextAttribute

**Имя нода** = Set_MTextAttribute;

**Описание** = Determines whether if the attribute is multiline;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;
* (System.Object)bMTextAttribute;

### Нод MTextAttributeContent

**Имя нода** = MTextAttributeContent;

**Описание** = Gets the multiline attribute content;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;

### Нод Set_MTextAttributeContent

**Имя нода** = Set_MTextAttributeContent;

**Описание** = Gets the multiline attribute content;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;
* (System.String)content;

### Нод UpdateMTextAttribute

**Имя нода** = UpdateMTextAttribute;

**Описание** = Updates multiline MText;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;

### Нод MTextBoundaryWidth

**Имя нода** = MTextBoundaryWidth;

**Описание** = Gets the width of text boundary of the Mtext;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;

### Нод Set_MTextBoundaryWidth

**Имя нода** = Set_MTextBoundaryWidth;

**Описание** = Gets the width of text boundary of the Mtext;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;

### Нод MTextDrawingDirection

**Имя нода** = MTextDrawingDirection;

**Описание** = Gets the drawing direction of the Mtext;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;

### Нод Set_MTextDrawingDirection

**Имя нода** = Set_MTextDrawingDirection;

**Описание** = Gets the drawing direction of the Mtext;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAttribute;

**Входные данные**:

* (System.Object)AcadAttribute;
* (System.Object)OdaX.AcDrawingDirection;

## Группа NVP_nanoCAD_COM.OdaX.AcadArc

### Нод _AcadArc_Constructor

**Имя нода** = _AcadArc_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadArc;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadArc_ConstructorCast

**Имя нода** = _AcadArc_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadArc;

**Входные данные**:

* (System.Object)dynamic;

### Нод StartPoint

**Имя нода** = StartPoint;

**Описание** = Returns the x, y, and z coordinates of an arc's start point.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadArc;

**Входные данные**:

* (System.Object)AcadArc;

### Нод Center

**Имя нода** = Center;

**Описание** = Specifies or returns the x, y, and z coordinates of an arc's center point.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadArc;

**Входные данные**:

* (System.Object)AcadArc;

### Нод Set_Center

**Имя нода** = Set_Center;

**Описание** = Specifies or returns the x, y, and z coordinates of an arc's center point.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadArc;

**Входные данные**:

* (System.Object)AcadArc;
* (System.Object)CenterPoint;

### Нод EndPoint

**Имя нода** = EndPoint;

**Описание** = Returns the x, y, and z coordinates of an arc's end point.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadArc;

**Входные данные**:

* (System.Object)AcadArc;

### Нод Radius

**Имя нода** = Radius;

**Описание** = Specifies or returns the radius of an arc.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadArc;

**Входные данные**:

* (System.Object)AcadArc;

### Нод Set_Radius

**Имя нода** = Set_Radius;

**Описание** = Specifies or returns the radius of an arc.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadArc;

**Входные данные**:

* (System.Object)AcadArc;
* (System.Double)Radius;

### Нод StartAngle

**Имя нода** = StartAngle;

**Описание** = Specifies or returns the start angle of an arc.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadArc;

**Входные данные**:

* (System.Object)AcadArc;

### Нод Set_StartAngle

**Имя нода** = Set_StartAngle;

**Описание** = Specifies or returns the start angle of an arc.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadArc;

**Входные данные**:

* (System.Object)AcadArc;
* (System.Object)Angle;

### Нод EndAngle

**Имя нода** = EndAngle;

**Описание** = Specifies or returns the end angle of an arc.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadArc;

**Входные данные**:

* (System.Object)AcadArc;

### Нод Set_EndAngle

**Имя нода** = Set_EndAngle;

**Описание** = Specifies or returns the end angle of an arc.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadArc;

**Входные данные**:

* (System.Object)AcadArc;
* (System.Object)Angle;

### Нод TotalAngle

**Имя нода** = TotalAngle;

**Описание** = Returns the total angle of an arc.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadArc;

**Входные данные**:

* (System.Object)AcadArc;

### Нод ArcLength

**Имя нода** = ArcLength;

**Описание** = Returns the length of an arc.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadArc;

**Входные данные**:

* (System.Object)AcadArc;

### Нод Thickness

**Имя нода** = Thickness;

**Описание** = z-direction value) of an arc.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadArc;

**Входные данные**:

* (System.Object)AcadArc;

### Нод Set_Thickness

**Имя нода** = Set_Thickness;

**Описание** = z-direction value) of an arc.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadArc;

**Входные данные**:

* (System.Object)AcadArc;
* (System.Double)Thickness;

### Нод Offset

**Имя нода** = Offset;

**Описание** = Not implemented. Creates a new arc at a specified distance from an original arc.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadArc;

**Входные данные**:

* (System.Object)AcadArc;
* (System.Double)Distance;

### Нод Area

**Имя нода** = Area;

**Описание** = Returns the area of an arc as if it was closed with a line.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadArc;

**Входные данные**:

* (System.Object)AcadArc;

### Нод Normal

**Имя нода** = Normal;

**Описание** = Specifies or returns the x, y, and z coordinates of the normal direction vector.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadArc;

**Входные данные**:

* (System.Object)AcadArc;

### Нод Set_Normal

**Имя нода** = Set_Normal;

**Описание** = Specifies or returns the x, y, and z coordinates of the normal direction vector.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadArc;

**Входные данные**:

* (System.Object)AcadArc;
* (System.Object)Normal;

## Группа NVP_nanoCAD_COM.OdaX.AcadAcCmColor

### Нод _AcadAcCmColor_Constructor

**Имя нода** = _AcadAcCmColor_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAcCmColor;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AcadAcCmColor_ConstructorCast

**Имя нода** = _AcadAcCmColor_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAcCmColor;

**Входные данные**:

* (System.Object)dynamic;

### Нод Set_EntityColor

**Имя нода** = Set_EntityColor;

**Описание** = Specifies or returns the AcCmEntityColor of a true color.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAcCmColor;

**Входные данные**:

* (System.Object)AcadAcCmColor;
* (System.Object)eColor;

### Нод EntityColor

**Имя нода** = EntityColor;

**Описание** = Specifies or returns the AcCmEntityColor of a true color.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAcCmColor;

**Входные данные**:

* (System.Object)AcadAcCmColor;

### Нод ColorName

**Имя нода** = ColorName;

**Описание** = Returns the name of a color.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAcCmColor;

**Входные данные**:

* (System.Object)AcadAcCmColor;

### Нод BookName

**Имя нода** = BookName;

**Описание** = Returns the book name of a color.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAcCmColor;

**Входные данные**:

* (System.Object)AcadAcCmColor;

### Нод SetNames

**Имя нода** = SetNames;

**Описание** = Specifies the color name and book name of a color.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAcCmColor;

**Входные данные**:

* (System.Object)AcadAcCmColor;
* (System.String)ColorName;
* (System.String)BookName;

### Нод Delete

**Имя нода** = Delete;

**Описание** = Deletes a true color.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAcCmColor;

**Входные данные**:

* (System.Object)AcadAcCmColor;

### Нод Red

**Имя нода** = Red;

**Описание** = Returns the red component of a true color.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAcCmColor;

**Входные данные**:

* (System.Object)AcadAcCmColor;

### Нод Blue

**Имя нода** = Blue;

**Описание** = Returns the blue component of a true color.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAcCmColor;

**Входные данные**:

* (System.Object)AcadAcCmColor;

### Нод Green

**Имя нода** = Green;

**Описание** = Returns the green component of a true color.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAcCmColor;

**Входные данные**:

* (System.Object)AcadAcCmColor;

### Нод SetRGB

**Имя нода** = SetRGB;

**Описание** = Specifies the RGB values of a true color.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAcCmColor;

**Входные данные**:

* (System.Object)AcadAcCmColor;
* (System.Object)Red;
* (System.Object)Green;
* (System.Object)Blue;

### Нод Set_ColorMethod

**Имя нода** = Set_ColorMethod;

**Описание** = Specifies or returns the color method.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAcCmColor;

**Входные данные**:

* (System.Object)AcadAcCmColor;
* (System.Object)OdaX.AcColorMethod;

### Нод ColorMethod

**Имя нода** = ColorMethod;

**Описание** = Specifies or returns the color method.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAcCmColor;

**Входные данные**:

* (System.Object)AcadAcCmColor;

### Нод ColorIndex

**Имя нода** = ColorIndex;

**Описание** = Specifies or returns the color index of an object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAcCmColor;

**Входные данные**:

* (System.Object)AcadAcCmColor;

### Нод Set_ColorIndex

**Имя нода** = Set_ColorIndex;

**Описание** = Specifies or returns the color index of an object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAcCmColor;

**Входные данные**:

* (System.Object)AcadAcCmColor;
* (System.Object)OdaX.AcColor;

### Нод SetColorBookColor

**Имя нода** = SetColorBookColor;

**Описание** = Sets the color to a color from a color book.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.AcadAcCmColor;

**Входные данные**:

* (System.Object)AcadAcCmColor;
* (System.String)BookName;
* (System.String)ColorName;

## Группа NVP_nanoCAD_COM.OdaX.Acad3DSolid

### Нод _Acad3DSolid_Constructor

**Имя нода** = _Acad3DSolid_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DSolid;

**Входные данные**:

* (System.Object)dynamic;

### Нод _Acad3DSolid_ConstructorCast

**Имя нода** = _Acad3DSolid_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DSolid;

**Входные данные**:

* (System.Object)dynamic;

### Нод Centroid

**Имя нода** = Centroid;

**Описание** = Not implemented. Returns the center of the area or mass for a 3D solid or region.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DSolid;

**Входные данные**:

* (System.Object)Acad3DSolid;

### Нод MomentOfInertia

**Имя нода** = MomentOfInertia;

**Описание** = Not implemented. Returns the moment of inertia for a 3D solid.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DSolid;

**Входные данные**:

* (System.Object)Acad3DSolid;

### Нод PrincipalDirections

**Имя нода** = PrincipalDirections;

**Описание** = Not implemented. Returns the principal directions of a 3D solid or region.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DSolid;

**Входные данные**:

* (System.Object)Acad3DSolid;

### Нод PrincipalMoments

**Имя нода** = PrincipalMoments;

**Описание** = Not implemented. Returns the principal moments of a 3D solid or region.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DSolid;

**Входные данные**:

* (System.Object)Acad3DSolid;

### Нод ProductOfInertia

**Имя нода** = ProductOfInertia;

**Описание** = Not implemented. Returns the product of inertia of a 3D solid or region.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DSolid;

**Входные данные**:

* (System.Object)Acad3DSolid;

### Нод RadiiOfGyration

**Имя нода** = RadiiOfGyration;

**Описание** = Not implemented. Returns the radius of gyration of a 3D solid or region.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DSolid;

**Входные данные**:

* (System.Object)Acad3DSolid;

### Нод Volume

**Имя нода** = Volume;

**Описание** = Not implemented. Returns the volume of a 3D solid.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DSolid;

**Входные данные**:

* (System.Object)Acad3DSolid;

### Нод Boolean

**Имя нода** = Boolean;

**Описание** = Not implemented. Performs a boolean operation on an object and another 3D solid or region.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DSolid;

**Входные данные**:

* (System.Object)Acad3DSolid;
* (System.Object)OdaX.AcBoolean;
* (System.Object)SolidObject;

### Нод CheckInterference

**Имя нода** = CheckInterference;

**Описание** = Not implemented. Checks the interference between two 3D solids.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DSolid;

**Входные данные**:

* (System.Object)Acad3DSolid;
* (System.Object)Object;
* (System.Object)CreateInterferenceSolid;

### Нод SectionSolid

**Имя нода** = SectionSolid;

**Описание** = Not implemented. Creates a section of a 3D solid; the resulting section is a region.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DSolid;

**Входные данные**:

* (System.Object)Acad3DSolid;
* (System.Object)Point1;
* (System.Object)Point2;
* (System.Object)point3;

### Нод SliceSolid

**Имя нода** = SliceSolid;

**Описание** = Not implemented. Creates a slice of a 3D solid; the resulting slice is an array of the 3D solid.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DSolid;

**Входные данные**:

* (System.Object)Acad3DSolid;
* (System.Object)Point1;
* (System.Object)Point2;
* (System.Object)point3;
* (System.Object)Negative;

### Нод SolidType

**Имя нода** = SolidType;

**Описание** = Not implemented. Returns the type of 3D solid.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DSolid;

**Входные данные**:

* (System.Object)Acad3DSolid;

### Нод Position

**Имя нода** = Position;

**Описание** = Not implemented. Specifies or returns the center of a 3D solid.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DSolid;

**Входные данные**:

* (System.Object)Acad3DSolid;

### Нод Set_Position

**Имя нода** = Set_Position;

**Описание** = Not implemented. Specifies or returns the center of a 3D solid.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DSolid;

**Входные данные**:

* (System.Object)Acad3DSolid;
* (System.Object)Position;

### Нод History

**Имя нода** = History;

**Описание** = Not implemented. Specifies or returns whether history is saved for a 3D solid.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DSolid;

**Входные данные**:

* (System.Object)Acad3DSolid;

### Нод Set_History

**Имя нода** = Set_History;

**Описание** = Not implemented. Specifies or returns whether history is saved for a 3D solid.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DSolid;

**Входные данные**:

* (System.Object)Acad3DSolid;
* (System.Object)bHistory;

### Нод ShowHistory

**Имя нода** = ShowHistory;

**Описание** = Not implemented. Specifies or returns whether to show the history of a 3D solid.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DSolid;

**Входные данные**:

* (System.Object)Acad3DSolid;

### Нод Set_ShowHistory

**Имя нода** = Set_ShowHistory;

**Описание** = Not implemented. Specifies or returns whether to show the history of a 3D solid.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DSolid;

**Входные данные**:

* (System.Object)Acad3DSolid;
* (System.Object)Position;

## Группа NVP_nanoCAD_COM.OdaX.Acad3DPolyline

### Нод _Acad3DPolyline_Constructor

**Имя нода** = _Acad3DPolyline_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DPolyline;

**Входные данные**:

* (System.Object)dynamic;

### Нод _Acad3DPolyline_ConstructorCast

**Имя нода** = _Acad3DPolyline_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DPolyline;

**Входные данные**:

* (System.Object)dynamic;

### Нод Coordinates

**Имя нода** = Coordinates;

**Описание** = Specifies or returns the current vertex of a 3D polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DPolyline;

**Входные данные**:

* (System.Object)Acad3DPolyline;

### Нод Set_Coordinates

**Имя нода** = Set_Coordinates;

**Описание** = Specifies or returns the current vertex of a 3D polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DPolyline;

**Входные данные**:

* (System.Object)Acad3DPolyline;
* (System.Object)Coordinates;

### Нод AppendVertex

**Имя нода** = AppendVertex;

**Описание** = Appends a vertex to a 3D polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DPolyline;

**Входные данные**:

* (System.Object)Acad3DPolyline;
* (System.Object)vertex;

### Нод Explode

**Имя нода** = Explode;

**Описание** = Explodes a 3D polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DPolyline;

**Входные данные**:

* (System.Object)Acad3DPolyline;

### Нод Coordinate

**Имя нода** = Coordinate;

**Описание** = Specifies or returns the coordinate of a single vertex in an object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DPolyline;

**Входные данные**:

* (System.Object)Acad3DPolyline;
* (System.Int32)Index;

### Нод Set_Coordinate

**Имя нода** = Set_Coordinate;

**Описание** = Specifies or returns the coordinate of a single vertex in an object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DPolyline;

**Входные данные**:

* (System.Object)Acad3DPolyline;
* (System.Int32)Index;
* (System.Object)pVal;

### Нод Type

**Имя нода** = Type;

**Описание** = Specifies or returns the type of line or surface curve fitting.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DPolyline;

**Входные данные**:

* (System.Object)Acad3DPolyline;

### Нод Set_Type

**Имя нода** = Set_Type;

**Описание** = Specifies or returns the type of line or surface curve fitting.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DPolyline;

**Входные данные**:

* (System.Object)Acad3DPolyline;
* (System.Object)OdaX.Ac3DPolylineType;

### Нод Closed

**Имя нода** = Closed;

**Описание** = Specifies or returns whether a 3D polyline is open or closed.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DPolyline;

**Входные данные**:

* (System.Object)Acad3DPolyline;

### Нод Set_Closed

**Имя нода** = Set_Closed;

**Описание** = Specifies or returns whether a 3D polyline is open or closed.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DPolyline;

**Входные данные**:

* (System.Object)Acad3DPolyline;
* (System.Object)fClose;

### Нод Length

**Имя нода** = Length;

**Описание** = Returns the length of a 3D polyline.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DPolyline;

**Входные данные**:

* (System.Object)Acad3DPolyline;

## Группа NVP_nanoCAD_COM.OdaX.Acad3DFace

### Нод _Acad3DFace_Constructor

**Имя нода** = _Acad3DFace_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DFace;

**Входные данные**:

* (System.Object)dynamic;

### Нод _Acad3DFace_ConstructorCast

**Имя нода** = _Acad3DFace_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DFace;

**Входные данные**:

* (System.Object)dynamic;

### Нод Coordinates

**Имя нода** = Coordinates;

**Описание** = Specifies the current vertex of a 3DFace object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DFace;

**Входные данные**:

* (System.Object)Acad3DFace;

### Нод Set_Coordinates

**Имя нода** = Set_Coordinates;

**Описание** = Specifies the current vertex of a 3DFace object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DFace;

**Входные данные**:

* (System.Object)Acad3DFace;
* (System.Object)corners;

### Нод VisibilityEdge1

**Имя нода** = VisibilityEdge1;

**Описание** = Specifies or returns whether edge 1 of a 3DFace is visible or hidden.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DFace;

**Входные данные**:

* (System.Object)Acad3DFace;

### Нод Set_VisibilityEdge1

**Имя нода** = Set_VisibilityEdge1;

**Описание** = Specifies or returns whether edge 1 of a 3DFace is visible or hidden.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DFace;

**Входные данные**:

* (System.Object)Acad3DFace;
* (System.Object)visibility;

### Нод VisibilityEdge2

**Имя нода** = VisibilityEdge2;

**Описание** = Specifies or returns whether edge 2 of a 3DFace is visible or hidden.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DFace;

**Входные данные**:

* (System.Object)Acad3DFace;

### Нод Set_VisibilityEdge2

**Имя нода** = Set_VisibilityEdge2;

**Описание** = Specifies or returns whether edge 2 of a 3DFace is visible or hidden.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DFace;

**Входные данные**:

* (System.Object)Acad3DFace;
* (System.Object)visibility;

### Нод VisibilityEdge3

**Имя нода** = VisibilityEdge3;

**Описание** = Specifies or returns whether edge 3 of a 3DFace is visible or hidden.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DFace;

**Входные данные**:

* (System.Object)Acad3DFace;

### Нод Set_VisibilityEdge3

**Имя нода** = Set_VisibilityEdge3;

**Описание** = Specifies or returns whether edge 3 of a 3DFace is visible or hidden.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DFace;

**Входные данные**:

* (System.Object)Acad3DFace;
* (System.Object)visibility;

### Нод VisibilityEdge4

**Имя нода** = VisibilityEdge4;

**Описание** = Specifies or returns whether edge 4 of a 3DFace is visible or hidden.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DFace;

**Входные данные**:

* (System.Object)Acad3DFace;

### Нод Set_VisibilityEdge4

**Имя нода** = Set_VisibilityEdge4;

**Описание** = Specifies or returns whether edge 4 of a 3DFace is visible or hidden.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DFace;

**Входные данные**:

* (System.Object)Acad3DFace;
* (System.Object)visibility;

### Нод GetInvisibleEdge

**Имя нода** = GetInvisibleEdge;

**Описание** = Returns the visibility status of a 3DFace edge.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DFace;

**Входные данные**:

* (System.Object)Acad3DFace;
* (System.Int32)Index;

### Нод SetInvisibleEdge

**Имя нода** = SetInvisibleEdge;

**Описание** = Specifies the visibility status of a 3DFace edge.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DFace;

**Входные данные**:

* (System.Object)Acad3DFace;
* (System.Int32)Index;
* (System.Object)State;

### Нод Coordinate

**Имя нода** = Coordinate;

**Описание** = Specifies the coordinate of a single vertex in an object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DFace;

**Входные данные**:

* (System.Object)Acad3DFace;
* (System.Int32)Index;

### Нод Set_Coordinate

**Имя нода** = Set_Coordinate;

**Описание** = Specifies the coordinate of a single vertex in an object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.OdaX.Acad3DFace;

**Входные данные**:

* (System.Object)Acad3DFace;
* (System.Int32)Index;
* (System.Object)pVal;

## Группа NVP_nanoCAD_COM.nanoCAD.nanoCADUtility

### Нод _nanoCADUtility_Constructor

**Имя нода** = _nanoCADUtility_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)dynamic;

### Нод _nanoCADUtility_ConstructorCast

**Имя нода** = _nanoCADUtility_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)dynamic;

### Нод AngleToReal

**Имя нода** = AngleToReal;

**Описание** = double) value;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.String)Angle;
* (System.Object)Unit;

### Нод AngleToString

**Имя нода** = AngleToString;

**Описание** = double) value to a string;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.Double)Angle;
* (System.Object)Unit;
* (System.Int32)precision;

### Нод DistanceToReal

**Имя нода** = DistanceToReal;

**Описание** = double) value;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.String)Distance;
* (System.Object)nanoCAD.AcUnits;

### Нод RealToString

**Имя нода** = RealToString;

**Описание** = double) value to a string;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.Double)Value;
* (System.Object)nanoCAD.AcUnits;
* (System.Int32)precision;

### Нод TranslateCoordinates

**Имя нода** = TranslateCoordinates;

**Описание** = Translates a point from one coordinate system to another coordinate system;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.Object)Point;
* (System.Object)nanoCAD.AcCoordinateSystem;
* (System.Object)nanoCAD.AcCoordinateSystem;
* (System.Int32)Displacement;
* (System.Object)OCSNormal;

### Нод InitializeUserInput

**Имя нода** = InitializeUserInput;

**Описание** = Initializes the GetKeyword method;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.Int32)Bits;
* (System.Object)KeyWordList;

### Нод GetInteger

**Имя нода** = GetInteger;

**Описание** = Gets an integer value from the user;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.Object)Prompt;

### Нод GetReal

**Имя нода** = GetReal;

**Описание** = double) value from the user;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.Object)Prompt;

### Нод GetInput

**Имя нода** = GetInput;

**Описание** = Converts an input string from the user into a keyword index;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;

### Нод GetKeyword

**Имя нода** = GetKeyword;

**Описание** = Gets a keyword string from the user;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.Object)Prompt;

### Нод GetString

**Имя нода** = GetString;

**Описание** = Gets a string from the user;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.Int32)HasSpaces;
* (System.Object)Prompt;

### Нод GetAngle

**Имя нода** = GetAngle;

**Описание** = Gets the angle specified. Considers the setting of the ANGBASE system variable;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.Object)Point;
* (System.Object)Prompt;

### Нод AngleFromXAxis

**Имя нода** = AngleFromXAxis;

**Описание** = Gets the angle of a line from the X axis;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.Object)StartPoint;
* (System.Object)EndPoint;

### Нод GetCorner

**Имя нода** = GetCorner;

**Описание** = Gets a corner of a rectangle;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.Object)Point;
* (System.Object)Prompt;

### Нод GetDistance

**Имя нода** = GetDistance;

**Описание** = Gets the distance from the prompt line or a selected set of points on the screen;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.Object)Point;
* (System.Object)Prompt;

### Нод GetOrientation

**Имя нода** = GetOrientation;

**Описание** = Gets the angle specified. Ignores the setting of the ANGBASE system variable;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.Object)Point;
* (System.Object)Prompt;

### Нод GetPoint

**Имя нода** = GetPoint;

**Описание** = Gets the point selected;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.Object)Point;
* (System.Object)Prompt;

### Нод PolarPoint

**Имя нода** = PolarPoint;

**Описание** = Gets the point at a specified angle and distance from a given point;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.Object)Point;
* (System.Double)Angle;
* (System.Double)Distance;

### Нод CreateTypedArray

**Имя нода** = CreateTypedArray;

**Описание** = Creates a variant that contains an array of typed arguments;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.Object)varArr;
* (System.Int32)Type;

### Нод GetEntity

**Имя нода** = GetEntity;

**Описание** = Gets an object interactively;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.Object)Object;
* (System.Object)PickedPoint;
* (System.Object)Prompt;

### Нод Prompt

**Имя нода** = Prompt;

**Описание** = Posts a prompt to the command line;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.String)Message;

### Нод GetSubEntity

**Имя нода** = GetSubEntity;

**Описание** = Gets an object or subentity interactively;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.Object)Object;
* (System.Object)PickedPoint;
* (System.Object)transMatrix;
* (System.Object)ContextData;
* (System.Object)Prompt;

### Нод IsURL

**Имя нода** = IsURL;

**Описание** = Validates a given URL;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.String)URL;

### Нод GetRemoteFile

**Имя нода** = GetRemoteFile;

**Описание** = Downloads the file specified by a URL;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.String)URL;
* (System.String)LocalFile;
* (System.Object)IgnoreCache;

### Нод PutRemoteFile

**Имя нода** = PutRemoteFile;

**Описание** = Uploads a file to a remote location specified by a URL;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.String)URL;
* (System.String)LocalFile;

### Нод IsRemoteFile

**Имя нода** = IsRemoteFile;

**Описание** = Returns the URL that a remote file was downloaded from;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.String)LocalFile;
* (System.Object)URL;

### Нод LaunchBrowserDialog

**Имя нода** = LaunchBrowserDialog;

**Описание** = Launches the Web Browser dialog that allows the user to navigate to any URL and select a URL;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.String)SelectedURL;
* (System.String)DialogTitle;
* (System.String)OpenButtonCaption;
* (System.String)StartPageURL;
* (System.String)RegistryRootKey;
* (System.Object)OpenButtonAlwaysEnabled;

### Нод CreateSafeArrayFromVector

**Имя нода** = CreateSafeArrayFromVector;

**Описание** = Creates a safearray from variant;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.Object)varVec;

### Нод CreateTypedArrayFromJSArray

**Имя нода** = CreateTypedArrayFromJSArray;

**Описание** = Creates a safearray from variant;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.Int32)Type;
* (System.Object)varJSArray;

### Нод AnalyzeEnt

**Имя нода** = AnalyzeEnt;

**Описание** = Analyze an ent;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.Object)ent;

### Нод GetPolyline

**Имя нода** = GetPolyline;

**Описание** = Analyze an ent;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.Object)Prompt;
* (System.Object)secondPrompt;
* (System.Object)closed;

### Нод Matrix4x4

**Имя нода** = Matrix4x4;

**Описание** = Create Matrix - safe array;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.String)str;

### Нод TranslateString

**Имя нода** = TranslateString;

**Описание** = Translates string;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.String)inStr;

### Нод ChooseFile

**Имя нода** = ChooseFile;

**Описание** = Displays ChooseFile dialog;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.String)title;
* (System.String)format;
* (System.Object)flags;
* (System.String)folder;

### Нод ChooseFolder

**Имя нода** = ChooseFolder;

**Описание** = Displays ChooseFolder dialog;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.String)title;
* (System.String)inifolder;

### Нод ShellExec

**Имя нода** = ShellExec;

**Описание** = ShellExec;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.String)op;
* (System.String)file;
* (System.String)param;
* (System.String)dir;
* (System.Object)flags;

### Нод NewDatabase

**Имя нода** = NewDatabase;

**Описание** = Create new empty database;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;

### Нод OpenDatabase

**Имя нода** = OpenDatabase;

**Описание** = Open database;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.String)Name;

### Нод SaveDatabase

**Имя нода** = SaveDatabase;

**Описание** = Save database;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.String)FullFileName;
* (System.Object)SaveAsType;
* (System.Object)vSecurityParams;

### Нод ShowMenu

**Имя нода** = ShowMenu;

**Описание** = Show menu;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.Object)menu;
* (System.Object)x;
* (System.Object)y;

### Нод GetWinNum

**Имя нода** = GetWinNum;

**Описание** = hostGetWinNum;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.Object)Point;

### Нод CoordFromPixelToWorld

**Имя нода** = CoordFromPixelToWorld;

**Описание** = hostCoordFromPixelToWorld;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.Object)num;
* (System.Object)Point;

### Нод CoordFromWorldToPixel

**Имя нода** = CoordFromWorldToPixel;

**Описание** = hostCoordFromWorldToPixel;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.Object)num;
* (System.Object)Point;

### Нод GetObjectIdString

**Имя нода** = GetObjectIdString;

**Описание** = Converts ObjectId to string;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADUtility;

**Входные данные**:

* (System.Object)nanoCADUtility;
* (System.Object)Object;
* (System.Object)asHex;

## Группа NVP_nanoCAD_COM.nanoCAD.nanoCADState

### Нод _nanoCADState_Constructor

**Имя нода** = _nanoCADState_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADState;

**Входные данные**:

* (System.Object)dynamic;

### Нод _nanoCADState_ConstructorCast

**Имя нода** = _nanoCADState_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADState;

**Входные данные**:

* (System.Object)dynamic;

### Нод IsQuiescent

**Имя нода** = IsQuiescent;

**Описание** = Checks whether App is idle and ready to accept automation calls from out-of-process applications;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADState;

**Входные данные**:

* (System.Object)nanoCADState;

### Нод Application

**Имя нода** = Application;

**Описание** = Gets the Application object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADState;

**Входные данные**:

* (System.Object)nanoCADState;

## Группа NVP_nanoCAD_COM.nanoCAD.nanoCADServices

### Нод _nanoCADServices_Constructor

**Имя нода** = _nanoCADServices_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADServices;

**Входные данные**:

* (System.Object)dynamic;

### Нод _nanoCADServices_ConstructorCast

**Имя нода** = _nanoCADServices_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADServices;

**Входные данные**:

* (System.Object)dynamic;

### Нод Item

**Имя нода** = Item;

**Описание** = Gets the member object at a given index in a collection, group, or selection set;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADServices;

**Входные данные**:

* (System.Object)nanoCADServices;
* (System.Object)Index;

### Нод Count

**Имя нода** = Count;

**Описание** = Gets the number of items in the collection, dictionary, group, or selection set;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADServices;

**Входные данные**:

* (System.Object)nanoCADServices;

### Нод Add

**Имя нода** = Add;

**Описание** = Adds new service to the appropriate collection;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADServices;

**Входные данные**:

* (System.Object)nanoCADServices;
* (System.String)Name;
* (System.Object)service;

### Нод Remove

**Имя нода** = Remove;

**Описание** = Removes services from collection;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADServices;

**Входные данные**:

* (System.Object)nanoCADServices;
* (System.String)Name;

### Нод RemoveAll

**Имя нода** = RemoveAll;

**Описание** = Removes all services from collection;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADServices;

**Входные данные**:

* (System.Object)nanoCADServices;

### Нод Application

**Имя нода** = Application;

**Описание** = Gets the Application object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADServices;

**Входные данные**:

* (System.Object)nanoCADServices;

## Группа NVP_nanoCAD_COM.nanoCAD.nanoCADSelectionSets

### Нод _nanoCADSelectionSets_Constructor

**Имя нода** = _nanoCADSelectionSets_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADSelectionSets;

**Входные данные**:

* (System.Object)dynamic;

### Нод _nanoCADSelectionSets_ConstructorCast

**Имя нода** = _nanoCADSelectionSets_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADSelectionSets;

**Входные данные**:

* (System.Object)dynamic;

### Нод Item

**Имя нода** = Item;

**Описание** = Gets the member object at a given index in a collection, group, or selection set;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADSelectionSets;

**Входные данные**:

* (System.Object)nanoCADSelectionSets;
* (System.Object)Index;

### Нод Count

**Имя нода** = Count;

**Описание** = Gets the number of items in the collection, dictionary, group, or selection set;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADSelectionSets;

**Входные данные**:

* (System.Object)nanoCADSelectionSets;

### Нод Add

**Имя нода** = Add;

**Описание** = Creates a member object and adds it to the appropriate collection;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADSelectionSets;

**Входные данные**:

* (System.Object)nanoCADSelectionSets;
* (System.String)Name;

### Нод Application

**Имя нода** = Application;

**Описание** = Gets the Application object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADSelectionSets;

**Входные данные**:

* (System.Object)nanoCADSelectionSets;

### Нод GetAll_SelectionSets

**Имя нода** = GetAll_SelectionSets;

**Описание** = Возвращает все Наборы выбора в виде nanoCAD.nanoCADSelectionSet;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADSelectionSets;

**Входные данные**:

* (System.Object)nanoCADSelectionSets;

## Группа NVP_nanoCAD_COM.nanoCAD.nanoCADSelectionSet

### Нод _nanoCADSelectionSet_Constructor

**Имя нода** = _nanoCADSelectionSet_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADSelectionSet;

**Входные данные**:

* (System.Object)dynamic;

### Нод _nanoCADSelectionSet_ConstructorCast

**Имя нода** = _nanoCADSelectionSet_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADSelectionSet;

**Входные данные**:

* (System.Object)dynamic;

### Нод Item

**Имя нода** = Item;

**Описание** = Gets the member object at a given index in a collection, group, or selection set;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADSelectionSet;

**Входные данные**:

* (System.Object)nanoCADSelectionSet;
* (System.Object)Index;

### Нод Count

**Имя нода** = Count;

**Описание** = Gets the number of items in the collection, dictionary, group, or selection set;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADSelectionSet;

**Входные данные**:

* (System.Object)nanoCADSelectionSet;

### Нод Name

**Имя нода** = Name;

**Описание** = Specifies the name of the object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADSelectionSet;

**Входные данные**:

* (System.Object)nanoCADSelectionSet;

### Нод Highlight

**Имя нода** = Highlight;

**Описание** = Sets the highlight status for the given object, or for all objects in a given selection set;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADSelectionSet;

**Входные данные**:

* (System.Object)nanoCADSelectionSet;
* (System.Object)bFlag;

### Нод Erase

**Имя нода** = Erase;

**Описание** = Erases all the objects in a selection set;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADSelectionSet;

**Входные данные**:

* (System.Object)nanoCADSelectionSet;

### Нод Update

**Имя нода** = Update;

**Описание** = Updates the object to the drawing screen;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADSelectionSet;

**Входные данные**:

* (System.Object)nanoCADSelectionSet;

### Нод Application

**Имя нода** = Application;

**Описание** = Gets the Application object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADSelectionSet;

**Входные данные**:

* (System.Object)nanoCADSelectionSet;

### Нод AddItems

**Имя нода** = AddItems;

**Описание** = Adds one or more objects to the specified selection set;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADSelectionSet;

**Входные данные**:

* (System.Object)nanoCADSelectionSet;
* (System.Object)pSelSet;

### Нод RemoveItems

**Имя нода** = RemoveItems;

**Описание** = Removes specified items from the group or selection set;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADSelectionSet;

**Входные данные**:

* (System.Object)nanoCADSelectionSet;
* (System.Object)Objects;

### Нод Clear

**Имя нода** = Clear;

**Описание** = Clears the specified selection set of all items;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADSelectionSet;

**Входные данные**:

* (System.Object)nanoCADSelectionSet;

### Нод Select

**Имя нода** = Select;

**Описание** = Selects objects and places them into a selection set;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADSelectionSet;

**Входные данные**:

* (System.Object)nanoCADSelectionSet;
* (System.Object)nanoCAD.AcSelect;
* (System.Object)Point1;
* (System.Object)Point2;
* (System.Object)FilterType;
* (System.Object)FilterData;

### Нод SelectAtPoint

**Имя нода** = SelectAtPoint;

**Описание** = Selects an object passing through a given point and places it into a selection set;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADSelectionSet;

**Входные данные**:

* (System.Object)nanoCADSelectionSet;
* (System.Object)Point;
* (System.Object)FilterType;
* (System.Object)FilterData;

### Нод SelectByPolygon

**Имя нода** = SelectByPolygon;

**Описание** = Selects entities within a fence and adds them to the selection set;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADSelectionSet;

**Входные данные**:

* (System.Object)nanoCADSelectionSet;
* (System.Object)nanoCAD.AcSelect;
* (System.Object)PointsList;
* (System.Object)FilterType;
* (System.Object)FilterData;

### Нод SelectOnScreen

**Имя нода** = SelectOnScreen;

**Описание** = Prompts the user to pick an object from the screen;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADSelectionSet;

**Входные данные**:

* (System.Object)nanoCADSelectionSet;
* (System.Object)FilterType;
* (System.Object)FilterData;

### Нод Delete

**Имя нода** = Delete;

**Описание** = Deletes a specified object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADSelectionSet;

**Входные данные**:

* (System.Object)nanoCADSelectionSet;

### Нод GetBoundingBox

**Имя нода** = GetBoundingBox;

**Описание** = Calculates the bounding box of selection;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADSelectionSet;

**Входные данные**:

* (System.Object)nanoCADSelectionSet;
* (System.Object)MinPoint;
* (System.Object)MaxPoint;

## Группа NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesUser

### Нод _nanoCADPreferencesUser_Constructor

**Имя нода** = _nanoCADPreferencesUser_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesUser;

**Входные данные**:

* (System.Object)dynamic;

### Нод _nanoCADPreferencesUser_ConstructorCast

**Имя нода** = _nanoCADPreferencesUser_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesUser;

**Входные данные**:

* (System.Object)dynamic;

### Нод Application

**Имя нода** = Application;

**Описание** = Returns the Application object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesUser;

**Входные данные**:

* (System.Object)nanoCADPreferencesUser;

### Нод Set_KeyboardAccelerator

**Имя нода** = Set_KeyboardAccelerator;

**Описание** = Specifies or returns which keyboard configuration is used.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesUser;

**Входные данные**:

* (System.Object)nanoCADPreferencesUser;
* (System.Object)nanoCAD.AcKeyboardAccelerator;

### Нод KeyboardAccelerator

**Имя нода** = KeyboardAccelerator;

**Описание** = Specifies or returns which keyboard configuration is used.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesUser;

**Входные данные**:

* (System.Object)nanoCADPreferencesUser;

### Нод Set_KeyboardPriority

**Имя нода** = Set_KeyboardPriority;

**Описание** = Specifies or returns how to obtain coordinate input.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesUser;

**Входные данные**:

* (System.Object)nanoCADPreferencesUser;
* (System.Object)nanoCAD.AcKeyboardPriority;

### Нод KeyboardPriority

**Имя нода** = KeyboardPriority;

**Описание** = Specifies or returns how to obtain coordinate input.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesUser;

**Входные данные**:

* (System.Object)nanoCADPreferencesUser;

### Нод Set_HyperlinkDisplayCursor

**Имя нода** = Set_HyperlinkDisplayCursor;

**Описание** = Specifies or returns wherther a hyperlink cursor displays when the pointer is over an object that has an assigned hyperlink.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesUser;

**Входные данные**:

* (System.Object)nanoCADPreferencesUser;
* (System.Object)Path;

### Нод HyperlinkDisplayCursor

**Имя нода** = HyperlinkDisplayCursor;

**Описание** = Specifies or returns wherther a hyperlink cursor displays when the pointer is over an object that has an assigned hyperlink.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesUser;

**Входные данные**:

* (System.Object)nanoCADPreferencesUser;

### Нод Set_HyperlinkDisplayTooltip

**Имя нода** = Set_HyperlinkDisplayTooltip;

**Описание** = Specifies or returns whether hyperlink tooltips display when the pointer is over an object that contains a hyperlink.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesUser;

**Входные данные**:

* (System.Object)nanoCADPreferencesUser;
* (System.Object)Path;

### Нод HyperlinkDisplayTooltip

**Имя нода** = HyperlinkDisplayTooltip;

**Описание** = Specifies or returns whether hyperlink tooltips display when the pointer is over an object that contains a hyperlink.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesUser;

**Входные данные**:

* (System.Object)nanoCADPreferencesUser;

### Нод Set_ADCInsertUnitsDefaultSource

**Имя нода** = Set_ADCInsertUnitsDefaultSource;

**Описание** = Specifies or returns the units for objects that are from a drawing without insert units assigned.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesUser;

**Входные данные**:

* (System.Object)nanoCADPreferencesUser;
* (System.Object)pIU;

### Нод ADCInsertUnitsDefaultSource

**Имя нода** = ADCInsertUnitsDefaultSource;

**Описание** = Specifies or returns the units for objects that are from a drawing without insert units assigned.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesUser;

**Входные данные**:

* (System.Object)nanoCADPreferencesUser;

### Нод Set_ADCInsertUnitsDefaultTarget

**Имя нода** = Set_ADCInsertUnitsDefaultTarget;

**Описание** = Specifies or returns the units for objects that are meant for a drawing without insert units assigned.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesUser;

**Входные данные**:

* (System.Object)nanoCADPreferencesUser;
* (System.Object)pSUunits;

### Нод ADCInsertUnitsDefaultTarget

**Имя нода** = ADCInsertUnitsDefaultTarget;

**Описание** = Specifies or returns the units for objects that are meant for a drawing without insert units assigned.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesUser;

**Входные данные**:

* (System.Object)nanoCADPreferencesUser;

### Нод Set_ShortCutMenuDisplay

**Имя нода** = Set_ShortCutMenuDisplay;

**Описание** = Specifies or returns whether a shortcut menu displays when right-clicking in a drawing.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesUser;

**Входные данные**:

* (System.Object)nanoCADPreferencesUser;
* (System.Object)pSCM;

### Нод ShortCutMenuDisplay

**Имя нода** = ShortCutMenuDisplay;

**Описание** = Specifies or returns whether a shortcut menu displays when right-clicking in a drawing.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesUser;

**Входные данные**:

* (System.Object)nanoCADPreferencesUser;

### Нод Set_SCMDefaultMode

**Имя нода** = Set_SCMDefaultMode;

**Описание** = Specifies or returns the action to take if a right-click occurs when no objects are selected and no commands are active.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesUser;

**Входные данные**:

* (System.Object)nanoCADPreferencesUser;
* (System.Object)nanoCAD.AcDrawingAreaSCMDefault;

### Нод SCMDefaultMode

**Имя нода** = SCMDefaultMode;

**Описание** = Specifies or returns the action to take if a right-click occurs when no objects are selected and no commands are active.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesUser;

**Входные данные**:

* (System.Object)nanoCADPreferencesUser;

### Нод Set_SCMEditMode

**Имя нода** = Set_SCMEditMode;

**Описание** = Specifies or returns the action to take if a right-click occurs when objects are selected and no commands are active.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesUser;

**Входные данные**:

* (System.Object)nanoCADPreferencesUser;
* (System.Object)nanoCAD.AcDrawingAreaSCMEdit;

### Нод SCMEditMode

**Имя нода** = SCMEditMode;

**Описание** = Specifies or returns the action to take if a right-click occurs when objects are selected and no commands are active.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesUser;

**Входные данные**:

* (System.Object)nanoCADPreferencesUser;

### Нод Set_SCMCommandMode

**Имя нода** = Set_SCMCommandMode;

**Описание** = Specifies or returns the action to take if a right-click occurs when a command is active.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesUser;

**Входные данные**:

* (System.Object)nanoCADPreferencesUser;
* (System.Object)nanoCAD.AcDrawingAreaSCMCommand;

### Нод SCMCommandMode

**Имя нода** = SCMCommandMode;

**Описание** = Specifies or returns the action to take if a right-click occurs when a command is active.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesUser;

**Входные данные**:

* (System.Object)nanoCADPreferencesUser;

### Нод Set_SCMTimeMode

**Имя нода** = Set_SCMTimeMode;

**Описание** = Specifies or returns whether a short right-click initiates Enter and a long right-click initiates a shortcut menu.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesUser;

**Входные данные**:

* (System.Object)nanoCADPreferencesUser;
* (System.Object)time;

### Нод SCMTimeMode

**Имя нода** = SCMTimeMode;

**Описание** = Specifies or returns whether a short right-click initiates Enter and a long right-click initiates a shortcut menu.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesUser;

**Входные данные**:

* (System.Object)nanoCADPreferencesUser;

### Нод Set_SCMTimeValue

**Имя нода** = Set_SCMTimeValue;

**Описание** = Specifies or returns the time for SCMTimeMode.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesUser;

**Входные данные**:

* (System.Object)nanoCADPreferencesUser;
* (System.Int32)time;

### Нод SCMTimeValue

**Имя нода** = SCMTimeValue;

**Описание** = Specifies or returns the time for SCMTimeMode.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesUser;

**Входные данные**:

* (System.Object)nanoCADPreferencesUser;

## Группа NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSystem

### Нод _nanoCADPreferencesSystem_Constructor

**Имя нода** = _nanoCADPreferencesSystem_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSystem;

**Входные данные**:

* (System.Object)dynamic;

### Нод _nanoCADPreferencesSystem_ConstructorCast

**Имя нода** = _nanoCADPreferencesSystem_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSystem;

**Входные данные**:

* (System.Object)dynamic;

### Нод Application

**Имя нода** = Application;

**Описание** = Returns the Application object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSystem;

**Входные данные**:

* (System.Object)nanoCADPreferencesSystem;

### Нод Set_SingleDocumentMode

**Имя нода** = Set_SingleDocumentMode;

**Описание** = Specifies or returns whether single or multiple drawings can be open at the same time.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSystem;

**Входные данные**:

* (System.Object)nanoCADPreferencesSystem;
* (System.Object)Path;

### Нод SingleDocumentMode

**Имя нода** = SingleDocumentMode;

**Описание** = Specifies or returns whether single or multiple drawings can be open at the same time.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSystem;

**Входные данные**:

* (System.Object)nanoCADPreferencesSystem;

### Нод Set_DisplayOLEScale

**Имя нода** = Set_DisplayOLEScale;

**Описание** = Specifies or returns whether the user is prompted to scale OLE objects when adding OLE objects to a drawing.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSystem;

**Входные данные**:

* (System.Object)nanoCADPreferencesSystem;
* (System.Object)Path;

### Нод DisplayOLEScale

**Имя нода** = DisplayOLEScale;

**Описание** = Specifies or returns whether the user is prompted to scale OLE objects when adding OLE objects to a drawing.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSystem;

**Входные данные**:

* (System.Object)nanoCADPreferencesSystem;

### Нод Set_StoreSQLIndex

**Имя нода** = Set_StoreSQLIndex;

**Описание** = Specifies or returns whether the SQL index is stored in drawings.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSystem;

**Входные данные**:

* (System.Object)nanoCADPreferencesSystem;
* (System.Object)Path;

### Нод StoreSQLIndex

**Имя нода** = StoreSQLIndex;

**Описание** = Specifies or returns whether the SQL index is stored in drawings.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSystem;

**Входные данные**:

* (System.Object)nanoCADPreferencesSystem;

### Нод Set_TablesReadOnly

**Имя нода** = Set_TablesReadOnly;

**Описание** = Specifies or returns whether to database tables are read-only.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSystem;

**Входные данные**:

* (System.Object)nanoCADPreferencesSystem;
* (System.Object)Path;

### Нод TablesReadOnly

**Имя нода** = TablesReadOnly;

**Описание** = Specifies or returns whether to database tables are read-only.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSystem;

**Входные данные**:

* (System.Object)nanoCADPreferencesSystem;

### Нод Set_EnableStartupDialog

**Имя нода** = Set_EnableStartupDialog;

**Описание** = Specifies or returns whether a Startup dialog box displays automatically when the application starts.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSystem;

**Входные данные**:

* (System.Object)nanoCADPreferencesSystem;
* (System.Object)Path;

### Нод EnableStartupDialog

**Имя нода** = EnableStartupDialog;

**Описание** = Specifies or returns whether a Startup dialog box displays automatically when the application starts.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSystem;

**Входные данные**:

* (System.Object)nanoCADPreferencesSystem;

### Нод Set_BeepOnError

**Имя нода** = Set_BeepOnError;

**Описание** = Specifies or returns whether a beep sounds automatically when user input results in an error.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSystem;

**Входные данные**:

* (System.Object)nanoCADPreferencesSystem;
* (System.Object)BeepOnError;

### Нод BeepOnError

**Имя нода** = BeepOnError;

**Описание** = Specifies or returns whether a beep sounds automatically when user input results in an error.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSystem;

**Входные данные**:

* (System.Object)nanoCADPreferencesSystem;

### Нод Set_ShowWarningMessages

**Имя нода** = Set_ShowWarningMessages;

**Описание** = Specifies or returns whether all warning dialog boxes display.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSystem;

**Входные данные**:

* (System.Object)nanoCADPreferencesSystem;
* (System.Object)Path;

### Нод ShowWarningMessages

**Имя нода** = ShowWarningMessages;

**Описание** = Specifies or returns whether all warning dialog boxes display.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSystem;

**Входные данные**:

* (System.Object)nanoCADPreferencesSystem;

### Нод Set_LoadAcadLspInAllDocuments

**Имя нода** = Set_LoadAcadLspInAllDocuments;

**Описание** = Not implemented.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSystem;

**Входные данные**:

* (System.Object)nanoCADPreferencesSystem;
* (System.Object)pALID;

### Нод LoadAcadLspInAllDocuments

**Имя нода** = LoadAcadLspInAllDocuments;

**Описание** = Not implemented.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSystem;

**Входные данные**:

* (System.Object)nanoCADPreferencesSystem;

## Группа NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSelection

### Нод _nanoCADPreferencesSelection_Constructor

**Имя нода** = _nanoCADPreferencesSelection_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSelection;

**Входные данные**:

* (System.Object)dynamic;

### Нод _nanoCADPreferencesSelection_ConstructorCast

**Имя нода** = _nanoCADPreferencesSelection_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSelection;

**Входные данные**:

* (System.Object)dynamic;

### Нод Application

**Имя нода** = Application;

**Описание** = Returns the Application object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSelection;

**Входные данные**:

* (System.Object)nanoCADPreferencesSelection;

### Нод Set_PickFirst

**Имя нода** = Set_PickFirst;

**Описание** = Specifies or returns whether you select objects before or after calling a command.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSelection;

**Входные данные**:

* (System.Object)nanoCADPreferencesSelection;
* (System.Object)Path;

### Нод PickFirst

**Имя нода** = PickFirst;

**Описание** = Specifies or returns whether you select objects before or after calling a command.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSelection;

**Входные данные**:

* (System.Object)nanoCADPreferencesSelection;

### Нод Set_PickAdd

**Имя нода** = Set_PickAdd;

**Описание** = Specifies or returns whether objects can be added to a selection set using the Shift key.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSelection;

**Входные данные**:

* (System.Object)nanoCADPreferencesSelection;
* (System.Object)Path;

### Нод PickAdd

**Имя нода** = PickAdd;

**Описание** = Specifies or returns whether objects can be added to a selection set using the Shift key.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSelection;

**Входные данные**:

* (System.Object)nanoCADPreferencesSelection;

### Нод Set_PickDrag

**Имя нода** = Set_PickDrag;

**Описание** = Specifies or returns how a selection window is defined.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSelection;

**Входные данные**:

* (System.Object)nanoCADPreferencesSelection;
* (System.Object)Path;

### Нод PickDrag

**Имя нода** = PickDrag;

**Описание** = Specifies or returns how a selection window is defined.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSelection;

**Входные данные**:

* (System.Object)nanoCADPreferencesSelection;

### Нод Set_PickAuto

**Имя нода** = Set_PickAuto;

**Описание** = Specifies or returns use of the selection window when selecting objects.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSelection;

**Входные данные**:

* (System.Object)nanoCADPreferencesSelection;
* (System.Object)Path;

### Нод PickAuto

**Имя нода** = PickAuto;

**Описание** = Specifies or returns use of the selection window when selecting objects.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSelection;

**Входные данные**:

* (System.Object)nanoCADPreferencesSelection;

### Нод Set_PickBoxSize

**Имя нода** = Set_PickBoxSize;

**Описание** = Specifies or returns the target height of object selection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSelection;

**Входные данные**:

* (System.Object)nanoCADPreferencesSelection;
* (System.Object)Path;

### Нод PickBoxSize

**Имя нода** = PickBoxSize;

**Описание** = Specifies or returns the target height of object selection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSelection;

**Входные данные**:

* (System.Object)nanoCADPreferencesSelection;

### Нод Set_DisplayGrips

**Имя нода** = Set_DisplayGrips;

**Описание** = Specifies or returns whether selection set grips display.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSelection;

**Входные данные**:

* (System.Object)nanoCADPreferencesSelection;
* (System.Object)Path;

### Нод DisplayGrips

**Имя нода** = DisplayGrips;

**Описание** = Specifies or returns whether selection set grips display.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSelection;

**Входные данные**:

* (System.Object)nanoCADPreferencesSelection;

### Нод Set_DisplayGripsWithinBlocks

**Имя нода** = Set_DisplayGripsWithinBlocks;

**Описание** = Specifies or returns whether grips display for the objects in a block when the block is selected.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSelection;

**Входные данные**:

* (System.Object)nanoCADPreferencesSelection;
* (System.Object)Path;

### Нод DisplayGripsWithinBlocks

**Имя нода** = DisplayGripsWithinBlocks;

**Описание** = Specifies or returns whether grips display for the objects in a block when the block is selected.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSelection;

**Входные данные**:

* (System.Object)nanoCADPreferencesSelection;

### Нод Set_GripColorSelected

**Имя нода** = Set_GripColorSelected;

**Описание** = Specifies or returns the color of selected grips.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSelection;

**Входные данные**:

* (System.Object)nanoCADPreferencesSelection;
* (System.Object)Path;

### Нод GripColorSelected

**Имя нода** = GripColorSelected;

**Описание** = Specifies or returns the color of selected grips.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSelection;

**Входные данные**:

* (System.Object)nanoCADPreferencesSelection;

### Нод Set_GripColorUnselected

**Имя нода** = Set_GripColorUnselected;

**Описание** = Specifies or returns the color of unselected grips.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSelection;

**Входные данные**:

* (System.Object)nanoCADPreferencesSelection;
* (System.Object)Path;

### Нод GripColorUnselected

**Имя нода** = GripColorUnselected;

**Описание** = Specifies or returns the color of unselected grips.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSelection;

**Входные данные**:

* (System.Object)nanoCADPreferencesSelection;

### Нод Set_GripSize

**Имя нода** = Set_GripSize;

**Описание** = Specifies or returns the size of grips.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSelection;

**Входные данные**:

* (System.Object)nanoCADPreferencesSelection;
* (System.Object)Path;

### Нод GripSize

**Имя нода** = GripSize;

**Описание** = Specifies or returns the size of grips.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSelection;

**Входные данные**:

* (System.Object)nanoCADPreferencesSelection;

### Нод Set_PickGroup

**Имя нода** = Set_PickGroup;

**Описание** = Specifies or returns whether selecting an object in a group selects the whole group.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSelection;

**Входные данные**:

* (System.Object)nanoCADPreferencesSelection;
* (System.Object)pick;

### Нод PickGroup

**Имя нода** = PickGroup;

**Описание** = Specifies or returns whether selecting an object in a group selects the whole group.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesSelection;

**Входные данные**:

* (System.Object)nanoCADPreferencesSelection;

## Группа NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesProfiles

### Нод _nanoCADPreferencesProfiles_Constructor

**Имя нода** = _nanoCADPreferencesProfiles_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesProfiles;

**Входные данные**:

* (System.Object)dynamic;

### Нод _nanoCADPreferencesProfiles_ConstructorCast

**Имя нода** = _nanoCADPreferencesProfiles_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesProfiles;

**Входные данные**:

* (System.Object)dynamic;

### Нод Application

**Имя нода** = Application;

**Описание** = Gets the Application object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesProfiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesProfiles;

### Нод Set_ActiveProfile

**Имя нода** = Set_ActiveProfile;

**Описание** = Specifies or returns the current profile.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesProfiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesProfiles;
* (System.String)Path;

### Нод ActiveProfile

**Имя нода** = ActiveProfile;

**Описание** = Specifies or returns the current profile.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesProfiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesProfiles;

### Нод ImportProfile

**Имя нода** = ImportProfile;

**Описание** = Imports a profile.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesProfiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesProfiles;
* (System.String)ProfileName;
* (System.String)RegFile;
* (System.Object)IncludePathInfo;

### Нод ExportProfile

**Имя нода** = ExportProfile;

**Описание** = Exports the current profile.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesProfiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesProfiles;
* (System.String)ProfileName;
* (System.String)RegFile;

### Нод DeleteProfile

**Имя нода** = DeleteProfile;

**Описание** = Deletes a profile.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesProfiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesProfiles;
* (System.String)ProfileName;

### Нод ResetProfile

**Имя нода** = ResetProfile;

**Описание** = Resets profile values to their default values.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesProfiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesProfiles;
* (System.String)Profile;

### Нод RenameProfile

**Имя нода** = RenameProfile;

**Описание** = Renames a profile.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesProfiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesProfiles;
* (System.String)origProfileName;
* (System.String)newProfileName;

### Нод CopyProfile

**Имя нода** = CopyProfile;

**Описание** = Copies a profile.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesProfiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesProfiles;
* (System.String)oldProfileName;
* (System.String)newProfileName;

### Нод GetAllProfileNames

**Имя нода** = GetAllProfileNames;

**Описание** = Gets all available profiles for the system;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesProfiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesProfiles;
* (System.Object)pNames;

## Группа NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOutput

### Нод _nanoCADPreferencesOutput_Constructor

**Имя нода** = _nanoCADPreferencesOutput_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOutput;

**Входные данные**:

* (System.Object)dynamic;

### Нод _nanoCADPreferencesOutput_ConstructorCast

**Имя нода** = _nanoCADPreferencesOutput_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOutput;

**Входные данные**:

* (System.Object)dynamic;

### Нод Application

**Имя нода** = Application;

**Описание** = Returns the Application object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOutput;

**Входные данные**:

* (System.Object)nanoCADPreferencesOutput;

### Нод Set_DefaultOutputDevice

**Имя нода** = Set_DefaultOutputDevice;

**Описание** = Specifies or returns the default printer for new layouts and modelspace.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOutput;

**Входные данные**:

* (System.Object)nanoCADPreferencesOutput;
* (System.String)Path;

### Нод DefaultOutputDevice

**Имя нода** = DefaultOutputDevice;

**Описание** = Specifies or returns the default printer for new layouts and modelspace.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOutput;

**Входные данные**:

* (System.Object)nanoCADPreferencesOutput;

### Нод Set_PrinterSpoolAlert

**Имя нода** = Set_PrinterSpoolAlert;

**Описание** = Specifies or returns whether to display a warning when printing must be spooled.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOutput;

**Входные данные**:

* (System.Object)nanoCADPreferencesOutput;
* (System.Object)nanoCAD.AcPrinterSpoolAlert;

### Нод PrinterSpoolAlert

**Имя нода** = PrinterSpoolAlert;

**Описание** = Specifies or returns whether to display a warning when printing must be spooled.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOutput;

**Входные данные**:

* (System.Object)nanoCADPreferencesOutput;

### Нод Set_PrinterPaperSizeAlert

**Имя нода** = Set_PrinterPaperSizeAlert;

**Описание** = Specifies or returns whether to display a warning when there is a conflict with the specified paper size.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOutput;

**Входные данные**:

* (System.Object)nanoCADPreferencesOutput;
* (System.Object)Path;

### Нод PrinterPaperSizeAlert

**Имя нода** = PrinterPaperSizeAlert;

**Описание** = Specifies or returns whether to display a warning when there is a conflict with the specified paper size.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOutput;

**Входные данные**:

* (System.Object)nanoCADPreferencesOutput;

### Нод Set_PlotLegacy

**Имя нода** = Set_PlotLegacy;

**Описание** = Specifies or returns whether older print scripts can be used.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOutput;

**Входные данные**:

* (System.Object)nanoCADPreferencesOutput;
* (System.Object)Path;

### Нод PlotLegacy

**Имя нода** = PlotLegacy;

**Описание** = Specifies or returns whether older print scripts can be used.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOutput;

**Входные данные**:

* (System.Object)nanoCADPreferencesOutput;

### Нод Set_OLEQuality

**Имя нода** = Set_OLEQuality;

**Описание** = Specifies or returns the print quality of OLE objects.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOutput;

**Входные данные**:

* (System.Object)nanoCADPreferencesOutput;
* (System.Object)nanoCAD.AcOleQuality;

### Нод OLEQuality

**Имя нода** = OLEQuality;

**Описание** = Specifies or returns the print quality of OLE objects.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOutput;

**Входные данные**:

* (System.Object)nanoCADPreferencesOutput;

### Нод Set_UseLastPlotSettings

**Имя нода** = Set_UseLastPlotSettings;

**Описание** = Specifies or returns whether the print settings are used from the previous successful print job.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOutput;

**Входные данные**:

* (System.Object)nanoCADPreferencesOutput;
* (System.Object)Path;

### Нод UseLastPlotSettings

**Имя нода** = UseLastPlotSettings;

**Описание** = Specifies or returns whether the print settings are used from the previous successful print job.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOutput;

**Входные данные**:

* (System.Object)nanoCADPreferencesOutput;

### Нод Set_PlotPolicy

**Имя нода** = Set_PlotPolicy;

**Описание** = Specifies or returns whether color-dependent plot style tables are used for new drawings.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOutput;

**Входные данные**:

* (System.Object)nanoCADPreferencesOutput;
* (System.Object)nanoCAD.AcPlotPolicy;

### Нод PlotPolicy

**Имя нода** = PlotPolicy;

**Описание** = Specifies or returns whether color-dependent plot style tables are used for new drawings.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOutput;

**Входные данные**:

* (System.Object)nanoCADPreferencesOutput;

### Нод Set_DefaultPlotStyleTable

**Имя нода** = Set_DefaultPlotStyleTable;

**Описание** = Specifies or returns the default plot style table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOutput;

**Входные данные**:

* (System.Object)nanoCADPreferencesOutput;
* (System.String)Path;

### Нод DefaultPlotStyleTable

**Имя нода** = DefaultPlotStyleTable;

**Описание** = Specifies or returns the default plot style table.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOutput;

**Входные данные**:

* (System.Object)nanoCADPreferencesOutput;

### Нод Set_DefaultPlotStyleForObjects

**Имя нода** = Set_DefaultPlotStyleForObjects;

**Описание** = Specifies or returns the default print style table for new drawings.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOutput;

**Входные данные**:

* (System.Object)nanoCADPreferencesOutput;
* (System.String)Path;

### Нод DefaultPlotStyleForObjects

**Имя нода** = DefaultPlotStyleForObjects;

**Описание** = Specifies or returns the default print style table for new drawings.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOutput;

**Входные данные**:

* (System.Object)nanoCADPreferencesOutput;

### Нод Set_DefaultPlotStyleForLayer

**Имя нода** = Set_DefaultPlotStyleForLayer;

**Описание** = Specifies or returns the default print style for new drawings.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOutput;

**Входные данные**:

* (System.Object)nanoCADPreferencesOutput;
* (System.String)Path;

### Нод DefaultPlotStyleForLayer

**Имя нода** = DefaultPlotStyleForLayer;

**Описание** = Specifies or returns the default print style for new drawings.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOutput;

**Входные данные**:

* (System.Object)nanoCADPreferencesOutput;

## Группа NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOpenSave

### Нод _nanoCADPreferencesOpenSave_Constructor

**Имя нода** = _nanoCADPreferencesOpenSave_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOpenSave;

**Входные данные**:

* (System.Object)dynamic;

### Нод _nanoCADPreferencesOpenSave_ConstructorCast

**Имя нода** = _nanoCADPreferencesOpenSave_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOpenSave;

**Входные данные**:

* (System.Object)dynamic;

### Нод Application

**Имя нода** = Application;

**Описание** = Gets the Application object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOpenSave;

**Входные данные**:

* (System.Object)nanoCADPreferencesOpenSave;

### Нод Set_SavePreviewThumbnail

**Имя нода** = Set_SavePreviewThumbnail;

**Описание** = Specifies or returns whether thumbnail preview images are saved with drawings.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOpenSave;

**Входные данные**:

* (System.Object)nanoCADPreferencesOpenSave;
* (System.Object)Path;

### Нод SavePreviewThumbnail

**Имя нода** = SavePreviewThumbnail;

**Описание** = Specifies or returns whether thumbnail preview images are saved with drawings.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOpenSave;

**Входные данные**:

* (System.Object)nanoCADPreferencesOpenSave;

### Нод Set_IncrementalSavePercent

**Имя нода** = Set_IncrementalSavePercent;

**Описание** = Specifies or returns when a full save is performed instead of an incremental save.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOpenSave;

**Входные данные**:

* (System.Object)nanoCADPreferencesOpenSave;
* (System.Int32)Path;

### Нод IncrementalSavePercent

**Имя нода** = IncrementalSavePercent;

**Описание** = Specifies or returns when a full save is performed instead of an incremental save.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOpenSave;

**Входные данные**:

* (System.Object)nanoCADPreferencesOpenSave;

### Нод Set_AutoSaveInterval

**Имя нода** = Set_AutoSaveInterval;

**Описание** = Specifies or returns how often drawings are saved automatically.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOpenSave;

**Входные данные**:

* (System.Object)nanoCADPreferencesOpenSave;
* (System.Int32)Path;

### Нод AutoSaveInterval

**Имя нода** = AutoSaveInterval;

**Описание** = Specifies or returns how often drawings are saved automatically.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOpenSave;

**Входные данные**:

* (System.Object)nanoCADPreferencesOpenSave;

### Нод Set_CreateBackup

**Имя нода** = Set_CreateBackup;

**Описание** = Specifies or returns whether backup files are saved.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOpenSave;

**Входные данные**:

* (System.Object)nanoCADPreferencesOpenSave;
* (System.Object)CreateBackup;

### Нод CreateBackup

**Имя нода** = CreateBackup;

**Описание** = Specifies or returns whether backup files are saved.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOpenSave;

**Входные данные**:

* (System.Object)nanoCADPreferencesOpenSave;

### Нод Set_FullCRCValidation

**Имя нода** = Set_FullCRCValidation;

**Описание** = Specifies or returns whether checking is turned on when hardware or software errors are suspected.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOpenSave;

**Входные данные**:

* (System.Object)nanoCADPreferencesOpenSave;
* (System.Object)Path;

### Нод FullCRCValidation

**Имя нода** = FullCRCValidation;

**Описание** = Specifies or returns whether checking is turned on when hardware or software errors are suspected.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOpenSave;

**Входные данные**:

* (System.Object)nanoCADPreferencesOpenSave;

### Нод Set_LogFileOn

**Имя нода** = Set_LogFileOn;

**Описание** = Specifies or returns whether a log file is saved to capture text window contents.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOpenSave;

**Входные данные**:

* (System.Object)nanoCADPreferencesOpenSave;
* (System.Object)Path;

### Нод LogFileOn

**Имя нода** = LogFileOn;

**Описание** = Specifies or returns whether a log file is saved to capture text window contents.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOpenSave;

**Входные данные**:

* (System.Object)nanoCADPreferencesOpenSave;

### Нод Set_TempFileExtension

**Имя нода** = Set_TempFileExtension;

**Описание** = Specifies or returns the file extension used for temporary files.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOpenSave;

**Входные данные**:

* (System.Object)nanoCADPreferencesOpenSave;
* (System.String)Path;

### Нод TempFileExtension

**Имя нода** = TempFileExtension;

**Описание** = Specifies or returns the file extension used for temporary files.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOpenSave;

**Входные данные**:

* (System.Object)nanoCADPreferencesOpenSave;

### Нод Set_XrefDemandLoad

**Имя нода** = Set_XrefDemandLoad;

**Описание** = Specifies or returns how external references are loaded.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOpenSave;

**Входные данные**:

* (System.Object)nanoCADPreferencesOpenSave;
* (System.Object)nanoCAD.AcXRefDemandLoad;

### Нод XrefDemandLoad

**Имя нода** = XrefDemandLoad;

**Описание** = Specifies or returns how external references are loaded.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOpenSave;

**Входные данные**:

* (System.Object)nanoCADPreferencesOpenSave;

### Нод Set_DemandLoadARXApp

**Имя нода** = Set_DemandLoadARXApp;

**Описание** = Specifies or returns how third-party applications are loaded if a drawing contains custom objects created in that application.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOpenSave;

**Входные данные**:

* (System.Object)nanoCADPreferencesOpenSave;
* (System.Object)nanoCAD.AcARXDemandLoad;

### Нод DemandLoadARXApp

**Имя нода** = DemandLoadARXApp;

**Описание** = Specifies or returns how third-party applications are loaded if a drawing contains custom objects created in that application.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOpenSave;

**Входные данные**:

* (System.Object)nanoCADPreferencesOpenSave;

### Нод Set_ProxyImage

**Имя нода** = Set_ProxyImage;

**Описание** = Specifies or returns how objects that were created in a third-party application display in a drawing.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOpenSave;

**Входные данные**:

* (System.Object)nanoCADPreferencesOpenSave;
* (System.Object)nanoCAD.AcProxyImage;

### Нод ProxyImage

**Имя нода** = ProxyImage;

**Описание** = Specifies or returns how objects that were created in a third-party application display in a drawing.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOpenSave;

**Входные данные**:

* (System.Object)nanoCADPreferencesOpenSave;

### Нод Set_ShowProxyDialogBox

**Имя нода** = Set_ShowProxyDialogBox;

**Описание** = Specifies or returns whether a warning displays when opening a drawing that contains custom objects.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOpenSave;

**Входные данные**:

* (System.Object)nanoCADPreferencesOpenSave;
* (System.Object)Path;

### Нод ShowProxyDialogBox

**Имя нода** = ShowProxyDialogBox;

**Описание** = Specifies or returns whether a warning displays when opening a drawing that contains custom objects.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOpenSave;

**Входные данные**:

* (System.Object)nanoCADPreferencesOpenSave;

### Нод Set_AutoAudit

**Имя нода** = Set_AutoAudit;

**Описание** = Specifies or returns whether an audit is performed after you render a DXFIN or DXBIN interchange command.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOpenSave;

**Входные данные**:

* (System.Object)nanoCADPreferencesOpenSave;
* (System.Object)bAudit;

### Нод AutoAudit

**Имя нода** = AutoAudit;

**Описание** = Specifies or returns whether an audit is performed after you render a DXFIN or DXBIN interchange command.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOpenSave;

**Входные данные**:

* (System.Object)nanoCADPreferencesOpenSave;

### Нод Set_SaveAsType

**Имя нода** = Set_SaveAsType;

**Описание** = Specifies or returns the default drawing type used for saving drawings.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOpenSave;

**Входные данные**:

* (System.Object)nanoCADPreferencesOpenSave;
* (System.Object)nanoCAD.AcSaveAsType;

### Нод SaveAsType

**Имя нода** = SaveAsType;

**Описание** = Specifies or returns the default drawing type used for saving drawings.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOpenSave;

**Входные данные**:

* (System.Object)nanoCADPreferencesOpenSave;

### Нод MRUNumber

**Имя нода** = MRUNumber;

**Описание** = Specifies the number of recent files to track on the File menu.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesOpenSave;

**Входные данные**:

* (System.Object)nanoCADPreferencesOpenSave;

## Группа NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles

### Нод _nanoCADPreferencesFiles_Constructor

**Имя нода** = _nanoCADPreferencesFiles_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)dynamic;

### Нод _nanoCADPreferencesFiles_ConstructorCast

**Имя нода** = _nanoCADPreferencesFiles_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)dynamic;

### Нод Application

**Имя нода** = Application;

**Описание** = Returns the Application object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;

### Нод Set_SupportPath

**Имя нода** = Set_SupportPath;

**Описание** = Specifies or returns the paths where the application searches for extra support files.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;
* (System.String)orient;

### Нод SupportPath

**Имя нода** = SupportPath;

**Описание** = Specifies or returns the paths where the application searches for extra support files.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;

### Нод Set_DriversPath

**Имя нода** = Set_DriversPath;

**Описание** = Specifies or returns the paths where the application searches for drivers.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;
* (System.String)Path;

### Нод DriversPath

**Имя нода** = DriversPath;

**Описание** = Specifies or returns the paths where the application searches for drivers.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;

### Нод Set_MenuFile

**Имя нода** = Set_MenuFile;

**Описание** = Specifies or returns the paths where the application searches for menu files.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;
* (System.String)Path;

### Нод MenuFile

**Имя нода** = MenuFile;

**Описание** = Specifies or returns the paths where the application searches for menu files.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;

### Нод Set_HelpFilePath

**Имя нода** = Set_HelpFilePath;

**Описание** = Specifies or returns the paths where the application searches for help files.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;
* (System.String)Path;

### Нод HelpFilePath

**Имя нода** = HelpFilePath;

**Описание** = Specifies or returns the paths where the application searches for help files.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;

### Нод Set_DefaultInternetURL

**Имя нода** = Set_DefaultInternetURL;

**Описание** = Specifies or returns the paths where the application searches for the default Internet address.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;
* (System.String)Path;

### Нод DefaultInternetURL

**Имя нода** = DefaultInternetURL;

**Описание** = Specifies or returns the paths where the application searches for the default Internet address.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;

### Нод ConfigFile

**Имя нода** = ConfigFile;

**Описание** = Returns the path of the configuration file.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;

### Нод LicenseServer

**Имя нода** = LicenseServer;

**Описание** = Returns a list of servers available for use by the network license manager.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;

### Нод Set_TextEditor

**Имя нода** = Set_TextEditor;

**Описание** = Specifies or returns the path and name of the text editor used for multiline text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;
* (System.String)Path;

### Нод TextEditor

**Имя нода** = TextEditor;

**Описание** = Specifies or returns the path and name of the text editor used for multiline text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;

### Нод Set_MainDictionary

**Имя нода** = Set_MainDictionary;

**Описание** = Specifies or returns the path and name of the dictionary to use with spell checking.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;
* (System.String)Path;

### Нод MainDictionary

**Имя нода** = MainDictionary;

**Описание** = Specifies or returns the path and name of the dictionary to use with spell checking.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;

### Нод Set_CustomDictionary

**Имя нода** = Set_CustomDictionary;

**Описание** = Specifies or returns the path and name of the custom dictionary to use when checking the spelling of text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;
* (System.String)Path;

### Нод CustomDictionary

**Имя нода** = CustomDictionary;

**Описание** = Specifies or returns the path and name of the custom dictionary to use when checking the spelling of text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;

### Нод Set_AltFontFile

**Имя нода** = Set_AltFontFile;

**Описание** = Specifies or returns the path and name of an alternate font file.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;
* (System.String)fontFile;

### Нод AltFontFile

**Имя нода** = AltFontFile;

**Описание** = Specifies or returns the path and name of an alternate font file.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;

### Нод Set_FontFileMap

**Имя нода** = Set_FontFileMap;

**Описание** = Specifies or returns the path and name of a font map file.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;
* (System.String)Path;

### Нод FontFileMap

**Имя нода** = FontFileMap;

**Описание** = Specifies or returns the path and name of a font map file.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;

### Нод Set_PrintFile

**Имя нода** = Set_PrintFile;

**Описание** = Specifies or returns the temporary print file name that is used when printing drawings.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;
* (System.String)Path;

### Нод PrintFile

**Имя нода** = PrintFile;

**Описание** = Specifies or returns the temporary print file name that is used when printing drawings.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;

### Нод Set_PrintSpoolExecutable

**Имя нода** = Set_PrintSpoolExecutable;

**Описание** = Specifies or returns the path and name of the application used for print spooling.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;
* (System.String)Path;

### Нод PrintSpoolExecutable

**Имя нода** = PrintSpoolExecutable;

**Описание** = Specifies or returns the path and name of the application used for print spooling.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;

### Нод Set_PostScriptPrologFile

**Имя нода** = Set_PostScriptPrologFile;

**Описание** = Specifies or returns the name of the prolog section used when printing to a PostScript file.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;
* (System.String)Path;

### Нод PostScriptPrologFile

**Имя нода** = PostScriptPrologFile;

**Описание** = Specifies or returns the name of the prolog section used when printing to a PostScript file.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;

### Нод Set_PrintSpoolerPath

**Имя нода** = Set_PrintSpoolerPath;

**Описание** = Specifies or returns the path used for print spool output when printing drawings.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;
* (System.String)Path;

### Нод PrintSpoolerPath

**Имя нода** = PrintSpoolerPath;

**Описание** = Specifies or returns the path used for print spool output when printing drawings.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;

### Нод Set_AutoSavePath

**Имя нода** = Set_AutoSavePath;

**Описание** = Specifies or returns the path for the file saved when saving a drawing automatically.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;
* (System.String)AutoSavePath;

### Нод AutoSavePath

**Имя нода** = AutoSavePath;

**Описание** = Specifies or returns the path for the file saved when saving a drawing automatically.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;

### Нод Set_TemplateDwgPath

**Имя нода** = Set_TemplateDwgPath;

**Описание** = Specifies or returns the paths where the application searches for template files when using a new drawing wizard.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;
* (System.String)Path;

### Нод TemplateDwgPath

**Имя нода** = TemplateDwgPath;

**Описание** = Specifies or returns the paths where the application searches for template files when using a new drawing wizard.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;

### Нод Set_LogFilePath

**Имя нода** = Set_LogFilePath;

**Описание** = Specifies or returns the path and name of the log file.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;
* (System.String)Path;

### Нод LogFilePath

**Имя нода** = LogFilePath;

**Описание** = Specifies or returns the path and name of the log file.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;

### Нод Set_TempFilePath

**Имя нода** = Set_TempFilePath;

**Описание** = Specifies or returns the path where the application stores temporary files.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;
* (System.String)Path;

### Нод TempFilePath

**Имя нода** = TempFilePath;

**Описание** = Specifies or returns the path where the application stores temporary files.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;

### Нод Set_TempXrefPath

**Имя нода** = Set_TempXrefPath;

**Описание** = Specifies or returns the path where the application stores external reference files that are loaded on demand.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;
* (System.String)Path;

### Нод TempXrefPath

**Имя нода** = TempXrefPath;

**Описание** = Specifies or returns the path where the application stores external reference files that are loaded on demand.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;

### Нод Set_TextureMapPath

**Имя нода** = Set_TextureMapPath;

**Описание** = Specifies or returns the path for texture maps used for rendering.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;
* (System.String)Path;

### Нод TextureMapPath

**Имя нода** = TextureMapPath;

**Описание** = Specifies or returns the path for texture maps used for rendering.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;

### Нод Set_AltTabletMenuFile

**Имя нода** = Set_AltTabletMenuFile;

**Описание** = Specifies or returns the path for an alternate menu to replace the tablet menu.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;
* (System.String)MenuFile;

### Нод AltTabletMenuFile

**Имя нода** = AltTabletMenuFile;

**Описание** = Specifies or returns the path for an alternate menu to replace the tablet menu.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;

### Нод SetProjectFilePath

**Имя нода** = SetProjectFilePath;

**Описание** = Specifies or returns the path where the application searches for external reference files.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;
* (System.String)ProjectName;
* (System.String)ProjectFilePath;

### Нод GetProjectFilePath

**Имя нода** = GetProjectFilePath;

**Описание** = Specifies or returns the path where the application searches for external reference files.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;
* (System.String)ProjectName;

### Нод Set_PrinterConfigPath

**Имя нода** = Set_PrinterConfigPath;

**Описание** = Specifies or returns the path for configuration files used for printing.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;
* (System.String)Path;

### Нод PrinterConfigPath

**Имя нода** = PrinterConfigPath;

**Описание** = Specifies or returns the path for configuration files used for printing.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;

### Нод Set_PrinterDescPath

**Имя нода** = Set_PrinterDescPath;

**Описание** = Specifies or returns the path for description files used for printing.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;
* (System.String)Path;

### Нод PrinterDescPath

**Имя нода** = PrinterDescPath;

**Описание** = Specifies or returns the path for description files used for printing.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;

### Нод Set_PrinterStyleSheetPath

**Имя нода** = Set_PrinterStyleSheetPath;

**Описание** = Specifies or returns the path for style sheets used for printing.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;
* (System.String)Path;

### Нод PrinterStyleSheetPath

**Имя нода** = PrinterStyleSheetPath;

**Описание** = Specifies or returns the path for style sheets used for printing.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;

### Нод Set_WorkspacePath

**Имя нода** = Set_WorkspacePath;

**Описание** = Specifies or returns the path for workspace files.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;
* (System.String)Path;

### Нод WorkspacePath

**Имя нода** = WorkspacePath;

**Описание** = Specifies or returns the path for workspace files.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;

### Нод Set_ObjectARXPath

**Имя нода** = Set_ObjectARXPath;

**Описание** = Specifies or returns the path for TX applications.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;
* (System.String)Path;

### Нод ObjectARXPath

**Имя нода** = ObjectARXPath;

**Описание** = Specifies or returns the path for TX applications.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;

### Нод Set_ColorBookPath

**Имя нода** = Set_ColorBookPath;

**Описание** = Specifies or returns the path for color books.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;
* (System.String)Path;

### Нод ColorBookPath

**Имя нода** = ColorBookPath;

**Описание** = Specifies or returns the path for color books.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;

### Нод Set_ToolPalettePath

**Имя нода** = Set_ToolPalettePath;

**Описание** = Specifies or returns the path for tool palettes.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;
* (System.String)Path;

### Нод ToolPalettePath

**Имя нода** = ToolPalettePath;

**Описание** = Specifies or returns the path for tool palettes.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesFiles;

**Входные данные**:

* (System.Object)nanoCADPreferencesFiles;

## Группа NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDrafting

### Нод _nanoCADPreferencesDrafting_Constructor

**Имя нода** = _nanoCADPreferencesDrafting_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDrafting;

**Входные данные**:

* (System.Object)dynamic;

### Нод _nanoCADPreferencesDrafting_ConstructorCast

**Имя нода** = _nanoCADPreferencesDrafting_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDrafting;

**Входные данные**:

* (System.Object)dynamic;

### Нод Application

**Имя нода** = Application;

**Описание** = Returns the Application object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDrafting;

**Входные данные**:

* (System.Object)nanoCADPreferencesDrafting;

### Нод Set_AutoSnapMarker

**Имя нода** = Set_AutoSnapMarker;

**Описание** = Specifies or returns whether the snap marker displays for snap locations.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDrafting;

**Входные данные**:

* (System.Object)nanoCADPreferencesDrafting;
* (System.Object)Path;

### Нод AutoSnapMarker

**Имя нода** = AutoSnapMarker;

**Описание** = Specifies or returns whether the snap marker displays for snap locations.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDrafting;

**Входные данные**:

* (System.Object)nanoCADPreferencesDrafting;

### Нод Set_AutoSnapMagnet

**Имя нода** = Set_AutoSnapMagnet;

**Описание** = Specifies or returns whether the snap magnet is turned on.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDrafting;

**Входные данные**:

* (System.Object)nanoCADPreferencesDrafting;
* (System.Object)Path;

### Нод AutoSnapMagnet

**Имя нода** = AutoSnapMagnet;

**Описание** = Specifies or returns whether the snap magnet is turned on.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDrafting;

**Входные данные**:

* (System.Object)nanoCADPreferencesDrafting;

### Нод Set_AutoSnapTooltip

**Имя нода** = Set_AutoSnapTooltip;

**Описание** = Specifies or returns whether snap tooltips are turned on.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDrafting;

**Входные данные**:

* (System.Object)nanoCADPreferencesDrafting;
* (System.Object)Path;

### Нод AutoSnapTooltip

**Имя нода** = AutoSnapTooltip;

**Описание** = Specifies or returns whether snap tooltips are turned on.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDrafting;

**Входные данные**:

* (System.Object)nanoCADPreferencesDrafting;

### Нод Set_AutoSnapAperture

**Имя нода** = Set_AutoSnapAperture;

**Описание** = Specifies or returns whether the aperture cursor displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDrafting;

**Входные данные**:

* (System.Object)nanoCADPreferencesDrafting;
* (System.Object)Path;

### Нод AutoSnapAperture

**Имя нода** = AutoSnapAperture;

**Описание** = Specifies or returns whether the aperture cursor displays.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDrafting;

**Входные данные**:

* (System.Object)nanoCADPreferencesDrafting;

### Нод Set_AutoSnapApertureSize

**Имя нода** = Set_AutoSnapApertureSize;

**Описание** = Specifies or returns the aperture cursor size.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDrafting;

**Входные данные**:

* (System.Object)nanoCADPreferencesDrafting;
* (System.Object)Path;

### Нод AutoSnapApertureSize

**Имя нода** = AutoSnapApertureSize;

**Описание** = Specifies or returns the aperture cursor size.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDrafting;

**Входные данные**:

* (System.Object)nanoCADPreferencesDrafting;

### Нод Set_AutoSnapMarkerColor

**Имя нода** = Set_AutoSnapMarkerColor;

**Описание** = Specifies or returns the snap marker color.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDrafting;

**Входные данные**:

* (System.Object)nanoCADPreferencesDrafting;
* (System.Object)Path;

### Нод AutoSnapMarkerColor

**Имя нода** = AutoSnapMarkerColor;

**Описание** = Specifies or returns the snap marker color.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDrafting;

**Входные данные**:

* (System.Object)nanoCADPreferencesDrafting;

### Нод Set_AutoSnapMarkerSize

**Имя нода** = Set_AutoSnapMarkerSize;

**Описание** = Specifies or returns the snap marker size in pixels.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDrafting;

**Входные данные**:

* (System.Object)nanoCADPreferencesDrafting;
* (System.Object)Path;

### Нод AutoSnapMarkerSize

**Имя нода** = AutoSnapMarkerSize;

**Описание** = Specifies or returns the snap marker size in pixels.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDrafting;

**Входные данные**:

* (System.Object)nanoCADPreferencesDrafting;

### Нод Set_PolarTrackingVector

**Имя нода** = Set_PolarTrackingVector;

**Описание** = Specifies or returns whether polar tracking is turned on.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDrafting;

**Входные данные**:

* (System.Object)nanoCADPreferencesDrafting;
* (System.Object)Path;

### Нод PolarTrackingVector

**Имя нода** = PolarTrackingVector;

**Описание** = Specifies or returns whether polar tracking is turned on.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDrafting;

**Входные данные**:

* (System.Object)nanoCADPreferencesDrafting;

### Нод Set_FullScreenTrackingVector

**Имя нода** = Set_FullScreenTrackingVector;

**Описание** = Specifies or returns whether full-screen tracking is turned on.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDrafting;

**Входные данные**:

* (System.Object)nanoCADPreferencesDrafting;
* (System.Object)Path;

### Нод FullScreenTrackingVector

**Имя нода** = FullScreenTrackingVector;

**Описание** = Specifies or returns whether full-screen tracking is turned on.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDrafting;

**Входные данные**:

* (System.Object)nanoCADPreferencesDrafting;

### Нод Set_AutoTrackTooltip

**Имя нода** = Set_AutoTrackTooltip;

**Описание** = Specifies or returns whether AutoTrack tooltips are turned on.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDrafting;

**Входные данные**:

* (System.Object)nanoCADPreferencesDrafting;
* (System.Object)Path;

### Нод AutoTrackTooltip

**Имя нода** = AutoTrackTooltip;

**Описание** = Specifies or returns whether AutoTrack tooltips are turned on.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDrafting;

**Входные данные**:

* (System.Object)nanoCADPreferencesDrafting;

### Нод Set_AlignmentPointAcquisition

**Имя нода** = Set_AlignmentPointAcquisition;

**Описание** = Specifies or returns how AutoAlignment points are obtained.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDrafting;

**Входные данные**:

* (System.Object)nanoCADPreferencesDrafting;
* (System.Object)nanoCAD.AcAlignmentPointAcquisition;

### Нод AlignmentPointAcquisition

**Имя нода** = AlignmentPointAcquisition;

**Описание** = Specifies or returns how AutoAlignment points are obtained.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDrafting;

**Входные данные**:

* (System.Object)nanoCADPreferencesDrafting;

## Группа NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay

### Нод _nanoCADPreferencesDisplay_Constructor

**Имя нода** = _nanoCADPreferencesDisplay_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)dynamic;

### Нод _nanoCADPreferencesDisplay_ConstructorCast

**Имя нода** = _nanoCADPreferencesDisplay_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)dynamic;

### Нод Application

**Имя нода** = Application;

**Описание** = Returns the Application object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;

### Нод Set_LayoutDisplayMargins

**Имя нода** = Set_LayoutDisplayMargins;

**Описание** = Specifies or returns whether margins display in layouts.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;
* (System.Object)Path;

### Нод LayoutDisplayMargins

**Имя нода** = LayoutDisplayMargins;

**Описание** = Specifies or returns whether margins display in layouts.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;

### Нод Set_LayoutDisplayPaper

**Имя нода** = Set_LayoutDisplayPaper;

**Описание** = Specifies or returns whether a paper background displays in layouts.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;
* (System.Object)Path;

### Нод LayoutDisplayPaper

**Имя нода** = LayoutDisplayPaper;

**Описание** = Specifies or returns whether a paper background displays in layouts.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;

### Нод Set_LayoutDisplayPaperShadow

**Имя нода** = Set_LayoutDisplayPaperShadow;

**Описание** = Specifies or returns whether a paper background shadow displays in layouts.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;
* (System.Object)Path;

### Нод LayoutDisplayPaperShadow

**Имя нода** = LayoutDisplayPaperShadow;

**Описание** = Specifies or returns whether a paper background shadow displays in layouts.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;

### Нод Set_LayoutShowPlotSetup

**Имя нода** = Set_LayoutShowPlotSetup;

**Описание** = Specifies or returns whether the application prompts for print settings whenever a new layout is created.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;
* (System.Object)Path;

### Нод LayoutShowPlotSetup

**Имя нода** = LayoutShowPlotSetup;

**Описание** = Specifies or returns whether the application prompts for print settings whenever a new layout is created.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;

### Нод Set_LayoutCreateViewport

**Имя нода** = Set_LayoutCreateViewport;

**Описание** = Specifies or returns whether a viewport is created automatically for each new layout.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;
* (System.Object)Path;

### Нод LayoutCreateViewport

**Имя нода** = LayoutCreateViewport;

**Описание** = Specifies or returns whether a viewport is created automatically for each new layout.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;

### Нод Set_DisplayScrollBars

**Имя нода** = Set_DisplayScrollBars;

**Описание** = Specifies or returns whether scroll bars display in the drawing window.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;
* (System.Object)Path;

### Нод DisplayScrollBars

**Имя нода** = DisplayScrollBars;

**Описание** = Specifies or returns whether scroll bars display in the drawing window.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;

### Нод Set_DisplayScreenMenu

**Имя нода** = Set_DisplayScreenMenu;

**Описание** = Specifies or returns whether a screen menu displays in the drawing window.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;
* (System.Object)Path;

### Нод DisplayScreenMenu

**Имя нода** = DisplayScreenMenu;

**Описание** = Specifies or returns whether a screen menu displays in the drawing window.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;

### Нод Set_CursorSize

**Имя нода** = Set_CursorSize;

**Описание** = Specifies or returns the size of the crosshair cursor as a percentage of the total screen size.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;
* (System.Int32)Path;

### Нод CursorSize

**Имя нода** = CursorSize;

**Описание** = Specifies or returns the size of the crosshair cursor as a percentage of the total screen size.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;

### Нод Set_DockedVisibleLines

**Имя нода** = Set_DockedVisibleLines;

**Описание** = Specifies or returns the number of text lines to display in the command window.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;
* (System.Int32)Path;

### Нод DockedVisibleLines

**Имя нода** = DockedVisibleLines;

**Описание** = Specifies or returns the number of text lines to display in the command window.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;

### Нод Set_ShowRasterImage

**Имя нода** = Set_ShowRasterImage;

**Описание** = Specifies or returns whether raster images display when using real-time zoom and pan.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;
* (System.Object)Path;

### Нод ShowRasterImage

**Имя нода** = ShowRasterImage;

**Описание** = Specifies or returns whether raster images display when using real-time zoom and pan.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;

### Нод Set_GraphicsWinModelBackgrndColor

**Имя нода** = Set_GraphicsWinModelBackgrndColor;

**Описание** = Specifies or returns the background color of the drawing window when in modelspace.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;
* (System.Object)color;

### Нод GraphicsWinModelBackgrndColor

**Имя нода** = GraphicsWinModelBackgrndColor;

**Описание** = Specifies or returns the background color of the drawing window when in modelspace.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;

### Нод Set_ModelCrosshairColor

**Имя нода** = Set_ModelCrosshairColor;

**Описание** = Specifies or returns the color of the crosshair cursor when in modelspace.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;
* (System.Object)crossHairColor;

### Нод ModelCrosshairColor

**Имя нода** = ModelCrosshairColor;

**Описание** = Specifies or returns the color of the crosshair cursor when in modelspace.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;

### Нод Set_GraphicsWinLayoutBackgrndColor

**Имя нода** = Set_GraphicsWinLayoutBackgrndColor;

**Описание** = Specifies or returns the background color when in paperspace.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;
* (System.Object)color;

### Нод GraphicsWinLayoutBackgrndColor

**Имя нода** = GraphicsWinLayoutBackgrndColor;

**Описание** = Specifies or returns the background color when in paperspace.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;

### Нод Set_TextWinBackgrndColor

**Имя нода** = Set_TextWinBackgrndColor;

**Описание** = Specifies or returns the background color of the text window.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;
* (System.Object)Path;

### Нод TextWinBackgrndColor

**Имя нода** = TextWinBackgrndColor;

**Описание** = Specifies or returns the background color of the text window.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;

### Нод Set_TextWinTextColor

**Имя нода** = Set_TextWinTextColor;

**Описание** = Specifies or returns the color of text that displays in the text window.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;
* (System.Object)Path;

### Нод TextWinTextColor

**Имя нода** = TextWinTextColor;

**Описание** = Specifies or returns the color of text that displays in the text window.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;

### Нод Set_LayoutCrosshairColor

**Имя нода** = Set_LayoutCrosshairColor;

**Описание** = Specifies or returns the color of the crosshair cursor when in paperspace.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;
* (System.Object)crossHairColor;

### Нод LayoutCrosshairColor

**Имя нода** = LayoutCrosshairColor;

**Описание** = Specifies or returns the color of the crosshair cursor when in paperspace.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;

### Нод Set_AutoTrackingVecColor

**Имя нода** = Set_AutoTrackingVecColor;

**Описание** = Specifies or returns the color of the auto-tracking vector.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;
* (System.Object)AutoTrackingVecColor;

### Нод AutoTrackingVecColor

**Имя нода** = AutoTrackingVecColor;

**Описание** = Specifies or returns the color of the auto-tracking vector.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;

### Нод Set_TextFont

**Имя нода** = Set_TextFont;

**Описание** = Specifies or returns the current font assigned to new text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;
* (System.String)Path;

### Нод TextFont

**Имя нода** = TextFont;

**Описание** = Specifies or returns the current font assigned to new text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;

### Нод Set_TextFontStyle

**Имя нода** = Set_TextFontStyle;

**Описание** = Specifies or returns the current font style assigned to new text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;
* (System.Object)nanoCAD.AcTextFontStyle;

### Нод TextFontStyle

**Имя нода** = TextFontStyle;

**Описание** = Specifies or returns the current font style assigned to new text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;

### Нод Set_TextFontSize

**Имя нода** = Set_TextFontSize;

**Описание** = Specifies or returns the current font size assigned to new text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;
* (System.Int32)Path;

### Нод TextFontSize

**Имя нода** = TextFontSize;

**Описание** = Specifies or returns the current font size assigned to new text.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;

### Нод Set_HistoryLines

**Имя нода** = Set_HistoryLines;

**Описание** = Specifies or returns the number of text lines to keep in memory from the text window.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;
* (System.Int32)Path;

### Нод HistoryLines

**Имя нода** = HistoryLines;

**Описание** = Specifies or returns the number of text lines to keep in memory from the text window.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;

### Нод Set_MaxWindow

**Имя нода** = Set_MaxWindow;

**Описание** = Specifies or returns whether the application displays in the whole screen area when the application starts.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;
* (System.Object)Path;

### Нод MaxWindow

**Имя нода** = MaxWindow;

**Описание** = Specifies or returns whether the application displays in the whole screen area when the application starts.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;

### Нод Set_DisplayLayoutTabs

**Имя нода** = Set_DisplayLayoutTabs;

**Описание** = Specifies or returns whether Model and Layout tabs display in the drawing window.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;
* (System.Object)Path;

### Нод DisplayLayoutTabs

**Имя нода** = DisplayLayoutTabs;

**Описание** = Specifies or returns whether Model and Layout tabs display in the drawing window.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;

### Нод Set_ImageFrameHighlight

**Имя нода** = Set_ImageFrameHighlight;

**Описание** = Specifies or returns whether raster images or their frames display when selecting entities.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;
* (System.Object)Path;

### Нод ImageFrameHighlight

**Имя нода** = ImageFrameHighlight;

**Описание** = Specifies or returns whether raster images or their frames display when selecting entities.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;

### Нод Set_TrueColorImages

**Имя нода** = Set_TrueColorImages;

**Описание** = Specifies or returns whether raster and render images display with the highest level of color capability.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;
* (System.Object)Path;

### Нод TrueColorImages

**Имя нода** = TrueColorImages;

**Описание** = Specifies or returns whether raster and render images display with the highest level of color capability.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;

### Нод Set_XRefFadeIntensity

**Имя нода** = Set_XRefFadeIntensity;

**Описание** = Specifies the fade percentage level used for external reference files.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;
* (System.Object)Path;

### Нод XRefFadeIntensity

**Имя нода** = XRefFadeIntensity;

**Описание** = Specifies the fade percentage level used for external reference files.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferencesDisplay;

**Входные данные**:

* (System.Object)nanoCADPreferencesDisplay;

## Группа NVP_nanoCAD_COM.nanoCAD.nanoCADPreferences

### Нод _nanoCADPreferences_Constructor

**Имя нода** = _nanoCADPreferences_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferences;

**Входные данные**:

* (System.Object)dynamic;

### Нод _nanoCADPreferences_ConstructorCast

**Имя нода** = _nanoCADPreferences_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferences;

**Входные данные**:

* (System.Object)dynamic;

### Нод Application

**Имя нода** = Application;

**Описание** = Returns the Application object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferences;

**Входные данные**:

* (System.Object)nanoCADPreferences;

### Нод Files

**Имя нода** = Files;

**Описание** = Returns the PreferencesFiles object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferences;

**Входные данные**:

* (System.Object)nanoCADPreferences;

### Нод Display

**Имя нода** = Display;

**Описание** = Returns the PreferencesDisplay object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferences;

**Входные данные**:

* (System.Object)nanoCADPreferences;

### Нод OpenSave

**Имя нода** = OpenSave;

**Описание** = Returns the PreferencesOpenSave object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferences;

**Входные данные**:

* (System.Object)nanoCADPreferences;

### Нод Output

**Имя нода** = Output;

**Описание** = Returns the PreferencesOutput object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferences;

**Входные данные**:

* (System.Object)nanoCADPreferences;

### Нод System

**Имя нода** = System;

**Описание** = Returns the PreferencesSystem object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferences;

**Входные данные**:

* (System.Object)nanoCADPreferences;

### Нод User

**Имя нода** = User;

**Описание** = Returns the PreferencesUser object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferences;

**Входные данные**:

* (System.Object)nanoCADPreferences;

### Нод Drafting

**Имя нода** = Drafting;

**Описание** = Returns the PreferencesDrafting object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferences;

**Входные данные**:

* (System.Object)nanoCADPreferences;

### Нод Selection

**Имя нода** = Selection;

**Описание** = Returns the PreferencesSelection object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferences;

**Входные данные**:

* (System.Object)nanoCADPreferences;

### Нод Profiles

**Имя нода** = Profiles;

**Описание** = Returns the PreferencesProfiles object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPreferences;

**Входные данные**:

* (System.Object)nanoCADPreferences;

## Группа NVP_nanoCAD_COM.nanoCAD.nanoCADPlotCustomParams

### Нод _nanoCADPlotCustomParams_Constructor

**Имя нода** = _nanoCADPlotCustomParams_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotCustomParams;

**Входные данные**:

* (System.Object)dynamic;

### Нод _nanoCADPlotCustomParams_ConstructorCast

**Имя нода** = _nanoCADPlotCustomParams_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotCustomParams;

**Входные данные**:

* (System.Object)dynamic;

### Нод Params

**Имя нода** = Params;

**Описание** = value;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotCustomParams;

**Входные данные**:

* (System.Object)nanoCADPlotCustomParams;

### Нод Set_Params

**Имя нода** = Set_Params;

**Описание** = value;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotCustomParams;

**Входные данные**:

* (System.Object)nanoCADPlotCustomParams;
* (System.Object)param;

### Нод Put_Params

**Имя нода** = Put_Params;

**Описание** = value;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotCustomParams;

**Входные данные**:

* (System.Object)nanoCADPlotCustomParams;
* (System.Object)param;

### Нод ColorMode

**Имя нода** = ColorMode;

**Описание** = Retrieves color mode value;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotCustomParams;

**Входные данные**:

* (System.Object)nanoCADPlotCustomParams;

### Нод Set_ColorMode

**Имя нода** = Set_ColorMode;

**Описание** = Retrieves color mode value;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotCustomParams;

**Входные данные**:

* (System.Object)nanoCADPlotCustomParams;
* (System.Object)val;

### Нод PrinterDPI

**Имя нода** = PrinterDPI;

**Описание** = Retrieves PrinterDPI value;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotCustomParams;

**Входные данные**:

* (System.Object)nanoCADPlotCustomParams;

### Нод Set_PrinterDPI

**Имя нода** = Set_PrinterDPI;

**Описание** = Retrieves PrinterDPI value;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotCustomParams;

**Входные данные**:

* (System.Object)nanoCADPlotCustomParams;
* (System.Object)val;

### Нод UsePrefinedNames

**Имя нода** = UsePrefinedNames;

**Описание** = Retrieves UsePrefinedNames value;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotCustomParams;

**Входные данные**:

* (System.Object)nanoCADPlotCustomParams;

### Нод Set_UsePrefinedNames

**Имя нода** = Set_UsePrefinedNames;

**Описание** = Retrieves UsePrefinedNames value;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotCustomParams;

**Входные данные**:

* (System.Object)nanoCADPlotCustomParams;
* (System.Object)val;

### Нод IfFileExist

**Имя нода** = IfFileExist;

**Описание** = Retrieves IfFileExist value;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotCustomParams;

**Входные данные**:

* (System.Object)nanoCADPlotCustomParams;

### Нод Set_IfFileExist

**Имя нода** = Set_IfFileExist;

**Описание** = Retrieves IfFileExist value;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotCustomParams;

**Входные данные**:

* (System.Object)nanoCADPlotCustomParams;
* (System.Object)nanoCAD.FileExistAction;

### Нод RunPDFApp

**Имя нода** = RunPDFApp;

**Описание** = Retrieves RunPDFApp value;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotCustomParams;

**Входные данные**:

* (System.Object)nanoCADPlotCustomParams;

### Нод Set_RunPDFApp

**Имя нода** = Set_RunPDFApp;

**Описание** = Retrieves RunPDFApp value;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotCustomParams;

**Входные данные**:

* (System.Object)nanoCADPlotCustomParams;
* (System.Object)val;

### Нод UseDWGPath

**Имя нода** = UseDWGPath;

**Описание** = Retrieves UseDWGPath value;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotCustomParams;

**Входные данные**:

* (System.Object)nanoCADPlotCustomParams;

### Нод Set_UseDWGPath

**Имя нода** = Set_UseDWGPath;

**Описание** = Retrieves UseDWGPath value;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotCustomParams;

**Входные данные**:

* (System.Object)nanoCADPlotCustomParams;
* (System.Object)val;

### Нод FileFolder

**Имя нода** = FileFolder;

**Описание** = Retrieves FileFolder value;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotCustomParams;

**Входные данные**:

* (System.Object)nanoCADPlotCustomParams;

### Нод Set_FileFolder

**Имя нода** = Set_FileFolder;

**Описание** = Retrieves FileFolder value;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotCustomParams;

**Входные данные**:

* (System.Object)nanoCADPlotCustomParams;
* (System.String)val;

### Нод FileMask

**Имя нода** = FileMask;

**Описание** = Retrieves FileMask value;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotCustomParams;

**Входные данные**:

* (System.Object)nanoCADPlotCustomParams;

### Нод Set_FileMask

**Имя нода** = Set_FileMask;

**Описание** = Retrieves FileMask value;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotCustomParams;

**Входные данные**:

* (System.Object)nanoCADPlotCustomParams;
* (System.String)val;

### Нод PlotToFile

**Имя нода** = PlotToFile;

**Описание** = Retrieves PlotToFile value;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotCustomParams;

**Входные данные**:

* (System.Object)nanoCADPlotCustomParams;

### Нод Set_PlotToFile

**Имя нода** = Set_PlotToFile;

**Описание** = Retrieves PlotToFile value;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotCustomParams;

**Входные данные**:

* (System.Object)nanoCADPlotCustomParams;
* (System.Object)val;

### Нод Multipage

**Имя нода** = Multipage;

**Описание** = Retrieves Multipage value;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotCustomParams;

**Входные данные**:

* (System.Object)nanoCADPlotCustomParams;

### Нод Set_Multipage

**Имя нода** = Set_Multipage;

**Описание** = Retrieves Multipage value;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotCustomParams;

**Входные данные**:

* (System.Object)nanoCADPlotCustomParams;
* (System.Object)val;

### Нод InflateWidth

**Имя нода** = InflateWidth;

**Описание** = Retrieves InflateWidth value;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotCustomParams;

**Входные данные**:

* (System.Object)nanoCADPlotCustomParams;

### Нод Set_InflateWidth

**Имя нода** = Set_InflateWidth;

**Описание** = Retrieves InflateWidth value;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotCustomParams;

**Входные данные**:

* (System.Object)nanoCADPlotCustomParams;
* (System.Double)val;

### Нод Alignment

**Имя нода** = Alignment;

**Описание** = Retrieves Alignment value;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotCustomParams;

**Входные данные**:

* (System.Object)nanoCADPlotCustomParams;

### Нод Set_Alignment

**Имя нода** = Set_Alignment;

**Описание** = Retrieves Alignment value;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotCustomParams;

**Входные данные**:

* (System.Object)nanoCADPlotCustomParams;
* (System.Object)val;

### Нод PaperOutMargins

**Имя нода** = PaperOutMargins;

**Описание** = Retrieves PaperOutMargins value;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotCustomParams;

**Входные данные**:

* (System.Object)nanoCADPlotCustomParams;

### Нод Set_PaperOutMargins

**Имя нода** = Set_PaperOutMargins;

**Описание** = Retrieves PaperOutMargins value;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotCustomParams;

**Входные данные**:

* (System.Object)nanoCADPlotCustomParams;
* (System.Object)val;

### Нод PlotAreas

**Имя нода** = PlotAreas;

**Описание** = Retrieves Plot Areas;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotCustomParams;

**Входные данные**:

* (System.Object)nanoCADPlotCustomParams;

## Группа NVP_nanoCAD_COM.nanoCAD.nanoCADPlotAreas

### Нод _nanoCADPlotAreas_Constructor

**Имя нода** = _nanoCADPlotAreas_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotAreas;

**Входные данные**:

* (System.Object)dynamic;

### Нод _nanoCADPlotAreas_ConstructorCast

**Имя нода** = _nanoCADPlotAreas_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotAreas;

**Входные данные**:

* (System.Object)dynamic;

### Нод Item

**Имя нода** = Item;

**Описание** = Returns plot area by index;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotAreas;

**Входные данные**:

* (System.Object)nanoCADPlotAreas;
* (System.Object)idx;

### Нод Count

**Имя нода** = Count;

**Описание** = Returns count of plot areas;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotAreas;

**Входные данные**:

* (System.Object)nanoCADPlotAreas;

### Нод Add

**Имя нода** = Add;

**Описание** = Create new plot area;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotAreas;

**Входные данные**:

* (System.Object)nanoCADPlotAreas;

### Нод Clear

**Имя нода** = Clear;

**Описание** = clears all plot areas;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotAreas;

**Входные данные**:

* (System.Object)nanoCADPlotAreas;

## Группа NVP_nanoCAD_COM.nanoCAD.nanoCADPlotArea2

### Нод _nanoCADPlotArea2_Constructor

**Имя нода** = _nanoCADPlotArea2_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotArea2;

**Входные данные**:

* (System.Object)dynamic;

### Нод _nanoCADPlotArea2_ConstructorCast

**Имя нода** = _nanoCADPlotArea2_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotArea2;

**Входные данные**:

* (System.Object)dynamic;

### Нод Position

**Имя нода** = Position;

**Описание** = Retrieves position val;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotArea2;

**Входные данные**:

* (System.Object)nanoCADPlotArea2;

### Нод Set_Position

**Имя нода** = Set_Position;

**Описание** = Retrieves position val;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotArea2;

**Входные данные**:

* (System.Object)nanoCADPlotArea2;
* (System.Object)val;

### Нод Target

**Имя нода** = Target;

**Описание** = Retrieves target val;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotArea2;

**Входные данные**:

* (System.Object)nanoCADPlotArea2;

### Нод Set_Target

**Имя нода** = Set_Target;

**Описание** = Retrieves target val;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotArea2;

**Входные данные**:

* (System.Object)nanoCADPlotArea2;
* (System.Object)val;

### Нод UpVector

**Имя нода** = UpVector;

**Описание** = Retrieves upVector val;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotArea2;

**Входные данные**:

* (System.Object)nanoCADPlotArea2;

### Нод Set_UpVector

**Имя нода** = Set_UpVector;

**Описание** = Retrieves upVector val;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotArea2;

**Входные данные**:

* (System.Object)nanoCADPlotArea2;
* (System.Object)val;

### Нод Field

**Имя нода** = Field;

**Описание** = Retrieves field val;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotArea2;

**Входные данные**:

* (System.Object)nanoCADPlotArea2;

### Нод Set_Field

**Имя нода** = Set_Field;

**Описание** = Retrieves field val;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotArea2;

**Входные данные**:

* (System.Object)nanoCADPlotArea2;
* (System.Object)val;

### Нод Perspective

**Имя нода** = Perspective;

**Описание** = Retrieves Perspective val;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotArea2;

**Входные данные**:

* (System.Object)nanoCADPlotArea2;

### Нод Set_Perspective

**Имя нода** = Set_Perspective;

**Описание** = Retrieves Perspective val;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotArea2;

**Входные данные**:

* (System.Object)nanoCADPlotArea2;
* (System.Object)val;

### Нод LensLength

**Имя нода** = LensLength;

**Описание** = Retrieves lensLength val;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotArea2;

**Входные данные**:

* (System.Object)nanoCADPlotArea2;

### Нод Set_LensLength

**Имя нода** = Set_LensLength;

**Описание** = Retrieves lensLength val;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotArea2;

**Входные данные**:

* (System.Object)nanoCADPlotArea2;
* (System.Double)val;

### Нод Elevation

**Имя нода** = Elevation;

**Описание** = Retrieves elevation val;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotArea2;

**Входные данные**:

* (System.Object)nanoCADPlotArea2;

### Нод Set_Elevation

**Имя нода** = Set_Elevation;

**Описание** = Retrieves elevation val;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotArea2;

**Входные данные**:

* (System.Object)nanoCADPlotArea2;
* (System.Double)val;

### Нод ViewOffset

**Имя нода** = ViewOffset;

**Описание** = Retrieves viewOffset val;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotArea2;

**Входные данные**:

* (System.Object)nanoCADPlotArea2;

### Нод Set_ViewOffset

**Имя нода** = Set_ViewOffset;

**Описание** = Retrieves viewOffset val;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotArea2;

**Входные данные**:

* (System.Object)nanoCADPlotArea2;
* (System.Object)val;

### Нод ptWindow1

**Имя нода** = ptWindow1;

**Описание** = Retrieves ptWindow1 val;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotArea2;

**Входные данные**:

* (System.Object)nanoCADPlotArea2;

### Нод Set_ptWindow1

**Имя нода** = Set_ptWindow1;

**Описание** = Retrieves ptWindow1 val;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotArea2;

**Входные данные**:

* (System.Object)nanoCADPlotArea2;
* (System.Object)val;

### Нод ptWindow2

**Имя нода** = ptWindow2;

**Описание** = Retrieves ptWindow2 val;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotArea2;

**Входные данные**:

* (System.Object)nanoCADPlotArea2;

### Нод Set_ptWindow2

**Имя нода** = Set_ptWindow2;

**Описание** = Retrieves ptWindow2 val;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotArea2;

**Входные данные**:

* (System.Object)nanoCADPlotArea2;
* (System.Object)val;

## Группа NVP_nanoCAD_COM.nanoCAD.nanoCADPlotArea

### Нод _nanoCADPlotArea_Constructor

**Имя нода** = _nanoCADPlotArea_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotArea;

**Входные данные**:

* (System.Object)dynamic;

### Нод _nanoCADPlotArea_ConstructorCast

**Имя нода** = _nanoCADPlotArea_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotArea;

**Входные данные**:

* (System.Object)dynamic;

### Нод Offset

**Имя нода** = Offset;

**Описание** = Retrieves offset val;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotArea;

**Входные данные**:

* (System.Object)nanoCADPlotArea;

### Нод Set_Offset

**Имя нода** = Set_Offset;

**Описание** = Retrieves offset val;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotArea;

**Входные данные**:

* (System.Object)nanoCADPlotArea;
* (System.Object)val;

### Нод ptMin

**Имя нода** = ptMin;

**Описание** = Retrieves ptMin val;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotArea;

**Входные данные**:

* (System.Object)nanoCADPlotArea;

### Нод Set_ptMin

**Имя нода** = Set_ptMin;

**Описание** = Retrieves ptMin val;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotArea;

**Входные данные**:

* (System.Object)nanoCADPlotArea;
* (System.Object)val;

### Нод ptMax

**Имя нода** = ptMax;

**Описание** = Retrieves ptMax val;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotArea;

**Входные данные**:

* (System.Object)nanoCADPlotArea;

### Нод Set_ptMax

**Имя нода** = Set_ptMax;

**Описание** = Retrieves ptMax val;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlotArea;

**Входные данные**:

* (System.Object)nanoCADPlotArea;
* (System.Object)val;

## Группа NVP_nanoCAD_COM.nanoCAD.nanoCADPlot

### Нод _nanoCADPlot_Constructor

**Имя нода** = _nanoCADPlot_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlot;

**Входные данные**:

* (System.Object)dynamic;

### Нод _nanoCADPlot_ConstructorCast

**Имя нода** = _nanoCADPlot_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlot;

**Входные данные**:

* (System.Object)dynamic;

### Нод Application

**Имя нода** = Application;

**Описание** = Gets the Application object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlot;

**Входные данные**:

* (System.Object)nanoCADPlot;

### Нод QuietErrorMode

**Имя нода** = QuietErrorMode;

**Описание** = Toggles the quiet error mode for plot error reporting;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlot;

**Входные данные**:

* (System.Object)nanoCADPlot;

### Нод Set_QuietErrorMode

**Имя нода** = Set_QuietErrorMode;

**Описание** = Toggles the quiet error mode for plot error reporting;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlot;

**Входные данные**:

* (System.Object)nanoCADPlot;
* (System.Object)bErrorMode;

### Нод NumberOfCopies

**Имя нода** = NumberOfCopies;

**Описание** = Specifies the number of copies to plot;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlot;

**Входные данные**:

* (System.Object)nanoCADPlot;

### Нод Set_NumberOfCopies

**Имя нода** = Set_NumberOfCopies;

**Описание** = Specifies the number of copies to plot;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlot;

**Входные данные**:

* (System.Object)nanoCADPlot;
* (System.Object)numCopies;

### Нод BatchPlotProgress

**Имя нода** = BatchPlotProgress;

**Описание** = Gets the current status of the batch plot, or terminates the batch plot;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlot;

**Входные данные**:

* (System.Object)nanoCADPlot;

### Нод Set_BatchPlotProgress

**Имя нода** = Set_BatchPlotProgress;

**Описание** = Gets the current status of the batch plot, or terminates the batch plot;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlot;

**Входные данные**:

* (System.Object)nanoCADPlot;
* (System.Object)bProgressStatus;

### Нод DisplayPlotPreview

**Имя нода** = DisplayPlotPreview;

**Описание** = Displays the Plot Preview dialog box with the specified partial or full view preview;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlot;

**Входные данные**:

* (System.Object)nanoCADPlot;
* (System.Object)nanoCAD.AcPreviewMode;

### Нод PlotToFile

**Имя нода** = PlotToFile;

**Описание** = Plots a layout to the specified file;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlot;

**Входные данные**:

* (System.Object)nanoCADPlot;
* (System.String)plotFile;
* (System.Object)plotConfig;

### Нод PlotToDevice

**Имя нода** = PlotToDevice;

**Описание** = Plots a layout to a device;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlot;

**Входные данные**:

* (System.Object)nanoCADPlot;
* (System.Object)plotConfig;

### Нод SetLayoutsToPlot

**Имя нода** = SetLayoutsToPlot;

**Описание** = Specifies the layout or layouts to plot;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlot;

**Входные данные**:

* (System.Object)nanoCADPlot;
* (System.Object)layoutList;

### Нод StartBatchMode

**Имя нода** = StartBatchMode;

**Описание** = Invokes batchmode printing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlot;

**Входные данные**:

* (System.Object)nanoCADPlot;
* (System.Object)entryCount;

### Нод CustomPlotSettings

**Имя нода** = CustomPlotSettings;

**Описание** = Retrieves custom plot settings;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlot;

**Входные данные**:

* (System.Object)nanoCADPlot;
* (System.Object)config;

### Нод Set_CustomPlotSettings

**Имя нода** = Set_CustomPlotSettings;

**Описание** = Retrieves custom plot settings;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlot;

**Входные данные**:

* (System.Object)nanoCADPlot;
* (System.Object)config;
* (System.Object)param;

### Нод Put_CustomPlotSettings

**Имя нода** = Put_CustomPlotSettings;

**Описание** = Retrieves custom plot settings;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlot;

**Входные данные**:

* (System.Object)nanoCADPlot;
* (System.Object)config;
* (System.Object)param;

### Нод BeginPage

**Имя нода** = BeginPage;

**Описание** = Triggered before page beging printing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlot;

**Входные данные**:

* (System.Object)nanoCADPlot;
* (System.String)TODOName;

### Нод EndPage

**Имя нода** = EndPage;

**Описание** = Triggered just after page has been plotted;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADPlot;

**Входные данные**:

* (System.Object)nanoCADPlot;
* (System.Object)Cancel;

## Группа NVP_nanoCAD_COM.nanoCAD.nanoCADDocuments

### Нод _nanoCADDocuments_Constructor

**Имя нода** = _nanoCADDocuments_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocuments;

**Входные данные**:

* (System.Object)dynamic;

### Нод _nanoCADDocuments_ConstructorCast

**Имя нода** = _nanoCADDocuments_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocuments;

**Входные данные**:

* (System.Object)dynamic;

### Нод Item

**Имя нода** = Item;

**Описание** = Gets the member object at a given index in a collection, group, or selection set;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocuments;

**Входные данные**:

* (System.Object)nanoCADDocuments;
* (System.Object)Index;

### Нод Count

**Имя нода** = Count;

**Описание** = Gets the number of items in the collection, dictionary, group, or selection set;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocuments;

**Входные данные**:

* (System.Object)nanoCADDocuments;

### Нод Application

**Имя нода** = Application;

**Описание** = Gets the Application object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocuments;

**Входные данные**:

* (System.Object)nanoCADDocuments;

### Нод Add

**Имя нода** = Add;

**Описание** = Creates a member object and adds it to the appropriate collection;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocuments;

**Входные данные**:

* (System.Object)nanoCADDocuments;
* (System.Object)TemplateName;

### Нод Open

**Имя нода** = Open;

**Описание** = DWG) and makes it the active document;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocuments;

**Входные данные**:

* (System.Object)nanoCADDocuments;
* (System.String)Name;
* (System.Object)ReadOnly;
* (System.Object)Password;

### Нод Close

**Имя нода** = Close;

**Описание** = Closes all open documents and empties the documents collection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocuments;

**Входные данные**:

* (System.Object)nanoCADDocuments;

### Нод GetAll_Documents

**Имя нода** = GetAll_Documents;

**Описание** = Возвращает все Документы в виде nanoCAD.nanoCADDocument;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocuments;

**Входные данные**:

* (System.Object)nanoCADDocuments;

## Группа NVP_nanoCAD_COM.nanoCAD.nanoCADDocument

### Нод _nanoCADDocument_Constructor

**Имя нода** = _nanoCADDocument_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)dynamic;

### Нод _nanoCADDocument_ConstructorCast

**Имя нода** = _nanoCADDocument_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)dynamic;

### Нод Plot

**Имя нода** = Plot;

**Описание** = Gets the Plot object for the document;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;

### Нод ActiveLayer

**Имя нода** = ActiveLayer;

**Описание** = Specifies the active layer;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;

### Нод Set_ActiveLayer

**Имя нода** = Set_ActiveLayer;

**Описание** = Specifies the active layer;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;
* (System.Object)pActLayer;

### Нод ActiveLinetype

**Имя нода** = ActiveLinetype;

**Описание** = Specifies the active linetype for the drawing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;

### Нод Set_ActiveLinetype

**Имя нода** = Set_ActiveLinetype;

**Описание** = Specifies the active linetype for the drawing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;
* (System.Object)pActLinetype;

### Нод ActiveDimStyle

**Имя нода** = ActiveDimStyle;

**Описание** = Specifies the active dimension style;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;

### Нод Set_ActiveDimStyle

**Имя нода** = Set_ActiveDimStyle;

**Описание** = Specifies the active dimension style;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;
* (System.Object)pActDimStyle;

### Нод ActiveTextStyle

**Имя нода** = ActiveTextStyle;

**Описание** = Specifies the active text style for the drawing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;

### Нод Set_ActiveTextStyle

**Имя нода** = Set_ActiveTextStyle;

**Описание** = Specifies the active text style for the drawing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;
* (System.Object)pActTextStyle;

### Нод ActiveUCS

**Имя нода** = ActiveUCS;

**Описание** = Specifies the active UCS for the drawing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;

### Нод Set_ActiveUCS

**Имя нода** = Set_ActiveUCS;

**Описание** = Specifies the active UCS for the drawing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;
* (System.Object)pActUCS;

### Нод ActiveViewport

**Имя нода** = ActiveViewport;

**Описание** = Specifies the active viewport for the drawing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;

### Нод Set_ActiveViewport

**Имя нода** = Set_ActiveViewport;

**Описание** = Specifies the active viewport for the drawing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;
* (System.Object)pActView;

### Нод ActivePViewport

**Имя нода** = ActivePViewport;

**Описание** = Specifies the active paper space viewport for the drawing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;

### Нод Set_ActivePViewport

**Имя нода** = Set_ActivePViewport;

**Описание** = Specifies the active paper space viewport for the drawing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;
* (System.Object)pActView;

### Нод ActiveSpace

**Имя нода** = ActiveSpace;

**Описание** = Toggles the active space between paper space and model space;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;

### Нод Set_ActiveSpace

**Имя нода** = Set_ActiveSpace;

**Описание** = Toggles the active space between paper space and model space;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;
* (System.Object)nanoCAD.AcActiveSpace;

### Нод SelectionSets

**Имя нода** = SelectionSets;

**Описание** = Gets the SelectionSets collection for the document;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;

### Нод ActiveSelectionSet

**Имя нода** = ActiveSelectionSet;

**Описание** = Gets the active selection set for the drawing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;

### Нод FullName

**Имя нода** = FullName;

**Описание** = Gets the name of the application or document, including the path;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;

### Нод Name

**Имя нода** = Name;

**Описание** = Specifies the name of the object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;

### Нод Path

**Имя нода** = Path;

**Описание** = Gets the path of the document, application, or external reference;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;

### Нод ObjectSnapMode

**Имя нода** = ObjectSnapMode;

**Описание** = Specifies the setting of the object snap mode;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;

### Нод Set_ObjectSnapMode

**Имя нода** = Set_ObjectSnapMode;

**Описание** = Specifies the setting of the object snap mode;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;
* (System.Object)fSnapMode;

### Нод ReadOnly

**Имя нода** = ReadOnly;

**Описание** = Specifies if the document is read-only or read-write;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;

### Нод Saved

**Имя нода** = Saved;

**Описание** = Specifies if the document has any unsaved changes;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;

### Нод MSpace

**Имя нода** = MSpace;

**Описание** = Allows editing of the model from floating paper space viewports;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;

### Нод Set_MSpace

**Имя нода** = Set_MSpace;

**Описание** = Allows editing of the model from floating paper space viewports;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;
* (System.Object)Mode;

### Нод Utility

**Имя нода** = Utility;

**Описание** = Gets the Utility object for the document;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;

### Нод Open

**Имя нода** = Open;

**Описание** = DWG) and makes it the active document;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;
* (System.String)FullName;
* (System.Object)Password;

### Нод AuditInfo

**Имя нода** = AuditInfo;

**Описание** = Evaluates the integrity of the drawing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;
* (System.Object)FixErr;

### Нод Import

**Имя нода** = Import;

**Описание** = Imports a drawing file in SAT, EPS, DXF, or WMF format;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;
* (System.String)FileName;
* (System.Object)InsertionPoint;
* (System.Double)ScaleFactor;

### Нод Export

**Имя нода** = Export;

**Описание** = Exports the drawing to a WMF, SAT, EPS, DXF, or BMP format;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;
* (System.String)FileName;
* (System.String)Extension;
* (System.Object)SelectionSet;

### Нод New

**Имя нода** = New;

**Описание** = Creates a new document in SDI mode;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;
* (System.String)TemplateFileName;

### Нод Save

**Имя нода** = Save;

**Описание** = Saves the document or menu group;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;

### Нод SaveAs

**Имя нода** = SaveAs;

**Описание** = Saves the document or menu group to a specified file;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;
* (System.String)FullFileName;
* (System.Object)SaveAsType;
* (System.Object)vSecurityParams;

### Нод Wblock

**Имя нода** = Wblock;

**Описание** = Writes out the given selection set as a new drawing file;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;
* (System.String)FileName;
* (System.Object)SelectionSet;

### Нод PurgeAll

**Имя нода** = PurgeAll;

**Описание** = Removes unused named references such as unused blocks or layers from the document;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;

### Нод GetVariable

**Имя нода** = GetVariable;

**Описание** = Gets the current setting of a system variable;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;
* (System.String)Name;

### Нод SetVariable

**Имя нода** = SetVariable;

**Описание** = Sets the value of a system variable;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;
* (System.String)Name;
* (System.Object)Value;

### Нод Regen

**Имя нода** = Regen;

**Описание** = Regenerates the entire drawing and recomputes the screen coordinates and view resolution for all objects;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;
* (System.Object)nanoCAD.AcRegenType;

### Нод PickfirstSelectionSet

**Имя нода** = PickfirstSelectionSet;

**Описание** = Gets the pickfirst selection set;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;

### Нод Active

**Имя нода** = Active;

**Описание** = Determines if the document is the active document for the session;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;

### Нод Activate

**Имя нода** = Activate;

**Описание** = Makes the specified drawing active;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;

### Нод Close

**Имя нода** = Close;

**Описание** = Closes the specified drawing, or all open drawings;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;
* (System.Object)SaveChanges;
* (System.Object)FileName;

### Нод Set_WindowState

**Имя нода** = Set_WindowState;

**Описание** = Specifies the state of the application or document window;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;
* (System.Object)nanoCAD.AcWindowState;

### Нод WindowState

**Имя нода** = WindowState;

**Описание** = Specifies the state of the application or document window;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;

### Нод Set_Width

**Имя нода** = Set_Width;

**Описание** = Specifies the width of the text boundary, view, image, toolbar, or main application window;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;
* (System.Int32)pWidth;

### Нод Width

**Имя нода** = Width;

**Описание** = Specifies the width of the text boundary, view, image, toolbar, or main application window;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;

### Нод Set_Height

**Имя нода** = Set_Height;

**Описание** = Height of the attribute, shape, text, or view toolbar or the main application window;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;
* (System.Int32)pHeight;

### Нод Height

**Имя нода** = Height;

**Описание** = Height of the attribute, shape, text, or view toolbar or the main application window;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;

### Нод Set_ActiveLayout

**Имя нода** = Set_ActiveLayout;

**Описание** = Specifies the active layout;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;
* (System.Object)pLayout;

### Нод ActiveLayout

**Имя нода** = ActiveLayout;

**Описание** = Specifies the active layout;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;

### Нод SendCommand

**Имя нода** = SendCommand;

**Описание** = Sends a command string from a VB or VBA application to the document for processing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;
* (System.String)Command;

### Нод HWND

**Имя нода** = HWND;

**Описание** = Gets the window handle of the document window frame;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;

### Нод WindowTitle

**Имя нода** = WindowTitle;

**Описание** = Gets the title of the document window;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;

### Нод Set_WindowTitle

**Имя нода** = Set_WindowTitle;

**Описание** = Gets the title of the document window;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;

### Нод Application

**Имя нода** = Application;

**Описание** = Gets the Application object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;

### Нод Database

**Имя нода** = Database;

**Описание** = Gets the database in which the object belongs;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;

### Нод StartUndoMark

**Имя нода** = StartUndoMark;

**Описание** = Marks the beginning of a block of operations;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;

### Нод EndUndoMark

**Имя нода** = EndUndoMark;

**Описание** = Marks the end of a block of operations;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;

### Нод AddCustomObjectToBlock

**Имя нода** = AddCustomObjectToBlock;

**Описание** = add custom object to block;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;
* (System.String)rxClass;
* (System.Object)blk;

### Нод Put_ActiveTextStyle

**Имя нода** = Put_ActiveTextStyle;

**Описание** = Specifies the active text style for the drawing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;
* (System.Object)pActTextStyle;

### Нод Put_ActiveLayer

**Имя нода** = Put_ActiveLayer;

**Описание** = Specifies the active layer;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;
* (System.Object)pActLayer;

### Нод Put_ActiveLinetype

**Имя нода** = Put_ActiveLinetype;

**Описание** = Specifies the active linetype for the drawing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;
* (System.Object)pActLinetype;

### Нод Put_ActiveDimStyle

**Имя нода** = Put_ActiveDimStyle;

**Описание** = Specifies the active dimension style;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;
* (System.Object)pActDimStyle;

### Нод Put_ActiveUCS

**Имя нода** = Put_ActiveUCS;

**Описание** = Specifies the active UCS for the drawing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;
* (System.Object)pActUCS;

### Нод Put_ActiveViewport

**Имя нода** = Put_ActiveViewport;

**Описание** = Specifies the active viewport for the drawing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;
* (System.Object)pActView;

### Нод Put_ActivePViewport

**Имя нода** = Put_ActivePViewport;

**Описание** = Specifies the active paper space viewport for the drawing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;
* (System.Object)pActView;

### Нод Put_ActiveLayout

**Имя нода** = Put_ActiveLayout;

**Описание** = Specifies the active layout;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADDocument;

**Входные данные**:

* (System.Object)nanoCADDocument;
* (System.Object)pLayout;

## Группа NVP_nanoCAD_COM.nanoCAD.nanoCADApplication

### Нод _nanoCADApplication_Constructor

**Имя нода** = _nanoCADApplication_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)dynamic;

### Нод _nanoCADApplication_ConstructorCast

**Имя нода** = _nanoCADApplication_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)dynamic;

### Нод Visible

**Имя нода** = Visible;

**Описание** = Specifies the visibility of an object or the application;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;

### Нод Set_Visible

**Имя нода** = Set_Visible;

**Описание** = Specifies the visibility of an object or the application;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;
* (System.Object)Visible;

### Нод Name

**Имя нода** = Name;

**Описание** = Specifies the name of the object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;

### Нод Set_Caption

**Имя нода** = Set_Caption;

**Описание** = Sets the text that the user sees displayed for the application or a menu item;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;
* (System.String)bstrCaption;

### Нод Caption

**Имя нода** = Caption;

**Описание** = Sets the text that the user sees displayed for the application or a menu item;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;

### Нод Application

**Имя нода** = Application;

**Описание** = Gets the Application object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;

### Нод ActiveDocument

**Имя нода** = ActiveDocument;

**Описание** = drawing file);

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;

### Нод Set_ActiveDocument

**Имя нода** = Set_ActiveDocument;

**Описание** = drawing file);

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;
* (System.Object)pActiveDoc;

### Нод FullName

**Имя нода** = FullName;

**Описание** = Gets the name of the application or document, including the path;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;

### Нод Height

**Имя нода** = Height;

**Описание** = Height of the attribute, shape, text, or view toolbar or the main application window;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;

### Нод Set_Height

**Имя нода** = Set_Height;

**Описание** = Height of the attribute, shape, text, or view toolbar or the main application window;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;
* (System.Int32)Height;

### Нод WindowLeft

**Имя нода** = WindowLeft;

**Описание** = Specifies the left edge of the application window;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;

### Нод Set_WindowLeft

**Имя нода** = Set_WindowLeft;

**Описание** = Specifies the left edge of the application window;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;
* (System.Int32)left;

### Нод Path

**Имя нода** = Path;

**Описание** = Gets the path of the document, application, or external reference;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;

### Нод LocaleId

**Имя нода** = LocaleId;

**Описание** = Gets the locale ID of the current session;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;

### Нод WindowTop

**Имя нода** = WindowTop;

**Описание** = Specifies the top edge of the application window;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;

### Нод Set_WindowTop

**Имя нода** = Set_WindowTop;

**Описание** = Specifies the top edge of the application window;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;
* (System.Int32)top;

### Нод Version

**Имя нода** = Version;

**Описание** = Gets the version of the application you are using;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;

### Нод Width

**Имя нода** = Width;

**Описание** = Specifies the width of the text boundary, view, image, toolbar, or main application window;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;

### Нод Set_Width

**Имя нода** = Set_Width;

**Описание** = Specifies the width of the text boundary, view, image, toolbar, or main application window;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;
* (System.Int32)Width;

### Нод StatusId

**Имя нода** = StatusId;

**Описание** = Gets the current active status of the viewport;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;
* (System.Object)VportObj;

### Нод Update

**Имя нода** = Update;

**Описание** = Updates the object to the drawing screen;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;

### Нод Quit

**Имя нода** = Quit;

**Описание** = Closes the drawing file and exits the application;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;

### Нод Documents

**Имя нода** = Documents;

**Описание** = Returns the documents collection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;

### Нод WindowState

**Имя нода** = WindowState;

**Описание** = Specifies the state of the application or document window;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;

### Нод Set_WindowState

**Имя нода** = Set_WindowState;

**Описание** = Specifies the state of the application or document window;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;
* (System.Object)nanoCAD.AcWindowState;

### Нод ZoomExtents

**Имя нода** = ZoomExtents;

**Описание** = Zooms the current viewport to the drawing extents;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;

### Нод ZoomAll

**Имя нода** = ZoomAll;

**Описание** = Zooms the current viewport to display the entire drawing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;

### Нод ZoomCenter

**Имя нода** = ZoomCenter;

**Описание** = Zooms the current viewport to a specified center point and magnification;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;
* (System.Object)Center;
* (System.Double)Magnify;

### Нод ZoomScaled

**Имя нода** = ZoomScaled;

**Описание** = Zooms the current viewport to given scale factor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;
* (System.Double)scale;
* (System.Object)nanoCAD.AcZoomScaleType;

### Нод ZoomWindow

**Имя нода** = ZoomWindow;

**Описание** = Zooms the current viewport to the area specified by two opposite corners of a rectangle;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;
* (System.Object)LowerLeft;
* (System.Object)UpperRight;

### Нод ZoomPickWindow

**Имя нода** = ZoomPickWindow;

**Описание** = Zooms the current viewport to a window defined by points picked on the screen;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;

### Нод GetState

**Имя нода** = GetState;

**Описание** = Retrieves an AcadState object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;

### Нод ZoomPrevious

**Имя нода** = ZoomPrevious;

**Описание** = Zooms the current viewport to its previous extents;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;

### Нод HWND

**Имя нода** = HWND;

**Описание** = Gets the window handle of the application window frame;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;

### Нод Utility

**Имя нода** = Utility;

**Описание** = Gets the Utility object for the application;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;

### Нод SendCommand

**Имя нода** = SendCommand;

**Описание** = Sends a command string from a VB or VBA application to the document for processing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;
* (System.String)Command;

### Нод Services

**Имя нода** = Services;

**Описание** = Returns sevices collection;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;

### Нод AllUsersAppData

**Имя нода** = AllUsersAppData;

**Описание** = Returns the AllUsersAppData;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;

### Нод CurUserAppData

**Имя нода** = CurUserAppData;

**Описание** = Returns the CurUserAppData;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;

### Нод Profile

**Имя нода** = Profile;

**Описание** = Returns the root Profile section;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;

### Нод Help

**Имя нода** = Help;

**Описание** = Invokes help with topic;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;
* (System.String)topic;

### Нод NewParam

**Имя нода** = NewParam;

**Описание** = Creates tepmorary param structure;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;

### Нод LoadModule

**Имя нода** = LoadModule;

**Описание** = Loads the specified application;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;
* (System.String)Path;

### Нод UnloadModule

**Имя нода** = UnloadModule;

**Описание** = Unloads the specified application;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;
* (System.String)Name;

### Нод ListModules

**Имя нода** = ListModules;

**Описание** = Gets the currently loaded applications;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;

### Нод LoadArx

**Имя нода** = LoadArx;

**Описание** = Loads the specified NRX application;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;
* (System.String)Path;

### Нод UnloadArx

**Имя нода** = UnloadArx;

**Описание** = Unloads the specified NRX application;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;
* (System.String)Name;

### Нод ListArx

**Имя нода** = ListArx;

**Описание** = Gets the currently loaded NRX applications;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;

### Нод Put_ActiveDocument

**Имя нода** = Put_ActiveDocument;

**Описание** = drawing file);

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;
* (System.Object)pActiveDoc;

### Нод Preferences

**Имя нода** = Preferences;

**Описание** = Returns the Preferences object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;

### Нод MenuGroups

**Имя нода** = MenuGroups;

**Описание** = Returns the current MenuGroups collection.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;

### Нод MenuBar

**Имя нода** = MenuBar;

**Описание** = Returns the current MenuBar object.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;

### Нод RunMacro

**Имя нода** = RunMacro;

**Описание** = Runs a VBA macro from the Application object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;
* (System.String)MacroPath;

### Нод ApplyProfile

**Имя нода** = ApplyProfile;

**Описание** = Apply profile changes;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;

### Нод GetInterfaceObject

**Имя нода** = GetInterfaceObject;

**Описание** = Loads an object using its program ID.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.nanoCADApplication;

**Входные данные**:

* (System.Object)nanoCADApplication;
* (System.String)ProgID;

## Группа NVP_nanoCAD_COM.nanoCAD.enum_ValueType

### Нод enum_eCS_VT_EMPTY

**Имя нода** = enum_eCS_VT_EMPTY;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_ValueType;


### Нод enum_eCS_VT_BOOL

**Имя нода** = enum_eCS_VT_BOOL;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_ValueType;


### Нод enum_eCS_VT_BYTE

**Имя нода** = enum_eCS_VT_BYTE;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_ValueType;


### Нод enum_eCS_VT_WORD

**Имя нода** = enum_eCS_VT_WORD;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_ValueType;


### Нод enum_eCS_VT_DWORD

**Имя нода** = enum_eCS_VT_DWORD;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_ValueType;


### Нод enum_eCS_VT_INT

**Имя нода** = enum_eCS_VT_INT;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_ValueType;


### Нод enum_eCS_VT_UINT

**Имя нода** = enum_eCS_VT_UINT;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_ValueType;


### Нод enum_eCS_VT_LONG

**Имя нода** = enum_eCS_VT_LONG;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_ValueType;


### Нод enum_eCS_VT_REAL

**Имя нода** = enum_eCS_VT_REAL;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_ValueType;


### Нод enum_eCS_VT_SREAL

**Имя нода** = enum_eCS_VT_SREAL;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_ValueType;


### Нод enum_eCS_VT_PSTR

**Имя нода** = enum_eCS_VT_PSTR;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_ValueType;


### Нод enum_eCS_VT_PWSTR

**Имя нода** = enum_eCS_VT_PWSTR;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_ValueType;


### Нод enum_eCS_VT_ETC

**Имя нода** = enum_eCS_VT_ETC;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_ValueType;


### Нод enum_eCS_VT_DEFAULT

**Имя нода** = enum_eCS_VT_DEFAULT;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_ValueType;


## Группа NVP_nanoCAD_COM.nanoCAD.enum_SerializationFormat

### Нод enum_eST_INI

**Имя нода** = enum_eST_INI;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_SerializationFormat;


### Нод enum_eST_XMLRPC

**Имя нода** = enum_eST_XMLRPC;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_SerializationFormat;


### Нод enum_eST_JSON

**Имя нода** = enum_eST_JSON;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_SerializationFormat;


## Группа NVP_nanoCAD_COM.nanoCAD.enum_FileExistAction

### Нод enum_eFEA_Attach

**Имя нода** = enum_eFEA_Attach;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_FileExistAction;


## Группа NVP_nanoCAD_COM.nanoCAD.enum_AcZoomScaleType

### Нод enum_acZoomScaledAbsolute

**Имя нода** = enum_acZoomScaledAbsolute;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcZoomScaleType;


### Нод enum_acZoomScaledRelative

**Имя нода** = enum_acZoomScaledRelative;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcZoomScaleType;


### Нод enum_acZoomScaledRelativePSpace

**Имя нода** = enum_acZoomScaledRelativePSpace;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcZoomScaleType;


## Группа NVP_nanoCAD_COM.nanoCAD.enum_AcXRefDemandLoad

### Нод enum_acDemandLoadDisabled

**Имя нода** = enum_acDemandLoadDisabled;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcXRefDemandLoad;


### Нод enum_acDemandLoadEnabled

**Имя нода** = enum_acDemandLoadEnabled;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcXRefDemandLoad;


### Нод enum_acDemandLoadEnabledWithCopy

**Имя нода** = enum_acDemandLoadEnabledWithCopy;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcXRefDemandLoad;


## Группа NVP_nanoCAD_COM.nanoCAD.enum_AcWindowState

### Нод enum_acNorm

**Имя нода** = enum_acNorm;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcWindowState;


### Нод enum_acMin

**Имя нода** = enum_acMin;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcWindowState;


### Нод enum_acMax

**Имя нода** = enum_acMax;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcWindowState;


## Группа NVP_nanoCAD_COM.nanoCAD.enum_AcUnits

### Нод enum_acDefaultUnits

**Имя нода** = enum_acDefaultUnits;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcUnits;


### Нод enum_acScientific

**Имя нода** = enum_acScientific;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcUnits;


### Нод enum_acDecimal

**Имя нода** = enum_acDecimal;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcUnits;


### Нод enum_acEngineering

**Имя нода** = enum_acEngineering;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcUnits;


### Нод enum_acArchitectural

**Имя нода** = enum_acArchitectural;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcUnits;


### Нод enum_acFractional

**Имя нода** = enum_acFractional;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcUnits;


## Группа NVP_nanoCAD_COM.nanoCAD.enum_AcToolbarItemType

### Нод enum_acToolbarButton

**Имя нода** = enum_acToolbarButton;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcToolbarItemType;


### Нод enum_acToolbarSeparator

**Имя нода** = enum_acToolbarSeparator;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcToolbarItemType;


### Нод enum_acToolbarControl

**Имя нода** = enum_acToolbarControl;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcToolbarItemType;


### Нод enum_acToolbarFlyout

**Имя нода** = enum_acToolbarFlyout;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcToolbarItemType;


## Группа NVP_nanoCAD_COM.nanoCAD.enum_AcToolbarDockStatus

### Нод enum_acToolbarDockTop

**Имя нода** = enum_acToolbarDockTop;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcToolbarDockStatus;


### Нод enum_acToolbarDockBottom

**Имя нода** = enum_acToolbarDockBottom;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcToolbarDockStatus;


### Нод enum_acToolbarDockLeft

**Имя нода** = enum_acToolbarDockLeft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcToolbarDockStatus;


### Нод enum_acToolbarDockRight

**Имя нода** = enum_acToolbarDockRight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcToolbarDockStatus;


### Нод enum_acToolbarFloating

**Имя нода** = enum_acToolbarFloating;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcToolbarDockStatus;


## Группа NVP_nanoCAD_COM.nanoCAD.enum_AcTextFontStyle

### Нод enum_acFontRegular

**Имя нода** = enum_acFontRegular;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcTextFontStyle;


### Нод enum_acFontItalic

**Имя нода** = enum_acFontItalic;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcTextFontStyle;


### Нод enum_acFontBold

**Имя нода** = enum_acFontBold;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcTextFontStyle;


### Нод enum_acFontBoldItalic

**Имя нода** = enum_acFontBoldItalic;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcTextFontStyle;


## Группа NVP_nanoCAD_COM.nanoCAD.enum_AcSelect

### Нод enum_acSelectionSetWindow

**Имя нода** = enum_acSelectionSetWindow;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSelect;


### Нод enum_acSelectionSetCrossing

**Имя нода** = enum_acSelectionSetCrossing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSelect;


### Нод enum_acSelectionSetFence

**Имя нода** = enum_acSelectionSetFence;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSelect;


### Нод enum_acSelectionSetPrevious

**Имя нода** = enum_acSelectionSetPrevious;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSelect;


### Нод enum_acSelectionSetLast

**Имя нода** = enum_acSelectionSetLast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSelect;


### Нод enum_acSelectionSetAll

**Имя нода** = enum_acSelectionSetAll;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSelect;


### Нод enum_acSelectionSetWindowPolygon

**Имя нода** = enum_acSelectionSetWindowPolygon;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSelect;


### Нод enum_acSelectionSetCrossingPolygon

**Имя нода** = enum_acSelectionSetCrossingPolygon;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSelect;


## Группа NVP_nanoCAD_COM.nanoCAD.enum_AcSaveAsType

### Нод enum_acR09_dxf

**Имя нода** = enum_acR09_dxf;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSaveAsType;


### Нод enum_acR10_dxf

**Имя нода** = enum_acR10_dxf;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSaveAsType;


### Нод enum_acR12_dwg

**Имя нода** = enum_acR12_dwg;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSaveAsType;


### Нод enum_acUnknown

**Имя нода** = enum_acUnknown;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSaveAsType;


### Нод enum_acR12_dxf

**Имя нода** = enum_acR12_dxf;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSaveAsType;


### Нод enum_acR13_dwg

**Имя нода** = enum_acR13_dwg;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSaveAsType;


### Нод enum_acR13_dxf

**Имя нода** = enum_acR13_dxf;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSaveAsType;


### Нод enum_acR14_dwg

**Имя нода** = enum_acR14_dwg;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSaveAsType;


### Нод enum_acR14_dxf

**Имя нода** = enum_acR14_dxf;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSaveAsType;


### Нод enum_acR15_dwg

**Имя нода** = enum_acR15_dwg;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSaveAsType;


### Нод enum_acR15_dxf

**Имя нода** = enum_acR15_dxf;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSaveAsType;


### Нод enum_acR15_Template

**Имя нода** = enum_acR15_Template;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSaveAsType;


### Нод enum_ac2000_dwg

**Имя нода** = enum_ac2000_dwg;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSaveAsType;


### Нод enum_ac2000_dxf

**Имя нода** = enum_ac2000_dxf;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSaveAsType;


### Нод enum_ac2000_Template

**Имя нода** = enum_ac2000_Template;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSaveAsType;


### Нод enum_acR18_dwg

**Имя нода** = enum_acR18_dwg;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSaveAsType;


### Нод enum_acR18_dxf

**Имя нода** = enum_acR18_dxf;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSaveAsType;


### Нод enum_acR18_Template

**Имя нода** = enum_acR18_Template;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSaveAsType;


### Нод enum_ac2004_dwg

**Имя нода** = enum_ac2004_dwg;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSaveAsType;


### Нод enum_ac2004_dxf

**Имя нода** = enum_ac2004_dxf;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSaveAsType;


### Нод enum_ac2004_Template

**Имя нода** = enum_ac2004_Template;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSaveAsType;


### Нод enum_ac2007_dwg

**Имя нода** = enum_ac2007_dwg;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSaveAsType;


### Нод enum_ac2007_dxf

**Имя нода** = enum_ac2007_dxf;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSaveAsType;


### Нод enum_ac2007_Template

**Имя нода** = enum_ac2007_Template;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSaveAsType;


### Нод enum_ac2010_dwg

**Имя нода** = enum_ac2010_dwg;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSaveAsType;


### Нод enum_ac2010_dxf

**Имя нода** = enum_ac2010_dxf;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSaveAsType;


### Нод enum_ac2010_Template

**Имя нода** = enum_ac2010_Template;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSaveAsType;


### Нод enum_ac2013_dwg

**Имя нода** = enum_ac2013_dwg;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSaveAsType;


### Нод enum_ac2013_dxf

**Имя нода** = enum_ac2013_dxf;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSaveAsType;


### Нод enum_ac2013_Template

**Имя нода** = enum_ac2013_Template;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSaveAsType;


### Нод enum_ac2018_dwg

**Имя нода** = enum_ac2018_dwg;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSaveAsType;


### Нод enum_ac2018_dxf

**Имя нода** = enum_ac2018_dxf;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSaveAsType;


### Нод enum_ac2018_Template

**Имя нода** = enum_ac2018_Template;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSaveAsType;


### Нод enum_acNative

**Имя нода** = enum_acNative;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcSaveAsType;


## Группа NVP_nanoCAD_COM.nanoCAD.enum_AcRegenType

### Нод enum_acActiveViewport

**Имя нода** = enum_acActiveViewport;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcRegenType;


### Нод enum_acAllViewports

**Имя нода** = enum_acAllViewports;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcRegenType;


## Группа NVP_nanoCAD_COM.nanoCAD.enum_AcProxyImage

### Нод enum_acProxyNotShow

**Имя нода** = enum_acProxyNotShow;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcProxyImage;


### Нод enum_acProxyShow

**Имя нода** = enum_acProxyShow;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcProxyImage;


### Нод enum_acProxyBoundingBox

**Имя нода** = enum_acProxyBoundingBox;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcProxyImage;


## Группа NVP_nanoCAD_COM.nanoCAD.enum_AcPrinterSpoolAlert

### Нод enum_acPrinterAlwaysAlert

**Имя нода** = enum_acPrinterAlwaysAlert;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcPrinterSpoolAlert;


### Нод enum_acPrinterAlertOnce

**Имя нода** = enum_acPrinterAlertOnce;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcPrinterSpoolAlert;


### Нод enum_acPrinterNeverAlertLogOnce

**Имя нода** = enum_acPrinterNeverAlertLogOnce;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcPrinterSpoolAlert;


### Нод enum_acPrinterNeverAlert

**Имя нода** = enum_acPrinterNeverAlert;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcPrinterSpoolAlert;


## Группа NVP_nanoCAD_COM.nanoCAD.enum_AcPreviewMode

### Нод enum_acPartialPreview

**Имя нода** = enum_acPartialPreview;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcPreviewMode;


### Нод enum_acFullPreview

**Имя нода** = enum_acFullPreview;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcPreviewMode;


## Группа NVP_nanoCAD_COM.nanoCAD.enum_AcPlotPolicy

### Нод enum_acPolicyNamed

**Имя нода** = enum_acPolicyNamed;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcPlotPolicy;


### Нод enum_acPolicyLegacy

**Имя нода** = enum_acPolicyLegacy;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcPlotPolicy;


## Группа NVP_nanoCAD_COM.nanoCAD.enum_AcOleQuality

### Нод enum_acOQLineArt

**Имя нода** = enum_acOQLineArt;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcOleQuality;


### Нод enum_acOQText

**Имя нода** = enum_acOQText;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcOleQuality;


### Нод enum_acOQGraphics

**Имя нода** = enum_acOQGraphics;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcOleQuality;


### Нод enum_acOQPhoto

**Имя нода** = enum_acOQPhoto;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcOleQuality;


### Нод enum_acOQHighPhoto

**Имя нода** = enum_acOQHighPhoto;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcOleQuality;


## Группа NVP_nanoCAD_COM.nanoCAD.enum_AcMenuItemType

### Нод enum_acMenuItem

**Имя нода** = enum_acMenuItem;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcMenuItemType;


### Нод enum_acMenuSeparator

**Имя нода** = enum_acMenuSeparator;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcMenuItemType;


### Нод enum_acMenuSubMenu

**Имя нода** = enum_acMenuSubMenu;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcMenuItemType;


## Группа NVP_nanoCAD_COM.nanoCAD.enum_AcMenuGroupType

### Нод enum_acBaseMenuGroup

**Имя нода** = enum_acBaseMenuGroup;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcMenuGroupType;


### Нод enum_acPartialMenuGroup

**Имя нода** = enum_acPartialMenuGroup;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcMenuGroupType;


## Группа NVP_nanoCAD_COM.nanoCAD.enum_AcMenuFileType

### Нод enum_acMenuFileCompiled

**Имя нода** = enum_acMenuFileCompiled;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcMenuFileType;


### Нод enum_acMenuFileSource

**Имя нода** = enum_acMenuFileSource;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcMenuFileType;


## Группа NVP_nanoCAD_COM.nanoCAD.enum_AcKeyboardPriority

### Нод enum_acKeyboardRunningObjSnap

**Имя нода** = enum_acKeyboardRunningObjSnap;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcKeyboardPriority;


### Нод enum_acKeyboardEntry

**Имя нода** = enum_acKeyboardEntry;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcKeyboardPriority;


### Нод enum_acKeyboardEntryExceptScripts

**Имя нода** = enum_acKeyboardEntryExceptScripts;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcKeyboardPriority;


## Группа NVP_nanoCAD_COM.nanoCAD.enum_AcKeyboardAccelerator

### Нод enum_acPreferenceClassic

**Имя нода** = enum_acPreferenceClassic;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcKeyboardAccelerator;


### Нод enum_acPreferenceCustom

**Имя нода** = enum_acPreferenceCustom;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcKeyboardAccelerator;


## Группа NVP_nanoCAD_COM.nanoCAD.enum_AcDrawingAreaSCMEdit

### Нод enum_acEdRepeatLastCommand

**Имя нода** = enum_acEdRepeatLastCommand;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcDrawingAreaSCMEdit;


### Нод enum_acEdSCM

**Имя нода** = enum_acEdSCM;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcDrawingAreaSCMEdit;


## Группа NVP_nanoCAD_COM.nanoCAD.enum_AcDrawingAreaSCMDefault

### Нод enum_acRepeatLastCommand

**Имя нода** = enum_acRepeatLastCommand;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcDrawingAreaSCMDefault;


### Нод enum_acSCM

**Имя нода** = enum_acSCM;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcDrawingAreaSCMDefault;


## Группа NVP_nanoCAD_COM.nanoCAD.enum_AcDrawingAreaSCMCommand

### Нод enum_acEnter

**Имя нода** = enum_acEnter;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcDrawingAreaSCMCommand;


### Нод enum_acEnableSCMOptions

**Имя нода** = enum_acEnableSCMOptions;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcDrawingAreaSCMCommand;


### Нод enum_acEnableSCM

**Имя нода** = enum_acEnableSCM;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcDrawingAreaSCMCommand;


## Группа NVP_nanoCAD_COM.nanoCAD.enum_AcCoordinateSystem

### Нод enum_acWorld

**Имя нода** = enum_acWorld;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcCoordinateSystem;


### Нод enum_acUCS

**Имя нода** = enum_acUCS;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcCoordinateSystem;


### Нод enum_acDisplayDCS

**Имя нода** = enum_acDisplayDCS;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcCoordinateSystem;


### Нод enum_acPaperSpaceDCS

**Имя нода** = enum_acPaperSpaceDCS;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcCoordinateSystem;


### Нод enum_acOCS

**Имя нода** = enum_acOCS;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcCoordinateSystem;


## Группа NVP_nanoCAD_COM.nanoCAD.enum_AcARXDemandLoad

### Нод enum_acDemanLoadDisable

**Имя нода** = enum_acDemanLoadDisable;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcARXDemandLoad;


### Нод enum_acDemandLoadOnObjectDetect

**Имя нода** = enum_acDemandLoadOnObjectDetect;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcARXDemandLoad;


### Нод enum_acDemandLoadCmdInvoke

**Имя нода** = enum_acDemandLoadCmdInvoke;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcARXDemandLoad;


## Группа NVP_nanoCAD_COM.nanoCAD.enum_AcAlignmentPointAcquisition

### Нод enum_acAlignPntAcquisitionAutomatic

**Имя нода** = enum_acAlignPntAcquisitionAutomatic;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcAlignmentPointAcquisition;


### Нод enum_acAlignPntAcquisitionShiftToAcquire

**Имя нода** = enum_acAlignPntAcquisitionShiftToAcquire;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcAlignmentPointAcquisition;


## Группа NVP_nanoCAD_COM.nanoCAD.enum_AcActiveSpace

### Нод enum_acPaperSpace

**Имя нода** = enum_acPaperSpace;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcActiveSpace;


### Нод enum_acModelSpace

**Имя нода** = enum_acModelSpace;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.enum_AcActiveSpace;


## Группа NVP_nanoCAD_COM.nanoCAD.AxDbDocument

### Нод _AxDbDocument_Constructor

**Имя нода** = _AxDbDocument_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.AxDbDocument;

**Входные данные**:

* (System.Object)dynamic;

### Нод _AxDbDocument_ConstructorCast

**Имя нода** = _AxDbDocument_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.AxDbDocument;

**Входные данные**:

* (System.Object)dynamic;

### Нод Name

**Имя нода** = Name;

**Описание** = Specifies the name of the document;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.AxDbDocument;

**Входные данные**:

* (System.Object)AxDbDocument;

### Нод Set_Name

**Имя нода** = Set_Name;

**Описание** = Specifies the name of the document;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.AxDbDocument;

**Входные данные**:

* (System.Object)AxDbDocument;
* (System.String)Name;

### Нод Open

**Имя нода** = Open;

**Описание** = Opens document.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.AxDbDocument;

**Входные данные**:

* (System.Object)AxDbDocument;
* (System.String)FileName;
* (System.Object)Password;

### Нод Save

**Имя нода** = Save;

**Описание** = Saves document.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.AxDbDocument;

**Входные данные**:

* (System.Object)AxDbDocument;

### Нод SaveAs

**Имя нода** = SaveAs;

**Описание** = Saves document with new name.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.AxDbDocument;

**Входные данные**:

* (System.Object)AxDbDocument;
* (System.String)FileName;
* (System.Object)vSecurityParams;

### Нод DxfIn

**Имя нода** = DxfIn;

**Описание** = DxfIn.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.AxDbDocument;

**Входные данные**:

* (System.Object)AxDbDocument;
* (System.String)FileName;
* (System.Object)LogFileName;

### Нод DxfOut

**Имя нода** = DxfOut;

**Описание** = DxfOut.;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.AxDbDocument;

**Входные данные**:

* (System.Object)AxDbDocument;
* (System.String)FileName;
* (System.Object)precision;
* (System.Object)SaveThumbnailImage;

### Нод Application

**Имя нода** = Application;

**Описание** = Retrieves an Application object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.AxDbDocument;

**Входные данные**:

* (System.Object)AxDbDocument;

### Нод Database

**Имя нода** = Database;

**Описание** = Retrieves an database object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.nanoCAD.AxDbDocument;

**Входные данные**:

* (System.Object)AxDbDocument;

## Группа NVP_nanoCAD_COM.McCOM2.ToleranceRecord

### Нод _ToleranceRecord_Constructor

**Имя нода** = _ToleranceRecord_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceRecord;

**Входные данные**:

* (System.Object)dynamic;

### Нод _ToleranceRecord_ConstructorCast

**Имя нода** = _ToleranceRecord_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceRecord;

**Входные данные**:

* (System.Object)dynamic;

### Нод Empty

**Имя нода** = Empty;

**Описание** = 0x00000592;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceRecord;

**Входные данные**:

* (System.Object)ToleranceRecord;

### Нод Set_Empty

**Имя нода** = Set_Empty;

**Описание** = 0x00000592;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceRecord;

**Входные данные**:

* (System.Object)ToleranceRecord;
* (System.Object)pvbValue;

### Нод Value

**Имя нода** = Value;

**Описание** = 0x00000593;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceRecord;

**Входные данные**:

* (System.Object)ToleranceRecord;

### Нод Set_Value

**Имя нода** = Set_Value;

**Описание** = 0x00000593;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceRecord;

**Входные данные**:

* (System.Object)ToleranceRecord;
* (System.String)pbstrValue;

### Нод Base

**Имя нода** = Base;

**Описание** = 0x00000594;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceRecord;

**Входные данные**:

* (System.Object)ToleranceRecord;

### Нод Set_Base

**Имя нода** = Set_Base;

**Описание** = 0x00000594;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceRecord;

**Входные данные**:

* (System.Object)ToleranceRecord;
* (System.String)pbstrValue;

### Нод Text

**Имя нода** = Text;

**Описание** = 0x00000595;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceRecord;

**Входные данные**:

* (System.Object)ToleranceRecord;

### Нод Set_Text

**Имя нода** = Set_Text;

**Описание** = 0x00000595;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceRecord;

**Входные данные**:

* (System.Object)ToleranceRecord;
* (System.String)pbstrValue;

### Нод Parameters

**Имя нода** = Parameters;

**Описание** = 0x00000596;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceRecord;

**Входные данные**:

* (System.Object)ToleranceRecord;

### Нод Object

**Имя нода** = Object;

**Описание** = 0x00000597;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceRecord;

**Входные данные**:

* (System.Object)ToleranceRecord;

## Группа NVP_nanoCAD_COM.McCOM2.ToleranceParameters

### Нод _ToleranceParameters_Constructor

**Имя нода** = _ToleranceParameters_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)dynamic;

### Нод _ToleranceParameters_ConstructorCast

**Имя нода** = _ToleranceParameters_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)dynamic;

### Нод Object

**Имя нода** = Object;

**Описание** = Parameter collection's owner object;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;

### Нод Count

**Имя нода** = Count;

**Описание** = 0x000005a4;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;

### Нод Contains

**Имя нода** = Contains;

**Описание** = 0x000005a5;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;
* (System.Object)McCOM2.SymToleranceParameter;

### Нод Value

**Имя нода** = Value;

**Описание** = 0x000005a6;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;

### Нод Set_Value

**Имя нода** = Set_Value;

**Описание** = 0x000005a6;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;
* (System.Object)pnValue;

### Нод ParamLinear

**Имя нода** = ParamLinear;

**Описание** = 0x000005a7;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;

### Нод Set_ParamLinear

**Имя нода** = Set_ParamLinear;

**Описание** = 0x000005a7;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;
* (System.Object)pvbValue;

### Нод ParamPlanar

**Имя нода** = ParamPlanar;

**Описание** = 0x000005a8;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;

### Нод Set_ParamPlanar

**Имя нода** = Set_ParamPlanar;

**Описание** = 0x000005a8;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;
* (System.Object)pvbValue;

### Нод ParamRound

**Имя нода** = ParamRound;

**Описание** = 0x000005a9;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;

### Нод Set_ParamRound

**Имя нода** = Set_ParamRound;

**Описание** = 0x000005a9;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;
* (System.Object)pvbValue;

### Нод ParamCylindrical

**Имя нода** = ParamCylindrical;

**Описание** = 0x000005aa;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;

### Нод Set_ParamCylindrical

**Имя нода** = Set_ParamCylindrical;

**Описание** = 0x000005aa;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;
* (System.Object)pvbValue;

### Нод ParamProfile

**Имя нода** = ParamProfile;

**Описание** = 0x000005ab;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;

### Нод Set_ParamProfile

**Имя нода** = Set_ParamProfile;

**Описание** = 0x000005ab;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;
* (System.Object)pvbValue;

### Нод ParamParallel

**Имя нода** = ParamParallel;

**Описание** = 0x000005ac;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;

### Нод Set_ParamParallel

**Имя нода** = Set_ParamParallel;

**Описание** = 0x000005ac;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;
* (System.Object)pvbValue;

### Нод ParamPerpendicular

**Имя нода** = ParamPerpendicular;

**Описание** = 0x000005ad;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;

### Нод Set_ParamPerpendicular

**Имя нода** = Set_ParamPerpendicular;

**Описание** = 0x000005ad;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;
* (System.Object)pvbValue;

### Нод ParamTilt

**Имя нода** = ParamTilt;

**Описание** = 0x000005ae;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;

### Нод Set_ParamTilt

**Имя нода** = Set_ParamTilt;

**Описание** = 0x000005ae;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;
* (System.Object)pvbValue;

### Нод ParamCoaxiality

**Имя нода** = ParamCoaxiality;

**Описание** = 0x000005af;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;

### Нод Set_ParamCoaxiality

**Имя нода** = Set_ParamCoaxiality;

**Описание** = 0x000005af;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;
* (System.Object)pvbValue;

### Нод ParamSymmetry

**Имя нода** = ParamSymmetry;

**Описание** = 0x000005b0;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;

### Нод Set_ParamSymmetry

**Имя нода** = Set_ParamSymmetry;

**Описание** = 0x000005b0;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;
* (System.Object)pvbValue;

### Нод ParamPosition

**Имя нода** = ParamPosition;

**Описание** = 0x000005b1;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;

### Нод Set_ParamPosition

**Имя нода** = Set_ParamPosition;

**Описание** = 0x000005b1;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;
* (System.Object)pvbValue;

### Нод ParamCross

**Имя нода** = ParamCross;

**Описание** = 0x000005b2;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;

### Нод Set_ParamCross

**Имя нода** = Set_ParamCross;

**Описание** = 0x000005b2;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;
* (System.Object)pvbValue;

### Нод ParamBeat

**Имя нода** = ParamBeat;

**Описание** = 0x000005b3;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;

### Нод Set_ParamBeat

**Имя нода** = Set_ParamBeat;

**Описание** = 0x000005b3;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;
* (System.Object)pvbValue;

### Нод ParamFullBeat

**Имя нода** = ParamFullBeat;

**Описание** = 0x000005b4;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;

### Нод Set_ParamFullBeat

**Имя нода** = Set_ParamFullBeat;

**Описание** = 0x000005b4;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;
* (System.Object)pvbValue;

### Нод ParamFormProfile

**Имя нода** = ParamFormProfile;

**Описание** = 0x000005b5;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;

### Нод Set_ParamFormProfile

**Имя нода** = Set_ParamFormProfile;

**Описание** = 0x000005b5;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;
* (System.Object)pvbValue;

### Нод ParamFormSurface

**Имя нода** = ParamFormSurface;

**Описание** = 0x000005b6;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;

### Нод Set_ParamFormSurface

**Имя нода** = Set_ParamFormSurface;

**Описание** = 0x000005b6;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ToleranceParameters;

**Входные данные**:

* (System.Object)ToleranceParameters;
* (System.Object)pvbValue;

## Группа NVP_nanoCAD_COM.McCOM2.Text

### Нод _Text_Constructor

**Имя нода** = _Text_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Text;

**Входные данные**:

* (System.Object)dynamic;

### Нод _Text_ConstructorCast

**Имя нода** = _Text_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Text;

**Входные данные**:

* (System.Object)dynamic;

### Нод Text

**Имя нода** = Text;

**Описание** = 0x00000576;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Text;

**Входные данные**:

* (System.Object)Text;

### Нод Set_Text

**Имя нода** = Set_Text;

**Описание** = 0x00000576;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Text;

**Входные данные**:

* (System.Object)Text;
* (System.String)pbstrText;

### Нод Raw

**Имя нода** = Raw;

**Описание** = 0x00000573;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Text;

**Входные данные**:

* (System.Object)Text;

### Нод Set_Raw

**Имя нода** = Set_Raw;

**Описание** = 0x00000573;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Text;

**Входные данные**:

* (System.Object)Text;
* (System.String)pbstrRawText;

### Нод Native

**Имя нода** = Native;

**Описание** = 0x00000572;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Text;

**Входные данные**:

* (System.Object)Text;

### Нод Set_Native

**Имя нода** = Set_Native;

**Описание** = 0x00000572;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Text;

**Входные данные**:

* (System.Object)Text;
* (System.String)pbstrNative;

### Нод Simple

**Имя нода** = Simple;

**Описание** = 0x00000575;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Text;

**Входные данные**:

* (System.Object)Text;

### Нод RichEdit

**Имя нода** = RichEdit;

**Описание** = 0x00000574;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Text;

**Входные данные**:

* (System.Object)Text;

### Нод InsertSub

**Имя нода** = InsertSub;

**Описание** = 0x0000056e;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Text;

**Входные данные**:

* (System.Object)Text;
* (System.String)bstrSubscript;
* (System.Object)Index;

### Нод InsertSup

**Имя нода** = InsertSup;

**Описание** = 0x0000056f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Text;

**Входные данные**:

* (System.Object)Text;
* (System.String)bstrSuperscript;
* (System.Object)Index;

### Нод Insert

**Имя нода** = Insert;

**Описание** = 0x0000056b;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Text;

**Входные данные**:

* (System.Object)Text;
* (System.String)bstrText;
* (System.Object)Index;

### Нод InsertNum

**Имя нода** = InsertNum;

**Описание** = 0x0000056d;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Text;

**Входные данные**:

* (System.Object)Text;
* (System.Double)rNumber;
* (System.Object)Index;

### Нод InsertDivision

**Имя нода** = InsertDivision;

**Описание** = 0x0000056c;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Text;

**Входные данные**:

* (System.Object)Text;
* (System.String)bstrUpper;
* (System.String)bstrLower;
* (System.Object)vbLine;
* (System.Object)Index;

### Нод Delete

**Имя нода** = Delete;

**Описание** = 0x00000569;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Text;

**Входные данные**:

* (System.Object)Text;
* (System.Object)idsFrom;
* (System.Object)nCount;

### Нод Split

**Имя нода** = Split;

**Описание** = 0x00000571;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Text;

**Входные данные**:

* (System.Object)Text;
* (System.String)bstrDelimiter;

### Нод Find

**Имя нода** = Find;

**Описание** = 0x0000056a;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Text;

**Входные данные**:

* (System.Object)Text;
* (System.String)bstrSubstring;
* (System.Object)Start;
* (System.Object)vbMatchCase;

### Нод Replace

**Имя нода** = Replace;

**Описание** = 0x00000570;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Text;

**Входные данные**:

* (System.Object)Text;
* (System.String)bstrWhat;
* (System.String)bstrWith;
* (System.Object)vbMatchCase;

## Группа NVP_nanoCAD_COM.McCOM2.SymWeld

### Нод _SymWeld_Constructor

**Имя нода** = _SymWeld_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymWeld;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymWeld_ConstructorCast

**Имя нода** = _SymWeld_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymWeld;

**Входные данные**:

* (System.Object)dynamic;

### Нод Type

**Имя нода** = Type;

**Описание** = 0x00000565;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymWeld;

**Входные данные**:

* (System.Object)SymWeld;

### Нод Set_Type

**Имя нода** = Set_Type;

**Описание** = 0x00000565;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymWeld;

**Входные данные**:

* (System.Object)SymWeld;
* (System.Object)McCOM2.SymWeldType;

### Нод Direction

**Имя нода** = Direction;

**Описание** = 0x0000055b;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymWeld;

**Входные данные**:

* (System.Object)SymWeld;
* (System.Object)Index;

### Нод Set_Direction

**Имя нода** = Set_Direction;

**Описание** = 0x0000055b;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymWeld;

**Входные данные**:

* (System.Object)SymWeld;
* (System.Object)Index;
* (System.Object)pnValue;

## Группа NVP_nanoCAD_COM.McCOM2.SymView

### Нод _SymView_Constructor

**Имя нода** = _SymView_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymView;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymView_ConstructorCast

**Имя нода** = _SymView_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymView;

**Входные данные**:

* (System.Object)dynamic;

### Нод Position

**Имя нода** = Position;

**Описание** = 0x00000547;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymView;

**Входные данные**:

* (System.Object)SymView;

### Нод Set_Position

**Имя нода** = Set_Position;

**Описание** = 0x00000547;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymView;

**Входные данные**:

* (System.Object)SymView;
* (System.Object)pvPoint;

### Нод Prefix

**Имя нода** = Prefix;

**Описание** = 0x00000548;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymView;

**Входные данные**:

* (System.Object)SymView;

### Нод Set_Prefix

**Имя нода** = Set_Prefix;

**Описание** = 0x00000548;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymView;

**Входные данные**:

* (System.Object)SymView;
* (System.String)pbstrResult;

### Нод Index

**Имя нода** = Index;

**Описание** = 0x00000540;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymView;

**Входные данные**:

* (System.Object)SymView;

### Нод Set_Index

**Имя нода** = Set_Index;

**Описание** = 0x00000540;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymView;

**Входные данные**:

* (System.Object)SymView;
* (System.String)pbstrResult;

### Нод Zone

**Имя нода** = Zone;

**Описание** = 0x0000054d;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymView;

**Входные данные**:

* (System.Object)SymView;

### Нод Set_Zone

**Имя нода** = Set_Zone;

**Описание** = 0x0000054d;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymView;

**Входные данные**:

* (System.Object)SymView;
* (System.String)pbstrResult;

### Нод Sheet

**Имя нода** = Sheet;

**Описание** = 0x0000054a;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymView;

**Входные данные**:

* (System.Object)SymView;

### Нод Set_Sheet

**Имя нода** = Set_Sheet;

**Описание** = 0x0000054a;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymView;

**Входные данные**:

* (System.Object)SymView;
* (System.Object)pnResult;

## Группа NVP_nanoCAD_COM.McCOM2.SymTT

### Нод _SymTT_Constructor

**Имя нода** = _SymTT_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTT;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymTT_ConstructorCast

**Имя нода** = _SymTT_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTT;

**Входные данные**:

* (System.Object)dynamic;

### Нод Text

**Имя нода** = Text;

**Описание** = 0x0000052f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTT;

**Входные данные**:

* (System.Object)SymTT;

### Нод Set_Text

**Имя нода** = Set_Text;

**Описание** = 0x0000052f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTT;

**Входные данные**:

* (System.Object)SymTT;
* (System.String)pbstrText;

### Нод IsDescription

**Имя нода** = IsDescription;

**Описание** = 0x00000526;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTT;

**Входные данные**:

* (System.Object)SymTT;

### Нод Frame

**Имя нода** = Frame;

**Описание** = 0x00000524;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTT;

**Входные данные**:

* (System.Object)SymTT;

### Нод Set_Frame

**Имя нода** = Set_Frame;

**Описание** = 0x00000524;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTT;

**Входные данные**:

* (System.Object)SymTT;
* (System.Object)ppFrame;

### Нод TextHeight

**Имя нода** = TextHeight;

**Описание** = 0x00000530;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTT;

**Входные данные**:

* (System.Object)SymTT;

### Нод Set_TextHeight

**Имя нода** = Set_TextHeight;

**Описание** = 0x00000530;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTT;

**Входные данные**:

* (System.Object)SymTT;
* (System.Double)prValue;

### Нод Slope

**Имя нода** = Slope;

**Описание** = 0x0000052e;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTT;

**Входные данные**:

* (System.Object)SymTT;

### Нод Set_Slope

**Имя нода** = Set_Slope;

**Описание** = 0x0000052e;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTT;

**Входные данные**:

* (System.Object)SymTT;
* (System.Double)prValue;

### Нод WordWrap

**Имя нода** = WordWrap;

**Описание** = 0x00000533;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTT;

**Входные данные**:

* (System.Object)SymTT;

### Нод Set_WordWrap

**Имя нода** = Set_WordWrap;

**Описание** = 0x00000533;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTT;

**Входные данные**:

* (System.Object)SymTT;
* (System.Object)pvbWrap;

### Нод Move

**Имя нода** = Move;

**Описание** = 0x0000051d;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTT;

**Входные данные**:

* (System.Object)SymTT;
* (System.Object)vTopLeftPoint;
* (System.Object)vBottomRightPoint;

## Группа NVP_nanoCAD_COM.McCOM2.SymTolerance

### Нод _SymTolerance_Constructor

**Имя нода** = _SymTolerance_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTolerance;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymTolerance_ConstructorCast

**Имя нода** = _SymTolerance_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTolerance;

**Входные данные**:

* (System.Object)dynamic;

### Нод TextPosition

**Имя нода** = TextPosition;

**Описание** = 0x0000058b;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTolerance;

**Входные данные**:

* (System.Object)SymTolerance;

### Нод Set_TextPosition

**Имя нода** = Set_TextPosition;

**Описание** = 0x0000058b;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTolerance;

**Входные данные**:

* (System.Object)SymTolerance;
* (System.Object)pvPoint;

### Нод Text

**Имя нода** = Text;

**Описание** = 0x0000058c;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTolerance;

**Входные данные**:

* (System.Object)SymTolerance;

### Нод Set_Text

**Имя нода** = Set_Text;

**Описание** = 0x0000058c;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTolerance;

**Входные данные**:

* (System.Object)SymTolerance;
* (System.String)pbstrValue;

### Нод Records

**Имя нода** = Records;

**Описание** = 0x0000058f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTolerance;

**Входные данные**:

* (System.Object)SymTolerance;

### Нод Leaders

**Имя нода** = Leaders;

**Описание** = 0x0000058e;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTolerance;

**Входные данные**:

* (System.Object)SymTolerance;

## Группа NVP_nanoCAD_COM.McCOM2.SymTableRow

### Нод _SymTableRow_Constructor

**Имя нода** = _SymTableRow_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableRow;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymTableRow_ConstructorCast

**Имя нода** = _SymTableRow_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableRow;

**Входные данные**:

* (System.Object)dynamic;

### Нод Cell

**Имя нода** = Cell;

**Описание** = 0x0000050f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableRow;

**Входные данные**:

* (System.Object)SymTableRow;
* (System.Object)nIndex;

### Нод Count

**Имя нода** = Count;

**Описание** = 0x00000510;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableRow;

**Входные данные**:

* (System.Object)SymTableRow;

### Нод Width

**Имя нода** = Width;

**Описание** = 0x00000517;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableRow;

**Входные данные**:

* (System.Object)SymTableRow;

### Нод Height

**Имя нода** = Height;

**Описание** = 0x00000512;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableRow;

**Входные данные**:

* (System.Object)SymTableRow;

### Нод Set_Height

**Имя нода** = Set_Height;

**Описание** = 0x00000512;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableRow;

**Входные данные**:

* (System.Object)SymTableRow;
* (System.Double)prValue;

### Нод Bounds

**Имя нода** = Bounds;

**Описание** = 0x0000050e;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableRow;

**Входные данные**:

* (System.Object)SymTableRow;

### Нод Index

**Имя нода** = Index;

**Описание** = 0x00000513;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableRow;

**Входные данные**:

* (System.Object)SymTableRow;

### Нод Table

**Имя нода** = Table;

**Описание** = 0x00000515;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableRow;

**Входные данные**:

* (System.Object)SymTableRow;

### Нод Range

**Имя нода** = Range;

**Описание** = 0x00000514;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableRow;

**Входные данные**:

* (System.Object)SymTableRow;

### Нод Title

**Имя нода** = Title;

**Описание** = 0x00000516;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableRow;

**Входные данные**:

* (System.Object)SymTableRow;

### Нод Set_Title

**Имя нода** = Set_Title;

**Описание** = 0x00000516;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableRow;

**Входные данные**:

* (System.Object)SymTableRow;
* (System.Object)pvbResult;

### Нод Format

**Имя нода** = Format;

**Описание** = 0x00000511;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableRow;

**Входные данные**:

* (System.Object)SymTableRow;

### Нод Copy

**Имя нода** = Copy;

**Описание** = 0x0000050b;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableRow;

**Входные данные**:

* (System.Object)SymTableRow;
* (System.Object)nIdxAfter;
* (System.Object)vbWithFormat;
* (System.Object)vbWithContent;

### Нод Delete

**Имя нода** = Delete;

**Описание** = 0x0000050c;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableRow;

**Входные данные**:

* (System.Object)SymTableRow;

### Нод Split

**Имя нода** = Split;

**Описание** = 0x0000050d;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableRow;

**Входные данные**:

* (System.Object)SymTableRow;
* (System.Object)vHeightsArray;

### Нод Object

**Имя нода** = Object;

**Описание** = 0x00000611;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableRow;

**Входные данные**:

* (System.Object)SymTableRow;

## Группа NVP_nanoCAD_COM.McCOM2.SymTableFormat2

### Нод _SymTableFormat2_Constructor

**Имя нода** = _SymTableFormat2_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableFormat2;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymTableFormat2_ConstructorCast

**Имя нода** = _SymTableFormat2_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableFormat2;

**Входные данные**:

* (System.Object)dynamic;

### Нод VertFit

**Имя нода** = VertFit;

**Описание** = 0x0000060a;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableFormat2;

**Входные данные**:

* (System.Object)SymTableFormat2;

### Нод Set_VertFit

**Имя нода** = Set_VertFit;

**Описание** = 0x0000060a;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableFormat2;

**Входные данные**:

* (System.Object)SymTableFormat2;
* (System.Object)McCOM2.SymTableVertFit;

### Нод HorzFit

**Имя нода** = HorzFit;

**Описание** = 0x0000060b;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableFormat2;

**Входные данные**:

* (System.Object)SymTableFormat2;

### Нод Set_HorzFit

**Имя нода** = Set_HorzFit;

**Описание** = 0x0000060b;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableFormat2;

**Входные данные**:

* (System.Object)SymTableFormat2;
* (System.Object)McCOM2.SymTableHorzFit;

### Нод FillColor

**Имя нода** = FillColor;

**Описание** = 0x0000060c;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableFormat2;

**Входные данные**:

* (System.Object)SymTableFormat2;

### Нод Set_FillColor

**Имя нода** = Set_FillColor;

**Описание** = 0x0000060c;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableFormat2;

**Входные данные**:

* (System.Object)SymTableFormat2;
* (System.Object)pnResult;

### Нод ReadOnly

**Имя нода** = ReadOnly;

**Описание** = 0x00000612;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableFormat2;

**Входные данные**:

* (System.Object)SymTableFormat2;

## Группа NVP_nanoCAD_COM.McCOM2.SymTableFormat

### Нод _SymTableFormat_Constructor

**Имя нода** = _SymTableFormat_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableFormat;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymTableFormat_ConstructorCast

**Имя нода** = _SymTableFormat_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableFormat;

**Входные данные**:

* (System.Object)dynamic;

### Нод BorderWeight

**Имя нода** = BorderWeight;

**Описание** = 0x00000501;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableFormat;

**Входные данные**:

* (System.Object)SymTableFormat;
* (System.Object)McCOM2.SymTableBorder;

### Нод Set_BorderWeight

**Имя нода** = Set_BorderWeight;

**Описание** = 0x00000501;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableFormat;

**Входные данные**:

* (System.Object)SymTableFormat;
* (System.Object)McCOM2.SymTableBorder;
* (System.Double)prValue;

### Нод BorderColor

**Имя нода** = BorderColor;

**Описание** = 0x00000500;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableFormat;

**Входные данные**:

* (System.Object)SymTableFormat;
* (System.Object)McCOM2.SymTableBorder;

### Нод Set_BorderColor

**Имя нода** = Set_BorderColor;

**Описание** = 0x00000500;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableFormat;

**Входные данные**:

* (System.Object)SymTableFormat;
* (System.Object)McCOM2.SymTableBorder;
* (System.Object)pnValue;

### Нод TextStyle

**Имя нода** = TextStyle;

**Описание** = 0x00000508;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableFormat;

**Входные данные**:

* (System.Object)SymTableFormat;

### Нод Set_TextStyle

**Имя нода** = Set_TextStyle;

**Описание** = 0x00000508;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableFormat;

**Входные данные**:

* (System.Object)SymTableFormat;
* (System.String)pbstrResult;

### Нод TextHeight

**Имя нода** = TextHeight;

**Описание** = 0x00000505;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableFormat;

**Входные данные**:

* (System.Object)SymTableFormat;

### Нод Set_TextHeight

**Имя нода** = Set_TextHeight;

**Описание** = 0x00000505;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableFormat;

**Входные данные**:

* (System.Object)SymTableFormat;
* (System.Double)prResult;

### Нод TextAlign

**Имя нода** = TextAlign;

**Описание** = 0x00000503;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableFormat;

**Входные данные**:

* (System.Object)SymTableFormat;

### Нод Set_TextAlign

**Имя нода** = Set_TextAlign;

**Описание** = 0x00000503;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableFormat;

**Входные данные**:

* (System.Object)SymTableFormat;
* (System.Object)McCOM2.TextAlignment;

### Нод TextMargin

**Имя нода** = TextMargin;

**Описание** = 0x00000506;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableFormat;

**Входные данные**:

* (System.Object)SymTableFormat;

### Нод Set_TextMargin

**Имя нода** = Set_TextMargin;

**Описание** = 0x00000506;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableFormat;

**Входные данные**:

* (System.Object)SymTableFormat;
* (System.Double)prResult;

### Нод TextAngle

**Имя нода** = TextAngle;

**Описание** = 0x00000504;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableFormat;

**Входные данные**:

* (System.Object)SymTableFormat;

### Нод Set_TextAngle

**Имя нода** = Set_TextAngle;

**Описание** = 0x00000504;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableFormat;

**Входные данные**:

* (System.Object)SymTableFormat;
* (System.Object)pnAngle;

### Нод TextWrap

**Имя нода** = TextWrap;

**Описание** = 0x00000509;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableFormat;

**Входные данные**:

* (System.Object)SymTableFormat;

### Нод Set_TextWrap

**Имя нода** = Set_TextWrap;

**Описание** = 0x00000509;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableFormat;

**Входные данные**:

* (System.Object)SymTableFormat;
* (System.Object)pvbResult;

### Нод TextScale

**Имя нода** = TextScale;

**Описание** = 0x00000507;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableFormat;

**Входные данные**:

* (System.Object)SymTableFormat;

### Нод Set_TextScale

**Имя нода** = Set_TextScale;

**Описание** = 0x00000507;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableFormat;

**Входные данные**:

* (System.Object)SymTableFormat;
* (System.Object)pvbResult;

### Нод Enabled

**Имя нода** = Enabled;

**Описание** = 0x00000502;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableFormat;

**Входные данные**:

* (System.Object)SymTableFormat;

### Нод Set_Enabled

**Имя нода** = Set_Enabled;

**Описание** = 0x00000502;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableFormat;

**Входные данные**:

* (System.Object)SymTableFormat;
* (System.Object)pvbResult;

### Нод CopyFrom

**Имя нода** = CopyFrom;

**Описание** = 0x000004ff;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableFormat;

**Входные данные**:

* (System.Object)SymTableFormat;
* (System.Object)pFrom;

## Группа NVP_nanoCAD_COM.McCOM2.SymTableCol

### Нод _SymTableCol_Constructor

**Имя нода** = _SymTableCol_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableCol;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymTableCol_ConstructorCast

**Имя нода** = _SymTableCol_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableCol;

**Входные данные**:

* (System.Object)dynamic;

### Нод Cell

**Имя нода** = Cell;

**Описание** = 0x0000050f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableCol;

**Входные данные**:

* (System.Object)SymTableCol;
* (System.Object)nIndex;

### Нод Count

**Имя нода** = Count;

**Описание** = 0x00000510;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableCol;

**Входные данные**:

* (System.Object)SymTableCol;

### Нод Width

**Имя нода** = Width;

**Описание** = 0x00000517;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableCol;

**Входные данные**:

* (System.Object)SymTableCol;

### Нод Set_Width

**Имя нода** = Set_Width;

**Описание** = 0x00000517;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableCol;

**Входные данные**:

* (System.Object)SymTableCol;
* (System.Double)prValue;

### Нод Height

**Имя нода** = Height;

**Описание** = 0x00000512;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableCol;

**Входные данные**:

* (System.Object)SymTableCol;

### Нод Bounds

**Имя нода** = Bounds;

**Описание** = 0x0000050e;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableCol;

**Входные данные**:

* (System.Object)SymTableCol;

### Нод Index

**Имя нода** = Index;

**Описание** = 0x00000513;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableCol;

**Входные данные**:

* (System.Object)SymTableCol;

### Нод Table

**Имя нода** = Table;

**Описание** = 0x00000515;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableCol;

**Входные данные**:

* (System.Object)SymTableCol;

### Нод Range

**Имя нода** = Range;

**Описание** = 0x00000514;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableCol;

**Входные данные**:

* (System.Object)SymTableCol;

### Нод Format

**Имя нода** = Format;

**Описание** = 0x00000511;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableCol;

**Входные данные**:

* (System.Object)SymTableCol;

### Нод Copy

**Имя нода** = Copy;

**Описание** = 0x0000050b;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableCol;

**Входные данные**:

* (System.Object)SymTableCol;
* (System.Object)nIdxAfter;
* (System.Object)vbWithFormat;
* (System.Object)vbWithContent;

### Нод Delete

**Имя нода** = Delete;

**Описание** = 0x0000050c;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableCol;

**Входные данные**:

* (System.Object)SymTableCol;

### Нод Split

**Имя нода** = Split;

**Описание** = 0x0000050d;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableCol;

**Входные данные**:

* (System.Object)SymTableCol;
* (System.Object)vWidthsArray;

## Группа NVP_nanoCAD_COM.McCOM2.SymTableCell

### Нод _SymTableCell_Constructor

**Имя нода** = _SymTableCell_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableCell;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymTableCell_ConstructorCast

**Имя нода** = _SymTableCell_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableCell;

**Входные данные**:

* (System.Object)dynamic;

### Нод Text

**Имя нода** = Text;

**Описание** = 0x000004ef;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableCell;

**Входные данные**:

* (System.Object)SymTableCell;

### Нод Set_Text

**Имя нода** = Set_Text;

**Описание** = 0x000004ef;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableCell;

**Входные данные**:

* (System.Object)SymTableCell;
* (System.String)pbstrResult;

### Нод DisplayText

**Имя нода** = DisplayText;

**Описание** = 0x000005f2;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableCell;

**Входные данные**:

* (System.Object)SymTableCell;

### Нод Source

**Имя нода** = Source;

**Описание** = 0x000004ed;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableCell;

**Входные данные**:

* (System.Object)SymTableCell;

### Нод Set_Source

**Имя нода** = Set_Source;

**Описание** = 0x000004ed;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableCell;

**Входные данные**:

* (System.Object)SymTableCell;
* (System.Object)ppResult;

### Нод Width

**Имя нода** = Width;

**Описание** = 0x000004f0;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableCell;

**Входные данные**:

* (System.Object)SymTableCell;

### Нод Height

**Имя нода** = Height;

**Описание** = 0x000004f0;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableCell;

**Входные данные**:

* (System.Object)SymTableCell;

### Нод Bounds

**Имя нода** = Bounds;

**Описание** = 0x000004e7;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableCell;

**Входные данные**:

* (System.Object)SymTableCell;

### Нод Row

**Имя нода** = Row;

**Описание** = 0x000004ec;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableCell;

**Входные данные**:

* (System.Object)SymTableCell;

### Нод Col

**Имя нода** = Col;

**Описание** = 0x000004e8;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableCell;

**Входные данные**:

* (System.Object)SymTableCell;

### Нод Table

**Имя нода** = Table;

**Описание** = 0x000004ee;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableCell;

**Входные данные**:

* (System.Object)SymTableCell;

### Нод Range

**Имя нода** = Range;

**Описание** = 0x000004eb;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableCell;

**Входные данные**:

* (System.Object)SymTableCell;

### Нод Format

**Имя нода** = Format;

**Описание** = 0x000004e9;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableCell;

**Входные данные**:

* (System.Object)SymTableCell;

### Нод Split

**Имя нода** = Split;

**Описание** = 0x000004e6;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTableCell;

**Входные данные**:

* (System.Object)SymTableCell;
* (System.Object)vWidthsArray;
* (System.Object)vHeightsArray;

## Группа NVP_nanoCAD_COM.McCOM2.SymTable

### Нод _SymTable_Constructor

**Имя нода** = _SymTable_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTable;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymTable_ConstructorCast

**Имя нода** = _SymTable_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTable;

**Входные данные**:

* (System.Object)dynamic;

### Нод Cell

**Имя нода** = Cell;

**Описание** = 0x000004ce;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTable;

**Входные данные**:

* (System.Object)SymTable;
* (System.Object)nRow;
* (System.Object)nCol;

### Нод Format

**Имя нода** = Format;

**Описание** = 0x000004d5;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTable;

**Входные данные**:

* (System.Object)SymTable;

### Нод Rows

**Имя нода** = Rows;

**Описание** = 0x000004e1;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTable;

**Входные данные**:

* (System.Object)SymTable;
* (System.Object)Index;

### Нод Cols

**Имя нода** = Cols;

**Описание** = 0x000004d3;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTable;

**Входные данные**:

* (System.Object)SymTable;
* (System.Object)Index;

### Нод RowCount

**Имя нода** = RowCount;

**Описание** = 0x000004e0;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTable;

**Входные данные**:

* (System.Object)SymTable;

### Нод Set_RowCount

**Имя нода** = Set_RowCount;

**Описание** = 0x000004e0;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTable;

**Входные данные**:

* (System.Object)SymTable;
* (System.Object)pnValue;

### Нод ColCount

**Имя нода** = ColCount;

**Описание** = 0x000004d1;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTable;

**Входные данные**:

* (System.Object)SymTable;

### Нод Set_ColCount

**Имя нода** = Set_ColCount;

**Описание** = 0x000004d1;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTable;

**Входные данные**:

* (System.Object)SymTable;
* (System.Object)pnValue;

### Нод Save

**Имя нода** = Save;

**Описание** = 0x000004ca;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTable;

**Входные данные**:

* (System.Object)SymTable;
* (System.Object)vDestination;

### Нод Load

**Имя нода** = Load;

**Описание** = 0x000004c8;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTable;

**Входные данные**:

* (System.Object)SymTable;
* (System.Object)vSource;

### Нод Position

**Имя нода** = Position;

**Описание** = 0x000004de;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTable;

**Входные данные**:

* (System.Object)SymTable;

### Нод Set_Position

**Имя нода** = Set_Position;

**Описание** = 0x000004de;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTable;

**Входные данные**:

* (System.Object)SymTable;
* (System.Object)pvPoint;

### Нод CellFromPoint

**Имя нода** = CellFromPoint;

**Описание** = 0x000004cf;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTable;

**Входные данные**:

* (System.Object)SymTable;
* (System.Object)vPoint;

### Нод Width

**Имя нода** = Width;

**Описание** = 0x000004e4;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTable;

**Входные данные**:

* (System.Object)SymTable;

### Нод Height

**Имя нода** = Height;

**Описание** = 0x000004d6;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTable;

**Входные данные**:

* (System.Object)SymTable;

### Нод Group

**Имя нода** = Group;

**Описание** = 0x000004c6;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTable;

**Входные данные**:

* (System.Object)SymTable;
* (System.Object)iRow;
* (System.Object)iCol;
* (System.Object)iRowCount;
* (System.Object)iColCount;

### Нод Ungroup

**Имя нода** = Ungroup;

**Описание** = 0x000004cc;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTable;

**Входные данные**:

* (System.Object)SymTable;
* (System.Object)iRow;
* (System.Object)iCol;
* (System.Object)iRowCount;
* (System.Object)iColCount;

### Нод Insert

**Имя нода** = Insert;

**Описание** = 0x00000610;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymTable;

**Входные данные**:

* (System.Object)SymTable;
* (System.Object)pSubtable;
* (System.Object)iRowFrom;
* (System.Object)iColFrom;
* (System.Object)McCOM2.SymSubtableMode;

## Группа NVP_nanoCAD_COM.McCOM2.SymSpdsRange

### Нод _SymSpdsRange_Constructor

**Имя нода** = _SymSpdsRange_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsRange;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymSpdsRange_ConstructorCast

**Имя нода** = _SymSpdsRange_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsRange;

**Входные данные**:

* (System.Object)dynamic;

### Нод BasePosition

**Имя нода** = BasePosition;

**Описание** = 0x000004b0;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsRange;

**Входные данные**:

* (System.Object)SymSpdsRange;

### Нод Set_BasePosition

**Имя нода** = Set_BasePosition;

**Описание** = 0x000004b0;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsRange;

**Входные данные**:

* (System.Object)SymSpdsRange;
* (System.Object)pvPoint;

### Нод Direction

**Имя нода** = Direction;

**Описание** = 0x000004b5;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsRange;

**Входные данные**:

* (System.Object)SymSpdsRange;

### Нод Set_Direction

**Имя нода** = Set_Direction;

**Описание** = 0x000004b5;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsRange;

**Входные данные**:

* (System.Object)SymSpdsRange;
* (System.Object)pvVector;

### Нод Upper

**Имя нода** = Upper;

**Описание** = 0x000004bf;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsRange;

**Входные данные**:

* (System.Object)SymSpdsRange;

### Нод Set_Upper

**Имя нода** = Set_Upper;

**Описание** = 0x000004bf;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsRange;

**Входные данные**:

* (System.Object)SymSpdsRange;
* (System.Object)pvValue;

### Нод Lower

**Имя нода** = Lower;

**Описание** = 0x000004bb;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsRange;

**Входные данные**:

* (System.Object)SymSpdsRange;

### Нод Set_Lower

**Имя нода** = Set_Lower;

**Описание** = 0x000004bb;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsRange;

**Входные данные**:

* (System.Object)SymSpdsRange;
* (System.Object)pvValue;

## Группа NVP_nanoCAD_COM.McCOM2.SymSpdsPosition

### Нод _SymSpdsPosition_Constructor

**Имя нода** = _SymSpdsPosition_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsPosition;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymSpdsPosition_ConstructorCast

**Имя нода** = _SymSpdsPosition_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsPosition;

**Входные данные**:

* (System.Object)dynamic;

### Нод Text

**Имя нода** = Text;

**Описание** = 0x000004a5;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsPosition;

**Входные данные**:

* (System.Object)SymSpdsPosition;

### Нод Set_Text

**Имя нода** = Set_Text;

**Описание** = 0x000004a5;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsPosition;

**Входные данные**:

* (System.Object)SymSpdsPosition;
* (System.String)pvValue;

### Нод TextPosition

**Имя нода** = TextPosition;

**Описание** = 0x000004a6;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsPosition;

**Входные данные**:

* (System.Object)SymSpdsPosition;

### Нод Set_TextPosition

**Имя нода** = Set_TextPosition;

**Описание** = 0x000004a6;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsPosition;

**Входные данные**:

* (System.Object)SymSpdsPosition;
* (System.Object)pvPoint;

### Нод Position

**Имя нода** = Position;

**Описание** = 0x000004a3;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsPosition;

**Входные данные**:

* (System.Object)SymSpdsPosition;

### Нод Set_Position

**Имя нода** = Set_Position;

**Описание** = 0x000004a3;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsPosition;

**Входные данные**:

* (System.Object)SymSpdsPosition;
* (System.Object)pvPoint;

### Нод Arrow

**Имя нода** = Arrow;

**Описание** = 0x00000497;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsPosition;

**Входные данные**:

* (System.Object)SymSpdsPosition;

### Нод Set_Arrow

**Имя нода** = Set_Arrow;

**Описание** = 0x00000497;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsPosition;

**Входные данные**:

* (System.Object)SymSpdsPosition;
* (System.Object)McCOM2.ArrowType;

## Группа NVP_nanoCAD_COM.McCOM2.SymSpdsPolymorph

### Нод _SymSpdsPolymorph_Constructor

**Имя нода** = _SymSpdsPolymorph_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsPolymorph;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymSpdsPolymorph_ConstructorCast

**Имя нода** = _SymSpdsPolymorph_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsPolymorph;

**Входные данные**:

* (System.Object)dynamic;

### Нод Text

**Имя нода** = Text;

**Описание** = 0x0000048d;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsPolymorph;

**Входные данные**:

* (System.Object)SymSpdsPolymorph;

### Нод Set_Text

**Имя нода** = Set_Text;

**Описание** = 0x0000048d;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsPolymorph;

**Входные данные**:

* (System.Object)SymSpdsPolymorph;
* (System.String)pbstrText;

### Нод Position

**Имя нода** = Position;

**Описание** = 0x0000048a;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsPolymorph;

**Входные данные**:

* (System.Object)SymSpdsPolymorph;

### Нод Set_Position

**Имя нода** = Set_Position;

**Описание** = 0x0000048a;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsPolymorph;

**Входные данные**:

* (System.Object)SymSpdsPolymorph;
* (System.Object)pvPoint;

### Нод Shape

**Имя нода** = Shape;

**Описание** = 0x0000048c;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsPolymorph;

**Входные данные**:

* (System.Object)SymSpdsPolymorph;

### Нод Set_Shape

**Имя нода** = Set_Shape;

**Описание** = 0x0000048c;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsPolymorph;

**Входные данные**:

* (System.Object)SymSpdsPolymorph;
* (System.Object)McCOM2.PolymorphShape;

## Группа NVP_nanoCAD_COM.McCOM2.SymSpdsPlaneLevel

### Нод _SymSpdsPlaneLevel_Constructor

**Имя нода** = _SymSpdsPlaneLevel_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsPlaneLevel;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymSpdsPlaneLevel_ConstructorCast

**Имя нода** = _SymSpdsPlaneLevel_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsPlaneLevel;

**Входные данные**:

* (System.Object)dynamic;

### Нод Value

**Имя нода** = Value;

**Описание** = 0x00000475;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsPlaneLevel;

**Входные данные**:

* (System.Object)SymSpdsPlaneLevel;

### Нод Set_Value

**Имя нода** = Set_Value;

**Описание** = 0x00000475;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsPlaneLevel;

**Входные данные**:

* (System.Object)SymSpdsPlaneLevel;
* (System.Object)pvValue;

### Нод Position

**Имя нода** = Position;

**Описание** = 0x00000472;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsPlaneLevel;

**Входные данные**:

* (System.Object)SymSpdsPlaneLevel;

### Нод Set_Position

**Имя нода** = Set_Position;

**Описание** = 0x00000472;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsPlaneLevel;

**Входные данные**:

* (System.Object)SymSpdsPlaneLevel;
* (System.Object)pvPoint;

### Нод Shape

**Имя нода** = Shape;

**Описание** = 0x00000474;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsPlaneLevel;

**Входные данные**:

* (System.Object)SymSpdsPlaneLevel;

### Нод Set_Shape

**Имя нода** = Set_Shape;

**Описание** = 0x00000474;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsPlaneLevel;

**Входные данные**:

* (System.Object)SymSpdsPlaneLevel;
* (System.Object)McCOM2.PlaneLevelShape;

### Нод Leaders

**Имя нода** = Leaders;

**Описание** = 0x0000046c;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsPlaneLevel;

**Входные данные**:

* (System.Object)SymSpdsPlaneLevel;

## Группа NVP_nanoCAD_COM.McCOM2.SymSpdsNoteSecant

### Нод _SymSpdsNoteSecant_Constructor

**Имя нода** = _SymSpdsNoteSecant_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteSecant;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymSpdsNoteSecant_ConstructorCast

**Имя нода** = _SymSpdsNoteSecant_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteSecant;

**Входные данные**:

* (System.Object)dynamic;

### Нод TextPosition

**Имя нода** = TextPosition;

**Описание** = 0x0000045c;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteSecant;

**Входные данные**:

* (System.Object)SymSpdsNoteSecant;

### Нод Set_TextPosition

**Имя нода** = Set_TextPosition;

**Описание** = 0x0000045c;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteSecant;

**Входные данные**:

* (System.Object)SymSpdsNoteSecant;
* (System.Object)pvResult;

### Нод Align

**Имя нода** = Align;

**Описание** = 0x00000449;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteSecant;

**Входные данные**:

* (System.Object)SymSpdsNoteSecant;

### Нод Set_Align

**Имя нода** = Set_Align;

**Описание** = 0x00000449;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteSecant;

**Входные данные**:

* (System.Object)SymSpdsNoteSecant;
* (System.Object)McCOM2.TextAlignment;

### Нод Start

**Имя нода** = Start;

**Описание** = 0x0000045b;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteSecant;

**Входные данные**:

* (System.Object)SymSpdsNoteSecant;

### Нод Set_Start

**Имя нода** = Set_Start;

**Описание** = 0x0000045b;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteSecant;

**Входные данные**:

* (System.Object)SymSpdsNoteSecant;
* (System.Object)pvResult;

### Нод End

**Имя нода** = End;

**Описание** = 0x0000044f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteSecant;

**Входные данные**:

* (System.Object)SymSpdsNoteSecant;

### Нод Set_End

**Имя нода** = Set_End;

**Описание** = 0x0000044f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteSecant;

**Входные данные**:

* (System.Object)SymSpdsNoteSecant;
* (System.Object)pvResult;

### Нод Direction

**Имя нода** = Direction;

**Описание** = 0x0000044e;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteSecant;

**Входные данные**:

* (System.Object)SymSpdsNoteSecant;

### Нод Set_Direction

**Имя нода** = Set_Direction;

**Описание** = 0x0000044e;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteSecant;

**Входные данные**:

* (System.Object)SymSpdsNoteSecant;
* (System.Double)prResult;

### Нод Index

**Имя нода** = Index;

**Описание** = 0x00000452;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteSecant;

**Входные данные**:

* (System.Object)SymSpdsNoteSecant;

### Нод Set_Index

**Имя нода** = Set_Index;

**Описание** = 0x00000452;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteSecant;

**Входные данные**:

* (System.Object)SymSpdsNoteSecant;
* (System.String)pbstrResult;

### Нод Footer

**Имя нода** = Footer;

**Описание** = 0x00000450;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteSecant;

**Входные данные**:

* (System.Object)SymSpdsNoteSecant;

### Нод Set_Footer

**Имя нода** = Set_Footer;

**Описание** = 0x00000450;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteSecant;

**Входные данные**:

* (System.Object)SymSpdsNoteSecant;
* (System.String)pbstrResult;

### Нод Sheet

**Имя нода** = Sheet;

**Описание** = 0x0000045a;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteSecant;

**Входные данные**:

* (System.Object)SymSpdsNoteSecant;

### Нод Set_Sheet

**Имя нода** = Set_Sheet;

**Описание** = 0x0000045a;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteSecant;

**Входные данные**:

* (System.Object)SymSpdsNoteSecant;
* (System.String)pbstrResult;

## Группа NVP_nanoCAD_COM.McCOM2.SymSpdsNotePosition

### Нод _SymSpdsNotePosition_Constructor

**Имя нода** = _SymSpdsNotePosition_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNotePosition;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymSpdsNotePosition_ConstructorCast

**Имя нода** = _SymSpdsNotePosition_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNotePosition;

**Входные данные**:

* (System.Object)dynamic;

### Нод Text

**Имя нода** = Text;

**Описание** = 0x0000043e;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNotePosition;

**Входные данные**:

* (System.Object)SymSpdsNotePosition;

### Нод Set_Text

**Имя нода** = Set_Text;

**Описание** = 0x0000043e;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNotePosition;

**Входные данные**:

* (System.Object)SymSpdsNotePosition;
* (System.String)vbstrResult;

### Нод Footer

**Имя нода** = Footer;

**Описание** = 0x00000434;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNotePosition;

**Входные данные**:

* (System.Object)SymSpdsNotePosition;

### Нод Set_Footer

**Имя нода** = Set_Footer;

**Описание** = 0x00000434;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNotePosition;

**Входные данные**:

* (System.Object)SymSpdsNotePosition;
* (System.String)vbstrResult;

### Нод Align

**Имя нода** = Align;

**Описание** = 0x0000042e;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNotePosition;

**Входные данные**:

* (System.Object)SymSpdsNotePosition;

### Нод Set_Align

**Имя нода** = Set_Align;

**Описание** = 0x0000042e;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNotePosition;

**Входные данные**:

* (System.Object)SymSpdsNotePosition;
* (System.Object)McCOM2.TextAlignment;

### Нод TextPosition

**Имя нода** = TextPosition;

**Описание** = 0x0000043f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNotePosition;

**Входные данные**:

* (System.Object)SymSpdsNotePosition;

### Нод Set_TextPosition

**Имя нода** = Set_TextPosition;

**Описание** = 0x0000043f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNotePosition;

**Входные данные**:

* (System.Object)SymSpdsNotePosition;
* (System.Object)pvResult;

### Нод Direction

**Имя нода** = Direction;

**Описание** = 0x00000433;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNotePosition;

**Входные данные**:

* (System.Object)SymSpdsNotePosition;

### Нод Set_Direction

**Имя нода** = Set_Direction;

**Описание** = 0x00000433;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNotePosition;

**Входные данные**:

* (System.Object)SymSpdsNotePosition;
* (System.Double)prResult;

### Нод Leaders

**Имя нода** = Leaders;

**Описание** = 0x00000437;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNotePosition;

**Входные данные**:

* (System.Object)SymSpdsNotePosition;

## Группа NVP_nanoCAD_COM.McCOM2.SymSpdsNoteMult

### Нод _SymSpdsNoteMult_Constructor

**Имя нода** = _SymSpdsNoteMult_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteMult;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymSpdsNoteMult_ConstructorCast

**Имя нода** = _SymSpdsNoteMult_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteMult;

**Входные данные**:

* (System.Object)dynamic;

### Нод Position

**Имя нода** = Position;

**Описание** = 0x00000421;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteMult;

**Входные данные**:

* (System.Object)SymSpdsNoteMult;

### Нод Set_Position

**Имя нода** = Set_Position;

**Описание** = 0x00000421;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteMult;

**Входные данные**:

* (System.Object)SymSpdsNoteMult;
* (System.Object)pvPoint;

### Нод TextPosition

**Имя нода** = TextPosition;

**Описание** = 0x00000424;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteMult;

**Входные данные**:

* (System.Object)SymSpdsNoteMult;

### Нод Set_TextPosition

**Имя нода** = Set_TextPosition;

**Описание** = 0x00000424;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteMult;

**Входные данные**:

* (System.Object)SymSpdsNoteMult;
* (System.Object)pvPoint;

### Нод Arrow

**Имя нода** = Arrow;

**Описание** = 0x00000410;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteMult;

**Входные данные**:

* (System.Object)SymSpdsNoteMult;

### Нод Set_Arrow

**Имя нода** = Set_Arrow;

**Описание** = 0x00000410;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteMult;

**Входные данные**:

* (System.Object)SymSpdsNoteMult;
* (System.Object)McCOM2.ArrowType;

### Нод Text

**Имя нода** = Text;

**Описание** = 0x00000423;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteMult;

**Входные данные**:

* (System.Object)SymSpdsNoteMult;

### Нод Set_Text

**Имя нода** = Set_Text;

**Описание** = 0x00000423;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteMult;

**Входные данные**:

* (System.Object)SymSpdsNoteMult;
* (System.String)vbstrResult;

### Нод Lines

**Имя нода** = Lines;

**Описание** = 0x0000041a;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteMult;

**Входные данные**:

* (System.Object)SymSpdsNoteMult;
* (System.Object)Index;

### Нод Set_Lines

**Имя нода** = Set_Lines;

**Описание** = 0x0000041a;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteMult;

**Входные данные**:

* (System.Object)SymSpdsNoteMult;
* (System.Object)Index;
* (System.String)vbstrResult;

### Нод LinesCount

**Имя нода** = LinesCount;

**Описание** = 0x0000041b;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteMult;

**Входные данные**:

* (System.Object)SymSpdsNoteMult;

### Нод Set_LinesCount

**Имя нода** = Set_LinesCount;

**Описание** = 0x0000041b;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteMult;

**Входные данные**:

* (System.Object)SymSpdsNoteMult;
* (System.Object)pnResult;

### Нод Align

**Имя нода** = Align;

**Описание** = 0x0000040f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteMult;

**Входные данные**:

* (System.Object)SymSpdsNoteMult;
* (System.Object)Index;

### Нод Set_Align

**Имя нода** = Set_Align;

**Описание** = 0x0000040f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteMult;

**Входные данные**:

* (System.Object)SymSpdsNoteMult;
* (System.Object)Index;
* (System.Object)McCOM2.TextAlignment;

### Нод Direction

**Имя нода** = Direction;

**Описание** = 0x00000415;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteMult;

**Входные данные**:

* (System.Object)SymSpdsNoteMult;

### Нод Set_Direction

**Имя нода** = Set_Direction;

**Описание** = 0x00000415;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteMult;

**Входные данные**:

* (System.Object)SymSpdsNoteMult;
* (System.Double)prResult;

### Нод Grow

**Имя нода** = Grow;

**Описание** = 0x00000416;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteMult;

**Входные данные**:

* (System.Object)SymSpdsNoteMult;

### Нод Set_Grow

**Имя нода** = Set_Grow;

**Описание** = 0x00000416;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteMult;

**Входные данные**:

* (System.Object)SymSpdsNoteMult;
* (System.Object)pvbValue;

### Нод Line

**Имя нода** = Line;

**Описание** = 0x00000419;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteMult;

**Входные данные**:

* (System.Object)SymSpdsNoteMult;

### Нод Set_Line

**Имя нода** = Set_Line;

**Описание** = 0x00000419;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteMult;

**Входные данные**:

* (System.Object)SymSpdsNoteMult;
* (System.Object)pvbValue;

## Группа NVP_nanoCAD_COM.McCOM2.SymSpdsNoteKnot

### Нод _SymSpdsNoteKnot_Constructor

**Имя нода** = _SymSpdsNoteKnot_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteKnot;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymSpdsNoteKnot_ConstructorCast

**Имя нода** = _SymSpdsNoteKnot_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteKnot;

**Входные данные**:

* (System.Object)dynamic;

### Нод Position

**Имя нода** = Position;

**Описание** = 0x000003fe;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteKnot;

**Входные данные**:

* (System.Object)SymSpdsNoteKnot;

### Нод Set_Position

**Имя нода** = Set_Position;

**Описание** = 0x000003fe;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteKnot;

**Входные данные**:

* (System.Object)SymSpdsNoteKnot;
* (System.Object)pvPoint;

### Нод TextPosition

**Имя нода** = TextPosition;

**Описание** = 0x00000405;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteKnot;

**Входные данные**:

* (System.Object)SymSpdsNoteKnot;

### Нод Set_TextPosition

**Имя нода** = Set_TextPosition;

**Описание** = 0x00000405;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteKnot;

**Входные данные**:

* (System.Object)SymSpdsNoteKnot;
* (System.Object)pvPoint;

### Нод Align

**Имя нода** = Align;

**Описание** = 0x000003f0;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteKnot;

**Входные данные**:

* (System.Object)SymSpdsNoteKnot;

### Нод Set_Align

**Имя нода** = Set_Align;

**Описание** = 0x000003f0;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteKnot;

**Входные данные**:

* (System.Object)SymSpdsNoteKnot;
* (System.Object)McCOM2.TextAlignment;

### Нод Text

**Имя нода** = Text;

**Описание** = 0x00000404;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteKnot;

**Входные данные**:

* (System.Object)SymSpdsNoteKnot;

### Нод Set_Text

**Имя нода** = Set_Text;

**Описание** = 0x00000404;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteKnot;

**Входные данные**:

* (System.Object)SymSpdsNoteKnot;
* (System.String)pbstrResult;

### Нод Direction

**Имя нода** = Direction;

**Описание** = 0x000003f5;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteKnot;

**Входные данные**:

* (System.Object)SymSpdsNoteKnot;

### Нод Set_Direction

**Имя нода** = Set_Direction;

**Описание** = 0x000003f5;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteKnot;

**Входные данные**:

* (System.Object)SymSpdsNoteKnot;
* (System.Double)prResult;

### Нод Footer

**Имя нода** = Footer;

**Описание** = 0x000003f6;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteKnot;

**Входные данные**:

* (System.Object)SymSpdsNoteKnot;

### Нод Set_Footer

**Имя нода** = Set_Footer;

**Описание** = 0x000003f6;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteKnot;

**Входные данные**:

* (System.Object)SymSpdsNoteKnot;
* (System.String)pbstrResult;

### Нод RadiusPerp

**Имя нода** = RadiusPerp;

**Описание** = 0x00000401;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteKnot;

**Входные данные**:

* (System.Object)SymSpdsNoteKnot;

### Нод Set_RadiusPerp

**Имя нода** = Set_RadiusPerp;

**Описание** = 0x00000401;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteKnot;

**Входные данные**:

* (System.Object)SymSpdsNoteKnot;
* (System.Double)prResult;

### Нод RadiusRef

**Имя нода** = RadiusRef;

**Описание** = 0x00000402;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteKnot;

**Входные данные**:

* (System.Object)SymSpdsNoteKnot;

### Нод Set_RadiusRef

**Имя нода** = Set_RadiusRef;

**Описание** = 0x00000402;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteKnot;

**Входные данные**:

* (System.Object)SymSpdsNoteKnot;
* (System.Double)prResult;

### Нод RadiusDirection

**Имя нода** = RadiusDirection;

**Описание** = 0x00000400;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteKnot;

**Входные данные**:

* (System.Object)SymSpdsNoteKnot;

### Нод Set_RadiusDirection

**Имя нода** = Set_RadiusDirection;

**Описание** = 0x00000400;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteKnot;

**Входные данные**:

* (System.Object)SymSpdsNoteKnot;
* (System.Double)prValue;

### Нод Sheet

**Имя нода** = Sheet;

**Описание** = 0x00000403;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteKnot;

**Входные данные**:

* (System.Object)SymSpdsNoteKnot;

### Нод Set_Sheet

**Имя нода** = Set_Sheet;

**Описание** = 0x00000403;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteKnot;

**Входные данные**:

* (System.Object)SymSpdsNoteKnot;
* (System.String)pbstrResult;

## Группа NVP_nanoCAD_COM.McCOM2.SymSpdsNoteComb

### Нод _SymSpdsNoteComb_Constructor

**Имя нода** = _SymSpdsNoteComb_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteComb;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymSpdsNoteComb_ConstructorCast

**Имя нода** = _SymSpdsNoteComb_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteComb;

**Входные данные**:

* (System.Object)dynamic;

### Нод Text

**Имя нода** = Text;

**Описание** = 0x000003e6;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteComb;

**Входные данные**:

* (System.Object)SymSpdsNoteComb;

### Нод Set_Text

**Имя нода** = Set_Text;

**Описание** = 0x000003e6;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteComb;

**Входные данные**:

* (System.Object)SymSpdsNoteComb;
* (System.String)vbstrResult;

### Нод Footer

**Имя нода** = Footer;

**Описание** = 0x000003db;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteComb;

**Входные данные**:

* (System.Object)SymSpdsNoteComb;

### Нод Set_Footer

**Имя нода** = Set_Footer;

**Описание** = 0x000003db;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteComb;

**Входные данные**:

* (System.Object)SymSpdsNoteComb;
* (System.String)vbstrResult;

### Нод Align

**Имя нода** = Align;

**Описание** = 0x000003d4;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteComb;

**Входные данные**:

* (System.Object)SymSpdsNoteComb;

### Нод Set_Align

**Имя нода** = Set_Align;

**Описание** = 0x000003d4;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteComb;

**Входные данные**:

* (System.Object)SymSpdsNoteComb;
* (System.Object)McCOM2.TextAlignment;

### Нод Start

**Имя нода** = Start;

**Описание** = 0x000003e5;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteComb;

**Входные данные**:

* (System.Object)SymSpdsNoteComb;

### Нод Set_Start

**Имя нода** = Set_Start;

**Описание** = 0x000003e5;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteComb;

**Входные данные**:

* (System.Object)SymSpdsNoteComb;
* (System.Object)pvResult;

### Нод End

**Имя нода** = End;

**Описание** = 0x000003da;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteComb;

**Входные данные**:

* (System.Object)SymSpdsNoteComb;

### Нод Set_End

**Имя нода** = Set_End;

**Описание** = 0x000003da;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteComb;

**Входные данные**:

* (System.Object)SymSpdsNoteComb;
* (System.Object)pvResult;

### Нод Direction

**Имя нода** = Direction;

**Описание** = 0x000003d9;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteComb;

**Входные данные**:

* (System.Object)SymSpdsNoteComb;

### Нод Set_Direction

**Имя нода** = Set_Direction;

**Описание** = 0x000003d9;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteComb;

**Входные данные**:

* (System.Object)SymSpdsNoteComb;
* (System.Double)prResult;

### Нод Leaders

**Имя нода** = Leaders;

**Описание** = 0x000003de;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteComb;

**Входные данные**:

* (System.Object)SymSpdsNoteComb;

## Группа NVP_nanoCAD_COM.McCOM2.SymSpdsNoteChain

### Нод _SymSpdsNoteChain_Constructor

**Имя нода** = _SymSpdsNoteChain_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteChain;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymSpdsNoteChain_ConstructorCast

**Имя нода** = _SymSpdsNoteChain_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteChain;

**Входные данные**:

* (System.Object)dynamic;

### Нод Text

**Имя нода** = Text;

**Описание** = 0x000003c9;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteChain;

**Входные данные**:

* (System.Object)SymSpdsNoteChain;

### Нод Set_Text

**Имя нода** = Set_Text;

**Описание** = 0x000003c9;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteChain;

**Входные данные**:

* (System.Object)SymSpdsNoteChain;
* (System.String)vbstrResult;

### Нод Footer

**Имя нода** = Footer;

**Описание** = 0x000003be;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteChain;

**Входные данные**:

* (System.Object)SymSpdsNoteChain;

### Нод Set_Footer

**Имя нода** = Set_Footer;

**Описание** = 0x000003be;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteChain;

**Входные данные**:

* (System.Object)SymSpdsNoteChain;
* (System.String)vbstrResult;

### Нод Align

**Имя нода** = Align;

**Описание** = 0x000003b8;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteChain;

**Входные данные**:

* (System.Object)SymSpdsNoteChain;

### Нод Set_Align

**Имя нода** = Set_Align;

**Описание** = 0x000003b8;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteChain;

**Входные данные**:

* (System.Object)SymSpdsNoteChain;
* (System.Object)McCOM2.TextAlignment;

### Нод TextPosition

**Имя нода** = TextPosition;

**Описание** = 0x000003ca;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteChain;

**Входные данные**:

* (System.Object)SymSpdsNoteChain;

### Нод Set_TextPosition

**Имя нода** = Set_TextPosition;

**Описание** = 0x000003ca;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteChain;

**Входные данные**:

* (System.Object)SymSpdsNoteChain;
* (System.Object)pvResult;

### Нод Direction

**Имя нода** = Direction;

**Описание** = 0x000003bd;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteChain;

**Входные данные**:

* (System.Object)SymSpdsNoteChain;

### Нод Set_Direction

**Имя нода** = Set_Direction;

**Описание** = 0x000003bd;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteChain;

**Входные данные**:

* (System.Object)SymSpdsNoteChain;
* (System.Double)prResult;

### Нод Leaders

**Имя нода** = Leaders;

**Описание** = 0x000003c2;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNoteChain;

**Входные данные**:

* (System.Object)SymSpdsNoteChain;

## Группа NVP_nanoCAD_COM.McCOM2.SymSpdsNode

### Нод _SymSpdsNode_Constructor

**Имя нода** = _SymSpdsNode_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNode;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymSpdsNode_ConstructorCast

**Имя нода** = _SymSpdsNode_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNode;

**Входные данные**:

* (System.Object)dynamic;

### Нод Text

**Имя нода** = Text;

**Описание** = 0x000003ae;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNode;

**Входные данные**:

* (System.Object)SymSpdsNode;

### Нод Set_Text

**Имя нода** = Set_Text;

**Описание** = 0x000003ae;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNode;

**Входные данные**:

* (System.Object)SymSpdsNode;
* (System.Object)pvResult;

### Нод Footer

**Имя нода** = Footer;

**Описание** = 0x000003a4;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNode;

**Входные данные**:

* (System.Object)SymSpdsNode;

### Нод Set_Footer

**Имя нода** = Set_Footer;

**Описание** = 0x000003a4;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNode;

**Входные данные**:

* (System.Object)SymSpdsNode;
* (System.String)pbstrResult;

### Нод Position

**Имя нода** = Position;

**Описание** = 0x000003ac;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNode;

**Входные данные**:

* (System.Object)SymSpdsNode;

### Нод Set_Position

**Имя нода** = Set_Position;

**Описание** = 0x000003ac;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsNode;

**Входные данные**:

* (System.Object)SymSpdsNode;
* (System.Object)pvPoint;

## Группа NVP_nanoCAD_COM.McCOM2.SymSpdsLevel

### Нод _SymSpdsLevel_Constructor

**Имя нода** = _SymSpdsLevel_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsLevel;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymSpdsLevel_ConstructorCast

**Имя нода** = _SymSpdsLevel_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsLevel;

**Входные данные**:

* (System.Object)dynamic;

### Нод Value

**Имя нода** = Value;

**Описание** = 0x00000396;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsLevel;

**Входные данные**:

* (System.Object)SymSpdsLevel;

### Нод Set_Value

**Имя нода** = Set_Value;

**Описание** = 0x00000396;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsLevel;

**Входные данные**:

* (System.Object)SymSpdsLevel;
* (System.Object)pvResult;

### Нод DisplayText

**Имя нода** = DisplayText;

**Описание** = 0x00000388;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsLevel;

**Входные данные**:

* (System.Object)SymSpdsLevel;

### Нод Footer

**Имя нода** = Footer;

**Описание** = 0x00000389;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsLevel;

**Входные данные**:

* (System.Object)SymSpdsLevel;

### Нод Set_Footer

**Имя нода** = Set_Footer;

**Описание** = 0x00000389;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsLevel;

**Входные данные**:

* (System.Object)SymSpdsLevel;
* (System.String)pbstrResult;

### Нод Base

**Имя нода** = Base;

**Описание** = 0x00000383;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsLevel;

**Входные данные**:

* (System.Object)SymSpdsLevel;

### Нод Set_Base

**Имя нода** = Set_Base;

**Описание** = 0x00000383;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsLevel;

**Входные данные**:

* (System.Object)SymSpdsLevel;
* (System.Object)ppBase;

### Нод Origin

**Имя нода** = Origin;

**Описание** = 0x00000392;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsLevel;

**Входные данные**:

* (System.Object)SymSpdsLevel;

### Нод Set_Origin

**Имя нода** = Set_Origin;

**Описание** = 0x00000392;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsLevel;

**Входные данные**:

* (System.Object)SymSpdsLevel;
* (System.Object)pvPoint;

### Нод Position

**Имя нода** = Position;

**Описание** = 0x00000393;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsLevel;

**Входные данные**:

* (System.Object)SymSpdsLevel;

### Нод Set_Position

**Имя нода** = Set_Position;

**Описание** = 0x00000393;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsLevel;

**Входные данные**:

* (System.Object)SymSpdsLevel;
* (System.Object)pvPoint;

### Нод TextPosition

**Имя нода** = TextPosition;

**Описание** = 0x00000395;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsLevel;

**Входные данные**:

* (System.Object)SymSpdsLevel;

### Нод Set_TextPosition

**Имя нода** = Set_TextPosition;

**Описание** = 0x00000395;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsLevel;

**Входные данные**:

* (System.Object)SymSpdsLevel;
* (System.Object)pvPoint;

### Нод Associative

**Имя нода** = Associative;

**Описание** = 0x00000382;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsLevel;

**Входные данные**:

* (System.Object)SymSpdsLevel;

### Нод Set_Associative

**Имя нода** = Set_Associative;

**Описание** = 0x00000382;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsLevel;

**Входные данные**:

* (System.Object)SymSpdsLevel;
* (System.Object)pvbValue;

### Нод Mirrored

**Имя нода** = Mirrored;

**Описание** = 0x0000038f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsLevel;

**Входные данные**:

* (System.Object)SymSpdsLevel;

### Нод Set_Mirrored

**Имя нода** = Set_Mirrored;

**Описание** = 0x0000038f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsLevel;

**Входные данные**:

* (System.Object)SymSpdsLevel;
* (System.Object)pvbValue;

### Нод AffectsTo

**Имя нода** = AffectsTo;

**Описание** = 0x0000037a;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsLevel;

**Входные данные**:

* (System.Object)SymSpdsLevel;

## Группа NVP_nanoCAD_COM.McCOM2.SymSpdsGrid

### Нод _SymSpdsGrid_Constructor

**Имя нода** = _SymSpdsGrid_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsGrid;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymSpdsGrid_ConstructorCast

**Имя нода** = _SymSpdsGrid_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsGrid;

**Входные данные**:

* (System.Object)dynamic;

### Нод AxesX

**Имя нода** = AxesX;

**Описание** = 0x00000600;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsGrid;

**Входные данные**:

* (System.Object)SymSpdsGrid;

### Нод AxesY

**Имя нода** = AxesY;

**Описание** = 0x00000601;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsGrid;

**Входные данные**:

* (System.Object)SymSpdsGrid;

### Нод Polar

**Имя нода** = Polar;

**Описание** = 0x00000602;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsGrid;

**Входные данные**:

* (System.Object)SymSpdsGrid;

### Нод Set_Polar

**Имя нода** = Set_Polar;

**Описание** = 0x00000602;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsGrid;

**Входные данные**:

* (System.Object)SymSpdsGrid;
* (System.Object)pvbValue;

## Группа NVP_nanoCAD_COM.McCOM2.SymSpdsFragment

### Нод _SymSpdsFragment_Constructor

**Имя нода** = _SymSpdsFragment_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsFragment;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymSpdsFragment_ConstructorCast

**Имя нода** = _SymSpdsFragment_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsFragment;

**Входные данные**:

* (System.Object)dynamic;

### Нод Text

**Имя нода** = Text;

**Описание** = 0x00000376;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsFragment;

**Входные данные**:

* (System.Object)SymSpdsFragment;

### Нод Set_Text

**Имя нода** = Set_Text;

**Описание** = 0x00000376;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsFragment;

**Входные данные**:

* (System.Object)SymSpdsFragment;
* (System.String)pbstrValue;

### Нод Begin

**Имя нода** = Begin;

**Описание** = 0x00000367;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsFragment;

**Входные данные**:

* (System.Object)SymSpdsFragment;

### Нод Set_Begin

**Имя нода** = Set_Begin;

**Описание** = 0x00000367;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsFragment;

**Входные данные**:

* (System.Object)SymSpdsFragment;
* (System.Object)pvPoint;

### Нод End

**Имя нода** = End;

**Описание** = 0x0000036c;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsFragment;

**Входные данные**:

* (System.Object)SymSpdsFragment;

### Нод Set_End

**Имя нода** = Set_End;

**Описание** = 0x0000036c;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsFragment;

**Входные данные**:

* (System.Object)SymSpdsFragment;
* (System.Object)pvPoint;

### Нод Peak

**Имя нода** = Peak;

**Описание** = 0x00000374;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsFragment;

**Входные данные**:

* (System.Object)SymSpdsFragment;

### Нод Set_Peak

**Имя нода** = Set_Peak;

**Описание** = 0x00000374;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsFragment;

**Входные данные**:

* (System.Object)SymSpdsFragment;
* (System.Object)pvPoint;

### Нод TextPosition

**Имя нода** = TextPosition;

**Описание** = 0x00000377;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsFragment;

**Входные данные**:

* (System.Object)SymSpdsFragment;

### Нод Set_TextPosition

**Имя нода** = Set_TextPosition;

**Описание** = 0x00000377;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsFragment;

**Входные данные**:

* (System.Object)SymSpdsFragment;
* (System.Object)pvPoint;

## Группа NVP_nanoCAD_COM.McCOM2.SymSpdsCorrection

### Нод _SymSpdsCorrection_Constructor

**Имя нода** = _SymSpdsCorrection_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsCorrection;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymSpdsCorrection_ConstructorCast

**Имя нода** = _SymSpdsCorrection_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsCorrection;

**Входные данные**:

* (System.Object)dynamic;

### Нод Text

**Имя нода** = Text;

**Описание** = 0x0000035d;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsCorrection;

**Входные данные**:

* (System.Object)SymSpdsCorrection;

### Нод Set_Text

**Имя нода** = Set_Text;

**Описание** = 0x0000035d;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsCorrection;

**Входные данные**:

* (System.Object)SymSpdsCorrection;
* (System.String)pbstrResult;

### Нод Footer

**Имя нода** = Footer;

**Описание** = 0x00000353;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsCorrection;

**Входные данные**:

* (System.Object)SymSpdsCorrection;

### Нод Set_Footer

**Имя нода** = Set_Footer;

**Описание** = 0x00000353;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsCorrection;

**Входные данные**:

* (System.Object)SymSpdsCorrection;
* (System.String)pbstrResult;

### Нод Leaders

**Имя нода** = Leaders;

**Описание** = 0x00000356;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsCorrection;

**Входные данные**:

* (System.Object)SymSpdsCorrection;

## Группа NVP_nanoCAD_COM.McCOM2.SymSpdsBoundForm

### Нод _SymSpdsBoundForm_Constructor

**Имя нода** = _SymSpdsBoundForm_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsBoundForm;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymSpdsBoundForm_ConstructorCast

**Имя нода** = _SymSpdsBoundForm_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsBoundForm;

**Входные данные**:

* (System.Object)dynamic;

### Нод Type

**Имя нода** = Type;

**Описание** = 0x00000345;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsBoundForm;

**Входные данные**:

* (System.Object)SymSpdsBoundForm;

### Нод Set_Type

**Имя нода** = Set_Type;

**Описание** = 0x00000345;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsBoundForm;

**Входные данные**:

* (System.Object)SymSpdsBoundForm;
* (System.Object)McCOM2.SymBoundFormType;

### Нод Direction

**Имя нода** = Direction;

**Описание** = 0x0000033b;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsBoundForm;

**Входные данные**:

* (System.Object)SymSpdsBoundForm;

### Нод Set_Direction

**Имя нода** = Set_Direction;

**Описание** = 0x0000033b;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsBoundForm;

**Входные данные**:

* (System.Object)SymSpdsBoundForm;
* (System.Object)pnValue;

## Группа NVP_nanoCAD_COM.McCOM2.SymSpdsAxis

### Нод _SymSpdsAxis_Constructor

**Имя нода** = _SymSpdsAxis_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsAxis;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymSpdsAxis_ConstructorCast

**Имя нода** = _SymSpdsAxis_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsAxis;

**Входные данные**:

* (System.Object)dynamic;

### Нод Index

**Имя нода** = Index;

**Описание** = 0x00000604;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsAxis;

**Входные данные**:

* (System.Object)SymSpdsAxis;

### Нод Set_Index

**Имя нода** = Set_Index;

**Описание** = 0x00000604;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsAxis;

**Входные данные**:

* (System.Object)SymSpdsAxis;
* (System.String)pbstrValue;

### Нод TextPosition

**Имя нода** = TextPosition;

**Описание** = 0x00000605;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsAxis;

**Входные данные**:

* (System.Object)SymSpdsAxis;

### Нод Set_TextPosition

**Имя нода** = Set_TextPosition;

**Описание** = 0x00000605;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsAxis;

**Входные данные**:

* (System.Object)SymSpdsAxis;
* (System.Object)pvPoint;

### Нод Position

**Имя нода** = Position;

**Описание** = 0x00000606;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsAxis;

**Входные данные**:

* (System.Object)SymSpdsAxis;

### Нод Set_Position

**Имя нода** = Set_Position;

**Описание** = 0x00000606;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsAxis;

**Входные данные**:

* (System.Object)SymSpdsAxis;
* (System.Object)pvPoint;

## Группа NVP_nanoCAD_COM.McCOM2.SymSpdsAnchor

### Нод _SymSpdsAnchor_Constructor

**Имя нода** = _SymSpdsAnchor_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsAnchor;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymSpdsAnchor_ConstructorCast

**Имя нода** = _SymSpdsAnchor_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsAnchor;

**Входные данные**:

* (System.Object)dynamic;

### Нод ArrowFirst

**Имя нода** = ArrowFirst;

**Описание** = 0x00000319;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsAnchor;

**Входные данные**:

* (System.Object)SymSpdsAnchor;

### Нод Set_ArrowFirst

**Имя нода** = Set_ArrowFirst;

**Описание** = 0x00000319;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsAnchor;

**Входные данные**:

* (System.Object)SymSpdsAnchor;
* (System.Object)McCOM2.SymAnchorType;

### Нод ArrowLast

**Имя нода** = ArrowLast;

**Описание** = 0x0000031a;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsAnchor;

**Входные данные**:

* (System.Object)SymSpdsAnchor;

### Нод Set_ArrowLast

**Имя нода** = Set_ArrowLast;

**Описание** = 0x0000031a;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSpdsAnchor;

**Входные данные**:

* (System.Object)SymSpdsAnchor;
* (System.Object)McCOM2.SymAnchorType;

## Группа NVP_nanoCAD_COM.McCOM2.SymSection

### Нод _SymSection_Constructor

**Имя нода** = _SymSection_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSection;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymSection_ConstructorCast

**Имя нода** = _SymSection_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSection;

**Входные данные**:

* (System.Object)dynamic;

### Нод Edges

**Имя нода** = Edges;

**Описание** = 0x00000302;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSection;

**Входные данные**:

* (System.Object)SymSection;

### Нод Set_Edges

**Имя нода** = Set_Edges;

**Описание** = 0x00000302;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSection;

**Входные данные**:

* (System.Object)SymSection;
* (System.Object)pvPointArray;

### Нод Direction

**Имя нода** = Direction;

**Описание** = 0x00000301;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSection;

**Входные данные**:

* (System.Object)SymSection;

### Нод Set_Direction

**Имя нода** = Set_Direction;

**Описание** = 0x00000301;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSection;

**Входные данные**:

* (System.Object)SymSection;
* (System.Object)pvVector;

### Нод Cut

**Имя нода** = Cut;

**Описание** = 0x000002ff;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSection;

**Входные данные**:

* (System.Object)SymSection;

### Нод Set_Cut

**Имя нода** = Set_Cut;

**Описание** = 0x000002ff;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSection;

**Входные данные**:

* (System.Object)SymSection;
* (System.Object)pvbResult;

### Нод Prefix

**Имя нода** = Prefix;

**Описание** = 0x0000030b;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSection;

**Входные данные**:

* (System.Object)SymSection;

### Нод Set_Prefix

**Имя нода** = Set_Prefix;

**Описание** = 0x0000030b;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSection;

**Входные данные**:

* (System.Object)SymSection;
* (System.String)pbstrResult;

### Нод Text

**Имя нода** = Text;

**Описание** = 0x0000030e;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSection;

**Входные данные**:

* (System.Object)SymSection;

### Нод Set_Text

**Имя нода** = Set_Text;

**Описание** = 0x0000030e;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSection;

**Входные данные**:

* (System.Object)SymSection;
* (System.String)pbstrResult;

### Нод Index

**Имя нода** = Index;

**Описание** = 0x00000304;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSection;

**Входные данные**:

* (System.Object)SymSection;

### Нод Set_Index

**Имя нода** = Set_Index;

**Описание** = 0x00000304;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSection;

**Входные данные**:

* (System.Object)SymSection;
* (System.String)pbstrResult;

### Нод Zone

**Имя нода** = Zone;

**Описание** = 0x00000311;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSection;

**Входные данные**:

* (System.Object)SymSection;

### Нод Set_Zone

**Имя нода** = Set_Zone;

**Описание** = 0x00000311;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSection;

**Входные данные**:

* (System.Object)SymSection;
* (System.String)pbstrResult;

### Нод Sheet

**Имя нода** = Sheet;

**Описание** = 0x0000030d;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSection;

**Входные данные**:

* (System.Object)SymSection;

### Нод Set_Sheet

**Имя нода** = Set_Sheet;

**Описание** = 0x0000030d;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymSection;

**Входные данные**:

* (System.Object)SymSection;
* (System.Object)pnResult;

## Группа NVP_nanoCAD_COM.McCOM2.SymRoughness

### Нод _SymRoughness_Constructor

**Имя нода** = _SymRoughness_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymRoughness;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymRoughness_ConstructorCast

**Имя нода** = _SymRoughness_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymRoughness;

**Входные данные**:

* (System.Object)dynamic;

### Нод TextPosition

**Имя нода** = TextPosition;

**Описание** = 0x000002f2;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymRoughness;

**Входные данные**:

* (System.Object)SymRoughness;

### Нод Set_TextPosition

**Имя нода** = Set_TextPosition;

**Описание** = 0x000002f2;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymRoughness;

**Входные данные**:

* (System.Object)SymRoughness;
* (System.Object)pvPoint;

### Нод Angle

**Имя нода** = Angle;

**Описание** = 0x000002dc;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymRoughness;

**Входные данные**:

* (System.Object)SymRoughness;

### Нод Set_Angle

**Имя нода** = Set_Angle;

**Описание** = 0x000002dc;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymRoughness;

**Входные данные**:

* (System.Object)SymRoughness;
* (System.Double)prValue;

### Нод Direction

**Имя нода** = Direction;

**Описание** = 0x000002e3;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymRoughness;

**Входные данные**:

* (System.Object)SymRoughness;

### Нод Set_Direction

**Имя нода** = Set_Direction;

**Описание** = 0x000002e3;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymRoughness;

**Входные данные**:

* (System.Object)SymRoughness;
* (System.Object)prDirection;

### Нод Technology

**Имя нода** = Technology;

**Описание** = 0x000002f1;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymRoughness;

**Входные данные**:

* (System.Object)SymRoughness;

### Нод Set_Technology

**Имя нода** = Set_Technology;

**Описание** = 0x000002f1;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymRoughness;

**Входные данные**:

* (System.Object)SymRoughness;
* (System.String)pbstrValue;

### Нод Parameter

**Имя нода** = Parameter;

**Описание** = 0x000002ed;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymRoughness;

**Входные данные**:

* (System.Object)SymRoughness;
* (System.Object)Index;

### Нод Set_Parameter

**Имя нода** = Set_Parameter;

**Описание** = 0x000002ed;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymRoughness;

**Входные данные**:

* (System.Object)SymRoughness;
* (System.Object)Index;
* (System.String)pbstrValue;

### Нод Default

**Имя нода** = Default;

**Описание** = 0x000002e1;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymRoughness;

**Входные данные**:

* (System.Object)SymRoughness;

### Нод Set_Default

**Имя нода** = Set_Default;

**Описание** = 0x000002e1;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymRoughness;

**Входные данные**:

* (System.Object)SymRoughness;
* (System.Object)pvbValue;

### Нод Closed

**Имя нода** = Closed;

**Описание** = 0x000002df;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymRoughness;

**Входные данные**:

* (System.Object)SymRoughness;

### Нод Set_Closed

**Имя нода** = Set_Closed;

**Описание** = 0x000002df;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymRoughness;

**Входные данные**:

* (System.Object)SymRoughness;
* (System.Object)pvbValue;

### Нод ProcessingType

**Имя нода** = ProcessingType;

**Описание** = 0x000002ef;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymRoughness;

**Входные данные**:

* (System.Object)SymRoughness;

### Нод Set_ProcessingType

**Имя нода** = Set_ProcessingType;

**Описание** = 0x000002ef;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymRoughness;

**Входные данные**:

* (System.Object)SymRoughness;
* (System.Object)McCOM2.RoughnessProcessingType;

### Нод Frame

**Имя нода** = Frame;

**Описание** = 0x000002e4;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymRoughness;

**Входные данные**:

* (System.Object)SymRoughness;

### Нод Set_Frame

**Имя нода** = Set_Frame;

**Описание** = 0x000002e4;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymRoughness;

**Входные данные**:

* (System.Object)SymRoughness;
* (System.Object)ppFrame;

### Нод ProcessingDir

**Имя нода** = ProcessingDir;

**Описание** = 0x000002ee;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymRoughness;

**Входные данные**:

* (System.Object)SymRoughness;

### Нод Set_ProcessingDir

**Имя нода** = Set_ProcessingDir;

**Описание** = 0x000002ee;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymRoughness;

**Входные данные**:

* (System.Object)SymRoughness;
* (System.Object)McCOM2.RoughnessProcessingDirection;

### Нод Leaders

**Имя нода** = Leaders;

**Описание** = 0x000002e7;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymRoughness;

**Входные данные**:

* (System.Object)SymRoughness;

## Группа NVP_nanoCAD_COM.McCOM2.SymPolyline

### Нод _SymPolyline_Constructor

**Имя нода** = _SymPolyline_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymPolyline;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymPolyline_ConstructorCast

**Имя нода** = _SymPolyline_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymPolyline;

**Входные данные**:

* (System.Object)dynamic;

### Нод Count

**Имя нода** = Count;

**Описание** = 0x0000057c;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymPolyline;

**Входные данные**:

* (System.Object)SymPolyline;

### Нод Set_Count

**Имя нода** = Set_Count;

**Описание** = 0x0000057c;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymPolyline;

**Входные данные**:

* (System.Object)SymPolyline;
* (System.Object)pnValue;

### Нод Points

**Имя нода** = Points;

**Описание** = 0x0000057d;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymPolyline;

**Входные данные**:

* (System.Object)SymPolyline;
* (System.Object)Index;

### Нод Set_Points

**Имя нода** = Set_Points;

**Описание** = 0x0000057d;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymPolyline;

**Входные данные**:

* (System.Object)SymPolyline;
* (System.Object)Index;
* (System.Object)pvPoint;

### Нод Bulges

**Имя нода** = Bulges;

**Описание** = 0x0000057e;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymPolyline;

**Входные данные**:

* (System.Object)SymPolyline;
* (System.Object)Index;

### Нод Set_Bulges

**Имя нода** = Set_Bulges;

**Описание** = 0x0000057e;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymPolyline;

**Входные данные**:

* (System.Object)SymPolyline;
* (System.Object)Index;
* (System.Double)prValue;

### Нод Closed

**Имя нода** = Closed;

**Описание** = 0x0000057f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymPolyline;

**Входные данные**:

* (System.Object)SymPolyline;

### Нод Set_Closed

**Имя нода** = Set_Closed;

**Описание** = 0x0000057f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymPolyline;

**Входные данные**:

* (System.Object)SymPolyline;
* (System.Object)pvbValue;

## Группа NVP_nanoCAD_COM.McCOM2.SymPart

### Нод _SymPart_Constructor

**Имя нода** = _SymPart_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymPart;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymPart_ConstructorCast

**Имя нода** = _SymPart_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymPart;

**Входные данные**:

* (System.Object)dynamic;

### Нод Init

**Имя нода** = Init;

**Описание** = 0x000002a2;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymPart;

**Входные данные**:

* (System.Object)SymPart;
* (System.Object)vPartId;

### Нод Origin

**Имя нода** = Origin;

**Описание** = 0x000002b4;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymPart;

**Входные данные**:

* (System.Object)SymPart;

### Нод Set_Origin

**Имя нода** = Set_Origin;

**Описание** = 0x000002b4;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymPart;

**Входные данные**:

* (System.Object)SymPart;
* (System.Object)pPoint;

### Нод Direction

**Имя нода** = Direction;

**Описание** = 0x000002ac;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymPart;

**Входные данные**:

* (System.Object)SymPart;

### Нод Set_Direction

**Имя нода** = Set_Direction;

**Описание** = 0x000002ac;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymPart;

**Входные данные**:

* (System.Object)SymPart;
* (System.Object)pVector;

### Нод Connect

**Имя нода** = Connect;

**Описание** = 0x0000029d;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymPart;

**Входные данные**:

* (System.Object)SymPart;
* (System.Object)vObjects;

### Нод AffectsTo

**Имя нода** = AffectsTo;

**Описание** = 0x000002a5;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymPart;

**Входные данные**:

* (System.Object)SymPart;

### Нод DependsOn

**Имя нода** = DependsOn;

**Описание** = 0x000002aa;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymPart;

**Входные данные**:

* (System.Object)SymPart;

### Нод Class

**Имя нода** = Class;

**Описание** = 0x000002a7;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymPart;

**Входные данные**:

* (System.Object)SymPart;

### Нод Type

**Имя нода** = Type;

**Описание** = 0x000002b7;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymPart;

**Входные данные**:

* (System.Object)SymPart;

### Нод SubType

**Имя нода** = SubType;

**Описание** = 0x000002b6;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymPart;

**Входные данные**:

* (System.Object)SymPart;

## Группа NVP_nanoCAD_COM.McCOM2.SymNote

### Нод _SymNote_Constructor

**Имя нода** = _SymNote_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymNote;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymNote_ConstructorCast

**Имя нода** = _SymNote_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymNote;

**Входные данные**:

* (System.Object)dynamic;

### Нод Text

**Имя нода** = Text;

**Описание** = 0x00000298;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymNote;

**Входные данные**:

* (System.Object)SymNote;

### Нод Set_Text

**Имя нода** = Set_Text;

**Описание** = 0x00000298;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymNote;

**Входные данные**:

* (System.Object)SymNote;
* (System.String)pbstrText;

### Нод Lines

**Имя нода** = Lines;

**Описание** = 0x0000028f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymNote;

**Входные данные**:

* (System.Object)SymNote;
* (System.Object)Index;

### Нод Set_Lines

**Имя нода** = Set_Lines;

**Описание** = 0x0000028f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymNote;

**Входные данные**:

* (System.Object)SymNote;
* (System.Object)Index;
* (System.String)vbstrResult;

### Нод LinesCount

**Имя нода** = LinesCount;

**Описание** = 0x00000290;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymNote;

**Входные данные**:

* (System.Object)SymNote;

### Нод Set_LinesCount

**Имя нода** = Set_LinesCount;

**Описание** = 0x00000290;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymNote;

**Входные данные**:

* (System.Object)SymNote;
* (System.Object)pnResult;

### Нод Align

**Имя нода** = Align;

**Описание** = 0x00000286;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymNote;

**Входные данные**:

* (System.Object)SymNote;
* (System.Object)Index;

### Нод Set_Align

**Имя нода** = Set_Align;

**Описание** = 0x00000286;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymNote;

**Входные данные**:

* (System.Object)SymNote;
* (System.Object)Index;
* (System.Object)McCOM2.TextAlignment;

### Нод TextPosition

**Имя нода** = TextPosition;

**Описание** = 0x00000299;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymNote;

**Входные данные**:

* (System.Object)SymNote;

### Нод Set_TextPosition

**Имя нода** = Set_TextPosition;

**Описание** = 0x00000299;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymNote;

**Входные данные**:

* (System.Object)SymNote;
* (System.Object)pvResult;

### Нод Direction

**Имя нода** = Direction;

**Описание** = 0x0000028b;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymNote;

**Входные данные**:

* (System.Object)SymNote;

### Нод Set_Direction

**Имя нода** = Set_Direction;

**Описание** = 0x0000028b;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymNote;

**Входные данные**:

* (System.Object)SymNote;
* (System.Double)prResult;

### Нод Multiline

**Имя нода** = Multiline;

**Описание** = 0x00000294;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymNote;

**Входные данные**:

* (System.Object)SymNote;

### Нод Set_Multiline

**Имя нода** = Set_Multiline;

**Описание** = 0x00000294;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymNote;

**Входные данные**:

* (System.Object)SymNote;
* (System.Object)pvbValue;

### Нод Leaders

**Имя нода** = Leaders;

**Описание** = 0x0000028e;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymNote;

**Входные данные**:

* (System.Object)SymNote;

## Группа NVP_nanoCAD_COM.McCOM2.SymMark

### Нод _SymMark_Constructor

**Имя нода** = _SymMark_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymMark;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymMark_ConstructorCast

**Имя нода** = _SymMark_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymMark;

**Входные данные**:

* (System.Object)dynamic;

### Нод Text

**Имя нода** = Text;

**Описание** = 0x0000027c;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymMark;

**Входные данные**:

* (System.Object)SymMark;

### Нод Set_Text

**Имя нода** = Set_Text;

**Описание** = 0x0000027c;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymMark;

**Входные данные**:

* (System.Object)SymMark;
* (System.String)pbstrResult;

### Нод Method

**Имя нода** = Method;

**Описание** = 0x00000276;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymMark;

**Входные данные**:

* (System.Object)SymMark;

### Нод Set_Method

**Имя нода** = Set_Method;

**Описание** = 0x00000276;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymMark;

**Входные данные**:

* (System.Object)SymMark;
* (System.String)pbstrResult;

### Нод Stamp

**Имя нода** = Stamp;

**Описание** = 0x0000027b;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymMark;

**Входные данные**:

* (System.Object)SymMark;

### Нод Set_Stamp

**Имя нода** = Set_Stamp;

**Описание** = 0x0000027b;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymMark;

**Входные данные**:

* (System.Object)SymMark;
* (System.Object)pvbResult;

### Нод Documentation

**Имя нода** = Documentation;

**Описание** = 0x0000026f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymMark;

**Входные данные**:

* (System.Object)SymMark;

### Нод Set_Documentation

**Имя нода** = Set_Documentation;

**Описание** = 0x0000026f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymMark;

**Входные данные**:

* (System.Object)SymMark;
* (System.String)pnResult;

### Нод Position

**Имя нода** = Position;

**Описание** = 0x00000279;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymMark;

**Входные данные**:

* (System.Object)SymMark;

### Нод Set_Position

**Имя нода** = Set_Position;

**Описание** = 0x00000279;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymMark;

**Входные данные**:

* (System.Object)SymMark;
* (System.Object)pvPoint;

### Нод Leaders

**Имя нода** = Leaders;

**Описание** = 0x00000272;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymMark;

**Входные данные**:

* (System.Object)SymMark;

## Группа NVP_nanoCAD_COM.McCOM2.SymGradient

### Нод _SymGradient_Constructor

**Имя нода** = _SymGradient_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymGradient;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymGradient_ConstructorCast

**Имя нода** = _SymGradient_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymGradient;

**Входные данные**:

* (System.Object)dynamic;

### Нод Taper

**Имя нода** = Taper;

**Описание** = 0x0000025e;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymGradient;

**Входные данные**:

* (System.Object)SymGradient;

### Нод Set_Taper

**Имя нода** = Set_Taper;

**Описание** = 0x0000025e;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymGradient;

**Входные данные**:

* (System.Object)SymGradient;
* (System.Object)pvbType;

### Нод RightToLeft

**Имя нода** = RightToLeft;

**Описание** = 0x0000025d;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymGradient;

**Входные данные**:

* (System.Object)SymGradient;

### Нод Set_RightToLeft

**Имя нода** = Set_RightToLeft;

**Описание** = 0x0000025d;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymGradient;

**Входные данные**:

* (System.Object)SymGradient;
* (System.Object)pvbType;

### Нод Units

**Имя нода** = Units;

**Описание** = 0x00000261;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymGradient;

**Входные данные**:

* (System.Object)SymGradient;

### Нод Set_Units

**Имя нода** = Set_Units;

**Описание** = 0x00000261;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymGradient;

**Входные данные**:

* (System.Object)SymGradient;
* (System.Object)McCOM2.GradientUnitType;

### Нод Associative

**Имя нода** = Associative;

**Описание** = 0x0000024a;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymGradient;

**Входные данные**:

* (System.Object)SymGradient;

### Нод Set_Associative

**Имя нода** = Set_Associative;

**Описание** = 0x0000024a;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymGradient;

**Входные данные**:

* (System.Object)SymGradient;
* (System.Object)pvbValue;

### Нод OnPlane

**Имя нода** = OnPlane;

**Описание** = 0x00000259;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymGradient;

**Входные данные**:

* (System.Object)SymGradient;

### Нод Set_OnPlane

**Имя нода** = Set_OnPlane;

**Описание** = 0x00000259;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymGradient;

**Входные данные**:

* (System.Object)SymGradient;
* (System.Object)pvbValue;

### Нод DisplayText

**Имя нода** = DisplayText;

**Описание** = 0x00000250;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymGradient;

**Входные данные**:

* (System.Object)SymGradient;

### Нод Text

**Имя нода** = Text;

**Описание** = 0x0000025f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymGradient;

**Входные данные**:

* (System.Object)SymGradient;

### Нод Set_Text

**Имя нода** = Set_Text;

**Описание** = 0x0000025f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymGradient;

**Входные данные**:

* (System.Object)SymGradient;
* (System.String)pbstrValue;

### Нод Position

**Имя нода** = Position;

**Описание** = 0x0000025a;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymGradient;

**Входные данные**:

* (System.Object)SymGradient;

### Нод Set_Position

**Имя нода** = Set_Position;

**Описание** = 0x0000025a;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymGradient;

**Входные данные**:

* (System.Object)SymGradient;
* (System.Object)pvPoint;

### Нод TextPosition

**Имя нода** = TextPosition;

**Описание** = 0x00000260;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymGradient;

**Входные данные**:

* (System.Object)SymGradient;

### Нод Set_TextPosition

**Имя нода** = Set_TextPosition;

**Описание** = 0x00000260;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymGradient;

**Входные данные**:

* (System.Object)SymGradient;
* (System.Object)pvPoint;

### Нод ReferenceDir

**Имя нода** = ReferenceDir;

**Описание** = 0x0000025c;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymGradient;

**Входные данные**:

* (System.Object)SymGradient;

### Нод Set_ReferenceDir

**Имя нода** = Set_ReferenceDir;

**Описание** = 0x0000025c;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymGradient;

**Входные данные**:

* (System.Object)SymGradient;
* (System.Object)pvVector;

### Нод Direction

**Имя нода** = Direction;

**Описание** = 0x0000024f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymGradient;

**Входные данные**:

* (System.Object)SymGradient;

### Нод Set_Direction

**Имя нода** = Set_Direction;

**Описание** = 0x0000024f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymGradient;

**Входные данные**:

* (System.Object)SymGradient;
* (System.Object)pvVector;

### Нод Object

**Имя нода** = Object;

**Описание** = 0x00000258;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymGradient;

**Входные данные**:

* (System.Object)SymGradient;

## Группа NVP_nanoCAD_COM.McCOM2.SymFrame

### Нод _SymFrame_Constructor

**Имя нода** = _SymFrame_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFrame;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymFrame_ConstructorCast

**Имя нода** = _SymFrame_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFrame;

**Входные данные**:

* (System.Object)dynamic;

### Нод SelectFor

**Имя нода** = SelectFor;

**Описание** = 0x0000022e;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFrame;

**Входные данные**:

* (System.Object)SymFrame;
* (System.Object)rBounds;

### Нод ZoneAtPoint

**Имя нода** = ZoneAtPoint;

**Описание** = 0x00000230;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFrame;

**Входные данные**:

* (System.Object)SymFrame;
* (System.Object)vPoint;

### Нод Prev

**Имя нода** = Prev;

**Описание** = 0x0000023d;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFrame;

**Входные данные**:

* (System.Object)SymFrame;

### Нод Next

**Имя нода** = Next;

**Описание** = 0x0000023c;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFrame;

**Входные данные**:

* (System.Object)SymFrame;

### Нод Set_Roughness

**Имя нода** = Set_Roughness;

**Описание** = 0x0000023f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFrame;

**Входные данные**:

* (System.Object)SymFrame;
* (System.Object)ppSymbol;

### Нод Roughness

**Имя нода** = Roughness;

**Описание** = 0x0000023f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFrame;

**Входные данные**:

* (System.Object)SymFrame;

### Нод Text

**Имя нода** = Text;

**Описание** = 0x00000240;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFrame;

**Входные данные**:

* (System.Object)SymFrame;

## Группа NVP_nanoCAD_COM.McCOM2.SymFix

### Нод _SymFix_Constructor

**Имя нода** = _SymFix_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFix;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymFix_ConstructorCast

**Имя нода** = _SymFix_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFix;

**Входные данные**:

* (System.Object)dynamic;

### Нод Position

**Имя нода** = Position;

**Описание** = 0x0000021e;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFix;

**Входные данные**:

* (System.Object)SymFix;

### Нод Set_Position

**Имя нода** = Set_Position;

**Описание** = 0x0000021e;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFix;

**Входные данные**:

* (System.Object)SymFix;
* (System.Object)pvPoint;

### Нод TextPosition

**Имя нода** = TextPosition;

**Описание** = 0x00000224;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFix;

**Входные данные**:

* (System.Object)SymFix;

### Нод Set_TextPosition

**Имя нода** = Set_TextPosition;

**Описание** = 0x00000224;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFix;

**Входные данные**:

* (System.Object)SymFix;
* (System.Object)pvPoint;

### Нод Text

**Имя нода** = Text;

**Описание** = 0x00000223;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFix;

**Входные данные**:

* (System.Object)SymFix;

### Нод Set_Text

**Имя нода** = Set_Text;

**Описание** = 0x00000223;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFix;

**Входные данные**:

* (System.Object)SymFix;
* (System.String)pbstrValue;

### Нод Footer

**Имя нода** = Footer;

**Описание** = 0x00000214;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFix;

**Входные данные**:

* (System.Object)SymFix;

### Нод Set_Footer

**Имя нода** = Set_Footer;

**Описание** = 0x00000214;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFix;

**Входные данные**:

* (System.Object)SymFix;
* (System.String)pbstrValue;

### Нод Documentation

**Имя нода** = Documentation;

**Описание** = 0x00000212;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFix;

**Входные данные**:

* (System.Object)SymFix;

### Нод Set_Documentation

**Имя нода** = Set_Documentation;

**Описание** = 0x00000212;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFix;

**Входные данные**:

* (System.Object)SymFix;
* (System.String)pbstrValue;

### Нод LeaderText

**Имя нода** = LeaderText;

**Описание** = 0x00000218;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFix;

**Входные данные**:

* (System.Object)SymFix;

### Нод Set_LeaderText

**Имя нода** = Set_LeaderText;

**Описание** = 0x00000218;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFix;

**Входные данные**:

* (System.Object)SymFix;
* (System.String)pbstrValue;

### Нод LeaderFooter

**Имя нода** = LeaderFooter;

**Описание** = 0x00000217;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFix;

**Входные данные**:

* (System.Object)SymFix;

### Нод Set_LeaderFooter

**Имя нода** = Set_LeaderFooter;

**Описание** = 0x00000217;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFix;

**Входные данные**:

* (System.Object)SymFix;
* (System.String)pbstrValue;

### Нод Type

**Имя нода** = Type;

**Описание** = 0x00000225;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFix;

**Входные данные**:

* (System.Object)SymFix;

### Нод Set_Type

**Имя нода** = Set_Type;

**Описание** = 0x00000225;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFix;

**Входные данные**:

* (System.Object)SymFix;
* (System.Object)McCOM2.SymFixType;

### Нод Technology

**Имя нода** = Technology;

**Описание** = 0x00000222;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFix;

**Входные данные**:

* (System.Object)SymFix;

### Нод Set_Technology

**Имя нода** = Set_Technology;

**Описание** = 0x00000222;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFix;

**Входные данные**:

* (System.Object)SymFix;
* (System.Object)McCOM2.SymFixTechnology;

### Нод Face

**Имя нода** = Face;

**Описание** = 0x00000213;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFix;

**Входные данные**:

* (System.Object)SymFix;

### Нод Set_Face

**Имя нода** = Set_Face;

**Описание** = 0x00000213;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFix;

**Входные данные**:

* (System.Object)SymFix;
* (System.Object)pvbValue;

### Нод Closed

**Имя нода** = Closed;

**Описание** = 0x0000020f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFix;

**Входные данные**:

* (System.Object)SymFix;

### Нод Set_Closed

**Имя нода** = Set_Closed;

**Описание** = 0x0000020f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFix;

**Входные данные**:

* (System.Object)SymFix;
* (System.Object)pvbValue;

### Нод Assembly

**Имя нода** = Assembly;

**Описание** = 0x0000020c;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFix;

**Входные данные**:

* (System.Object)SymFix;

### Нод Set_Assembly

**Имя нода** = Set_Assembly;

**Описание** = 0x0000020c;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFix;

**Входные данные**:

* (System.Object)SymFix;
* (System.Object)pvbAssembly;

### Нод SeamCount

**Имя нода** = SeamCount;

**Описание** = 0x00000220;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFix;

**Входные данные**:

* (System.Object)SymFix;

### Нод Set_SeamCount

**Имя нода** = Set_SeamCount;

**Описание** = 0x00000220;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFix;

**Входные данные**:

* (System.Object)SymFix;
* (System.Object)pnValue;

### Нод SeamDistance

**Имя нода** = SeamDistance;

**Описание** = 0x00000221;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFix;

**Входные данные**:

* (System.Object)SymFix;

### Нод Set_SeamDistance

**Имя нода** = Set_SeamDistance;

**Описание** = 0x00000221;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymFix;

**Входные данные**:

* (System.Object)SymFix;
* (System.Double)prValue;

## Группа NVP_nanoCAD_COM.McCOM2.SymDimRadius

### Нод _SymDimRadius_Constructor

**Имя нода** = _SymDimRadius_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimRadius;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymDimRadius_ConstructorCast

**Имя нода** = _SymDimRadius_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimRadius;

**Входные данные**:

* (System.Object)dynamic;

### Нод Position

**Имя нода** = Position;

**Описание** = 0x000001f5;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimRadius;

**Входные данные**:

* (System.Object)SymDimRadius;

### Нод Set_Position

**Имя нода** = Set_Position;

**Описание** = 0x000001f5;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimRadius;

**Входные данные**:

* (System.Object)SymDimRadius;
* (System.Object)pvPoint;

### Нод Center

**Имя нода** = Center;

**Описание** = 0x000001e6;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimRadius;

**Входные данные**:

* (System.Object)SymDimRadius;

### Нод Set_Center

**Имя нода** = Set_Center;

**Описание** = 0x000001e6;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimRadius;

**Входные данные**:

* (System.Object)SymDimRadius;
* (System.Object)pvPoint;

### Нод Arrow

**Имя нода** = Arrow;

**Описание** = 0x000001e3;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimRadius;

**Входные данные**:

* (System.Object)SymDimRadius;

### Нод Set_Arrow

**Имя нода** = Set_Arrow;

**Описание** = 0x000001e3;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimRadius;

**Входные данные**:

* (System.Object)SymDimRadius;
* (System.Object)McCOM2.ArrowType;

## Группа NVP_nanoCAD_COM.McCOM2.SymDimNumber

### Нод _SymDimNumber_Constructor

**Имя нода** = _SymDimNumber_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimNumber;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymDimNumber_ConstructorCast

**Имя нода** = _SymDimNumber_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimNumber;

**Входные данные**:

* (System.Object)dynamic;

### Нод TextPosition

**Имя нода** = TextPosition;

**Описание** = 0x000001d9;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimNumber;

**Входные данные**:

* (System.Object)SymDimNumber;

### Нод Set_TextPosition

**Имя нода** = Set_TextPosition;

**Описание** = 0x000001d9;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimNumber;

**Входные данные**:

* (System.Object)SymDimNumber;
* (System.Object)pvResult;

### Нод Position

**Имя нода** = Position;

**Описание** = 0x000001d6;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimNumber;

**Входные данные**:

* (System.Object)SymDimNumber;

### Нод Set_Position

**Имя нода** = Set_Position;

**Описание** = 0x000001d6;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimNumber;

**Входные данные**:

* (System.Object)SymDimNumber;
* (System.Object)pvPoint;

### Нод Index

**Имя нода** = Index;

**Описание** = 0x000001cf;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimNumber;

**Входные данные**:

* (System.Object)SymDimNumber;

### Нод Set_Index

**Имя нода** = Set_Index;

**Описание** = 0x000001cf;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimNumber;

**Входные данные**:

* (System.Object)SymDimNumber;
* (System.Object)pResult;

### Нод Autonum

**Имя нода** = Autonum;

**Описание** = 0x000001c8;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimNumber;

**Входные данные**:

* (System.Object)SymDimNumber;

### Нод Set_Autonum

**Имя нода** = Set_Autonum;

**Описание** = 0x000001c8;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimNumber;

**Входные данные**:

* (System.Object)SymDimNumber;
* (System.Object)pvbResult;

### Нод Text

**Имя нода** = Text;

**Описание** = 0x000001d8;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimNumber;

**Входные данные**:

* (System.Object)SymDimNumber;

### Нод Set_Text

**Имя нода** = Set_Text;

**Описание** = 0x000001d8;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimNumber;

**Входные данные**:

* (System.Object)SymDimNumber;
* (System.String)pbstrResult;

### Нод DisplayText

**Имя нода** = DisplayText;

**Описание** = 0x000001cd;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimNumber;

**Входные данные**:

* (System.Object)SymDimNumber;

### Нод Align

**Имя нода** = Align;

**Описание** = 0x000001c0;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimNumber;

**Входные данные**:

* (System.Object)SymDimNumber;
* (System.Object)vBounds;

### Нод Arrow

**Имя нода** = Arrow;

**Описание** = 0x000001c7;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimNumber;

**Входные данные**:

* (System.Object)SymDimNumber;

### Нод Set_Arrow

**Имя нода** = Set_Arrow;

**Описание** = 0x000001c7;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimNumber;

**Входные данные**:

* (System.Object)SymDimNumber;
* (System.Object)McCOM2.ArrowType;

### Нод Owner

**Имя нода** = Owner;

**Описание** = 0x000005b7;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimNumber;

**Входные данные**:

* (System.Object)SymDimNumber;

### Нод Set_Owner

**Имя нода** = Set_Owner;

**Описание** = 0x000005b7;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimNumber;

**Входные данные**:

* (System.Object)SymDimNumber;
* (System.Object)ppValue;

## Группа NVP_nanoCAD_COM.McCOM2.SymDimLinear

### Нод _SymDimLinear_Constructor

**Имя нода** = _SymDimLinear_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimLinear;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymDimLinear_ConstructorCast

**Имя нода** = _SymDimLinear_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimLinear;

**Входные данные**:

* (System.Object)dynamic;

### Нод Position1

**Имя нода** = Position1;

**Описание** = 0x000001ae;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimLinear;

**Входные данные**:

* (System.Object)SymDimLinear;

### Нод Set_Position1

**Имя нода** = Set_Position1;

**Описание** = 0x000001ae;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimLinear;

**Входные данные**:

* (System.Object)SymDimLinear;
* (System.Object)pvPoint;

### Нод Position2

**Имя нода** = Position2;

**Описание** = 0x000001af;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimLinear;

**Входные данные**:

* (System.Object)SymDimLinear;

### Нод Set_Position2

**Имя нода** = Set_Position2;

**Описание** = 0x000001af;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimLinear;

**Входные данные**:

* (System.Object)SymDimLinear;
* (System.Object)pvPoint;

### Нод Arrow1

**Имя нода** = Arrow1;

**Описание** = 0x0000019a;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimLinear;

**Входные данные**:

* (System.Object)SymDimLinear;

### Нод Set_Arrow1

**Имя нода** = Set_Arrow1;

**Описание** = 0x0000019a;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimLinear;

**Входные данные**:

* (System.Object)SymDimLinear;
* (System.Object)McCOM2.ArrowType;

### Нод Arrow2

**Имя нода** = Arrow2;

**Описание** = 0x0000019b;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimLinear;

**Входные данные**:

* (System.Object)SymDimLinear;

### Нод Set_Arrow2

**Имя нода** = Set_Arrow2;

**Описание** = 0x0000019b;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimLinear;

**Входные данные**:

* (System.Object)SymDimLinear;
* (System.Object)McCOM2.ArrowType;

### Нод LinePosition

**Имя нода** = LinePosition;

**Описание** = 0x000001a7;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimLinear;

**Входные данные**:

* (System.Object)SymDimLinear;

### Нод Set_LinePosition

**Имя нода** = Set_LinePosition;

**Описание** = 0x000001a7;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimLinear;

**Входные данные**:

* (System.Object)SymDimLinear;
* (System.Object)pvPoint;

### Нод Direction

**Имя нода** = Direction;

**Описание** = 0x000001a1;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimLinear;

**Входные данные**:

* (System.Object)SymDimLinear;

### Нод Set_Direction

**Имя нода** = Set_Direction;

**Описание** = 0x000001a1;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimLinear;

**Входные данные**:

* (System.Object)SymDimLinear;
* (System.Object)pvVector;

### Нод Type

**Имя нода** = Type;

**Описание** = 0x00000163;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimLinear;

**Входные данные**:

* (System.Object)SymDimLinear;

### Нод Set_Type

**Имя нода** = Set_Type;

**Описание** = 0x00000163;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimLinear;

**Входные данные**:

* (System.Object)SymDimLinear;
* (System.Object)McCOM2.SymDimensionType;

## Группа NVP_nanoCAD_COM.McCOM2.SymDimHugeRadius

### Нод _SymDimHugeRadius_Constructor

**Имя нода** = _SymDimHugeRadius_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimHugeRadius;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymDimHugeRadius_ConstructorCast

**Имя нода** = _SymDimHugeRadius_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimHugeRadius;

**Входные данные**:

* (System.Object)dynamic;

### Нод MeanCenter

**Имя нода** = MeanCenter;

**Описание** = 0x00000180;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimHugeRadius;

**Входные данные**:

* (System.Object)SymDimHugeRadius;

### Нод Set_MeanCenter

**Имя нода** = Set_MeanCenter;

**Описание** = 0x00000180;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimHugeRadius;

**Входные данные**:

* (System.Object)SymDimHugeRadius;
* (System.Object)pvPoint;

### Нод BreakPosition

**Имя нода** = BreakPosition;

**Описание** = 0x00000172;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimHugeRadius;

**Входные данные**:

* (System.Object)SymDimHugeRadius;

### Нод Set_BreakPosition

**Имя нода** = Set_BreakPosition;

**Описание** = 0x00000172;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimHugeRadius;

**Входные данные**:

* (System.Object)SymDimHugeRadius;
* (System.Object)pvPoint;

## Группа NVP_nanoCAD_COM.McCOM2.SymDimension

### Нод _SymDimension_Constructor

**Имя нода** = _SymDimension_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimension;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymDimension_ConstructorCast

**Имя нода** = _SymDimension_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimension;

**Входные данные**:

* (System.Object)dynamic;

### Нод TextPosition

**Имя нода** = TextPosition;

**Описание** = 0x00000161;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimension;

**Входные данные**:

* (System.Object)SymDimension;

### Нод Set_TextPosition

**Имя нода** = Set_TextPosition;

**Описание** = 0x00000161;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimension;

**Входные данные**:

* (System.Object)SymDimension;
* (System.Object)pvPoint;

### Нод Value

**Имя нода** = Value;

**Описание** = 0x00000165;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimension;

**Входные данные**:

* (System.Object)SymDimension;

### Нод Upper

**Имя нода** = Upper;

**Описание** = 0x00000164;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimension;

**Входные данные**:

* (System.Object)SymDimension;

### Нод Set_Upper

**Имя нода** = Set_Upper;

**Описание** = 0x00000164;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimension;

**Входные данные**:

* (System.Object)SymDimension;
* (System.Object)pvResult;

### Нод Lower

**Имя нода** = Lower;

**Описание** = 0x00000156;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimension;

**Входные данные**:

* (System.Object)SymDimension;

### Нод Set_Lower

**Имя нода** = Set_Lower;

**Описание** = 0x00000156;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimension;

**Входные данные**:

* (System.Object)SymDimension;
* (System.Object)pvResult;

### Нод Quality

**Имя нода** = Quality;

**Описание** = 0x0000015c;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimension;

**Входные данные**:

* (System.Object)SymDimension;

### Нод Set_Quality

**Имя нода** = Set_Quality;

**Описание** = 0x0000015c;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimension;

**Входные данные**:

* (System.Object)SymDimension;
* (System.Object)pnResult;

### Нод Field

**Имя нода** = Field;

**Описание** = 0x0000014e;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimension;

**Входные данные**:

* (System.Object)SymDimension;

### Нод Set_Field

**Имя нода** = Set_Field;

**Описание** = 0x0000014e;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimension;

**Входные данные**:

* (System.Object)SymDimension;
* (System.String)pbstrResult;

### Нод TolView

**Имя нода** = TolView;

**Описание** = 0x00000162;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimension;

**Входные данные**:

* (System.Object)SymDimension;

### Нод Set_TolView

**Имя нода** = Set_TolView;

**Описание** = 0x00000162;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimension;

**Входные данные**:

* (System.Object)SymDimension;
* (System.Object)McCOM2.SymDimensionTolView;

### Нод Brackets

**Имя нода** = Brackets;

**Описание** = 0x0000014a;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimension;

**Входные данные**:

* (System.Object)SymDimension;

### Нод Set_Brackets

**Имя нода** = Set_Brackets;

**Описание** = 0x0000014a;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimension;

**Входные данные**:

* (System.Object)SymDimension;
* (System.Object)McCOM2.SymDimensionBracket;

### Нод Rect

**Имя нода** = Rect;

**Описание** = 0x0000015e;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimension;

**Входные данные**:

* (System.Object)SymDimension;

### Нод Set_Rect

**Имя нода** = Set_Rect;

**Описание** = 0x0000015e;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimension;

**Входные данные**:

* (System.Object)SymDimension;
* (System.Object)pvbValue;

### Нод Leader

**Имя нода** = Leader;

**Описание** = 0x00000152;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimension;

**Входные данные**:

* (System.Object)SymDimension;

### Нод Set_Leader

**Имя нода** = Set_Leader;

**Описание** = 0x00000152;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimension;

**Входные данные**:

* (System.Object)SymDimension;
* (System.Object)pvbValue;

### Нод Real

**Имя нода** = Real;

**Описание** = 0x0000015d;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimension;

**Входные данные**:

* (System.Object)SymDimension;

### Нод Set_Real

**Имя нода** = Set_Real;

**Описание** = 0x0000015d;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimension;

**Входные данные**:

* (System.Object)SymDimension;
* (System.Object)pvbValue;

### Нод Text

**Имя нода** = Text;

**Описание** = 0x00000160;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimension;

**Входные данные**:

* (System.Object)SymDimension;

### Нод Set_Text

**Имя нода** = Set_Text;

**Описание** = 0x00000160;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimension;

**Входные данные**:

* (System.Object)SymDimension;
* (System.String)pbstrText;

### Нод Prefix

**Имя нода** = Prefix;

**Описание** = 0x0000015a;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimension;

**Входные данные**:

* (System.Object)SymDimension;

### Нод Set_Prefix

**Имя нода** = Set_Prefix;

**Описание** = 0x0000015a;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimension;

**Входные данные**:

* (System.Object)SymDimension;
* (System.String)pbstrText;

### Нод Suffix

**Имя нода** = Suffix;

**Описание** = 0x0000015f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimension;

**Входные данные**:

* (System.Object)SymDimension;

### Нод Set_Suffix

**Имя нода** = Set_Suffix;

**Описание** = 0x0000015f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimension;

**Входные данные**:

* (System.Object)SymDimension;
* (System.String)pbstrText;

### Нод Postfix

**Имя нода** = Postfix;

**Описание** = 0x00000159;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimension;

**Входные данные**:

* (System.Object)SymDimension;

### Нод Set_Postfix

**Имя нода** = Set_Postfix;

**Описание** = 0x00000159;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimension;

**Входные данные**:

* (System.Object)SymDimension;
* (System.String)pbstrText;

### Нод Footer

**Имя нода** = Footer;

**Описание** = 0x0000014f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimension;

**Входные данные**:

* (System.Object)SymDimension;

### Нод Set_Footer

**Имя нода** = Set_Footer;

**Описание** = 0x0000014f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimension;

**Входные данные**:

* (System.Object)SymDimension;
* (System.String)pbstrText;

### Нод Symbol

**Имя нода** = Symbol;

**Описание** = 0x00000588;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimension;

**Входные данные**:

* (System.Object)SymDimension;

### Нод Set_Symbol

**Имя нода** = Set_Symbol;

**Описание** = 0x00000588;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimension;

**Входные данные**:

* (System.Object)SymDimension;
* (System.Object)McCOM2.SymDimensionSymbol;

## Группа NVP_nanoCAD_COM.McCOM2.SymDimDiameter

### Нод _SymDimDiameter_Constructor

**Имя нода** = _SymDimDiameter_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimDiameter;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymDimDiameter_ConstructorCast

**Имя нода** = _SymDimDiameter_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimDiameter;

**Входные данные**:

* (System.Object)dynamic;

### Нод Position1

**Имя нода** = Position1;

**Описание** = 0x00000131;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimDiameter;

**Входные данные**:

* (System.Object)SymDimDiameter;

### Нод Set_Position1

**Имя нода** = Set_Position1;

**Описание** = 0x00000131;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimDiameter;

**Входные данные**:

* (System.Object)SymDimDiameter;
* (System.Object)pvPoint;

### Нод Position2

**Имя нода** = Position2;

**Описание** = 0x00000132;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimDiameter;

**Входные данные**:

* (System.Object)SymDimDiameter;

### Нод Set_Position2

**Имя нода** = Set_Position2;

**Описание** = 0x00000132;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimDiameter;

**Входные данные**:

* (System.Object)SymDimDiameter;
* (System.Object)pvPoint;

### Нод Arrow1

**Имя нода** = Arrow1;

**Описание** = 0x0000011e;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimDiameter;

**Входные данные**:

* (System.Object)SymDimDiameter;

### Нод Set_Arrow1

**Имя нода** = Set_Arrow1;

**Описание** = 0x0000011e;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimDiameter;

**Входные данные**:

* (System.Object)SymDimDiameter;
* (System.Object)McCOM2.ArrowType;

### Нод Arrow2

**Имя нода** = Arrow2;

**Описание** = 0x0000011f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimDiameter;

**Входные данные**:

* (System.Object)SymDimDiameter;

### Нод Set_Arrow2

**Имя нода** = Set_Arrow2;

**Описание** = 0x0000011f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimDiameter;

**Входные данные**:

* (System.Object)SymDimDiameter;
* (System.Object)McCOM2.ArrowType;

### Нод Center

**Имя нода** = Center;

**Описание** = 0x00000122;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimDiameter;

**Входные данные**:

* (System.Object)SymDimDiameter;

### Нод Set_Center

**Имя нода** = Set_Center;

**Описание** = 0x00000122;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimDiameter;

**Входные данные**:

* (System.Object)SymDimDiameter;
* (System.Object)pvPoint;

## Группа NVP_nanoCAD_COM.McCOM2.SymDimArc

### Нод _SymDimArc_Constructor

**Имя нода** = _SymDimArc_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimArc;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymDimArc_ConstructorCast

**Имя нода** = _SymDimArc_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimArc;

**Входные данные**:

* (System.Object)dynamic;

### Нод Position1

**Имя нода** = Position1;

**Описание** = 0x00000106;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimArc;

**Входные данные**:

* (System.Object)SymDimArc;

### Нод Set_Position1

**Имя нода** = Set_Position1;

**Описание** = 0x00000106;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimArc;

**Входные данные**:

* (System.Object)SymDimArc;
* (System.Object)pvPoint;

### Нод Position2

**Имя нода** = Position2;

**Описание** = 0x00000107;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimArc;

**Входные данные**:

* (System.Object)SymDimArc;

### Нод Set_Position2

**Имя нода** = Set_Position2;

**Описание** = 0x00000107;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimArc;

**Входные данные**:

* (System.Object)SymDimArc;
* (System.Object)pvPoint;

### Нод Arrow1

**Имя нода** = Arrow1;

**Описание** = 0x000000f2;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimArc;

**Входные данные**:

* (System.Object)SymDimArc;

### Нод Set_Arrow1

**Имя нода** = Set_Arrow1;

**Описание** = 0x000000f2;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimArc;

**Входные данные**:

* (System.Object)SymDimArc;
* (System.Object)McCOM2.ArrowType;

### Нод Arrow2

**Имя нода** = Arrow2;

**Описание** = 0x000000f3;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimArc;

**Входные данные**:

* (System.Object)SymDimArc;

### Нод Set_Arrow2

**Имя нода** = Set_Arrow2;

**Описание** = 0x000000f3;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimArc;

**Входные данные**:

* (System.Object)SymDimArc;
* (System.Object)McCOM2.ArrowType;

### Нод Center

**Имя нода** = Center;

**Описание** = 0x000000f6;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimArc;

**Входные данные**:

* (System.Object)SymDimArc;

### Нод Set_Center

**Имя нода** = Set_Center;

**Описание** = 0x000000f6;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimArc;

**Входные данные**:

* (System.Object)SymDimArc;
* (System.Object)pvPoint;

### Нод LinePosition

**Имя нода** = LinePosition;

**Описание** = 0x000000ff;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimArc;

**Входные данные**:

* (System.Object)SymDimArc;

### Нод Set_LinePosition

**Имя нода** = Set_LinePosition;

**Описание** = 0x000000ff;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimArc;

**Входные данные**:

* (System.Object)SymDimArc;
* (System.Object)pvPoint;

## Группа NVP_nanoCAD_COM.McCOM2.SymDimAngle

### Нод _SymDimAngle_Constructor

**Имя нода** = _SymDimAngle_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimAngle;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymDimAngle_ConstructorCast

**Имя нода** = _SymDimAngle_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimAngle;

**Входные данные**:

* (System.Object)dynamic;

### Нод Position1

**Имя нода** = Position1;

**Описание** = 0x00000582;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimAngle;

**Входные данные**:

* (System.Object)SymDimAngle;

### Нод Set_Position1

**Имя нода** = Set_Position1;

**Описание** = 0x00000582;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimAngle;

**Входные данные**:

* (System.Object)SymDimAngle;
* (System.Object)pvPoint;

### Нод Position2

**Имя нода** = Position2;

**Описание** = 0x00000583;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimAngle;

**Входные данные**:

* (System.Object)SymDimAngle;

### Нод Set_Position2

**Имя нода** = Set_Position2;

**Описание** = 0x00000583;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimAngle;

**Входные данные**:

* (System.Object)SymDimAngle;
* (System.Object)pvPoint;

### Нод Arrow1

**Имя нода** = Arrow1;

**Описание** = 0x00000584;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimAngle;

**Входные данные**:

* (System.Object)SymDimAngle;

### Нод Set_Arrow1

**Имя нода** = Set_Arrow1;

**Описание** = 0x00000584;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimAngle;

**Входные данные**:

* (System.Object)SymDimAngle;
* (System.Object)McCOM2.ArrowType;

### Нод Arrow2

**Имя нода** = Arrow2;

**Описание** = 0x00000585;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimAngle;

**Входные данные**:

* (System.Object)SymDimAngle;

### Нод Set_Arrow2

**Имя нода** = Set_Arrow2;

**Описание** = 0x00000585;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimAngle;

**Входные данные**:

* (System.Object)SymDimAngle;
* (System.Object)McCOM2.ArrowType;

### Нод Center

**Имя нода** = Center;

**Описание** = 0x00000586;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimAngle;

**Входные данные**:

* (System.Object)SymDimAngle;

### Нод Set_Center

**Имя нода** = Set_Center;

**Описание** = 0x00000586;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimAngle;

**Входные данные**:

* (System.Object)SymDimAngle;
* (System.Object)pvPoint;

### Нод LinePosition

**Имя нода** = LinePosition;

**Описание** = 0x00000587;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimAngle;

**Входные данные**:

* (System.Object)SymDimAngle;

### Нод Set_LinePosition

**Имя нода** = Set_LinePosition;

**Описание** = 0x00000587;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymDimAngle;

**Входные данные**:

* (System.Object)SymDimAngle;
* (System.Object)pvPoint;

## Группа NVP_nanoCAD_COM.McCOM2.SymBreak

### Нод _SymBreak_Constructor

**Имя нода** = _SymBreak_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymBreak;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymBreak_ConstructorCast

**Имя нода** = _SymBreak_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymBreak;

**Входные данные**:

* (System.Object)dynamic;

### Нод PositionTop

**Имя нода** = PositionTop;

**Описание** = 0x000000e6;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymBreak;

**Входные данные**:

* (System.Object)SymBreak;

### Нод Set_PositionTop

**Имя нода** = Set_PositionTop;

**Описание** = 0x000000e6;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymBreak;

**Входные данные**:

* (System.Object)SymBreak;
* (System.Object)pvPoint;

### Нод PositionBottom

**Имя нода** = PositionBottom;

**Описание** = 0x000000e4;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymBreak;

**Входные данные**:

* (System.Object)SymBreak;

### Нод Set_PositionBottom

**Имя нода** = Set_PositionBottom;

**Описание** = 0x000000e4;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymBreak;

**Входные данные**:

* (System.Object)SymBreak;
* (System.Object)pvPoint;

### Нод PositionRight

**Имя нода** = PositionRight;

**Описание** = 0x000000e5;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymBreak;

**Входные данные**:

* (System.Object)SymBreak;

### Нод Set_PositionRight

**Имя нода** = Set_PositionRight;

**Описание** = 0x000000e5;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymBreak;

**Входные данные**:

* (System.Object)SymBreak;
* (System.Object)pvPoint;

### Нод Type

**Имя нода** = Type;

**Описание** = 0x000000e8;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymBreak;

**Входные данные**:

* (System.Object)SymBreak;

### Нод Set_Type

**Имя нода** = Set_Type;

**Описание** = 0x000000e8;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymBreak;

**Входные данные**:

* (System.Object)SymBreak;
* (System.Object)McCOM2.SymBreakType;

## Группа NVP_nanoCAD_COM.McCOM2.SymBOMLeader

### Нод _SymBOMLeader_Constructor

**Имя нода** = _SymBOMLeader_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymBOMLeader;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymBOMLeader_ConstructorCast

**Имя нода** = _SymBOMLeader_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymBOMLeader;

**Входные данные**:

* (System.Object)dynamic;

### Нод Position

**Имя нода** = Position;

**Описание** = 0x000005ca;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymBOMLeader;

**Входные данные**:

* (System.Object)SymBOMLeader;

### Нод Set_Position

**Имя нода** = Set_Position;

**Описание** = 0x000005ca;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymBOMLeader;

**Входные данные**:

* (System.Object)SymBOMLeader;
* (System.Object)pvPoint;

### Нод TextPosition

**Имя нода** = TextPosition;

**Описание** = 0x000005cb;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymBOMLeader;

**Входные данные**:

* (System.Object)SymBOMLeader;

### Нод Set_TextPosition

**Имя нода** = Set_TextPosition;

**Описание** = 0x000005cb;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymBOMLeader;

**Входные данные**:

* (System.Object)SymBOMLeader;
* (System.Object)pvPoint;

### Нод Count

**Имя нода** = Count;

**Описание** = 0x000005cc;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymBOMLeader;

**Входные данные**:

* (System.Object)SymBOMLeader;

### Нод Item

**Имя нода** = Item;

**Описание** = 0x000005cd;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymBOMLeader;

**Входные данные**:

* (System.Object)SymBOMLeader;
* (System.Object)nIndex;

### Нод CreateItem

**Имя нода** = CreateItem;

**Описание** = 0x000005ce;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymBOMLeader;

**Входные данные**:

* (System.Object)SymBOMLeader;
* (System.Object)pRecord;
* (System.Object)pObject;
* (System.Object)ppCreated;

### Нод DeleteItem

**Имя нода** = DeleteItem;

**Описание** = 0x000005cf;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymBOMLeader;

**Входные данные**:

* (System.Object)SymBOMLeader;
* (System.Object)nItemIndex;

### Нод BOM

**Имя нода** = BOM;

**Описание** = 0x000005d0;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymBOMLeader;

**Входные данные**:

* (System.Object)SymBOMLeader;

## Группа NVP_nanoCAD_COM.McCOM2.SymBase

### Нод _SymBase_Constructor

**Имя нода** = _SymBase_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymBase;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SymBase_ConstructorCast

**Имя нода** = _SymBase_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymBase;

**Входные данные**:

* (System.Object)dynamic;

### Нод Name

**Имя нода** = Name;

**Описание** = 0x00000599;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymBase;

**Входные данные**:

* (System.Object)SymBase;

### Нод Set_Name

**Имя нода** = Set_Name;

**Описание** = 0x00000599;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymBase;

**Входные данные**:

* (System.Object)SymBase;
* (System.String)pbstrValue;

### Нод Position

**Имя нода** = Position;

**Описание** = 0x0000059a;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymBase;

**Входные данные**:

* (System.Object)SymBase;

### Нод Set_Position

**Имя нода** = Set_Position;

**Описание** = 0x0000059a;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymBase;

**Входные данные**:

* (System.Object)SymBase;
* (System.Object)pvPoint;

### Нод TextPosition

**Имя нода** = TextPosition;

**Описание** = 0x0000059b;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymBase;

**Входные данные**:

* (System.Object)SymBase;

### Нод Set_TextPosition

**Имя нода** = Set_TextPosition;

**Описание** = 0x0000059b;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SymBase;

**Входные данные**:

* (System.Object)SymBase;
* (System.Object)pvPoint;

## Группа NVP_nanoCAD_COM.McCOM2.SpdsGridAxesSet

### Нод _SpdsGridAxesSet_Constructor

**Имя нода** = _SpdsGridAxesSet_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SpdsGridAxesSet;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SpdsGridAxesSet_ConstructorCast

**Имя нода** = _SpdsGridAxesSet_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SpdsGridAxesSet;

**Входные данные**:

* (System.Object)dynamic;

### Нод Index

**Имя нода** = Index;

**Описание** = 0x000005f4;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SpdsGridAxesSet;

**Входные данные**:

* (System.Object)SpdsGridAxesSet;

### Нод Set_Index

**Имя нода** = Set_Index;

**Описание** = 0x000005f4;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SpdsGridAxesSet;

**Входные данные**:

* (System.Object)SpdsGridAxesSet;
* (System.String)pbstrValue;

### Нод Distance

**Имя нода** = Distance;

**Описание** = 0x000005f5;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SpdsGridAxesSet;

**Входные данные**:

* (System.Object)SpdsGridAxesSet;

### Нод Set_Distance

**Имя нода** = Set_Distance;

**Описание** = 0x000005f5;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SpdsGridAxesSet;

**Входные данные**:

* (System.Object)SpdsGridAxesSet;
* (System.Double)prValue;

### Нод Owner

**Имя нода** = Owner;

**Описание** = 0x000005f6;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SpdsGridAxesSet;

**Входные данные**:

* (System.Object)SpdsGridAxesSet;

### Нод Count

**Имя нода** = Count;

**Описание** = 0x000005f7;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SpdsGridAxesSet;

**Входные данные**:

* (System.Object)SpdsGridAxesSet;

### Нод Set_Count

**Имя нода** = Set_Count;

**Описание** = 0x000005f7;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SpdsGridAxesSet;

**Входные данные**:

* (System.Object)SpdsGridAxesSet;
* (System.Object)pnValue;

## Группа NVP_nanoCAD_COM.McCOM2.SpdsGridAxesCollection

### Нод _SpdsGridAxesCollection_Constructor

**Имя нода** = _SpdsGridAxesCollection_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SpdsGridAxesCollection;

**Входные данные**:

* (System.Object)dynamic;

### Нод _SpdsGridAxesCollection_ConstructorCast

**Имя нода** = _SpdsGridAxesCollection_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SpdsGridAxesCollection;

**Входные данные**:

* (System.Object)dynamic;

### Нод Item

**Имя нода** = Item;

**Описание** = 0x000005f9;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SpdsGridAxesCollection;

**Входные данные**:

* (System.Object)SpdsGridAxesCollection;

### Нод Count

**Имя нода** = Count;

**Описание** = 0x000005fa;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SpdsGridAxesCollection;

**Входные данные**:

* (System.Object)SpdsGridAxesCollection;

### Нод Add

**Имя нода** = Add;

**Описание** = 0x000005fb;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SpdsGridAxesCollection;

**Входные данные**:

* (System.Object)SpdsGridAxesCollection;
* (System.Double)rDistance;
* (System.Object)nCount;
* (System.String)bstrIndex;

### Нод Remove

**Имя нода** = Remove;

**Описание** = 0x000005fc;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SpdsGridAxesCollection;

**Входные данные**:

* (System.Object)SpdsGridAxesCollection;
* (System.Object)nIndex;

### Нод DimTotal

**Имя нода** = DimTotal;

**Описание** = 0x000005fd;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SpdsGridAxesCollection;

**Входные данные**:

* (System.Object)SpdsGridAxesCollection;

### Нод Set_DimTotal

**Имя нода** = Set_DimTotal;

**Описание** = 0x000005fd;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SpdsGridAxesCollection;

**Входные данные**:

* (System.Object)SpdsGridAxesCollection;
* (System.Object)pvbValue;

### Нод DimChain

**Имя нода** = DimChain;

**Описание** = 0x000005fe;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SpdsGridAxesCollection;

**Входные данные**:

* (System.Object)SpdsGridAxesCollection;

### Нод Set_DimChain

**Имя нода** = Set_DimChain;

**Описание** = 0x000005fe;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.SpdsGridAxesCollection;

**Входные данные**:

* (System.Object)SpdsGridAxesCollection;
* (System.Object)pvbValue;

## Группа NVP_nanoCAD_COM.McCOM2.Server2

### Нод _Server2_Constructor

**Имя нода** = _Server2_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Server2;

**Входные данные**:

* (System.Object)dynamic;

### Нод _Server2_ConstructorCast

**Имя нода** = _Server2_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Server2;

**Входные данные**:

* (System.Object)dynamic;

### Нод Message64

**Имя нода** = Message64;

**Описание** = 0x00000607;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Server2;

**Входные данные**:

* (System.Object)Server2;
* (System.String)bstrText;
* (System.Object)McCOM2.MessageIcon;
* (System.Object)vParent;

### Нод Update

**Имя нода** = Update;

**Описание** = 0x00000608;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Server2;

**Входные данные**:

* (System.Object)Server2;

## Группа NVP_nanoCAD_COM.McCOM2.Server

### Нод _Server_Constructor

**Имя нода** = _Server_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Server;

**Входные данные**:

* (System.Object)dynamic;

### Нод _Server_ConstructorCast

**Имя нода** = _Server_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Server;

**Входные данные**:

* (System.Object)dynamic;

### Нод Properties

**Имя нода** = Properties;

**Описание** = 0x000000ca;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Server;

**Входные данные**:

* (System.Object)Server;

### Нод Title

**Имя нода** = Title;

**Описание** = 0x000000cc;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Server;

**Входные данные**:

* (System.Object)Server;

### Нод DimScale

**Имя нода** = DimScale;

**Описание** = 0x000000c8;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Server;

**Входные данные**:

* (System.Object)Server;

### Нод Set_DimScale

**Имя нода** = Set_DimScale;

**Описание** = 0x000000c8;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Server;

**Входные данные**:

* (System.Object)Server;
* (System.Double)prDimScale;

### Нод ViewScale

**Имя нода** = ViewScale;

**Описание** = 0x000000cd;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Server;

**Входные данные**:

* (System.Object)Server;

### Нод Set_ViewScale

**Имя нода** = Set_ViewScale;

**Описание** = 0x000000cd;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Server;

**Входные данные**:

* (System.Object)Server;
* (System.Double)prViewScale;

### Нод Scales

**Имя нода** = Scales;

**Описание** = 0x000000cb;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Server;

**Входные данные**:

* (System.Object)Server;

### Нод ScaleToStr

**Имя нода** = ScaleToStr;

**Описание** = 0x000000c6;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Server;

**Входные данные**:

* (System.Object)Server;
* (System.Double)rScale;

### Нод ScaleFromStr

**Имя нода** = ScaleFromStr;

**Описание** = 0x000000c5;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Server;

**Входные данные**:

* (System.Object)Server;
* (System.String)bstrScale;

### Нод Object

**Имя нода** = Object;

**Описание** = 0x000000c9;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Server;

**Входные данные**:

* (System.Object)Server;
* (System.Object)vID;

### Нод CreateObject

**Имя нода** = CreateObject;

**Описание** = 0x000000c1;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Server;

**Входные данные**:

* (System.Object)Server;
* (System.String)bstrClass;

### Нод Query

**Имя нода** = Query;

**Описание** = 0x000000c3;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Server;

**Входные данные**:

* (System.Object)Server;
* (System.Object)bstrClasses;
* (System.Object)vProps;
* (System.Object)rBoundBlock;

### Нод Select

**Имя нода** = Select;

**Описание** = 0x000000c7;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Server;

**Входные данные**:

* (System.Object)Server;
* (System.String)bstrPropmt;
* (System.Object)bSingleObject;

### Нод RegisterCalculator

**Имя нода** = RegisterCalculator;

**Описание** = 0x000000c4;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Server;

**Входные данные**:

* (System.Object)Server;
* (System.String)bstrName;
* (System.String)bstrSubName;
* (System.Object)vScriptId;
* (System.String)bstrCLSID;

### Нод Message

**Имя нода** = Message;

**Описание** = 0x000000c2;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Server;

**Входные данные**:

* (System.Object)Server;
* (System.String)bstrText;
* (System.Object)McCOM2.MessageIcon;
* (System.Object)idParent;

## Группа NVP_nanoCAD_COM.McCOM2.Recent

### Нод _Recent_Constructor

**Имя нода** = _Recent_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Recent;

**Входные данные**:

* (System.Object)dynamic;

### Нод _Recent_ConstructorCast

**Имя нода** = _Recent_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Recent;

**Входные данные**:

* (System.Object)dynamic;

### Нод Key

**Имя нода** = Key;

**Описание** = 0x000000bd;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Recent;

**Входные данные**:

* (System.Object)Recent;

### Нод Set_Key

**Имя нода** = Set_Key;

**Описание** = 0x000000bd;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Recent;

**Входные данные**:

* (System.Object)Recent;
* (System.String)pbstrKey;

### Нод Values

**Имя нода** = Values;

**Описание** = 0x000000bf;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Recent;

**Входные данные**:

* (System.Object)Recent;
* (System.String)bstrSubkey;

### Нод NamedValues

**Имя нода** = NamedValues;

**Описание** = 0x000000be;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Recent;

**Входные данные**:

* (System.Object)Recent;
* (System.String)bstrSubkey;

### Нод Add

**Имя нода** = Add;

**Описание** = 0x000000bb;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Recent;

**Входные данные**:

* (System.Object)Recent;
* (System.String)bstrValue;
* (System.String)bstrSubkey;

### Нод Remove

**Имя нода** = Remove;

**Описание** = 0x000000bc;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Recent;

**Входные данные**:

* (System.Object)Recent;
* (System.String)bstrValue;
* (System.String)bstrSubkey;

## Группа NVP_nanoCAD_COM.McCOM2.Property

### Нод _Property_Constructor

**Имя нода** = _Property_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Property;

**Входные данные**:

* (System.Object)dynamic;

### Нод _Property_ConstructorCast

**Имя нода** = _Property_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Property;

**Входные данные**:

* (System.Object)dynamic;

### Нод Name

**Имя нода** = Name;

**Описание** = 0x000000b5;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Property;

**Входные данные**:

* (System.Object)Property;

### Нод Alias

**Имя нода** = Alias;

**Описание** = 0x000000ad;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Property;

**Входные данные**:

* (System.Object)Property;

### Нод Type

**Имя нода** = Type;

**Описание** = 0x000000b8;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Property;

**Входные данные**:

* (System.Object)Property;

### Нод Description

**Имя нода** = Description;

**Описание** = 0x000000b1;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Property;

**Входные данные**:

* (System.Object)Property;

### Нод Category

**Имя нода** = Category;

**Описание** = 0x000000b0;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Property;

**Входные данные**:

* (System.Object)Property;

### Нод Enabled

**Имя нода** = Enabled;

**Описание** = 0x000000b2;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Property;

**Входные данные**:

* (System.Object)Property;

### Нод Object

**Имя нода** = Object;

**Описание** = 0x000000b6;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Property;

**Входные данные**:

* (System.Object)Property;

### Нод Value

**Имя нода** = Value;

**Описание** = 0x000000b9;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Property;

**Входные данные**:

* (System.Object)Property;

### Нод Set_Value

**Имя нода** = Set_Value;

**Описание** = 0x000000b9;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Property;

**Входные данные**:

* (System.Object)Property;
* (System.Object)pResult;

### Нод Bounds

**Имя нода** = Bounds;

**Описание** = 0x000000af;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Property;

**Входные данные**:

* (System.Object)Property;

### Нод Raw

**Имя нода** = Raw;

**Описание** = 0x000000b7;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Property;

**Входные данные**:

* (System.Object)Property;

### Нод Items

**Имя нода** = Items;

**Описание** = 0x000000b4;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Property;

**Входные данные**:

* (System.Object)Property;

### Нод Global

**Имя нода** = Global;

**Описание** = 0x000000b3;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Property;

**Входные данные**:

* (System.Object)Property;

### Нод Binded

**Имя нода** = Binded;

**Описание** = 0x000000ae;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Property;

**Входные данные**:

* (System.Object)Property;

### Нод Set_Binded

**Имя нода** = Set_Binded;

**Описание** = 0x000000ae;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Property;

**Входные данные**:

* (System.Object)Property;
* (System.Object)pvbResult;

## Группа NVP_nanoCAD_COM.McCOM2.PropertiesCollection

### Нод _PropertiesCollection_Constructor

**Имя нода** = _PropertiesCollection_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.PropertiesCollection;

**Входные данные**:

* (System.Object)dynamic;

### Нод _PropertiesCollection_ConstructorCast

**Имя нода** = _PropertiesCollection_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.PropertiesCollection;

**Входные данные**:

* (System.Object)dynamic;

### Нод Item

**Имя нода** = Item;

**Описание** = 0x000000a9;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.PropertiesCollection;

**Входные данные**:

* (System.Object)PropertiesCollection;
* (System.Object)Index;

### Нод Count

**Имя нода** = Count;

**Описание** = 0x000000a8;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.PropertiesCollection;

**Входные данные**:

* (System.Object)PropertiesCollection;

### Нод Object

**Имя нода** = Object;

**Описание** = 0x000000ab;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.PropertiesCollection;

**Входные данные**:

* (System.Object)PropertiesCollection;

### Нод Names

**Имя нода** = Names;

**Описание** = 0x000000aa;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.PropertiesCollection;

**Входные данные**:

* (System.Object)PropertiesCollection;

## Группа NVP_nanoCAD_COM.McCOM2.ObjectsCollection

### Нод _ObjectsCollection_Constructor

**Имя нода** = _ObjectsCollection_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ObjectsCollection;

**Входные данные**:

* (System.Object)dynamic;

### Нод _ObjectsCollection_ConstructorCast

**Имя нода** = _ObjectsCollection_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ObjectsCollection;

**Входные данные**:

* (System.Object)dynamic;

### Нод Item

**Имя нода** = Item;

**Описание** = 0x000000a6;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ObjectsCollection;

**Входные данные**:

* (System.Object)ObjectsCollection;
* (System.Object)Index;

### Нод Count

**Имя нода** = Count;

**Описание** = 0x000000a5;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ObjectsCollection;

**Входные данные**:

* (System.Object)ObjectsCollection;

### Нод Add

**Имя нода** = Add;

**Описание** = 0x0000009e;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ObjectsCollection;

**Входные данные**:

* (System.Object)ObjectsCollection;
* (System.Object)pItem;

### Нод Remove

**Имя нода** = Remove;

**Описание** = 0x000000a3;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ObjectsCollection;

**Входные данные**:

* (System.Object)ObjectsCollection;
* (System.Object)Item;

### Нод Contains

**Имя нода** = Contains;

**Описание** = 0x000000a0;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ObjectsCollection;

**Входные данные**:

* (System.Object)ObjectsCollection;
* (System.Object)pValue;

### Нод Or

**Имя нода** = Or;

**Описание** = 0x000000a2;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ObjectsCollection;

**Входные данные**:

* (System.Object)ObjectsCollection;
* (System.Object)vSecond;

### Нод And

**Имя нода** = And;

**Описание** = 0x0000009f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ObjectsCollection;

**Входные данные**:

* (System.Object)ObjectsCollection;
* (System.Object)vSecond;

### Нод Not

**Имя нода** = Not;

**Описание** = 0x000000a1;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ObjectsCollection;

**Входные данные**:

* (System.Object)ObjectsCollection;
* (System.Object)vSecond;

### Нод Xor

**Имя нода** = Xor;

**Описание** = 0x000000a4;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.ObjectsCollection;

**Входные данные**:

* (System.Object)ObjectsCollection;
* (System.Object)vSecond;

## Группа NVP_nanoCAD_COM.McCOM2.Object

### Нод _Object_Constructor

**Имя нода** = _Object_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Object;

**Входные данные**:

* (System.Object)dynamic;

### Нод _Object_ConstructorCast

**Имя нода** = _Object_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Object;

**Входные данные**:

* (System.Object)dynamic;

### Нод Properties

**Имя нода** = Properties;

**Описание** = 0x0000009a;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Object;

**Входные данные**:

* (System.Object)Object;

### Нод Visible

**Имя нода** = Visible;

**Описание** = 0x0000009c;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Object;

**Входные данные**:

* (System.Object)Object;

### Нод Set_Visible

**Имя нода** = Set_Visible;

**Описание** = 0x0000009c;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Object;

**Входные данные**:

* (System.Object)Object;
* (System.Object)vResult;

### Нод ViewScale

**Имя нода** = ViewScale;

**Описание** = 0x0000009b;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Object;

**Входные данные**:

* (System.Object)Object;

### Нод Set_ViewScale

**Имя нода** = Set_ViewScale;

**Описание** = 0x0000009b;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Object;

**Входные данные**:

* (System.Object)Object;
* (System.Double)prViewScale;

### Нод DimScale

**Имя нода** = DimScale;

**Описание** = 0x00000092;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Object;

**Входные данные**:

* (System.Object)Object;

### Нод Set_DimScale

**Имя нода** = Set_DimScale;

**Описание** = 0x00000092;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Object;

**Входные данные**:

* (System.Object)Object;
* (System.Double)prDimScale;

### Нод Native

**Имя нода** = Native;

**Описание** = 0x00000098;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Object;

**Входные данные**:

* (System.Object)Object;

### Нод Bounds

**Имя нода** = Bounds;

**Описание** = 0x0000008f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Object;

**Входные данные**:

* (System.Object)Object;

### Нод ClassName

**Имя нода** = ClassName;

**Описание** = 0x00000090;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Object;

**Входные данные**:

* (System.Object)Object;

### Нод Id

**Имя нода** = Id;

**Описание** = 0x00000093;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Object;

**Входные данные**:

* (System.Object)Object;

### Нод NativeId

**Имя нода** = NativeId;

**Описание** = 0x00000099;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Object;

**Входные данные**:

* (System.Object)Object;

### Нод Place

**Имя нода** = Place;

**Описание** = 0x0000008d;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Object;

**Входные данные**:

* (System.Object)Object;
* (System.Object)vbDialog;
* (System.Object)vbPosition;
* (System.Object)vParams;

### Нод Edit

**Имя нода** = Edit;

**Описание** = 0x0000008a;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Object;

**Входные данные**:

* (System.Object)Object;
* (System.Object)atPoint;

### Нод Erase

**Имя нода** = Erase;

**Описание** = 0x0000008b;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Object;

**Входные данные**:

* (System.Object)Object;

### Нод Copy

**Имя нода** = Copy;

**Описание** = 0x00000089;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Object;

**Входные данные**:

* (System.Object)Object;

### Нод Transform

**Имя нода** = Transform;

**Описание** = 0x0000008e;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Object;

**Входные данные**:

* (System.Object)Object;
* (System.Object)vMatrix4x4;

### Нод Highlight

**Имя нода** = Highlight;

**Описание** = 0x0000008c;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Object;

**Входные данные**:

* (System.Object)Object;
* (System.Object)vbHighlight;
* (System.Object)rgbColor;
* (System.Object)vbPan;

### Нод Layer

**Имя нода** = Layer;

**Описание** = 0x00000094;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Object;

**Входные данные**:

* (System.Object)Object;

### Нод Set_Layer

**Имя нода** = Set_Layer;

**Описание** = 0x00000094;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Object;

**Входные данные**:

* (System.Object)Object;
* (System.String)pbstrValue;

### Нод Color

**Имя нода** = Color;

**Описание** = 0x00000091;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Object;

**Входные данные**:

* (System.Object)Object;

### Нод Set_Color

**Имя нода** = Set_Color;

**Описание** = 0x00000091;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Object;

**Входные данные**:

* (System.Object)Object;
* (System.Object)prgbValue;

### Нод LineType

**Имя нода** = LineType;

**Описание** = 0x00000095;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Object;

**Входные данные**:

* (System.Object)Object;

### Нод Set_LineType

**Имя нода** = Set_LineType;

**Описание** = 0x00000095;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Object;

**Входные данные**:

* (System.Object)Object;
* (System.Object)McCOM2.LineType;

### Нод LineTypeScale

**Имя нода** = LineTypeScale;

**Описание** = 0x00000096;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Object;

**Входные данные**:

* (System.Object)Object;

### Нод Set_LineTypeScale

**Имя нода** = Set_LineTypeScale;

**Описание** = 0x00000096;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Object;

**Входные данные**:

* (System.Object)Object;
* (System.Double)prValue;

### Нод LineWeight

**Имя нода** = LineWeight;

**Описание** = 0x00000097;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Object;

**Входные данные**:

* (System.Object)Object;

### Нод Set_LineWeight

**Имя нода** = Set_LineWeight;

**Описание** = 0x00000097;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Object;

**Входные данные**:

* (System.Object)Object;
* (System.Double)prValue;

## Группа NVP_nanoCAD_COM.McCOM2.McTransaction

### Нод _McTransaction_Constructor

**Имя нода** = _McTransaction_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.McTransaction;

**Входные данные**:

* (System.Object)dynamic;

### Нод _McTransaction_ConstructorCast

**Имя нода** = _McTransaction_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.McTransaction;

**Входные данные**:

* (System.Object)dynamic;

### Нод Start

**Имя нода** = Start;

**Описание** = 0x0000057a;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.McTransaction;

**Входные данные**:

* (System.Object)McTransaction;
* (System.Object)pObjectToWatch;

### Нод Commit

**Имя нода** = Commit;

**Описание** = 0x00000578;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.McTransaction;

**Входные данные**:

* (System.Object)McTransaction;

### Нод Rollback

**Имя нода** = Rollback;

**Описание** = 0x00000579;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.McTransaction;

**Входные данные**:

* (System.Object)McTransaction;

## Группа NVP_nanoCAD_COM.McCOM2.McCOMReportManager

### Нод _McCOMReportManager_Constructor

**Имя нода** = _McCOMReportManager_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.McCOMReportManager;

**Входные данные**:

* (System.Object)dynamic;

### Нод _McCOMReportManager_ConstructorCast

**Имя нода** = _McCOMReportManager_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.McCOMReportManager;

**Входные данные**:

* (System.Object)dynamic;

### Нод AddKeyword

**Имя нода** = AddKeyword;

**Описание** = method AddKeyword;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.McCOMReportManager;

**Входные данные**:

* (System.Object)McCOMReportManager;
* (System.String)Name;
* (System.String)Value;

## Группа NVP_nanoCAD_COM.McCOM2.McCOMPlugin2

### Нод _McCOMPlugin2_Constructor

**Имя нода** = _McCOMPlugin2_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.McCOMPlugin2;

**Входные данные**:

* (System.Object)dynamic;

### Нод _McCOMPlugin2_ConstructorCast

**Имя нода** = _McCOMPlugin2_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.McCOMPlugin2;

**Входные данные**:

* (System.Object)dynamic;

### Нод Activate

**Имя нода** = Activate;

**Описание** = method Activate;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.McCOMPlugin2;

**Входные данные**:

* (System.Object)McCOMPlugin2;
* (System.Object)pServer;

## Группа NVP_nanoCAD_COM.McCOM2.McCOMCalculator

### Нод _McCOMCalculator_Constructor

**Имя нода** = _McCOMCalculator_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.McCOMCalculator;

**Входные данные**:

* (System.Object)dynamic;

### Нод _McCOMCalculator_ConstructorCast

**Имя нода** = _McCOMCalculator_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.McCOMCalculator;

**Входные данные**:

* (System.Object)dynamic;

### Нод OnCreate

**Имя нода** = OnCreate;

**Описание** = method OnCreate;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.McCOMCalculator;

**Входные данные**:

* (System.Object)McCOMCalculator;
* (System.Object)HWND;

### Нод OnEdit

**Имя нода** = OnEdit;

**Описание** = method OnEdit;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.McCOMCalculator;

**Входные данные**:

* (System.Object)McCOMCalculator;
* (System.Object)HWND;

### Нод GetValueByName

**Имя нода** = GetValueByName;

**Описание** = method Value;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.McCOMCalculator;

**Входные данные**:

* (System.Object)McCOMCalculator;
* (System.String)Name;

### Нод PutValueByName

**Имя нода** = PutValueByName;

**Описание** = method Value;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.McCOMCalculator;

**Входные данные**:

* (System.Object)McCOMCalculator;
* (System.String)Name;
* (System.Object)newVal;

### Нод OnRead

**Имя нода** = OnRead;

**Описание** = method OnRead;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.McCOMCalculator;

**Входные данные**:

* (System.Object)McCOMCalculator;
* (System.Object)vArray;

### Нод OnWrite

**Имя нода** = OnWrite;

**Описание** = method OnWrite;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.McCOMCalculator;

**Входные данные**:

* (System.Object)McCOMCalculator;

### Нод GetValueByIndex

**Имя нода** = GetValueByIndex;

**Описание** = method ValueByIndex;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.McCOMCalculator;

**Входные данные**:

* (System.Object)McCOMCalculator;
* (System.Object)Index;

### Нод PutValueByIndex

**Имя нода** = PutValueByIndex;

**Описание** = method ValueByIndex;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.McCOMCalculator;

**Входные данные**:

* (System.Object)McCOMCalculator;
* (System.Object)Index;
* (System.Object)newVal;

### Нод GetKeywords

**Имя нода** = GetKeywords;

**Описание** = method Keywords for report;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.McCOMCalculator;

**Входные данные**:

* (System.Object)McCOMCalculator;
* (System.Object)pManager;

## Группа NVP_nanoCAD_COM.McCOM2.LeadersCollection

### Нод _LeadersCollection_Constructor

**Имя нода** = _LeadersCollection_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.LeadersCollection;

**Входные данные**:

* (System.Object)dynamic;

### Нод _LeadersCollection_ConstructorCast

**Имя нода** = _LeadersCollection_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.LeadersCollection;

**Входные данные**:

* (System.Object)dynamic;

### Нод Item

**Имя нода** = Item;

**Описание** = 0x00000086;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.LeadersCollection;

**Входные данные**:

* (System.Object)LeadersCollection;
* (System.Object)Index;

### Нод Count

**Имя нода** = Count;

**Описание** = 0x00000085;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.LeadersCollection;

**Входные данные**:

* (System.Object)LeadersCollection;

### Нод Add

**Имя нода** = Add;

**Описание** = 0x00000083;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.LeadersCollection;

**Входные данные**:

* (System.Object)LeadersCollection;
* (System.Object)vPoint;
* (System.Object)McCOM2.ArrowType;
* (System.Double)rArrowSize;

### Нод Remove

**Имя нода** = Remove;

**Описание** = 0x00000084;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.LeadersCollection;

**Входные данные**:

* (System.Object)LeadersCollection;
* (System.Object)Item;

### Нод Object

**Имя нода** = Object;

**Описание** = 0x00000087;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.LeadersCollection;

**Входные данные**:

* (System.Object)LeadersCollection;

## Группа NVP_nanoCAD_COM.McCOM2.Leader

### Нод _Leader_Constructor

**Имя нода** = _Leader_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Leader;

**Входные данные**:

* (System.Object)dynamic;

### Нод _Leader_ConstructorCast

**Имя нода** = _Leader_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Leader;

**Входные данные**:

* (System.Object)dynamic;

### Нод Position

**Имя нода** = Position;

**Описание** = 0x00000081;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Leader;

**Входные данные**:

* (System.Object)Leader;

### Нод Set_Position

**Имя нода** = Set_Position;

**Описание** = 0x00000081;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Leader;

**Входные данные**:

* (System.Object)Leader;
* (System.Object)pvPoint;

### Нод Arrow

**Имя нода** = Arrow;

**Описание** = 0x0000007e;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Leader;

**Входные данные**:

* (System.Object)Leader;

### Нод Set_Arrow

**Имя нода** = Set_Arrow;

**Описание** = 0x0000007e;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Leader;

**Входные данные**:

* (System.Object)Leader;
* (System.Object)McCOM2.ArrowType;

### Нод ArrowSize

**Имя нода** = ArrowSize;

**Описание** = 0x0000007f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Leader;

**Входные данные**:

* (System.Object)Leader;

### Нод Set_ArrowSize

**Имя нода** = Set_ArrowSize;

**Описание** = 0x0000007f;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Leader;

**Входные данные**:

* (System.Object)Leader;
* (System.Double)pnSize;

### Нод Object

**Имя нода** = Object;

**Описание** = 0x00000080;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Leader;

**Входные данные**:

* (System.Object)Leader;

## Группа NVP_nanoCAD_COM.McCOM2.Form

### Нод _Form_Constructor

**Имя нода** = _Form_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Form;

**Входные данные**:

* (System.Object)dynamic;

### Нод _Form_ConstructorCast

**Имя нода** = _Form_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Form;

**Входные данные**:

* (System.Object)dynamic;

### Нод Item

**Имя нода** = Item;

**Описание** = 0x000005e9;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Form;

**Входные данные**:

* (System.Object)Form;
* (System.Object)vIndex;

### Нод Count

**Имя нода** = Count;

**Описание** = 0x000005ea;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Form;

**Входные данные**:

* (System.Object)Form;

### Нод Width

**Имя нода** = Width;

**Описание** = 0x000005f0;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Form;

**Входные данные**:

* (System.Object)Form;

### Нод Height

**Имя нода** = Height;

**Описание** = 0x000005f1;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Form;

**Входные данные**:

* (System.Object)Form;

### Нод Show

**Имя нода** = Show;

**Описание** = 0x000005eb;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Form;

**Входные данные**:

* (System.Object)Form;

### Нод Load

**Имя нода** = Load;

**Описание** = 0x000005ec;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Form;

**Входные данные**:

* (System.Object)Form;
* (System.Object)vSource;

## Группа NVP_nanoCAD_COM.McCOM2.enum_TextAlignment

### Нод enum_mcAlignBottomLeft

**Имя нода** = enum_mcAlignBottomLeft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_TextAlignment;


### Нод enum_mcAlignBottomCenter

**Имя нода** = enum_mcAlignBottomCenter;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_TextAlignment;


### Нод enum_mcAlignBottomRight

**Имя нода** = enum_mcAlignBottomRight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_TextAlignment;


### Нод enum_mcAlignMiddleLeft

**Имя нода** = enum_mcAlignMiddleLeft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_TextAlignment;


### Нод enum_mcAlignMiddleCenter

**Имя нода** = enum_mcAlignMiddleCenter;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_TextAlignment;


### Нод enum_mcAlignMiddleRight

**Имя нода** = enum_mcAlignMiddleRight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_TextAlignment;


### Нод enum_mcAlignTopLeft

**Имя нода** = enum_mcAlignTopLeft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_TextAlignment;


### Нод enum_mcAlignTopCenter

**Имя нода** = enum_mcAlignTopCenter;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_TextAlignment;


### Нод enum_mcAlignTopRight

**Имя нода** = enum_mcAlignTopRight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_TextAlignment;


### Нод enum_mcAlignInconsistent

**Имя нода** = enum_mcAlignInconsistent;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_TextAlignment;


## Группа NVP_nanoCAD_COM.McCOM2.enum_SymWeldType

### Нод enum_mcWeldFactoryAngularSolidVisible

**Имя нода** = enum_mcWeldFactoryAngularSolidVisible;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymWeldType;


### Нод enum_mcWeldFactoryAngularSolidInvisible

**Имя нода** = enum_mcWeldFactoryAngularSolidInvisible;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymWeldType;


### Нод enum_mcWeldFactoryAngularDescreteVisible

**Имя нода** = enum_mcWeldFactoryAngularDescreteVisible;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymWeldType;


### Нод enum_mcWeldFactoryAngularDescreteInvisible

**Имя нода** = enum_mcWeldFactoryAngularDescreteInvisible;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymWeldType;


### Нод enum_mcWeldAssemblyAngularSolidVisible

**Имя нода** = enum_mcWeldAssemblyAngularSolidVisible;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymWeldType;


### Нод enum_mcWeldAssemblyAngularSolidInvisible

**Имя нода** = enum_mcWeldAssemblyAngularSolidInvisible;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymWeldType;


### Нод enum_mcWeldAssemblyAngularDescreteVisible

**Имя нода** = enum_mcWeldAssemblyAngularDescreteVisible;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymWeldType;


### Нод enum_mcWeldAssemblyAngularDescreteInvisible

**Имя нода** = enum_mcWeldAssemblyAngularDescreteInvisible;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymWeldType;


### Нод enum_mcWeldFactoryButtSolidVisible

**Имя нода** = enum_mcWeldFactoryButtSolidVisible;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymWeldType;


### Нод enum_mcWeldFactoryButtSolidInvisible

**Имя нода** = enum_mcWeldFactoryButtSolidInvisible;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymWeldType;


### Нод enum_mcWeldFactoryButtDescreteVisible

**Имя нода** = enum_mcWeldFactoryButtDescreteVisible;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymWeldType;


### Нод enum_mcWeldFactoryButtDescreteInvisible

**Имя нода** = enum_mcWeldFactoryButtDescreteInvisible;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymWeldType;


### Нод enum_mcWeldAssemblyButtSolidVisible

**Имя нода** = enum_mcWeldAssemblyButtSolidVisible;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymWeldType;


### Нод enum_mcWeldAssemblyButtSolidInvisible

**Имя нода** = enum_mcWeldAssemblyButtSolidInvisible;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymWeldType;


### Нод enum_mcWeldAssemblyButtDescreteVisible

**Имя нода** = enum_mcWeldAssemblyButtDescreteVisible;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymWeldType;


### Нод enum_mcWeldAssemblyButtDescreteInvisible

**Имя нода** = enum_mcWeldAssemblyButtDescreteInvisible;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymWeldType;


## Группа NVP_nanoCAD_COM.McCOM2.enum_SymToleranceParameter

### Нод enum_mcSymToleranceUndefined

**Имя нода** = enum_mcSymToleranceUndefined;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymToleranceParameter;


### Нод enum_mcSymToleranceLinear

**Имя нода** = enum_mcSymToleranceLinear;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymToleranceParameter;


### Нод enum_mcSymTolerancePlanar

**Имя нода** = enum_mcSymTolerancePlanar;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymToleranceParameter;


### Нод enum_mcSymToleranceRound

**Имя нода** = enum_mcSymToleranceRound;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymToleranceParameter;


### Нод enum_mcSymToleranceCylindrical

**Имя нода** = enum_mcSymToleranceCylindrical;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymToleranceParameter;


### Нод enum_mcSymToleranceProfile

**Имя нода** = enum_mcSymToleranceProfile;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymToleranceParameter;


### Нод enum_mcSymToleranceParallel

**Имя нода** = enum_mcSymToleranceParallel;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymToleranceParameter;


### Нод enum_mcSymTolerancePerpendicular

**Имя нода** = enum_mcSymTolerancePerpendicular;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymToleranceParameter;


### Нод enum_mcSymToleranceTilt

**Имя нода** = enum_mcSymToleranceTilt;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymToleranceParameter;


### Нод enum_mcSymToleranceCoaxiality

**Имя нода** = enum_mcSymToleranceCoaxiality;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymToleranceParameter;


### Нод enum_mcSymToleranceSymmetry

**Имя нода** = enum_mcSymToleranceSymmetry;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymToleranceParameter;


### Нод enum_mcSymTolerancePosition

**Имя нода** = enum_mcSymTolerancePosition;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymToleranceParameter;


### Нод enum_mcSymToleranceCross

**Имя нода** = enum_mcSymToleranceCross;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymToleranceParameter;


### Нод enum_mcSymToleranceBeat

**Имя нода** = enum_mcSymToleranceBeat;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymToleranceParameter;


### Нод enum_mcSymToleranceFullBeat

**Имя нода** = enum_mcSymToleranceFullBeat;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymToleranceParameter;


### Нод enum_mcSymToleranceFormProfile

**Имя нода** = enum_mcSymToleranceFormProfile;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymToleranceParameter;


### Нод enum_mcSymToleranceFormSurface

**Имя нода** = enum_mcSymToleranceFormSurface;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymToleranceParameter;


## Группа NVP_nanoCAD_COM.McCOM2.enum_SymTableVertFit

### Нод enum_mcVertFitNone

**Имя нода** = enum_mcVertFitNone;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymTableVertFit;


### Нод enum_mcVertFitShrink

**Имя нода** = enum_mcVertFitShrink;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymTableVertFit;


### Нод enum_mcVertFitExpandRow

**Имя нода** = enum_mcVertFitExpandRow;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymTableVertFit;


### Нод enum_mcVertFitAddRow

**Имя нода** = enum_mcVertFitAddRow;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymTableVertFit;


## Группа NVP_nanoCAD_COM.McCOM2.enum_SymTableHorzFit

### Нод enum_mcHorzFitNone

**Имя нода** = enum_mcHorzFitNone;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymTableHorzFit;


### Нод enum_mcHorzFitShrink

**Имя нода** = enum_mcHorzFitShrink;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymTableHorzFit;


### Нод enum_mcHorzFitWrap

**Имя нода** = enum_mcHorzFitWrap;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymTableHorzFit;


## Группа NVP_nanoCAD_COM.McCOM2.enum_SymTableBorder

### Нод enum_mcBorderLeft

**Имя нода** = enum_mcBorderLeft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymTableBorder;


### Нод enum_mcBorderTop

**Имя нода** = enum_mcBorderTop;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymTableBorder;


### Нод enum_mcBorderRight

**Имя нода** = enum_mcBorderRight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymTableBorder;


### Нод enum_mcBorderBottom

**Имя нода** = enum_mcBorderBottom;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymTableBorder;


### Нод enum_mcBorderInnerVert

**Имя нода** = enum_mcBorderInnerVert;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymTableBorder;


### Нод enum_mcBorderInnerHorz

**Имя нода** = enum_mcBorderInnerHorz;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymTableBorder;


## Группа NVP_nanoCAD_COM.McCOM2.enum_SymSubtableMode

### Нод enum_mcSubtableInSingleCell

**Имя нода** = enum_mcSubtableInSingleCell;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymSubtableMode;


### Нод enum_mcSubtableCellByCell

**Имя нода** = enum_mcSubtableCellByCell;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymSubtableMode;


### Нод enum_mcSubtableOver

**Имя нода** = enum_mcSubtableOver;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymSubtableMode;


## Группа NVP_nanoCAD_COM.McCOM2.enum_SymFixType

### Нод enum_mcFixTypeWeldLeg

**Имя нода** = enum_mcFixTypeWeldLeg;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymFixType;


### Нод enum_mcFixTypeWeldPoint

**Имя нода** = enum_mcFixTypeWeldPoint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymFixType;


### Нод enum_mcFixTypeMark

**Имя нода** = enum_mcFixTypeMark;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymFixType;


## Группа NVP_nanoCAD_COM.McCOM2.enum_SymFixTechnology

### Нод enum_mcFixTechnologyWelded

**Имя нода** = enum_mcFixTechnologyWelded;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymFixTechnology;


### Нод enum_mcFixTechnologySoldered

**Имя нода** = enum_mcFixTechnologySoldered;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymFixTechnology;


### Нод enum_mcFixTechnologyGlued

**Имя нода** = enum_mcFixTechnologyGlued;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymFixTechnology;


### Нод enum_mcFixTechnologyAngularClamp

**Имя нода** = enum_mcFixTechnologyAngularClamp;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymFixTechnology;


### Нод enum_mcFixTechnologyOverlapClamp

**Имя нода** = enum_mcFixTechnologyOverlapClamp;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymFixTechnology;


### Нод enum_mcFixTechnologyCrossLinked

**Имя нода** = enum_mcFixTechnologyCrossLinked;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymFixTechnology;


## Группа NVP_nanoCAD_COM.McCOM2.enum_SymDimensionType

### Нод enum_mcDimtypeUnknown

**Имя нода** = enum_mcDimtypeUnknown;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymDimensionType;


### Нод enum_mcDimtypeLinear

**Имя нода** = enum_mcDimtypeLinear;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymDimensionType;


### Нод enum_mcDimtypeVertical

**Имя нода** = enum_mcDimtypeVertical;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymDimensionType;


### Нод enum_mcDimtypeHorizontal

**Имя нода** = enum_mcDimtypeHorizontal;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymDimensionType;


### Нод enum_mcDimtypeAligned

**Имя нода** = enum_mcDimtypeAligned;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymDimensionType;


### Нод enum_mcDimtypeDiameter

**Имя нода** = enum_mcDimtypeDiameter;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymDimensionType;


### Нод enum_mcDimtypeRadius

**Имя нода** = enum_mcDimtypeRadius;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymDimensionType;


### Нод enum_mcDimtypeHugeRadius

**Имя нода** = enum_mcDimtypeHugeRadius;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymDimensionType;


### Нод enum_mcDimtypeAngular

**Имя нода** = enum_mcDimtypeAngular;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymDimensionType;


### Нод enum_mcDimtypeArc

**Имя нода** = enum_mcDimtypeArc;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymDimensionType;


### Нод enum_mcDimtypeOrdinate

**Имя нода** = enum_mcDimtypeOrdinate;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymDimensionType;


## Группа NVP_nanoCAD_COM.McCOM2.enum_SymDimensionTolView

### Нод enum_mcDimtolAll

**Имя нода** = enum_mcDimtolAll;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymDimensionTolView;


### Нод enum_mcDimtolValuesOnly

**Имя нода** = enum_mcDimtolValuesOnly;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymDimensionTolView;


### Нод enum_mcDimtolFitInLine

**Имя нода** = enum_mcDimtolFitInLine;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymDimensionTolView;


### Нод enum_mcDimtolFitInStack

**Имя нода** = enum_mcDimtolFitInStack;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymDimensionTolView;


### Нод enum_mcDimtolFieldOnly

**Имя нода** = enum_mcDimtolFieldOnly;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymDimensionTolView;


### Нод enum_mcDimtolNone

**Имя нода** = enum_mcDimtolNone;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymDimensionTolView;


## Группа NVP_nanoCAD_COM.McCOM2.enum_SymDimensionSymbol

### Нод enum_mcDimSymbolNone

**Имя нода** = enum_mcDimSymbolNone;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymDimensionSymbol;


### Нод enum_mcDimSymbolRadius

**Имя нода** = enum_mcDimSymbolRadius;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymDimensionSymbol;


### Нод enum_mcDimSymbolDiameter

**Имя нода** = enum_mcDimSymbolDiameter;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymDimensionSymbol;


### Нод enum_mcDimSymbolSquare

**Имя нода** = enum_mcDimSymbolSquare;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymDimensionSymbol;


### Нод enum_mcDimSymbolMetric

**Имя нода** = enum_mcDimSymbolMetric;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymDimensionSymbol;


### Нод enum_mcDimSymbolLeftGradient

**Имя нода** = enum_mcDimSymbolLeftGradient;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymDimensionSymbol;


### Нод enum_mcDimSymbolRightGradient

**Имя нода** = enum_mcDimSymbolRightGradient;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymDimensionSymbol;


### Нод enum_mcDimSymbolLeftConicity

**Имя нода** = enum_mcDimSymbolLeftConicity;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymDimensionSymbol;


### Нод enum_mcDimSymbolRightConicity

**Имя нода** = enum_mcDimSymbolRightConicity;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymDimensionSymbol;


### Нод enum_mcDimSymbolSRadius

**Имя нода** = enum_mcDimSymbolSRadius;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymDimensionSymbol;


### Нод enum_mcDimSymbolSDiameter

**Имя нода** = enum_mcDimSymbolSDiameter;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymDimensionSymbol;


### Нод enum_mcDimSymbolP

**Имя нода** = enum_mcDimSymbolP;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymDimensionSymbol;


### Нод enum_mcDimSymbolM

**Имя нода** = enum_mcDimSymbolM;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymDimensionSymbol;


## Группа NVP_nanoCAD_COM.McCOM2.enum_SymDimensionBracket

### Нод enum_mcDimbracketNone

**Имя нода** = enum_mcDimbracketNone;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymDimensionBracket;


### Нод enum_mcDimbracketSquare

**Имя нода** = enum_mcDimbracketSquare;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymDimensionBracket;


### Нод enum_mcDimbracketRound

**Имя нода** = enum_mcDimbracketRound;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymDimensionBracket;


### Нод enum_mcDimbracketAngular

**Имя нода** = enum_mcDimbracketAngular;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymDimensionBracket;


## Группа NVP_nanoCAD_COM.McCOM2.enum_SymBreakType

### Нод enum_mcBreakSingleLine

**Имя нода** = enum_mcBreakSingleLine;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymBreakType;


### Нод enum_mcBreakDoubleLine

**Имя нода** = enum_mcBreakDoubleLine;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymBreakType;


### Нод enum_mcBreakSingleCurve

**Имя нода** = enum_mcBreakSingleCurve;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymBreakType;


### Нод enum_mcBreakDoubleCurve

**Имя нода** = enum_mcBreakDoubleCurve;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymBreakType;


### Нод enum_mcBreakTubular

**Имя нода** = enum_mcBreakTubular;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymBreakType;


## Группа NVP_nanoCAD_COM.McCOM2.enum_SymBoundFormType

### Нод enum_mcBoundLineOnly

**Имя нода** = enum_mcBoundLineOnly;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymBoundFormType;


### Нод enum_mcBoundDashBand

**Имя нода** = enum_mcBoundDashBand;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymBoundFormType;


### Нод enum_mcBoundHeatIsolation

**Имя нода** = enum_mcBoundHeatIsolation;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymBoundFormType;


### Нод enum_mcBoundWaterIsolation

**Имя нода** = enum_mcBoundWaterIsolation;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymBoundFormType;


### Нод enum_mcBoundBoundHatch

**Имя нода** = enum_mcBoundBoundHatch;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymBoundFormType;


### Нод enum_mcBoundGroundBorder

**Имя нода** = enum_mcBoundGroundBorder;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymBoundFormType;


## Группа NVP_nanoCAD_COM.McCOM2.enum_SymAnchorType

### Нод enum_mcAnchorEmpty

**Имя нода** = enum_mcAnchorEmpty;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymAnchorType;


### Нод enum_mcAnchorMarkT

**Имя нода** = enum_mcAnchorMarkT;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymAnchorType;


### Нод enum_mcAnchorMarkJLeft

**Имя нода** = enum_mcAnchorMarkJLeft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymAnchorType;


### Нод enum_mcAnchorMarkJRight

**Имя нода** = enum_mcAnchorMarkJRight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymAnchorType;


### Нод enum_mcAnchorMarkULeft

**Имя нода** = enum_mcAnchorMarkULeft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymAnchorType;


### Нод enum_mcAnchorMarkURight

**Имя нода** = enum_mcAnchorMarkURight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymAnchorType;


### Нод enum_mcAnchorMarkLLeft

**Имя нода** = enum_mcAnchorMarkLLeft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymAnchorType;


### Нод enum_mcAnchorMarkLRight

**Имя нода** = enum_mcAnchorMarkLRight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymAnchorType;


### Нод enum_mcAnchorMarkPlus

**Имя нода** = enum_mcAnchorMarkPlus;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymAnchorType;


### Нод enum_mcAnchorMarkX

**Имя нода** = enum_mcAnchorMarkX;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymAnchorType;


### Нод enum_mcAnchorMarkOFilled

**Имя нода** = enum_mcAnchorMarkOFilled;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymAnchorType;


### Нод enum_mcAnchorMarkOBlank

**Имя нода** = enum_mcAnchorMarkOBlank;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymAnchorType;


### Нод enum_mcAnchorMarkDelta

**Имя нода** = enum_mcAnchorMarkDelta;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymAnchorType;


### Нод enum_mcAnchorMarkD

**Имя нода** = enum_mcAnchorMarkD;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymAnchorType;


### Нод enum_mcAnchorArrowOFilled

**Имя нода** = enum_mcAnchorArrowOFilled;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymAnchorType;


### Нод enum_mcAnchorArrowOpen20

**Имя нода** = enum_mcAnchorArrowOpen20;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymAnchorType;


### Нод enum_mcAnchorArrowOpen

**Имя нода** = enum_mcAnchorArrowOpen;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymAnchorType;


### Нод enum_mcAnchorArrowClosed20

**Имя нода** = enum_mcAnchorArrowClosed20;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymAnchorType;


### Нод enum_mcAnchorArrowShallow20

**Имя нода** = enum_mcAnchorArrowShallow20;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymAnchorType;


### Нод enum_mcAnchorArrowOBlank

**Имя нода** = enum_mcAnchorArrowOBlank;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymAnchorType;


### Нод enum_mcAnchorArrowDFilled

**Имя нода** = enum_mcAnchorArrowDFilled;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymAnchorType;


### Нод enum_mcAnchorArrowDBlank

**Имя нода** = enum_mcAnchorArrowDBlank;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymAnchorType;


### Нод enum_mcAnchorArrowS

**Имя нода** = enum_mcAnchorArrowS;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymAnchorType;


### Нод enum_mcAnchorArrowSqrBlank

**Имя нода** = enum_mcAnchorArrowSqrBlank;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymAnchorType;


### Нод enum_mcAnchorArrowSqrFilled

**Имя нода** = enum_mcAnchorArrowSqrFilled;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymAnchorType;


### Нод enum_mcAnchorArrowShallowLeft

**Имя нода** = enum_mcAnchorArrowShallowLeft;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymAnchorType;


### Нод enum_mcAnchorArrowShallowRight

**Имя нода** = enum_mcAnchorArrowShallowRight;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymAnchorType;


### Нод enum_mcAnchorArrow60Blank

**Имя нода** = enum_mcAnchorArrow60Blank;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymAnchorType;


### Нод enum_mcAnchorArrow60Filled

**Имя нода** = enum_mcAnchorArrow60Filled;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_SymAnchorType;


## Группа NVP_nanoCAD_COM.McCOM2.enum_RoughnessProcessingType

### Нод enum_mcProcessingTypeDefault

**Имя нода** = enum_mcProcessingTypeDefault;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_RoughnessProcessingType;


### Нод enum_mcProcessingTypeCut

**Имя нода** = enum_mcProcessingTypeCut;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_RoughnessProcessingType;


### Нод enum_mcProcessingTypeNoCut

**Имя нода** = enum_mcProcessingTypeNoCut;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_RoughnessProcessingType;


## Группа NVP_nanoCAD_COM.McCOM2.enum_RoughnessProcessingDirection

### Нод enum_mcProcessingDirNone

**Имя нода** = enum_mcProcessingDirNone;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_RoughnessProcessingDirection;


### Нод enum_mcProcessingDirParallel

**Имя нода** = enum_mcProcessingDirParallel;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_RoughnessProcessingDirection;


### Нод enum_mcProcessingDirPerpendicular

**Имя нода** = enum_mcProcessingDirPerpendicular;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_RoughnessProcessingDirection;


### Нод enum_mcProcessingDirCrossing

**Имя нода** = enum_mcProcessingDirCrossing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_RoughnessProcessingDirection;


### Нод enum_mcProcessingDirRadial

**Имя нода** = enum_mcProcessingDirRadial;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_RoughnessProcessingDirection;


### Нод enum_mcProcessingDirCircular

**Имя нода** = enum_mcProcessingDirCircular;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_RoughnessProcessingDirection;


### Нод enum_mcProcessingDirFree

**Имя нода** = enum_mcProcessingDirFree;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_RoughnessProcessingDirection;


### Нод enum_mcProcessingDirPoint

**Имя нода** = enum_mcProcessingDirPoint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_RoughnessProcessingDirection;


## Группа NVP_nanoCAD_COM.McCOM2.enum_PropertyType

### Нод enum_mcPtEmpty

**Имя нода** = enum_mcPtEmpty;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_PropertyType;


### Нод enum_mcPtInteger

**Имя нода** = enum_mcPtInteger;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_PropertyType;


### Нод enum_mcPtIntegerArray

**Имя нода** = enum_mcPtIntegerArray;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_PropertyType;


### Нод enum_mcPtString

**Имя нода** = enum_mcPtString;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_PropertyType;


### Нод enum_mcPtStringArray

**Имя нода** = enum_mcPtStringArray;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_PropertyType;


### Нод enum_mcPtDouble

**Имя нода** = enum_mcPtDouble;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_PropertyType;


### Нод enum_mcPtDoubleArray

**Имя нода** = enum_mcPtDoubleArray;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_PropertyType;


### Нод enum_mcPtPoint

**Имя нода** = enum_mcPtPoint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_PropertyType;


### Нод enum_mcPtVector

**Имя нода** = enum_mcPtVector;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_PropertyType;


### Нод enum_mcPtBoolean

**Имя нода** = enum_mcPtBoolean;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_PropertyType;


### Нод enum_mcPtObject

**Имя нода** = enum_mcPtObject;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_PropertyType;


### Нод enum_mcPtPlane

**Имя нода** = enum_mcPtPlane;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_PropertyType;


## Группа NVP_nanoCAD_COM.McCOM2.enum_PolymorphShape

### Нод enum_mcMarkerShapeCircle

**Имя нода** = enum_mcMarkerShapeCircle;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_PolymorphShape;


### Нод enum_mcMarkerShapeSquare

**Имя нода** = enum_mcMarkerShapeSquare;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_PolymorphShape;


### Нод enum_mcMarkerShapeRomb

**Имя нода** = enum_mcMarkerShapeRomb;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_PolymorphShape;


## Группа NVP_nanoCAD_COM.McCOM2.enum_PlaneLevelShape

### Нод enum_mcLevelShapeSimple

**Имя нода** = enum_mcLevelShapeSimple;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_PlaneLevelShape;


### Нод enum_mcLevelShapeUnderlined

**Имя нода** = enum_mcLevelShapeUnderlined;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_PlaneLevelShape;


### Нод enum_mcLevelShapeFramed

**Имя нода** = enum_mcLevelShapeFramed;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_PlaneLevelShape;


## Группа NVP_nanoCAD_COM.McCOM2.enum_MessageIcon

### Нод enum_mcMessageSimple

**Имя нода** = enum_mcMessageSimple;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_MessageIcon;


### Нод enum_mcMessageText

**Имя нода** = enum_mcMessageText;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_MessageIcon;


### Нод enum_mcMessageWarning

**Имя нода** = enum_mcMessageWarning;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_MessageIcon;


### Нод enum_mcMessageCure

**Имя нода** = enum_mcMessageCure;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_MessageIcon;


### Нод enum_mcMessageHint

**Имя нода** = enum_mcMessageHint;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_MessageIcon;


### Нод enum_mcMessageError

**Имя нода** = enum_mcMessageError;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_MessageIcon;


## Группа NVP_nanoCAD_COM.McCOM2.enum_LineType

### Нод enum_mcLinetypeByBlock

**Имя нода** = enum_mcLinetypeByBlock;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_LineType;


### Нод enum_mcLinetypeByLayer

**Имя нода** = enum_mcLinetypeByLayer;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_LineType;


### Нод enum_mcLinetypeMainBold

**Имя нода** = enum_mcLinetypeMainBold;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_LineType;


### Нод enum_mcLinetypeMainThin

**Имя нода** = enum_mcLinetypeMainThin;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_LineType;


### Нод enum_mcLinetypeWaveThin

**Имя нода** = enum_mcLinetypeWaveThin;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_LineType;


### Нод enum_mcLinetypeDashThin

**Имя нода** = enum_mcLinetypeDashThin;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_LineType;


### Нод enum_mcLinetypeDashDotThin

**Имя нода** = enum_mcLinetypeDashDotThin;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_LineType;


### Нод enum_mcLinetypeDashDotBold

**Имя нода** = enum_mcLinetypeDashDotBold;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_LineType;


### Нод enum_mcLinetypeBrokenBold

**Имя нода** = enum_mcLinetypeBrokenBold;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_LineType;


### Нод enum_mcLinetypeZigzagThin

**Имя нода** = enum_mcLinetypeZigzagThin;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_LineType;


### Нод enum_mcLinetypeDashDotDotThin

**Имя нода** = enum_mcLinetypeDashDotDotThin;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_LineType;


## Группа NVP_nanoCAD_COM.McCOM2.enum_GradientUnitType

### Нод enum_mcGradUnitNone

**Имя нода** = enum_mcGradUnitNone;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_GradientUnitType;


### Нод enum_mcGradUnitRatio

**Имя нода** = enum_mcGradUnitRatio;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_GradientUnitType;


### Нод enum_mcGradUnitDecimal

**Имя нода** = enum_mcGradUnitDecimal;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_GradientUnitType;


### Нод enum_mcGradUnitPercent

**Имя нода** = enum_mcGradUnitPercent;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_GradientUnitType;


### Нод enum_mcGradUnitPermille

**Имя нода** = enum_mcGradUnitPermille;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_GradientUnitType;


### Нод enum_mcGradUnitAngle

**Имя нода** = enum_mcGradUnitAngle;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_GradientUnitType;


## Группа NVP_nanoCAD_COM.McCOM2.enum_BOMExportFormat

### Нод enum_mcExportDrawing

**Имя нода** = enum_mcExportDrawing;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_BOMExportFormat;


### Нод enum_mcExportExcel

**Имя нода** = enum_mcExportExcel;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_BOMExportFormat;


### Нод enum_mcExportTechnologiCS

**Имя нода** = enum_mcExportTechnologiCS;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_BOMExportFormat;


### Нод enum_mcExportDBF

**Имя нода** = enum_mcExportDBF;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_BOMExportFormat;


## Группа NVP_nanoCAD_COM.McCOM2.enum_ArrowType

### Нод enum_mcArrowUnknown

**Имя нода** = enum_mcArrowUnknown;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_ArrowType;


### Нод enum_mcArrowEmpty

**Имя нода** = enum_mcArrowEmpty;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_ArrowType;


### Нод enum_mcArrowFilled

**Имя нода** = enum_mcArrowFilled;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_ArrowType;


### Нод enum_mcArrowBlank

**Имя нода** = enum_mcArrowBlank;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_ArrowType;


### Нод enum_mcArrowOpen

**Имя нода** = enum_mcArrowOpen;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_ArrowType;


### Нод enum_mcArrowHalfUp

**Имя нода** = enum_mcArrowHalfUp;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_ArrowType;


### Нод enum_mcArrowHalfDown

**Имя нода** = enum_mcArrowHalfDown;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_ArrowType;


### Нод enum_mcArrowOpen90

**Имя нода** = enum_mcArrowOpen90;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_ArrowType;


### Нод enum_mcArrowDotFilled

**Имя нода** = enum_mcArrowDotFilled;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_ArrowType;


### Нод enum_mcArrowDotBlank

**Имя нода** = enum_mcArrowDotBlank;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_ArrowType;


### Нод enum_mcArrowSlash

**Имя нода** = enum_mcArrowSlash;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_ArrowType;


### Нод enum_mcArrowDatumFilled

**Имя нода** = enum_mcArrowDatumFilled;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_ArrowType;


### Нод enum_mcArrowDatumBlank

**Имя нода** = enum_mcArrowDatumBlank;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.enum_ArrowType;


## Группа NVP_nanoCAD_COM.McCOM2.Control

### Нод _Control_Constructor

**Имя нода** = _Control_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Control;

**Входные данные**:

* (System.Object)dynamic;

### Нод _Control_ConstructorCast

**Имя нода** = _Control_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Control;

**Входные данные**:

* (System.Object)dynamic;

### Нод Value

**Имя нода** = Value;

**Описание** = 0x000005d9;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Control;

**Входные данные**:

* (System.Object)Control;

### Нод Set_Value

**Имя нода** = Set_Value;

**Описание** = 0x000005d9;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Control;

**Входные данные**:

* (System.Object)Control;
* (System.Object)pvValue;

### Нод ClassName

**Имя нода** = ClassName;

**Описание** = 0x000005da;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Control;

**Входные данные**:

* (System.Object)Control;

### Нод Name

**Имя нода** = Name;

**Описание** = 0x000005db;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Control;

**Входные данные**:

* (System.Object)Control;

### Нод Visible

**Имя нода** = Visible;

**Описание** = 0x000005dc;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Control;

**Входные данные**:

* (System.Object)Control;

### Нод Set_Visible

**Имя нода** = Set_Visible;

**Описание** = 0x000005dc;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Control;

**Входные данные**:

* (System.Object)Control;
* (System.Object)pvbValue;

### Нод Enabled

**Имя нода** = Enabled;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Control;

**Входные данные**:

* (System.Object)Control;

### Нод Set_Enabled

**Имя нода** = Set_Enabled;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Control;

**Входные данные**:

* (System.Object)Control;
* (System.Object)pvbValue;

### Нод Form

**Имя нода** = Form;

**Описание** = 0x000005ef;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Control;

**Входные данные**:

* (System.Object)Control;

### Нод Left

**Имя нода** = Left;

**Описание** = 0x000005df;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Control;

**Входные данные**:

* (System.Object)Control;

### Нод Set_Left

**Имя нода** = Set_Left;

**Описание** = 0x000005df;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Control;

**Входные данные**:

* (System.Object)Control;
* (System.Object)pnValue;

### Нод Top

**Имя нода** = Top;

**Описание** = 0x000005e0;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Control;

**Входные данные**:

* (System.Object)Control;

### Нод Set_Top

**Имя нода** = Set_Top;

**Описание** = 0x000005e0;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Control;

**Входные данные**:

* (System.Object)Control;
* (System.Object)pnValue;

### Нод Right

**Имя нода** = Right;

**Описание** = 0x000005e1;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Control;

**Входные данные**:

* (System.Object)Control;

### Нод Set_Right

**Имя нода** = Set_Right;

**Описание** = 0x000005e1;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Control;

**Входные данные**:

* (System.Object)Control;
* (System.Object)pnValue;

### Нод Bottom

**Имя нода** = Bottom;

**Описание** = 0x000005e2;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Control;

**Входные данные**:

* (System.Object)Control;

### Нод Set_Bottom

**Имя нода** = Set_Bottom;

**Описание** = 0x000005e2;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Control;

**Входные данные**:

* (System.Object)Control;
* (System.Object)pnValue;

### Нод Width

**Имя нода** = Width;

**Описание** = 0x000005e3;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Control;

**Входные данные**:

* (System.Object)Control;

### Нод Set_Width

**Имя нода** = Set_Width;

**Описание** = 0x000005e3;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Control;

**Входные данные**:

* (System.Object)Control;
* (System.Object)pnValue;

### Нод Height

**Имя нода** = Height;

**Описание** = 0x000005e4;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Control;

**Входные данные**:

* (System.Object)Control;

### Нод Set_Height

**Имя нода** = Set_Height;

**Описание** = 0x000005e4;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.Control;

**Входные данные**:

* (System.Object)Control;
* (System.Object)pnValue;

## Группа NVP_nanoCAD_COM.McCOM2.BOMRecord

### Нод _BOMRecord_Constructor

**Имя нода** = _BOMRecord_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.BOMRecord;

**Входные данные**:

* (System.Object)dynamic;

### Нод _BOMRecord_ConstructorCast

**Имя нода** = _BOMRecord_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.BOMRecord;

**Входные данные**:

* (System.Object)dynamic;

### Нод Properties

**Имя нода** = Properties;

**Описание** = 0x000005c5;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.BOMRecord;

**Входные данные**:

* (System.Object)BOMRecord;

### Нод Remove

**Имя нода** = Remove;

**Описание** = 0x000005c6;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.BOMRecord;

**Входные данные**:

* (System.Object)BOMRecord;

### Нод Order

**Имя нода** = Order;

**Описание** = 0x000005d6;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.BOMRecord;

**Входные данные**:

* (System.Object)BOMRecord;

### Нод Set_Order

**Имя нода** = Set_Order;

**Описание** = 0x000005d6;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.BOMRecord;

**Входные данные**:

* (System.Object)BOMRecord;
* (System.Object)pnValue;

## Группа NVP_nanoCAD_COM.McCOM2.BOMLeaderPosition

### Нод _BOMLeaderPosition_Constructor

**Имя нода** = _BOMLeaderPosition_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.BOMLeaderPosition;

**Входные данные**:

* (System.Object)dynamic;

### Нод _BOMLeaderPosition_ConstructorCast

**Имя нода** = _BOMLeaderPosition_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.BOMLeaderPosition;

**Входные данные**:

* (System.Object)dynamic;

### Нод Owner

**Имя нода** = Owner;

**Описание** = 0x000005d2;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.BOMLeaderPosition;

**Входные данные**:

* (System.Object)BOMLeaderPosition;

### Нод Record

**Имя нода** = Record;

**Описание** = 0x000005d3;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.BOMLeaderPosition;

**Входные данные**:

* (System.Object)BOMLeaderPosition;

### Нод Set_Record

**Имя нода** = Set_Record;

**Описание** = 0x000005d3;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.BOMLeaderPosition;

**Входные данные**:

* (System.Object)BOMLeaderPosition;
* (System.Object)ppValue;

### Нод Object

**Имя нода** = Object;

**Описание** = 0x000005d4;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.BOMLeaderPosition;

**Входные данные**:

* (System.Object)BOMLeaderPosition;

### Нод Set_Object

**Имя нода** = Set_Object;

**Описание** = 0x000005d4;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.BOMLeaderPosition;

**Входные данные**:

* (System.Object)BOMLeaderPosition;
* (System.Object)ppValue;

### Нод BOM

**Имя нода** = BOM;

**Описание** = 0x000005d5;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.BOMLeaderPosition;

**Входные данные**:

* (System.Object)BOMLeaderPosition;

## Группа NVP_nanoCAD_COM.McCOM2.BOM

### Нод _BOM_Constructor

**Имя нода** = _BOM_Constructor;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.BOM;

**Входные данные**:

* (System.Object)dynamic;

### Нод _BOM_ConstructorCast

**Имя нода** = _BOM_ConstructorCast;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.BOM;

**Входные данные**:

* (System.Object)dynamic;

### Нод Item

**Имя нода** = Item;

**Описание** = 0x000005ba;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.BOM;

**Входные данные**:

* (System.Object)BOM;
* (System.Object)nIndex;

### Нод Count

**Имя нода** = Count;

**Описание** = 0x000005bb;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.BOM;

**Входные данные**:

* (System.Object)BOM;

### Нод CreateRecord

**Имя нода** = CreateRecord;

**Описание** = 0x000005bc;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.BOM;

**Входные данные**:

* (System.Object)BOM;

### Нод RemoveRecord

**Имя нода** = RemoveRecord;

**Описание** = 0x000005bd;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.BOM;

**Входные данные**:

* (System.Object)BOM;
* (System.Object)nIndex;

### Нод Style

**Имя нода** = Style;

**Описание** = 0x000005be;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.BOM;

**Входные данные**:

* (System.Object)BOM;

### Нод Set_Style

**Имя нода** = Set_Style;

**Описание** = 0x000005be;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.BOM;

**Входные данные**:

* (System.Object)BOM;
* (System.String)pbstrValue;

### Нод Sections

**Имя нода** = Sections;

**Описание** = 0x000005bf;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.BOM;

**Входные данные**:

* (System.Object)BOM;

### Нод Edit

**Имя нода** = Edit;

**Описание** = 0x000005c0;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.BOM;

**Входные данные**:

* (System.Object)BOM;

### Нод Update

**Имя нода** = Update;

**Описание** = 0x000005c1;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.BOM;

**Входные данные**:

* (System.Object)BOM;
* (System.Object)fUpdateRelatedObjects;

### Нод Sort

**Имя нода** = Sort;

**Описание** = 0x000005c7;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.BOM;

**Входные данные**:

* (System.Object)BOM;
* (System.String)bstrPartition;
* (System.Object)iBomPerformance;

### Нод Renumber

**Имя нода** = Renumber;

**Описание** = 0x000005d7;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.BOM;

**Входные данные**:

* (System.Object)BOM;

### Нод Export

**Имя нода** = Export;

**Описание** = 0x000005c3;

**Автор** = GeorgGrebenyuk;

**Папка** = NVP_nanoCAD_COM.McCOM2.BOM;

**Входные данные**:

* (System.Object)BOM;
* (System.Object)McCOM2.BOMExportFormat;
* (System.Object)vParams;

